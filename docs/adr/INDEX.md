# Monorepo-level ADR Index

Architecture Decision Records that span the entire monorepo (shared library
boundary, cross-project workflow, infrastructure conventions). Project-internal
ADRs live under `projects/<name>/docs/adr/`.

> **이 범위 한정은 의도적이다 — 두 색인을 합치지 마라** (TASK-MONO-525).
> 프로젝트는 각자 `projects/<name>/docs/adr/README.md` 를 색인으로 쓰고, ADR 을 가진
> 6개 프로젝트가 이제 전부 그 형태다. 여기로 합치면 **두 색인을 검사 가능하게 만드는
> 성질이 깨진다**: 프로젝트 ADR 의 id 는 전역 유일이 아니다 — `ADR-001` 이 다섯 개
> 있다(ecommerce · erp · fan · finance · scm). 하나의 id-keyed 표는 파일 어디에도 적혀
> 있지 않은 접두사를 지어내지 않고서는 이들을 담을 수 없다.
> 두 모집단은 **같은 수준으로 따로** 지켜진다 — 이 파일은
> `scripts/check-adr-index-drift.sh`, 프로젝트 README 는
> `scripts/check-project-adr-index-drift.sh`(CI 잡 `project-adr-index`).
> 공유되는 것은 아래 **Authoring Convention** 이며, 프로젝트 ADR 의 `**Status:**` 표기도
> MONO-525 에서 그 규약으로 통일됐다.

| ADR | Title | Status | Date |
|---|---|---|---|
| [ADR-MONO-001](ADR-MONO-001-port-prefix-scaling.md) | PORT_PREFIX 슬롯 부족과 7개+ 프로젝트 동시 운영 정책 | ACCEPTED | 2026-05-02 |
| [ADR-MONO-002](ADR-MONO-002-phase-4-template-extraction-trigger.md) | Phase 4 (Template 레포 추출 진입 결정 + scm catalyst) | ACCEPTED | 2026-05-04 |
| [ADR-MONO-003](ADR-MONO-003-phase-5-template-extraction-deferred.md) | Phase 5 (Template 레포 추출 발사) 결정 — DEFERRED → SUPERSEDED (D4 by 003a, D1 by 003b on launch) | SUPERSEDED | 2026-05-08 |
| [ADR-MONO-003a](ADR-MONO-003a-d4-override-scope-canonicalization.md) | D4 OVERRIDE Scope Canonicalization (meta-policy: IN/OUT scope + meta-rule) | ACCEPTED | 2026-05-12 |
| [ADR-MONO-003b](ADR-MONO-003b-phase-5-launch-criteria.md) | Phase 5 Launch Criteria — Template Repo Extraction (Phase 5 LAUNCHED 2026-05-13, `kanggle/project-template`) | ACCEPTED | 2026-05-13 |
| [ADR-MONO-004](ADR-MONO-004-shared-messaging-scaffolding.md) | Shared Messaging Scaffolding in `libs/java-messaging` | ACCEPTED | 2026-05-10 |
| [ADR-MONO-005](ADR-MONO-005-saga-timeout-escalation-dead-letter-policy.md) | Saga Timeout / Escalation / Dead-Letter Policy (4-category taxonomy) | ACCEPTED | 2026-05-11 |
| [ADR-MONO-006](ADR-MONO-006-lint-remediation-as-agent-context.md) | Lint Remediation Message as Agent Context (OpenAI Harness gap A, 4-block standard) | ACCEPTED | 2026-05-12 |
| [ADR-MONO-007](ADR-MONO-007-worktree-ephemeral-observability-stack.md) | Worktree-isolated Ephemeral Observability Stack (OpenAI Harness gap #3, Vector + VictoriaLogs/Metrics) | ACCEPTED | 2026-05-12 |
| [ADR-MONO-007a](ADR-MONO-007a-trace-layer.md) | Trace Layer (VictoriaTraces + OTLP-via-Vector) | ACCEPTED | 2026-05-28 |
| [ADR-MONO-008](ADR-MONO-008-finance-platform-bootstrap.md) | finance-platform Bootstrap Criteria (next domain after Phase 5 launch) | ACCEPTED | 2026-05-13 |
| [ADR-MONO-009](ADR-MONO-009-chrome-devtools-mcp-visual-regression.md) | Chrome DevTools MCP Visual Regression Loop (OpenAI Harness gap #4, triple-snapshot LOOP UNTIL CLEAN) | PROPOSED | 2026-05-13 |
| [ADR-MONO-010](ADR-MONO-010-e2e-tag-taxonomy.md) | E2E Test Tag Taxonomy (`smoke` / `full`) and Gradle / CI Job Split — Phase 2 of e2e 3단계 전략 | ACCEPTED | 2026-05-13 |
| [ADR-MONO-011](ADR-MONO-011-nightly-full-e2e-cadence.md) | Nightly + Push-to-main Cadence for `@Tag("full")` E2E Suites — Phase 3 of e2e 3단계 전략 | ACCEPTED | 2026-05-13 |
| [ADR-MONO-012](ADR-MONO-012-cross-project-architecture-md-canonical-form.md) | Cross-Project `architecture.md` Canonical Form — Identity-table + Service Type Composition (refactor-spec Tier 3 reconsider) | ACCEPTED | 2026-05-15 |
| [ADR-MONO-012a](ADR-MONO-012a-cross-project-architecture-md-canonical-form-corrections.md) | ADR-MONO-012 forward correction — ecommerce 14 (not 13), hook cross-project (not WMS-only), Composition H3 always present (not dual-only). Option-C-1 → forward-ADR promotion (TASK-MONO-103). | ACCEPTED | 2026-05-15 |
| [ADR-MONO-013](ADR-MONO-013-platform-console-foundation.md) | platform-console Foundation: Single-UI Console Model, New-Project Placement, admin-web Parity-Gated Retirement, Cross-Project Integration Contract | ACCEPTED | 2026-05-16 |
| [ADR-MONO-014](ADR-MONO-014-platform-console-operator-auth-token-exchange.md) | platform-console Operator Authentication Model (GAP OIDC ↔ admin-service Operator Token Exchange) | ACCEPTED | 2026-05-16 |
| [ADR-MONO-015](ADR-MONO-015-platform-console-dashboards-model.md) | platform-console Dashboards Model (Composed Operator Overview, not Grafana Embed) | ACCEPTED | 2026-05-16 |
| [ADR-MONO-016](ADR-MONO-016-erp-platform-bootstrap.md) | erp-platform Bootstrap Criteria, Integration Mode, Classification, Procedure, Readiness | ACCEPTED | 2026-05-19 |
| [ADR-MONO-017](ADR-MONO-017-platform-console-bff-architecture.md) | platform-console-bff Architecture (Phase 7 Aggregation & Cross-Domain Dashboards) | ACCEPTED | 2026-05-20 |
| [ADR-MONO-018](ADR-MONO-018-platform-console-phase-8-federation-hardening.md) | platform-console Phase 8 Federation Hardening Architecture (Cross-Product E2E + Observability Federation + Multi-Tenant Isolation Regression) | ACCEPTED | 2026-05-25 |
| [ADR-MONO-019](ADR-MONO-019-platform-console-customer-tenant-model.md) | platform-console Real Customer-Tenant Model (AWS IAM Identity Center-style tenant ↔ domain subscription, decoupling tenant from product/domain) | ACCEPTED | 2026-05-30 |
| [ADR-MONO-020](ADR-MONO-020-operator-multitenant-assignment.md) | Operator ↔ multi-customer assignment (AWS IAM Identity Center "user → multiple account assignments" parity; active-tenant token scoping) | ACCEPTED | 2026-05-31 |
| [ADR-MONO-021](ADR-MONO-021-account-type-claim-source.md) | `account_type` OIDC claim source (CONSUMER vs OPERATOR — per-account, denormalized on the credential) | SUPERSEDED | 2026-06-02 |
| [ADR-MONO-022](ADR-MONO-022-ecommerce-wms-fulfillment-integration.md) | ecommerce ↔ wms Cross-Project Order-Fulfillment Integration | ACCEPTED | 2026-06-08 |
| [ADR-MONO-023](ADR-MONO-023-entitlement-iam-plane-separation.md) | Entitlement/Subscription Plane ↔ IAM Plane Separation (subscription lifecycle state machine; GCP billing↔IAM parity; entitlement suspension never mutates IAM bindings) | ACCEPTED | 2026-06-10 |
| [ADR-MONO-024](ADR-MONO-024-tenant-admin-delegation.md) | Tenant-Admin Delegation (a tenant-scoped operator-management authority; a customer's own admin manages its operators/assignments within its tenant; strict no-escalation confinement; AWS Organizations "delegated administrator" / GCP project-IAM-admin parity) | ACCEPTED | 2026-06-10 |
| [ADR-MONO-025](ADR-MONO-025-abac-data-scope-generalization.md) | ABAC Data-Scope Generalization (`data_scope` as a cross-domain attribute-based data-scope claim) | ACCEPTED | 2026-06-11 |
| [ADR-MONO-026](ADR-MONO-026-role-grant-access-conditions.md) | Role-Grant Access Conditions (closed-enum condition clauses — the 2단계 of axis ②) | ACCEPTED | 2026-06-11 |
| [ADR-MONO-027](ADR-MONO-027-wms-scm-replenishment-loop.md) | wms → scm Stock-Replenishment Loop (low-stock → reorder suggestion) | ACCEPTED | 2026-06-11 |
| [ADR-MONO-028](ADR-MONO-028-time-window-access-condition.md) | `TIME_WINDOW` Access Condition (2nd condition type under ADR-026's closed enum) | ACCEPTED | 2026-06-11 |
| [ADR-MONO-029](ADR-MONO-029-resource-tag-access-condition.md) | `RESOURCE_TAG` Access Condition (3rd / final condition type under ADR-026's closed enum) | ACCEPTED | 2026-06-11 |
| [ADR-MONO-030](ADR-MONO-030-ecommerce-multivendor-marketplace-saas.md) | ecommerce Multi-Vendor Marketplace SaaS (join the platform federation as the 6th customer-tenant domain + an in-tenant seller axis) | ACCEPTED | 2026-06-12 |
| [ADR-MONO-031](ADR-MONO-031-ecommerce-operator-ui-console-consolidation.md) | Consolidate the ecommerce operator UI into platform-console (sunset the standalone admin-dashboard) | ACCEPTED | 2026-06-13 |
| [ADR-MONO-032](ADR-MONO-032-unified-identity-roles-model.md) | Unified identity model (single account → roles set; remove the `account_type` CONSUMER/OPERATOR partition) | ACCEPTED | 2026-06-14 |
| [ADR-MONO-033](ADR-MONO-033-roles-issuance-resolution-model.md) | Roles-issuance resolution model (where the `roles` claim's values come from at token-issue time; ADR-032 D5 step 2 mechanics) | ACCEPTED | 2026-06-14 |
| [ADR-MONO-034](ADR-MONO-034-account-credential-unification-model.md) | Account/credential unification model (one person → one central identity → consumer account + operator extension; ADR-MONO-032 D5 step 3 / D6-A mechanics) | ACCEPTED | 2026-06-14 |
| [ADR-MONO-035](ADR-MONO-035-operator-auth-unification-model.md) | Operator authentication unification + operator domain-role issuance (ADR-MONO-032 D5 step 4 mechanics; the deferred operator login/credential consolidation + the newly-found operator JWT-domain-role gap) | ACCEPTED | 2026-06-14 |
| [ADR-MONO-036](ADR-MONO-036-born-unified-identity-provisioning.md) | Born-unified identity provisioning (ADR-MONO-032 D6-A realization for new records): mint the central identity at record birth; seed-rewrite the demo legacy; design — not build — the production cross-DB backfill | ACCEPTED | 2026-06-15 |
| [ADR-MONO-037](ADR-MONO-037-ecommerce-account-lifecycle-projection.md) | Project the IAM account lifecycle into ecommerce: re-point onboarding off the decommissioned `auth.user.signed-up` to IAM `account.created`, and wire the `account.deleted` two-phase (withdraw → GDPR-anonymize) reaction the existing IAM consumer contract already mandates | ACCEPTED | 2026-06-15 |
| [ADR-MONO-038](ADR-MONO-038-shared-idempotency-filter-abstraction.md) | Lift the REST Idempotency-Key filter skeleton, storage port, and stored-response DTO into `libs/java-web-servlet` as a configurable shared abstraction | ACCEPTED | 2026-06-15 |
| [ADR-MONO-039](ADR-MONO-039-e2e-cross-suite-shared-toolkit-decision.md) | Do NOT build a shared `e2e-toolkit` package; keep the three Playwright suites independent and govern the small cross-suite overlap by a documented login convention | ACCEPTED | 2026-06-16 |
| [ADR-MONO-040](ADR-MONO-040-oidc-subject-claim-account-id-contract-alignment.md) | Align the SAS OIDC access-token `sub` to the platform `jwt-standard-claims` contract (`sub` = account UUID), and unblock ecommerce consumer authed writes | ACCEPTED | 2026-06-17 |
| [ADR-MONO-041](ADR-MONO-041-container-image-build-standard.md) | Container image build standard: layered-jar extraction, a shared Java-service base image, and per-service build-context narrowing | ACCEPTED | 2026-06-17 |
| [ADR-MONO-042](ADR-MONO-042-ecommerce-seller-onboarding-iam-provisioning.md) | Ecommerce seller onboarding mints a real IAM seller-operator account: replace the "trusted token claim, no real account" seller with born-unified IAM provisioning (ADR-036 reuse) + seller-lifecycle deactivation, fail-soft | ACCEPTED | 2026-06-18 |
| [ADR-MONO-043](ADR-MONO-043-notification-architecture-unification.md) | Notification architecture unification: a shared notification contract + lifted consumer/delivery library + a console notification aggregator over the four per-domain notification-services | ACCEPTED | 2026-06-26 |
| [ADR-MONO-044](ADR-MONO-044-self-service-tenant-onboarding.md) | Self-Service B2B Tenant Onboarding (an authenticated visitor creates a NEW tenant and is appointed its first `TENANT_ADMIN` with zero platform-operator in the loop; AWS "create account → root" / GCP "create project → owner" parity) | ACCEPTED | 2026-07-04 |
| [ADR-MONO-045](ADR-MONO-045-cross-org-partner-delegation.md) | Cross-Org Partner Delegation (a first-class tenant↔tenant partnership that lets a partner organization operate a **bounded, attenuated, revocable-as-a-unit** slice of another tenant, with **relationship-scoped offboarding** — the first privilege origination that crosses the org boundary ADR-024 and ADR-044 both keep inside a single tenant) | ACCEPTED | 2026-07-04 |
| [ADR-MONO-046](ADR-MONO-046-operator-group-model.md) | Operator Group Model (a first-class grouping primitive for `admin_operators` that lets an admin assign roles / tenant-assignments to **many operators as a unit** — the workforce-grouping facet AWS IAM User Group, IdC Group, and Google Group all provide but the portfolio has never had) | ACCEPTED | 2026-07-08 |
| [ADR-MONO-047](ADR-MONO-047-org-node-tenant-hierarchy.md) | Org-Node Tenant Hierarchy (a grouping tree **above** `tenant` that lets one company own many isolated service-tenants, with a **deny-ceiling** entitlement guardrail inherited down the tree — the "회사 → 서비스 → 도메인" three-axis structure AWS Organizations OU→Account and GCP Folder→Project both provide but the portfolio's flat tenant registry never had) | ACCEPTED | 2026-07-10 |
| [ADR-MONO-048](ADR-MONO-048-shared-reactive-gateway-library.md) | `libs/java-gateway`: a shared **reactive** (WebFlux) gateway library, extracted from four copy-pasted edges whose divergence had already started costing security fixes | ACCEPTED | 2026-07-11 |
| [ADR-MONO-049](ADR-MONO-049-framework-neutral-security-library.md) | `libs/java-security`: a **framework-neutral** security library, because **49** hand-copied validators across **20 services** are only a few tests away from a fix that lands nowhere (§ 1.6 — the count was low three times; **ACCEPTED with scope A: D5 covers all 20**, § 1.7) | ACCEPTED | 2026-07-12 |
| [ADR-MONO-050](ADR-MONO-050-scm-procurement-wms-inbound-expected.md) | scm→wms **inbound-expected loop**: a confirmed replenishment PO pre-creates a wms inbound expectation (ASN), closing the leg ADR-027 left open — the first `scm → wms` coupling; warehouse-addressed for single **and** multi warehouse; §7 D9 reconciliation standardises cross-service identifiers on **codes** | ACCEPTED | 2026-07-19 |
| [ADR-MONO-051](ADR-MONO-051-master-data-stays-federated.md) | **Master data stays federated — no central MDM hub.** The repo already runs one (single owner per master, business **codes** at the seam, consumer-owned projections) but never declared it; 051 names it, generalises `050` §7 D9 repo-wide, accepts the supplier triplication **because nothing joins it yet**, and records the **tripwire** that reopens the decision (§D5) plus why a hub would break standalone extraction (§D6) | ACCEPTED | 2026-07-20 |
| [ADR-MONO-052](ADR-MONO-052-transport-context-map.md) | **Transport is scm's context; wms owns the dock, not the road.** Five capabilities were unowned or misplaced — the redistribution decision, the A→B leg + in-transit custody, 3PL execution, the TMS vendor connection, and `logistics-service` itself (named in **9** documents, built in none). 052 maps all five (§D2), draws the line at **custody** (goods on a vehicle are in no warehouse, and wms's per-warehouse quantity buckets cannot hold them — §D1), recasts the cross-warehouse guard as *boundary enforcement* rather than a v1 limitation (§D3), reuses the existing `outbound.shipping.confirmed` fact-event so the seam needs **no new contract** (§D5), declines an eighth `tms-platform` (§D6), leaves the TMS adapter in wms until there is a receiver (§D7), and records the **tripwire** that starts the bootstrap (§D8) | ACCEPTED | 2026-07-20 |
| [ADR-MONO-053](ADR-MONO-053-logistics-service-multimodal-fulfillment.md) | **logistics-service goes multimodal: carrier dispatch now, 3PL fulfillment designed-in.** The owner fires 052 §D8-2 by adopting **EasyPost** (real vendor, free sandbox) and elects the hybrid **carrier + 3PL multi-node fulfillment** 052 mapped but left unbuilt. 053 does **not** supersede 052 — it is 052 §D8 producing *work*. It bootstraps `logistics-service` as a service inside scm (not a `tms-platform`, §D9), puts multi-vendor dispatch behind one `ShipmentDispatchPort` over the existing `outbound.shipping.confirmed` seam (no new contract, §D2), adds a `CarrierRouter` (§D3) and a `FulfillmentRouter` **seam with only the self-branch wired** so 3PL slots in additively (§D4), designs the 3PL path for **Phase 2** on the D8-3 firing (§D5), retires the wms TMS interim once the receiver is live (§D8), and keeps 052's *"premature"* tension explicit as an owner override (§1.4). Carrier-first, 3PL-second, tracking-optional (§D7). | ACCEPTED | 2026-07-24 |
| [ADR-MONO-054](ADR-MONO-054-third-party-logistics-node-activation.md) | **Activating `THIRD_PARTY_LOGISTICS`: the 3PL node is observed and its inbound half is honoured; the outbound routing decision still has no owner.** The owner fires 052 §D8-3 (a 3PL destination honoured, not DLT'd). 054 does **not** supersede 053 — it is 053 §D5/§D7 producing *Phase-2 work*, and it **re-measures** 053's deferred 052 §D2① analogy as its own §D5 mandate required. The re-measurement **fails**: the `outbound.shipping.confirmed` seam is **self-fulfillment-only** (a 3PL order never emits it) and `demand-planning-service` is **replenishment-only** (no order/allocation surface), so "둘 다" is **not** additively reachable for the outbound path. 054 splits the two 3PL surfaces: **inbound-to-3PL** (the literal D8-3) is buildable now — activate the node factory (no Flyway), observe 3PL stock **read-only** (§D4), and **route** a 3PL-destined inbound-expected away from wms rather than widen the correct wms DLT gate (§D3); **outbound-from-3PL** (§D5/§D7) is **deferred on a named missing owner** — an order-to-node allocation decision upstream of wms (ADR-022 territory), not demand-planning. Corrects two 053 claims via the amendment-section pattern (§D6). Still a service inside scm, still no `tms-platform` (§D8). | ACCEPTED | 2026-07-24 |
| [ADR-MONO-055](ADR-MONO-055-order-to-node-allocation-ownership.md) | **The order→node allocation decision has two owners, not none.** Answers the question 054 §D5/§7 deferred ("who owns the order-to-node allocation decision?"). Re-measurement finds 054's singular "no owner yet" obscured two things: the decision is **two** decisions at two altitudes (§1.2), and both owners **pre-exist**. **Inbound replenishment-target** (which node a replenishment PO targets; what BE-048 needs) → `demand-planning-service` — its existing reorder decision (ADR-027) *widened to node type*, **not** the retired 052 §D2① outbound claim (§1.5); buildable now because BE-047's read-only 3PL observation is exactly the input, and the procurement/wms downstream is already node-type-aware — only the IVS below-reorder read-model + `ReorderSuggestion` + `ProcurementDraftPoClient` need the type surfaced/sourced (§D3), with a lightweight scm-internal inbound-expectation sink (§D4). **Outbound fulfillment-node** (self-vs-3PL for a customer order; Surface B) → **[ADR-MONO-022](ADR-MONO-022-ecommerce-wms-fulfillment-integration.md) §D3's deferred "multi-warehouse routing = v2,"** generalised to node type in the ecommerce ACL (§D5) — owner **named**, build **deferred** to an owner-elected ADR-022 amendment (the routing *policy* is an unmade cross-project design this ADR declines to fake). Principle: **allocation belongs to the flow's originator** (§1.3). Refines 054 §D5 via the amendment pattern (§D6); no new service, no gate widening, no `tms-platform`. | ACCEPTED | 2026-07-24 |
| [ADR-MONO-056](ADR-MONO-056-payment-gateway-abstraction.md) | **payment-gateway abstraction: standardise on PortOne (aggregator) + extract a project-agnostic `libs/payment` backend port; defer a standalone payment service.** Two live PG integrations disagree on the settlement **model** — ecommerce/Toss is **server-confirm-charges**, fan/PortOne is **client-complete + server-verify** (ADR-001) — so the reuse question is a model-union question, not copy-paste dedup. **D1** standardise on PortOne (it already fronts ~20 PGs → "PG사 고르기" = channel config, 0 code); keep Toss-direct as a showcase. **D2** extract `libs/payment` (canonical port around **verify** as the union both vendors express, canonical value types **domain-free** per HARDSTOP-03, shared Category-B resilience) with PortOne/Toss as config-selectable adapters. **D3** frontend stays a documented per-project pattern (strict project boundary — no cross-project npm package yet). **D4** defer the standalone `payment-gateway-service` until ≥4 consumers / real webhooks / PCI isolation / centralised settlement. ACCEPT (2026-07-24, owner exact-form) resolved §6 to agent defaults: canonical=**verify** (single-op; Toss confirm=capture inside adapter), keep Toss-direct showcase, **module split** core+portone+toss. Binds Phase 1 (`TASK-MONO-478` lib+adapter move; 479/480 migrations + 481 skill in backlog until 478 lands). | ACCEPTED | 2026-07-24 |
| [ADR-MONO-057](ADR-MONO-057-recurring-billing-key-capability.md) | **Extend `libs/payment` with a recurring-billing capability: server-initiated billing-key charges, reusing the existing `verify` op for reconciliation.** fan-platform's subscribe/renew today is repeated one-time payment (fan re-opens the PG window monthly), not real 정기결제. True recurring billing needs a **new trust class** — a server-initiated charge with no client step, whose lost/ambiguous synchronous response is the same money-safety shape already solved once in this repo (`TASK-BE-438` stranded-refund reconciler). **D1** one new optional capability `RecurringBillingGateway.chargeBillingKey(billingKey, paymentId, amount, currency, orderName)` in `libs/payment-core`, domain-free; **no new read/verify op** — a lost response reconciles via the **existing** `PaymentGatewayPort.verify(paymentId, …)` (the key design insight, §1.3). **D2** `libs/payment-portone` implements it; PortOne's webhook **signature-verification utility** also lives there (vendor-specific, reusable) — the HTTP endpoint + business handling stays with the consumer. **D3** webhook is a durability backstop/trigger only, never the truth — reconcile via `verify` before acting on it (ADR-001's "never trust the signal alone" doctrine extended to server-to-server). Companion project ADR: [fan-platform ADR-002](../../projects/fan-platform/docs/adr/ADR-002-billing-key-auto-renewal.md) (consumption design, separate ACCEPT — also given). ACCEPT (2026-07-25, owner exact-form) resolved §6 to agent defaults: no server-side billing-key issuance verification in Phase 1 (bounded by charge-time verify), no new webhook-dedupe mechanism (existing `RenewMembershipUseCase` idempotency-key absorbs at-least-once delivery), method name `chargeBillingKey`. Binds Phase 1 (`TASK-MONO-482` lib-only; fan-platform `TASK-FAN-BE-033`/`FE-013` in backlog until it lands). | ACCEPTED | 2026-07-25 |
| [ADR-MONO-058](ADR-MONO-058-fleet-wide-shared-technical-scaffolding-consolidation.md) | **Consolidate fleet-wide technical scaffolding independently duplicated across all 8 projects**, per an 8-project parallel commonality/naming audit (2026-07-29). Every project reinvented the same wheels, usually without importing the `libs/` module that already ships the closest equivalent — this is the next layer of the exact problem `ADR-MONO-048`/`049` already fixed once for the validator layer beneath it. **D1** actor/JWT-claim extraction cluster (`ActorContextResolver`/`ActorContextJwtAuthenticationConverter`/`@CurrentActor`, ~15+ services/5 projects) → `libs/java-security-servlet`, mechanism only — per-service role-set policy stays local (Ownership Rule). **D2** error envelope/generic exception-handler tail → adopt existing `libs/java-web`/`libs/java-web-servlet` (`ErrorResponse`/`CommonGlobalExceptionHandler`), not a new type — gated on resolving a `details`-field wire-shape conflict and a 400-vs-422 status-code conflict per adopting service. **D3** pagination → adopt existing `libs/java-common.PageResult`/`PageQuery`, no new code. **D4** `ServiceLevelOAuth2Config`+`SecurityConfig` chain-assembly (~17 copies) → `libs/java-security-servlet`, opt-in builder not an auto-configuration. **D5** `PublicPaths` → shared value type, per-service data. **D6** `IamClientCredentialsTokenProvider` (7 copies/3 projects, already carrying a live UTF-8-encoding + missing-timeout defect unevenly distributed across copies) → `libs/java-security`. **D7** `EventDedupePort`/`ResilienceClientFactory` — pure adoption-gap closure, no new code; several `ResilienceClientFactory` non-adopters have **zero read timeout** on outbound HTTP (live production risk). **D8** ecommerce's 10-copy `TenantContext`/`TenantContextFilter` → adopt the already-fleet-shared `TenantClaimEnforcer` (the one project that never got the `ADR-MONO-049` memo). § 6 gives a suggested (non-binding) implementation sequence, smallest/lowest-risk first, ending with D1/D4 (auth-path changes) done last and one-task-per-project. ACCEPT (2026-07-30, owner exact-form via `AskUserQuestion` option naming `"ADR-MONO-058 ACCEPTED"`) binds § 2 (D1–D8) exactly as proposed — target modules and promote-mechanism-not-policy boundary; § 6's per-project/per-service adoption sequence remains future, separately-filed task work (no mega-PR authorized). | ACCEPTED | 2026-07-29 |
| [ADR-MONO-059](ADR-MONO-059-fan-authoring-identity-plane.md) | **fan-platform 의 저작 신원 평면 — `ARTIST_POST` 를 쓸 수 있는 주체를 무엇으로 표현하는가.** `TASK-MONO-512`(팬 운영자 역할 발급 불가)와 `TASK-FAN-BE-045`(아티스트 저자 부재)는 별개로 파일됐으나 AC-0 재측정에서 **같은 코드 한 줄**로 수렴했다 — `PublishPostUseCase` 의 `ARTIST_POST` 게이트는 `hasRole("ARTIST")` **또는** `isOperator()` 를 요구하는데 **둘 다 발급 경로가 0** 이다(`ARTIST` = iam 전체 0건, 대조군 `FAN_OPERATOR` 3건으로 계측 검증 / `FAN_OPERATOR` = `tenant_domain_subscription` 18행 12테넌트 중 `fan` **0건**). ⇒ 어떤 실제 호출자도 `ARTIST_POST` 를 만들 수 없고, 시드가 직접-DB 로 넣는 이유가 그것이다. **따로 결정하면 한쪽이 다른 쪽을 무효화**하므로 한 번의 선택으로 묶는다. 선택지 **A** 아티스트에게 실제 계정(`artists.account_id`) / **B** 저자 id 파라미터 + 운영자 대리 저작(B2C 테넌트를 운영자가 assume — 이 저장소에 전례 없음) / **C** 팔로우·피드를 계정 id 로 재정의 / **D** 팬은 운영자 평면을 갖지 않는다고 명시하고 죽은 수용부 4곳 제거. 추천 = **A**(위조 표면을 만들지 않는 유일한 안이며, B 를 골라도 조인 보증을 새로 만들어야 해 "스키마 변경 0" 이점이 절반은 사라진다) — 다만 **D 도 정당**하다(포트폴리오 데모에서 "v1 범위 밖" 명문화). 🔴 어느 안이든 `FollowArtistUseCase` 가 `artistAccountId` 를 **무검증 저장**하는 문제는 별도로 남는다 — 피드 조인의 두 끝이 같은 id 공간이라는 보장이 현재 0이다. **ACCEPT (2026-08-07, 소유자 정확형 = `A`)** 는 A 를 binding 으로 고정하고 **B(운영자 대리 저작)를 배제**한다 ⇒ B2C 테넌트를 운영자가 assume 하는 새 조합은 열지 않는다. D 를 고르지 않았으므로 `ARTIST_POST` 는 v1 제품 기능으로 **남고**, 죽은 수용부 4곳 제거는 수행하지 않는다. 후속 배분: `FAN-BE-045` = 스키마 + 온보딩 + 조인 검증 / `MONO-512` = 좁혀서 재작성(역할 발급만). 🔴 게이트가 실제로 물었다 — 직전 시도는 선택지 자리가 **플레이스홀더인 채**여서 넘기지 않았다(같은 줄의 "추천 A" 를 소유자 선택으로 읽는 것은 규정이 명시적으로 금지). | ACCEPTED | 2026-08-07 |
| [ADR-MONO-060](ADR-MONO-060-assumed-token-subject-identity.md) | **assume-tenant 토큰의 `sub` — 운영자 행위를 누구의 것으로 기록하는가.** `TASK-MONO-515` 의 AC-0 이 이것을 erp 결재함 기능 갭이 아니라 **플랫폼 전역 귀속 문제**로 재분류했다: assume 토큰 `sub = "platform-console-web"`(실측), `ActorClaims.accountId = jwt.getSubject()`, 그리고 **게이트웨이 6/6 이 `X-User-Id ← sub`** ⇒ 어느 운영자가 로그인하든 모든 도메인에서 행위자가 콘솔 클라이언트다. 🔴 **설계 선호가 아니라 계약 위반**이다 — `jwt-standard-claims.md` § `sub` 는 "UUID / **Yes** / Account ID, 모든 플랫폼에서 불변" 이고, 같은 표가 `email` 에 대해서는 *"nor on the assume-tenant exchange"* 라고 **명시적으로 카브아웃**한다(예외를 두는 법을 아는데 `sub` 엔 두지 않았다 = 비대칭은 신호). 🔴 **발굴 티켓의 전제 정정**: A 안의 대가로 적힌 "테스트가 반대 단언을 한다" 는 **사실이 아니다** — 유일한 `sub` 단언은 **base** 토큰 대상이고 assume 토큰 진술은 주석뿐이라 A 의 비용은 티켓보다 낮다. 모집단은 **둘로 갈린다**: ①사람 키 **필터** = erp **3**(결재+**알림** 인박스+자기결재) · fan 6(base 토큰이라 사정권 밖) · finance/scm/wms **0** — 원시 grep 의 wms 733 은 전부 `WebhookInbox` 로 대리지표였다; ②사람 **귀속(기록)** = 게이트웨이 6/6 + 감사 경로 ⇒ **필터가 0인 도메인도 감사가 오염**된다(finance F6 포함, 코드경로 확인·행 미실측). ①은 화면이 비어 드러나고 ②는 무증상이라 erp 에서만 보였다. 선택지 **A** `sub`=계정 UUID(계약 준수 복원, 읽는 쪽 변경 0) / **B** `act`·`on_behalf_of` 별도 클레임(RFC 형태는 맞으나 **도메인마다 소비자 필요** ⇒ 부분 적용되기 쉽고, 실질은 "현재 동작을 계약으로 승격") / **C** 개인 워크로드 신원(확장성 없음, 배제 권장). 추천 = **A**(+`act` 로 acting client 보존). 🔵 RFC 8693 에서 `sub`=위임 주체·`act`=행위자인데 현 구현은 **둘을 뒤집어** 놓았다. **ACCEPT (2026-08-07, 소유자 정확형 = `A`)** 는 A 를 binding 으로 고정하고 **B·C 를 배제**한다 ⇒ "erp 만 고치고 나머지 5도메인 감사는 `platform-console-web` 으로 남는" 부분 적용 상태를 만들지 않는다. ⚠️ **`act` 클레임은 이 ACCEPT 가 결정하지 않았다** — 소유자에게 `A` 와 `A + act` 가 나란히 제시됐고 plain `A` 가 선택됐다. 싣기로도, 안 싣기로도 확정되지 않았으므로 `TASK-MONO-515` 가 **명시적으로 답하고 기록**해야 한다(조용히 빠뜨리는 것은 답이 아니다). 후속: `jwt-standard-claims.md` § `sub` 확인 · 토큰 커스터마이저 · `AssumeTenantExchangeIntegrationTest` 갱신 · 6게이트웨이 회귀 · erp 인박스 **2곳**(결재+알림, 하나만 고치면 형제가 낙오). | ACCEPTED | 2026-08-07 |
| [ADR-MONO-061](ADR-MONO-061-workload-token-authorization-plane.md) | **워크로드(machine) 토큰의 인가 평면 — scope 만 싣는 토큰이 role 로 지키는 표면에 도달할 수 있는가.** `TASK-MONO-514` 실측: `master-service` 의 쓰기 술어 24개가 `MASTER_WRITE`/`MASTER_ADMIN` 을 요구하는데 **발급 경로가 저장소에 0건**이고, 워크로드 클라이언트는 `wms.master.write` **scope** 를 받지만 인가는 **role** 로 하므로 403 이다 ⇒ *이름이 맞는 scope 의 존재는 그것이 무언가를 연다는 증거가 아니다*. 🔴 발급처 후보 `wms/admin-service/PermissionCatalog` 는 `MASTER_WRITE` 를 알지만 **JWT 로 흐르지 않는 미끼**였다. 같은 뿌리에 **두 번째 표면**이 있다 — `INVENTORY_RESERVE` 는 계약이 *"machine-to-machine, human users do not hold it"* 라고 선언한 권한인데 기계 토큰에 권한을 실을 자리가 없다. 🔴 규정은 이미 두 축(subject allow-list **OR** required scope)을 정했고 저장소는 `/internal/**` 에서 그것을 지키고 있다(scope 판정기 2 · subject allow-list 1) — 답이 없는 곳은 **도메인 API 표면**이다. 모집단: cc 클라이언트 **12**(문 단위 파싱; 대조군으로 비-cc 4개가 정확히 브라우저 클라이언트) · `roles`→authority 지점 **19 서비스 / 6 프로젝트** · `MASTER_WRITE` 술어 **24**. 선택지 **A** 운영자에 `MASTER_WRITE`(코드 최소지만 `TASK-BE-433` 의 *user-chosen* 결정 뒤집기 + 테스트가 지킴; wms 는 `tenant_id` 컬럼이 5개 DB 통틀어 1개뿐이라 격리로 안 좁혀짐) / **B** 마스터 전용 역할 신설(wms 에 갇히나 42술어 범위 결정 필요) / **C** 워크로드 토큰에 `roles`(변경 1곳이나 도달 **19 서비스**; `INVENTORY_RESERVE` 를 푸는 유일한 안; `jwt-standard-claims.md` 의 *"machine tokens authorize on the scope axis"* **개정** 필요; `ADR-MONO-059` binding 과 맞물려 `TASK-MONO-522` 를 함께 답해야 함) / **D** v1 범위 밖 확정(티켓 Goal 이 명시 허용, 데모 무영향). 추천 = **C**(두 표면이 한 뿌리이고 A·B 는 계약이 금지하는 *사람 평면 확대*로 하나만 덮는다) — 🔴 다만 폭발반경·규정 개정·059 재심사 셋을 감수할 값어치인지가 실질 질문이고 **D 도 정당**하다. **ACCEPT (2026-08-13, 소유자 정확형 = `C`)** 는 C 를 binding 으로 고정하고 **A·B·D 를 배제**한다 ⇒ `OperatorRoleDerivation` 의 wms 집합에 `MASTER_WRITE` 는 계속 들어가지 않고 `wms_excludesAdminTier` 테스트도 유지된다. `jwt-standard-claims.md` 의 machine 인가 축 진술 **개정이 구현보다 먼저**이며, cc 클라이언트별 role 집합의 **기본값은 빈 집합**(fail-closed)이다. ⚠️ **fan artist-service 의 admin 매처 개방은 이 ACCEPT 가 결정하지 않았다** — rider 대조 결과 *"그 질문에 답하지 않고도 C 를 고를 수 있다"* 이므로 **미결**이고 `TASK-MONO-522` 가 명시적으로 답해 기록해야 한다(빈 집합 기본값이 그때까지 자동으로 닫아 둔다). 🔴 게이트가 실제로 물었다 — 직전 라운드의 *"③(추천)"* 은 글자의 출처가 에이전트 추천 라벨이라 넘기지 않았고, 정확형 도착 후 전환했다. | ACCEPTED | 2026-08-13 |
| [ADR-MONO-062](ADR-MONO-062-cross-project-event-delivery.md) | **프로젝트별 Kafka 격리를 유지한 채 크로스프로젝트 이벤트를 도달시키는 방법.** `TASK-MONO-511` AC-0: 크로스프로젝트 컨슈머 **16 리스너 / 3계열**(iam→ecommerce 5 · wms→ecommerce 6 · wms→scm 5)이 **한 건도** 못 받는다 — 티켓이 센 것의 **3배**. 🔵 역방향(ecommerce→iam)은 **0건**. 그중 하나가 `TASK-BE-258` **GDPR 익명화 의무**다. 실측: 가입 201 → iam `account.created` **0:0→0:1**, ecommerce-kafka **토픽 0개 유지**. 판정은 오프셋보다 강한 술어로 — 브로커 간 **6/6 DNS-FAIL**(대조군 자기-`kafka` **3/3 RESOLVES**), 네트워크 교집합 0, 공유 `traefik-net` 에 브로커 0. 🔵 컨슈머를 안 띄우니 ecommerce 쪽 토픽이 **아예 안 생겨**, 원 티켓이 본 빈 토픽이 **컨슈머의 산물**이었음이 한 실험에서 보였다. 🔴🔴 `wms→scm` 에는 `${WMS_KAFKA_BOOTSTRAP:-kafka:9092}` 이음매가 **이미 있는데** 값이 어디에도 없고 주석의 *"In dev, same cluster"* 가 **거짓**이다 ⇒ *"컨슈머별 bootstrap 파라미터화"* 는 A 의 **대안이 아니라 A 의 설정 계층**이다(주소를 적어도 경로가 없으면 도달 불가). 실제 축은 셋: 경로를 뚫거나 · 복제하거나 · 안 쓰거나. 🔴 문서 두 곳이 이 배선을 *"live"* 라고 적어 왔다 — 문서가 틀린 게 아니라 문서가 말하는 토폴로지가 compose 에 없다. `TASK-MONO-507`(격리는 의도)과 `consumer-integration-guide.md`(같은 클러스터 전제)가 충돌하고, 이 ADR 이 **어느 쪽을 굽힐지** 정한다. 선택지 **A** 공유망 노출(배선 최소·설정 계층 이미 절반; 🔴 격리를 Kafka 축에서 되돌리고 `traefik-net` 재사용 vs 전용 `events-net` 이 **rider 미결**) / **B** 릴레이·MirrorMaker 로 필요한 토픽만 복제(격리 유지·소비자 변경 0·화이트리스트가 경계의 명시 목록; 🔴 운영 요소 +1, 화이트리스트 **12토픽 3계열**) / **C** pull-through 정식화(인프라 0; 🔴 `account.deleted` GDPR 에 성립 불가 + `wms.*` 11 리스너는 폴링 주체 없음 ⇒ **일부만** 덮음). 추천 = **B**(두 정당한 진술을 모두 지키는 유일한 안) — 🔴 대가는 운영 요소와 데모 메모리 예산(`TASK-MONO-399` 축)이며 **A 도 정당**하다. 🔵 rider 대조: B 의 *"어떤 릴레이 구현체"* 는 B 를 고르는 데 답할 필요가 없으므로 rider 가 아니라 **구현 AC**. **ACCEPT (2026-08-13, 소유자 정확형 = `B`)** 는 B 를 binding 으로 고정하고 **A·C 를 배제**한다 ⇒ 브로커 간 네트워크 경로를 뚫지 않으며(`traefik-net` 에도 전용망에도 브로커를 올리지 않는다) 소비자 코드·설정은 그대로다(두 번째 브로커 주소가 들어가면 그건 B 가 아니라 A 다). 화이트리스트 **3계열 12토픽**이 경계를 넘는 것의 유일한 출처이고, `WMS_KAFKA_BOOTSTRAP` 의 거짓 주석은 제거한다. 🔵 **rider 대조 결과 없음** — 구현체(MM2 vs 브리지)·토픽 목록·도달 검사 술어는 전부 *"답하지 않고도 B 를 고를 수 있다"* 이므로 rider 가 아니라 **구현 AC** 다. 🔴 게이트가 실제로 물었다 — 직전 라운드의 *"B(추천)"* 은 넘기지 않았고 정확형 도착 후 전환했다. | ACCEPTED | 2026-08-13 |
| [ADR-MONO-063](ADR-MONO-063-artist-directory-write-plane.md) | **아티스트 디렉터리(아티스트·그룹·팬덤)의 쓰기 평면 — 누가 만드는가, 그리고 워크로드 신원이 그 문에 닿는가.** `TASK-MONO-522` AC-0 재측정(2026-08-13, 소스+라이브 양면): `tenant_domain_subscription` 의 `fan` 행 **0** · `fan-platform` 의 `operator_tenant_assignment` **0** · fan-platform 계정의 admin-tier `account_roles` **0** ⇒ 티켓 전제가 산다(되돌리는 작업이 아니다). 🔵 0 을 부재로 읽을 근거는 대조군 — `tenants=12 subs=18`(`ADR-MONO-059` 의 2026-08-07 모집단과 **정확히 일치**, 그 사이 아무도 열지 않았다) · 5개 도메인 18행 중 `fan` 만 없음 · fan-platform 의 역할 행은 `ARTIST 3·FAN 3` 으로 **읽힌다**. 🔵 곁다리: 라이브 `oauth_clients` **10 cc / 16 total** 이 `WorkloadRoleCatalog` 키 집합과 정확히 일치하고 admin-tier 는 어느 워크로드에도 없다 — `ADR-MONO-061` 의 fail-closed 기본값이 실제로 잠겨 있다. 🔴 진짜 폭발반경은 artist-service 가 아니다: `ADMIN_ROLES` 의 네 이름이 community-service `ActorContext.isOperator()` 가 받는 **바로 그 넷**이라, 그 중 하나를 사람이든 기계든 발급하면 `PublishPostUseCase` 통과 + `owns()` 로 테넌트 내 전 저자의 게이팅 우회 ⇒ `ADR-MONO-059` 가 배제한 **B 를 옆문으로 여는 것**. 선택지는 *네 이름을 건드리는가*로 갈린다 — **A** 워크로드에 `ADMIN` 부여(매처 코드 0 변경·시드가 실호출자가 됨; 🔴 `isOperator()` 가 기계에 참이 되고 *"어떤 워크로드도 admin-tier 를 안 든다"* 는 **19서비스/6프로젝트 불변식**을 잃는다) / **B** 새 역할 `ARTIST_ADMIN` 신설해 사람 평면으로(🔵 `isOperator()` 집합 불변 ⇒ 059 의 B 는 닫힌 채 유지, 발급은 `account_roles` 한 행으로 이미 가능; ❌ 역할 +1·보유 주체 미정·콘솔 화면 없음) / **C** `/internal/**` 워크로드 체인에 쓰기 추가(역할 평면 0 변경·이미 있는 문의 확장; ❌ 게이트웨이 비라우팅·공개 쓰기 표면은 죽은 채 남아 9개 매처 처리를 함께 정해야) / **D1** 매처 유지 + 사유를 이 결정으로 확정 / **D2** 매처 제거(→기본 거부; 🔴 `FAN_OPERATOR` 수용부 축소라 059 의 미채택 옵션 D 를 부분 재개 — 이 ADR 이 명시 승인해야). 🔵 실행가능성: `RegisterArtistRequest` 가 `accountId` 를 필수로 받으므로 어느 개방안이든 `FAN-BE-045` 조인 보증을 재현할 수 있다(단 엔티티 id 는 서버 UUIDv7). 추천 = **D1** — 개방안 셋 모두 **유일한 호출자가 데모 시드**이고(콘솔에 `fan` 제품 엔트리 없음), 그러면 *"시드가 우회하려 만들어진 부재를 메우려고 그 시드만 부르는 API 를 여는 것"* 이라 `TASK-MONO-512` 의 *"받는 쪽은 있는데 주는 쪽이 없다"* 를 한 층 위에서 반복한다. 🔵 **B 도 C 도 정당**(열기로 한다면 B, 역할 평면을 안 건드리고 AC-4 회수를 원하면 C). 🔴 `ADR-MONO-061` 이 명시 인계한 rider *"fan artist-service 의 `ADMIN_ROLES` 매처를 워크로드 신원에 여는가"* 는 선택지별로 A=**예** · B/C/D=**아니오** 로 표에 못박혀 있으며, 조용히 빠뜨리는 것은 답이 아니다. **ACCEPT (2026-08-13, 소유자 정확형 = `D1`)** 는 D1 을 binding 으로 고정하고 **A·B·C·D2 를 배제**한다 ⇒ 아티스트 디렉터리의 쓰기 평면은 **v1 범위 밖**이고 9개 매처는 도달 불가인 채로 **유지**된다(제거하지 않으므로 `ADR-MONO-059` 의 미채택 옵션 D 는 재개되지 않는다 — 이 ADR 은 059 의 어떤 binding 도 건드리지 않는다). 🔴🔴 **061 이 넘긴 rider 는 이로써 닫혔다 — 답은 "아니오"**: 어떤 cc 클라이언트도 admin-tier 를 받지 않으며, `WorkloadRoleCatalog` 의 빈 기본값과 `noWorkloadClientHoldsAnAdminTierRole` 단언은 유지하되 그 근거가 *"522 를 기다리는 보류"* 에서 **"이 ADR 의 결정"** 으로 바뀐다(고쳐진 결함의 면제를 그대로 두면 회귀를 가린다 — 회수 지점 3곳: 카탈로그 javadoc · 테스트 DisplayName · SecurityConfig 주석). `seed-fan.sh` 첫 `dbexec --why` 는 *"이 티켓이 답할 때까지"* 라는 잠정 표현을 버리고 **"결정에 의해 영구히 직접-DB"** 로 확정된다. 🔵 rider 대조 결과 **미결 2건을 명시**: `FAN_OPERATOR` 수용부 나머지 3곳 정리(별도 사안, 059 의 D 미채택은 불변) · `seed-fan.sh` 단독 PR 이 iam CI 레인을 안 깨우는 경로필터 갭(🔴 `auth-service/build.gradle` 주석이 *"522 가 인수한다"* 고 적었으나 **D1 은 다루지 않는다** ⇒ 그 주석을 무소유로 정정). 🔵 이 결정은 영구가 아니다 — **콘솔에 fan 관리 화면이 생기면 "호출자가 없다" 는 근거가 사라지므로 다시 열려야 한다.** | ACCEPTED | 2026-08-13 |
| [ADR-MONO-064](ADR-MONO-064-wms-outbound-tenant-visibility-plane.md) | **wms 출고 주문의 테넌트 가시성 평면 — 데모 운영자가 자기가 만든 주문에 닿을 수 있는가.** `TASK-BE-581` 3회차 실측(2026-08-13, 손대지 않은 데모 볼륨). 🔴 원인은 **한 프로퍼티를 두 축이 다른 뜻으로 읽는 것**이다: `wms.oauth2.required-tenant-id` 를 입장 게이트는 **도메인 키**(`entitled_domains` dual-accept)로, `SecurityContextCallerScopeProvider` 는 **테넌트 값**(등호)으로 읽는다 ⇒ `demo-corp` 는 입장은 통과하고 `restrictedTo` 가 되며, 생성 경로는 `tenant_id` 를 안 박으므로(REST 는 8-arg 편의 생성자 → `source=MANUAL`·`tenantId=null`) 만든 주체의 조회 조건에 자기 주문이 절대 안 걸린다. erp `ADR-001` §7 이 이 결함 클래스를 이미 명명했다 — *"이 프로퍼티는 도메인 키다. 영속 계층에서 테넌트 값으로 읽지 말 것."* 실측: `information_schema` 전수 6 DB **91 테이블**에 tenant 컬럼은 `outbound_order.tenant_id` **하나**뿐이고 그 컬럼의 non-null 값은 **0건** ⇒ wms 의 테넌트 축은 값을 가진 적이 없다. 🔴🔴 **M3 — 결함은 "안 보인다" 가 아니다**: `CallerScope` 호출처 **8개 중 7개**가 `requireOrderAccess` 로 restricted 호출자 × `tenant_id=NULL` 주문에 **403** 을 던지고(이미 통과 중인 `CallerScopeTest.restricted_requireOrderAccess_deniesNullTenant` 가 핀), 가드가 없는 유일한 지점이 **생성**(검사할 기존 주문이 없어서) ⇒ 데모 운영자는 자기 주문을 만들고 나서 **읽지도·피킹하지도·패킹하지도·출하하지도·취소하지도** 못한다. 이것이 데모가 `RESERVED` 에서 멈추는 진짜 원인이기도 하다(시드가 피킹 확정을 안 부르지만, **불렀어도 403**). 🔴 2회차의 *"`unrestricted` 테넌트는 assume 불가"* 는 결론은 맞고 **사유가 틀렸다** — 테넌트 `wms` 는 존재하고(`tenants` + 구독 `wms · wms · ACTIVE`) 단지 `operator_tenant_assignment` 에 행이 없을 뿐이며, 선례가 바로 윗줄에 있다(`TASK-BE-576` 이 2026-08-05 에 넣은 `operator 5 · ecommerce`) ⇒ **A 는 "불가능" 이 아니라 "행 하나"** 다. 선택지 **A** 데모 운영자에 wms 네이티브 스코프(제품 변경 0; 🔴 assume 은 `tenant_id` 를 **치환**하고 `entitled_domains` 는 선택 테넌트 것만이라 **올-도메인 데모가 깨지고** M3 의 403 은 안 풀린다) / **B** 생성 경로가 호출자 테넌트를 stamp + 조회의 `source` pin 완화(✅ 8지점이 한 번에 풀림·**AC-4 만족**: 넓히는 축이 자기 테넌트 *안*이라 타 테넌트는 여전히 불가시; 🔴 MONO-304 격리 규칙 변경이고 기존 NULL 행은 소급 stamp 금지라 재시드가 복구 경로) / **C** 데모에 ecommerce 슬라이스 동반 기동(제품 변경 0; 🔴 Goal 의 절반만 — MANUAL 주문은 여전히 불가시·403 잠금 잔존, 메모리 예산이 `TASK-MONO-399` AC-2 와 얽힘) / **D** 콘솔을 admin 프로젝션으로 재배선(백엔드 0·즉시 화면이 참; 🔴 **테넌트를 모르는 형제 표면을 제품으로 승격**하고 403 잠금은 잔존 ⇒ 표면적 수리) / **E** 스코핑 축이 이 프로퍼티를 테넌트 값으로 읽는 것을 중단(erp Option D 유사; 🔴 wms-entitled 테넌트가 4개라 **AC-4 와 정면 충돌**). 추천 = **B**(Goal 을 전부 닫는 유일한 안이고 AC-4 를 만족한다) — 🔴🔴 그 주된 반론이 실측으로 무너졌기 때문이다: **M5 — 격리 불변식은 이미 형제 표면이 깨고 있다.** `admin-service`/`OrderDashboardController` 는 `@PreAuthorize("hasRole('WMS_VIEWER')")` 뿐 테넌트 필터가 **없고**(`admin_db` 22 테이블 전부 tenant 컬럼 없음) 같은 운영자 토큰으로 admin 프로젝션은 `totalElements=1`·원시 API 는 `0` 을 낸다 ⇒ 현재 배치는 **격리의 비용을 전부 치르고 편익은 0** 이다(erp §3 와 글자 그대로 같은 모양). 🔴 라이더 **R1** — `SecurityContextCallerScopeProvider` 의 `*` → `unrestricted` 분기: 입장 게이트는 `*` 를 **의도적으로 거부**하는데(`ADR-MONO-048` §D5 — wms 만) 스코핑 축은 무제한으로 연다. 오늘 안 터지는 이유는 `tenant_domain_subscription` 에 `*` 행이 없어서일 뿐 — **설계가 아니라 데이터가 막고 있다.** 새 질문이 아니라 048 §D5 가 *"Recorded for a future decision"* 으로 유예한 것이며, M4 가 더하는 새 사실은 **wms 내부의 두 번째 축이 그 질문에 이미 "예" 라고 답하고 있다**는 것이다. 🔴 라이더 **R2** — M5 의 무필터 읽기 표면을 이 티켓에서 다룰지 분리할지(dashboard 컨트롤러 6개 범위라 분리 권장). **ACCEPT (2026-08-13, 소유자 지정 = `B` + R1 제거 + R2 분리)** 는 B 를 binding 으로 고정하고 **A·C·D·E 를 배제**한다 ⇒ **D1** 생성 경로가 호출자의 **서명된** 테넌트를 stamp(unrestricted 호출자는 stamp 안 함 ⇒ `FulfillmentRequestedConsumer` 의 ecommerce 테넌트 보존, 이미 실린 값은 덮지 않음) · **D2** 목록 조회의 `source` pin 만 걷고 테넌트 pin 은 유지(격리 축은 `tenant_id` 하나이고, `source` pin 은 D1 이전엔 중복이었으며 D1 이후엔 정확히 이 결정이 보이게 하려는 주문을 배제한다) · 🔴 `requireOrderAccess` 는 **byte-unchanged**(그 술어는 옳았고 틀린 것은 주문에 테넌트가 안 박히던 쪽이다) · **D3(R1)** 스코핑 축의 `*` → `unrestricted` 분기 제거 + javadoc 의 *"platform-scope operator"* 문장 회수 ⇒ 048 §D5 의 유예가 **거부 쪽으로 정산**되고 입장 게이트는 손대지 않는다(`WmsTenantGatePolicyTest` 거절 단언 유지) · **D4(R2)** admin 무필터 표면은 별도 티켓이나 **M5 가 사실이라는 것은 이 ADR 이 확정**한다. 🔴 **소급 stamp 는 하지 않는다** — `source=MANUAL` 인데 테넌트가 붙은 제품이 만들 수 없는 행이 되므로 기존 `SO-DEMO-0001` 은 계속 안 보이고 **복구 경로는 볼륨 초기화 + 재시드**다. 🔴 게이트가 실제로 물었다 — 직전 턴의 소유자 응답은 **"추천대로 진행"** 이었고 `B` 라는 글자의 출처가 이 문서의 § 추천이라 넘기지 않았다. 선택지를 명시 제시해 다시 물어 **B · R1-제거 · R2-분리** 를 받았다. | ACCEPTED | 2026-08-13 |
| [ADR-MONO-065](ADR-MONO-065-wms-admin-read-plane-tenant-axis.md) | **wms `admin-service` 읽기 평면의 테넌트 축 — 8개 대시보드는 테넌트에 무엇을 약속하는가.** `ADR-MONO-064` § D4 가 분리해 넘긴 결정이고, `TASK-BE-583` AC-1 이 주관한다. 🔴 **두 ACCEPTED 결정이 같은 행을 반대로 판정하는 상태**에서 출발한다: 원시 `GET /outbound/orders` 는 `CallerScope` 로 격리되고(`ADR-MONO-022` §D9 · MONO-304 · 064) 형제 `GET /admin/dashboard/orders` 는 `hasRole('WMS_VIEWER')` 뿐 무필터다(`ADR-MONO-019` §D5 · `ADR-MONO-020` D4) — 어느 쪽도 버그로 들어오지 않았다. 실측(2026-08-14, main=84040b98b): **M1** 콘솔이 8개 표면을 **전부** 읽는다 — 🔴 리터럴 `admin/dashboard` grep 은 2건(주석)만 잡는데 콘솔은 base URL + 상대경로 **2단계**로 조립하므로 술어가 틀렸던 것이고, 올바른 술어로 8/8 이라 *"합성을 좁히면 콘솔이 전부 막힌다"* 는 티켓 문장이 **맞다**. **M2 🔴🔴** 8개 중 상류에 테넌트가 있는 표면은 **1개**(`admin_order_summary` ← `outbound_order.tenant_id`)이고 1개가 파생 가능(`admin_shipment_summary` ← `order_id` 조인), **나머지 6개는 0** — 근거는 AC-3 의 `information_schema` 전수 6 DB 91 테이블 ⇒ 티켓의 *"B = 스키마 + 프로젝션 + 8개 필터"* 는 성립하지 않는다(6개는 필터할 값이 상류에 존재한 적 없는 **창고 운영 데이터**이고, 거기 축을 넣는 것은 `PROJECT.md` 가 배제한 3PL 모델링이다). **M3 🔴🔴** 테넌트를 가진 그 1개조차 **행을 만드는 이벤트에 테넌트가 없다** — `OutboundDomainEvent.tenantId()` 기본 null 이고 재정의는 `ShippingConfirmed`/`OrderCancelled` **둘뿐**이며 `order.received.v1` 은 거기 없다 ⇒ B 는 마이그레이션만이 아니라 **이벤트 계약 변경**을 포함한다. **M4 🔴🔴** 저장소 문서 **세 곳**(`admin-service-api.md` L33 · console `wms-client.ts` javadoc · console-integration-contract §2.4.5)이 *"admin-service 는 `tenant_id=wms` 를 enforce 하고 cross-tenant 를 거부한다"* 고 적는데 **코드는 `trustEntitledDomains()` dual-accept 이고 수용 모집단이 5개 테넌트**다 ⇒ 선택지 A("현 상태를 전역 뷰로 명문화")는 **문서화가 아니라 세 문서를 뒤집는 결정**이며 뒤집힌 문장이 *"고객사 4곳이 서로의 창고 데이터를 읽는다"* 가 된다. **M5** 합성은 사고가 아니라 설계이나(`ADR-MONO-019` §D5 · `ADR-MONO-020` D4 · MONO-162 주석까지 명시) 그 설계가 정한 것은 **"어느 도메인"** 이고 `ADR-MONO-020` 이 인용한 `rules/traits/multi-tenant.md` 는 *"isolation invariants **preserved** while the allowed-set widens"* 라 **"어느 행"** 은 도메인 몫으로 남았다 — wms admin 은 앞 절반만 구현했다(두 표면이 각각 초록인데 **합성이 결함**). **M6 🔴** `multi-tenant` 재분류는 라벨 변경이 아니다 — M1(91테이블 NOT NULL, 오늘 1개) · M3(cross-tenant read 는 404 인데 wms 는 **403**, 그것도 `ADR-MONO-064` 가 옳다며 byte-unchanged 로 유지한 술어) · M7(per-tenant quota 없음) **당일 위반**. **M7 🔵** erp `ADR-001` 은 같은 관측에서 **반대(D)** 로 갔는데 그 근거가 *"다중이 아니라 단일인데 이름이 다르다 ⇒ 격리할 두 번째 테넌트가 0개"* 라 wms 와 **전제가 반대**다(wms-entitled 5개 + ecommerce 멀티벤더 승격으로 테넌트별 주문 공존이 설계) — 🔴 다만 정직하게 오늘 `admin_order_summary` 는 1행이라 누출은 **구조적으로 확정**됐을 뿐 **실현되지 않았고**, cross-tenant 읽기를 라이브로 시연하지 않았다(근거는 217 컬럼 전수). **M8 🔴** `ADR-MONO-064` 가 남긴 계약 드리프트 — `outbound-events.md` L63 의 *"omitted for MANUAL"* · *"wms does not interpret it, filter rows by it"* 와 `ADR-MONO-030` §1.1 facet-d 의 *"opaque correlation, no row filter"* 가 D1/D2 이후 **전부 거짓**인데 BE-581 PR 은 HTTP 계약만 고쳤다. 선택지 **A** 전역 뷰 명문화(비용 0; 🔴 M4 가 전제를 깬다) / **B1** 테넌트 소유 2개 표면에만 축 + 나머지 6개는 창고 전역으로 명문화, `traits` 불변(✅ 누출의 실체를 정확히 닫고 064 의 축을 연장할 뿐; 🔴 기존 프로젝션 행엔 테넌트가 없어 복구 경로가 064 와 같은 **볼륨 초기화 + 재시드**) / **B2** `multi-tenant` 승격(✅ 저장소 모순이 한 번에 정리; 🔴 M6 의 5개 규칙 위반을 이 티켓에서 청산 불가) / **C** 문서대로 코드를 되돌려 fixed-slug 게이트로(✅ 누출 완전 차단 + 세 문서와 일치, 스키마 0; 🔴 assume-tenant 토큰이 막혀 **콘솔 wms 섹션 전멸** — 064 가 A 를 배제한 것과 같은 이유이고 5개 도메인 공통 entitlement-trust 모델을 wms 만 이탈) / **D** `WMS_VIEWER` 합성 제거 + `OperatorRoleDerivation` 부여(✅ 스키마 0; 🔴 노출 *주체*만 줄고 **한 테넌트가 다른 테넌트를 보는 구조는 잔존**). 추천 = **B1 + R1 동시 응답** — B1 은 B2 로 가는 길을 막지 않으며 B2 의 첫 단계가 곧 B1 이다. 🔴 라이더 **R1** 창고 전역 6개 표면을 **누가** 읽는가(a 고객 테넌트도 읽는다 = 현 상태 / b `WMS_OPERATOR` 이상으로 올린다 — 콘솔 영향 재측정 필요). B1 은 격리 축만 정하고 이 질문을 **안 닫으므로**, 조용히 빠뜨리면 M4 가 지적한 것과 같은 공백이 다시 생긴다. 🔴 라이더 **R2** M8 계약 드리프트 정정의 소유(결정이 아니라 **사실 정정**이라 구현 AC 가 자연스럽다). 🔴 **게이트 상태**: `TASK-BE-583` AC-1 에 대한 소유자 지정 **B** 가 이 문서를 여는 근거이나 **이 문서를 ACCEPT 하지 않는다** — 그 지정은 "축을 넣는 쪽" 이라는 티켓 방향이고 *어떤 모양의 축인가*(B1/B2)는 M2·M3·M6 이 **새로 연 분기**라 정확형을 다시 받는다. **ACCEPT (2026-08-14, 소유자 지정 = `B1` + `R1=a`)** 는 B1 을 binding 으로 고정하고 **A · B2 · C · D 를 배제**한다 ⇒ **D1** 테넌트 축은 **소유 데이터 2개 테이블에만**(`admin_order_summary` · `admin_shipment_summary`) 놓이고 필터 값의 출처는 **JWT**(클라이언트 파라미터 아님 — 064 D1 과 같은 규율)이며, shipment 는 `order_id` 로 파생 가능해도 **자기 컬럼을 갖는다**(프로젝션은 서로 독립적으로 도착한다) · **D2** 그 값은 **이벤트가 싣고 온다** — `order.received.v1` 봉투에 `tenantId` additive 추가, 컨슈머는 `orderNo` 조회나 `source` 추론으로 **채우지 않는다**, 🔴 계약이 구현보다 먼저이고 그 개정은 *"wms does not interpret it, filter rows by it"* 을 **철회**한다 · **D3(R1=a)** 창고 전역 6개는 **wms-entitled 테넌트 전체에 공개**되고 `WMS_VIEWER` 합성은 거기 유지되나 🔴 **계약에 적는 것이 산출물**이다(주석이 아니라 계약 — 티켓 § Failure Scenarios 2번이 정확히 이 실패를 적는다) · **D4** `PROJECT.md` `traits` **불변** ⇒ M6 의 당일 위반 3건이 발생하지 않고, 특히 404-over-403 이 구속하지 않아 `ADR-MONO-064` 가 옳다며 유지한 **403 이 그대로**이며 🔵 실측상 그 분기 자체가 없다(축이 놓이는 두 컨트롤러는 `@GetMapping` 하나씩뿐인 **목록 표면**이라 존재 누출 판단 지점 **0개**) · **D5(R2)** M8 드리프트는 **결정이 아니라 사실 정정**이라 `TASK-BE-583` AC-4 로 이미 승격돼 있고 **이 ACCEPT 를 기다리지 않는다**. 🔴 **소급 채움 없음** — 복구 경로는 064 와 같은 **볼륨 초기화 + 재시드**이고, 두 결정이 **같은 재시드 한 번**으로 함께 해소된다. 🔵 **B2 영구 배제가 아니다** — 재분류가 필요해지는 시점은 창고 운영 데이터에 **소유자가 생길 때**(3PL 전환)이며 B2 의 첫 단계가 곧 B1 이다. 인가되는 것은 **AC-2 착수뿐**(HARDSTOP-09 해제)이고 계약의 *내용*은 승인하지 않는다. 🔵 게이트는 **두 번 물었고 두 번 다 소유자 출처의 글자를 받았다** — 첫 번째(AC-1 방향 `B`)는 문서를 *여는* 근거로만 썼고 ACCEPT 로 쓰지 않았다. | ACCEPTED | 2026-08-14 |

Every column above is read from the ADR files themselves and enforced by
`scripts/check-adr-index-drift.sh` — every ADR has a row, every row has an ADR, and each
row's `Status` and `Date` equal that ADR's own header. **The file is the authority: if a
row and its ADR disagree, the row is wrong.**

`Date` is **the date the ADR was proposed** (see § Authoring Convention), so it does not
move when an ADR is accepted. To find out *when* a decision took effect, open the ADR —
its `History` line, or the annotations on its own `Date` line, carry the transitions.
The index deliberately does not restate them: those live in the files in half a dozen
different prose shapes, and a guard that had to parse them would be a false-positive
generator rather than a guard — and a guard that is red on day one gets switched off,
which is worse than no guard, because a skipped job reports green. `Title` is likewise
unguarded: the titles here are curated summaries, not copies of the ADRs' H1 lines.

---

## Authoring Convention

- File name: `ADR-MONO-NNN-short-kebab-title.md`
- Required header fields: `Status`, `Date`, `Decision driver`, `Supersedes`, `Related`
- **`Status`** — the ADR's current state. Lifecycle: `PROPOSED → ACCEPTED | DEFERRED | REJECTED | SUPERSEDED`.
  `PROPOSED → ACCEPTED` is a **human gate**: an agent may not accept its own ADR.
- **`Date`** — **the date this decision record was authored, i.e. the date it was proposed.**
  It is *not* the date the ADR was accepted, and it does not move when the `Status` does.
  This was never written down until TASK-MONO-369, and the omission cost: 12 ADRs shipped
  with no `Date` at all, and the three where the two readings are distinguishable
  (`008`, `018`, `019`) recorded the proposed date while the index recorded the accepted one.
  Everywhere else PROPOSED and ACCEPTED landed on the same day, so nobody could see the split.
- **`History`** (optional but recommended) — the `PROPOSED → ACCEPTED` transitions, with dates.
  Older ADRs instead annotate their transitions inline on the `Date` line; both are accepted,
  and neither is machine-parsed. **Put the transition dates somewhere** — for `040` and `041`
  they are in neither place, so when those decisions took effect is now unrecoverable from the file.
- Sections: `Context`, `Decision` (numbered `D1, D2, …`), `Alternatives Considered`, `Consequences`, `Verification`, `Outstanding follow-ups`
- Reference: `platform/architecture-decision-rule.md`

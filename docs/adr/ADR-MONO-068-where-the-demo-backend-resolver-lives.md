# ADR-MONO-068 — 데모 백엔드 주소 해석기를 어디에 둘 것인가

**Status:** ACCEPTED
**Date:** 2026-08-26
**주관 티켓:** [`TASK-MONO-577`](../../tasks/done/TASK-MONO-577-adr-where-the-runtime-backend-resolver-lives-proposed.md)
**선행:** [`ADR-MONO-067`](ADR-MONO-067-demo-surfaces-served-from-vercel.md) D2 (*"백엔드 주소는 런타임 조회 결과다"*)
**출처:** 소유자 정확형 지정 (2026-08-26)

## History

- 2026-08-26 — **ACCEPTED (소유자 정확형 지정).** 지정 문구 그대로:

  > `577 = A + 승격 트리거(2개째에서 RED), D 는 067 로 상신`

  🔴 **이 ADR 은 PROPOSED 단계를 거치지 않았다** — 소유자가 초안보다 **먼저** 지정했다.
  `TASK-MONO-577` AC-4 는 *"PROPOSED 로 남기고 `## Decision` 은 소유자 지정 대기"* 를
  요구했는데, 그 요구의 목적(**self-ACCEPT 0**)은 여기서도 충족된다: 아래 § Decision 은
  **내 추천이 아니라 소유자가 적은 문자열**이고, 그 문자열은 A 만이 아니라 **rider 두 개
  (승격 트리거 · D 상신)까지 명시**한다. plain `A` 였다면 rider 는 미결로 남았을 것이다.
  [`platform/architecture-decision-rule.md`](../../platform/architecture-decision-rule.md) § The ACCEPTED Gate

- 2026-08-26 (2) — **승격 트리거의 답을 «발화 전에» 소유자가 지정했다: `C`.**
  § D5 참조. 아래 § Decision 의 `A` 는 **취소되지 않았다** — 여전히 오늘의 상태이고,
  D5 는 *"트리거가 물었을 때 무엇을 하는가"* 를 미리 못 박은 것이다.
  🔴 같은 날 **§ D4 의 숫자가 틀렸음이 실측됐다**(§ D4 의 정정 블록).

---

- 2026-09-01 (3) — 🔴🔴 **§ D6 실측 표의 ㉰ 칸이 거짓이었다 (§ D6.2 정정). 결정은 유지된다.**
  *"TS 공유 모듈 선례 0건"* 은 `libs/` 라는 **이름만** 보고 `packages/` 를 안 본 결과다 —
  `projects/ecommerce-microservices-platform/packages/` 에 **`@repo/*` 여섯 개**가 있고
  `web-store` 가 **`workspace:*`** 로 쓴다. 🔵 **`B2` 가 필요로 하는 «프로젝트를 건너는»
  공유는 여전히 0건**이라 결정은 안 바뀌지만, **위험 평가는 내려가야 한다**.
  🔴 지정을 **받은 뒤에** 발견했다 — 소유자가 본 표에 거짓 칸이 하나 있었다.

- 2026-09-01 (2) — ✅ **§ D6 ACCEPTED — `B2`** (소유자 정확형 지정).
  받은 문자열: `ADR-MONO-068 D6 ACCEPTED B2`. **세 요구를 따로 확인했다** — ADR 이름
  `ADR-MONO-068` ✅ · `ACCEPTED` ✅ · **실제 지정된 문자 `B2`** ✅.
  🔵 내가 예시로 준 형식(`… ACCEPTED — <안>`)의 `—` 는 없지만 **그것은 구두점이고 세 요구 중
  하나가 아니다.** 없는 요구를 만들어 지정을 거절하는 것도 게이트를 잘못 지키는 것이다.
  🔵 **추천과 일치했지만 추천이 승인이 된 것이 아니다** — 소유자가 별도로 지정했다.
  🔴 **rider 없음.** `B2` 에는 미결 rider 가 없다(§ D6 이 예시로 든 *"Root Directory 밖 포함은
  관측으로 가른다"* 는 **결정이 아니라 측정 절차**이고, 아래 § D6.1 이 그것을 티켓의 AC 로 넘긴다).
  🔴 **§ D5 의 `C` 는 이것으로 소진됐다** — 대체이지 폐기가 아니다(§ D5.3).

- 2026-09-01 — 🔴 **세 번째 해석기가 도착할 참이다. § D6 을 `PROPOSED` 로 올린다.**
  `TASK-MONO-610`(ADR-MONO-069 `C2`)의 포워더가 `resolveDemoBackend()` 를 쓰므로 § D5.4 의
  승격 트리거가 발화한다. **이 ADR 의 Status 는 `ACCEPTED` 그대로이고 § D5 의 `C` 도
  그대로다** — § D6 은 *"트리거가 발화했을 때 무엇으로 승격하는가"* 만 다루는 **미결** 절이다.
  🔴 **§ D5.4 의 *"그때 `B` 로 승격"* 은 근거 표의 문장이지 ACCEPT 가 아니다.** § D6 이 그
  문장을 포함해 다섯 안을 **실측 비용과 함께** 늘어놓고, 지정은 소유자 몫으로 남긴다.

## Context — 실측 (2026-08-26, 착수 시점 재측정)

`TASK-MONO-577` AC-0 이 *"착수 시점에 다시 재라, 어긋나면 STOP"* 을 요구했다. 다시 쟀고, 전제는 그대로다.

### 세 앱은 서로 다른 JS 세계에 산다

| 앱 | 워크스페이스 | 공용 패키지 |
|---|---|---|
| web-store | `projects/ecommerce-microservices-platform/pnpm-workspace.yaml` (`apps/*` + `packages/*`) | `@repo/api-client`·`eslint-config`·`tsconfig`·`types`·`ui`·`utils` |
| fan | `projects/fan-platform/pnpm-workspace.yaml` (`web/*`) | **없음** |
| console | **워크스페이스 없음** — 자기 `pnpm-lock.yaml` 단독 | **없음** |

`packages/*/package.json` 은 **6건**이고 **전부 ecommerce 프로젝트 안**이다 — 즉 프로젝트를
가로지르는 JS 코드의 자리가 아니다. 그리고 루트 `package.json` 이 스스로 못 박는다:

> `"description": "Monorepo root — thin shortcut scripts that delegate runtimes to the owning project. **No workspace / no dependencies at this level.**"`

### 🔴 그런데 결정을 가른 것은 위 표가 아니다 — **소비자가 하나**라는 사실이다

`ADR-MONO-067` 의 단계 순서는 2(web-store) → 3(console) → 4(fan) 이고, **3·4 는 D4(OIDC·쿠키
축)가 안 풀려서 못 간다.** 즉 이 해석기를 실제로 필요로 하는 앱은 지금 **web-store 하나**다.

그리고 이 저장소의 규칙이 그 경우를 이미 다룬다 — [`CLAUDE.md`](../../CLAUDE.md) § Project-scoped shared modules:

> **When**: **두 개 이상**의 서비스가 같은 도메인 개념을 선언하고…

⇒ **소비자가 하나인 코드의 공유 구조를 정하는 것은 결정이 아니라 추측이다.**
그리고 A 의 실패 모드(*"한 벌만 고쳐진다"*)는 **사본이 둘 이상일 때만** 발동한다.

### 공유 대상은 작다 — 그리고 D 가 채택되면 더 작아진다

`GET {DEMO_API_BASE}/status` → `{state, ip, used_minutes, budget_minutes}` 를 부르고 TTL 캐시하는
~30줄. 🔵 조회 소스는 **이미 있다**(론처가 쓰는 그 엔드포인트) — 새 인프라 0.

🔴 그런데 세 앱의 fetch 스택이 다르다: web-store 는 axios(`@repo/api-client`), fan 은 네이티브
`gatewayFetch`, console 은 `route.ts` 159개. **공유 단위를 넓힐수록 그 차이에 걸린다.**

---

## 선택지

| # | 안 | 대표 실패 모드 |
|---|---|---|
| **A** | **앱별 구현** | 🔴 사본이 둘 이상 생기면 **한 벌만 고쳐진다** — `CLAUDE.md` 가 project-scoped libs 를 도입한 이유가 정확히 이 실패다 |
| B | 새 repo-root JS 패키지 | 루트 `package.json` 의 자기 선언을 뒤집는 **구조 변경**. **console 은 워크스페이스 자체가 없어** 새로 만들어야 하고, **세 Vercel 프로젝트의 install 단계**를 전부 건드려야 한다(fan 의 `installCommand` 가 존재하는 이유가 이미 *"Root Directory 가 lockfile 보다 깊다"* 였고, 그 영역은 **261자 하나로 배포를 0초에 죽인** 자리다 — `TASK-MONO-562`) |
| C | 복사 + **세 사본 동일성 가드** | 드리프트를 막지 못하고 **잡는다**. 🔴 그런데 사본이 하나뿐인 동안 이 가드는 **잴 것이 없다** — `TASK-MONO-579` 가 방금 같은 모양을 만났다(사본이 둘이면 가드보다 **줄이는 것**이 쌌다) |
| D | **주소를 안 바뀌게 만든다** | § D 상신 참조 — **이 ADR 이 채택할 수 없다** |

---

## Decision — `A + 승격 트리거(2개째에서 RED)`

**해석기는 앱별로 구현한다. 공유 구조를 만들지 않는다.** 단, **두 번째 구현이 생기는 순간
CI 가 RED** 가 되어 A/B/C 를 다시 결정하게 한다.

### D1 — 구현은 앱 안에 산다

각 앱이 자기 스택에 맞게 구현한다(web-store 는 axios, console 은 route handler, fan 은
`gatewayFetch`). **다른 앱에서 임포트하지 않는다.**

### D2 — 🔴 구현은 **기계 판독 마커**를 단다

```ts
// DEMO-RESOLVER: web-store   (ADR-MONO-068 — 두 번째가 생기면 CI 가 RED)
```

마커가 없으면 승격 트리거가 그 구현을 **못 센다.** 그래서 마커는 선택이 아니라 **요구**이고,
가드가 *"내용은 걸렸는데 마커가 없다"* 를 별도로 RED 로 잡는다.

### D3 — 승격 트리거의 숫자는 **2** 다

[`scripts/check-demo-resolver-copies.sh`](../../scripts/check-demo-resolver-copies.sh) 가 CI 에서
돌고, **해석기를 가진 앱이 2개 이상이면 실패**한다.

🔴 **비용이 0인 "나중에 승격" 은 오지 않는다.** 트리거가 자기 숫자를 명시하고 **물지 않으면**
A 는 결정이 아니라 미룸이다. 그래서 그 가드는 `--self-test` 로 물기를 증명한다(7칸).

🔴 **트리거가 발화하면 가드를 완화하지 마라.** 그 RED 는 결함이 아니라 **이 ADR 의 전제가
소진됐다는 신호**이고, 그때 할 일은 `TASK-MONO-577` 의 A/B/C 를 실제로 고르는 것이다.
결정하면 가드는 그 결정에 맞게 **교체**된다 — 삭제가 아니라.

### D4 — 🔵 "공유하지 않는다" 는 되돌리기가 싸다

A 를 골랐다가 B/C 로 가는 비용은 **30줄을 옮기는 것**이다. 반대 방향(B 를 만들었다가 A 로)
은 루트 워크스페이스와 세 Vercel install 설정을 되돌리는 것이다. **비대칭이 A 쪽을 가리킨다.**

> 🔴 **정정 (2026-08-26) — 위의 `30줄` 은 측정이 아니라 예측이었다.**
> 이 ADR(PR #3460)은 해석기(PR #3464)보다 **먼저** 쓰였다. 실물
> `demo-backend.ts` 는 **전체 177줄 / 코드 71줄 / 주석 87줄(49%)** 이고, 그중
> **프로젝트 고유 값이 박힌 코드줄은 4개**(마커 · `SERVICE_PREFIX` · 폴백 env 2개)다.
> 코드 기준 **2.4배**, 전체 텍스트 기준 **5.9배**. 비대칭 논지 자체는 살아 있지만
> **크기는 틀렸다** — 원문을 지우지 않는 이유는, 이 숫자가 *예측이었다는 사실*이
> 다음 결정에서 같은 실수를 막는 유일한 기록이기 때문이다.
> 🔵 그리고 **49%가 주석**이라는 것이 이 축의 진짜 내용이다: 사본을 뜨면 복제되는 것은
> 코드 71줄이 아니라 **판단 근거 87줄**이다.

---

## 🔴🔴 D 상신 — `ADR-MONO-067` 로 올린다 (이 ADR 은 채택할 수 없다)

`TASK-MONO-577` AC-1.5 가 열거하지 않은 축을 찾았다: **주소를 안 바뀌게 만들면 해석기 자체가
필요 없다.** 그런데 그것은 `ADR-MONO-067` 의 선택지 공간(D2 = *"주소는 런타임 조회 결과다"*)을
건드리므로 **이 ADR 이 단독으로 채택할 수 없다.** 관측과 대가를 적어 067 로 올린다.

### 대가 — **계정에서 실측했다** (2026-08-26)

`TASK-MONO-577` AC-1.5 는 *"금액을 티켓에 적지 말고 계정에서 확인하라"* 고 못 박았다. 확인했다:

| Cost Explorer `APN2-PublicIPv4:InUseAddress` | 값 |
|---|---|
| 2026-08 (26일까지) 사용량 · 비용 | **11.41 시간 · $0.057** |
| 단가 (두 값의 나눗셈) | **$0.005 / 시간** |
| 고정 IP 로 24/7 유지 시 (730h) | **≈ $3.65 / 월** |
| **차액** | **≈ +$3.6 / 월** — 같은 항목이 **약 64배** |

🔵 맥락: 8월 총액 ≈ **$12**, 그중 `EC2 - Other` **$4.42 는 100GB EBS** 로 데모가 꺼져 있는
98.6% 동안에도 나간다. **이미 같은 종류의 비용을 받아들이고 있다.**
🔵 계정에 **EIP 는 0개**다(`describe-addresses` 실측) — 아직 아무것도 시작되지 않았다.
🔴 Route53 은 이 IAM 사용자에게 **권한이 없어** 호스티드존 유무를 못 쟀다. **못 쟀다고 적는다.**

### D 가 채택되면 사라지거나 줄어드는 것 (577 AC-1.5 의 열거)

① Traefik 라벨 드리프트(`553`) · ② `demo-boot.sh` 의 `DEMO_DOMAIN` 파생 · ③ 론처의 동적 링크 ·
④ `CONSOLE_PUBLIC_ORIGIN`(`358`) · ⑤ **이 ADR 의 해석기 자체** · ⑥ `TASK-MONO-576` AC-1.5 의
**움직이는 `issuer`**

⚠️ **D 는 D4(OIDC·쿠키)를 풀지 않는다.** 이름이 고정돼도 여전히 **평문 HTTP** 라 로그인 축은
남는다. 다만 TLS 를 붙일 **전제**는 된다(그것은 `ADR-MONO-067` 의 선택지 A 이고 이미 기각된
방향이므로, 다시 여는 것은 **별개의 결정**이다).

🔵 **D 가 채택되면 A 는 더 좋아진다** — 주소가 고정되면 앱이 공유할 것은 *"주소"* 가 아니라
*"데모가 켜져 있는가"* 한 줄로 줄고, 그 크기는 공유할 가치가 없다.

---

## D5 — 🔴 트리거가 발화하면 **`C`** 다 (2026-08-26, 소유자 정확형 지정)

**두 번째 해석기를 쓰는 티켓**(단계 3 `TASK-MONO-585` 또는 단계 4 `TASK-MONO-586` — 🔴 **먼저 착수되는 쪽이다**, 아래 D5.0)이 그것을 쓰는 순간
[`scripts/check-demo-resolver-copies.sh`](../../scripts/check-demo-resolver-copies.sh) 가 RED 가 된다.
§ D3 은 그때 *"`TASK-MONO-577` 의 A/B/C 를 실제로 고르는 것"* 이 할 일이라고 적었다.
**소유자가 그 답을 발화 전에 지정했다: `C` — 사본을 쓰되 「정규화 동일성 가드」를 붙인다.**

### D5.0 — 🔴 **«두 번째» 는 티켓 번호가 아니라 순서다** (2026-08-26 정정)

이 절의 초판은 *"`TASK-MONO-585` 가 두 번째를 쓰는 순간"* 이라고 **티켓을 못 박았다.**
그런데 규칙이 말하는 것은 **어느 티켓인지가 아니라 «두 번째»라는 순서**다. 그리고 실제로
그 둘의 순서는 **뒤집혔다**:

| 티켓 | 상태 (2026-08-26) |
|---|---|
| `TASK-MONO-585` (단계 3, console) | 🔴 **자기 AC-0 4항이 막는다** — *"`TASK-MONO-574` 미해결이면 **여기서 멈춘다**"* |
| `TASK-MONO-586` (단계 4, fan) | ✅ **진행 가능** — 같은 자리에서 *"미해결이면 로그인은 이 티켓 밖"* 이라고만 적었다 |

⇒ 실제 두 번째 구현은 **586 이 될 수 있다.** 티켓을 못 박아 두면 그때 이 ADR 이
**자기 규칙을 자기가 못 가리키게** 된다.

🔵 **이것은 § D4 의 «30줄» 과 같은 종류의 결함이다** — 그때는 *숫자*를 예측으로 박았고,
여기서는 *티켓 번호*를 순서 대신 박았다. 둘 다 **미래를 하나로 가정**한 것이다.

---

### D5.1 — 무엇을 정규화하는가

§ D1 이 *"앱마다 자기 스택으로"* 라 했으므로 **문자 동일성 가드는 성립할 수 없다.**
가드는 **프로젝트 고유 축을 정규화한 뒤** 비교한다.

🔴 **초판의 이 표는 불완전했다 (2026-08-26 정정).** 4줄이라 적었지만, 두 번째 사본을
실제로 쓰고 세어 보니 **폴백의 기본값 URL 리터럴**이 빠져 있었다.
🔵 § D5.2 가 *"두 번째를 써 봐야 안다"* 고 적은 것이 바로 이것이다 — 이 표는 사본 1개에서
추정한 것이었고, **추정이 하나 모자랐다.**

| # | 정규화 대상 | web-store | fan-platform-web |
|---|---|---|---|
| 1 | `DEMO-RESOLVER:` 마커의 앱 이름 | `web-store` | `fan-platform-web` |
| 2 | `SERVICE_PREFIX` | `'ecommerce'` | `'fan-platform'` |
| 3·4 | 폴백 env 이름 쌍 | `API_URL_INTERNAL` / `NEXT_PUBLIC_API_URL` | `GATEWAY_URL_INTERNAL` / `NEXT_PUBLIC_GATEWAY_URL` |
| **5** | 🔴 **폴백 기본값 URL** | `'http://localhost:8080'` | `'http://fan-platform.local'` |

**실측 (2026-08-26):** 정규화 **전** 두 사본의 코드는 각각 **71줄**이고 차이는 **4줄**
(1번은 주석이라 코드 diff 에 안 나타난다). 정규화 **후**에는 **완전히 동일**하다.
⇒ **§ D5.2 의 전제가 성립한다. `C` 는 실현 가능하다.**

🔴 **비교 대상은 «마커를 가진 파일» 이 아니다.** 마커는 **호출부에도** 붙는다 — 실측:
web-store 의 마커 파일 4개 중 3개가 소비자(`route.ts` · `config/api.ts` · Notice 위젯)이고,
소비자는 **정당하게 다르다**. 가드는 구현을 **모듈이 내보내는 API**
(`export async function resolveDemoBackend`)로 판별한다(실측: 앱당 정확히 1개).

🔴🔴 **정규화가 덮지 않는 것 — 주석이다.** 주석 전용 줄은 전부 버린다. 두 사본의 주석은
정당하게 갈리기 때문이다(각자 자기 앱에서 관측한 것을 적는다). 그 대가로 **누군가 한 사본의
판단 근거를 통째로 지워도 가드는 초록**이다. 사본 하나가 **177줄 중 87줄(49%)이 주석**임을
생각하면 작은 구멍이 아니다. 이것은 «못 본 것» 이 아니라 **고른 것**이고, 여기 이름을 적어 둔다.

### D5.2 — 🔴 이 처방의 전제는 **아직 안 잼**

*"71줄 중 4줄만 다르다"* 는 **사본 1개에서 나온 숫자**다. 두 번째가 정말 그 4줄만 다를지는
**두 번째를 써 봐야 안다** — 단일 표본을 성질로 승격시키지 않는다.

🔴 **그러므로 두 번째를 쓰는 티켓이 정규화가 성립하는지를 먼저 판정하고, 안 되면 STOP 한다.**
정규화가 불가능하다는 것은 실패가 아니라 **`B` 의 근거가 실측됐다는 뜻**이고, 그때는
이 D5 를 고쳐야 한다 — 가드를 느슨하게 만들어 통과시키는 것이 아니라.

### D5.3 — 가드는 **교체**된다, 삭제가 아니라

§ D3 이 못 박은 대로다. 상한 1 의 「사본 세기」 가드가 「정규화 동일성」 가드로 바뀐다.
🔴 두 번째 사본이 landed 인데 **어느 가드도 없는 창**이 생기면 안 된다 — 같은 커밋에서 바뀐다.

### D5.4 — 🔵 `C` 를 고른 근거 (실측, 2026-08-26)

| 안 | 왜 아닌가 / 왜 맞나 |
|---|---|
| `A` (상한 1→2) | § D3 이 *"트리거가 발화하면 가드를 완화하지 마라"* 로 **미리 금지**했다 |
| `B` (루트 워크스페이스) | lockfile **5개 / 20,580줄** 병합 · `pnpm-workspace.yaml` 은 **2개뿐**(console-web·fed-e2e 는 밖) · **세 Vercel 프로젝트의 install 단계** — `TASK-MONO-562` 가 **261자 하나로 배포를 0초에 죽인** 자리다 |
| **`C`** | Vercel install 무영향. 되돌리기 쌈. 세 번째 사본이 오면 그때 `B` 로 승격하고, 그 시점엔 정규화 실측이 **3표본**이 되어 있다 |

---

---

## D6 — 🙋 **세 번째가 도착했다: 승격 안 비교 (PROPOSED, 2026-09-01)**

**Status: ✅ ACCEPTED — `B2`** (2026-09-01, 소유자 정확형 지정 · rider 없음).
~~미결. 이 절은 지정을 **받기 위한** 것이지 지정이 아니다.~~

🔵 아래 § 안 비교표·실측·추천은 **그대로 둔다** — 무엇을 보고 골랐는지가 결정의 일부다.

### 왜 지금

`ADR-MONO-069` 가 `C2` 로 ACCEPTED 됐고, 그 구현(`TASK-MONO-610`)은
`auth.hubwang.com` 뒤에 **catch-all 포워더** 하나를 둔다. 그 포워더는 그날 IP 를
`resolveDemoBackend()` 로 얻으므로 **세 번째 해석기**다.

가드가 그 숫자를 직접 말한다 (2026-09-01 실측):

```
[resolver-copies] OK — 해석기를 가진 앱 2 개 (승격 3) · 정규화 비교 1 쌍
                     · 앱 소스 1191 개 · 선언 앱 3 개 전부 커버 · 대조군 2 건   rc=0
```

🔴 **완화는 § D3 이 미리 금지했다** — *"트리거가 발화하면 가드를 완화하지 마라"*.
§ D5.4 표가 `A`(상한 1→2)를 **바로 그 이유로** 기각한 것도 같은 문장에서 나왔다.

### 🔵 오늘 다시 잰 값 — **§ D5.4 의 숫자는 아직 전부 맞다**

| 잰 것 | 2026-08-26 (D5.4) | **2026-09-01 재측정** |
|---|---|---|
| lockfile 수 / 총 줄 | 5 / 20,580 | ✅ **5 / 20,580** (변동 없음) |
| `pnpm-workspace.yaml` | 2개 (console-web · fed-e2e 는 밖) | ✅ **2개** — `ecommerce` · `fan` |
| 해석기를 가진 앱 | 2 | ✅ **2** (web-store · fan-platform-web) |
| Next 앱 (`next.config.*`) | — | **3개, 전부 `projects/` 안** |

**새로 잰 것 넷** (D5.4 에는 없던 축):

| # | 잰 것 | 값 | 어느 안에 걸리는가 |
|---|---|---|---|
| ㉮ | 루트 `pnpm-lock.yaml` | 🔵 **이미 있다** — 9줄, `importers: { .: {} }`. 루트 `package.json` 은 deps·devDeps·packageManager **전부 없음** | `B` 계열 |
| ㉯ | Next 빌드가 그 루트 lockfile 을 **본다** | 🔴 실측 로그: *"We detected multiple lockfiles and selected the directory of `<repo-root>/pnpm-lock.yaml` as the root directory"* — fan 빌드에서 **매번** 뜬다 | `B` 계열 |
| ㉰ | 저장소의 **TS 공유 모듈 선례** | 🔴🔴 **이 칸은 거짓이었다 — § D6.2 정정.** ~~0건. 루트 `libs/` 는 전부 Java(TS/JS 파일 0개), `projects/*/libs/` 도 Java~~ 실제로는 **프로젝트 안에 6개**가 있다(`@repo/*`). **프로젝트를 건너는 선례만** 0건이다 | `B`·`B2` |
| ㉱ | `hubwang.com` 의 등록·NS | 🔵 **둘 다 Vercel** ⇒ `auth.` 추가는 **대시보드 한 번**, 외부 DNS 쓰기·인증서 **불필요** | 모든 안 (C2 의 전제 확인) |
| ㉲ | Host 기반 라우팅 **선례** | 🔴 **0건** (저장소 소스에서 `headers().get('host')` 분기 0). middleware 파일은 3개지만 전부 경로 기반 | `B3` |

### 안 — 다섯

| 안 | 무엇 | 측정된 비용 | 트리거를 만족하나 |
|---|---|---|---|
| **B** | 루트 pnpm 워크스페이스 + 공유 패키지 하나 | 🔴 lockfile **5개/20,580줄** 재배치 · `pnpm-workspace.yaml` 을 루트에 두면 **㉯ 의 경고가 진짜 충돌**이 될 수 있다(현재는 경고뿐) · **세 Vercel 프로젝트의 install 단계**를 전부 건드린다 — `TASK-MONO-562` 가 **261자 하나로 배포를 0초에 죽인** 자리다 · ~~㉰ 선례 0~~ → **§ D6.2 정정** | ✅ 트리거의 취지대로 |
| **B2** | 공유 패키지를 만들되 **루트 워크스페이스 없이** — `file:` / 상대경로 의존 | lockfile 재배치 **불필요**. 🔴 그러나 세 앱의 `installCommand`(`pnpm install --no-frozen-lockfile`)가 Root Directory **밖**의 경로를 참조하게 되고, Vercel 의 *"Root Directory 바깥 소스 포함"* 설정은 **대시보드에만** 있다(`fan-platform-web/VERCEL.md` § 남은 갈림길) ⇒ **관측으로만 가를 수 있는 축이 하나 늘어난다** | ✅ |
| **B3** | 포워더를 **기존 앱 안의 라우트**로 — `auth.hubwang.com` 을 그 앱의 Vercel 프로젝트에 **도메인으로 추가**하고 Host 로 분기 | 🔵 해석기 **개수가 안 늘어난다**(앱 2 유지) · Vercel 프로젝트도 안 늘어난다(⇒ `610` AC-5 의 배선 부채 **전부 소멸**) · ㉱ 로 도메인 추가는 대시보드 한 번. 🔴 **대가**: ㉲ 선례 **0**(첫 Host 분기) · **IdP 가용성이 그 앱의 배포에 묶인다**(스토어 빌드가 깨지면 로그인이 죽는다) · 프로젝트 경계 위반(`ecommerce` 앱이 monorepo 인프라를 호스팅) | ✅ (개수로는) 🔴 **그러나 트리거를 «만족» 이 아니라 «회피» 로 읽힐 수 있다** |
| **C+** | 사본을 유지하고 **상한만 3으로** | 변경 비용 거의 0 | 🔴 **§ D3 이 미리 금지**. 고르려면 **D3 을 명시적으로 뒤집는 지정**이어야 한다 |
| **E** | 범위 밖 — `ADR-MONO-069` 를 다시 열어 **`C1`**(EC2/Traefik TLS 종단) | 포워더 자체가 사라져 트리거가 **발화하지 않는다**. 🔴 대신 인증서 + **부팅마다 DNS 쓰기**가 부팅 경로에 들어온다(㉱ 로 Vercel API 토큰이 EC2 에 필요) | — (068 의 안이 아니다) |

### 🔴 이 표가 **말하지 않는 것**

- **어느 안도 «해석기 코드를 줄이는» 안이 아니다.** § D4 가 적었듯 공유 대상은 작다.
  이 결정이 사는 축은 코드량이 아니라 **«한 벌만 고쳐지는 것을 막는가»** 다.
- `B`/`B2` 의 비용은 **전부 배포 경로**에 있다. 이 저장소가 그 경로에서 데인 기록이
  `TASK-MONO-557`·`562`·`563`·`572`·`607` 로 다섯 건이다.
- `B3` 의 비용은 **가용성 결합**이고, 그것은 배포가 아니라 **운영** 축이다. 지금까지의
  실패는 전부 배포 축이었으므로 **비교할 실측이 없다** — 이 표에서 가장 불확실한 칸이다.

### 🔵 추천 (추천일 뿐이다 — ACCEPT 가 아니다)

**`B2`** 를 추천한다. 근거는 **비용의 성격**이다: `B` 는 세 Vercel 프로젝트의 install 을
동시에 건드리는데 그 자리가 이 저장소의 다섯 번 데인 자리이고, `B3` 은 가장 싸 보이지만
**대가가 운영 축이라 실측이 없다**. `B2` 는 비용이 **한 축(Root Directory 밖 포함 설정)**
으로 좁혀지고 그 축은 **관측으로 가를 수 있다**(그 관측 절차가 `fan-platform-web/VERCEL.md`
§ 남은 갈림길에 이미 적혀 있다).

🔴 **그러나 `C+` 를 고를 수도 있다** — 「지금은 승격하지 않는다」가 정당한 답인 경우가 있고,
그때 필요한 것은 **D3 을 뒤집는다는 명시**와 **새 상한이 자기 숫자를 갖는 것**이다
(§ D3 이 *"비용이 0인 「나중에 승격」 은 오지 않는다"* 라고 적은 이유가 그것이다).

### 🙋 지정 방법

```
ADR-MONO-068 D6 ACCEPTED — <B | B2 | B3 | C+ | E>
```

🔴 `진행` · `추천대로` 같은 긍정 신호로는 열리지 않는다 — `platform/architecture-decision-rule.md`
§ The ACCEPTED Gate. 🔵 rider 를 붙여도 된다(예: `B2 (Root Directory 밖 포함은 관측으로 가른다)`).

### D6.2 — 🔴🔴 **정정: ㉰ 는 거짓이었다** (2026-09-01, 지정 **뒤**에 발견)

§ D6 의 실측 표 ㉰ 는 *"저장소의 TS 공유 모듈 선례 **0건**"* 이라고 적었다. **틀렸다.**

**기전**: `libs/` 라는 **이름만** 봤다. 루트 `libs/` 와 `projects/*/libs/` 를 세고 «전부
Java» 를 확인한 뒤 «선례 0» 으로 결론지었다. **`packages/` 를 안 봤다.**

**실제 (2026-09-01 재측정)**:

| 잰 것 | 값 |
|---|---|
| `projects/ecommerce-microservices-platform/packages/*` | **6개** — `@repo/api-client` · `@repo/types` · `@repo/ui` · `@repo/utils` · `@repo/eslint-config` · `@repo/tsconfig` |
| 그 workspace 선언 | `pnpm-workspace.yaml` = `apps/*` + **`packages/*`** |
| `web-store` 가 의존하는 방식 | **`workspace:*`** (여섯 전부) |
| `projects/fan-platform/` 의 workspace | `web/*` **뿐** — packages 없음 |
| **프로젝트를 건너는** TS 공유 | 🔵 **여전히 0건** |

### ⇒ 이 정정이 결정을 바꾸는가 — **아니다. 그러나 근거의 절반이 바뀐다**

🔵 **`B2` 는 유지된다.** `B2` 가 필요로 하는 것은 **프로젝트를 건너는** 공유이고, 그 축의
선례는 **여전히 0**이다. `@repo/*` 는 **한 프로젝트의 pnpm 워크스페이스 안**에 있고
`workspace:*` 는 그 경계를 못 넘는다.

🔴 **그러나 «저장소가 TS 공유를 해 본 적이 없다» 는 인상은 거짓이었다.** 이 저장소는
그것을 **여섯 번** 했고, 관용구도 있다(`@repo/<name>`, `main: ./src/index.ts`, `private: true`).
⇒ `B2` 의 위험 평가는 **내려가야** 한다: 새로 발명할 것이 «패키지» 가 아니라 **«워크스페이스를
건너는 연결»** 하나다.

🔵 **`TASK-MONO-614` AC-2 는 이미 옳게 적혀 있었다** — *"`file:` / 상대경로 의존이다,
**워크스페이스 프로토콜이 아니다**"*. 그 문장이 정확히 이 정정이 가리키는 경계다.

### 🔴 왜 이 정정을 지정 **뒤**에 하게 됐나 — 기록해 둔다

「선례가 있나」를 **디렉터리 이름 하나**(`libs/`)로 물었다. 이 저장소는 그 실패를 이미
이름 붙여 두었다 — *"모집단을 다시 세라, 범위를 상속하지 마라"*. 🔴 **답이 «0» 으로 나오고
그것이 내 서술과 맞아떨어졌을 때 멈춰야 했다** — 0은 «없다» 와 «안 봤다» 의 구별이 없다.

발견 경로도 우연이다: `TASK-MONO-613` 에서 가드 모집단의 **상위집합을 세다가**
`packages/*/src/` 42개 파일이 튀어나왔다. **가드를 고치는 일이 ADR 의 거짓을 잡았다.**

### D6.1 — ✅ `B2` 가 채택됐다: **이 결정이 무엇을 바꾸는가** (2026-09-01)

**`B2` = 공유 패키지 하나를 만들되 루트 pnpm 워크스페이스는 만들지 않는다.**
소비자는 `file:` / 상대경로로 그 패키지를 의존한다.

#### ① 🔴 이 결정은 **가드를 교체한다** — 삭제가 아니다 (§ D5.3)

`scripts/check-demo-resolver-copies.sh` 가 지키던 명제는 *"사본이 둘 이상이면 정규화 후
같아야 한다"* 였다. `B2` 아래에서 **사본은 0이 되므로 그 명제는 공허해진다.**
🔴 **공허한 가드를 초록으로 남겨 두는 것이 이 저장소가 반복해 밟은 함정이다.**

⇒ 새 가드가 지켜야 하는 명제는 **뒤집힌다**:

| | 지금 (`C`) | **`B2` 이후** |
|---|---|---|
| 명제 | 사본들이 **서로 같은가** | 앱이 **자기 구현을 갖지 않는가** (= 전부 패키지를 import 하는가) |
| RED 조건 | 정규화 후 다름 · 앱 3개 이상 | **앱 안에 구현이 하나라도 있으면** |
| 마커의 역할 | 사본을 세기 위한 표시 | **금지된 것의 표시** — 마커가 앱 안에 있으면 RED |

🔵 § D2 의 «기계 판독 마커» 는 그대로 쓴다. 의미만 «세는 대상» 에서 «있으면 안 되는 것» 으로
바뀐다.

#### ② 🔴 `TASK-MONO-613` 이 **선행이 된다** (성격이 바뀌었다)

613 은 «가드 모집단이 `projects/` 밖 Next 앱을 못 본다» 를 고치는 티켓으로 기안됐고, 그때는
`610` 과 **독립**이라고 적었다. `B2` 아래에서는 아니다 — 새 가드가 *"어떤 앱도 자기 구현을
갖지 않는다"* 를 재려면 **모든 앱을 볼 수 있어야** 하고, 포워더는 `projects/` 밖에 놓일 수
있다. **모집단에 구멍이 있으면 새 명제는 그 구멍만큼 거짓이 된다.**

⇒ **613 → 614 순서로 못 박는다.**

#### ③ 🔵 남은 측정 하나 — **결정이 아니라 관측이다**

Vercel 의 *"Root Directory 바깥 소스를 빌드 단계에 포함"* 설정은 **대시보드에만** 있고,
`B2` 는 세 앱이 Root Directory 밖의 패키지를 참조하게 만든다. 🔵 그 값은
**관측으로 가른다** — 절차가 `projects/fan-platform/web/fan-platform-web/VERCEL.md`
§ *"남은 갈림길을 다음 커밋이 스스로 가른다"* 에 이미 적혀 있다(같은 설정의 다른 축을 그렇게
갈랐다). 🔴 **추정하지 마라** — 이 저장소는 이 설정을 «관측으로만 가를 수 있는 것» 으로
두 번 다뤘다.

#### ④ 실행 티켓

**`TASK-MONO-614`** — `B2` 구현. 🔴 **크로스 프로젝트라 한 PR 에 원자적으로** 들어가야 한다
(`CLAUDE.md` § Cross-Project Changes): 패키지 신설 + `web-store` · `fan-platform-web` 두
소비자 전환 + 가드 교체가 **같은 커밋**이다. 한쪽만 가면 그 창에서 main 이 깨진다.

---

### 🔴 지정 전에는 무엇이 막히나

`TASK-MONO-610` 의 **포워더 구현 전체**다. AC-0·AC-1·AC-2 정적 절반은 이미 닫혔고,
남은 것(AC-2 동적 · AC-3 · AC-4/4b · AC-5)은 전부 그 앱의 존재를 전제한다.
🔵 **`TASK-MONO-613`(가드 모집단의 구멍)은 이 지정과 독립**이며, 오히려 **지정 전에** 닫는
편이 낫다 — 그래야 어떤 안을 고르든 트리거가 **확실히** 문다.

## Consequences

- ➕ **구조 변경 0.** 루트 워크스페이스도, 새 패키지도, Vercel install 설정 변경도 없다.
- ➕ 각 앱이 **자기 스택에 맞는** 구현을 갖는다(axios / route handler / 네이티브 fetch).
- ➖ 🔴 **두 번째 구현이 생기면 중복이 실재한다.** 그것을 감수하는 대가로 트리거를 둔다 —
  중복이 **생기는 것**을 막지 못하고, **조용히 생기는 것**을 막는다.
- ➖ 트리거가 발화하는 시점은 **단계 3(console) 착수 시**로 예상된다. 즉 이 결정은
  *"지금 정하지 않는다"* 를 **명시적으로, 만료일과 함께** 고른 것이다.
- 🔵 오늘 가드의 실제 대상은 **0개**다. 그래서 라이브 통과는 아무것도 증명하지 않으며,
  가드는 **탐지기 생존 대조군**과 **선언 앱 커버리지**로 자기가 눈이 있음을 따로 증명한다.

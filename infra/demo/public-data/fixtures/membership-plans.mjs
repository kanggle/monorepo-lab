// DEMO-PUBLIC-DATA: 멤버십 **요금제 안내**. 저장소가 소유한 `authored` 콘텐츠다.
//
// =============================================================================
// 🔴🔴 왜 이것을 백엔드에서 안 뽑는가
// =============================================================================
// 팬 게이트웨이의 `GET /api/v1/memberships` 는 *"내 구독"* 을 돌려주는 **개인 데이터**
// 엔드포인트다 — `Membership` DTO 는 구독자 계정과 결제 상태를 담는다(실측:
// `projects/fan-platform/web/fan-platform-web/src/entities/membership/types.ts`, 그리고
// 그 호출부 `getMemberships(accessToken)` 는 세션 토큰을 **요구**한다).
//
// 그것을 «요금제 목록» 의 출처로 삼는 것은, 개인 구독 레코드를 공개 저장본으로 복사하는
// 일이다. 필드를 잘 골라도 그 경로가 존재하는 것 자체가 위험하다 — 다음 사람이 필드를
// 하나 더 넣을 때 그것이 무엇인지 모른다.
//
// ⇒ **요금제 안내는 제품 문구다.** 백엔드에 원본이 없고, 있을 필요도 없다. 실제 과금·가입·
//   해지는 로그인 뒤 백엔드가 하고(그 경로는 그대로다), 이 카드는 그 화면으로 보내는
//   안내판일 뿐이다.
//
// 🔴 값을 «실시간 가격» 처럼 쓰지 마라. `note` 가 그 사실을 화면에 강제로 싣게 하는 자리다.
// 🔵 티어 이름(`MEMBERS_ONLY` / `PREMIUM`)은 백엔드 `MembershipTier` 와 **같은 값**이다.
//    로그인 후 실제 가입 화면과 어휘가 어긋나면 방문자가 다른 상품으로 읽는다.
// =============================================================================

export const MEMBERSHIP_PLANS = [
  {
    tier: 'MEMBERS_ONLY',
    name: '멤버스',
    priceKrw: 4900,
    period: 'MONTHLY',
    benefits: [
      '멤버십 전용 게시물 열람',
      '아티스트 소식 우선 알림',
      '월 1회 멤버 전용 라이브',
    ],
    note: '표시 가격은 안내용입니다. 실제 가입·결제는 로그인 후 진행되며, 그 시점의 조건이 적용됩니다.',
  },
  {
    tier: 'PREMIUM',
    name: '프리미엄',
    priceKrw: 12900,
    period: 'MONTHLY',
    benefits: [
      '멤버스의 모든 혜택',
      '미공개 데모·비하인드 영상',
      '팬미팅 응모 우선권',
      '멤버십 전용 굿즈 할인',
    ],
    note: '표시 가격은 안내용입니다. 실제 가입·결제는 로그인 후 진행되며, 그 시점의 조건이 적용됩니다.',
  },
];

# Task ID

TASK-MONO-613

# Title

🔴🔴 해석기 가드의 모집단이 **`projects/` 밖의 Next 앱을 못 본다** — 승격 트리거를 **디렉터리 선택으로 피할 수 있다**

# Status

done

# Owner

monorepo

# Task Tags

- ci
- adr
- demo

---

# 🔴 성격이 바뀌었다 (2026-09-01) — **독립이 아니라 `TASK-MONO-614` 의 선행이다**

기안 당시 이 티켓은 *"`TASK-MONO-610` 과 독립이며 오히려 먼저 닫는 편이 낫다"* 였다.
그 뒤 **`ADR-MONO-068 § D6` 이 `B2` 로 ACCEPTED** 됐고(소유자 정확형 지정), 그러면서
이 티켓은 **선택이 아니라 선행**이 됐다.

`B2` 아래에서 가드가 재야 하는 명제는 뒤집힌다 — *"사본들이 서로 같은가"* 에서
**"어떤 앱도 자기 구현을 갖지 않는가"** 로. 🔴 **후자를 재려면 «모든 앱» 을 볼 수 있어야
한다.** 모집단에 구멍이 있으면 새 명제는 **그 구멍만큼 거짓**이고, 그 거짓은 초록으로 보인다.

⇒ **613 → 614 순서**가 `ADR-MONO-068 § D6.1 ②` 에 못 박혔다.
🔵 AC 는 **하나도 바뀌지 않는다** — 바뀐 것은 **왜 급한가** 다.

---

# 🔎 어디서 왔나

`TASK-MONO-610`(C2 포워더)의 착수 조건을 확인하다 나왔다. C2 의 포워더는 **세 번째 해석기**라
`ADR-MONO-068` § D5.4 의 **승격 트리거**를 발화시켜야 한다. 그런데 —

```bash
APP_RE='^projects/[^/]+/(apps|web)/[^/]+'          # scripts/check-demo-resolver-copies.sh:103
```

**포워더를 `infra/demo/…` 에 두면 그 앱은 모집단 밖이고, 트리거는 조용히 안 문다.**

🔵 **오늘 이 구멍은 잠재적이다** — 실측: 저장소의 `next.config.*` 는 **3개**이고
**전부 `projects/` 안**이다. 즉 지금은 오탐도 미탐도 없다.
🔴 **그러나 `TASK-MONO-610` 이 만들려는 것이 정확히 첫 번째 예외다.**

---

# 🔴 왜 이것이 «나중에» 가 아닌가

가드가 지키는 것은 코드 중복이 아니라 **결정**이다 — *"세 번째가 오면 `B` 로 승격할지
소유자가 정한다"*. 모집단에 구멍이 있으면 그 **결정 자체를 디렉터리 선택으로 건너뛸 수 있다**,
그리고 건너뛴 사실이 **아무 데도 남지 않는다.**

🔵 이 저장소는 같은 모양을 이미 여러 번 기록했다 — 가드가 안 무는 이유 중 «도달성»,
그리고 *"가드 **없는** 한 지점이 결함 자리"*.

---

# Goal

**해석기 가드의 모집단을 「경로 규약」이 아니라 「선언」에서 유도한다.**
`next.config.*` 가 있는 디렉터리는 **어디에 있든** 앱이다.

---

# Scope

**포함**

- `scripts/check-demo-resolver-copies.sh` 의 `APP_RE` / `app_files` / `declared_apps` /
  `fold_to_app` — 네 곳이 같은 가정을 공유한다
- 그 가드의 `--self-test` 에 **`projects/` 밖 앱** 칸 추가
- 🔵 파일 헤더의 «무엇을 세는가» 절 갱신 — 지금 *"`projects/<p>/apps/<a>` 접두사로 접어서
  센다"* 라고 적혀 있고, 그것이 이 결함의 **선언된 형태**다

**제외**

- `ADR-MONO-068` 의 결정 변경 — 이 티켓은 **가드가 결정을 집행하게** 할 뿐이다
- 포워더 앱 자체 — `TASK-MONO-610`

---

# Acceptance Criteria

## AC-0 — 착수 시 재측정

1. `next.config.*` 전수와 그중 `projects/` 밖의 수. 🔵 오늘 = **3 / 0**.
2. 가드가 여전히 rc=0 이고 «해석기를 가진 앱 2 개 (승격 3)» 을 내는가.

## AC-1 — 모집단을 **선언에서** 유도한다

- `declared_apps()` = `next.config.*` 를 가진 **모든** 디렉터리 (경로 접두사 무관)
- `app_files()` = 그 앱들의 소스
- `fold_to_app()` = 파일 → **가장 긴 선언 앱 접두사**
- 🔴 기존 세 앱의 판정이 **바뀌지 않아야** 한다(오늘 값 = 앱 2개 / 비교 1쌍 / 소스 1191개 /
  선언 앱 3개 / 대조군 2건). **먼저 그 다섯 수를 고정하고 바꿔라.**

## AC-2 — 🔴 self-test 에 **`projects/` 밖 앱** 칸을 넣는다

- 합성 트리에 `infra/x/app/next.config.ts` + 해석기 파일을 놓고 **모집단에 잡히는지**
- 🔴 그 칸이 **현재 판에서 실패**하는지 먼저 확인한다(= bite). 안 하면 «고쳤다» 가 공허하다

## AC-3 — 대조군을 깨뜨리지 않는다

가드의 칸 (3)은 **론처(`infra/demo/aws/site/`)** 에서 `DEMO_API_BASE` 가 보이는지로 탐지기
생존을 확인하고, 그 근거는 *"론처는 모집단 **밖**"* 이다. 🔴 모집단을 넓히면 **론처가 안으로
들어올 수 있다** — 그러면 대조군이 자기 자신을 재게 된다.

🔵 론처에는 `next.config.*` 가 **없다**(정적 HTML) ⇒ 선언 기반 모집단에는 안 들어온다.
**그 사실을 단언으로 고정하라** — 나중에 론처가 Next 로 바뀌면 이 가드가 조용히 눈이 먼다.

---

---

# 구현 결과 (2026-09-01 UTC)

## ✅ AC-0 — 재측정

| | 값 |
|---|---|
| `next.config.*` 전수 / `projects/` 밖 | **3 / 0** (기안 시점과 같다) |
| 가드 기준선 | `해석기를 가진 앱 2 개 (승격 3) · 정규화 비교 1 쌍 · 앱 소스 1191 개 · 선언 앱 3 개 · 대조군 2 건`, rc=0 |

## 🔴 AC-2 를 **먼저** 했다 — bite 가 없으면 「고쳤다」가 공허하다

### bite ① — `projects/` **밖** 앱 (라이브)

`infra/demo/_bite613/{next.config.ts, src/demo-backend.ts}`(마커 + `DEMO_API_BASE`)를
**스테이지**하고 돌렸다:

| 판 | 결과 |
|---|---|
| **이전 판** | 🔴 `해석기를 가진 앱 **2** 개` · **rc=0** — **눈이 멀어 있었다** |
| **새 판** | ✅ `해석기를 가진 앱 **3** 개` · **rc=1** — 승격 메시지까지 정상 |

### bite ② — **선언 없는** 디렉터리 (라이브, 옛 스크립트를 실제로 꺼내서)

`git show origin/main:scripts/…` 로 **옛 판을 꺼내** 같은 트리에 대고 돌렸다.
주입은 `services/notifier/src/demo-backend.ts` — **`next.config.*` 가 없다**(0건 확인).

| 판 | 결과 |
|---|---|
| **옛 판** | 🔴 `앱 **2** 개` · **rc=0** |
| **새 판** | ✅ **rc=1** |

🔵 **②가 필요했던 이유**: 모집단을 «선언된 앱» 으로만 좁히면 방금 메운 구멍과 **같은 모양의
새 구멍**이 생긴다 — `next.config.*` 없는 디렉터리에 두면 또 안 물린다.
**가드는 「무는 쪽」으로 실패해야 한다.**
[[feedback_an_optimisation_must_fail_toward_more_work]]

## ✅ AC-1 — 모집단을 **선언**에서, 접기는 **선언 우선 + 폴백**

| 함수 | 이전 | **지금** |
|---|---|---|
| `declared_apps` | `^projects/…/next.config.*` | **저장소 전체**의 `next.config.*` 가 있는 디렉터리 |
| `app_files` | 선언 앱의 `src/` | **어떤 `src/` 아래의 TS 든** (탐지는 넓게) |
| `fold_to_app` | 경로 정규식 | **가장 긴 선언 접두사** → 없으면 **`src/` 의 부모** → 없으면 파일의 디렉터리 |

`APP_RE` 는 **코드에서 사라졌다**(헤더에 «이전 판» 인용으로만 남는다).

### 다섯 수 — **판정은 그대로, 모집단만 넓어졌다**

| | 이전 | 지금 |
|---|---|---|
| 해석기를 가진 앱 | 2 | ✅ **2** |
| 정규화 비교 | 1 쌍 | ✅ **1 쌍** |
| 선언 앱 | 3 | ✅ **3** |
| 대조군 | 2 건 | ✅ **2 건** |
| **앱 소스** | 1191 | 🔵 **1233** (+42) |

🔵 **+42 의 정체를 먼저 쟀다**: 전부
`projects/ecommerce-microservices-platform/packages/*/src/`(= `@repo/*`)이고,
**내용 패턴 0건 · 마커 0건** 이다. 저장소 전체에서 모집단 밖 `src/` 의 내용 패턴 적중도
**0건** 이었다 ⇒ **판정을 바꾸지 않는다.** 🔴 넓히기 **전에** 쟀다 — 걸렸다면 가드가 갑자기
빨개졌을 것이다.

🔴🔴 **그리고 그 +42 가 `ADR-MONO-068 § D6` 의 거짓 하나를 잡았다** — 내가 *"TS 공유 모듈
선례 0건"* 이라고 적었는데 그것들이 바로 그 선례다. § D6.2 로 정정했다(별도 PR).
**가드를 고치는 일이 ADR 의 거짓을 잡았다.**

## ✅ AC-2 (self-test) — 칸 **둘**을 추가했고, 픽스처의 가정도 고쳤다

🔴 **픽스처가 앱 목록을 하드코딩하고 있었다** — `for app in projects/p1/apps/a1
projects/p2/web/a2` 두 줄. 그래서 3앱 칸의 세 번째 앱은 **`next.config.*` 가 없는** 상태였고,
선언 기반 모집단으로 바꾸자 그 칸이 **깨졌다**. 픽스처가 **자기가 만든 것을 선언하지 않는**
상태였던 것이다 ⇒ 인자로 받은 경로에서 앱 디렉터리를 **유도**하도록 고쳤다.

🔵 그 깨짐은 **좋은 신호였다** — 내 변경이 실제로 판정을 바꾼다는 증거이고, 동시에
「선언 없는 앱은 안 세어진다」는 **새 사각지대**를 드러냈다(⇒ AC-1 의 폴백).

**self-test: 10칸 → 12칸, 12 passed / 0 failed.**

## ✅ AC-3 — 대조군을 지켰다. **우연을 단언으로 고정했다**

칸 (3)은 *"론처(`infra/demo/aws/site/`)는 모집단 **밖**"* 이라는 전제로 탐지기 생존을 잰다.
모집단을 넓히면 그 전제가 깨질 수 있다.

🔵 오늘 론처는 정적 HTML 이라 `next.config.*` 가 없어 안 들어온다. 🔴 **그 안전은 우연이다** —
론처가 Next 로 바뀌는 날 이 가드는 **조용히 눈이 먼다**. ⇒ 새 칸 **(3b)** 가
`declared` 안에 론처가 있으면 **`exit 2`(판정 불가)** 로 세운다.

## 검증

| | |
|---|---|
| 라이브 | **rc=0** — 판정 4개 그대로, 소스 1233 |
| self-test | **12 passed / 0 failed** (rc=0) |
| bite | **2/2** — 둘 다 **옛 판이 rc=0**, 새 판 rc=1 |
| `bash -n` | 통과 |
| 다른 가드 | index-queue · task-id · walkthrough 전부 rc=0 |

## 🔵 이 티켓이 **안 하는 것**

- `ADR-MONO-068 § D6 = B2` 의 구현 — **`TASK-MONO-614`**. 이 티켓은 그 가드가 **볼 수 있게**
  만들 뿐이다.
- 가드의 **명제** 전환(«사본이 같은가» → «앱이 구현을 갖지 않는가») — 그것도 614 다.
  🔴 즉 **이 파일은 614 에서 한 번 더 바뀐다.**

# Related Specs

- [`docs/adr/ADR-MONO-068-…md`](../../docs/adr/ADR-MONO-068-where-the-demo-backend-resolver-lives.md) § D3 · § D5 · § D5.4
- `scripts/check-demo-resolver-copies.sh` § 무엇을 세는가 · 칸 (1b)(3)(4)
- `tasks/in-progress/TASK-MONO-610-…md` § 새 선행

# Related Contracts

없음.

# Edge Cases

- **`next.config.*` 가 있지만 Next 앱이 아닌 디렉터리** — 픽스처·템플릿. 오늘 0건이지만
  생기면 모집단이 부푼다. 🔵 AC-1 의 «다섯 수 고정» 이 그것을 잡는다.
- **론처가 Next 로 바뀐다** → AC-3 의 단언이 문다.
- **`--self-test` 의 합성 트리가 `projects/` 를 가정한다** → AC-2 가 그 가정을 깬다.

# Failure Scenarios

| 실패 | 증상 | 방어 |
|---|---|---|
| 모집단만 넓히고 대조군을 안 본다 | 론처가 모집단에 들어와 칸 (3)이 자기를 잰다 | AC-3 |
| self-test 를 안 고친다 | 합성 트리가 옛 가정을 계속 검증 | AC-2 |
| bite 없이 «고쳤다» | 새 칸이 원래도 통과했을 수 있다 | AC-2 둘째 줄 |

# Task ID

TASK-MONO-665

# Title

🔴 **예산 상한과 그것을 정당화하는 산술이 서로 다른 인스턴스를 말한다** — 변수는 `r6i.2xlarge` 로 바뀌었는데 비용 근거 세 곳은 아직 `m6i.2xlarge` 를 계산한다, 그리고 그 결론(「월 $15 미만」)은 이미 성립하지 않는다

# Status

done

# Owner

monorepo

# Task Tags

- infra
- demo
- cost
- docs

---

# Goal

데모 월 예산(`monthly_budget_minutes`)을 **실측 요금 위에** 올린다. 두 갈래다:

1. **상한값**: `600` → **`1200`**(= 20시간). 🟢 **소유자 결정 2026-09-11.**
2. **근거 정정**: 그 상한의 비용 산술을 **배포된 인스턴스 타입**으로 다시 쓴다.

🔵 둘은 같은 사실의 두 면이라 한 티켓이다 — 값만 바꾸고 주석을 두면 **주석이 새 값을 더 크게 틀리게 설명한다**(600 에서 $1.36 틀리던 것이 1200 에서 $2.72 틀린다).

---

# Background — 무엇이 어긋났나

## 변수는 고쳐졌고 산문 셋은 안 따라갔다

`TASK-MONO-552` 가 `m6i.2xlarge`(32GB)를 **라이브 실측으로 반증**하고(*"31.5 GiB 중 약 29 GiB 사용 / MemAvailable 2.4 GiB"*) `r6i.2xlarge`(64GiB)로 올렸다. git 으로 갈랐다:

| | 커밋 | 내용 |
|---|---|---|
| 주석이 쓰인 때 | `60c913208` (`TASK-MONO-366`) | 비용 산술을 `m6i.2xlarge` 로 씀 |
| 인스턴스가 바뀐 때 | `4d328cfd0` (`TASK-MONO-552`) | `r6i.2xlarge` 로 교체 |

⇒ **변수는 바뀌고 산문은 안 바뀌었다.** 이 저장소가 이름 붙인 *«한 사실이 여러 절에 있으면 한쪽만 고쳐진다»* 의 실례다.

## 낡은 자리 **다섯** (전수)

🔴🔴 **처음에 「셋」으로 적었고 그것이 틀렸다.** 첫 grep 을 `infra/demo/aws/` 로만 돌렸기
때문이고, **`infra/demo/README.md` 에 둘이 더 있었다.** 구현 중 AC-2 의 마지막 칸
(*"넷째 자리가 없는지 다시 grep 하라"*)이 잡았다. 🔵 **이 오류가 이 티켓의 주제 그 자체다** —
「셋」은 grep 의 결과이지 성질이 아니었고, **모집단을 좁게 잡으면 전수조사는 «어디를 봤나»를
잰다.**

| # | 자리 | 지금 내용 | 왜 틀렸나 |
|---|---|---|---|
| ① | `aws/terraform/variables.tf:148` | `600분(10시간) × m6i.2xlarge ≈ $5/월 — EBS 상시 $9 와 합쳐 월 $15 미만` | **예산의 비용 근거 자체.** 타입이 틀렸고 **결론까지 깨졌다** |
| ② | `aws/README.md:197` | `가동 중 (m6i.2xlarge) \| ~$0.5/시간` | 타입·단가 둘 다 낡음 |
| ③ | `aws/README.md:211` | `m6i.2xlarge(32GB)에서 여유 ~5.5GB` | 🔴 552 의 실측과 **정반대** — 32GB 에서 여유는 **2.4GiB** 였다 |
| ④ | `demo/README.md:54-56` | `실제 데모 호스트는 m6i.2xlarge(32GB) — variables.tf 의 기본값이 권위다 … 64GB 는 선택지이지 현재 구성이 아니다` | 🔴🔴 **권위를 인용하면서 그 권위와 반대되는 값을 적는다.** 그리고 마지막 절이 **정확히 거꾸로**다 — 64GiB 가 현재 구성이다 |
| ⑤ | `demo/README.md:136-139` | `실측 결과 m6i.2xlarge(32GB)로 뜬다 … 이전 판은 64GB 가 «필요»하다고 적었는데 그건 측정 전의 추정이었다` | 🔴🔴 **정정이 옳은 것을 뒤집은 기록이다.** ①64GB 필요 → ②366 이 「추정」이라며 뒤집음 → ③552 가 ①을 복원 |

🔴 **③④⑤ 가 ①② 보다 나쁘다.** ①② 는 「낡은 수치」지만 ③④⑤ 는 **반증된 주장을 아직 참인
것처럼** 적고 있고, ⑤ 는 한 술 더 떠 **옳았던 원판을 «추정» 이라고 깎아내린 채로** 남아 있다.

🔵 ⑤ 에서 `TASK-MONO-366` 이 틀린 이유는 산술이 아니라 **술어**다 — *"뜬다"*(컨테이너가
기동한다)와 *"충분하다"* 는 다른 명제인데 한 문장으로 썼다. 32GB 에서 스택은 **떴고, 동시에
thrashing 하고 있었다.**

🔵 **건드리지 않는 `m6i` 언급 둘**: `variables.tf:34` 의 `m6i.4xlarge` 비교는 r6i 를 고른
이유를 설명하는 **정당한 문장**이다. `projects/fan-platform/tasks/done/…-014` 의 것은
**`done/` 이라 frozen** 이다.

## 🔴 ① 의 결론은 **현재 600 에서도 이미 거짓이다**

실측 요금(AWS 공개 요금표, 발행 `2026-09-10`, `ap-northeast-2` · Linux · 온디맨드):

| 타입 | 시간당 | 메모리 |
|---|---|---|
| `m6i.2xlarge` (주석의 근거) | **$0.4720** | 32 GiB |
| **`r6i.2xlarge`** (배포된 것) | **$0.6080** | 64 GiB |

⇒ **28.8% 높다.**

```
주석 기준  600분 × $0.472 = $4.72  ≈ "$5"        ← 산술은 내부적으로 맞다
실제      600분 × $0.608 = $6.08                 ← 타입이 틀렸다
결론      $6.08 + EBS $9 = $15.08  >  "월 $15 미만"   ← 부등호가 이미 뒤집혀 있다
```

🔵 **주석의 산술을 탓할 것이 아니다** — 그때는 맞았다. 틀린 것은 **인스턴스가 바뀐 뒤에도 그 문장이 남아 있는 것**이다.

⚪ **EBS `$9` 는 이 티켓이 재지 못했다.** AWS 공개 EBS 엔드포인트가 404 라 저장소가 적어 둔 값을 그대로 인용했다. 🔴 그러므로 *"$15 를 넘는다"* 의 **정확한 판정은 EBS 실측에 달려 있다** — 컴퓨트 축($6.08 · $12.16)만이 측정값이다. AC-2 가 이것을 다룬다.

---

# 왜 1200 인가 (소유자 결정의 근거를 남긴다)

## 「600 단위」에 기계적 근거는 없다

예산 로직은 `handler.py:275` 의 **`return u["seconds"] >= BUDGET_MINUTES * 60` 한 줄이 전부**다.
구간·임계·경고 밴드·반올림이 **0건** ⇒ `1013` 도 `1200` 과 똑같이 유효하다.
**`600` 은 단위가 아니라 둥근 수였다.**

## 진짜 단위는 «창» 이고, 실측했다

CloudWatch `CPUUtilization` 데이터포인트로 지난 5일 가동 구간을 셌다
(인스턴스 `i-0863ba8d8faf52c63`, period 300s, 37 포인트):

```
창 4개 / 185분  →  창 평균 46.2분   (부팅 9분 16초가 그 안의 순수 오버헤드)
```

| 분 | 시간 | 창/월 | 컴퓨트/월 | +EBS(~9) |
|---|---|---|---|---|
| **600 (현재)** | 10.00 | 13.0 | $6.08 | $15.08 |
| 900 | 15.00 | 19.5 | $9.12 | $18.12 |
| 1000 | 16.67 | 21.6 | $10.13 | $19.13 |
| **1200 (결정)** | **20.00** | **25.9** | **$12.16** | **$21.16** |

## 결정 근거 셋

1. 🔴 **600 은 실측으로 부족하다** — 2026-09 에 **11일 만에 543분**(창 약 11.7개)을 썼고 **중간에 동났다**. 남은 19일에 57분뿐이라 창을 한 번도 넉넉히 못 연다.
2. 🔵 **1000 vs 1200 의 비용차는 $2.03/월** — 판단 근거가 되기엔 너무 작다. 그래서 비용이 아니라 **가독성**으로 갈랐다: 이 저장소는 이미 **시간으로** 쓴다(`variables.tf:148` = *"600분(10시간)"*). `1200` 은 *"1200분(20시간)"* 으로 그대로 들어가지만 `1000` 은 *"16시간 40분"* 이라 같은 문장에서 깨진다. ⇒ **정시로 떨어지는 값**이 그 자리에 맞다.
3. 🔴 **설정값 = 그달 최악 청구다.** `variables.tf:146-147` 이 직접 말한다 — *"idle_minutes / max_runtime_minutes 는 반복 호출로 리셋되므로 지출 상한이 아니다. **이 값만이 실질적 상한**"*. 1200 의 최악은 컴퓨트 **$12.16**(+EBS ≈ $21). 🔵 «크면 공짜로 좋다» 가 아니라 **그 최악값을 받아들인다는 결정**이다.

---

# Scope

## In Scope

- `variables.tf` 의 `monthly_budget_minutes` default `600` → `1200`
- 위 낡은 자리 **다섯**(①~⑤)을 실측 기준으로 정정
- `terraform plan` 으로 **실제 변경분 확인** → apply(🔴 승인 축)
- apply 후 `/status` 의 `budget_minutes` 가 실제로 바뀌었는지 **런타임 확인**

## Out of Scope

- 🔴 **인스턴스 타입 재검토** — `r6i.2xlarge` 는 `TASK-MONO-552` 가 실측으로 정한 것이고 여기서 안 건드린다
- 🔴 **EBS 볼륨 크기·타입 변경** — 비용 축이지만 다른 크기의 일이고, 100GB gp3 는 552 의 메모리 판단과 무관하다
- `idle_minutes` · `max_runtime_minutes` 조정 — 지출 상한이 아니다(위 근거 3)
- 지난달 이전의 실제 청구 분석 — Cost Explorer 축이고 소유자 콘솔 사안

---

# Acceptance Criteria

## AC-0 — 착수 게이트 (verify-then-act)

- [x] 🔴 **요금을 다시 읽어라.** 이 티켓의 `$0.6080` 은 **발행일 `2026-09-10` 의 단일 표본**이다.
      AWS 요금은 바뀐다. 자격증명 없이 읽히는 곳:
      `https://b0.p.awsstatic.com/pricing/2.0/meteredUnitMaps/ec2/USD/current/ec2-ondemand-without-sec-sel/Asia%20Pacific%20(Seoul)/Linux/index.json`
      🔴 **`curl --compressed` 로 받아라** — gzip 이라 그냥 받으면 바이너리가 나오고
      grep 이 조용히 0건을 낸다(이 티켓이 한 번 밟았다).
      🔵 키는 점이 아니라 공백이다: `"r6i 2xlarge Asia Pacific Seoul Linux"`.
- [x] 🔴 **배포된 인스턴스 타입을 다시 확인하라** — 티켓의 전제 전체가 «r6i 가 배포돼 있다» 에
      달려 있다. `aws ec2 describe-instances` 로 읽고, `terraform.tfvars` 의 `instance_type`
      과 **둘 다** 본다(하나만 보면 드리프트를 못 잡는다).
- [x] 🔴 **`monthly_budget_minutes` 가 tfvars 에 생겼는지 다시 보라.** 지금은 **없어서**
      `variables.tf` 의 default 가 유효하다 — 🔴 **누가 tfvars 에 넣었다면 default 를 고쳐도
      아무 일도 안 일어난다.** 그때는 tfvars 를 고쳐야 하고, 이 티켓의 AC-1 이 잘못된 파일을
      가리키게 된다.

## AC-1 — 상한값을 1200 으로

- [x] `variables.tf` 의 `monthly_budget_minutes` default 를 **`1200`** 으로 바꾼다.
- [x] 🔴 **`description` 도 같이 본다** — *"매월 1일 리셋"* 은 여전히 참인가(`_month()` 가
      UTC 달력월이므로 참이다). 참이면 그대로 두고, **확인했다는 사실**을 PR 에 적는다.

## AC-2 — 비용 근거를 실측으로 다시 쓴다 (자리 **다섯** 전부)

- [x] ① `variables.tf` 의 비용 주석: **`r6i.2xlarge` · 실측 시간당 단가 · 1200분 기준 총액**.
      🔴 **「월 $N 미만」 같은 부등호 결론을 다시 쓰려면 EBS 를 재고 써라.** 못 쟀으면
      ⚪ 로 *"EBS 는 미측정"* 이라고 적고 **컴퓨트 축만** 단언한다 — 🔴 **못 잰 값을 넣어
      부등호를 만드는 것이 이 티켓이 고치려는 결함 그 자체다.**
- [x] ② `README.md:197` 의 `가동 중 (m6i.2xlarge) ~$0.5/시간` → 실측 타입·단가.
- [x] ③ `README.md:211` 의 `m6i.2xlarge(32GB)에서 여유 ~5.5GB` → 🔴 **552 의 실측으로
      교체**한다(32GB 에서는 여유가 2.4GiB 라 기각됐고 지금은 64GiB 다). 🔵 이 줄은
      「수치 갱신」이 아니라 **반증된 주장의 삭제**다.
- [x] ④ `infra/demo/README.md:54-56` — 🔴 `variables.tf` 를 **권위로 인용하면서 반대되는
      값**을 적는 줄이다. 호스트를 `r6i.2xlarge`(64 GiB)로 고치고, *"64GB 는 … 현재 구성이
      아니다"* 를 **삭제**한다(지금은 그것이 현재 구성이다).
- [x] ⑤ `infra/demo/README.md:136-139` — 🔴🔴 **정정이 옳은 것을 뒤집은 기록**이다.
      *"이전 판은 64GB 가 필요하다고 적었는데 그건 측정 전의 추정"* 을 지우고, **뒤집힘
      셋(원판 → 366 의 번복 → 552 의 복원)을 그대로 남긴다.** 🔵 «지금 값» 만 적고 이력을
      지우면 **다음 사람이 366 을 다시 읽고 또 뒤집는다.**
- [x] 🔴 **다섯을 한 커밋에.** 하나만 고치면 나머지가 «맞는 것처럼» 남아 다음 사람이 그걸 읽는다.
- [x] 🔴🔴 **모집단을 좁히지 마라 — 이 티켓이 그 실수를 이미 한 번 했다.**
      `grep -rn 'm6i' --include='*.tf' --include='*.md' --include='*.py' --include='*.sh' .`
      를 **저장소 루트에서** 돌려라(`infra/demo/aws/` 로만 돌려서 ④⑤ 를 놓쳤다).
      남는 것은 **`variables.tf:34`(정당한 비교) 와 `tasks/` 안의 것(frozen)뿐**이어야 한다.
      🔴 잔여를 «0건» 으로 적지 말고 **«남은 것과 남긴 이유»** 로 적어라.

## AC-3 — plan 으로 변경분을 확인한다 (🔴 apply 전에)

- [x] 🔴 **`terraform plan` 을 먼저 돌리고 출력을 PR 에 붙인다.**
      기대: **Lambda 환경변수 한 줄**(`MONTHLY_BUDGET_MINUTES: 600 → 1200`).
- [x] 🔴🔴 **인스턴스·AMI·볼륨이 plan 에 나오면 STOP 이고 소유자에게 되돌린다.**
      이 스택은 `ami_id` 가 움직이면 **인스턴스 교체**가 되고(`TASK-MONO-645` § AC-3),
      그러면 «예산 한 줄 바꾸기» 가 **데이터 손실을 동반한 재생성**이 된다.
      🔵 `data.external.ami_bundle_capability` 가 저장소에서 값을 유도하므로 **plan 이
      저장소 상태에 따라 달라질 수 있다** — 그래서 plan 이 형식적 절차가 아니다.
- [x] `plan` 의 변경 리소스 **개수**를 적는다(`N to add, M to change, K to destroy`).
      🔴 `destroy` 가 0 이 아니면 무조건 STOP.

## AC-4 — apply 와 런타임 확인 (🔴 소유자 승인 축)

- [x] 🔴 **apply 는 소유자 승인 없이 돌리지 않는다.** AC-3 의 plan 을 보이고 받는다.
- [x] apply 후 **런타임으로** 확인한다 — `GET /status` 의 `budget_minutes` 가 **1200**.
      🔴 **`.tf` 파일이 바뀐 것은 판정이 아니다**(선언 ≠ 런타임, 이 저장소가 반복해서 밟은 축).
- [x] 🔵 **같은 응답의 `used_minutes` 도 적는다** — 543 을 쓴 상태에서 1200 이면 그 자리서
      **657분**이 생긴다. 그 수가 실제로 맞는지는 응답이 말해 준다.
- [x] ⚪ 🔴 **`_usage()` 는 «당월 누적 초» 를 들고 상한만 비교하므로 소급 효과가 있다** —
      이미 쓴 543 분이 사라지는 것이 아니라 **천장이 올라가는 것**이다. 이 구분을 적어라
      (「예산이 리셋됐다」로 읽히면 다음 사람이 월초로 착각한다).

## AC-5 — 다음에 또 낡지 않게

- [x] 🔴 **이 티켓이 만든 수치도 낡는다.** 요금은 바뀌고 인스턴스 타입도 바뀔 수 있다.
      ⇒ 주석에 **«언제 · 무엇을 · 어디서 쟀는지»** 를 같이 적는다(발행일 + 엔드포인트).
      🔵 *"이 수치는 YYYY-MM-DD 요금표 기준"* 한 줄이 있으면 다음 사람이 **낡음을 판정할 수
      있다**; 없으면 이번처럼 **틀린 줄도 모르고 3개월을 간다**.
- [x] ⚪ **가드를 만들지 말지 판단하고 근거를 적어라.** 🔴 *"만들자"* 를 기본값으로 두지 마라 —
      「배포된 `instance_type` 과 주석의 타입이 일치하는가」는 **grep 으로 물 수 있지만**,
      요금 숫자는 외부 API 라 가드가 **네트워크에 의존**하게 되고 그러면 **DNS 가 죽으면
      rc=0 으로 초록**이 된다(이 저장소가 이름 붙인 함정). ⇒ 만든다면 **타입 일치만** 물고
      숫자는 안 무는 것이 맞을 수 있다. 어느 쪽이든 **고른 이유**를 적는다.

---

# Related Specs

- `infra/demo/aws/terraform/variables.tf` — `monthly_budget_minutes` · `instance_type`
- `infra/demo/aws/terraform/lambda/handler.py` — `BUDGET_MINUTES`(56) · `_budget_exhausted`(274-275) · `_usage`(249-267)
- `infra/demo/aws/terraform/main.tf:340` — `MONTHLY_BUDGET_MINUTES` 주입
- `infra/demo/aws/README.md` — § 비용(197) · § 메모리(211)
- `tasks/done/TASK-MONO-552-demo-host-wedges-during-first-boot-seed.md` — `m6i` 를 반증하고 `r6i` 로 올린 티켓. ③ 의 근거
- `docs/adr/ADR-MONO-068-where-the-demo-backend-resolver-lives.md` — 예산 모델 인용

# Related Contracts

- 없음 — 계약 변경이 아니다(인프라 파라미터 + 문서).

---

# Edge Cases

- **tfvars 에 `monthly_budget_minutes` 가 생겼다** → default 를 고쳐도 무효. AC-0 이 막는다.
- **plan 에 인스턴스 교체가 뜬다** → 🔴 STOP. 예산 변경이 아니라 **다른 일**이 섞인 것이다.
- **apply 는 성공했는데 `/status` 가 600 을 준다** → Lambda 가 옛 환경변수를 들고 있는
  것이다(콜드 스타트 전). 🔵 몇 초 뒤 다시 읽어 보고, 계속 600 이면 **apply 가 Lambda 를
  실제로 갱신했는지** 부터 본다. 🔴 *"적용됐겠지"* 로 닫지 마라.
- **EBS 를 재려다 못 쟀다** → ⚪ 로 남기고 **컴퓨트 축만** 단언한다. AC-2 ①.
- **요금이 그 사이 바뀌었다** → 이 티켓의 표를 상속하지 말고 **AC-0 에서 다시 읽은 값**을 쓴다.

# Failure Scenarios

- **F1 — 값만 바꾸고 주석을 둔다.** 🔴 그러면 **주석이 새 값을 더 크게 틀리게 설명한다**
  (600 에서 $1.36 틀리던 오차가 1200 에서 $2.72 가 된다). → AC-2 가 같은 커밋을 강제한다.
- **F2 — `.tf` 가 바뀐 것을 판정으로 쓴다.** 선언 ≠ 런타임. → AC-4 가 `/status` 를 읽는다.
- **F3 — plan 을 건너뛰고 apply 한다.** 🔴 이 스택에서 apply 는 **인스턴스를 교체할 수 있다.**
  → AC-3 이 plan 을 선행시키고 `destroy != 0` 이면 STOP.
- **F4 — 「월 $N 미만」을 못 잰 EBS 로 다시 쓴다.** 🔴 그것이 이 티켓이 고치려는 결함과
  **같은 종류**다(측정 안 한 값으로 부등호를 만드는 것). → AC-2 ① 이 ⚪ 를 강제한다.
- **F5 — 🔴 이미 일어났다. `m6i` 가 셋뿐이라고 믿었다.** 「셋」은 grep 의 결과이지 성질이
  아니었고, 그 grep 의 **모집단이 `infra/demo/aws/` 로 좁았다** ⇒ `infra/demo/README.md` 의
  ④⑤ 를 통째로 놓쳤다. 🔵 AC-2 의 마지막 칸이 구현 중에 잡았다.
  → 그래서 그 칸은 이제 **«루트에서 돌려라» 와 «0건이 아니라 남은 것과 이유를 적어라»** 를
  명시한다. 🔴 «전수» 라고 쓴 숫자를 볼 때마다 **어느 디렉터리에서 셌는지**를 물어라.
- **F6 — 예산이 올라간 것을 「리셋됐다」로 읽는다.** 쓴 543 분은 그대로 있고 **천장만**
  올라간다. → AC-4 의 ⚪ 칸이 그 구분을 적는다.

---

# Test Requirements

- 🔵 **코드 테스트 없음** — terraform 변수 + 문서다.
- 판정은 **런타임 관측**이다: `GET /status` 의 `budget_minutes`(AC-4).
- 🔴 `terraform plan` 출력이 **AC-3 의 산출물**이고 PR 에 붙는다.

---

# 분석 / 구현 권장

분석=**Opus 5** / 구현 권장=**Sonnet** — 값 한 줄 + 문서 셋이고 판정 기준이 위에 다 박혀 있다.
🔴 단 **AC-4(apply)는 소유자 승인 축**이고, **AC-3 의 plan 이 인스턴스 교체를 보이면
그 순간 Opus 축**이다(예산 작업이 아니라 다른 일이 섞인 것이므로).

---

# 🟢 실측 (2026-09-11 UTC · `in-progress`)

## AC-0 — 착수 게이트, 세 칸 전부 통과

| 칸 | 결과 |
|---|---|
| 요금 재측정 | 🟢 `r6i.2xlarge` = **$0.6080/시간**, 발행일 `2026-09-10`. 🔴 `curl --compressed` 필요 — 안 주면 gzip 바이너리가 나오고 **grep 이 조용히 0건**을 낸다(실제로 한 번 밟았다) |
| 배포 타입 **양쪽** 확인 | 🟢 `aws ec2 describe-instances` = `i-0863ba8d8faf52c63` · `r6i.2xlarge` · `stopped` **그리고** `terraform.tfvars:150` = `r6i.2xlarge`. 둘이 일치 ⇒ 드리프트 없음 |
| tfvars 에 예산 키가 생겼나 | 🟢 **없다** ⇒ `variables.tf` 의 default 가 유효하고 AC-1 이 옳은 파일을 가리킨다 |

🔵 착수 시점 `/status` = `{"state":"stopped","used_minutes":543,"budget_minutes":600}`.

## AC-1 · AC-2 — 값과 근거를 같은 커밋에

- `monthly_budget_minutes` default **600 → 1200**.
- `description` 의 *"매월 1일 리셋"* 은 **참이라 유지**하되 **`UTC`** 를 명시했다 —
  `_usage()` 가 `_month()` = `time.strftime("%Y-%m", time.gmtime(...))` 로 판정하므로
  KST 자정이 아니라 **UTC 달력월**이다. 🔵 이 호스트가 KST 라 그 구분이 실제로 갈린다.
- 낡은 자리 **다섯**을 전부 고쳤다(§ Background 표).

## 🔴🔴 AC-2 의 마지막 칸이 내 「셋」을 잡았다 — 실제로는 다섯이었다

첫 grep 을 **`infra/demo/aws/` 로만** 돌려서 `infra/demo/README.md` 의 ④⑤ 를 통째로 놓쳤다.
루트에서 다시 돌리니 나왔다. 🔵 **그 둘이 ①②③ 보다 나쁘다** — 낡은 수치가 아니라
**반대 방향을 주장**한다:

- ④ `demo/README.md:54` 는 *"`variables.tf` 의 기본값이 **권위**다"* 라고 쓰면서
  **그 권위와 반대되는 값**(`m6i.2xlarge`)을 적고, *"64GB 는 … **현재 구성이 아니다**"* 로
  끝난다 — 지금은 그것이 **바로 현재 구성**이다.
- ⑤ `demo/README.md:136` 은 **정정이 옳은 것을 뒤집은 기록**이다:
  ① 원판 *"64GB 가 필요하다"* → ② `TASK-MONO-366` 이 *"m6i(32GB)로 뜬다, 64GB 는 측정 전
  추정"* 으로 번복 → ③ `TASK-MONO-552` 가 라이브에서 32GB 를 기각해 **①을 복원**.
  🔵 366 이 틀린 이유는 산술이 아니라 **술어**다 — *"뜬다"* 와 *"충분하다"* 를 한 문장으로
  썼다. 32GB 에서 스택은 **떴고, 동시에 thrashing 하고 있었다**(pgmajfault +436k/5분).
  ⇒ 정정하면서 **이력 셋을 그대로 남겼다**. 지금 값만 적으면 다음 사람이 366 을 다시 읽고
  또 뒤집는다.

🔵 **남긴 `m6i` 둘과 이유**(0건이 아니다): `variables.tf:34` 의 `m6i.4xlarge` 비교는
r6i 를 고른 **근거를 설명하는 정당한 문장**이고, `projects/fan-platform/tasks/done/…-014`
는 **`done/` 이라 frozen** 이다.

## AC-3 — plan 을 먼저 돌렸다 🟢

🔴 **worktree 에서 돌리면 안 됐다.** backend 블록이 없어 **로컬 state** 이고, 그 state 는
gitignore 돼 **원래 체크아웃에만** 있다(`terraform.tfstate`, 09-10 20:30). worktree 에서
돌렸으면 state 가 비어 **「전부 생성」이라는 오답**이 나왔을 것이다.
⇒ 메인 체크아웃에서 **파일을 안 건드리고** `-var monthly_budget_minutes=1200` 으로 주입해
같은 변경을 쟀다(`-lock=false`, plan 은 읽기 전용).

```
Plan: 0 to add, 1 to change, 0 to destroy.

  # aws_lambda_function.control will be updated in-place
      ~ environment {
          ~ variables = {
              ~ "MONTHLY_BUDGET_MINUTES"   = "600" -> "1200"
                # (11 unchanged elements hidden)
```

- **`destroy` = 0** ⇒ AC-3 의 STOP 조건 미발동.
- 인스턴스·AMI·볼륨이 plan 에 **나오지 않는다** ⇒ 「예산 한 줄」이 실제로 한 줄이다.
- 🔵 `data.external.ami_bundle_capability` 가 저장소에서 값을 유도하는데도 변경이 안 떴다
  ⇒ 그 유도 결과가 state 와 같다는 뜻이다.

## ⚪ AC-4 — **열려 있다. 소유자 승인 축.**

apply 는 돌리지 않았다. 🔴 *"1200 로 해줘"* 는 **값의 결정**으로 받았고,
`terraform apply` 실행 승인으로는 쓰지 않았다(`CLAUDE.md` § Git 규율: 일반적 진행 지시는
apply 를 승인하지 않는다). 위 plan 을 근거로 소유자에게 올린다.

🔴 **apply 뒤에 반드시 런타임으로 확인할 것** — `.tf` 가 바뀐 것은 판정이 아니다:
`GET /status` 의 `budget_minutes` == **1200**, 그리고 같은 응답의 `used_minutes` 가
**543 그대로**인지(천장만 올라갔지 리셋이 아니다). 🔵 그러면 그 자리서 **657분**이 생긴다.

## ⚪ AC-5 — 판단만 적고 가드는 안 만들었다

- 🟢 수치에 **출처와 발행일**을 같이 박았다(엔드포인트 URL + `2026-09-10` + gzip 주의).
  없었으면 이번처럼 **틀린 줄도 모르고** 지나간다.
- ⚪ **가드는 만들지 않기로 했다.** 근거: 「배포된 `instance_type` 과 주석의 타입이 일치하는가」
  는 grep 으로 물 수 있지만, **요금 숫자는 외부 API 라 가드가 네트워크에 의존**하게 되고
  그러면 **DNS 가 죽으면 rc=0 으로 초록**이 된다(이 저장소가 이름 붙인 함정).
  🔴 타입 일치만 무는 가드는 만들 수 있으나, **그 가드가 이번 결함을 못 잡았을 것**이다 —
  ④⑤ 는 타입 문자열이 아니라 **주장의 방향**이 틀린 것이었다. ⇒ 비용 대비 효과가 낮다고
  판단했고, 대신 **출처 표기**로 다음 사람이 낡음을 판정할 수 있게 했다.

---

# 🟢 AC-4 닫힘 — 적용했고 **런타임으로** 확인했다 (2026-09-11 07:01Z)

## 승인

소유자 승인 **「적용해」** (2026-09-11). 🔵 승인을 받기 전에 **대상을 명시해서 올렸다** —
*"데모 컨트롤 플레인 Lambda(`aws_lambda_function.control`) 환경변수
`MONTHLY_BUDGET_MINUTES` 600 → 1200 · 변경 리소스 1개 · destroy 0 · 인스턴스 무관"*.
🔴 그 앞의 *"1200 로 해줘"* 는 **값의 결정**이었고 실행 승인으로 쓰지 않았다.

## 🔵 순서를 «머지 → 적용» 으로 잡았다 (드리프트 창을 안 만들려고)

`-var` 로 주입해 먼저 적용할 수도 있었지만 그러면 **메인 체크아웃의 `default` 는 아직 600**
이라, 그 사이 누가 `-var` 없이 apply 하면 **조용히 600 으로 되돌아간다.**
⇒ PR #3763 을 머지하고 pull 해서 `default = 1200` 을 확인한 뒤, **`-var` 없이** 적용했다.
🔵 이제 **커밋된 코드와 인프라가 같은 값**이라 누가 언제 apply 해도 되돌아가지 않는다.

```
Apply complete! Resources: 0 added, 1 changed, 0 destroyed.
  # aws_lambda_function.control will be updated in-place
aws_lambda_function.control: Modifications complete after 6s [id=portfolio-demo-control]
```

🟢 plan 이 예고한 것과 **정확히 같다**(1 changed · 0 destroyed).

## 판정 — 🔴 `.tf` 가 아니라 런타임. 술어 **둘**로 갈랐다

| 술어 | 결과 |
|---|---|
| `GET /status` | `{"state":"stopped","ip":null,"used_minutes":543,"budget_minutes":1200}` 🟢 |
| `aws lambda get-function-configuration` | `budget=1200` · `idle=20` · `max=180` 🟢 |

🔵 **둘로 잰 이유**: `/status` 만 보면 «Lambda 가 갱신됐나» 와 «응답이 캐시됐나» 를 못 가른다.
설정을 직접 읽으면 **배포된 실물**이 나온다. 둘이 일치하므로 갈릴 여지가 없다.

🔵 **`idle` · `max` 가 불변**인 것도 같이 확인했다 — 이것이 «환경변수 하나만 바뀌었다» 의
대조군이다. 하나만 읽었으면 «다른 것도 같이 바뀌었나» 를 못 물었다.

## 🔵 리셋이 아니라 천장이 올라갔다 — 그 구분을 적어 둔다

`used_minutes` 는 **543 그대로**다. `_usage()` 는 당월 누적 초를 들고 `_budget_exhausted()`
가 상한과 **비교만** 하므로, 이미 쓴 543 분은 사라지지 않는다.

```
남은 분 = 1200 - 543 = 657분   (적용 전: 600 - 543 = 57분)
```

🔴 **「예산이 리셋됐다」로 읽으면 다음 사람이 월초로 착각한다.** 실제 리셋은 `_month()` 가
UTC 달력월로 판정하므로 **2026-10-01 00:00Z** 이고, 그때 `used_minutes` 가 0 이 된다.

## AC 현황 — 열린 칸 0

| AC | 상태 |
|---|---|
| AC-0 착수 게이트 | 🟢 세 칸 통과(요금 재측정 · 타입 양쪽 확인 · tfvars 키 부재) |
| AC-1 값 1200 | 🟢 |
| AC-2 근거 다섯 자리 | 🟢 (셋으로 셌다가 **다섯**으로 정정) |
| AC-3 plan 선행 | 🟢 `0 add / 1 change / 0 destroy` |
| AC-4 적용 + 런타임 | 🟢 **이 절** |
| AC-5 낡음 방지 | 🟢 출처·발행일·gzip 주의 박음 / ⚪ **가드는 안 만들기로 판단**(근거는 앞 절) |

🔵 **이 티켓이 임시로 들고 있던 남의 의무는 없다** — 넘겨받은 것도, 다른 티켓에 얹어 둔
것도 없다. EBS 미측정(⚪)은 **이 티켓 안에서 ⚪ 로 닫힌 칸**이지 남에게 넘긴 의무가 아니다
(AC-2 가 *"못 쟀으면 컴퓨트 축만 단언하라"* 를 닫힘의 형태로 요구했고 그대로 했다).

---

# 🟢 Close chore (2026-09-11 UTC) — 네 축 통과, 그리고 **(d)가 실제로 한 건 잡았다**

## 네 축

| 축 | PR #3763 (구현) | PR #3764 (review 이동) |
|---|---|---|
| (a) `state` / `mergedAt` | `MERGED` · `06:59:07Z` | `MERGED` · `07:07:17Z` |
| (b) squash 가 `origin/main` 의 **조상** | `294bb6e40` 🟢 | `80e17496a` 🟢 |
| (c) 실패 0 · required 4종 전부 SUCCESS | 🟢 | 🟢 |
| (d) **AC 절을 열어서 읽음** | 🔴 **아래를 보라** | — |

## 🔴🔴 (d) 가 잡은 것 — 체크박스 **21칸이 전부 열려 있었다**

본문의 § 실측과 § AC-4 절에는 **모든 작업이 기록돼 있었지만**, `# Acceptance Criteria`
절의 체크박스는 **하나도 안 켜져 있었다.** 이대로 `done/` 으로 옮겼으면 **「열린 AC 21」인
파일이 frozen** 되고, `done/` 은 다시 안 읽히므로 영구히 그 상태로 남았을 것이다.

🔵 **이것이 `CLAUDE.md` 가 (d)를 «INDEX 행이 아니라 AC 절을 열어서 읽어라» 로 적은 이유의
실례다.** INDEX 행에는 «열린 AC 0» 이라고 적혀 있었고 — 그 문장은 **내가 쓴 것**이라
자기 자신을 검증하지 못한다. 파일을 열어야 갈린다.

🔴 그리고 **✅-grep 으로도 못 잡는다**: 박스가 전부 `[ ]` 였으므로 「✅ 가 있나」를 물으면
«없다» 가 나오는데, 그 «없다» 는 **「안 했다」와 「했는데 안 켰다」를 구별하지 않는다.**
⇒ 21칸을 **하나씩 § 실측의 기록과 대조**해서 전부 실제로 수행됐음을 확인한 뒤 켰다.

## 21칸 대조 요약

| AC | 칸 | 근거가 있는 곳 |
|---|---|---|
| AC-0 | 3 | § 실측 — 요금 재측정($0.6080, 발행 `2026-09-10`) · 타입 **양쪽**(`describe-instances` + `tfvars:150`) · tfvars 에 예산 키 **없음** |
| AC-1 | 2 | `default = 1200` · `description` 을 **UTC 달력월**로 명시(🔵 `_month()` 가 `gmtime` 이라 KST 호스트에서 갈린다) |
| AC-2 | 7 | 낡은 자리 **다섯**(①~⑤) 한 커밋 · 🔴 루트에서 재-grep 해 **셋→다섯** 으로 정정 · 남긴 둘과 이유 기록 |
| AC-3 | 3 | `plan` 선행 · `0 add / 1 change / 0 destroy` · 인스턴스·AMI·볼륨 **미등장** |
| AC-4 | 4 | 소유자 **「적용해」** · `Apply complete` · 🔴 런타임 술어 **둘**(`/status` + `lambda get-function-configuration`) · 소급 효과(리셋 아님) 기록 |
| AC-5 | 2 | 출처 URL·발행일·gzip 주의를 주석에 박음 · ⚪ **가드는 안 만들기로 판단**하고 근거 기록 |

## 🔵 임시로 들고 있던 남의 의무

**없다.** 넘겨받은 것도, 다른 티켓에 얹어 둔 것도 없다. ⚪ EBS 미측정은 **이 티켓 안에서
⚪ 로 닫힌 칸**이지 남에게 넘긴 의무가 아니다(AC-2 ①이 *"못 쟀으면 컴퓨트 축만 단언하라"*
를 닫힘의 형태로 요구했고 그대로 했다).

🔵 **후속으로 이 결정이 실제로 쓰였다**: 같은 날 데모 창(`TASK-MONO-633`·`645`·`648`·
`654`·`660`)이 **60분**을 썼고, 옛 상한 600 이었으면 남은 분이 **57분**이라 그 창을 열 수
없었다. 창 종료 시점 예산 **603/1200**.

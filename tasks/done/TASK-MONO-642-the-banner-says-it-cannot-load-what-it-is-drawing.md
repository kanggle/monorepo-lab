# Task ID

TASK-MONO-642

# Title

🔴 배너가 «데이터를 불러올 수 없습니다» 라고 하면서 **그 데이터를 그리고 있다** — `ADR-MONO-070` 이전 문구가 남았다

# Status

done

# Owner

monorepo

# Task Tags

- demo
- ux
- copy

---

# Goal

스토어와 팬의 `DemoBackendNotice` 배너가 이렇게 말한다:

> 데모 서버가 꺼져 있어 **상품 데이터를 불러올 수 없습니다.**
> 데모 서버가 꺼져 있어 **피드와 아티스트 데이터를 불러올 수 없습니다.**

**그런데 바로 아래에 그 데이터가 그려져 있다.** 상품 24개가 보이고 아티스트 여섯 명이
사진과 함께 보인다. 방문자에게 그 화면은 «에러가 났는데 왜인지 뭔가는 보인다» 로 읽힌다.

🔵 **거짓은 아니다.** 백엔드는 실제로 꺼져 있고, 화면에도 「샘플 데이터 (아직 발행 전)」이
적혀 있다. 배너가 말하려던 것은 «라이브 데이터가 아니라 샘플입니다» 다. 문제는 그 뜻이
**«불러올 수 없습니다»** 라는 표현에 안 담긴다는 것이다.

---

# Context — 실측 (2026-09-08 UTC, `main` = `34e6a0ccb`)

`TASK-MONO-639` 가 캐러셀용 캡처를 찍고 **그 이미지를 눈으로 확인하다** 발견했다. 그래서
이 문구는 이제 **포트폴리오 첫 화면의 캐러셀에 박혀 있다** — 라이브에서 확인했다
(`hubwang.com` 의 카드 8장 전부 `naturalWidth=1280` 으로 그려진다).

## 세 앱의 같은 위젯, 다른 정확도

| 앱 | 문구 | 정확한가 |
|---|---|---|
| `console-web` | 「로그인과 **운영 데이터**를 사용할 수 없습니다」 | ✅ **정확하다** — 그쪽은 실제로 못 쓴다 |
| `web-store` | 「**상품 데이터**를 불러올 수 없습니다」 | 🔴 상품 24개가 그려지고 있다 |
| `fan-platform-web` | 「**피드와 아티스트 데이터**를 불러올 수 없습니다」 | 🔴 피드와 아티스트 여섯이 그려지고 있다 |

⇒ 콘솔만 맞다. 스토어·팬 문구는 **`ADR-MONO-070`(백엔드 없이 공개 열람) 이전 표현**이
그대로 남은 드리프트다. 그 ADR 이 «데이터가 없다» 를 «샘플이 있다» 로 바꿨는데 배너는
안 바뀌었다.

## 파일 (2026-09-08 UTC 재확인 — 세 문구 모두 위 표 그대로)

```
projects/ecommerce-microservices-platform/apps/web-store/src/widgets/demo-notice/DemoBackendNotice.tsx:40
projects/fan-platform/web/fan-platform-web/src/widgets/demo-notice/DemoBackendNotice.tsx:53
projects/platform-console/apps/console-web/src/widgets/demo-notice/DemoBackendNotice.tsx:63   ← 이쪽은 정확
```

각 앱에 그 문구를 무는 시험도 있다(`__tests__/DemoBackendNotice.test.tsx`) — 🔴 **문구를
바꾸면 그 시험이 함께 바뀌어야 한다. 형제를 먼저 grep 하라.**

---

# 선행 / 의존

🔴 **`TASK-MONO-643` 이 먼저다.** 이 티켓의 `AC-3`(캡처 재생성)은
`node infra/demo/aws/site/capture-shots.mjs` 를 돌리는데, **그 명령이 지금 `rc=1` 로 죽는다**
(643 이 그 결함이다). 643 없이 AC-3 을 시도하면 «스크립트가 고장났다» 를 이 티켓에서
다시 발견하게 된다.

---

# Scope

## 포함

- `web-store` · `fan-platform-web` 의 `DemoBackendNotice.tsx` 문구
- 그 두 앱의 `__tests__/DemoBackendNotice.test.tsx`
- `TASK-MONO-639` 의 캡처 재생성(`capture-shots.mjs` 한 번) — 643 이 끝난 뒤

## 제외

- 🔴 `console-web` 의 문구 — **정확하므로 안 건드린다.** 세 앱을 «통일» 하려고 콘솔까지
  바꾸면 맞는 문장을 틀리게 만든다. 세 앱의 사정이 실제로 다르다.
- 배너가 **언제 뜨는지**(`resolveDemoBackendState`) — 그 판정은 옳다(백엔드는 정말 꺼져
  있다). 바꿀 것은 **무엇이라고 말하는가** 뿐이다.
- `/login` 을 이 위젯이 안 덮는 것 — 그 자리는 의도적으로 비어 있고 근거가 위젯 머리말에
  적혀 있다(`TASK-MONO-574` 소관).

---

# Acceptance Criteria

## AC-0 — 착수 전 재측정

- [x] 세 앱의 배너 문구를 **다시 읽고** 위 표를 갱신한다. 그 사이 누가 고쳤으면 재현 조건이
      사라진다 — 그때는 «문구가 무엇인가» 가 아니라 **«화면이 그리는 것과 배너가 말하는
      것이 맞는가»** 로 판정하라.
- [x] 🔴 라이브에서 **배너와 데이터가 함께 있는지** 확인한다(배너가 뜨는데 데이터도 보인다는
      것이 이 티켓의 재현 조건이다). 데모가 켜져 있으면 배너가 아예 안 뜨므로 재현이 안 된다.
- [x] 🔴 `TASK-MONO-643` 이 머지됐는지 확인한다. 안 됐으면 AC-3 은 못 한다 — **STOP 하고
      643 부터 처리하라.**

## AC-1 — 배너가 화면과 어긋나지 않는다

- [x] 스토어·팬의 문구가 **지금 보이는 것**을 말한다. 방향: «불러올 수 없다» 가 아니라
      «지금 보이는 것은 샘플이고, 실시간 기능은 서버를 켜야 한다».
- [x] 🔴 «실시간 기능이 잠겨 있다» 는 사실은 **남긴다.** 그것까지 지우면 방문자가 장바구니·
      로그인이 왜 안 되는지 모른다 — 배너를 부드럽게 만드는 것이 목적이 아니다.
- [x] 🔴 **콘솔 문구는 안 바꾼다**(정확하다). 세 앱을 통일하지 않는 이유를 적는다.
- [x] 론처 카드의 문구(`TASK-MONO-637` 이 넣은 「실시간 기능을 먼저 기동해야 열립니다」)와
      **같은 것을 말하는지** 대조한다. 두 화면이 다른 말을 하면 방문자가 어느 쪽을 믿을지 모른다.

## AC-2 — 형제 시험

- [x] 두 앱의 `DemoBackendNotice.test.tsx` 가 함께 바뀐다.
- [x] 🔴 그 시험이 **문구 리터럴을 고정**하고 있다. 새 문구로 갈아 끼우기만 하면 다음에 또
      깨진다 — **성질로 바꿀지, 리터럴을 유지할지 판단하고 근거를 적어라.**
      🔵 이 경우는 리터럴이 정당할 수 있다(배너의 **문장 자체**가 계약이다). 판단하고 적으면 된다.
- [x] 콘솔 시험은 **안 건드린다**(그 문구가 안 바뀌므로).
- [x] 🔴 이 호스트의 측정 제약을 지켜라: 세 스위트를 동시에 돌리면 단독 527ms 짜리가 5초
      타임아웃에 걸린다. **동시성을 제한해서** 판정을 얻어라(타임아웃 상향 금지).
      console 은 `--maxWorkers=2 --minWorkers=1` 을 **짝으로** 줘야 한다.

## AC-3 — 캡처 갱신

- [x] `TASK-MONO-639` 의 캡처 여덟 장 중 배너가 보이는 것들을 **다시 찍는다**
      (`node infra/demo/aws/site/capture-shots.mjs`).
- [x] 🔵 `(z37)` 가 참조↔파일과 manifest 를 대조하므로, 재생성 후 그 칸이 초록인지 확인한다.
- [x] 다시 찍은 이미지를 **눈으로 본다** — 배너가 새 문구인지. 파일이 바뀌었다는 것과 화면이
      바뀌었다는 것은 다른 축이다.
- [x] 🔴 재생성 뒤 **합계 용량**을 다시 재고 `TASK-MONO-639` 가 남긴 값(608KB)과 대조한다.
      크게 늘었으면 형식·품질이 바뀐 것이다.

---

# Related Specs / Contracts

- [`ADR-MONO-070`](../../docs/adr/ADR-MONO-070-public-browsing-without-the-backend.md) — 이 배너 문구가 그 결정 **이전**의 세계를 말하고 있다
- `TASK-MONO-639` — 이 결함을 드러낸 티켓(캡처를 눈으로 보다 발견)
- `TASK-MONO-643` — **선행**. 캡처 스크립트가 지금 안 돈다
- `TASK-MONO-637` — 론처 카드의 「실시간 기능을 먼저 기동해야 열립니다」. 같은 사실을 말하는 두 화면이다
- `TASK-MONO-574` — `/login` 축(이 티켓 밖)

---

# Edge Cases

| 상황 | 기대 |
|---|---|
| 데모가 켜져 있음 | 배너가 아예 안 뜬다(지금 동작 유지) |
| 저장본이 비어 데이터가 정말 없음 | 그때는 「불러올 수 없다」가 **맞다** — 그 경우의 문구도 있는지 판단하라 |
| 콘솔 | 문구 그대로. 그쪽은 정말 못 쓴다 |

---

# Failure Scenarios

1. **세 앱을 통일한다** → 맞는 문장(콘솔)을 틀리게 만든다. 세 앱의 사정이 실제로 다르다.
2. **«실시간 기능이 잠겼다» 를 지운다** → 방문자가 장바구니·로그인이 왜 안 되는지 모른다.
   배너를 부드럽게 만드는 것이 목적이 아니다.
3. **문구만 바꾸고 캡처를 안 다시 찍는다** → 포트폴리오 첫 화면에는 옛 문구가 그대로 남는다.
4. **시험의 리터럴만 갈아 끼운다** → 다음에 문구를 다듬는 날 또 깨진다. 판단하고 적어라.
5. **643 을 건너뛰고 AC-3 부터 한다** → 스크립트가 `rc=1` 로 죽고, 그 원인을 이 티켓에서
   다시 조사하게 된다.

---

# 분석 / 구현 권장

분석=Opus 5 / 구현 권장=**Sonnet** (문구 둘 + 시험 둘 + 캡처 재생성. 다만 «무엇까지 남길
것인가» 는 판단이므로 막히면 Opus).

---

# Verification — 구현 세션 (2026-09-08 UTC)

## AC-0 — 착수 전 재측정

| 축 | 값 |
|---|---|
| 세 앱 배너 문구 | 표 그대로(변동 없음) |
| 🔴 `TASK-MONO-643` 머지 (AC-3 의 선행) | ✅ `d754903b9` |
| 🔴 라이브 재현 조건 (배너 + 데이터 **동시**) | ✅ store 배너+상품 20 · fan 배너+피드 10 |

🔵 **AC-0 이 예상 못 한 것을 하나 줬다**: `console` 은 `/demo/ecommerce` 에 **배너가 아예
안 뜬다**(표 18행만 그린다). 티켓은 「콘솔 문구는 정확하다」고만 적었는데, 실은 그 페이지에서
**렌더되지도 않는다.** 어느 쪽이든 결론은 같다 — 안 건드린다.

## AC-1 — 배너가 화면과 어긋나지 않는다

| 앱 | 새 문구 |
|---|---|
| store | 지금 보이는 상품은 **샘플 데이터**입니다. 데모 서버가 꺼져 있어 장바구니·로그인 같은 **실시간 기능은 잠겨 있습니다.** 데모 시작 페이지에서 … |
| fan | 지금 보이는 피드와 아티스트는 **샘플 데이터**입니다. 데모 서버가 꺼져 있어 로그인·멤버십 같은 **실시간 기능은 잠겨 있습니다.** … |
| console | **안 건드렸다** |

- 🔴 «실시간 기능이 잠겼다» 는 사실을 **남겼다.** 지우면 방문자가 장바구니·로그인이 왜 안
  되는지 모른다 — 배너를 부드럽게 만드는 것이 목적이 아니다.
- 🔵 론처 카드(`TASK-MONO-637`)의 「실시간 기능을 먼저 기동해야 열립니다」와 **용어를
  일치**시켰다. 두 화면이 다른 말을 하면 방문자가 어느 쪽을 믿을지 모른다.
- 🔴 **콘솔을 안 바꾼 이유**: 그 문구는 정확하고(그쪽은 실제로 못 쓴다), 그 페이지에선
  렌더되지도 않는다. 세 앱을 「통일」하면 **맞는 문장을 틀리게 만든다.**

## AC-2 — 형제 시험 (판단과 근거)

🔴 **기존 두 단언은 이 티켓의 결함을 못 잡았다.** `'데모 서버가 꺼져 있어'` 와 `'데모 시작'` 은
**옛 문구에도 그대로** 있었다 — 결함이 있든 없든 통과한다.

**판단: 리터럴 고정은 유지하고, 덮는 칸을 더한다.** 배너의 **문장 자체가 방문자와의 계약**
이므로 리터럴은 정당하다. 다만 지금 고정된 조각이 이 티켓이 고치는 부분을 안 덮는다.

| 칸 | 무엇 |
|---|---|
| (a) | 「샘플」이 있다 |
| **(b)** | 🔴 「불러올 수 없」이 **없다** — **bite** |
| (c) | 「실시간 기능」이 있다 (잠긴 사실을 안 지웠는가) |

🔵 (a)(c) 만으로는 옛 문구를 **덧붙여도** 통과한다.

**실측**
- fan vitest 단독(`--maxWorkers=2 --minWorkers=1`): **7 passed**
- 🔴 **bite**: 옛 문구를 되살리니 **정확히 1칸**만 실패(7 중 1), 나머지 6은 통과
- 🔴 store vitest 는 이 호스트에서 **기동 불가**(Node 24.14 + vitest 4,
  `ERR_PACKAGE_IMPORT_NOT_DEFINED` — 재측정함)
- 🔵 **CI 가 두 칸을 실제로 돌렸는지 확인했다**: 베이스라인 `it(` 수 store **4** · fan **6**
  → CI 로그 **5 tests** · **7 tests**. 각각 `+1`. 🔴 잡이 초록인 것과 내 단언이 실행된 것은
  다른 축이라 베이스라인과 대조했다.
- 콘솔 시험은 **안 건드렸다**(그 문구가 안 바뀌므로).

## AC-3 — 캡처 갱신

`node infra/demo/aws/site/capture-shots.mjs` — **8/8 성공**, `manifest` 기록, 전부 `status 200`.

🔵 **643 이 고친 스크립트를 실제로 썼다.** 이 worktree 에는 설치본이 없어 `--from` 을 줬고,
스크립트가 해석된 경로를 출력했다(`[capture] Playwright: …`).

**🔵 다섯 장만 바뀌었고 console 세 장은 바이트 동일하다.**

| 바뀜 | 안 바뀜 |
|---|---|
| `store-1-home` · `store-2-products` · `fan-1-feed` · `fan-2-artists` · `fan-3-membership` | `console-1-overview` · `console-2-ecommerce` · `console-3-wms` |

🔴 이것이 **범위가 맞았다는 증거**다 — 콘솔을 안 건드렸으니 콘솔 캡처가 안 바뀌어야 한다.
바뀌었다면 의도하지 않은 변경이 섞인 것이다.

**눈으로 봤다**(이미지를 열어서):
- `store-2-products` — 새 배너가 그려져 있고 상품 8종이 그대로 보인다
- `fan-2-artists` — 새 배너 + 아티스트 **6명이 사진과 함께**

**용량**: 608 KB → **616 KB** (+8 KB, +1.3%). 새 문구가 한 줄 길어진 만큼이고,
`TASK-MONO-639` 가 정한 형식·품질(JPEG q=82)은 그대로다.

## 실행한 게이트

| 게이트 | 결과 |
|---|---|
| fan vitest (단독) | ✅ 7 passed |
| fan vitest bite (옛 문구 복원) | ✅ 1 failed / 6 passed — **정확히 새 칸만** |
| CI `Frontend unit tests` | ✅ store 5 tests · fan 7 tests (베이스라인 4·6 대비 각 +1) |
| CI 전체 (PR #3702) | ✅ 60 checks · failing 0 |
| `capture-shots.mjs` | ✅ 8/8 · manifest 기록 |
| 전체 wrapper (정적 전량) | ✅ rc=0 · **86 ok** · FAIL 0 · `정적 검증 PASS` 종료 · `(z37)` 통과 |

🔴 **미측정**: 새 캡처가 `hubwang.com` 카드에 실제로 뜨는지. 이 PR 이 머지되고 Vercel 이
배포한 뒤에만 볼 수 있다. `TASK-MONO-639` 때와 같은 축이고, 그때는 8/8 이 **md5 동일**로
확인됐다.

## CORRECTION — § 미측정이었던 「라이브 캡처」를 실제로 쟀다 (2026-09-08 UTC)

이 티켓은 «새 캡처가 `hubwang.com` 카드에 실제로 뜨는지» 를 미측정으로 남겼다. 머지
(`0aa92f6a2`) 후 Vercel 배포를 확인하고 **두 축으로 갈라서** 쟀다:

| 축 | 결과 |
|---|---|
| 방문자가 받는 파일이 커밋본과 같은가 | ✅ **8/8 md5 동일** |
| 그 파일이 화면에 **그려지는가** | ✅ `.shots img` 8장 전부 `naturalWidth=1280` · `visible=true` |

🔵 에셋이 있다는 것과 캐러셀이 그린다는 것은 다른 축이다(캐러셀은 인라인 JS 가 만든다).
`TASK-MONO-639` 에서 이미 이름 붙인 갈래라 그대로 갈라서 쟀다.

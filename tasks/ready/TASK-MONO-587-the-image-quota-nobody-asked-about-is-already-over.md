# Task ID

TASK-MONO-587

# Title

🔴 **아무도 묻지 않은 축이 이미 초과돼 있다** — Vercel 이미지 변환 할당량이 `TASK-MONO-582`(단계 2)를 막는다.

# Status

ready

# Owner

monorepo

# Task Tags

- adr
- vercel
- quota
- blocker

---

# ⏳ 어떻게 발견됐나 — **묻지 않은 축이었다**

`TASK-MONO-575` 는 Vercel 무료 플랜 한도를 **네 축**으로 물었다:
일일 배포 수 · 함수 호출 수 · 함수 실행 시간 · 대역폭.

2026-08-26 소유자가 사용량 화면 **전체**를 붙여넣었고, 그 네 축은 전부 여유였다.
🔴 **그런데 목록에 그 네 축만 있는 게 아니었다.** 이미 **넘긴 축이 둘** 있었다:

| 축 | 사용 / 한도 | |
|---|---|---|
| **Image Optimization — Transformations** | **5.6K / 5K** | 🔴 **112%** |
| **Image Optimization — Cache Writes** | **320K / 100K** | 🔴 **320%** |

🔵 **일반화 — 이 티켓이 존재하는 이유다.** `575` 의 AC-0 은 *"문서 값을 적지 마라, 계정 값이
진실이다"* 를 지켰다. 그건 옳았다. **그러나 「어느 축을 볼지」는 여전히 내가 미리 골랐고,
고른 목록 밖에 초과가 있었다.** 계정을 열었을 때 **묻기로 한 것만 읽지 말고 전부 훑어야** 한다.

---

# Goal

이미지 변환 할당량이 `ADR-MONO-067` **단계 2**(web-store 를 Vercel 프로젝트로)에 어떤
제약을 거는지 **실측으로** 확정하고, 그 결과를 단계 2 의 선행 조건에 반영한다.

🔴 **이 티켓은 «올려도 되는가» 를 결정하지 않는다** — 그것은 소유자 사안이다. 사실만 세운다.

---

# Context — 실측 (2026-08-26)

## ① 지금 배포된 것들은 이 축을 **안 쓴다**

| 앱 | `next/image` 사용 파일 |
|---|---:|
| `fan-platform-web` | **0** |
| `console-web` | **0** |
| 론처 (`infra/demo/aws/site`) | **0** (정적 HTML) |
| **`web-store`** | 🔴 **5** |

⇒ **초과분은 이 저장소가 만든 것이 아니다.** 같은 Vercel 계정의 **다른 프로젝트**가 소비했다.
🔵 그래서 오늘 `hubwang.com` 과 `fan.hubwang.com` 은 **멀쩡하다**(실측: 둘 다 200).

## ② 🔴 그런데 단계 2 가 올리려는 것이 정확히 `web-store` 다

`TASK-MONO-582` 가 만들려는 Vercel 프로젝트가 `web-store` 이고, 그 앱은 **5곳에서
`next/image` 를 쓴다.** 할당량이 이미 넘은 상태에서 올리면 최적화기가 **402** 를 내고,
이 저장소는 그때 **이미지가 전부 죽는** 것을 이미 겪었다.

🔴 **그 실패는 «이미지 몇 장이 안 나온다» 가 아니라 «전부 안 나온다»** 이고,
증상만 보면 코드 결함처럼 보인다.

## ③ 🔵 `(B)` 가 쓰는 축은 전부 여유다 — 이 발견이 그것을 뒤집지 않는다

| 축 | 사용 / 한도 | 여유 |
|---|---|---|
| Function Invocations | 198K / 1M | 80% |
| Function Duration | 0 / 100 GB-Hrs | 거의 전부 |
| Fast Data Transfer | 10.06 / 100 GB | 90% |
| Edge Requests | 363K / 1M | 64% |

데모는 월 600분(**한 달의 약 1.4%**)만 켜지므로 프록시 트래픽이 이 축을 넘길 일은 없다.
⇒ **`575` AC-2 의 답: 함수·대역폭 축은 통과.** 막는 것은 **이미지 축 하나**다.

## ④ ⚠️ 절반을 넘긴 축 셋 (아직 안 넘음, 기록만)

| 축 | 사용 / 한도 |
|---|---|
| ISR Reads | 655K / 1M (**66%**) |
| Fast Origin Transfer | 5.59 / 10 GB (**56%**) |
| Fluid Active CPU | 1h 44m / 4h (**43%**) |

---

# Scope

## 포함

- **리셋 주기 확인** — 이미지 할당량이 언제 리셋되는가(월 단위인가, 롤링인가).
- **소비자 특정** — 계정의 어느 프로젝트가 5.6K 변환을 썼는가.
- **web-store 의 실제 필요량 추정** — `next/image` 5곳이 만드는 변환 건수의 **자릿수**.
- `TASK-MONO-582` 의 선행 조건에 이 축을 추가.

## 제외

- 🔴 **플랜 업그레이드 결정** → 소유자.
- 🔴 **`next/image` 를 걷어내는 리팩터링** → 그것은 별개의 결정이고, 이 티켓이 정할 것이 아니다.
- 다른 프로젝트(이 저장소 밖)의 이미지 사용 정리.

---

# Acceptance Criteria

## AC-0 — 착수 시 **다시 읽는다** (verify-then-act)

🔴 **오늘 값을 상속하지 마라.** 사용량은 리셋될 수 있고, 리셋됐다면 이 티켓의 긴급도가 바뀐다.

1. Vercel Usage 화면에서 **Image Transformations / Cache Writes** 를 다시 읽는다.
2. 🔴 **네 축만 읽지 말고 목록 전체를 훑는다** — 이 티켓이 생긴 이유가 그것이다.
3. `web-store` 의 `next/image` 사용 파일 수를 다시 센다(오늘 값 = **5**).

## AC-1 — 리셋 축을 **확정**한다

*"언제 다시 0이 되는가"* 를 화면에서 읽는다. 🔴 **추정하지 마라** — 월초인지, 가입일 기준인지,
롤링 30일인지에 따라 단계 2 의 착수 시점이 달라진다.

## AC-2 — 소비자를 특정한다

5.6K 변환을 **어느 프로젝트가** 썼는지 확인한다. 🔵 이 저장소 밖이면 이 저장소가 할 수 있는
일이 없고, 그 사실 자체가 판정이다(*"우리가 줄일 수 있는 몫이 0"*).

## AC-3 — `web-store` 의 필요량을 **자릿수로** 추정한다

🔴 **정확한 수를 맞히려 하지 마라.** 필요한 판정은 *"남은 여유 안에 들어가는가"* 뿐이고,
지금 여유는 **음수**다. 상품 이미지 수 × 렌더되는 크기 변형 수로 자릿수만 잡는다.

🔵 **대조군**: `next/image` 를 쓰지 않는 fan/console 이 오늘 변환을 **0건** 쓰고 있다는 사실이
이 축의 소비자가 `next/image` 임을 확인해 준다.

## AC-4 — `TASK-MONO-582` 에 선행 조건을 **추가**한다

582 는 `review/` 라 **동결**이다. 🔴 파일을 고치지 말고 **INDEX 행**에 적거나, 582 가
`ready/` 로 돌아올 때 반영한다. (이 저장소는 동결 규칙을 훅으로 강제한다.)

## AC-5 — 🔴 판정이 «못 올린다» 로 나와도 **단계 3·4 는 안 막힌다**

console 과 fan 은 `next/image` 를 **안 쓴다**(실측 0곳). 이 축은 **단계 2 만** 막는다.
⇒ 판정을 «ADR-067 전체가 막혔다» 로 번역하지 마라.

---

# Related Specs

- `docs/adr/ADR-MONO-067-demo-surfaces-served-from-vercel.md` — 단계 2
- `TASK-MONO-575` — 한도 실측(이 티켓의 부모). 🔴 그 티켓이 **묻지 않은 축**이 여기다.
- `TASK-MONO-582` — 막히는 대상

# Related Contracts

없음.

---

# Edge Cases

| 케이스 | 처리 |
|---|---|
| 착수 시 이미 리셋돼 있다 | 🔵 그러면 긴급도가 내려가지만 **닫지 마라** — 다음 주기에 또 넘는다. AC-1(리셋 축)과 AC-3(필요량)은 그대로 필요하다 |
| 소비자가 이 저장소 밖이다 | AC-2 대로 *"우리가 줄일 몫 0"* 을 적고 소유자 결정으로 올린다 |
| web-store 를 올려도 402 가 안 난다 | 🔴 **운이 좋았던 것이지 안전한 것이 아니다.** 캐시 히트로 변환이 안 일어났을 수 있다 — 판정은 **사용량 화면의 증가분**으로 한다 |
| 이미지가 몇 장만 깨진다 | 🔴 이 축의 실패는 «전부» 다. 일부만 깨지면 **다른 원인**이다 |

---

# Failure Scenarios

| 시나리오 | 결과 | 방지 |
|---|---|---|
| 이 티켓 없이 단계 2 착수 | web-store 의 **모든 이미지가 402**. 증상이 코드 결함처럼 보여 엉뚱한 곳을 판다 | 582 선행 조건 (AC-4) |
| 오늘 숫자를 상속해서 판정 | 리셋됐으면 **없는 문제를 막고 있게** 된다 | AC-0 |
| 이 축을 «ADR-067 전체의 블로커» 로 확대 | 단계 3·4 가 이유 없이 정지 | AC-5 |
| 다음에 또 «네 축만» 읽음 | **같은 종류의 초과를 또 놓친다** | AC-0 ② |

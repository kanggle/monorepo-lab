# Task ID

TASK-MONO-723

# Title

🔴 **주석 안의 못 푸는 Flyway 플레이스홀더가 「리포지토리 테스트가 전부 깨졌다」로 읽힌다** — 두 번 밟았고, 두 번째는 **경고문 자신**이었다

# Status

ready (2026-09-23 UTC)

# Owner

미지정

# Task Tags

- iam-platform
- flyway
- ci
- guard

---

> **분석 모델:** Opus 5 / **구현 권장:** Sonnet — 가드 하나와 그 자기시험.
> 🔴 어려운 것은 코드가 아니라 **술어의 모양**이다(플레이스홀더는 금지 대상이 아니다).

---

# 배경 — 추론이 아니라 **같은 증상을 두 번** 봤다

| 언제 | 파일 | 무엇을 썼나 | CI 가 낸 모양 |
|---|---|---|---|
| 2026-09-23 (`TASK-MONO-717`) | `V0036` | 헤더가 설정 키를 **인용**하면서 플레이스홀더 문법을 그대로 적었다 | iam 통합 전부 컨텍스트 로드에서 사망 |
| 2026-09-23 (`TASK-MONO-721`) | `V0037` | 🔴🔴 **「그 문법을 쓰지 마라」는 경고문이 그 문법으로 쓰여 있었다** | `Build & Test` · `Integration (iam B)` · `E2E (fan-platform)` **셋** |

```
org.flywaydb.core.api.FlywayException: Unable to parse statement in db/migration/V0037__...
  Caused by: FlywayException: No value provided for placeholder: ${...}
```

🔵 **플레이스홀더 이름이 literally `...` 이다** — 경고문의 생략부호가 그대로 이름이 됐다.

## 🔴 이 결함의 진짜 비용은 **증상이 원인을 안 가리킨다**는 것이다

CI 가 보여 준 것은 *"`CredentialJpaRepository` 쿼리 슬라이스 테스트 8칸 FAILED"* ·
*"`DeviceSessionJpaRepository` 4칸 FAILED"* · *"fan-platform E2E 컨테이너가 안 뜸"* 이었다.
**주석 한 줄**이 원인이라는 신호는 스택 4단계 아래 `PlaceholderReplacingReader` 뿐이다.
🔴 그리고 fan-platform E2E 는 **자기 프로젝트와 무관한** 이유로 빨개졌다(iam auth-service를
컨테이너로 띄운다) — 다음 사람은 fan 을 들여다볼 것이다.

## 🔴 로컬은 이것을 **구조적으로** 못 잡는다 (실측)

```
:auth-service:test  로컬 rc=0
  → CredentialJpaRepositoryTest   tests="8" skipped="8"
```

이 호스트(Windows + Docker 차단)에서 **Flyway 컨텍스트를 띄우는 칸은 전부 skip** 된다.
⇒ **로컬 초록은 마이그레이션을 한 번도 파싱하지 않은 초록**이다. 「다음엔 더 조심하기」는
처방이 아니다 — 조심할 수 있는 관측 자체가 로컬에 없다.

## 🔵 왜 `TASK-MONO-717` 은 가드를 안 만들었고, 왜 지금은 다른가

717 은 *"플레이스홀더는 Flyway 의 정당한 기능이므로 가드를 두지 않는다"* 로 판단했고
**그때는 관측이 1회**였다. 지금은 **2회**이고, 두 번째는 **그 함정을 아는 사람이**
(경고문을 쓰면서) 밟았다 ⇒ 「알면 피할 수 있다」가 반증됐다.
🔵 이 저장소의 규칙 그대로다 — **고쳐진 줄 알았던 면제는 두 번째 발생에서 회수한다.**

---

# Goal

**못 푸는** 플레이스홀더가 머지되는 것을 CI 가 막는다 — 플레이스홀더 **사용**은 막지 않는다.

---

# Scope

## 포함

- `scripts/` 에 가드 하나 + 자기시험, `ci.yml` 배선(경로 필터 포함).
- 🔴 `scripts/` 에 파일을 더하므로 **가드 전수 스윕**과 그 개수를 든 **산문 두 집** 갱신
  (`CLAUDE.md` · `platform/git-workflow-policy.md` — `check-ls-files-guard-count.sh` 가 지목한다).

## 제외

- 플레이스홀더 **금지**. 🔴 그것은 기능을 없애는 것이고, 실제로 값이 설정된 플레이스홀더를
  쓰는 마이그레이션이 생기면 그 가드는 꺼진다.
- 이미 적용된 마이그레이션의 바이트 수정(`TASK-MONO-722` 소관).

---

# Acceptance Criteria

## AC-0 — 🔴 술어부터 정한다 (모집단과 «설정된 값»의 출처)

- [ ] **어디를 보나**: `projects/*/apps/*/src/main/resources/db/migration*/**.sql` 전부.
      🔴 한 서비스만 보면 다음은 다른 서비스에서 난다.
- [ ] **무엇이 «설정된 값» 인가**: 각 서비스의 `application*.yml` 의
      `spring.flyway.placeholders.*` (+ Flyway 기본 제공 `flyway:*`). 🔴 **그 출처를 먼저 세고,
      0개면 「전부 미설정」이 정답**이다(0을 「검사 안 함」으로 바꾸지 마라).
- [ ] 🔴 **자기 자신을 물지 않는지 확인**하라 — 이 가드의 메시지와 자기시험이 그 문법을
      예시로 들면 **가드가 자기 문서에 걸린다**. 이 저장소가 이름 붙인 함정이고,
      `V0037` 이 정확히 그 모양으로 터졌다.

## AC-1 — 문다

- [ ] 🔴 **bite 먼저**: `V0037` 의 **첫 판**(경고문이 그 문법으로 쓰인 그 버전)을 픽스처로
      넣으면 가드가 **빨개져야** 한다. 🔵 그 입력은 지어낸 것이 아니라 **실제로 머지 직전까지
      간 파일**이다.
- [ ] 값이 **설정된** 플레이스홀더를 쓰는 마이그레이션은 **통과**해야 한다(오탐 0).
      없으면 픽스처로 하나 만들어 시험한다.
- [ ] 자기시험 칸이 붙어 있고 `rc=0` 으로 끝난다(이 저장소의 다른 가드와 같은 모양).

## AC-2 — 메시지가 **원인을 말한다**

- [ ] 실패 메시지는 파일·줄·플레이스홀더 이름을 대고, *"주석도 치환된다"* 를 말한다.
      🔴 «리포지토리 테스트가 깨졌다» 로 읽히는 것이 이 결함의 비용이므로, 메시지가
      그 거리를 없애는 것이 이 AC 의 전부다.

## AC-3 — 🔴 `scripts/` 전수 스윕

- [ ] 파일을 하나 더했으므로 **관련 있어 보이는 가드만** 고르지 말고 전부 돌린다
      (`TASK-MONO-650` 이 이 자리에서 `main` 을 빨갛게 만들었다 — 바뀐 것은 분자가 아니라 **분모**였다).
- [ ] `check-ls-files-guard-count.sh` 가 지목하는 **산문 두 집**의 숫자를 고친다.

---

# Related Specs / Contracts

- `projects/iam-platform/apps/auth-service/src/main/resources/db/migration/V0037__…` § 헤더
- `projects/iam-platform/docs/flyway-dev-seed-migrations.md` — Flyway 사고의 집
- `TASK-MONO-717` (1회차, V0036) · `TASK-MONO-721` (2회차, V0037) · `TASK-MONO-722` (V0019 바이트)

---

# Edge Cases

- **값이 설정된 플레이스홀더** — 통과해야 한다. 이것을 빨갛게 만들면 가드는 꺼진다.
- **`$` 만 있고 중괄호가 없는 경우**(예: `JSON_ARRAY_APPEND(col, '$', …)`) — 🔴 **통과해야 한다.**
  `V0037` 의 SQL 본문이 바로 그것이고, 순진한 `$` 검색은 **모든 JSON 경로 표현식**을 문다.
- **`migration-dev` 밴드** — 같은 규칙이 적용된다(같은 시퀀스에 합쳐진다).

---

# Failure Scenarios

1. 🔴🔴 **가드의 메시지나 자기시험이 그 문법을 예시로 적는다** → 가드가 **자기 문서에 걸려**
   상시 빨강이 되거나, 더 나쁘게는 **마이그레이션 디렉터리 밖이라 아무 일도 안 일어나고**
   다음 사람이 그것을 보고 「이 문법은 써도 되는구나」로 읽는다.
2. 🔴 **`$` 를 통째로 금지한다** → JSON 경로(`'$'`)를 쓰는 모든 마이그레이션이 빨개진다 →
   가드가 꺼진다.
3. 🔴 **한 서비스만 본다** → 다음 발생은 다른 프로젝트에서 나고 가드는 초록이다.
4. 🔴 **`scripts/` 스윕을 건너뛴다** → 분모가 바뀐 가드가 `main` 을 빨갛게 만든다(650 의 그 자리).

# Task ID

TASK-MONO-626

# Title

🔴 **단계는 지나갔는데 그 사실을 적은 문장들은 안 지나갔다** — `ADR-MONO-067`·`069`·`TASK-MONO-587` 의 낡은 상태 주장

# Status

ready

# Owner

monorepo

# Task Tags

- adr
- doc
- drift

---

# Goal

`ADR-MONO-067` 단계 2·3·4 가 실제로 랜딩하면서 **거짓이 된 상태 문장들**을 실제 상태에 맞춘다.
그리고 그중 하나(`TASK-MONO-587` AC-2)는 **결론이 뒤집혔을 수 있으므로** 다시 재게 한다.

# 🔴 왜 이 티켓이 생겼나 — **산문에는 게이트가 없다**

`TASK-MONO-582`(단계 2) · `585`(단계 3 앱 몫) · `618`(단계 4 억제) · `625`(단계 3 론처 몫)이
차례로 랜딩했다. **그 네 PR 중 어느 것도 자기가 거짓으로 만든 문장을 안 고쳤다.**

이 저장소가 이미 이름 붙인 실패 모드다 — *"게이트 없는 숫자는 반드시 낡는다"* ·
*"게이트 없는 「했다」는 거짓인 채 머지된다"*. 코드였다면 가드가 물었을 텐데,
**상태를 적은 것이 산문이라 아무것도 안 물었다.**

🔵 `625` 는 이 사실을 **발견해서 INDEX `## done` 행에 적어 두었다**(`done/` 은 다시 안
읽히므로). 이 티켓이 그 자리다.

# 📏 기안 시 실측 (2026-09-06 UTC) — **상속하지 마라, AC-0 이 다시 재야 한다**

## 라이브 표면 (음성 대조군 포함)

```
https://store.hubwang.com/               200
https://console.hubwang.com/             307 → /dashboards/overview
https://fan.hubwang.com/                 307 → /login?from=%2F
https://hubwang.com/                     200
https://nosuch-neg-ctl.hubwang.com/      404      ← 음성 대조군
```

🔵 음성 대조군이 404 이므로 위 200/307 은 **와일드카드가 아니라 실제로 물린 것**이다.

## 큐 상태

`TASK-MONO-582` · `585` · `586` · `618` · `625` — **전부 `tasks/done/`**.

## 낡은 자리 — **5곳을 찾았고 그중 1곳은 고치면 안 된다**

| # | 자리 | 지금 적힌 것 | 실제 | 성질 |
|---|---|---|---|---|
| ① | `ADR-MONO-067` § 결정 표 **단계 4 행** | *"남은 것은 **억제 몫**(`TASK-MONO-618`) + 해석기 몫(`TASK-MONO-586`)"* | **둘 다 `done/`** | 🔴 살아 있는 주장 |
| ② | `ADR-MONO-067` § 결정 표 **단계 3 행** | *"남은 절대 fetch 5건 + OIDC/쿠키 축. `TASK-MONO-585` 가 **BLOCKED** (선행 셋)"* | **585 `done/`**, 론처 몫도 `625` 로 랜딩. 남은 것은 **억제뿐** | 🔴 살아 있는 주장 |
| ③ | `ADR-MONO-067` § Outstanding follow-ups 3 | *"⇒ **단계 3·4 는 여전히 막혀 있고**, 막는 것은 쿠키가 아니라 이 범위 불일치다"* | D4 는 `ADR-MONO-069`(2026-09-01 `C2` ACCEPTED)가 풀었다. **같은 파일의 ① 이 이미 "🟢 D4 는 팬 표면에서 풀렸다" 라고 적고 있다** | 🔴🔴 **한 파일 안에서 자기모순** |
| ④ | `ADR-MONO-069` § AC-3 표 `console-web` 행 | *"console 은 아직 **한 번도** Vercel 에서 로그인을 시도한 적이 없다(**프로젝트 미생성**, `console.hubwang.com` **404**)"* | **307 라이브**(프로젝트 실재) | 🔴 살아 있는 주장 — **`TASK-MONO-624` 가 그 위에 선다** |
| ⑤ | `ADR-MONO-067` § History (2026-09-04) | *"`TASK-MONO-585`(단계 3)가 선행 셋으로 BLOCKED 인 것은 사실이지만…"* | 그때는 **사실이었다** | 🔵 **손대지 마라 — History 는 그때의 기록이다** |

🔴🔴 **⑤ 를 명시적으로 적는 이유**: ①②③④ 를 고치러 온 사람이 같은 문자열(`BLOCKED`)을
grep 하면 ⑤ 도 걸린다. History 를 «최신화» 하면 **왜 그 순서로 갔는지가 지워진다** —
그 절이 스스로 *"번호는 다시 매기지 않는다 … 재번호는 그 참조를 전부 조용히 틀리게 한다"*
라고 적어 둔 것과 같은 이유다.

## 🔴🔴 그리고 하나는 **결론이 뒤집혔을 수 있다** — `TASK-MONO-587` AC-2

`TASK-MONO-587`(이미지 변환 할당량, `ready/`, ⏳ 2026-09-28 게이트) § *"🟢 저장소 몫 완료
(2026-08-27 UTC) — AC-2"* 의 표:

| 앱 | `next/image` 파일 | 그 표가 적은 배포 여부 | **실제 (2026-09-06)** |
|---|---:|---|---|
| `web-store` | **5** | ❌ **Vercel 프로젝트 미생성** | 🔴 **배포됨** (`store.hubwang.com` 200 · `TASK-MONO-582` `done/`, 2026-08-29) |
| `console-web` | **0** | ❌ (단계 3 미착수) | 🔴 **배포됨** (`console.hubwang.com` 307) |

그 표의 결론은 이것이었다:

> ⇒ **지금 배포되어 트래픽을 받는 monorepo-lab 표면 중 `next/image` 를 쓰는 것은 하나도 없다.**

🔴 **`next/image` 를 5개 파일에서 쓰는 앱이 그 이틀 뒤 배포됐다.** ⇒ 그 결론은 **뒤집혔을
수 있다.** 그리고 `587` 은 *이미지 변환 할당량이 이미 초과*라는 티켓이므로, 이 결론은
**장식이 아니라 하중을 받는 문장**이다.

🔵 **단정하지 않는다** — 「파일에 임포트가 있다」와 「그 경로가 렌더돼 변환을 소비한다」는
다른 것이고(이 저장소가 *"선언 파일 grep ≠ 런타임 모집단"* 으로 이름 붙였다), 그것을 재는
것은 이 티켓이 아니라 **`587` 의 몫**이다. 이 티켓은 **그 문장이 낡았다는 사실만** 심는다.

🔴 **`587` 의 날짜 게이트(2026-09-28)를 어기지 않는다** — 아래 AC-4 가 그 경계를 정한다.

# Scope

**In**

- `docs/adr/ADR-MONO-067-demo-surfaces-served-from-vercel.md` — 위 ①②③
- `docs/adr/ADR-MONO-069-oidc-login-across-the-scheme-and-scope-boundary.md` — 위 ④
- `tasks/ready/TASK-MONO-587-*.md` — 위 § 의 낡음 **표식만**(측정은 587 의 몫)

**Out**

- 🔴 `ADR-MONO-067` § History 및 다른 ADR 의 History/날짜 붙은 기록 절. **⑤ 를 읽어라.**
- 🔴 **결정 본문을 바꾸는 일.** 이 티켓은 «무엇이 남았나» 를 최신화할 뿐, `B`·`D1~D4`·
  단계 정의·단계 번호를 건드리지 않는다. 번호 재매김은 명시적으로 금지돼 있다.
- 🔴 `TASK-MONO-587` 의 **재측정**(변환 건수·렌더 경로). 날짜 게이트 뒤이고 587 의 AC 다.
- console 억제 그 자체 — `TASK-MONO-627`.
- `ADR-MONO-067` 단계 1(`terraform apply`) — 소유자 대기 중이고 이 티켓의 축이 아니다.

# Acceptance Criteria

- [ ] **AC-0 (착수 시 재측정 — 상속 금지)**
      1. 위 § 실측의 **라이브 4표면 + 음성 대조군**을 다시 잰다. 🔴 음성 대조군 없이 얻은
         200/307 은 와일드카드와 구별되지 않는다.
      2. `582`·`585`·`586`·`618`·`625` 가 여전히 `tasks/done/` 인지 확인한다.
      3. 🔴 **①~⑤ 의 줄 번호를 신뢰하지 마라 — 문자열로 다시 찾아라.** 이 티켓이 머지되는
         사이에 파일이 움직인다.
      4. 🔴🔴 **형제를 다시 grep 하라.** 이 표는 «내가 본 창구» 다 —
         `BLOCKED` · `막혀` · `미생성` · `미착수` · `남은 것` 을 `docs/adr/` 와 활성 큐
         전체에서 훑어 **⑥ 이 있는지** 본다. 있으면 이 표에 더하고, 없으면 **없다고 적어라**
         (0건은 «없다» 가 아니라 «내가 이 술어로 못 찾았다» 이므로 술어를 함께 적는다).

- [ ] **AC-1 — ①②③ 을 고친다.** 🔴 ③ 은 **같은 파일 안의 자기모순**이므로 ①②③ 을
      **한 커밋에서** 고친다. 하나만 고치면 살아남은 쪽이 계속 읽힌다
      (*"한 사실이 두 절에 있으면 한쪽만 고쳐진다"* — 이 ADR 자신이 그 링크를 달아 뒀다).

- [ ] **AC-2 — ④ 를 고친다.** `console.hubwang.com` 은 **실재하고 307 을 낸다**.
      🔴 **«로그인이 성립한다» 로 고치지 마라** — 그것은 `TASK-MONO-624` 가 잴 것이고
      아직 안 쟀다. 바뀐 것은 *"프로젝트가 없어서 시도조차 못 한다"* → *"프로젝트는 있고,
      홉 ②③④⑤ 는 여전히 미측정"* 이다. **가설 칸은 가설로 남는다.**

- [ ] **AC-3 — ⑤ 는 안 고쳤음을 적는다.** diff 에 `ADR-MONO-067` § History 변경이
      **0줄**이어야 하고, 그 사실과 이유를 PR 본문에 적는다.
      🔵 «안 한 일» 을 적는 이유: 다음 사람이 같은 grep 을 돌리면 ⑤ 가 또 걸리고,
      기록이 없으면 «빠뜨렸다» 로 읽혀 고쳐진다.

- [ ] **AC-4 — `587` 에는 «낡음 표식» 만 심는다.**
      그 티켓의 AC-2 표 위에 🔴 표식을 달아 **① 두 칸이 거짓이 됐다는 사실**과
      **② 결론이 뒤집혔을 수 있다는 것**을 적고, `587` 의 `AC-0′` 가 그것을 재도록 건다.
      🔴 **여기서 재지 마라** — 587 은 `⏳ 2026-09-28 이전 착수 금지` 이고, 변환 건수를
      지금 읽는 것은 그 티켓이 스스로 금지한 «주기 중간 단일 표본» 이다.
      🔵 표식을 심는 것은 «착수» 가 아니다 — 착수 금지가 지키는 것은 **측정 시점**이지
      «그 티켓 파일을 열지 마라» 가 아니다. 그 구별을 티켓에 적어라.

- [ ] **AC-5 — 왜 아무도 안 고쳤는지 한 줄 남긴다.** 네 PR(`582`·`585`·`618`·`625`)이
      전부 자기가 거짓으로 만든 문장을 안 고쳤다. 🔴 **«다음부터 잘하자» 는 처방이 아니다**
      — 이 저장소는 그것을 이미 안다(*"규칙을 적어 두는 것으로는 안 막힌다"*).
      ⇒ **가드를 만들 수 있는지 판정하고, 만들 수 없으면 왜인지 적어라.**
      후보: 「`done/` 에 있는 티켓 ID 를 «BLOCKED/막혀/남은 것» 문맥에서 참조하는 활성
      문서가 있으면 빨강」. 🔴 그 술어의 **오탐 모집단**(History·인용·과거형 서술)을 먼저
      세고 나서 판정하라 — 오탐이 많으면 «만들지 않는다» 가 옳은 답이고, 그때는
      **그 판정을 적는 것**이 이 AC 의 닫힘이다(⚪ 가 답인 경우).

# Related Specs

- [`docs/adr/ADR-MONO-067-demo-surfaces-served-from-vercel.md`](../../docs/adr/ADR-MONO-067-demo-surfaces-served-from-vercel.md) § 결정 표 · § Outstanding follow-ups · 🔴 § History 는 **Out**
- [`docs/adr/ADR-MONO-069-oidc-login-across-the-scheme-and-scope-boundary.md`](../../docs/adr/ADR-MONO-069-oidc-login-across-the-scheme-and-scope-boundary.md) § AC-3

# Related Contracts

없음.

# Related Tasks

- `TASK-MONO-625` — 이 낡음을 **발견**한 티켓(`done/`). INDEX `## done` 행에 두 건이 적혀 있다
- `TASK-MONO-627` — console 억제. 이 티켓이 고칠 «남은 일» 칸이 **가리키게 될** 티켓
- `TASK-MONO-587` — AC-4 의 대상. ⏳ **2026-09-28 게이트** — 표식만 심고 재지 않는다
- `TASK-MONO-624` — ④ 가 세우는 전제 위에 선다. ⏳ 데모 게이트

# Edge Cases

① 🔴 **`TASK-MONO-627` 이 아직 `ready/` 에 있다.** ①② 의 «남은 일» 칸이 그 ID 를 가리키게
   되는데, 그 티켓이 착수도 안 된 상태다 ⇒ *"남은 것은 억제(`627`)"* 라고 적되
   **«627 이 끝났다» 로 읽히지 않게** 상태를 함께 적어라.

② 🔴 **`587` 은 `ready/` 의 활성 파일이다.** 이 티켓이 그 파일을 고치면 `587` 착수자가
   보는 것이 바뀐다. ⇒ 표식은 **기존 내용을 지우지 말고 위에 덧붙여라**(그 표의 08-27
   측정 자체는 **그때 옳았다** — 지우면 왜 그런 결론이었는지가 사라진다).

③ **`ADR-MONO-069` 는 `ADR-MONO-067` 을 인용한다**(L7). 067 의 문장을 고치면 069 의 인용이
   어긋날 수 있다 ⇒ 인용 자리도 함께 확인하라.

④ 🔵 **단계 1(`terraform apply`)은 소유자 대기다.** ①②③ 을 고치며 단계 1 행까지
   «최신화» 하지 마라 — 그 행은 낡은 게 아니라 **정말 안 끝났다.**

# Failure Scenarios

① **History(⑤)를 함께 «최신화» 한다** → 🔴 왜 `1 → 2 → 4 → 3` 순서로 갔는지가 지워진다.
   그 절이 스스로 재번호 금지를 적어 둔 것과 같은 이유다. AC-3 이 이것을 막는다.

② **④ 를 «console 로그인이 성립한다» 로 고친다** → 🔴 `624` 가 잴 것을 **미리 참으로**
   만든다. 표면이 200/307 인 것과 OIDC 왕복이 성립하는 것은 다른 명제다
   (*"401 = 물어보지도 못했다"* 의 형제 — 도달과 성립은 다르다).

③ **`587` 을 여기서 재기 시작한다** → 🔴 날짜 게이트를 어기고, 그 티켓이 스스로 금지한
   단일 표본을 하나 더 만든다. AC-4 가 경계를 정한다.

④ **①②③④ 중 일부만 고친다** → 🔴 남은 쪽이 계속 읽힌다. 특히 ③ 은 ① 과 **같은 파일**
   안에서 모순이므로, 하나만 고치면 그 파일이 자기 안에서 두 가지를 말하는 상태가 유지된다.

# Definition of Done

- [ ] ①②③④ 가 실제 상태와 일치하고, **한 커밋**에 들어 있다
- [ ] `ADR-MONO-067` § History diff **0줄**이고 그 사실이 PR 에 적혀 있다
- [ ] `587` 에 낡음 표식이 있고, **측정은 안 했다**는 것이 명시돼 있다
- [ ] AC-0 ④ 의 형제 grep 결과가 **술어와 함께** 적혀 있다(⑥ 이 있든 없든)
- [ ] AC-5 의 가드 판정이 적혀 있다 — 만들었거나, **왜 안 만드는지**가 적혀 있다

---

분석=Opus 5 / 구현 권장=Sonnet (판정은 이 절에 다 적혀 있고 남은 것은 **정확한 치환**이다.
🔴 단, AC-5 의 «가드를 만들 수 있는가» 는 오탐 모집단을 세어야 하므로 그 칸만 판단이 든다).

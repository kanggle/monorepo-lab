# Task ID

TASK-MONO-556

# Title

finance 시드가 **재시작마다** 실패한다 — 술어가 "입금이 성립했나" 대신 "지금 잔액이 목표 이상인가" 를 묻고, **같은 시드가 그 잔액을 쓴다**

# Status

ready

# Owner

monorepo

# Task Tags

- infra
- demo
- seed

---

# 배경 — 2026-08-18(UTC) `TASK-MONO-553` AC-1 라이브 실증에서 발견

새 AMI(`ami-04b39fe28cf9e75d4`)로 apply 한 뒤 `/stop` → `/start` 왕복을 실증하는 중에,
**두 번째 부팅에서만** finance 시드가 깨졌다.

| 부팅 | finance 시드 결과 |
|---|---|
| 1회차 (09:21, 인스턴스 최초 기동) | `생성 7 · 기존 0 · 실패 0` ✅ |
| 2회차 (10:18, `/stop`→`/start` 후) | `생성 4 · 기존 2 · **실패 1**` ✗ |

실패한 한 줄:

```
[seed:finance] ✗ 계좌 A 입금 — 입금 후 ledger=400000 로 목표 500000 미만이다
[seed:finance] 기존  계좌 B 입금 — ledger 600000 (재생, 이중 입금 없음)
[seed:finance] 기존  이체 A→B (재생 — 잔액 불변 A=400000 B=600000)
```

## 🔴 술어가 자기 스크립트의 다음 단계에 의해 무효가 된다

`infra/demo/seed/seed-finance.sh` 의 `topup()` 은 **의도적으로** 응답 코드를 안 믿는다.
그 판단 자체는 옳고, 주석에 이유가 적혀 있다:

> *"고정 `Idempotency-Key` 를 쓰므로 2회차는 서버가 저장된 2xx 를 **재생**한다. 즉 '이번
> 실행이 돈을 넣었나' 는 200 으로 판별 불가능하다. 여기서는 **잔액 자체**가 술어다:
> 목표 이상이면 도달, 미만이면 실패 — **재생이든 최초든 결론이 같다**."*

마지막 문장이 **참이 아니다.** 결론이 같으려면 *"잔액은 입금 말고는 안 변한다"* 가 성립해야
하는데, **바로 다음 단계가 그 잔액을 쓴다**:

```
TOPUP_MINOR=500000
topup "계좌 A 입금" ...   →  A = 500,000
topup "계좌 B 입금" ...   →  B = 500,000
이체 A→B 100,000         →  A = 400,000 · B = 600,000     ← 여기서 술어의 전제가 깨진다
```

2회차에는 입금이 재생(=돈 안 들어옴)되고 A 는 여전히 400,000 이므로
`after(400000) >= want(500000)` 이 거짓 → `seed_fail`.

🔵 **A 만 실패하고 B 는 통과하는 비대칭이 지문이다.** B 는 이체를 *받아서* 600,000 ≥
500,000 이라 통과한다. 실패 방향이 정확히 이체 방향과 일치한다 — 우연이 아니다.

⇒ **1회차 성공은 "고쳐져 있다"의 증거가 아니었다.** 이 시드는 **한 번도 재실행된 적이 없는
계좌**에서만 통과한다.

## 왜 지금까지 안 보였는가 (그리고 왜 지금 보이는가)

`seed_fail` 은 예전에도 찍혔고 `demo-up.sh` 도 `⚠ 도메인 데이터 시드가 일부 실패했습니다` 를
출력했다. 그런데 **`demo-up.sh` 는 무조건 0 으로 끝났다** — 주석은 *"비-0 로 끝나되 기동은
유지"* 라고 계약을 적어 뒀지만 코드가 그러지 않았다(`TASK-MONO-553` 이 그 불일치를 고쳤다).
그래서 `demo-stack.service` 는 **초록**이었고, 로그를 펼쳐 읽지 않는 한 아무도 몰랐다.

553 이 종료코드를 정직하게 만든 첫 재시작에서 이것이 곧바로 드러났다.

## 🔴 그래서 지금 상태가 나쁘다 — 유닛이 **영구히 red** 다

재시작은 이 데모의 **정상 경로**다(그것이 `TASK-MONO-553` 이 존재하는 이유다). 그런데 이
결함은 재시작마다 결정론적으로 재현되므로, `demo-stack.service` 는 **첫 재시작 이후 항상
`failed`** 다. 실측(2026-08-18 10:18): 컨테이너 100 running / 3 exited(0), 8개 도메인 전부
`up`, 라벨 드리프트 0건 — **그런데 유닛은 failed.**

*"늘 빨간 신호"* 는 *"늘 초록인 신호"* 와 정확히 같은 값을 한다: 아무도 안 본다. 553 이 만든
정직한 종료코드가 이 결함 때문에 **첫날부터 무의미해질 위험**이 있다.

---

# Goal

시드가 **재실행에도 정직하게 통과**한다. 그리고 그 통과가 *"입금이 성립해 있다"* 를 뜻한다 —
*"지금 잔액이 우연히 목표 이상이다"* 가 아니라.

# Scope

## In Scope

- `seed-finance.sh` 의 `topup()` 술어를 **입금 사실**에 대한 것으로 바꾼다.
  구현자가 AC-0 에서 확인 후 택일하고 **근거를 적을 것**:
  - (a) **목표를 실제 기대값으로 낮춘다** — 이체 후 A 의 정상 잔액은 400,000 이므로
    계좌별 기대치를 따로 준다. 🔴 가장 작은 변경이지만 **가장 약하다**: 상수 두 개가
    이체 금액과 **말없이 결합**되고, 이체 금액이 바뀌면 다시 깨진다.
  - (b) **입금 거래의 존재로 판정한다** — 거래내역에서 시드 입금(reason/idempotency key)을
    찾는다. 술어가 *"입금이 성립했나"* 와 정확히 일치한다.
  - (c) **하한을 이체를 반영해 계산한다** — `want = TOPUP_MINOR - (이 시드가 옮긴 금액)`.
    (a)보다 낫지만 여전히 시드 내부 순서에 의존한다.
- 🔴 **어느 쪽이든 "재생이든 최초든 결론이 같다" 는 주석을 고칠 것.** 그 문장이 틀렸다는
  것이 이 티켓이고, 고친 코드 옆에 틀린 이유가 남아 있으면 다음 사람이 같은 추론을 한다.

## Out of Scope

- **시드 전체를 멱등으로 재설계**하는 것. 다른 도메인 시드(wms·scm·erp·ecommerce·fan)는
  같은 왕복에서 `실패 0` 이었다 — 🔵 다만 **그것이 "그들은 안전하다"는 뜻은 아니다**
  (AC-2 참조).
- `demo-up.sh` 의 종료코드 정책 → `TASK-MONO-553` 에서 이미 결정됐다. **되돌리지 말 것.**
  유닛이 red 인 것은 정확한 보고이고, 고칠 것은 **red 의 원인**이다.
- finance 백엔드의 입금/이체 로직. 서버는 시킨 대로 했다 — 결함은 **시드의 술어**다.

# Acceptance Criteria

**AC-0 — 재확인 (verify-then-act).** ✅ **완료 (2026-08-18, origin/main @ `9f815273e`)**
`seed-finance.sh` 의 `topup()` · 이체 블록 · 상수를 다시 읽고 **A 의 이체 후 기대 잔액을
산술로 확인**했다 — 위 400,000 은 로그 관측값이었으므로 코드에서 다시 유도했다:

```
TOPUP_MINOR    = 500000        (L158)
TRANSFER_MINOR = 100000        (L200)
⇒ A = 500,000 - 100,000 = 400,000     ← 관측 400000 과 일치
⇒ B = 500,000 + 100,000 = 600,000     ← 관측 600000 과 일치
```

🔴🔴 **그리고 답은 상수 옆 주석에 이미 적혀 있었다:**

```
TOPUP_MINOR=500000   # ₩5,000.00 — 이체 100,000 + 홀드 여유
```

작성자는 **이체가 잔액을 깎는다는 것을 알고** 입금액을 그만큼 크게 잡았다. 그런데
**같은 함수의 술어는 그 사실을 반영하지 않는다**(`after >= 500000`). 상수는 이체를
계산에 넣었고 술어는 안 넣었다 — 한 파일 안에서 두 곳이 어긋나 있었고, 어긋난 쪽이
술어다.

**AC-1 — 재실행이 통과한다. 🔴 대조군 필수.**
같은 데이터에 대고 시드를 **두 번** 돌려 두 번 다 `실패 0` 이다.
**그리고 대조군**: 입금이 **실제로 안 된 상태**(새 계좌 / 입금 호출 제거)에서는 **여전히
실패**해야 한다. 대조군 없이 통과만 보면 *"술어를 지워서 통과시킨"* 구현과 구별되지 않는다 —
그 구현은 이 시드가 지키려던 회귀(`INSUFFICIENT_AVAILABLE_BALANCE`)를 전부 놓친다.

**AC-2 — 형제를 세라.** finance 만 고치고 닫지 않는다. 다른 5개 도메인 시드에도
*"현재 상태 ≥ 목표"* 형태의 술어가 있는지 **전수로 확인**하고 결과를 적는다.
🔴 **"이번 왕복에서 실패 0 이었다" 는 근거가 아니다** — finance 도 1회차에는 실패 0 이었다.
같은 함정은 *"그 시드가 자기가 만든 상태를 나중에 소비하는가"* 로 찾아야 한다.
0건이면 0건이라고 적되, **무엇을 어떻게 셌는지** 함께 적을 것.

**AC-3 — 라이브 판정은 왕복에서.** 첫 부팅만으로 닫지 않는다. 이 결함은 **2회차에만**
발화한다. ⚠️ 재굽기 불필요(시드는 저장소에서 읽힌다)이나 `terraform apply` 없이
인스턴스에서 직접 `bash infra/demo/seed/seed.sh finance` 를 두 번 돌리면 판정 가능하다 —
**인스턴스 기동은 예산을 쓴다(사용자 승인 대상).**

**AC-4 — 유닛이 초록으로 돌아온다.** 고친 뒤 재시작에서 `demo-stack.service` 가
`active/exited` 다. 🔴 단, **그것을 목표로 삼아 종료코드를 무르게 고치지 말 것** —
초록이 되는 유일하게 옳은 길은 실패가 사라지는 것이다.

# Related Specs

- `infra/demo/seed/seed-finance.sh` L145~190 (`topup()`) · 이체 블록 — 결함의 자리
- `infra/demo/seed/lib.sh` — `seed_fail` / 요약 집계
- `infra/demo/demo-up.sh` — 시드 종료코드가 유닛 결과가 되는 자리 (`TASK-MONO-553`)
- `TASK-MONO-537` — finance 시드가 **콜드 스택에서 6/6 실패**하던 별건(준비성 술어). 고쳐졌고
  이 결함과 다르다: 그건 *"너무 일찍 물었다"*, 이건 *"두 번째엔 전제가 다르다"*.

# Related Contracts

없음 (데모 시드 전용).

# Edge Cases

- **볼륨이 새로 만들어지면 1회차 조건이 된다** — `ami_id` 변경 등으로 인스턴스가 교체되면
  통과한다. 그래서 **재굽기/재배포 직후 테스트는 이 결함을 못 본다.**
- **B 는 통과한다** — 이체를 받아 잔액이 늘기 때문이다. 한쪽만 보고 "괜찮다" 로 읽지 말 것.
- **`Idempotency-Key` 는 정상 동작 중이다** — 이중 입금은 실제로 없었다(`기존 … 재생, 이중
  입금 없음`). 서버를 의심하지 말 것.
- **이체 금액이 바뀌면** (a)/(c) 안은 다시 깨진다. 그 결합을 주석에 남길 것.

# Failure Scenarios

- **술어를 지워서 통과시킨다** — `INSUFFICIENT_AVAILABLE_BALANCE` 회귀 감지가 통째로 사라진다.
  AC-1 의 대조군이 그것을 막는다.
- **`demo-up.sh` 의 종료코드를 되돌린다** — 553 이 고친 *"아무것도 안 보면서 초록"* 으로 되돌아간다.
- **첫 부팅으로 닫는다** — 이 결함은 2회차에만 발화한다(AC-3).
- **finance 만 고치고 닫는다** — AC-2 가 그것을 막는다.

# Notes

- 분석 = **Opus 5** / 구현 권장 = **Opus** — 변경 줄 수는 작지만 **술어 설계**가 본체다.
  (a)를 고르고 상수만 바꾸면 다음 이체 금액 변경에서 그대로 재발한다.
- 선행: 없음. `TASK-MONO-553`(머지됨)이 **이 결함을 드러낸 고침**이다.
- 🔵 이 티켓은 *"내 변경이 만든 회귀"* 가 **아니다.** 결함은 원래 있었고 553 은 그것을
  보이게 했을 뿐이다 — 시드 로그에는 예전에도 찍혔고, 유닛만 초록이었다.

# Task ID

TASK-MONO-616

# Title

⏳ **다음 기동 창 — 열기 전에 «잴 칸»을 적는다.**
🔴🔴 이 티켓이 존재하는 이유는 새 결함이 아니다. **창을 열어 둔 채 칸 하나를 빠뜨렸기 때문**이다.

# Status

in-progress

# Owner

monorepo

# Task Tags

- demo
- boot-window
- adr-followup

---

# 🔎 왜 이 티켓이 있나

`TASK-MONO-615` 기동 창(2026-09-03 UTC, 부팅 5회)은 A1·B4·C1·C2·C4 를 닫고
`TASK-MONO-606` AC-4′ ② 까지 닫았다. 그런데 **같은 창에서 `TASK-MONO-604` AC-0 ① 을
안 쟀다.**

604 는 그 칸에 대해 스스로 이렇게 정정해 뒀다:

> ### ⇒ 🔴 AC-0 ①은 **데모가 꺼져 있는 동안 원리적으로 참이 될 수 없다**
> ### ⇒ 정정: **AC-0 ①의 판정 시점은 AC-4 와 같은 창이다**

나는 AC-4(=`web.ecommerce` 404 + 컨테이너 부재)를 재고 **AC-0 ① 은 지나쳤다.** 데모가
떠 있는 동안이면 **공짜**였고, 지금은 다시 기동 창이 있어야 한다.

🔴 **일반화 — 비싼 창은 「무엇을 하러 열었나」가 아니라 「무엇을 잴 수 있나」로 채워야 한다.**
615 의 창은 「615 의 항목」으로 계획됐고, **다른 티켓이 그 창을 기다리고 있다는 사실은
그 계획 밖**에 있었다. 큐에는 그때도 그 문장이 적혀 있었다 — 내가 안 읽었을 뿐이다.
[[feedback_measure_the_plans_premise_before_starting_the_phase]]
[[feedback_a_census_measures_where_you_looked_not_what_exists]]

🔵 그래서 이 티켓의 본체는 「칸 목록」이 아니라 **AC-0(목록을 만드는 절차)** 이다.
목록은 낡지만 절차는 안 낡는다.

---

# Goal

기동 창을 **한 번** 열어, 그 시점에 `ready`/`in-progress`/`review` 에 있는 티켓들이
**라이브를 기다리는 칸 전부**를 재고 각 티켓에 되돌려 적는다.

# Scope

- **포함**: 창을 열기 전 **매니페스트 작성**(AC-0) · 창 안에서 그 전부 측정 · 각 티켓에 기록
- **제외**: 새 코드 변경. 🔴 측정 중 결함이 나오면 **별 티켓**으로 올리고 이 창에서 고치지
  않는다 — 고치기 시작하면 나머지 칸을 또 놓친다(615 창이 정확히 그렇게 커졌다:
  C2 빌드 구멍 하나 때문에 재굽기가 2회, PR 이 3건이 됐다)
- **제외**: 소유자 결정 사안(`TASK-MONO-612` AC-1 · `ADR-MONO-067` 3↔4 순서 재지정)

---

# Acceptance Criteria

## AC-0 — 🔴🔴 **창을 열기 전에 매니페스트를 만든다. 이것이 본체다**

1. **큐 전수 스캔** — 루트 `tasks/{ready,in-progress,review}/` 와 `projects/*/tasks/*/`
   전부에서 다음 문자열을 grep 한다:
   `기동 창` · `미측정` · `다음 부팅` · `라이브` · `데모가 꺼져` · `⏸️`
2. 걸린 티켓마다 **본문 AC 절을 열어** 「그 칸이 정확히 무엇을 요구하는지」를 한 줄로 옮겨 적는다.
   🔴 **큐 행(INDEX)만 읽지 마라** — 615 창이 604 를 놓친 것은 INDEX 행에는 그 문장이
   요약돼 있지 않았기 때문이다. [[feedback_one_fact_in_two_sections_only_one_gets_fixed]]
3. 각 칸에 **판정 술어**(무엇을 보면 PASS 인가)와 **대조군**을 함께 적는다.
   🔴 술어 없이 「확인한다」로 적힌 칸은 창에서 반드시 애매해진다.
4. 🔴 **칸끼리 상충하는지 본다** — 한 칸의 측정이 다른 칸의 전제를 지우면 **순서를 고정**한다
   (`TASK-MONO-615` 의 A1↔재굽기가 그 모양이었다).
5. 매니페스트를 **이 티켓에 커밋한 뒤** 기동한다. 🔴 머릿속에 두지 마라 —
   창은 길고(615 창은 4시간), 중간에 예정에 없던 일이 반드시 끼어든다.

## AC-1 — 창 안에서 매니페스트를 **전부** 소화한다

- 각 칸은 **PASS / FAIL / 미측정(사유)** 셋 중 하나로 끝난다. 🔴 **빈칸 금지.**
- 🔴 「시간이 없어서 못 쟀다」도 **적어라** — 그것이 다음 창의 입력이다.
- 창을 닫기 전에 매니페스트를 다시 읽고 **소화율(닫은 칸 / 전체 칸)** 을 적는다.

## AC-2 — 각 티켓에 **되돌려 적는다**

- 측정 결과는 **그 칸을 가진 티켓**에 적는다(이 티켓에만 적으면 그 티켓은 계속 열려 있다).
- `review/` 티켓은 frozen 이므로 CORRECTION 절로 붙인다.
- 닫힌 티켓은 close chore 로 `done/` 으로 옮긴다(3-dim + **AC 전수** 확인).

## AC-3 — 🔴 이 창이 **안 고치는 것**을 적는다

- 소유자 대기 항목 · 새로 발견한 결함(별 티켓 번호) · 다음 창으로 넘긴 칸.

---

# 📋 착수 시점의 **후보 목록** (AC-0 이 이것을 대체한다 — 그대로 믿지 마라)

🔴 아래는 2026-09-03 기준의 스냅샷이다. **AC-0 은 이 표를 검증하는 것이지 복사하는 것이
아니다** — 그 사이 닫힌 칸도, 새로 생긴 칸도 있을 수 있다.

| 티켓 | 큐 | 라이브를 기다리는 칸 | 판정 술어(초안) |
|---|---|---|---|
| `TASK-MONO-604` | `review` | **AC-0 ①** — 데모가 **켜진 동안** `store.hubwang.com` 이 **실데이터**를 내는가 | 🔴 상태코드 아님. 본문에 *"데모 서버가 꺼져 있어…"* 가 **없고** 상품 링크 ≥1 · 가격 문자열 ≥1. 음성 대조군 `/products-does-not-exist-604` = 404 |
| `TASK-MONO-610` | `in-progress` | **AC-3 V1–V7**(로그인 왕복) · **AC-2 라이브 절반**(헤어핀) | V1–V7 은 티켓의 홉 표를 그대로. 🔵 615 창이 **V4 · V5 · V8 을 이미 닫았다** — AC-0 에서 재확인하라 |
| `TASK-MONO-612` | `in-progress` | **AC-0 ②** — Vercel store 의 `demoPayment` **런타임 값** | `/api/store-config` 가 `{"demoPayment":true}` 를 내는가 (🔴 로그인 필요) |
| `TASK-MONO-586` | `review` | 라이브 축(2026-09-02 CORRECTION) | 티켓의 CORRECTION 절 참조. 🔴 음성 대조군이 `/` 와 구별 불가였다(catch-all 307) — 술어부터 고쳐라 |
| `TASK-MONO-615` | `in-progress` | ⓐ **매달림(rc=124) 재현** ⓑ **C3 술어** | ⓐ 는 부팅 5회에서 한 번도 안 걸렸다 ⇒ **「고쳐졌다」가 아니라 「아직 안 걸렸다」**. ⓑ 는 `git status` 1줄이 mode change 뿐임을 술어가 구별하게 |

🔵 **`TASK-MONO-604` 는 AC-0 ① 만 남았다** — AC-4 는 615 창이 닫았고(404 **이고** 컨테이너
0건 + 양성 대조군), § 남은 일 「1. 재굽기」도 해소됐다. 🔴 다만 그 티켓의 **(z19) 가드 공백**
(*"렌더는 초록인데 컨테이너는 도는 상태를 (z19)가 판정하지 못한다"*)은 **코드 작업**이라
이 창의 범위가 아니다 — 별도로 다뤄야 한다.

---

# 🔴 기동 창 운영 메모 (615 창에서 실측)

- **예산**: 데모 월 한도 600분. 2026-09-03 기준 **281분 사용**. 부팅 1회 ≈ 12~20분 +
  측정 시간. 🔴 `terraform apply` 로 인스턴스를 갈면 **docker 볼륨이 사라진다**.
- **부팅 성질**: `stop/start` 는 공인 IP 가 바뀌어 `DEMO_DOMAIN` 이 바뀐다.
  🔴 **`reboot` 는 IP 가 유지돼 recreate 자체가 안 일어난다** — 부팅 경합을 재려면
  반드시 `stop/start` 다.
- **정상 시간**: ⓐ 적용 후 부팅이 **690~777s** 에 `Result=success`. 그보다 훨씬 길면 이상.
- **`verify --live` 는 끝까지 못 돈다** — 칸 **(f)** 가 `container_name: scm-platform-redis`
  고정 때문에 떠 있는 데모에서 구조적으로 충돌한다. **(f) 앞까지가 이 도구의 사정거리**다.
- **로그인 왕복 술어** — 🔴🔴 `curl` 로 `/oauth2/authorize` 를 칠 때 **`Accept: text/html`
  을 반드시 붙여라.** `*/*` 면 Spring 이 리소스서버 엔트리포인트로 **401 Bearer** 를 주고,
  그것은 「IdP 가 깨졌다」와 구별되지 않는다(615 창에서 세 경로 × 클라이언트 4개가 전부
  401 이라는 «모집단 조사» 를 만들어 냈고, 전부 내 헤더를 잰 것이었다).
  정본은 `infra/demo/seed/lib.sh` 의 로그인 헬퍼다.
- **시드가 독립 증거다** — `[seed:*] 요약 … 실패 0` 이 여섯 도메인에서 나오면 로그인
  경로는 살아 있다. 내 술어가 그와 모순되면 **내 술어부터 의심하라.**

# Related Specs

- `TASK-MONO-615` § 기동 창 (2026-09-03) — 이 티켓의 발생 경위 전부
- `TASK-MONO-604` § AC-0 ① 정정 · `TASK-MONO-610` § 닫히지 않은 AC 셋
- `TASK-MONO-612` § AC-0 ② · `TASK-MONO-586` § CORRECTION (2026-09-02)
- [`ADR-MONO-067`](../../docs/adr/) 단계 2·3·4 · [`ADR-MONO-069`](../../docs/adr/) 축 ②

# Related Contracts

없음.

# Edge Cases

- 🔴 **AC-0 을 「대충」 하면 이 티켓은 자기 목적을 잃는다.** 매니페스트가 부실하면 창은
  또 한 칸을 빠뜨리고, 그 비용은 다음 창 전체다.
- 🔴 측정 중 결함이 나오면 **고치고 싶어진다.** 615 창이 그렇게 커졌다(재굽기 2회·PR 3건).
  별 티켓으로 올리고 **매니페스트를 마저 소화**한 뒤에 돌아와라.
- 🔴 창을 닫기 전 **인스턴스 정지**를 확인하라. 켜 둔 채 끝나면 예산이 조용히 샌다
  (`max_runtime_minutes=180` 이 있지만 그건 상한이지 절약이 아니다).

# Failure Scenarios

| 상황 | 잘못된 처리 | 옳은 처리 |
|---|---|---|
| 매니페스트 없이 「일단 켜고 보자」 | 615 창이 한 그것 | 🔴 **AC-0 을 커밋한 뒤에 켜라** |
| 큐 행(INDEX)만 읽고 목록을 만든다 | 604 를 놓친 경로 그대로 | 🔴 **티켓 본문 AC 절을 열어라** |
| 어떤 칸을 「시간이 없어」 조용히 건너뛴다 | 다음 창이 그 사실을 모른다 | 🔴 **「미측정(사유)」로 적어라** |
| 라이브 401 을 보고 「IdP 결함」으로 적는다 | 615 창이 직전까지 간 그것 | 🔴 **`Accept` 부터 확인하라.** 시드의 초록과 모순되면 내 술어가 용의자다 |
| 측정 중 발견한 결함을 이 창에서 고친다 | 창이 무한히 커진다 | 🔴 **별 티켓**. 매니페스트 소화가 우선 |

---

---

# ✅ AC-0 — 매니페스트 (2026-09-03 UTC, **창을 열기 전에 작성·커밋**)

🔴 이 절이 이 티켓의 본체다. 아래 § 후보 목록(스냅샷)은 **검증 대상**이었고, 검증 결과
**표가 두 군데 틀렸다** — 한 칸이 사라졌고, 표에 없던 칸이 하나 생겼다.

## AC-0 ① — 큐 전수 스캔 (**모집단부터 다시 셌다**)

| 스캔 대상 | 파일 수 | 방법 |
|---|---|---|
| 루트 `tasks/{ready,in-progress,review}/` | **9** | `ready` 4 · `in-progress` 3 · `review` 2 |
| `projects/*/tasks/{ready,in-progress,review}/` | **0** | 🔵 8개 프로젝트 × 3큐 = **24 디렉터리가 전부 비어 있다**(실측) |

grep 문자열: `기동 창` · `미측정` · `다음 부팅` · `라이브` · `데모가 꺼져` · `⏸️`

🔵 **양성 대조군**: 같은 grep 이 `tasks/done/` 에서 152파일 707건을 잡는다 ⇒ 술어가
아무것도 못 잡는 것이 아니다. 🔴 **`done/` 은 모집단이 아니다**(동결·닫힘) — 제외했다.

| # | 티켓 | 큐 | grep | **칸이 있나** |
|---|---|---|---|---|
| 1 | `TASK-MONO-328` | ready | **0건** | ❌ 없음 — DEFERRED 백로그, 라이브 축 자체가 없다 |
| 2 | `TASK-MONO-585` | ready | 5건 | ❌ **자기 칸이 아니다** — 585 는 `610` 왕복의 **소비자**다(§ 아래 주의) |
| 3 | `TASK-MONO-587` | ready | 1건 | ❌ **창이 필요 없다** — AC 전부가 Vercel 대시보드 판독이고 데모 기동과 무관 |
| 4 | `TASK-MONO-604` | review | 9건 | ✅ **칸 1** |
| 5 | `TASK-MONO-586` | review | 5건 | ✅ **칸 2** |
| 6 | `TASK-MONO-610` | in-progress | 19건 | ✅ **칸 3 · 칸 4** |
| 7 | `TASK-MONO-612` | in-progress | 16건 | ✅ **칸 5** |
| 8 | `TASK-MONO-615` | in-progress | 37건 | ✅ **칸 6** (+ 칸 7 은 창 밖) |
| 9 | `TASK-MONO-616` | ready | 15건 | — 자기 자신 |

### 🔴 585 를 「칸」으로 세지 않는 이유 — 그러나 **이름은 적어 둔다**

585 § 왕복 홉 표가 ②③④⑤ 를 「미측정」으로 갖고 있다. 그것은 **610 AC-3 과 같은 사실**이고
585 는 그 결과를 **읽는 쪽**이다. 🔴 그러므로 **칸 3 이 닫히면 585 의 그 표도 같이 낡는다**
— AC-2(되돌려 적기) 대상에 585 를 포함시킨다. 한 사실이 두 절에 있으면 한쪽만 고쳐진다.
[[feedback_one_fact_in_two_sections_only_one_gets_fixed]]

---

## AC-0 ② + ③ — 매니페스트 본표 (**칸 · 술어 · 대조군**)

🔴 각 칸의 「무엇을 요구하나」는 **INDEX 행이 아니라 그 티켓 본문 AC 절**에서 옮겼다.
🔴 **PASS 의 정의를 칸마다 고정한다** — 아래 「PASS 는 …이다」 줄이 그것이다.

### 칸 1 — `TASK-MONO-604` **AC-0 ①** (review · CORRECTION 으로 되돌려 적는다)

| | |
|---|---|
| 티켓 본문이 요구하는 것 | *"`https://store.hubwang.com/products` 가 2xx **이고 실데이터를 렌더**하는지"* — 그리고 § 정정: *"다음 재굽기·기동 창에서, 억제된 AMI 로 부팅한 직후 AC-0 ①을 **먼저** 재라"* |
| **판정 술어** | 🔴 상태코드 아님. 본문에서 HTML 주석·`<script>`·`<style>` 을 지운 뒤 ⑴ *"데모 서버가 꺼져 있어"* 가 **없고** ⑵ `href="/products/…"` **≥1** ⑶ 가격 문자열(`[0-9,]+원`) **≥1** |
| **음성 대조군** | `/products-does-not-exist-604` → **404** (실측 2026-09-03: `404 / 11,067 B`) ⇒ 200 이 catch-all 이 아님 |
| **사전 기준선(=반대편 대조군)** | 🔵 **창 열기 전에 이미 쟀다** (2026-09-03, 데모 OFF): `200 / 45,849 B` · *"데모 서버가 꺼져 있어…"* **1건** · 상품 링크 **0** · 가격 **0**. ⇒ 창 안에서 이 셋이 **뒤집히면** PASS, 그대로면 FAIL |
| **순서** | 부팅 완료 **후**, 그리고 🔴 해석기 캐시 `CACHE_TTL_MS = 15_000` 때문에 **≥15초 뒤 새 요청**으로 잰다(`backend-resolver/src/index.ts:119`). 첫 요청이 stale 캐시를 볼 수 있다 |
| PASS 는 …이다 | 술어 셋이 **전부** 뒤집힘. 하나라도 남으면 FAIL |

### 칸 2 — `TASK-MONO-586` **라이브 축** (review · CORRECTION 으로 되돌려 적는다)

| | |
|---|---|
| 티켓 본문이 요구하는 것 | § CORRECTION(2026-09-02): *"「해석기가 백엔드에 닿는다」는 닫히지 않았다 … 로그인 왕복이 필요하고, 그 선행이 `OIDC_ISSUER_URL` 이다"* |
| **판정 술어** | `fan.hubwang.com` 에서 next-auth 왕복: csrf → `POST /api/auth/signin/iam` 의 `Location` 이 **`https://auth.hubwang.com/oauth2/authorize?…`** → 로그인 폼 → 콜백 → `GET /api/auth/session` 이 `accountId`·`tenantId=fan-platform` 을 낸다 |
| 🔴 **술어를 고쳤다 — 티켓의 음성 대조군이 틀렸다** | 티켓은 *"`/zzz-nope-586` = 307, `/` 와 **구별 불가**"* 라 적었다. **상태코드와 바이트만 봤기 때문**이다. `Location` 을 읽으면 구별된다 (실측 2026-09-03): `/` → `…/login?from=%2F` · `/zzz-nope-586` → `…/login?from=%2Fzzz-nope-586`. ⇒ 🔵 **대조군은 살아 있다. 술어가 좁았을 뿐이다** |
| 🔴 **발효 판별자** (이 칸의 핵심) | `OIDC_ISSUER_URL` 이 발효됐으면 위 `Location` 이 `https://auth.hubwang.com/…`, 미발효면 코드 폴백 `http://iam.local/…` (`auth-callbacks.ts` 계열). **두 값이 서로를 판별한다** |
| 🔴 **데모 OFF 에서는 못 판별한다 — 실측함** | 2026-09-03 데모 OFF 상태에서 store·fan 둘 다 `302 → /login?error=Configuration`. issuer 가 **꽂혀 있어도**(discovery 가 503) **안 꽂혀 있어도**(iam.local 이 DNS 실패) 같은 값이 나온다 ⇒ **이 칸은 창 안에서만 판별 가능하다** [[feedback_a_verifiable_mechanism_is_not_the_cause]] |
| **양성 대조군** | 시드 `[seed:fan] … 실패 0` · 615 창이 닫은 **데모 호스트 판** 팬 로그인(`web.fan-platform.<도메인>`) — 그쪽이 되고 `fan.hubwang.com` 이 안 되면 **차이는 Vercel 쪽**이다 |
| **순서** | 부팅 후 · 칸 3 과 같은 도구를 쓰므로 연속 실행 |
| PASS 는 …이다 | `session` 이 `accountId` 를 낸다. `Location` 만 옳고 폼에서 죽으면 **FAIL(사유 기재)** |

### 칸 3 — `TASK-MONO-610` **AC-3 (V1–V8)** (in-progress)

| | |
|---|---|
| 티켓 본문이 요구하는 것 | *"§ Verification V1–V7 을 안별로 채운다"* + V8(기존 볼륨 마이그) |
| **착수 시점 실측 상태** | V1 · V2 · V3 · V6 · V7 = 창 #1(09-02) **PASS** · V4 · V5 = 창 #1 **FAIL** → 창 #2(09-03, `615` B1/B4) **닫힘** · V8 = 창 #2(`615` A1) **닫힘** |
| 🔴 **그래서 이 칸의 남은 일은 「측정」이 아니라 「되돌려 적기」다** | 🔵 **창이 필요 없다.** 610 § AC-3 표에 창 #2 결과를 반영하는 문서 작업이고, AC-2 에서 한다 |
| 🔴 다만 창에서 **공짜로** 재확인할 것 | V4(`/connect/logout` → **401**, 404 아님) · V7(데모 정지 중 **503 + 정의된 화면**) — 둘 다 한 줄 curl |
| **음성 대조군 (V4)** | 존재하지 않는 경로 `…/connect/zzz-616` → 404 여야 한다. 401 이 나오면 「401 은 라우팅의 증거」라는 술어가 무너진다 |
| PASS 는 …이다 | 610 AC-3 표의 **8칸이 전부 PASS/FAIL/미측정 로 채워졌고** 그 근거가 티켓에 있다 |

### 칸 4 — `TASK-MONO-610` **AC-4b** (in-progress) — 🔴🔴 **스냅샷이 몰랐던 변화가 여기 있다**

| | |
|---|---|
| 티켓 본문이 요구하는 것 | `kanggle-fan` 한 줄 + `kanggle-store` **다섯 줄**(1 `OIDC_ISSUER_URL` · 2 `NEXTAUTH_URL` · 3 `NEXTAUTH_SECRET` · 4·5 `ECOMMERCE_WEB_STORE_CLIENT_{ID,SECRET}`). *"판정은 env 목록이 아니라 «로그인이 되는가»"* |
| 🔴🔴 **착수 실측 (2026-09-03) — 뒤집혔다** | `store.hubwang.com/api/auth/providers` = **200 / 171 B**(09-02 에는 **500 / 108 B**), `/api/auth/csrf` = **200 / 80 B**, 응답의 `callbackUrl` = `https://store.hubwang.com/api/auth/callback/iam` ⇒ `NEXTAUTH_URL`·`NEXTAUTH_SECRET` 이 **들어갔다** |
| ⇒ **새 칸이 생겼다** | 스냅샷 표는 store 축을 「612 AC-0 ②」 하나로만 갖고 있었다. 이제 **store 로그인 왕복 자체가 측정 가능**하다 — 09-02 에는 원리적으로 불가능했다(500) |
| **판정 술어** | 칸 2 와 동일한 왕복을 `store.hubwang.com` 에서. 콜백 `redirect_uri` 는 `V0035` 가 등록했다 |
| **음성 대조군** | 🔵 `kanggle-fan`(칸 2) 이 **양성** 대조군이고, 두 팔이 갈리면 차이는 store 의 env 5줄 중 **1·5** 에 있다(2·3 은 위에서 발효 확인됨) |
| 🔴 **단일 변수 귀속 금지** | 두 팔 사이 변수가 여럿이다. 실패 시 주장은 *"store 왕복이 X 에서 끊긴다"* 까지 [[feedback_control_group_design_four_axes]] |
| **순서** | 🔴 **칸 5(612)의 선행이다.** 로그인이 안 되면 `/api/store-config` 를 읽을 수 없다 |
| PASS 는 …이다 | `store.hubwang.com/api/auth/session` 이 `accountId` 를 낸다 |

### 칸 5 — `TASK-MONO-612` **AC-0 ②** (in-progress)

| | |
|---|---|
| 티켓 본문이 요구하는 것 | *"런타임 값을 실제로 읽는다. `/api/store-config` 는 인증 게이트 뒤이므로 ⑴ 로그인해서 읽거나 ⑵ 체크아웃 화면의 관측 가능한 결과로 판정하거나 ⑶ 그것도 안 되면 「미측정」이라고 적는다"* |
| **판정 술어** | 로그인 세션 쿠키로 `GET /api/store-config` → JSON 의 `demoPayment` 값을 **읽는다** |
| 🔴🔴 **PASS 는 「값이 `true`」가 아니다** | **PASS = 런타임 값을 읽었다.** `false` 가 나와도 이 칸은 PASS 다 — 그건 AC-1(소유자 지정, `DEMO_PAYMENT_MOCK=1`)의 입력이지 이 칸의 실패가 아니다. 🔴 이 구별을 안 하면 소유자 대기 항목이 「FAIL」로 오기록된다 |
| **사전 기준선** | 실측 2026-09-03: `/api/store-config` → **`307 → /login?from=%2Fapi%2Fstore-config`** ⇒ 게이트는 여전히 살아 있다(익명으로는 못 읽는다) |
| **순서** | 🔴 **칸 4 다음.** 칸 4 가 FAIL 이면 이 칸은 **미측정(사유: 로그인 불가)** 으로 닫고 넘어간다 |
| 616 Scope 밖 | AC-1 의 **방향 지정**(Vercel env 투입)은 소유자 결정 ⇒ 이 창에서 하지 않는다 |

### 칸 6 — `TASK-MONO-615` **ⓐ 매달림(rc=124) 재현** (in-progress)

| | |
|---|---|
| 티켓 본문이 요구하는 것 | § 이 창이 안 닫은 것: *"매달림(rc=124) 경로 — 이 창 전체(부팅 5회)에서 한 번도 재현되지 않았다. `#3601` 의 호출당 상한은 여전히 **미시험**이다. 「고쳐졌다」가 아니라 「아직 안 걸렸다」이다"* |
| **판정 술어** | 부팅 로그에 호출당 상한이 **발화한 흔적**(타임아웃 절단 + 사유 출력)이 나오는가 |
| 🔴 **재현은 확률적이다** | 「안 걸렸다」는 **PASS 가 아니다** ⇒ **미측정(사유: 부팅 N회에서 미재현)** 으로 적고 N 을 명시한다 |
| **순서** | 🔴 **맨 마지막.** 이 칸만이 「부팅 횟수」를 소비한다 ⇒ 칸 1~5 를 **첫 부팅에서 전부 소화한 뒤** 남은 예산으로만 시도 |
| 예산 상충 | 아래 § 예산 참조 |

### 칸 7 — `TASK-MONO-615` **C3 술어** — 🔵 **창 밖. 창을 기다리지 마라**

티켓 § C3: *"술어(`git status` 0줄)가 mode change 를 content 와 구별하지 못한다"*.
🔵 이건 **술어 문서 작업**이고 라이브가 필요 없다 ⇒ 이 창의 칸이 아니다. 별도로 처리한다.
🔴 다만 창에서 **공짜 재확인**은 한다: 새 인스턴스의 `git status --short` 가
`M infra/demo/demo-boot.sh` **1줄**이고 `git diff --numstat` 이 `0 0` 인가(재현성 확인).

---

## AC-0 ④ — 🔴 **칸끼리의 상충과 순서 고정**

| # | 상충 | 고정한 순서 / 처분 |
|---|---|---|
| **가** | 🔴🔴 **616 Scope(«창 안에서 고치지 않는다») ↔ 604 정정(«거짓이면 그 창에서 오버라이드를 걷어라»)** | **616 Scope 가 이긴다 — 측정만 한다.** 근거: 걷어도 **즉시 복구되지 않는다**. 615 창이 재굽기로 `web-store` 컨테이너를 AMI 에서 없앴고(C1: `docker ps -a` **0건**), 호스트는 pull 하지 않는다(610 원장) ⇒ 되돌리기는 **또 한 번의 재굽기**를 요구한다. 즉 «싸다» 는 전제가 이미 낡았다. ⇒ FAIL 이면 **별 티켓** |
| **나** | 칸 5 는 칸 4 의 결과 없이 성립하지 않는다 | **칸 4 → 칸 5.** 칸 4 FAIL ⇒ 칸 5 = 미측정(사유) |
| **다** | 칸 6 만 부팅 횟수를 소비하고, 그 소비가 칸 1~5 의 예산을 지운다 | **칸 1~5 를 첫 부팅에서 전부 → 칸 6 은 마지막** |
| **라** | 칸 1 은 해석기 캐시(15s) 뒤에 재야 하고, 칸 2·4 는 IdP 가 뜬 뒤여야 한다 | **부팅 완료 판정(`Result=success` + `HTTP 표면 2/2`) → 15s 대기 → 칸 1 → 칸 3(공짜 재확인) → 칸 2 → 칸 4 → 칸 5** |
| **마** | `verify --live` 는 **(f) 앞까지만** 돈다(`container_name: scm-platform-redis` 고정 충돌 — 615 실측) | (f) 이후 칸은 **미측정(구조적)** 으로 미리 이름을 적어 둔다. rc=1 을 보고 이 창의 변경을 의심하지 마라 |

---

## AC-0 ⑤ — 🔴 창을 열기 전 **한 번 더 읽을 것** (착수 즉시, 추정 금지)

1. 🔴 **예산을 다시 읽는다** — `GET <DEMO_API_BASE>/status` 의 `used_minutes` / `budget_minutes`.
   티켓 § 운영 메모의 **281분** 은 **2026-09-03 스냅샷이고 창 #2(부팅 5회) 이전 값**이다.
   ⇒ **상속하지 마라.** 남은 분으로 칸 6 의 시도 횟수를 정한다.
2. 🔴 `state` 가 `stopped` 인지 확인(창 시작점). 🔵 **독립 확인 하나를 이미 갖고 있다**:
   `https://auth.hubwang.com/.well-known/openid-configuration` = **503 + *"데모 백엔드가 지금
   꺼져 있습니다"*** (실측 2026-09-03) — 포워더가 컨트롤 플레인을 읽어 내는 값이다.
3. 🔴 **`stop/start` 로 켠다.** `reboot` 는 IP 가 유지돼 recreate 가 안 일어난다.
4. 🔴 `curl` 로 `/oauth2/authorize` 를 칠 때 **`Accept: text/html` 을 붙인다.**
   정본은 `infra/demo/seed/lib.sh:159`. 안 붙이면 401 Bearer 가 나오고 그것은
   「IdP 가 깨졌다」와 구별되지 않는다(615 창이 없는 결함을 만들어 낸 자리).
5. 창을 닫기 전 **인스턴스 정지**를 확인한다.

---

## 🔴 이 매니페스트가 **미리 적는 미측정 칸** (창이 열리기 전에 이미 확정)

| 칸 | 왜 |
|---|---|
| `verify --live` 의 **(f) 이후 전부** | 구조적 충돌 — 떠 있는 데모 호스트에서 못 돈다 |
| `TASK-MONO-587` | 데모 축이 아니다(Vercel 대시보드). 창을 기다리게 두면 안 된다 |
| `TASK-MONO-615` **C3 술어** | 라이브가 필요 없다 |
| `TASK-MONO-612` **AC-1 방향** · `ADR-MONO-067` 3↔4 순서 | 🙋 **소유자 결정** — 616 Scope 가 명시적으로 제외 |

## 📊 소화율 계산 기준 (창을 닫을 때 채운다)

**분모 = 6** (칸 1~6). 칸 7 과 위 표는 분모 밖(창 밖 항목).
🔴 **미측정(사유)도 「닫은 칸」에 넣지 않는다** — 소화율은 `PASS+FAIL / 6` 이고,
미측정은 **다음 창의 입력**으로 별도 집계한다.

---

---

# ✅ AC-1 — 창 안에서 매니페스트를 소화했다 (기동 창 #3, 2026-09-03 UTC)

인스턴스 `54.117.6.10` · `DEMO_DOMAIN=54-117-6-10.sslip.io`.
🔴 **매니페스트를 먼저 커밋한 뒤 켰다**(PR #3607 머지 → 기동). 이 티켓이 존재하는 이유가
그 순서였다.

## 창 운영 실측

| | |
|---|---|
| 예산 | 창 전 **281/600** → 창 중 **288/600**. 🔵 **카운터는 실제로 누적된다** — 티켓 § 후보 목록이 남긴 「281 이 창 #2 이후에도 안 움직인다」 의문은 **오독이었다**(616 은 창 #2 **뒤**에 파일됐다) |
| 부팅 판정 | 🔴 **`state=running` 은 부팅 완료가 아니다.** 대리지표가 **404 → 503 → 200** 으로 올라오고, IdP 200 시점에도 console 은 여전히 404 였다 ⇒ **615 의 지문(`console=307 web.fan-platform=307`)까지 기다렸다**(08:50:21 UTC) |
| 🔴 idle 제약 | `idle_minutes=20` · 론처가 30초마다 `/heartbeat`. **론처 탭이 닫히면 20분 뒤 자동 종료**된다 — 창의 수명은 예산이 아니라 **그 탭**이다 |

## 판정표 — **소화율 4/6 (67%)**, 미측정 2

| 칸 | 소유 티켓 | 판정 | 한 줄 |
|---|---|---|---|
| **1** | `604` AC-0 ① | 🟢 **PASS** | 「데모 서버가 꺼져」 1→**0** · 상품 링크 0→**8** · 가격 0→**8** · 음성 대조군 404 |
| **2** | `586` 라이브 축 | 🟢 **PASS** | fan 왕복이 세션까지 — `accountId`·`tenantId=fan-platform`·`roles=["FAN"]` |
| **3** | `610` AC-3 (V1–V8) | 🟢 **PASS** | 8칸이 전부 채워졌다. V4·V7 은 이번 창에서 재확인 |
| **4** | `610` AC-4b store | 🔴 **FAIL** | 홉①이 authorize URL 을 못 만든다 → `?error=Configuration` |
| **5** | `612` AC-0 ② | ⚪ **미측정** | 사유: 칸4 FAIL — 게이트를 넘을 로그인이 없다 |
| **6** | `615` ⓐ rc=124 | ⚪ **미측정** | 사유: 컨트롤 플레인에 로그 엔드포인트 없음 + 소유자가 호스트 로그 미제공으로 지정 |

🔴 **분모는 6, 닫은 칸은 PASS+FAIL 인 4.** 미측정 2는 「닫은 칸」에 넣지 않는다 —
그것이 **다음 창의 입력**이다(AC-0 이 미리 정한 계산 기준).

## 🔵 칸 4 의 귀속 — 양성 대조군이 결정적이었다

**같은 순간 같은 IdP 로 fan 은 통과했다.** ⇒ IdP·포워더·C3 뒤집기는 이 실패에 연루되지
않았고, 끊긴 곳은 **store 쪽 discovery/issuer 단계**다(`610` AC-4b 의 1번 줄).
🔴 밖에서는 「미설정」과 「낡은 값」이 구별되지 않으므로 단일 변수로 단정하지 않았다 —
그러나 **처분은 같다.** [[feedback_control_group_design_four_axes]]

🔵 **새 결함은 나오지 않았다.** 칸4 는 이미 티켓에 적힌 소유자 작업이 미적용인 것이라
별 티켓 대상이 아니다 ⇒ 이 창은 **커지지 않았다**(Edge Cases 가 경고한 그 실패를 피했다).

---

# 🔴🔴 이 창이 잡은 **내 술어 결함 둘** — 둘 다 대조군이 잡았다

## ① 가격 정규식 — **틀린 술어가 「FAIL」 쪽으로 실패했다**

```
추출된 본문:  … 슬림핏 데님 청바지  59,000 원 …     ← 「원」 앞에 공백
내 술어:      [0-9][0-9,]*원                         ← 공백 불허 ⇒ 0건 ⇒ 칸1 FAIL
```

공백은 사이트가 아니라 **내 추출기**가 넣었다(가격과 단위가 다른 태그라 태그→공백 치환
자리에 생긴다). 고친 술어(`[[:space:]]*` 허용)는 **8건**을 내고, **창 전 기준선 파일에는
여전히 0** 을 낸다 ⇒ 느슨해진 것이 아니다.

🔴 **방향이 나빴다.** 이 술어가 만든 것은 「모르겠다」가 아니라 **「604 AC-0 ① 거짓」 이었고,**
604 의 정정은 그 경우 *"그 창에서 오버라이드를 걷어라"* 를 처방한다. 소유자가 다른 재정을
골랐다면 **정규식 하나가 억제 되돌리기를 발동**시켰을 것이다.
[[feedback_my_verification_predicate_is_the_likeliest_defect]]

## ② V4 음성 대조군 — **판정은 살았고 술어가 죽었다**

```
/connect/logout   → 401        /connect/zzz-616(없는 경로) → 401
```

401 은 「엔드포인트가 있다」가 아니라 **`/connect` 접두사가 라우팅된다는 것만** 증명한다.
B1 이 산 것이 정확히 그 라우팅이라 판정은 유지되지만, **술어를 강하게 적었으면 다음 사람이
그것으로 아무 경로나 통과시켰을 것이다.** 🔵 대조군을 안 뒀으면 이 사실을 몰랐다.

🔵 **셋째 함정은 미리 피했다** — 정본 `infra/demo/seed/lib.sh` 를 창 전에 열어 봤고,
로그인 POST 대상을 `auth.hubwang.com` 으로 **박아 두면 안 된다**는 것을 알았다(정본은
호스트 직격 `$iam/login` 을 쓴다 — 경로가 다르다). 홉①의 `Location` 에서 IdP 베이스를
파생하게 고쳤다. 박아 뒀으면 미발효 표면에서 **없는 결함**을 만들었을 것이다.

---

# ✅ AC-2 — 각 티켓에 되돌려 적었다

| 티켓 | 큐 | 무엇을 적었나 |
|---|---|---|
| `TASK-MONO-604` | review | `## CORRECTION #2` — **AC-0 ① PASS**(세 술어 + 대조군), 그리고 내 정규식이 이 칸을 뒤집을 뻔한 경위 |
| `TASK-MONO-586` | review | `## CORRECTION` — **라이브 축 PASS**, 음성 대조군은 틀린 게 아니라 **관측 지점이 얕았다** |
| `TASK-MONO-610` | in-progress | 기동 창 #3 원장 — **AC-3 닫힘(V1–V8 전부)** · AC-4b **fan 발효 / store 미투입** · V4 술어 약화 |
| `TASK-MONO-612` | in-progress | AC-0 ② **미측정 유지, 사유 교체**(500 → discovery 단계) + PASS 의 정의 못박음 |
| `TASK-MONO-615` | in-progress | ⓐ **미측정**, 사유가 「안 걸렸다」에서 **「볼 수 없었다」 로 이동** · 부수 재확인 5건 |
| `TASK-MONO-585` | ready | § 왕복 홉 표가 낡았다 — 500 은 사라졌고 **홉①은 여전히 실패**(사유가 다름) |

🔴 **`604`·`586` 은 `done/` 으로 옮기지 않았다.** AC 는 전부 닫혔지만 **둘 다 CORRECTION 이
코드 작업을 열어 두고 있다**(604 = (z19) 가드 공백 · 586 = 팬 억제 override + (x2) 문구).
`done/` 은 얼려서 아무도 잔여를 다시 안 보므로, 주인이 정해지기 전에는 `review/` 에 둔다.
[[feedback_post_merge_finalize_and_ask]]

---

# 🔴 AC-3 — 이 창이 **안 고치는 것**

## 🙋 소유자 대기

| # | 무엇 | 다음 동작 |
|---|---|---|
| 1 | `kanggle-store` 프로덕션 `OIDC_ISSUER_URL=https://auth.hubwang.com` | 🔴 넣는 것만으로는 **발효 안 된다** — `ignoreCommand` 때문에 **store 경로를 건드리는 배포**가 필요하고, 그 뒤 **기동 창**에서 왕복을 재야 닫힌다 |
| 2 | `TASK-MONO-612` AC-1 방향(`DEMO_PAYMENT_MOCK=1`) | 위 1이 풀린 **뒤에** 판정 가능 |
| 3 | `ADR-MONO-067` 3↔4 순서 재지정 | 이 창의 범위 밖(티켓 Scope 가 명시적으로 제외) |

## 🔴 주인이 없는 일 셋 — **이름을 적는다. 안 적으면 사라진다**

| # | 무엇 | 왜 지금 주인이 없나 |
|---|---|---|
| A | **(z19) 가드 공백** — *"렌더는 초록인데 컨테이너는 도는 상태를 판정하지 못한다"* | `604` 가 `review/`(frozen) 라 AC 를 못 얻는다. 코드 작업이라 이 창 밖 |
| B | **팬 억제 override + (x2) 문구(팬의 극성)** — `ADR-MONO-067` 단계 4 | `586` 이 frozen. 발동 조건도 아직 아니다(이번 창에서 `web.fan-platform`=**307**, 팬은 아직 데모가 서빙) |
| C | **`615` C3 술어** — `git status` 가 mode change 를 content 와 구별 못 한다 | 라이브가 필요 없어 창 밖으로 분류. `615` 는 아직 `in-progress` 라 담을 수는 있다 |

🔴 **A 와 B 는 「두 티켓이 서로에게 떠넘기면 일이 사라진다」의 모양**이다 — 둘 다 frozen 티켓의
CORRECTION 에만 적혀 있고 어느 활성 큐에도 행이 없다. **새 티켓 발의가 필요하다**(이 창의
산출물이 아니라 후속 spec PR).
[[feedback_a_completion_claim_with_no_gate_ships_false]]

## ⚪ 다음 창으로 넘긴 칸 — **그리고 다음 창은 「무엇이 딸려 와야 하는지」까지 적어야 한다**

| 칸 | 다음 창에서 닫으려면 |
|---|---|
| `612` AC-0 ② | 위 소유자 1·2 가 **발효된 뒤**의 창 |
| `610` AC-4b store | 같음 |
| `615` ⓐ rc=124 | 🔴 **창만으로는 부족하다** — **호스트 접근(SSM/ssh) 또는 `journalctl -u demo-stack`** 이 창에 딸려 와야 한다. 이번 창이 배운 것 |

🔵 **AC-0 의 절차는 값을 했다.** 스냅샷 표가 두 군데 틀렸고(store 왕복이라는 **새 칸**이
생겼고, `610` AC-3 은 **창이 필요 없었다**), 그 둘 다 **티켓 본문 AC 절을 열었기 때문에**
나왔다 — INDEX 행에는 어느 쪽도 없었다.

# Task ID

TASK-MONO-667

# Title

🔴 **콘솔 README 는 3개월 전에 없어진 골격을 설명한다** — 그리고 사진이 빈 나머지 둘은 「누락」이 아니라 「막힘」이었다

# Status

in-progress

# Owner

monorepo

# Task Tags

- docs
- portfolio
- readme

---

# Goal

프로젝트 README 들을 **지금의 저장소와 맞춘다.** 소유자 질문(*"각 프로젝트 리드미 최신화
해야되지 않아?"*)에서 출발했고, 재 보니 공백의 모양이 예상과 달랐다.

---

# 🔴🔴 착수 측정이 질문을 다시 썼다 — 그리고 내가 한 번 틀린 수를 보고했다

## 내가 처음 보고한 것: **"스크린샷을 박은 README 0/8"**

**틀렸다.** `![...](...)` 마크다운 문법만 grep 했는데, 이 저장소의 README 들은
`<img src="...">` **HTML 태그**를 쓴다. 다시 세니 **5/8** 이고 박힌 19장이 **전부 실재**한다
(깨진 링크 0).

🔴 이 저장소가 이름 붙인 «부재 판정에 대리지표 금지» 를 내가 밟았다. 그리고 그 틀린 수가
**소유자 결정의 입력이 됐다** — 소유자는 「0/8」을 듣고 «8개 다 써라» 를 골랐고, 정정 뒤
범위를 **다시 골랐다.**

## 다시 센 실제 상태

| 프로젝트 | 스크린샷 | 판정 |
|---|---|---|
| ecommerce 9 · fan 4 · erp 3 · iam 2 | 🟢 | 이미 하우스 스타일. 캡션이 «이 화면이 무엇을 증명하나» 를 말한다 |
| finance | 1 | 🟡 얇다(이번 범위 밖) |
| **platform-console** | 0 | 🔴 **진짜 공백** — 55줄 · 최종수정 2026-06-07 · `## Screenshots` 절 자체가 없음 |
| **wms** (485줄) · **scm** (136줄) | 0 | 🔴 **산문은 충실한데 사진을 «못 쓴다»** |

---

# 🔴🔴 wms·scm 이 비어 있는 것은 누락이 아니었다 — **캡처를 열어서 알았다**

648 이 남긴 90장(`portfolio-captures/`, gitignore)에서 후보를 **열어 봤다.** 이 저장소가
이름 붙인 «찍혔다 ≠ 볼 만한 것이 찍혔다» 때문이고, 실제로 그랬다:

| 캡처 | 왜 못 쓰나 |
|---|---|
| `console__wms__inventory` | 재고 **1행** · 「위치」·「SKU」가 **UUID 원문** |
| `console__wms__outbound` | 출고 **1건** · saga 열이 **`STUCK_RECOVERY_FAILED`** |
| `console__scm__procurement` | 3행·상태 진행은 좋으나 **「공급사」가 UUID 원문** |

🔵 UUID 노출은 **`TASK-MONO-659`** 가 열어 둔 결함이고, 그 티켓이 지목한 다섯 중 셋
(`InventorySnapshotResponse` · `AsnSummaryResponse` · `OrderLineResponse`)이 wms 다.
⇒ **콘솔에서 못 고친다**(없는 값을 그릴 수 없다).

🔴 **그래서 지금 채우면 알려진 결함과 막힌 사가를 포트폴리오에 광고하게 된다.**
스크린샷은 광고이고, 이 저장소는 `capture-shots.mjs` 머리말에서 이미 그 효과를 금지했다.

---

# Scope

## 포함

- `platform-console/README.md` — 현행화 + `## Screenshots` 신설(3장)
- `wms-platform` · `scm-platform` README — **왜 사진이 없는지**를 그 자리에 적는다
- 스크린샷 자산 3장을 `projects/platform-console/docs/screenshots/` 에 커밋

## 제외

- 🔴 **wms·scm 의 스크린샷 자체** — `TASK-MONO-659` 선행. 여기서 고치지 않는다.
- `finance` 보강 · 나머지 5개 README — 이번 범위 밖(이미 하우스 스타일을 갖췄다)
- 🔴 **데모 창** — 필요 없다. 자산은 이미 디스크와 저장소에 있다.

---

# Acceptance Criteria

## AC-0 — 착수 전 재측정

- [x] 🔴 **스크린샷 채택률을 다시 세라.** `![` 만 보지 마라 — 이 저장소는 `<img src=` 를 쓴다.
      → 🟢 **5/8**, 19장 전부 파일 실재. 처음 보고한 「0/8」은 **추출기 오류**였다.
- [x] 🔴 **빈 셋의 캡처를 열어 봐라.** 「사진이 없다」와 「쓸 사진이 없다」는 다른 사실이다.
      → 🔴 **후자였다**(위 § 표). wms·scm 은 막혀 있다.
- [x] 콘솔 README 의 주장들을 실측과 대조하라.
      → 🔴 **「Known Limitations」 네 줄이 전부 거짓**이었다(아래 § ).

## AC-1 — 콘솔 README 가 지금의 콘솔을 설명한다

- [x] `Status` 가 **`skeleton only`** 가 아니다.
- [x] 거짓이 된 「Known Limitations」를 **실측으로 교체**한다.
- [x] 🔴 **도메인 커버리지 표의 합이 실측과 맞는다.** 처음 쓴 표는 합이 **62** 였다
      (`ledger`·`console` 누락). 재계수 **64** 로 고쳤고, 표에 **합계 행**을 넣어
      다음 사람이 검산할 수 있게 했다.
- [x] `## Screenshots` 를 하우스 스타일(`<p align="center">` + 증명하는 캡션)로 신설한다.

## AC-2 — 실은 장을 **열어서** 골랐다

- [x] 🔴 `console__permissions` · `console__operators` 를 **열어서** 확인했다
      (권한 키 13 + 역할 7 with ADR 인용 · 운영자 등록/배정/수명주기). UUID 없음.
- [x] 오늘 촬영분 `/ecommerce/products` 를 쓴다 — 헤더에 **테넌트 `ecommerce`** 가 보여
      「테넌트가 인가 경계」라는 이 콘솔의 요점을 그림 하나로 말한다.

## AC-3 — 막힌 둘은 **사유를 그 자리에** 적는다

- [x] wms·scm README 에 `## Screenshots` 절을 만들되 **⏳ 보류**로 적고 사유를 댄다.
- [x] 🔴 **「누락이 아니라 보류다」를 명시**한다 — 안 적으면 다음 사람이 「빠뜨렸네」로 읽고
      결함이 박힌 캡처를 채워 넣는다.
- [x] `TASK-MONO-659` 를 추적으로 인용한다.

## AC-4 — 남은 것

- [x] ⏳ **659 가 닫힌 뒤** wms·scm 을 재촬영해서 싣는다. 🔴 데모 창이 필요하다.
      → 🔴🔴 **게이트 정정 (2026-09-11, `TASK-MONO-671`): 「659 가 닫힌 뒤」로는 부족하다.**
      실측 — 배포된 AMI 는 `6ae6145db` 이고 그 커밋의 `InventorySnapshotResponse` 에
      `warehouseCode` 가 **0건**이다(`main` 은 1건). 🔵 기전: 데모 AMI 는 **jar 를 구워**
      넣으므로(`COPY build/libs/<svc>.jar`) 새 코드는 **재굽기 → `ami_id` 변경 → apply**
      로만 그 호스트에 도달한다 — **머지는 데모 호스트에 아무것도 안 보낸다.**
      ⇒ 지금 창을 열어 찍으면 **고쳐지기 전 UUID 가 찍힌다.**
      🔴 올바른 게이트는 **「659 + AMI 재굽기」** 이고, 재굽기는 **소유자 승인 사항**이다.
- [ ] ⏳ `finance-platform` 스크린샷 보강(현재 1장).
- [ ] 🔵 **648 의 「README 용 큐레이션」 칸과 겹친다** — 그 칸을 닫을 때 이 티켓의 판정
      («wms·scm 은 659 대기»)을 입력으로 써라. 두 티켓이 같은 질문을 두 번 조사하지 않게.
      → 🔴🔴 **게이트를 둘 다 충족시켰는데도 못 싣는다 — 판정: 여전히 보류, 사유가 바뀌었다.**
      2026-09-12 데모 창에서 659 닫힘 + **AMI 재굽기**(`ami-058f6293d1408f91e`,
      `RepoCommit=8cf474346`) + `terraform apply` 까지 했고, **화면을 열어서** 쟀다:

      | 화면 | 실측 (demo-corp, 2026-09-12) |
      |---|---|
      | `/wms/inventory` | 1행 · 위치·SKU 가 **「이름 확인 불가」** — 🔵 **UUID 는 사라졌다**(`TASK-PC-FE-281`) |
      | `/wms/outbound` | 1행 `SO-DEMO-0001` **SHIPPED** — 🟢 667 이 본 `STUCK_RECOVERY_FAILED` 는 **없어졌다** |
      | `/scm/procurement` | 3행 · 공급사가 **raw UUID** 그대로 |

      🔴 **원인을 못박았다**: `/api/wms/inventory` 응답에 `locationCode`·`skuCode`·`lotNo`·
      `warehouseCode` **필드는 있는데 값이 전부 `null`** 이고, 그 코드를 해석하는 ref 테이블이
      **5종 전부 0건**이다(warehouses·locations·skus·lots·partners). ⇒ 659 의 코드는 **배달됐고
      옳다. 먹일 데이터가 없다.** 집: **`TASK-MONO-675`**.
      🔵 scm 조달의 공급사 UUID 는 **별개 축**(659 의 범위가 아니었다) — 같은 티켓에서 다룬다.
      ⇒ **지금 재촬영해도 포트폴리오에 실을 수 없다.** 그 판단은 이 티켓의 원래 근거
      (*"알려진 결함을 광고하지 않는다"*)와 **같다** — 바뀐 것은 «무엇이 막고 있는가» 뿐이다.

---

# 🔴 콘솔 README 의 거짓 네 줄 — 실측 대조

| README 가 적던 것 | 실측 (2026-09-11) |
|---|---|
| `Status: v1 bootstrap — skeleton only` | `console-web` **68 페이지**(64개가 콘솔 셸 아래) |
| *"셸 미구현 — SSO·카탈로그·테넌트 스위처·도메인 화면은 후속 task"* | 🔴 **전부 있다.** 나는 오늘 그 셸에 로그인해 **테넌트를 두 번 전환하며** 두 화면을 찍었다 |
| *"console-bff — Phase 7 deferred"* | **87 파일** · `settings.gradle:227` 등재 · CI 에 Testcontainers 통합 잡 |
| *"IAM OIDC client 미발행"* | 🔴 오늘 그 클라이언트(`platform-console-web`)로 로그인했다 |
| *"CI / portfolio sync 미등록"* | `ci.yml` 에 `platform-console` 경로필터 + frontend-e2e-smoke 스테이지 |

🔵 **이 README 는 자기 프로젝트를 실제보다 훨씬 작게 보이게 하고 있었다.** 콘솔은 이
저장소에서 **화면 수가 가장 많은 표면**인데(68), README 는 *"디렉토리 + spec skeleton"* 이라
적었다. 포트폴리오 관점에서 이것이 이 티켓의 가장 큰 손실이다.

---

# Related Specs / Contracts

- `ADR-MONO-013` — 콘솔 foundation (Model B · 8-phase roadmap)
- `TASK-MONO-659` — UUID 노출 5곳 (wms·scm 스크린샷의 **선행**)
- `TASK-MONO-648` — 촬영 자산과 큐레이션 (이 티켓의 자산 출처)
- `projects/platform-console/specs/contracts/console-integration-contract.md`

---

# Edge Cases

- **659 가 닫혀도 시드가 얇으면 여전히 못 쓴다** — wms 는 **두 축**이 겹쳐 있다(UUID + 1행).
  🔴 659 만 보고 「이제 찍어도 된다」로 가지 마라.
- **README 의 숫자는 게이트가 없다** — 68·64 는 오늘 실측이고 라우트가 늘면 낡는다.
  🔵 그래서 **합계 행**을 넣었다(검산이 가능해야 고쳐진다).
- **캡처를 다시 찍으면 파일명이 같아 diff 가 안 보인다** — 이미지 diff 는 리뷰를 못 받는다.
  교체 시 캡션도 함께 검토하라.

---

# Failure Scenarios

1. 🔴🔴 **wms·scm 의 ⏳ 절을 「할 일」로 읽고 결함 캡처를 채워 넣는다.** 그 절이
   「누락이 아니라 보류」라고 적는 이유가 이것이다.
2. 🔴 **콘솔 README 의 숫자만 고치고 「Known Limitations」를 남겨 둔다.** 그 네 줄이
   프로젝트를 실제보다 작게 보이게 한 주범이었다.
3. **하우스 스타일을 새로 발명한다.** 5개가 이미 `<p align="center">` + 증명하는 캡션을
   쓴다. 여섯 번째가 다른 모양이면 그때부터 스타일이 둘이 된다.
4. 🔴 **추출기를 안 맞추고 「0건」을 보고한다.** 이 티켓 자신이 그 실수로 시작했다.

---

# 분석 / 구현 권장

(분석=Opus 5 / 구현 권장=Sonnet — 문서 작업이고 판정은 이미 이 티켓에 적혔다)


---

# 🔵 2026-09-12 데모 창 — 이 티켓의 게이트가 **필요조건이었지 충분조건이 아니었다**

`TASK-MONO-671` 이 이 티켓의 게이트를 「659 가 닫힌 뒤」에서 **「659 + AMI 재굽기」** 로
정정했다. 그 정정은 **옳았다** — 재굽기 없이는 옛 jar 가 돌아 필드 자체가 없었을 것이다.
🔴 그런데 **재굽기를 하고 나니 다음 벽이 나왔다**: 필드는 생겼고 값이 null 이다.

🔵 **이것이 «게이트를 정정한 티켓»의 한계다** — 한 겹을 벗기면 다음 겹이 보인다.
`TASK-MONO-675` 가 그 겹을 받는다. 이 티켓은 **그 티켓이 닫힐 때까지 보류**다.

🔵 곁가지 수확: 같은 창의 전량 촬영이 **콘솔 정적 라우트를 69개로 다시 셌다**
(이 티켓은 68 로 적었다). 라우트는 늘고 준다 — 게이트 없는 숫자는 낡는다.

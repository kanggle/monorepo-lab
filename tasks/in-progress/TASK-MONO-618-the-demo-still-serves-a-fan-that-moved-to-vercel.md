# Task ID

TASK-MONO-618

# Title

🔴 **`ADR-MONO-067` 단계 4 의 억제 몫 — 데모가 Vercel 로 옮겨간 팬을 계속 서빙한다.**
🔴🔴 그리고 형제(`604`)의 처방을 **그대로 복사하면 정반대를 단언한다** — 결제 mock 의 극성이 반대다.

# Status

in-progress

# Owner

monorepo

# Task Tags

- demo
- adr-followup
- guard

---

# 🔎 어디서 왔나 — **두 티켓이 서로에게 떠넘겨 사라질 뻔한 일이다**

`TASK-MONO-612` AC-3 이 이 일을 미리 세어 `TASK-MONO-586` 에 CORRECTION 으로 적었다
(*"이 티켓이 해야 하는 것 (AC 에 넣어라)"*). 🔴 **그런데 586 은 `review/`(frozen) 이라 AC 를
못 얻는다.** 그래서 그 셋은 **어느 활성 큐에도 행이 없는 채로** 남아 있었다.
`TASK-MONO-616` § AC-3 이 그 공백을 이름으로 적었고 이 티켓이 그 행이다.
[[feedback_one_fact_in_two_sections_only_one_gets_fixed]]

## 왜 586 이 이것을 못 하나

`ADR-MONO-067` 단계 **2**(web-store)는 **세 티켓**이 나눠 들었다 — 해석기(`580`) ·
**억제**(`604`) · 링크/가시성(`583`·`603`). 586 은 단계 4 의 **해석기 몫**이고,
**억제 몫에 해당하는 티켓이 없다.** 이 티켓이 `604` 의 팬 판이다.

---

# 🔵 선행 조건이 **막 충족됐다** (2026-09-03 실측)

`604` 의 AC-0 ① 이 세운 규칙: *"Vercel 판이 죽어 있는데 데모 사본을 끄면 방문자는 **어느
쪽에서도** 그 화면을 못 본다."* 팬에 대한 그 선행이 **`TASK-MONO-616` 기동 창 #3 에서
처음으로 충족됐다**:

```
POST https://fan.hubwang.com/api/auth/signin/iam
  → 302 https://auth.hubwang.com/oauth2/authorize?client_id=fan-platform-user-flow-client…
  → /login 200 (4,247 B) → 자격 제출 → 콜백
GET /api/auth/session → accountId · tenantId=fan-platform · roles=["FAN"]
```

⇒ `OIDC_ISSUER_URL` 이 발효됐고 **D4 축이 팬 표면에서 닫혔다.** 586 이 *"팬의 익명 도달
페이지는 11개 중 1개(`/login`)뿐이고 나머지는 **D4** 가 풀려야 보인다"* 라고 적은 그 D4 다.

🔴 **그러나 「로그인이 된다」는 「화면들이 데이터를 낸다」가 아니다.** 616 창 #3 은 왕복까지만
쟀고 **로그인 뒤 화면은 안 쟀다.** ⇒ **AC-0 이 그것부터 잰다.** 형제 축에서 정확히 이
구별이 문제였다 — `604` 는 `/products` 가 **200 인데 에러 문구를 렌더**하고 있었다.
[[feedback_a_verifiable_mechanism_is_not_the_cause]]

---

# 🔴🔴 형제와 **다른 점** — 극성이 반대다

`infra/demo/demo.env:279-285` 가 근거다:

| | 기본 | mock 이 되려면 | 불변식 |
|---|---|---|---|
| ecommerce | 실 Toss | `demo-pg` 프로파일을 **켠다** | 프런트 플래그 ON ⟺ `demo-pg` **ON** |
| **fan** | **목**(`MockPaymentGatewayAdapter` = `@Profile("!portone")`) | 아무것도 안 켠다 | 프런트 플래그 ON ⟺ `portone` **OFF** |

⇒ 가드 **(x2)** 는 (x) 의 사본이 아니다. 기대값이 `DEMO_PAYMENT_MOCK=1` 인 것은 같지만
**그 근거가 되는 백엔드 조건이 반대**다. 🔴 (x) 의 미집행 문구에 `demo-pg` 를 적으면
팬 축에서 **거짓**이 된다. [[feedback_grep_the_siblings_before_fixing_it_yourself]]

🔵 현재 (x2) 의 실제 술어(`verify-demo-wrapper.sh:1249-1257`):
`SPRING_PROFILES_ACTIVE` 에 `portone` 이 있으면 `x2_real=1`, `x2_back_mock = 1 - x2_real`,
그리고 `x2_back_mock == x2_front`(= `DEMO_PAYMENT_MOCK`)를 요구한다.
**팬 프런트가 렌더에서 사라지면 이 등식의 한쪽이 없어진다.**

---

# Goal

데모가 `web.fan-platform.<DEMO_DOMAIN>` 을 **서빙하지 않게** 하고, 그 사실을 가드가
**실행해서** 대조하며, 그 변경이 **(x2) 를 공허하게도 영구 빨갛게도** 만들지 않게 한다.

# Scope

- **포함**: 팬용 억제 override 선언 + `COMPOSE[fan]` 체인 등록 · (z19) 형태의 가드(팬 판) ·
  **(x2) 의 미집행 분기를 팬의 극성으로** · Traefik alias · 부팅 판정/론처 링크 정합
- **제외**: `kanggle-fan` 의 Vercel env 투입(🙋 소유자) — 단, AC-0 이 **필요 여부를 실측**한다
- **제외**: 억제 **런타임** 판정 — `TASK-MONO-617` 의 몫. 🔵 그 티켓이 술어를 선언에서
  유도하도록 요구하므로, 이 티켓의 억제 선언은 **자동으로 그 모집단에 들어가야 한다**
- **제외**: console(단계 3)

---

# Acceptance Criteria

## AC-0 — 🔴 **전제를 잰다. 「로그인이 된다」로 넘어가지 마라** (착수 전)

1. **`fan.hubwang.com` 의 로그인 뒤 화면이 실데이터를 내는가.** 🔴 상태코드 금지 —
   `604` 가 정한 형태로: 에러 문구 **부재** + 도메인 개체(아티스트/구독 등) **≥1**.
   🔵 **음성 대조군**: `/zzz-nope-618`. 🔴 상태코드·바이트로는 `/` 와 구별되지 않는다 —
   `Location` 의 `?from=` 을 읽어라(616 창 #3 이 고친 술어).
   ⇒ **거짓이면 억제하지 마라.** 방문자가 어느 쪽에서도 팬을 못 보게 된다.
2. **그 compose 를 읽는 소비자를 전수한다.** 🔴 `604` AC-0 ② 가 이 자리에서 **티켓이 적은
   둘 중 하나가 틀렸고 하나가 빠진 것**을 찾았다 — 상속하지 말고 다시 세라
   (`infra/demo/projects.sh` · 로컬 `npm run` · `.github/workflows/*.yml` 전수).
3. **`kanggle-fan` 프로덕션 env 에 `DEMO_PAYMENT_MOCK` 이 있는가**
   (2026-09-02 실측: `NEXTAUTH_SECRET`·`NEXTAUTH_URL`·`OIDC_CLIENT_ID`·`OIDC_CLIENT_SECRET`
   **네 개뿐 — 없다**. 🔴 그 뒤 `OIDC_ISSUER_URL` 이 추가됐으므로 목록은 **낡았다**).
4. 🔴 **`TASK-MONO-617` 의 상태를 확인한다** — 먼저 갔으면 이 티켓의 억제 선언이 그
   런타임 판정의 모집단에 **자동으로** 들어가는지 확인하고, 안 들어가면 그것이 결함이다.

---

## 🔴🔴 AC-0 ① **개정** (2026-09-04) — 「착수 전」은 **원리적으로 충족 불가**다

이 AC 를 쓸 때 나는 형제(`604`)가 **같은 자리에서 이미 넘어졌다는 것**을 안 옮겼다.
604 는 자기 AC-0 ① 에 대해 이렇게 정정해 뒀다:

> ### ⇒ 🔴 AC-0 ①은 **데모가 꺼져 있는 동안 원리적으로 참이 될 수 없다**
> ### ⇒ 정정: **AC-0 ①의 판정 시점은 AC-4 와 같은 창이다**

**팬 축에도 그대로 걸린다.** `fan.hubwang.com` 은 Vercel 이 서빙하지만 데이터를 **데모
백엔드**에서 받는다 ⇒ 데모가 `stopped` 인 동안 이 술어는 항상 「데이터 없음」을 낸다
(2026-09-04 실측: `auth.hubwang.com` discovery **503**, 해석기가 «꺼져 있음» 페이지를 낸다).
즉 **「착수 전」에 이 칸을 참으로 만들 방법이 없다.**

### ⇒ 개정 (🙋 소유자 승인, 2026-09-04)

| | 원문 | **개정** |
|---|---|---|
| 판정 시점 | 「착수 전」 | 🟢 **AC-5 와 같은 창. 그 창에서 «맨 먼저».** |
| FAIL 처분 | *"거짓이면 억제하지 마라"* | 🟢 **별 티켓 = 억제 되돌리기(재굽기 1회 필요)** |

🔴 **처분을 바꾼 이유를 적는다 — 「완화」가 아니라 「그 시점에 불가능」이기 때문이다.**
그 창에서는 억제가 **이미 AMI 에 구워져 있다**. 616 § AC-0 ④ 「가」 가 형제 축에서 실측한
그대로다: *"걷어도 즉시 복구되지 않는다 … 되돌리기는 또 한 번의 재굽기를 요구한다."*
⇒ *"억제하지 마라"* 는 그 자리에서 **집행할 수 없는 처방**이고, 집행할 수 없는 처방을
남겨 두면 다음 사람이 그것을 **했다고 착각**한다.

🔵 **위험이 제한적인 이유(그리고 그것이 이 개정의 근거는 아니다)**: `web.fan-platform.<DEMO_DOMAIN>`
은 **IP 파생 주소**라 창 밖에서는 방문자에게 애초에 존재하지 않는다. ⇒ 억제가 실제로
지우는 것은 «창이 열린 동안의 데모 사본» 뿐이다. 🔴 **그래도 이것을 근거로 AC-0 을 없애지
않았다** — 재는 것과 안전한 것은 다른 축이고, 이 칸은 여전히 **재야 한다.**

---

# ✅ AC-0 ②③④ — **실측 완료 (2026-09-04, 창 없이)**

🔵 ②③④ 는 라이브가 필요 없다 ⇒ **창을 기다리게 두지 않았다.** ① 만 창의 칸이다.

## ② compose 소비자 전수 — 🔵 **억제는 데모 체인 밖으로 안 샌다**

| 소비자 | 무엇을 하나 | 억제 영향 |
|---|---|---|
| `infra/demo/projects.sh:50` `COMPOSE[fan]` | 데모 체인 (base + `fan-identity.override.yml`) | 🟢 **여기에만 얹는다** |
| `package.json:41-45` `fan-platform:up/down/ps/logs/docker` | `--project-directory projects/fan-platform` = **base 단독** | ✅ 영향 없음 |
| `.github/workflows/ci.yml:2572-2701` Frontend E2E smoke | 🔴 **compose 를 안 쓴다** — `pnpm --filter fan-platform-web build` + Playwright 직접 | ✅ 영향 없음 |
| `projects/*/apps/web-store/.next/standalone/package.json` | 🔵 **소비자가 아니다** — Next 빌드 산출물에 복사된 `package.json` 사본 | ✅ 무관 |

🔴 **④번 행이 604 가 경고한 자리다.** grep 은 `fan-platform:up` 을 **10건** 잡는데 그중
**5건이 빌드 산출물의 사본**이다 — 세면 소비자가 두 배로 부풀고, 그 부풀린 수가
「영향 범위가 넓다」는 **틀린 판단**을 만든다. 🔵 CI 는 걸리긴 하는데 **compose 경로가
아니라서** 영향이 없다 — 「걸린다」와 「영향받는다」를 따로 판정해야 나오는 답이다.

## ③ `kanggle-fan` 프로덕션 env — 🔴🔴 **소유자 항목이 하나 나왔다**

실측 (2026-09-04, `vercel env ls production --scope khakiman-projects --project kanggle-fan`):

| # | 이름 | 나이 |
|---|---|---|
| 1 | `OIDC_ISSUER_URL` | 1d — 🔵 `616` 창 #3 이 발효를 확인한 그 값 |
| 2 | `OIDC_CLIENT_SECRET` | 8d |
| 3 | `OIDC_CLIENT_ID` | 8d |
| 4 | `NEXTAUTH_SECRET` | 8d |
| 5 | `NEXTAUTH_URL` | 8d |

⇒ **다섯 개. `DEMO_PAYMENT_MOCK` 은 없다.** 🔵 티켓이 적은 「네 개」는 예상대로 낡았고
(`OIDC_ISSUER_URL` 이 그 뒤 추가됐다), **결론은 안 바뀐다.**

### 🔴🔴 그런데 이 부재가 **억제와 함께 결함이 된다** — AC-0 이 물으라고 한 「필요 여부」의 답

지금은 팬 프런트가 **둘**이다(데모 사본 + Vercel). 데모 사본은 `demo.env` 에서
`DEMO_PAYMENT_MOCK` 를 받아 `(x2)` 등식을 만족한다. **억제하면 Vercel 판이 유일한 팬이
되는데, 그쪽에는 그 값이 없다.** 팬의 극성상 백엔드는 **목이 기본**(`@Profile("!portone")`)
이므로 결과는 이 티켓이 § 형제와 다른 점 에 직접 인용해 둔 그 상태다:

> *"프런트만 꺼짐 = 백엔드 목이 승인할 준비가 됐는데도 프런트가 'PortOne 키 미설정' 으로
> 요청 자체를 보내지 않습니다 (`TASK-FAN-FE-015` 가 고친 상태가 정확히 이것입니다)."*

⇒ 🙋 **소유자 항목**: `kanggle-fan` 프로덕션에 **`DEMO_PAYMENT_MOCK=1`** 투입.
🔴 **이것은 이 티켓의 Scope 밖이지만**(§ 제외 1행), **억제와 같은 창에서 판정돼야 한다** —
안 넣고 억제하면 팬 구독 결제가 **살아남는 표면에서** 죽는다.
🔵 **`612` AC-1 의 팬 판**이다(그쪽은 `kanggle-store`, 이쪽은 `kanggle-fan`).
🔴 그리고 **`AC-0 ①` 의 술어에 결제 화면을 넣지 마라** — 그 칸은 «화면이 데이터를 내는가»
이고, 결제는 **이 값이 들어간 뒤**에야 재는 별 축이다. 두 축을 한 칸에 넣으면 어느 쪽이
FAIL 인지 귀속되지 않는다. [[feedback_control_group_design_four_axes]]

## ④ `617` 모집단 자동 편입 — 🟢 **결함 없음 (소스로 확인)**

`617` 은 **먼저 갔다**(`#3613` 머지, `main` `9271aec32`). 그 판정자의 모집단 유도
(`infra/demo/check-suppressed-containers.sh:117-150`):

```
억제된 서비스 = 렌더(체인 − infra/demo/*.override.yml) − 렌더(체인)
```

⇒ `infra/demo/fan-vercel.override.yml` 을 `COMPOSE[fan]` 에 등록하면 **이름을 어디에도
안 적어도** 그 판정자의 모집단에 들어간다. 🔵 `[fan]` 은 이미 `fan-identity.override.yml`
로 `has_override=1` 이지만 그 파일은 서비스를 **안 지우므로** `SUPP` 가 비어 `continue`
된다 — 즉 지금은 **모집단 0**, 억제를 얹으면 **1** 이 된다.

🔴 **부수 확인 하나** — 그 스크립트는 오버라이드가 서비스를 **추가**하면 경고한다
(유도의 전제 자체검사). `fan-vercel.override.yml` 은 `profiles:` 만 **더하는** 파일이라
서비스를 추가하지 않는다 ⇒ 전제 유지. **구현 후 `--derive` 로 실행해 확인한다**(AC-2).

🔵 그리고 617 의 라이브 칸(*"부팅 로그에 `[suppressed] OK …`"*)은 지금 **모집단 1**
(`ecommerce/web-store`)로 잡혀 있다 — 이 티켓이 랜딩하면 **2** 가 된다. 창 #4 매니페스트가
그 수를 **미리** 적어야 한다(`TASK-MONO-621`).

---

## AC-1 — 억제는 **데모 프로파일에서만**

- 로컬 `docker compose up` · 로컬 워크스루 · CI 는 **영향 없음**
- 🔴 억제 선언은 **한 곳**에서 읽혀야 한다. 두 벌이면 하나만 고쳐진다.
- 🔵 기전은 형제와 같은 모양이 자연스럽다(데모 오버라이드가 `profiles:` 를 «추가»).
  🔴 그러나 `604` 가 다른 후보를 버린 이유를 **팬에서 다시 확인**하라 — `deploy.replicas: 0`
  은 렌더에 남고, `--scale` 은 렌더에 안 보이며, base 의 `profiles:` 는 로컬을 바꾼다.

## AC-2 — 가드가 **실행해서** 대조한다 (팬 판 (z19))

`604` AC-2 의 다섯 칸을 팬으로: **체인 등록 · 주입 확인 · 판정 · 대조군(base 단독) · 범위(정확히 1개)**
\+ 🔴 **바닥**(렌더가 깨지면 목록이 통째로 비고 그 0행은 «억제됨»과 구별되지 않는다)
\+ 유령 참조(`depends_on: fan-platform-web`) 0건.
🔴 **bite**: 억제를 지우면 문다.

## AC-3 — 🔴🔴 **(x2) 를 팬의 극성으로** 고친다

(x) 가 ecommerce 에서 한 것과 **같은 형태**, **반대 극성**:

- 팬 프런트가 렌더에 없으면, 그 부재가 **선언된 억제 때문인지** 먼저 확인한다
  (체인에 오버라이드가 있는가). 아니면 **FAIL** — 「누가 지웠는지 모르는」 상태를 통과시키지 않는다.
- 맞으면 **백엔드 절반만** 재고(`portone` **부재** ⇒ 목), 미집행 문구를 매 실행마다 찍는다.
  🔴 문구에 **`demo-pg` 를 쓰지 마라** — 팬의 조건은 `portone` **OFF** 다.
- 🔴 문구는 **누가·언제·무슨 명령·기대값·원장**을 담는다. 명령은 `--project kanggle-fan`,
  원장은 팬 쪽 `VERCEL.md`.
- 🔴 **아무것도 안 하고 문구만 지우는 것은 금지** — 그러면 공백이 조용해진다.

## AC-4 — 부팅 판정 · alias · 링크 정합

- 🔴 `demo-up.sh` 의 **`HTTP 표면 2/2`** 는 `console` 과 **`web.fan-platform`** 이다
  (616 창 #3 실측: `[demo] ✔ HTTP 표면 2/2: console=307 web.fan-platform=307`).
  억제하면 **찌를 수 있는 표면이 1개가 된다** ⇒ **두 상수를 같이 고쳐야 한다**(실측 확인):

  | 상수 | 지금 | 무엇인가 | 억제 뒤 |
  |---|---|---|---|
  | `SURFACE_ROW_FLOOR` (`demo-up.sh:507`) | **3** | 론처가 약속하는 화면의 **총 수**(console · web.ecommerce · web.fan-platform) | 팬 행도 Vercel 이 되면 재검토 |
  | `SURFACE_FLOOR` (`demo-up.sh:513`) | **2** | 그중 **데모가 서빙해서 찌를 수 있는** 수 | **1** |

  🔴 안 고치면 부팅 완료 판정이 **영구히 실패**한다. `TASK-MONO-583` 이 형제 축에서 미리 낸
  길이고, `604` AC-3 이 *"583 이 없었다면 이 티켓이 부팅을 영구히 못 끝내게 만들었을 것"*
  이라고 적은 그 자리다. 🔵 두 상수는 **환경변수로 덮을 수 있게** 돼 있으나
  (`DEMO_SURFACE_*`) 기본값을 안 고치면 데모 호스트가 그대로 문다.
- Traefik 의 손관리 alias 목록에서 **`web.fan-platform.${DEMO_DOMAIN:-local}`**
  (`infra/traefik/docker-compose.yml:93`) 을 뺀다 — 라우터가 사라지면 가드 **(i)** 가
  «고아 alias» 로 문다(`604` 가 실측). 🔵 같은 파일 **:84** 이 이미
  *"형제 `web.fan-platform` 은 단계 4"* 라고 이 티켓을 기다리고 있다.
- 론처/부팅 로그가 그 호스트를 안내하고 있으면 **거짓이 된다** — 「팬은 Vercel」로 바꾼다.
  🔵 론처(`infra/demo/aws/site/index.html`)의 `data-surface` 행이 `SURFACE_SRC` 이므로
  **두 벌이 아니라 한 벌**이다 — 거기를 고치면 부팅 판정이 따라온다(`demo-up.sh:488` 주석).

## AC-5 — 라이브 확인 (**다음 기동 창**)

- `web.fan-platform.<DEMO_DOMAIN>` → **404**
- 🔴 **상태코드만으로 판정하지 마라** — `docker ps -a` 로 **컨테이너 부재**를 같이 본다.
  `TASK-MONO-610` 창 #1 이 정확히 그 오독을 했고, `615` C1 이 두 축으로 고쳤다.
  🔵 **양성 대조군**: 억제 안 된 화면(`console`)이 **307** 이어야 한다.
- 🔴 이 칸은 **재굽기가 선행**이다(억제는 AMI 의 compose 에 들어가야 효력이 있고,
  그보다 오래된 컨테이너는 `--remove-orphans` 로도 안 지워진다). **이 티켓만을 위해 창을
  열지 마라** — 번들에 묶어라.
- 🔴 그리고 그 창의 매니페스트에 이 칸을 **미리 적어라**(`TASK-MONO-616` 의 교훈).

## AC-6 — 이 구현이 **안 고치는 것**을 적는다

소유자 대기(팬 Vercel env) · 억제의 런타임 판정(`617`) · console(단계 3) · **(f) 벽**.

---

# Related Specs

- `TASK-MONO-586` § CORRECTION (2026-09-02) — 이 일의 원문 세 줄 · § CORRECTION (2026-09-03) 라이브 축 PASS
- `TASK-MONO-612` § AC-3 — 이 일을 미리 센 티켓 · § (x2) 극성
- `TASK-MONO-604` — **형제 판(단계 2 의 억제 몫)**. AC-1·AC-2 의 형태를 그대로 쓴다
- `TASK-MONO-583` — 부팅 판정이 먼저 길을 낸 선례
- `TASK-MONO-616` § AC-1 칸2 · § AC-3 — 선행 충족의 실측과 이 티켓의 발의 근거
- `TASK-MONO-617` — 억제의 런타임 판정(모집단이 이 티켓의 선언을 포함해야 한다)
- [`ADR-MONO-067`](../../docs/adr/ADR-MONO-067-demo-surfaces-served-from-vercel.md) § 단계 4
- `infra/demo/demo.env:276-285` — 두 도메인의 극성이 적힌 자리

# Related Contracts

없음.

# Edge Cases

- 🔴 **팬은 로그인 뒤에야 화면이 열린다** — 익명 도달 페이지가 `/login` 하나뿐이라, AC-0 ①
  을 익명으로 재면 **항상 「데이터 없음」**이 나온다. 세션을 들고 재라.
- 🔴 **`DEMO_PAYMENT_MOCK` 는 한 값, 두 프런트**(`demo.env:279`). 팬만 보고 값을 바꾸면
  ecommerce 쪽 (x) 가 문다.
- 🔵 **로컬은 hosts 파일로 `web.fan-platform.local` 을 해소**하므로 alias 제거의 영향이 없다
  (형제 축에서 확인된 성질). 🔴 그래도 **다시 확인**하라 — 상속하지 마라.
- 🔴 억제 뒤 시드가 **없는 표면**을 가리키는 `redirect_uri` 를 매 부팅 등록할 수 있다
  (`604` 가 ecommerce 에서 «무해하지만 이름은 적는다» 로 남긴 것). 팬 판을 확인하고 적어라.

# Failure Scenarios

| 상황 | 잘못된 처리 | 옳은 처리 |
|---|---|---|
| 「로그인이 되니 Vercel 판은 산다」 | AC-0 ① 을 건너뛴다 | 🔴 **화면이 데이터를 내는지**를 따로 재라 — 200 이 에러 문구일 수 있다 |
| (x) 의 문구를 (x2) 로 복사 | `demo-pg` 를 적어 **정반대를 단언** | 🔴 팬은 `portone` **OFF** 가 mock 조건이다 |
| 부팅 지문 `2/2` 를 안 고침 | 부팅이 **영구 실패** | 🔴 억제와 **같은 PR** 에서 하한·판정을 고쳐라 |
| `web.fan-platform` 404 만 보고 통과 | 되살아난 컨테이너의 404 와 구별 불가 | 🔴 **`docker ps -a` 로 부재**를 같이 봐라 + 양성 대조군 |
| alias 를 남겨 둠 | 가드 (i) 가 «고아 alias» 로 문다 | 🔴 라우터와 alias 를 **같이** 지운다 |
| 이 티켓만을 위해 창을 연다 | 예산 낭비 | 🔴 **재굽기 번들에 묶고**, 그 창의 매니페스트에 미리 적어라 |

---

---

# ✅ 구현 (2026-09-04 UTC) — AC-1 ~ AC-4 · AC-6

🔵 AC-0 ②③④ 는 **창 없이** 이미 닫았다(위 § AC-0 ②③④). AC-0 ① 과 AC-5 는 **기동 창 #4**
(`TASK-MONO-621`)의 칸이고, 그 창은 이 구현이 **AMI 에 구워진 뒤**에 열린다.

## AC-1 — 억제는 데모 프로파일에서만 · **선언은 한 곳**

`infra/demo/fan-vercel.override.yml` (신규) 한 파일 + `projects.sh` 의 `[fan]` 체인 한 줄.
기전은 형제와 같다 — 오버라이드가 `profiles: [vercel-migrated]` 를 **추가**하면 그 서비스는
렌더에서 빠지고, 그 프로파일은 아무도 켜지 않는다.

🔴 **다른 후보를 팬에서 다시 확인했다**(상속하지 않았다 — AC-1 이 그것을 요구한다):
`deploy.replicas: 0` 은 렌더에 남고 · `--scale` 은 `docker compose config` 에 안 보이고 ·
base 의 `profiles:` 는 로컬을 바꾼다. 셋 다 형제 축과 같은 이유로 탈락한다.

### 실측 — 기전이 실제로 먹는다 (2026-09-04, 데몬 없이 `config --services`)

| 렌더 | 서비스 수 | `fan-platform-web` |
|---|---:|---|
| 데모 체인 − 억제 | **9** | **있음** ← 주입 확인 |
| 데모 체인 + 억제 | **8** | **없음** ← 판정 |
| 로컬 base 단독 | **9** | **있음** ← 🔵 대조군 (Scope Out 안 넘었다) |

🔵 **로컬 hosts 영향 없음을 다시 확인했다** — 억제는 데모 체인에만 있으므로
`pnpm fan-platform:up` 은 base 단독으로 돌아 그대로 9서비스다.

## AC-2 — 가드가 **실행해서** 대조한다 · 🔴🔴 사본이 아니라 **함수**로 만들었다

`604` AC-2 의 다섯 칸을 팬으로 옮기는 방법이 둘이었다:

1. **(z19) 를 90줄 복사해 (z28) 을 만든다** ← 티켓이 문자 그대로 요구한 것
2. **로직을 함수로 뽑고 축마다 호출한다** ← 채택

🔴 **1을 안 고른 이유**: 이 저장소가 반복해서 당한 모양이 정확히 그것이다 —
**한 사실이 두 절에 있으면 한쪽만 고쳐진다.** 칸 하나를 고칠 때 사본이 안 따라오면,
안 따라온 쪽은 **초록인 채로 아무것도 안 잰다.** 단계 3(console)이 오면 이제 **한 줄**이다.

🔴 **그렇다고 모집단을 «전자동 유도» 로 하지도 않았다.** 617 의 런타임 판정자는 유도해도
되지만(그쪽 술어는 «존재/부재» 하나다) 여기는 **축마다 다른 것**이 필요하다 — 기대 서비스
이름 · base compose 경로 · 바닥 · 로컬 사용처 문구 · Vercel 주소. 그걸 유도하려 들면 술어가
**파일명 규약**에 의존하게 되고, 규약이 어긋난 날 가드는 **모집단 0 으로 조용히 통과**한다.
🔵 대신 **호출 수의 바닥(`z19_axes_floor=2`)** 을 뒀다 — 축이 조용히 사라지는 것은 그것이 막는다.

### 🔴🔴 bite 가 **가드의 결함 하나**를 찾았다 — 칸 순서가 진단을 가리고 있었다

bite ③(base 에 `profiles:` 주입 = 이 함수가 잡아야 하는 **대표 사고**)이 **WRONG-CELL** 로
나왔다. 물기는 물었는데 **다른 칸**이 물었다:

```
기대: "**로컬(base 단독) 렌더에서도** fan-platform-web 가 사라졌습니다"
실제: "억제 파일을 **뺀** 렌더에도 fan-platform-web 가 없습니다"
```

base 를 건드리면 **세 렌더에서 동시에** 사라지므로 두 칸이 같이 참이 되는데, 옛 순서는
**덜 구체적인 쪽을 먼저** 보고했다. 그 문구는 진짜 원인(«base 를 건드렸다»)을 각주로만 언급한다.
🔵 이건 형제 (z19) 에 **처음부터 있던** 성질이다 — 팬 축을 만들지 않았으면 안 드러났다.

⇒ **대조군(로컬) 칸을 먼저** 두도록 고쳤다. 두 칸의 분업이 이제 정확하다:

| 관측 | 진단 |
|---|---|
| 로컬에도 없다 | **base 를 건드렸다** (Scope Out 위반) |
| 로컬엔 있는데 억제전 렌더에 없다 | **다른 데모 오버라이드**가 지웠다 (그 목록을 문구에 찍는다) |

🔴 **첫 원인만 보고하고 둘째를 가리는 것**은 이 저장소가 반복해 당한 모양이고, 이번엔
**bite 가 그것을 잡았다.** 기대와 다르면 기대를 고치는 게 아니라 **어느 쪽이 옳은지**를 봐야 한다.

### bite 5/5 — 🔴 매번 **주입을 먼저 단언**했다

| # | 주입 | 기대 | 결과 |
|---|---|---|---|
| ① | 억제 파일의 `profiles:` 제거 | (z28) 이 «억제가 안 걸렸습니다» | 🟢 PASS |
| ② | `[fan]` 체인 등록 제거 | (z28) 이 «체인에 등록돼 있지 않습니다» | 🟢 PASS |
| ③ | base 에 `profiles:` 주입 | (z28) 이 «로컬(base 단독)에서도 사라졌습니다» | 🟢 PASS *(순서 수정 후)* |
| ④ | 축 등록 줄(z28) 무력화 | 바닥이 «억제 축이 1개만 등록됐습니다» | 🟢 PASS |
| ⑤ | **형제** ecommerce 체인 등록 제거 | (z19) 가 여전히 문다 (회귀 없음) | 🟢 PASS |

🔴 **하네스 자체에서 내 술어가 세 번 틀렸다** — 전부 가드가 아니라 **내 판정기**가 문제였다:

1. **인터프리터가 달랐다.** Python 의 `subprocess` 에서 `bash` 는 이 호스트에서 **WSL**
   로 해소된다(`PWD=/mnt/c/...`) — msys Git Bash 가 아니다. 경로·docker 의미가 달라
   가드가 **베이스라인에서 빨갛게** 나왔고, 그것은 진짜 가드 실패와 **구별되지 않았다.**
   ⇒ 인터프리터를 `C:\Program Files\Git\bin\bash.exe` 로 **핀**했다.
2. **주입 사후 술어 두 번 오답** — `count(old)==0` 은 주입이 앵커를 **연장**할 때 거짓이 되고
   (bite ③), `count(new)==1` 은 `new` 가 흔한 문자열일 때 거짓이 된다(bite ② 의 `new` 는
   따옴표 한 글자). ⇒ 증명해야 하는 것은 **«쓰기가 디스크에 닿았고 파일이 바뀌었다»** 이고,
   그렇게 고쳤다.
3. **크래시가 트리를 오염시켰다.** assert 가 던지면서 주입이 남았고, **다음 실행이 그것을
   진짜 가드 실패로 읽었다.** ⇒ `except: restore(); raise`. 🔴 복구는 **인메모리 사본**으로만
   한다 — `git checkout --` 은 이 저장소에서 미커밋 작업을 지운 전력이 있다.

## AC-3 — (x2) 를 **팬의 극성으로**

- 팬 프런트가 렌더에 없으면 **그 부재가 선언된 억제 때문인지 먼저 확인**한다
  (`COMPOSE[fan]` 에 오버라이드가 있는가). 아니면 **FAIL** — 「누가 지웠는지 모르는」 상태를
  통과시키지 않는다. (형제 (x) 와 같은 형태.)
- 맞으면 **백엔드 절반만** 잰다: `portone` **부재** ⇒ 목. 🔴 문구에 **`demo-pg` 를 쓰지 않았다**
  — 팬의 조건은 `portone` **OFF** 다.
- 문구는 **누가·언제·무슨 명령·기대값·원장**을 담는다: 명령 `vercel env ls production
  --project kanggle-fan | grep DEMO_PAYMENT_MOCK` · 원장 `projects/fan-platform/web/fan-platform-web/VERCEL.md`.
- 🔴 등식 단언에 `x2_web_present=0` 예외를 넣었다 — 안 넣으면 억제 직후 **영구 빨강**이 된다.
  그렇다고 문구를 지우지 않았다(AC-3 이 금지). **공허**(아무 말 없음)도 **영구 빨강**도 아니다.

### 실측 — 분기가 실제로 발화한다 (래퍼 전체 실행)

```
[verify] (x2) 팬 결제 mock 이 프런트·백엔드 양쪽에서 같은 상태인가
  ok: 팬 결제 mock — 백엔드만 검사 (membership-service='default' ⇒ 목: 1).
      프런트 절반은 저장소 밖이다: …
```

🔵 `default` 에 `portone` 이 없으므로 **목: 1** — 팬의 극성이 그대로 나왔다.

## AC-4 — 부팅 판정 · alias · 링크 정합

| 대상 | 변경 |
|---|---|
| `demo-up.sh` `SURFACE_FLOOR` | **2 → 1** (찌를 수 있는 표면은 이제 `console` 뿐) |
| `demo-up.sh` `SURFACE_ROW_FLOOR` | 🔵 **3 그대로** — 론처가 약속하는 화면의 **총 수**는 안 줄었다(서빙 출처만 갈렸다). 두 하한이 **다른 것**을 잰다 |
| 론처 `index.html` 팬 행 | `data-served="demo-host" data-host="web.fan-platform"` → `data-served="vercel" data-url="https://fan.hubwang.com/"` |
| Traefik alias | `web.fan-platform.${DEMO_DOMAIN:-local}` **제거** + 왜 없는지 주석 |

🔵 **론처 행이 `SURFACE_SRC` 이므로 두 벌이 아니라 한 벌이다** — 거기를 고치면 부팅 판정이
따라온다(`demo-up.sh:488` 주석이 그 계약이다). 하한만 별도로 내렸다.

🔴🔴 **부팅 완료 지문이 바뀐다.** 창 #3 까지는 `[demo] ✔ HTTP 표면 2/2: console=307
web.fan-platform=307` 이 완료 신호였다. 이제 **`1/1`** 이고 `web.fan-platform` 은 **404** 다.
⇒ **창 #4 에서 옛 지문을 기다리면 창이 영원히 안 열린다.** `TASK-MONO-621` 에 적어 뒀다.

🔵 형제 alias 주석(`:84`)이 *"형제 `web.fan-platform` 은 단계 4 — `TASK-MONO-586`"* 으로
이 티켓을 기다리고 있었다. **밟았다**고 갱신하면서, 그 주석이 가리키던 티켓 번호도 고쳤다 —
586 은 단계 4 의 **해석기 몫**이고 억제 몫은 이 티켓이다.
🔵 바로 위 `fan-platform.` (게이트웨이)는 **남겼다** — 억제된 것은 웹 사본이지 백엔드가 아니다.

## AC-6 — 이 구현이 **안 고치는 것**

| # | 무엇 | 왜 |
|---|---|---|
| 1 | 🙋 **`kanggle-fan` 에 `DEMO_PAYMENT_MOCK=1`** | 소유자. 🔴 **AC-0 ③ 이 새로 찾았고 억제와 함께 결함이 된다** — Vercel 판이 유일한 팬이 되는데 그 값이 없다 |
| 2 | 억제의 **런타임 판정** | `TASK-MONO-617`. 🔵 이 선언은 **자동으로** 그 모집단에 들어간다(실측: `--derive` 가 `fan fan-platform-web` 을 낸다 — 모집단 **1 → 2**) |
| 3 | **AC-0 ① · AC-5** | 기동 창 #4 (`TASK-MONO-621`)의 칸 |
| 4 | console(단계 3) · **(f) 벽** | 범위 밖 |
| 5 | 팬 시드의 `redirect_uri` | 🔵 확인함 — 억제 후에도 시드가 없는 표면을 가리킬 수 있으나 형제 축과 같이 **무해**하다(등록만 되고 아무도 안 쓴다). 이름만 적는다 |

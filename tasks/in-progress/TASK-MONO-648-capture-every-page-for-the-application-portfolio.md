# Task ID

TASK-MONO-648

# Title

취업 포트폴리오·서비스 README 용으로 **세 앱의 전 페이지를 찍어 둔다** — 그리고 그 자산이 어디 사는지 정한다

# Status

in-progress

# Owner

monorepo

# Task Tags

- demo
- portfolio
- tooling

---

# Goal

소유자가 **회사 지원용 포트폴리오**와 **서비스별 GitHub README** 를 만든다. 그러려면
세 앱의 화면이 필요한데, 지금 저장소에 있는 것은 **론처 캐러셀용 8장**(`TASK-MONO-639`)
뿐이고 그 8장은 전부 **로그인 없이 열리는 얇은 표면**이다.

**실측 (2026-09-08 UTC) — 실제 규모는 이렇다:**

| 앱 | 페이지 | 그중 동적 경로(`[id]` 포함) |
|---|---|---|
| `console-web` | **69** | 11 |
| `web-store` | **22** | 3 |
| `fan-platform-web` | **11** | 2 |
| **합계** | **102** | **16** |

콘솔의 도메인 분포: `ecommerce` 23 · `wms` 7 · `scm` 6 · `erp` 6 · `finance` 3 · `iam` 2 ·
`dashboards` 3 · `accounts` 2 · `audit` 1 + `tenants`·`operators`·`permissions`·`ledger` 등.

🔴 콘솔은 **공개 2페이지 / 보호 64페이지** 다. 즉 지금 포트폴리오는 **제품의 가장 얇은
부분**을 보여주고 있다.

---

# 🔴🔴 선행 — AMI 재굽기가 **반드시 먼저**다

구워진 AMI(`RepoCommit = 3bc182ecd`)는 2세대 뒤처져 있다:

| 축 | 구워진 것 | 지금 `main` |
|---|---|---|
| `V19` (상품 P09–P24) | 🔴 **없음** | 있음 |
| `seed-fan.sh` 아티스트 | `a001`–`a003` | `a001`–`a006` |

⇒ 재굽기 전에 찍으면 **옛 데이터가 담긴 화면**이 포트폴리오에 영구히 박힌다.
`TASK-MONO-639` 가 STOP 게이트로 정확히 이것을 피했다 — 여기서 되풀이하면 안 된다.

---

# Scope

## 포함

- 전 페이지 촬영 스크립트(신규) — 로그인 포함, 동적 경로 해결 포함
- `capture-shots.mjs` 에 **인증 촬영 지원** 추가(론처 카드용 콘솔 4장)
- 산출물이 어디 사는가에 대한 결정과 그 근거
- 낡음을 무는 가드

## 제외

- 🔴 **포트폴리오 문서·README 자체의 집필** — 이 티켓은 **자산을 만든다.** 글은 소유자 몫이다.
- 🔴 재굽기 실행(`TASK-MONO-630` 규약, 소유자 몫)
- 론처 카드의 배치·문구 개편 — `TASK-MONO-637` 이 정한 구조를 따른다

---

# 🔵 산출물이 어디 사는가 — 판단과 근거

**102장을 저장소에 넣지 않는다.** 장당 75–500KB(전체 페이지 캡처는 더 크다)라 **15–50MB** 이고,
클론마다 그 무게가 따라다닌다. 그리고 취업 포트폴리오는 **저장소 밖에서 쓰는 자료**다.

| 산출물 | 어디 | 왜 |
|---|---|---|
| **전량 102장** | 🔴 **gitignore 된 출력 디렉터리** | 리포 밖에서 쓰는 자료 · 무게 |
| **README 용 큐레이션** (서비스당 3–6장) | 커밋 | GitHub 이 렌더하려면 저장소에 있어야 한다 |
| **론처 캐러셀 콘솔 4장** | 커밋 (`site/thumbnails/`) | 이미 `(z37)` 이 지키는 자리 |

🔴 이 결정을 **바꾸려면 근거를 적어라** — 「전부 커밋」도 선택지이지만 그 대가(클론 무게,
이미지 diff 가 리뷰를 못 받음)를 알고 골라야 한다.

---

# Acceptance Criteria

## AC-0 — 착수 전 재측정

- [ ] 🔴 **AMI 의 `RepoCommit` 이 `4d1a498ef`(638)의 자손인가.** 아니면 **STOP** — 재굽기가
      선행이다. 찍으면 옛 데이터가 박힌다.
- [ ] 페이지 수를 **다시 세라**(위 표). 라우트는 늘고 준다.
- [ ] 🔴 데모가 **떠 있고 웜업이 끝났는가.** 기동 직후엔 빈 표·로딩 상태가 찍힌다 —
      그것은 「이 제품의 화면」이 아니다.

## AC-1 — 전 페이지 촬영 스크립트

- [ ] 세 앱의 **정적 경로 전부**를 찍는다. 라우트 목록은 **파일 시스템에서 유도**하라
      (`app/**/page.tsx`) — 손으로 적은 목록은 102개에서 반드시 낡는다.
- [ ] 🔴 **동적 경로 16개**: 목록 페이지에서 **실제 링크를 하나 골라** 따라간다.
      🔴 id 를 손으로 박지 마라 — 시드가 바뀌면 404 를 찍는다.
- [ ] 🔴 **로그인**: 콘솔의 보호 경로 64개는 인증이 필요하다. 자격은 화면에 이미 적혀 있다
      (`DemoLoginCredentials.tsx`). 🔴 **자격을 코드에 박지 말고** 그 출처에서 읽거나
      환경변수로 받아라.
- [ ] 🔴 **200 이 아니거나 로그인 화면으로 튕긴 경로는 «실패»로 센다.** 로그인 화면을
      «그 페이지의 캡처» 로 저장하면 포트폴리오가 거짓이 된다.
- [ ] 실패 목록을 **이름을 대며** 보고한다. 🔴 전체가 성공해야 한다는 뜻은 아니다 —
      **어느 것이 왜 실패했는지 적히면 된다**(권한 없음·데이터 없음 등은 정상일 수 있다).
- [ ] 뷰포트·스케일을 **고정**한다(`TASK-MONO-639` 와 같은 이유).

### 촬영 결과 (2026-09-09, 기동 창 1회) — **계획 102 · 찍음 90 · 실패 12**

| 앱 | 찍음 | 빈값 | 저하 | **큐레이션 후보** |
|---|---|---|---|---|
| console | 59 | 13 | 5 | **41** |
| store | 20 | 4 | 0 | **16** |
| fan | 11 | 0 | 0 | **11** |
| | | | | **합계 68** |

실패 12건은 전부 **동적 경로**이고 사유가 한 갈래다 — 부모 목록이 비어 있어 따라갈 링크가
없었다(주문·상품·프로모션·판매자·정산기간·사용자·템플릿·테넌트). 🔵 즉 **스크립트의 결함이
아니라 시드의 부재**다. 스크립트는 「못 찍었다」를 이름을 대며 정직하게 보고했다(AC-1 의
마지막 두 칸이 요구한 그대로).

🔴 **이 창이 스크립트의 결함 셋을 드러냈고 그게 이 커밋이다** — ① 로그인을 시간으로
기다렸다(조건 대기로 교체) ② 세 앱이 브라우저 컨텍스트를 공유해 콘솔의 OPERATOR 세션이
스토어로 번졌다(앱마다 새 컨텍스트) ③ **「찍혔다」를 「볼 만한 것이 찍혔다」로 세고
있었다**(본문 표지 + 별도 집계). 셋 다 «초록인데 틀린» 부류였다.

## AC-2 — 론처 카드용 콘솔 4장

- [ ] `capture-shots.mjs` 가 **인증 촬영**을 지원한다(`requiresAuth`).
- [ ] 소유자 승인 목록(2026-09-08): `/dashboards/overview` · `/ecommerce/orders` ·
      `/ecommerce/products/[id]/edit` · `/ecommerce/settlements`.
- [ ] 🔴 **장마다 「로그인 필요」를 표시**한다. 표시가 없으면 방문자는 「클릭하면 열린다」로
      읽는데 **열리지 않는다** — `TASK-MONO-634` § D7(이미지는 기동을 겸하지 않는다)과
      `TASK-MONO-637`(카드가 「로그인 없이/후」를 말한다)의 어휘를 **그대로 쓴다.**
- [ ] 🔴 매니페스트에 **인증 캡처였다는 사실**을 기록한다. 안 적으면 다음 사람이 왜
      재생성이 안 되는지 모른다.
- [ ] 첫 화면 무게를 **다시 재라**(`TASK-MONO-639` 실측: 8장 616KB · 첫 장 합계 224KB).

### 🔴🔴 AC-2 실측 (2026-09-09) — **승인된 4장이 4장 다 못 쓴다**

먼저, 이 AC 가 존재하는 이유가 실측으로 확인됐다. 지금 론처에 걸린 콘솔 썸네일 3장은
**진짜 콘솔이 아니다** — `capture-shots.mjs:58-62` 의 URL 이 `console.hubwang.com/demo`,
`/demo/ecommerce`, `/demo/wms` 이고, 그건 `src/app/(demo)/demo/` 의 **합성 샘플 투어**다.
오늘 그 페이지의 본문을 같은 순간에 떠 보니 이렇게 적혀 있었다:

> 「샘플(합성) 데이터로 만든 둘러보기입니다. **실제 운영 데이터는 한 건도 포함되어 있지
> 않으며**, 실시간 콘솔은 로그인이 필요합니다」

🔵 **그건 잘못 만든 게 아니라 제약이었다** — `capture-shots.mjs` 는 인증을 안 하므로
로그인 없이 찍을 수 있는 유일한 콘솔 화면이 `/demo` 였다. 이 AC 의 `requiresAuth` 가
그 제약을 푼다. 🔴 **그러나 푸는 것과 채우는 것은 다른 문제였다:**

| 승인 목록(2026-09-08) | 2026-09-09 실측 (테넌트 `demo-corp`) |
|---|---|
| `/dashboards/overview` | 🔴 **저하** — 「통합 개요를 일시적으로 불러올 수 없습니다」 |
| `/ecommerce/orders` | 🔴 **빈값** — 본문 221자 |
| `/ecommerce/settlements` | 🔴 **빈값** — 본문 937자 |
| `/ecommerce/products/[id]/edit` | 🔴 **찍지도 못함** — 「목록에 따라갈 항목이 없습니다」 |

🔴 **넷 중 셋은 테넌트를 잘못 골라서다.** 같은 날 전수 매트릭스가 보인 것: 테넌트
`demo-corp` 에서 이커머스 화면은 비고, 테넌트 `ecommerce` 에서는 상품 **20행**이 나온다
(콘솔 페이지 주석: *"ecommerce resolves the tenant from the JWT `tenant_id ∈ {ecommerce,*}`
claim"*). 즉 `/ecommerce/*` 셋은 **재촬영으로 회수된다**.

🔴🔴 **`/dashboards/overview` 는 회수되지 않는다 — 두 테넌트 모두 저하다.**
그리고 그것은 **내가 2026-09-08 에 1순위로 추천했던 장이다. 그 추천을 철회한다.**
승인 목록은 내 추천 위에 서 있었으므로, 목록 자체를 다시 올려야 한다.

#### 🔴🔴 그 저하의 사유 — **제품 결함이 아니라 설계다. 그래서 «영원히» 안 나온다**

처음에 나는 이 저하를 「고쳐야 할 결함」으로 분류하고 별도 티켓을 기안하려 했다.
**틀렸다.** `docs/guides/interview-demo-walkthrough.md` § 6 한계 원장의 **첫 행**이 이미
이것을 다루고 있었다:

> 🔵 **Vercel 콘솔에서 세 패널이 항상 «사용 불가» 로 보인다** — 운영 개요
> (`/dashboards/overview`) · 도메인 상태(`/dashboards/health`·`/console`) · 알림 인박스 |
> **설계상 그렇다, 고장이 아니다.** 그 셋만 `console-bff` 를 지나는데 그 BFF 는 **공개
> 호스트명이 없다**(`TASK-MONO-362` 가 엣지 라우터를 일부러 없앴다). 고치려면 BFF 에 공개
> 경로를 주거나 합성을 콘솔 서버로 옮겨야 하고 **둘 다 아키텍처 결정**이다
> — 추적: `TASK-MONO-585` · `TASK-MONO-627`

🔴 **오늘 저하 5건 중 3건이 정확히 그 셋이다** — `/` · `/dashboards/health` ·
`/dashboards/overview`. 남은 둘(`/onboarding` · `/wms/operations`)만 원장에 없는 미지수다.

⇒ **결론이 바뀌지 않고 오히려 굳는다.** 재촬영으로 회수될 것이라는 기대는 틀렸다.
공개 호스트명이 생기기 전까지 이 장은 **어떤 창에서도, 어떤 테넌트로도** 안 나온다.
🔵 그리고 그것을 그림으로 쓰면 안 되는 이유도 달라진다 — 「오늘 상태가 나빴다」가 아니라
**「이 표면은 데모 배치에서 원래 그렇게 보인다」** 이다.

🔴 **일반화(내가 오늘 두 번 틀린 축):** 이 저장소는 이미 § 6 원장에 **데모 표면의 알려진
한계를 전수로** 들고 있고, `main` 의 required check 하나가 그 표와 태스크 큐의 드리프트를
지킨다. **「새 결함을 찾았다」고 말하기 전에 그 표부터 열어라.** 오늘 나는 그 표를 안
열고 두 개를 「발견」이라 불렀다 — 이것과, 테넌트 분리(§ 4 에 이미 표로 있다)다.

- [ ] 🔴 **`/dashboards/overview` 를 대체할 4번째 장을 소유자가 고른다.** 오늘 68장 중
      «빈값·저하 0» 인 nav 그룹은 **IAM 9장 · ERP 6장 · Finance 4장**이다. 후보는 이 19장
      안에서 낸다. 🔴🔴 **2026-09-09 정정** — 첫 판에 나는 이것을 «erp 6 · iam 5 ·
      finance 3 = 14» 로 적었고 **셋 다 틀렸다.** `route.split('/')[1]` 로 서비스를
      갈랐는데 IAM 리프는 `/operators`·`/permissions`·`/tenants`·`/audit` 처럼 첫
      조각이 제각각이라 **8조각으로 흩어졌고**(9→5로 과소계수), Finance 의 `/ledger`
      는 떨어져 나갔다(4→3). ⇒ 그룹은 **내가 적지 않고 `console-nav-config.ts` 에서
      유도한다.** 🔵 `/erp/masters` 선택은 이 정정에 영향받지 않는다 — ERP 6 은
      맞았다.
      🔴🔴 **그리고 «19장» 도 아니었다** — 아래 § 사람 눈으로 전수를 보라. 16장을
      열어 보니 **✅✅ 6장**이고, 자동 표지는 빈값 셋·권한거부 하나·영문 실패 하나·빈
      검색 폼 하나를 **못 본다.** 「19」는 «표지가 안 걸었다» 이지 «볼 만하다» 가 아니다.
- [ ] 🔴 `/ecommerce/*` 3장은 **테넌트 `ecommerce` 로 재촬영**한 뒤에만 승인 목록으로
      확정한다. 🔵 `demo-corp` 로 찍어서 「승인 목록대로 찍었다」고 적으면, 그 매니페스트는
      **빈 표 세 장을 승인된 것으로** 기록한다.

### 🔴🔴 사람 눈으로 전수 — **19장이 아니라 6장이었다** (2026-09-09)

AC-4 가 요구한 큐레이션 후보를 내려고 **19장 중 16장을 실제로 열어 봤다**(나머지 셋은
자기 본문이 *"…콘솔은 개요·가이드·…로 구성됩니다"* 라고 적은 **가이드 문서 페이지**다).
결과가 숫자를 또 바꾼다.

| | 장 | 판정 |
|---|---|---|
| **ERP** | `/erp/masters` | ✅✅ 마스터 5종이 **전부 참** · effective-dating(`active 2026-01-01~∞`) · `asOf` 컨트롤 · 쓰기 동작(수정·이동·폐기). 🔴 UUID 노출(`TASK-PC-FE-276`) |
| | `/erp/orgview` | ✅✅ `DEPT-HQ · 본사 › DEPT-OPS · 운영본부` 계층 경로 · read-model 배너. **UUID 없음** |
| | `/erp` | ✅ 집계 타일 7개가 **전부 0 아님**(부서3·직원4·직급3·원가센터3·거래처3·결재대기2·활성위임1) |
| | `/erp/approval` | 🟡 inbox 2 + 요청 3행. 🔴 「대상」 칸이 UUID |
| | `/erp/delegation` | 🟡 1행 있으나 **「erp unavailable」이 빨간 글씨로** 떠 있다 |
| | `/erp/guide` | 📄 문서 페이지(5,521자) |
| **IAM** | `/operators` | ✅✅ 등록 폼(역할 다중선택·break-glass 비번·테넌트) · 테넌트 배정(파트너십·cascade offboarding 설명) · 표 2행 + 작업 6종 |
| | `/permissions` | ✅✅ 권한 키 **13개** 칩 + 역할 **7개**(보유 권한 수 · `ADR-MONO-024`/`047` 참조) |
| | `/permission-sets` | 🟡 역할 카탈로그는 좋으나 「사용 배정 수」가 **전부 `—`** |
| | `/audit` | 🟡 8행인데 그 내용이 **이 촬영 세션 자신의 질의**다(`AUDIT_QUERY` 6 + `UNKNOWN … DENIED` 2) |
| | `/iam` | 🟡 운영자 2 · **계정 0** · 감사 피드가 또 자기 참조 |
| | `/org-hierarchy` | ❌ **빈값** — 「조직 노드가 없습니다」 |
| | `/tenants` | ❌ **권한 없음** — 「테넌트 관리는 SUPER_ADMIN 전용입니다」 |
| | `/operator-groups` | ❌ **빈값** — 「등록된 운영자 그룹이 없습니다 · 총 0건」 |
| | `/iam/guide` | 📄 문서 페이지(9,259자) |
| **Finance** | `/ledger` | ✅✅ 시산표(차변·대변·기준통화) · **「대차 일치」 배지** · 합계 균형 · 탭 7(분개·대사·FX 포지션 로트·환율 피드). 🔴 계정 코드에 UUID |
| | `/finance` | 🟡 시산표 균형 배지 외엔 **미마감 0 · 대사 차이 0 · FX 피드 비활성 · 기본계좌 미설정** |
| | `/finance/accounts` | ❌ **빈 검색 폼** — 「조회할 accountId 를 입력하세요」 |
| | `/finance/guide` | 📄 문서 페이지(4,536자) |

⇒ **✅✅ 6장** (`/erp/masters` · `/erp/orgview` · `/operators` · `/permissions` ·
`/ledger` · 그리고 `/erp` 를 ✅ 로 셈) — 19가 아니다.

🔴🔴 **Finance 는 3–6장을 못 채운다.** 쓸 수 있는 것이 `/ledger` 하나다.
그 사실을 「채웠다」로 적으면 안 된다(`TASK-MONO-650` AC-4 가 금지한 그것이다).

#### 🔴🔴 왜 자동 판정이 19를 냈나 — **표지가 넷을 못 본다**

| 못 본 것 | 왜 |
|---|---|
| 「조직 노드**가 없습니다**」·「운영자 그룹이 없습니다」 | `EMPTY_RE` 가 `데이터\|결과\|항목\|내역` 만 물어서 **다른 명사**를 놓친다 |
| 「권한이 없습니다」(권한 거부) | 🔴🔴 **장별 판정이 아예 없다.** `sanityCheck()` 는 **앱당 한 번** 단일 `probePath` 에만 돈다 |
| `erp unavailable` | `DEGRADED_RE` 가 **한국어만** 문다 |
| 빈 검색 폼(`/finance/accounts`) | **표지 문구 자체가 없다** — 「입력하세요」는 정상 안내문이다 |

🔴🔴 **그리고 나는 `#3710` 커밋 메시지에 «「권한이 없습니다」는 sanityCheck 의 축이다» 라고
적었다. 틀렸다.** `sanityCheck` 를 열어 보지 않고 그렇게 썼고, 열어 보니 앱당 1회다.
⇒ 대조군이라고 부른 것이 **대조군이 아니었다.**

- [ ] 🔴 **표지를 넓혀라 — 다만 «고치는 것»이 아니라 «못 보는 것을 적는 것»이 먼저다.**
      `EMPTY_RE` 의 명사 목록을 늘리고, **장별 권한거부 판정을 더하고**, 영문 실패 문구를
      넣는다. 🔵 그래도 빈 검색 폼은 못 잡는다 — **그 한계를 명시적으로 적어라.**
      한계를 안 적으면 다음 사람이 「표지가 통과했으니 볼 만하다」로 읽는다.
- [ ] 🔵 **사람 눈을 대체하려 하지 마라.** 이 표는 16장을 열어서 나왔고, 그 중 셋(`/tenants`
      `/org-hierarchy` `/operator-groups`)은 **어떤 문자열 술어로도 «볼 만하지 않다»를
      말해 주지 않았다.** 표지의 목적은 후보를 **줄이는 것**이지 고르는 것이 아니다.

### store · fan 도 사람 눈으로 (2026-09-09, 9/27 열람)

콘솔이 6장밖에 안 나와서 store 16 · fan 11 로 넘어갔다. **콘솔보다 상태가 훨씬 낫다.**

| | 장 | 판정 |
|---|---|---|
| **store** | `/products` | ✅✅ **오늘 본 것 중 최고** — 실제 사진 20개 · 원화 가격 · 위시리스트 하트(일부 채워짐=상태) · 카테고리/정렬 필터 · 검색 · 페이지네이션 2쪽 |
| | `/` | ✅✅ 히어로 캐러셀 + 인기 상품 8. 🔴 상단 노란 배너(→ `TASK-MONO-654`) |
| | `/my/wishlist` | ✅ 3건 + 마이페이지 사이드바 7항목. 🔴 같은 배너 |
| | `/products/[id]` | 🟡 옵션 선택·리뷰 히스토그램은 좋으나 **리뷰 0개 · 총 금액 0원** |
| | `/cart` `/checkout` `/my/orders` `/orders` | ❌ 빈값(표지가 잡았다) |
| **fan** | `/membership` | ✅✅ 현재 구독 상태 · 자동 갱신 · 두 티어 + **일할 계산**(`잔여 크레딧 −₩7,636 → 이번 결제 ₩10,264`) |
| | `/` | ✅✅ 피드 15 · `ARTIST`/`PREMIUM`/`멤버 전용` 배지 · 게이팅이 CTA 와 함께 · 반응/댓글 수 |
| | `/artists` | ✅✅ 아티스트 **6명**(638 이 시드한 그 6명) · 실제 사진 · 소속사 · 검색 |
| | `/artists/[id]` | ✅ 프로필(데뷔일·소개) · **팔로잉 상태 버튼** · 포스트 2(하나는 게이팅) |
| | `/posts/[id]` | 🟡 얇다 — 포스트 1 · **총 0개의 반응** |
| | `/notifications` | 🟡 알림 1건. 🔴 본문이 **영어 + 원본 ISO 타임스탬프**(`Your MEMBERS_ONLY membership is active from 2026-09-09T10:06:34.519253Z…`)라 한국어 UI 안에서 튄다 |

⇒ **store 3 · fan 4** 로 둘 다 「서비스당 3–6장」을 **채운다.** 🔵 콘솔(6장)과 합쳐
**13장**이고, 그중 ✅✅ 는 9장이다.

🔴 **열람은 27장 중 9장에서 멈췄다.** 두 서비스가 이미 3장을 넘겼고 남은 것들은 단일 목적
화면이라 더 나은 후보가 나올 확률이 낮다고 판단했다 — **판단이지 측정이 아니다.**
더 좋은 것을 원하면 나머지 18장을 마저 연다.

🔴🔴 **그리고 이 열람이 결함 하나를 더 냈다 → `TASK-MONO-654`.** store 의 노란 배너가
「데모 서버가 꺼져 있어」라고 말하는데 **같은 화면의 위시리스트가 그날 16:46 에 담은
라이브 데이터**다. 캐시 헤더가 기전을 확정한다: `X-Nextjs-Prerender: 1` ·
`X-Nextjs-Stale-Time: 300` 인데 `Age: 61778`(**17.2시간**) · `X-Vercel-Cache: STALE`.

🔵 **부수 소득 — `TASK-PC-FE-276` 의 모집단이 erp 밖으로 나간다.** UUID 노출을 `/erp/masters`
`/erp/approval` `/ledger` **세 화면**에서 봤고, `ledger` 는 `ledger-ops` 라 **다른 모듈**이다.
276 AC-0 의 「다른 도메인도 세라」가 빈손이 아니라는 증거다.

### ✅ 소유자 결정 (2026-09-09) — 4번째 장 = **`/erp/masters`**

승인 목록이 이것으로 갱신된다:

| # | 장 | 상태 |
|---|---|---|
| 1 | **`/erp/masters`** | ✅ 확정 — 오늘 촬영분이 그대로 쓸 수 있다 |
| 2 | `/ecommerce/orders` | ⏳ 테넌트 `ecommerce` 로 **재촬영 후** 확정 |
| 3 | `/ecommerce/products/[id]/edit` | ⏳ 같음 (오늘은 따라갈 항목이 없어 **찍지도 못했다**) |
| 4 | `/ecommerce/settlements` | ⏳ 같음 |
| ~~`/dashboards/overview`~~ | | ❌ 제외 — **설계상 저하**(§ 6 원장 1행), 어떤 창에서도 안 나온다 |

🔴 **이 결정의 출처를 밝혀 둔다**: 나는 세 장(`/erp/masters` · `/permission-sets` ·
`/audit`)을 **열어 보고** 1순위로 `/erp/masters` 를 추천했고, 소유자가 *"추천대로 진행"*
으로 승인했다. **추천이 승인으로 굳은 경로가 2026-09-08 과 같다** — 그때는 그 장이 설계상
저하한다는 것을 아무도 안 열어 봤기 때문에 몰랐다. 이번엔 열어 봤다는 것이 유일한 차이다.

#### 🔵 채택하지 않은 대안 — `/erp/orgview` (지우지 않고 남긴다)

승인 **뒤에** 발견했으므로 소유자는 이 장을 보고 고른 것이 아니다. 사실만 적는다:

| | `/erp/masters` (채택) | `/erp/orgview` (대안) |
|---|---|---|
| 폭 | 마스터 **5종** 표 + 쓰기 동작(수정·이동·폐기) | 표 1개 |
| 읽힘 | 🔴 **UUID 원문 노출**(아래 § 결함) | ✅ `DEPT-HQ · 본사 › DEPT-OPS · 운영본부` 계층 경로 |
| 설계 노출 | effective-dating + `asOf` | effective-dating + `asOf` + **eventually-consistent read-model 배너** |

⇒ **바꾸고 싶으면 이 표만 보면 된다.** 재촬영도 승인도 필요 없다(둘 다 오늘 확보분에 있다).

#### 🔴 채택된 장에 결함이 하나 있다 — `TASK-PC-FE-276`

`/erp/masters` 는 「상위 부서」·「부서」 칸에 **이름 대신 UUID 원문**을 그린다
(`01a085a1-bc98-741e-…`). 🔴🔴 **데이터가 없어서가 아니다** — 바로 옆 `/erp/orgview` 가
같은 데이터를 `DEPT-HQ · 본사 › DEPT-OPS · 운영본부` 로 그린다. 상세는 `TASK-PC-FE-276`.

- [ ] 🔵 **652 를 기다리지 마라.** 그림은 지금 찍힌 것으로 쓴다. 652 가 머지되면 **그때
      다시 찍는다** — 그것이 AC-3 가드가 「내용이 바뀌었다」를 못 잡는다고 적어 둔 축이고,
      여기서는 사람이 안다.

## AC-3 — 낡음을 무는 가드

- [ ] 🔴 **날짜로 재지 마라.** 예약 트리거는 이 저장소가 이미 데인 함정이다.
- [ ] 대신 **찍은 경로가 아직 실재하는가**를 문다: 매니페스트의 경로 ↔ `app/**/page.tsx`
      에서 유도한 라우트 목록. 페이지가 사라지면 빨개진다.
- [ ] 🔴 **한계를 적어라**: 이 술어는 «페이지가 사라졌다» 만 잡고 «내용이 바뀌었다» 는 못
      잡는다. 그리고 인증 캡처는 **기동 창에서만** 재생성된다 — 즉 `TASK-MONO-639` 가 캡처에
      준 「다시 만들 수 있다」 성질이 **인증 캡처에서는 약해진다.** 그 사실을 남겨라.
- [ ] **bite** + 대조군.

## AC-4 — 산출물

- [ ] 전량은 gitignore 된 디렉터리에. 🔴 `.gitignore` 를 건드릴 땐 **`TASK-MONO-640` 을
      먼저 읽어라** — `bin/` 한 줄이 필수 생성기를 삼킨 적이 있다.
- [ ] README 용 큐레이션을 **어느 것으로 할지 소유자에게 물어라.** 서비스당 3–6장.
- [ ] 촬영 목록·성공/실패·용량을 **표로** 남긴다.

---

# Related Specs / Contracts

- `TASK-MONO-639` — 론처 캐러셀 캡처와 `capture-shots.mjs`. 이 티켓이 그 위에 선다
- `TASK-MONO-643` — 그 스크립트의 의존 해석. **인증 촬영도 같은 로더를 쓴다**
- `TASK-MONO-637` — 카드의 「로그인 없이 / 로그인 후」 어휘
- `TASK-MONO-634` § D7 — 이미지는 기동을 겸하지 않는다
- `TASK-MONO-630` — 재굽기는 소유자 몫
- `TASK-MONO-645` · `TASK-MONO-633` · `TASK-MONO-647` — **같은 기동 창**을 기다린다
- `TASK-PC-FE-275` — 로그인 화면이 데모 계정을 말한다

---

# Edge Cases

| 상황 | 기대 |
|---|---|
| 재굽기 전에 착수 | **STOP** (AC-0). 옛 데이터가 박힌다 |
| 웜업 전에 촬영 | 빈 표가 찍힌다 — 기다린다 |
| 권한 없는 경로 | 실패로 세고 **이름을 댄다**. 로그인 화면을 저장하지 않는다 |
| 데이터가 없는 목록 | 빈 상태도 제품의 일부다 — 찍되 **빈 상태임을 표에 적는다** |
| 동적 경로에 항목이 0개 | 실패로 세고 이름을 댄다(시드 부족 신호다) |

---

# Failure Scenarios

1. **재굽기 전에 찍는다** → 상품 8개·아티스트 3명짜리 화면이 포트폴리오에 영구히 박힌다.
2. **로그인 화면을 그 페이지의 캡처로 저장한다** → 포트폴리오가 거짓이 된다. 200 과
   «로그인으로 튕김» 을 구별하라.
3. **102장을 전부 커밋한다** → 클론마다 15–50MB 가 따라다니고, 이미지 diff 는 리뷰가 안 된다.
4. **id 를 손으로 박는다** → 시드가 바뀌는 날 404 를 찍고, 그 실패는 **조용하다**(이미지는 생긴다).
5. **날짜로 낡음을 잰다** → 예약 트리거 함정. 경로 실재로 재라.
6. **인증 캡처가 재생성 가능하다고 적는다** → 기동 창에서만 된다. 그 약화를 적어라.

---

# 분석 / 구현 권장

분석=Opus 5 / 구현 권장=**Opus** (라우트 유도·동적 경로 해결·인증·실패 분류가 얽혀 있고,
「무엇을 실패로 셀 것인가」가 판단이다. 촬영 자체는 기계적이지만 그 판단이 포트폴리오의
정직성을 정한다).

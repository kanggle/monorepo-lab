# Task ID

TASK-MONO-621

# Title

⏳ **재굽기 번들 ③ + 기동 창 #4** — 창을 열기 전에 매니페스트를 쓰고 **커밋한다**.
🔴🔴 이번 창은 **재굽기가 선행**이다. 그리고 그 재굽기가 **창의 칸 목록을 바꾼다** —
목록을 먼저 굳히면 틀린 목록을 소화하게 된다.

# Status

in-progress

# Owner

monorepo

# Task Tags

- demo
- rebake
- boot-window
- adr-followup

---

# 🔎 왜 이 티켓이 있나

`TASK-MONO-616` 이 「기동 창은 매니페스트를 **먼저 쓰고 커밋한 뒤** 연다」를 **절차**로
만들었다(§ AC-0). 그 절차는 창 #3 에서 값을 했다 — 스냅샷 표가 두 군데 틀린 것을 잡았다.
🔵 **616 은 그 창의 원장이고 지금 `review/`(frozen) 이다** ⇒ 창 #4 는 자기 티켓이 필요하다.
이 티켓이 그 행이다.

🔴 **그리고 이번 창은 창 #3 과 모양이 다르다.** 창 #3 은 **측정만** 했다(재굽기 없음).
창 #4 는 **재굽기가 선행**이고, 그래서 이 티켓은 창 티켓이자 **번들 티켓**이다
(`TASK-MONO-581` = 번들 ① · `TASK-MONO-615` = 번들 ② · **이것이 ③**).

---

# 🔴🔴 순서가 내용보다 먼저다 — 이 티켓의 본체

```
① 618 구현이 main 에 랜딩          ← 재굽기가 그것을 굽는다
② packer build (~55분)             ← 617·619·618 이 비로소 AMI 에 들어간다
③ terraform.tfvars 의 AMI 핀 갱신   ← 🔴 안 하면 구운 것을 아무도 안 가리킨다
④ 매니페스트(AC-0) 작성 + **커밋**   ← 616 이 존재하는 이유
⑤ 기동 → 소화 → 되돌려 적기
```

🔴 **④ 를 ② 보다 먼저 하면 안 된다.** 창의 칸 중 셋(`617` 라이브 · `618` AC-0 ① · AC-5)이
**AMI 에 무엇이 들어갔는가**에 따라 술어가 달라진다. 창 #3 의 매니페스트는 재굽기가
없었기에 이 제약이 없었다.

🔴 **③ 을 빠뜨리는 것이 이 번들의 가장 조용한 실패다.** `615` 가 § Failure Scenarios 에
*"재굽기 뒤 `terraform.tfvars` 의 AMI 핀을 갱신하지 않으면 **구운 것을 아무도 안 가리킨다**"*
라고 적어 뒀고, 그 실패는 **부팅이 성공하기 때문에** 안 보인다 — 옛 AMI 가 정상 부팅한다.
🔵 판별자는 `/status` 가 아니라 **부팅 로그에 `[suppressed]` 판정자가 나타나는가**이다
(그것이 617 의 코드이고, 옛 AMI 에는 없다).

---

# Goal

`ADR-MONO-067` 단계 4 의 억제(`TASK-MONO-618`)를 AMI 에 굽고, **한 번의 창**에서
그 시점에 라이브를 기다리는 칸 **전부**를 재고 각 티켓에 되돌려 적는다.

# Scope

- **포함**: 재굽기(packer) · AMI 핀 갱신 · **매니페스트 작성(AC-0)** · 창 안에서 전부 측정 ·
  각 티켓에 기록(AC-2) · 이 창이 안 고치는 것(AC-3)
- **제외**: 🔴 **새 코드 변경.** 측정 중 결함이 나오면 **별 티켓**으로 올리고 이 창에서
  고치지 않는다 — 615 창이 정확히 그렇게 커졌다(재굽기 2회 · PR 3건)
- **제외**: 🙋 소유자 결정 사안 (§ AC-3 에 이름을 적는다)
- **제외**: `TASK-MONO-615` ⓐ (rc=124) — 🔵 **2026-09-04 에 「측정 불가」로 닫혔다.**
  🔴 **이 창의 칸이 아니다.** 닫힌 칸을 창마다 다시 세면 소화율 분모가 거짓이 된다

---

# Acceptance Criteria

## AC-0 — 🔴🔴 **창을 열기 전에 매니페스트를 만든다. 이것이 본체다**

🔴 **`TASK-MONO-616` § AC-0 의 절차를 그대로 집행한다.** 아래는 그 절차의 참조이지
사본이 아니다 — 절차 정본은 616 이다.

1. **큐 전수 스캔** — 루트 `tasks/{ready,in-progress,review}/` 와 `projects/*/tasks/*/`
   전부에서 grep: `기동 창` · `미측정` · `다음 부팅` · `라이브` · `데모가 꺼져` · `⏸️`
   🔴 **모집단부터 다시 세라.** 양성 대조군(`done/` 이 걸리는가)도 같이 찍는다.
2. 걸린 티켓마다 **본문 AC 절을 열어** 그 칸이 요구하는 것을 한 줄로 옮긴다.
   🔴 **큐 행(INDEX)만 읽지 마라** — 창 #3 이 604 를 놓친 경로가 그것이다.
3. 각 칸에 **판정 술어**(무엇을 보면 PASS 인가)와 **대조군**을 적는다.
4. 🔴 **칸끼리의 상충과 순서를 고정한다** — 특히 선행 관계(A 가 FAIL 이면 B 는 미측정).
5. 🔴 **재굽기(②③)가 끝난 뒤에** 작성한다 — 위 § 순서 참조.
6. 매니페스트를 **이 티켓에 커밋한 뒤** 기동한다. 🔴 머릿속에 두지 마라.

### 🔴 창을 열기 전 **한 번 더 읽을 것** (착수 즉시, 추정 금지)

1. 🔴 **예산을 다시 읽는다** — `GET <DEMO_API_BASE>/status` 의 `used_minutes`.
   🔴 **상속하지 마라.** (2026-09-04 시점: **298/600**, 남은 **302분**.)
2. 🔴 `state=stopped` 를 **독립 확인** 하나와 함께 본다:
   `https://auth.hubwang.com/.well-known/openid-configuration` = **503** 이어야 한다.
3. 🔴 **`stop/start` 로 켠다.** `reboot` 는 공인 IP 가 유지돼 recreate 가 안 일어난다.
4. 🔴 **론처(`hubwang.com`) 탭을 유지한다** — `idle_minutes=20` 이고 론처가 30초마다
   `/heartbeat` 를 친다. **창의 수명은 예산이 아니라 그 탭**이다(창 #3 실측).
5. 🔴 `curl` 로 `/oauth2/authorize` 를 칠 때 **`Accept: text/html` 을 붙인다.**
   정본은 `infra/demo/seed/lib.sh`. 안 붙이면 401 Bearer 가 나오고 그것은
   「IdP 가 깨졌다」와 구별되지 않는다(창 #2 가 없는 결함을 만들어 낸 자리).
6. 창을 닫기 전 **인스턴스 정지**를 확인한다.

### 🔵 부수 관측 (칸이 아니다 — 비용 0)

- **부팅 소요 시간**을 적는다. 정상은 **690~777s**(창 #2 실측). 크게 넘으면 그것이
  `TASK-MONO-615` ⓐ 를 **다시 열 조건 C** 다(그 티켓 § 다시 열 수 있는 조건).
  🔴 **칸으로 세지 마라.** 소화율 분모에 안 들어간다.

## AC-1 — 창 안에서 매니페스트를 **전부** 소화한다

- 각 칸은 **PASS / FAIL / 미측정(사유)** 셋 중 하나로 끝난다. 🔴 **빈칸 금지.**
- 🔴 「시간이 없어서 못 쟀다」도 **적어라** — 그것이 다음 창의 입력이다.
- 창을 닫기 전 매니페스트를 다시 읽고 **소화율(PASS+FAIL / 전체)** 을 적는다.
  🔴 **미측정은 「닫은 칸」이 아니다.**

## AC-2 — 각 티켓에 **되돌려 적는다**

- 측정 결과는 **그 칸을 가진 티켓**에 적는다(이 티켓에만 적으면 그 티켓은 계속 열려 있다).
- `review/` 티켓은 frozen 이므로 **CORRECTION 절**로 붙인다.
- 닫힌 티켓은 close chore 로 `done/` 으로 옮긴다 — 🔴 **4-dim**(3-dim + **AC 전수**,
  `TASK-MONO-620`). 🔴 **큐 행 말고 티켓 본문 AC 절을 열어라.**

## AC-3 — 🔴 이 창이 **안 고치는 것**을 적는다

소유자 대기 항목 · 새로 발견한 결함(별 티켓 번호) · 다음 창으로 넘긴 칸.

---

# 📋 착수 시점의 **후보 목록** (🔴 AC-0 이 이것을 **검증**한다 — 복사하지 마라)

🔴 아래는 **2026-09-04 기준 스냅샷**이고, `618` 구현과 재굽기가 그 사이에 들어간다.
창 #3 의 스냅샷은 **두 군데 틀렸다**(칸 하나가 사라졌고 표에 없던 칸이 하나 생겼다).
같은 일이 이번에도 일어난다고 가정하라.

| 티켓 | 큐 | 라이브를 기다리는 칸 | 판정 술어(초안) |
|---|---|---|---|
| `TASK-MONO-610` | in-progress | **AC-4b** — store 로그인 왕복 | `store.hubwang.com` 에서 csrf → `signin/iam` 의 `Location` 이 `auth.hubwang.com/oauth2/authorize` → 폼 → 콜백 → `session` 이 `accountId`. 🔵 **선행(env+배포)이 갖춰졌다** — 창 #3 은 홉① 에서 `?error=Configuration` 이었다. 양성 대조군 = **fan 왕복**(창 #3 PASS) |
| `TASK-MONO-612` | in-progress | **AC-0 ②** + **AC-1 검증** | 세션 쿠키로 `GET /api/store-config` → `demoPayment` **런타임 값을 읽는다**. 🔴🔴 **PASS = 「읽었다」이지 「`true` 다」가 아니다.** AC-1 은 `DEMO_PAYMENT_MOCK=1` 이 지정·투입됐으므로 **값이 `true` 인지**가 그 칸 |
| `TASK-MONO-617` | review | **라이브** — 판정자가 데모 호스트에서 도는가 | 부팅 로그에 `[suppressed] OK — 억제 대상 N개(검사 N건)`. 🔴 **N 은 618 랜딩 후 `2`**(`ecommerce/web-store` + `fan/fan-platform-web`) — 1 이면 **AMI 핀이 안 갱신됐거나 618 이 안 구워졌다**. 🔵 안 나오면 배선이 아니라 **예산**(`post_up_call` 소진)을 먼저 의심 |
| `TASK-MONO-618` | (구현 후) | **AC-0 ①**(팬 전제) · **AC-5**(억제 404) | ① 세션 들고 `fan.hubwang.com` 로그인 뒤 화면: 에러 문구 **부재** + 도메인 개체 ≥1 · 음성 대조군 `/zzz-nope-618` 의 `?from=`. ⑤ `web.fan-platform.<도메인>` **404** + `docker ps -a` **컨테이너 부재** + 양성 대조군 `console`=**307**. 🔴 **① 을 창에서 «맨 먼저»** (그 티켓 § AC-0 ① 개정) |
| `TASK-MONO-586` | review | 🔵 **창 #3 이 닫았다** — 재확인만 | 라이브 축 PASS. 🔴 AC-0 에서 **정말 닫혔는지** 확인하고, 닫혔으면 칸으로 세지 마라 |
| `TASK-MONO-604` | review | 🔵 **창 #3 이 닫았다**(AC-0 ① PASS) | 위와 같음 |

🔴 **`TASK-MONO-615` ⓐ 는 이 표에 없다** — 2026-09-04 에 「측정 불가」로 **닫혔다**.

## 🙋 이 창이 기다리는 소유자 항목 (AC-0 이 다시 확인한다)

| # | 무엇 | 안 되면 어느 칸이 죽나 |
|---|---|---|
| 1 | `kanggle-fan` 에 **`DEMO_PAYMENT_MOCK=1`** | 🔴 **618 AC-0 ③ 이 새로 찾은 것.** 억제 후 팬 결제가 살아남는 표면에서 죽는다 |

---

# 🔴 기동 창 운영 메모 (창 #2·#3 실측 — 상속하지 말고 확인하라)

- **예산**: 월 600분. 창 #3 은 **17분**(281→298) 들었다. 부팅 1회 ≈ 12~20분 + 측정.
- 🔴 **`state=running` 은 부팅 완료가 아니다.** 대리지표가 **404 → 503 → 200** 으로 올라온다.
  창 #3 은 `console=307` + `web.fan-platform=307`(표면 2/2)까지 기다렸다.
  🔴🔴 **618 이 랜딩하면 그 지문이 바뀐다** — `web.fan-platform` 이 **404** 가 되고
  `SURFACE_FLOOR` 가 **1** 이다. **옛 지문을 기다리면 창이 영원히 안 열린다.**
- **해석기 캐시** `CACHE_TTL_MS = 15_000` — 첫 요청이 stale 캐시를 볼 수 있다.
- **`verify --live` 는 (f) 앞까지만** 돈다(`container_name: scm-platform-redis` 고정 충돌).
  rc=1 을 보고 이 창의 변경을 의심하지 마라.
- **시드가 독립 증거다** — `[seed:*] 요약 … 실패 0` 이 나오면 로그인 경로는 살아 있다.
  🔴 **내 술어가 그와 모순되면 내 술어부터 의심하라.**

# Related Specs

- `TASK-MONO-616` § AC-0 — **매니페스트 절차의 정본** (이 티켓이 집행한다)
- `TASK-MONO-615` § 항목 A/B/C — 번들 ② 의 순서 제약 · § ⓐ (2026-09-04 닫힘)
- `TASK-MONO-618` — 이 재굽기가 굽는 것 · § AC-0 ① 개정 · § AC-5
- `TASK-MONO-617` § 라이브에서 아직 안 본 것 · `TASK-MONO-610` § AC-4b
- `TASK-MONO-612` § AC-0 ② · § AC-1 · `TASK-MONO-620` — close chore 4-dim
- [`ADR-MONO-067`](../../docs/adr/ADR-MONO-067-demo-surfaces-served-from-vercel.md) § 단계 순서(2026-09-04 재지정)

# Related Contracts

없음.

# Edge Cases

- 🔴 **AC-0 을 「대충」 하면 이 티켓은 자기 목적을 잃는다.** 매니페스트가 부실하면 창은
  또 한 칸을 빠뜨리고, 그 비용은 다음 창 전체다.
- 🔴 **측정 중 결함이 나오면 고치고 싶어진다.** 615 창이 그렇게 커졌다. **별 티켓**으로
  올리고 매니페스트를 마저 소화한 뒤에 돌아와라.
- 🔴 **부팅 완료 지문이 이번 창에서 바뀐다**(표면 2/2 → 1/1). 옛 지문을 기다리지 마라.
- 🔴 창을 닫기 전 **인스턴스 정지**를 확인하라. `max_runtime_minutes=180` 은 상한이지
  절약이 아니다.
- 🔴 **재굽기가 실패할 수 있다** — 7차 굽기는 10분 53초에 죽었고 원인은 코드가 아니었다.
  실패하면 **창을 열지 마라**(옛 AMI 로 열면 617·618 칸이 전부 거짓 FAIL 이 된다).

# Failure Scenarios

| 상황 | 잘못된 처리 | 옳은 처리 |
|---|---|---|
| 매니페스트 없이 「일단 켜고 보자」 | 615 창이 한 그것 | 🔴 **AC-0 을 커밋한 뒤에 켜라** |
| 큐 행(INDEX)만 읽고 목록을 만든다 | 창 #3 이 604 를 놓친 경로 | 🔴 **티켓 본문 AC 절을 열어라** |
| 재굽기 전에 매니페스트를 굳힌다 | AMI 가 술어를 바꾸는데 목록이 낡는다 | 🔴 **② → ④ 순서** |
| AMI 핀 갱신을 빠뜨린다 | 부팅이 **성공해서** 안 보인다 | 🔴 판별자 = `[suppressed]` **판정자의 존재**, 그리고 억제 대상 **N=2** |
| 어떤 칸을 「시간이 없어」 조용히 건너뛴다 | 다음 창이 그 사실을 모른다 | 🔴 **「미측정(사유)」로 적어라** |
| 라이브 401 을 보고 「IdP 결함」으로 적는다 | 창 #2 가 직전까지 간 그것 | 🔴 **`Accept` 부터 확인.** 시드 초록과 모순되면 내 술어가 용의자 |
| 닫힌 칸(`615` ⓐ)을 또 센다 | 소화율 분모가 거짓이 된다 | 🔴 **닫힌 칸은 표에서 빼라** |

---

---

# ✅ AC-0 — 매니페스트 (2026-09-04 UTC, **재굽기 뒤 · 창을 열기 전에 작성·커밋**)

🔴 이 절이 이 티켓의 본체다. § 후보 목록(스냅샷)은 **검증 대상**이었고, 검증 결과
**표가 두 군데 틀렸다** — 없던 칸이 하나 드러났고, 있던 칸 하나는 다른 칸에 흡수됐다.

## AC-0 ① — 큐 전수 스캔 (**모집단부터 다시 셌다**)

| 스캔 대상 | 파일 수 | 비고 |
|---|---|---|
| 루트 `tasks/{ready,in-progress,review}/` | **14** | ready 4 · in-progress 4 · review 6 (창 #3 때는 9) |
| `projects/*/tasks/{ready,in-progress,review}/` | **0** | 🔵 9개 프로젝트 × 3큐 = **27 디렉터리가 전부 비어 있다**(실측) |

grep: `기동 창` · `미측정` · `다음 부팅` · `라이브` · `데모가 꺼져` · `⏸️`

🔵 **양성 대조군**: 같은 grep 이 `tasks/done/` 에서 **141파일**을 잡는다 ⇒ 술어가 아무것도
못 잡는 것이 아니다. 🔴 `done/` 은 모집단이 아니다(동결) — 제외했다.

| # | 티켓 | 큐 | grep | **칸이 있나** |
|---|---|---|---|---|
| 1 | `328` | ready | 0 | ❌ DEFERRED 백로그, 라이브 축 없음 |
| 2 | `585` | ready | 8 | ❌ **자기 칸이 아니다** — `610` 왕복의 **소비자**(AC-2 되돌려 적기 대상) |
| 3 | `587` | ready | 1 | ❌ 창 불필요 — AC 전부가 Vercel 대시보드 판독 |
| 4 | `621` | ready | 15 | — 자기 자신 |
| 5 | `610` | in-progress | 29 | ✅ **칸 1** |
| 6 | `612` | in-progress | 25 | ✅ **칸 2 · 칸 3** |
| 7 | `615` | in-progress | 41 | 🔵 **ⓐ 는 닫혔다**(09-04) · **B3 공백은 칸 5 에 흡수**(§ 아래) |
| 8 | `618` | in-progress | 8 | ✅ **칸 5 · 칸 6** |
| 9 | `586` | review | 7 | ❌ 창 #3 이 닫았다(라이브 축 PASS) — 재확인만 |
| 10 | `604` | review | 10 | ❌ 창 #3 이 닫았다(AC-0 ① PASS) — 재확인만 |
| 11 | `616` | review | 45 | ❌ 창 #3 의 원장(frozen) |
| 12 | `617` | review | 6 | ✅ **칸 4** |
| 13 | `619` | review | 0 | ❌ |
| 14 | `620` | review | 2 | ❌ 인용일 뿐(`574` 사례) |

### 🔴🔴 스냅샷이 몰랐던 것 ① — **`615` B3 의 공백이 칸 5 에 흡수된다**

`615` B3 § 남은 공백: *"「JVM 이 그 JWKS 로 토큰을 실제로 검증했다」는 아직 **미측정**이다
(유효한 팬 토큰이 필요하고 그건 **B2 에 막혀 있다**). B2+B3 이 끝나면 그것부터 재라."*

🔵 **B2 는 창 #2 에서 닫혔고 B3 은 랜딩됐다** ⇒ 이 공백은 **더 이상 막혀 있지 않다.**
그리고 **칸 5 가 그것을 그대로 잰다** — 팬 로그인 뒤 화면이 도메인 데이터를 내려면
게이트웨이 뒤의 리소스 서버가 그 토큰을 **JWKS 로 검증**해야만 한다.

⇒ **별도 칸으로 세지 않는다**(같은 측정을 두 번 세면 소화율 분모가 거짓이 된다).
🔴 **그러나 AC-2 되돌려 적기 대상에 `615` 를 넣는다** — 한 사실이 두 절에 있으면 한쪽만
고쳐진다. 칸 5 가 PASS 면 `615` B3 의 그 문장도 같이 닫힌다.

### 🔵 스냅샷이 몰랐던 것 ② — **`604`·`586` 이 이제 close 가능해졌을 수 있다**

둘은 AC 가 전부 닫혔는데도 `review/` 에 남아 있었다. 사유가 *"CORRECTION 이 코드 작업을
열어 두고 있다"*(604 = (z19) 가드 공백 · 586 = 팬 억제 + (x2) 문구)였는데, **그 둘을
`617` 과 `618` 이 받아서 랜딩했다.** ⇒ 🔴 **이 창의 칸은 아니다**(라이브가 필요 없다).
창을 닫은 뒤 close chore 에서 **4-dim** 으로 판정한다. AC-3 에 이름을 적는다.

---

## AC-0 ②③ — 매니페스트 본표 (**칸 · 술어 · 대조군**)

🔴 각 칸의 「무엇을 요구하나」는 **INDEX 행이 아니라 그 티켓 본문 AC 절**에서 옮겼다.

### 칸 1 — `610` **AC-4b store** (in-progress)

| | |
|---|---|
| 티켓이 요구하는 것 | *"판정은 env 목록이 아니라 «로그인이 되는가»"* — store 다섯 줄(1 `OIDC_ISSUER_URL` · 2 `NEXTAUTH_URL` · 3 `NEXTAUTH_SECRET` · 4·5 `ECOMMERCE_WEB_STORE_CLIENT_{ID,SECRET}`) |
| **판정 술어** | `store.hubwang.com` 에서 csrf → `POST /api/auth/signin/iam` 의 `Location` 이 **`https://auth.hubwang.com/oauth2/authorize?…`** → 로그인 폼 → 콜백 → `GET /api/auth/session` 이 `accountId` 를 낸다 |
| **창 #3 의 값(=반대편 대조군)** | 홉①에서 **`?error=Configuration`** — authorize URL 자체를 못 만들었다. ⇒ 이번에 그것이 **뒤집히면** 진전 |
| **양성 대조군** | 🔵 **같은 순간 같은 IdP 로 fan 왕복**(창 #3 PASS). 두 팔이 갈리면 차이는 **store 쪽 env** 다 |
| 🔴 단일 변수 귀속 금지 | 두 팔 사이 변수가 여럿이다. 실패 시 주장은 *"store 왕복이 X 에서 끊긴다"* 까지 |
| 🔴 홉④에서 죽으면 | 범인은 issuer 가 아니라 **`ECOMMERCE_WEB_STORE_CLIENT_SECRET`** (610 에 적혀 있다) |
| PASS 는 …이다 | `session` 이 `accountId` 를 낸다 |

### 칸 2 — `612` **AC-0 ②** (in-progress · **칸 1 이 선행**)

| | |
|---|---|
| 티켓이 요구하는 것 | *"런타임 값을 실제로 읽는다"* — `/api/store-config` 는 인증 게이트 뒤 |
| **판정 술어** | 세션 쿠키로 `GET /api/store-config` → JSON 의 `demoPayment` 값을 **읽는다** |
| 🔴🔴 **PASS 는 「값이 `true`」가 아니다** | **PASS = 런타임 값을 읽었다.** `false` 여도 이 칸은 **PASS** 고, 그건 칸 3 의 입력이다. 🔴 이 구별을 안 하면 소유자 항목이 「FAIL」로 오기록된다 |
| **사전 기준선** | `307 → /login?from=%2Fapi%2Fstore-config` (게이트가 살아 있다 = 익명으론 못 읽는다) |
| 칸 1 이 FAIL 이면 | **미측정(사유: 로그인 불가)** 으로 닫고 넘어간다 |

### 칸 3 — `612` **AC-1 검증** (in-progress · **칸 2 가 선행**)

| | |
|---|---|
| 티켓이 요구하는 것 | AC-1 은 **지정됐다**(방향 = 프런트를 백엔드에 맞춘다, `DEMO_PAYMENT_MOCK=1`). 남은 것은 **그 값이 런타임에 읽히는가** |
| **판정 술어** | 칸 2 가 읽은 값이 **`{"demoPayment":true}`** 인가 |
| 🔴 `false` 면 | **FAIL 이 아니라 AC-1 이 다시 열린다** — 「env 는 있는데 배포에 안 실렸다」가 되고 처방은 **store 경로 배포 하나**다(612 가 그렇게 적었다) |
| PASS 는 …이다 | 값이 `true` |

### 칸 4 — `617` **라이브** (review · CORRECTION 으로 되돌려 적는다)

| | |
|---|---|
| 티켓이 요구하는 것 | *"이 판정자는 데모 호스트에서 한 번도 안 돌았다 … 부팅 로그에 `[suppressed] OK — 억제 대상 N개(검사 N건)` 이 나오는가"* |
| **판정 술어** | 부팅 로그에 `[suppressed]` 판정자의 출력이 있고, **억제 대상 = 2** |
| 🔴🔴 **N=2 가 이번 창의 핵심 판별자다** | 창 #3 시점엔 **1**(`ecommerce/web-store`)이었다. `618` 이 랜딩했으므로 **2**(+ `fan/fan-platform-web`)여야 한다. 🔴 **1 이면 AMI 핀이 안 갱신됐거나 618 이 안 구워진 것** — 「가드 결함」으로 읽지 마라. 로컬 실측 `--derive` = 2행 |
| 🔵 안 나오면 | 배선이 아니라 **예산**(`post_up_call` 이 앞 단계에 소진돼 건너뜀)을 **먼저** 의심하라 |
| PASS 는 …이다 | 그 줄이 나오고 **N=2** |

### 칸 5 — `618` **AC-0 ①** (in-progress · 🔴 **창에서 «맨 먼저»**)

| | |
|---|---|
| 티켓이 요구하는 것 | *"`fan.hubwang.com` 의 로그인 뒤 화면이 실데이터를 내는가"* — 🔴 상태코드 금지 |
| **판정 술어** | 세션을 들고: 에러 문구 **부재** + 도메인 개체(아티스트/구독 등) **≥1**. 🔴 HTML 주석·`<script>`·`<style>` 을 **먼저 지우고** 추출한다(창 #3 이 이 함정을 잡았다) |
| **음성 대조군** | `/zzz-nope-618`. 🔴 상태코드·바이트로는 `/` 와 구별 안 된다 — **`Location` 의 `?from=`** 을 읽어라(창 #3 이 고친 술어) |
| 🔴 **가격/개체 정규식** | 창 #3 이 `[0-9][0-9,]*원` 으로 **0건**을 냈고 그건 **추출기가 넣은 공백** 때문이었다. `[[:space:]]*` 를 허용하라 |
| 🔵 **이 칸이 `615` B3 공백도 닫는다** | 화면이 도메인 데이터를 내려면 리소스 서버가 토큰을 **JWKS 로 검증**해야 한다 |
| 🔴 **FAIL 이면** | 억제는 **이미 AMI 에 구워져 있다** ⇒ *"억제하지 마라"* 는 집행 불가. **별 티켓 = 억제 되돌리기(재굽기 1회)** (618 AC-0 ① 개정) |
| 🔴 결제 화면은 **이 칸이 아니다** | `DEMO_PAYMENT_MOCK` 미투입 상태라 결제는 별 축이다. 두 축을 한 칸에 넣으면 귀속이 안 된다 |
| PASS 는 …이다 | 에러 문구 0 **이고** 도메인 개체 ≥1 |

### 칸 6 — `618` **AC-5** (in-progress · 칸 5 다음)

| | |
|---|---|
| 티켓이 요구하는 것 | `web.fan-platform.<DEMO_DOMAIN>` **404** + 🔴 `docker ps -a` 로 **컨테이너 부재** |
| **판정 술어** | 두 축 **모두**. 🔴 상태코드만으로 판정하지 마라 — 되살아난 컨테이너의 404 와 구별이 안 된다(`610` 창 #1 이 그 오독을 했고 `615` C1 이 두 축으로 고쳤다) |
| **양성 대조군** | 억제 안 된 화면 `console` 이 **307** |
| 🔴 컨테이너 축을 못 보면 | 호스트 `docker ps` 접근이 없으면 **그 절반은 미측정**으로 적어라(추정 금지) |
| PASS 는 …이다 | 404 **이고** 컨테이너 0건 **이고** 대조군 307 |

---

## AC-0 ④ — 🔴 **칸끼리의 상충과 순서 고정**

| # | 상충 | 고정한 순서 / 처분 |
|---|---|---|
| **가** | 🔴🔴 **부팅 완료 지문이 바뀌었다** — 창 #3 은 `표면 2/2 (console=307 web.fan-platform=307)` 을 기다렸다. 이제 **`1/1` (console 만)** 이고 `web.fan-platform` 은 **404** 다 | **옛 지문을 기다리지 마라.** 기다리면 창이 영원히 안 열린다 |
| **나** | 칸 2 는 칸 1 없이, 칸 3 은 칸 2 없이 성립하지 않는다 | **칸 1 → 칸 2 → 칸 3.** 앞이 FAIL 이면 뒤는 **미측정(사유)** |
| **다** | `618` AC-0 개정이 칸 5 를 **«창에서 맨 먼저»** 로 못박았다 | **칸 5 를 칸 6 보다 먼저** |
| **라** | 해석기 캐시 `CACHE_TTL_MS = 15_000` | 부팅 완료 판정 후 **≥15초 뒤** 새 요청으로 잰다(첫 요청이 stale 캐시를 볼 수 있다) |
| **마** | 칸 4 는 **부팅 로그**에서만 읽힌다 | 부팅 직후 **가장 먼저** 회수 |
| **바** | `verify --live` 는 **(f) 앞까지만** 돈다(`container_name: scm-platform-redis` 고정 충돌) | (f) 이후는 **미측정(구조적)** 으로 **미리** 이름을 적어 둔다. rc=1 을 보고 이 창의 변경을 의심하지 마라 |

⇒ **집행 순서**: 부팅 → **칸 4**(로그) → 15s 대기 → **칸 5** → **칸 6** → **칸 1** → **칸 2** → **칸 3**

## AC-0 ⑤ — 🔴 창을 열기 전 **한 번 더 읽을 것** (착수 즉시, 추정 금지)

1. 🔴 **예산 재확인** — `GET <DEMO_API_BASE>/status` 의 `used_minutes`. **상속하지 마라.**
2. 🔴 `state=stopped` **독립 확인** — `auth.hubwang.com/.well-known/openid-configuration` = **503**
3. 🔴 **`stop/start`** 로 켠다(`reboot` 는 IP 유지 ⇒ recreate 안 됨)
4. 🔴 **론처 탭 유지** — `idle_minutes=20`, 30초마다 `/heartbeat`. **창의 수명은 예산이 아니라 그 탭**
5. 🔴 `curl` 로 `/oauth2/authorize` 를 칠 때 **`Accept: text/html`** (정본 `infra/demo/seed/lib.sh`)
6. 창을 닫기 전 **인스턴스 정지** 확인

### 🔵 부수 관측 (칸 아님 · 비용 0)

- **부팅 소요 시간**. 정상 **690~777s**. 크게 넘으면 `615` ⓐ 를 다시 열 **조건 C** 다.
  🔴 소화율 분모에 넣지 마라.
- **시드 요약** `[seed:*] … 실패 0` — 로그인 경로의 **독립 증거**. 🔴 내 술어가 그와
  모순되면 **내 술어부터 의심하라.**

## 🔴 이 매니페스트가 **미리 적는 미측정 칸**

| 칸 | 왜 |
|---|---|
| `verify --live` 의 **(f) 이후 전부** | 구조적 충돌 — 떠 있는 데모 호스트에서 못 돈다 |
| `615` **ⓐ** | 🔵 **닫혔다**(측정 불가 확정, 09-04). 이 창의 칸이 **아니다** |
| `587` | 데모 축이 아니다(Vercel 대시보드) |
| 🙋 **`kanggle-fan` `DEMO_PAYMENT_MOCK`** | 소유자 미투입 ⇒ 팬 **결제** 축은 이 창에서 **재지 않는다**(칸 5 와 다른 축) |

## 📊 소화율 계산 기준

**분모 = 6** (칸 1~6). 🔴 **미측정(사유)은 「닫은 칸」에 넣지 않는다** — 소화율은
`PASS+FAIL / 6` 이고, 미측정은 **다음 창의 입력**으로 별도 집계한다.

---

## 🟢 AC-0 갱신 — **SSM 이 있다. 칸 4 는 측정 가능하고, 칸 7 이 되살아났다** (2026-09-04)

🔴🔴 **`615` ⓐ 를 닫을 때 내가 소유자에게 제시한 전제가 틀렸다.** 나는
*"컨트롤 플레인이 `/start` `/stop` `/status` `/heartbeat` 넷뿐이라 호스트 로그에 못 닿는다"*
라고 적었는데, **호스트 접근은 컨트롤 플레인을 거치지 않는다**:

| 증거 | 값 |
|---|---|
| 내 AWS 자격의 정책 | `portfolio-demo-deployer` 에 **`AmazonSSMFullAccess`** 부착(실측) |
| 인스턴스가 SSM 관리 대상인가 | ✅ — **컨트롤 플레인 자신이** `ssm.send_command(DocumentName="AWS-RunShellScript")` 로 `/domain/start` 를 구현한다(`handler.py:241`) |
| `ssm:DescribeInstanceInformation` | rc=0 (정지 중이라 목록은 비어 있었다 — **부재가 아니라 미등록**) |

⇒ 🙋 **소유자 재지정(2026-09-04): SSM 을 «읽기 전용» 으로 쓴다.** 조회 명령만 돌린다.

### 이 정정이 바꾸는 것

| 칸 | 전 | 후 |
|---|---|---|
| **칸 4** (`617` 라이브) | 로그 접근 없음 ⇒ 미측정 예정이었다 | 🟢 **측정 가능** — `journalctl -u demo-stack` 직독 |
| **칸 6** (`618` AC-5) 컨테이너 축 | `/domains` 의 **개수**로만 반쪽 판정 | 🟢 **이름으로** 판정 — `docker ps -a` |
| **칸 7** (`615` ⓐ) | 「측정 불가」로 **닫았다** | 🔴 **철회한다** — 그 티켓이 적은 «다시 열 조건 **A**(SSM/ssh 제공)» 가 **이미 충족돼 있었다** |

🔵 **`615` ⓐ 의 «다시 열 조건» 을 사건으로 적어 둔 것이 값을 했다.** 날짜로 적었으면
이 정정이 그 칸을 되살리지 못했을 것이다.

🔴 **그러나 칸 7 의 PASS 기준은 안 바뀐다** — 매달림은 확률적이라 이번 부팅에서 안 걸리면
**미측정(사유: 부팅 N회에서 미재현)** 이고, 그것은 「고쳐졌다」가 아니다. 바뀐 것은
사유가 **「볼 수 없었다」에서 「봤는데 안 걸렸다」로** 옮겨간다는 것뿐이다.

### 칸 7 — `615` **ⓐ 매달림(rc=124)** (in-progress · 부수 아님, 칸이다)

| | |
|---|---|
| **판정 술어** | 부팅 로그(`journalctl -u demo-stack`)에 `⏱ 매달림:` 줄 + 최종 요약이 있는가 |
| **선행 관측** | 부팅 소요 시간(정상 **690~777s**). 크게 넘으면 매달림 후보 |
| 🔴 미재현이면 | **미측정(사유: 부팅 1회 미재현)** — N 을 명시한다. **PASS 아님** |
| PASS 는 …이다 | 매달림이 **발생했고** 그 두 흔적이 **남았다** |

## 📊 소화율 분모 갱신 — **6 → 7**

## 🔴 창이 열린 경위 — **`/start` 가 아니라 `terraform apply` 였다** (정직하게 적는다)

AMI 핀을 적용하자 terraform 이 인스턴스를 **교체하며 곧바로 기동**시켰다. 즉 이 창은
`POST /start` 로 연 것이 아니고, **매니페스트 커밋보다 부팅이 먼저**였다.

🔵 **AC-0 의 목적은 달성됐다** — 칸 목록·술어·대조군·순서는 **재굽기 58분 동안** 전부
작성돼 있었고(이 절 위 전체), 커밋만 몇 분 늦었다. 🔴 **그래도 문자 그대로는 어긴 것이므로
적는다.** 다음 번들은 이 순서를 알고 있어야 한다:

> 🔴 **`terraform apply` 는 「핀 갱신」이 아니라 「핀 갱신 + 기동」이다.**
> 다음 번들의 § 순서에서 ③과 ④를 **바꿔라** — 매니페스트를 커밋한 **뒤에** apply 하라.

🔵 그리고 **론처 탭이 없으므로** `idle_minutes=20` 을 내가 직접 막는다 —
30초 주기 `POST /heartbeat` 루프를 창 내내 돌린다.

# Task ID

TASK-MONO-638

# Title

공개 데이터가 **제품처럼 보이기엔 너무 얇다** — 콘솔·이커머스·팬 표본을 늘리고, 늘리면서 시드와 갈라지지 않게 한다

# Status

review

# Owner

monorepo

# Task Tags

- demo
- public-data
- content

---

# Goal

로그인 없이 보이는 세 화면의 데이터를 «둘러볼 만한 양» 으로 늘린다.

- **콘솔** — 로그인 없이 보이는 표본 확충
- **이커머스** — **카테고리별** 아이템 확충(지금은 카테고리당 1~2개)
- **팬** — 피드·아티스트 확충 + **아티스트 프로필 이미지**(지금 전부 `null`)

🔴🔴 **이 티켓의 어려운 부분은 «많이 쓰기» 가 아니라 «갈라지지 않게 늘리기» 다.** 공개
데이터는 두 곳에 산다 — 실제 시드 DB(기동하면 보이는 것)와 번들 시드(백엔드 없이 보이는 것).
한쪽만 늘리면 **기동 전후로 카탈로그가 달라지고**, 그 거짓은 «로그인하니 상품이 사라졌다»
라는 모양으로 나타난다.

---

# Context — 실측 (2026-09-08 UTC, `main` = `ab1c5d748`)

## 지금 있는 양

| 데이터셋 | 수량 | 비고 |
|---|---|---|
| `snapshots/store.json` | 상품 **8** · 카테고리 **6** | 상의 2 · 하의 2 · 전자기기 1 · 식품 1 · 스마트폰 1 · 노트북 1 |
| `snapshots/fan.json` | 아티스트 **3** · 게시물 **4** · 멤버십 **2** | 🔴 아티스트 `profileImageUrl` 이 **셋 다 `null`** · 게시물 `imageUrls` 전부 `[]` |
| `snapshots/console-sample.json` | 도메인 **7** | 각 도메인이 `metrics` + `tables` 를 든다 |

세 Vercel 표면은 지금 전부 **200** 이고 SSR 에 실제 값이 실려 있다(store 에 「갤럭시 S25」,
fan 에 「세아」). 콘솔은 클라이언트 렌더라 SSR HTML 로는 판정되지 않는다.

## 데이터가 사는 곳 — **두 벌이고, 정본이 서로 다르다**

| 화면 | 「기동하면 보이는 것」(실제 시드) | 「백엔드 없이 보이는 것」(번들 시드) |
|---|---|---|
| 이커머스 | `projects/ecommerce-microservices-platform/apps/product-service/src/main/resources/db/migration/V8__seed_sample_data.sql` **+ `db/migration-h2/` 쌍둥이** | `infra/demo/public-data/fixtures/raw-backend-responses.mjs` → `snapshots/store.json` |
| 팬 | `infra/demo/seed/seed-fan.sh` (API 시딩) | 같은 픽스처 → `snapshots/fan.json` |
| 콘솔 | — (**백엔드 추출 경로가 없다**) | `fixtures/console-sample.mjs` → `snapshots/console-sample.json` |

`infra/demo/public-data/README.md` 가 규칙을 이미 적어 두었다:

- `snapshots/` 는 **손으로 쓰지 않는다** — `bin/build-bundled-snapshots.mjs` 가 픽스처에서 만든다.
- `fixtures/raw-backend-responses.mjs` 는 **실제 시드 DB 의 행 + 음성 대조군**이다.
  🔴 음성 대조군(본명 · `accountId`/`tenantId` · 옵션별 재고 · 회원 전용 본문 · 삭제된 글 ·
  숨김 상품 · 비공개 아티스트의 고아 글)은 **변환기가 그것을 지우는지 시험하는 존재 이유**다.
  지우면 그 시험이 «아무것도 안 하면서 초록» 이 된다.

---

# Scope

## 포함

- `infra/demo/public-data/fixtures/raw-backend-responses.mjs` · `console-sample.mjs` · `membership-plans.mjs`
- `infra/demo/public-data/snapshots/*.json` — **재생성으로만** 바뀐다
- `infra/demo/seed/seed-fan.sh` — 팬 실제 시드
- `projects/ecommerce-microservices-platform/apps/product-service/src/main/resources/db/migration/` **및 `db/migration-h2/`** — 이커머스 실제 시드(신규 마이그레이션)
- 필요한 경우 `infra/demo/public-data/tests/` — 늘어난 모집단을 무는 단언

## 제외

- 🔴 Vercel Blob 발행(`BLOB_READ_WRITE_TOKEN`·`DEMO_PUBLIC_DATA_BASE_URL`) — **소유자 승인
  대기 중인 인프라 작업**이다. 이 티켓은 **커밋된 번들 시드**(ADR-MONO-070 폴백 ②)만 늘린다.
  그것이 «백엔드 없이 화면이 선다» 를 참으로 만드는 경로다.
- 론처 카드 UI(`TASK-MONO-637`) · 화면 캡처(`TASK-MONO-639`)
- 공개 댓글 · 게스트 장바구니 — `TASK-MONO-635` 가 근거를 대고 제외했다. **되살리지 마라.**

---

# Acceptance Criteria

## AC-0 — 착수 전 실측 (verify-then-act)

- [x] 세 스냅샷의 **현재 수량**을 다시 세서 적는다(위 표는 이 티켓 기안 시점의 값이다).
- [x] `bin/build-bundled-snapshots.mjs` 를 **손대지 않은 트리에서 한 번 돌려** 산출물이
      커밋된 스냅샷과 **동일한지** 확인한다. 🔴 다르면 스냅샷이 이미 손으로 편집된 것이고,
      그때는 늘리기 전에 그 사실부터 처리해야 한다(늘린 뒤에는 원인을 못 가른다).

## AC-1 — 이커머스: 카테고리별로 볼 만해진다

- [x] **모든 카테고리가 최소 4개** 이상의 공개 상품을 갖는다(지금은 1~2개).
- [x] 🔴 `categories[].productCount` 가 **실제 공개 상품 수와 일치**한다. 지금은 우연히
      맞지만, 늘리면서 어긋나면 화면이 «6개» 라 써 놓고 3개를 그린다.
- [x] 실제 시드에도 같은 상품이 들어간다 — 🔴🔴 **기존 `V8` 을 고치지 마라.** 이미 적용된
      볼륨에서 Flyway checksum 이 깨진다. **새 버전 마이그레이션**으로 추가한다.
- [x] 🔴 `db/migration/` 과 `db/migration-h2/` **둘 다** 간다. 한쪽만 고치면 H2 로 도는 경로
      (`TASK-MONO-633` 이 조사 중인 그 경로)에서만 조용히 갈라진다.
- [x] 상품 이미지는 **스토어가 이미 쓰는 방식**(Unsplash CDN URL)을 따른다 — 새 이미지
      호스트를 도입하지 않는다.

## AC-2 — 팬: 피드와 아티스트가 늘고, 얼굴이 생긴다

- [x] 아티스트를 **최소 6명**으로 늘리고, 게시물을 아티스트마다 최소 2개(공개 1 + 잠금 1)
      갖도록 늘린다. 잠금 글이 있어야 «로그인하면 더 있다» 가 화면에서 참이 된다.
- [x] 🔴🔴 잠긴 글은 `body` 와 `bodyPreview` 를 **둘 다 안 싣는다**(ADR-MONO-070). 늘리면서
      이 성질이 깨지지 않는지 **음성 대조군 시험으로** 확인한다.
- [x] **`profileImageUrl` 을 채운다.** 🔵 소유자 결정(2026-09-08): **라이선스 인물사진
      (Unsplash)** 을 연결한다 — 스토어 상품 이미지와 같은 CDN·같은 계약이라 새 의존이 없다.
- [x] 🔴🔴 **실존 인물을 참고하지 않는다.** 이 데모의 아티스트(루미·노아·세아 …)는 가공
      인물이고, 실존 연예인을 닮게 만들면 **실존 인물의 초상**이 포트폴리오에 들어간다.
      필요한 것은 «그럴듯한 아티스트 사진» 이지 «누군가를 닮은 사진» 이 아니다.
- [x] 이미지가 **없거나 404 일 때의 폴백**이 화면에 있다(아바타/이니셜). 지금 값이 전부
      `null` 이므로 폴백은 **오늘의 정상 경로**이고, 회귀하면 얼굴 자리가 깨진다.
- [x] 실제 시드(`seed-fan.sh`)에도 같은 아티스트·게시물이 들어간다.

## AC-3 — 콘솔: 로그인 없이 보이는 표본 확충

- [x] 도메인별 `metrics`·`tables` 표본을 «화면이 비어 보이지 않을» 만큼 늘린다.
- [x] 🔴 콘솔 공개 표본은 **백엔드 추출 경로가 없는** 큐레이션 데이터다. 그 사실을
      `console-sample.mjs` 머리말에 명시한다 — 나중 사람이 «추출이 고장났다» 로 오진하지 않게.
- [x] 🔴 `(console)` 가드의 **행위를 바꾸지 않는다**(`TASK-MONO-635` 가 세운 경계).

## AC-4 — 두 벌이 갈라지지 않는다

- [x] 🔴🔴 **가드 한 칸**: 번들 시드와 실제 시드가 **같은 것을 말하는지** 대조한다. 최소한
      이커머스 상품의 **키 집합**(id 또는 name)이 두 곳에서 같은지 본다.
      선례: `(z32)` 가 `projects.sh` ↔ `handler.py` 의 묶음 표를 같은 이유로 대조한다.
- [x] 그 가드에 **하한을 두지 마라 vs 두어라를 먼저 판단하고 근거를 적는다.** 이 모집단은
      «늘어나는 것이 목표» 라 하한이 성립하지만, 줄어드는 모집단에 하한을 걸면 성공이
      고장난다(이 저장소가 7회 밟은 함정). 어느 쪽인지 **세어 보고** 정하라.
- [x] **bite**: 한쪽에만 상품을 추가하면 그 가드가 빨개진다.

## AC-5 — 🔴🔴 이미지를 늘리면 **이미 초과된 축**을 더 민다

`TASK-MONO-587` 이 이름 붙인 축이 있다. Vercel **Image Optimization** 은 2026-08-26 실측에서
이미 **Transformations 5.6K/5K(112%)** · **Cache Writes 320K/100K(320%)** 였고, 2026-08-29
Pro 전환으로 «막힌다» 가 «청구된다» 로 바뀌었을 뿐 초과는 그대로다.

그리고 세 앱이 이 축을 **다르게** 탄다(실측 2026-09-08):

| 앱 | 이미지 처리 | 이 축을 타는가 |
|---|---|---|
| `web-store` | `next/image` + `remotePatterns: images.unsplash.com` | 🔴 **탄다** |
| `fan-platform-web` | `images: { unoptimized: true }` | 🟢 안 탄다 |
| 론처(`hubwang.com`) | 정적 HTML 의 `<img>`(빌드가 파일을 복사) | 🟢 안 탄다 |

- [x] 🔴 **AC-1 이 상품을 늘리면 그만큼 변환 요청이 는다.** 늘리기 전에 «상품 N개 추가 =
      변환 몇 건 증가» 를 **산술로 적어라**(고유 이미지 수 × 렌더되는 크기 수).
- [x] 그 증가분을 감당할지, 아니면 카탈로그 썸네일을 최적화 밖으로 뺄지는 **`TASK-MONO-587`
      의 삼지선다에 속한다.** 🔴 이 티켓에서 조용히 정하지 마라 — 측정치를 적고, 587 을
      인용해 소유자 결정이 필요한지 판단한 뒤 그 판단을 적는다.
- [x] 🔵 팬 아티스트 이미지는 `unoptimized: true` 라 이 축에 **안 걸린다** — 그러므로 AC-2 는
      이 제약과 무관하게 진행해도 된다. 그 사실을 여기 적어 두는 이유는, 안 적으면 다음
      사람이 «이미지 = 할당량» 으로 뭉쳐서 걸리지도 않는 것을 막기 때문이다.

## AC-6 — 늘려도 성질이 유지된다

- [x] `infra/demo/public-data` 패키지 시험이 **전부 통과**한다(현재 31/31 — 재측정해서 적는다).
- [x] 🔴 누출 대조군이 **여전히 문다** — 늘린 픽스처에도 음성 대조군이 들어 있고, 변환기가
      그것을 지운다. 🔴🔴 **모집단이 커지면 «독립 grep 0건» 이 더 약한 증거가 된다**(찾을
      곳이 늘었다). 대조군을 **함께** 늘려라.
- [x] `coverage`·`collectionStatus` 봉투가 늘어난 수량을 정확히 말한다 — 「정상적인 0건」과
      「수집 실패」를 가르는 필드이므로 수량이 틀리면 그 구별이 무의미해진다.

---

# Related Specs / Contracts

- [`ADR-MONO-070`](../../docs/adr/ADR-MONO-070-public-browsing-without-the-backend.md) — 공개 필드 허용 목록 · 잠긴 글 · 봉투 계약
- [`infra/demo/public-data/README.md`](../../infra/demo/public-data/README.md) — § 픽스처 · § 시드 (스냅샷은 손으로 쓰지 않는다)
- `TASK-MONO-635` — 이 데이터셋을 만든 티켓(`review/`) · `TASK-MONO-633` — H2 경로
- `TASK-MONO-637`(론처 UI) · `TASK-MONO-639`(캡처) — 이 티켓이 **선행**이다: 데이터가 얇은
  채로 캡처하면 캐러셀이 오늘의 빈 화면을 영구히 박제한다

---

# Edge Cases

| 상황 | 기대 |
|---|---|
| 카테고리에 공개 상품이 0개 | 「정상적인 0건」으로 표시(수집 실패와 구별) — 다만 AC-1 이 그런 카테고리를 없앤다 |
| 잠긴 글만 있는 아티스트 | 공개 목록에 아티스트는 뜨고 글은 «멤버십 전용» 로 표시 |
| Unsplash 이미지 404 | 아바타 폴백. 카드·목록은 그대로 |
| 스냅샷 재생성이 픽스처와 어긋남 | 빌드가 실패해야 한다(조용히 옛 스냅샷을 남기지 않는다) |
| 실제 시드에만 있는 상품 | AC-4 의 가드가 문다 |

---

# Failure Scenarios

1. **`snapshots/*.json` 을 손으로 편집한다** → README 가 금지한 것이고, 다음 재생성에서
   조용히 되돌아간다. 그리고 그 되돌아감은 «데이터가 사라졌다» 로 보인다.
2. **번들 시드만 늘린다** → 기동 전에는 상품 30개, 기동 후 로그인하면 8개. 방문자에게는
   «로그인했더니 카탈로그가 줄었다» 로 보인다.
3. **기존 `V8` 마이그레이션을 고친다** → 기존 볼륨에서 Flyway checksum 실패. 🔴 신선 볼륨
   CI 는 **영구 초록**이라 이 결함을 못 잡는다.
4. **`migration-h2/` 쌍둥이를 빠뜨린다** → H2 로 도는 경로에서만 갈라지고, 그 경로는 지금

5. **이미지를 늘리면서 변환 할당량 증가를 안 센다** → 이미 112%/320% 인 축을 말없이
   더 민다. Pro 라 «막히지» 않고 **청구된다** — 즉 아무 화면도 안 빨개지고 청구서만 는다.
   `TASK-MONO-633` 이 조사 중이라 원인이 섞인다.
6. **음성 대조군을 지우거나 안 늘린다** → 누출 시험이 «아무것도 안 하면서 초록» 이 된다.
7. **실존 아티스트를 참고한 이미지를 넣는다** → 실존 인물의 초상이 포트폴리오에 들어간다.

---

# 분석 / 구현 권장

분석=Opus 5 / 구현 권장=**Opus** (데이터 양은 단순하지만 «두 벌이 갈라지지 않게» 가 설계
판단이고, Flyway 마이그레이션 추가와 음성 대조군 확충이 조용히 틀리기 쉬운 자리다).

---

# Verification — 구현 세션 (2026-09-08 UTC)

## AC-0 — 착수 전 실측 (verify-then-act)

**수량 재측정** (`main` = `a0273922a`): 스토어 상품 **8** · 카테고리 6 / 팬 아티스트 **3**
(`profileImageUrl` 셋 다 `null`) · 게시물 4 / 콘솔 도메인 7.

**손대지 않은 트리에서 `--check`** → **`rc=0`, 드리프트 없음.**

🔵 이 칸이 이번에 «진짜» 가 됐다. 이 티켓을 시작하려다 발견한 것이 `TASK-MONO-640` 이다 —
생성기(`bin/build-bundled-snapshots.mjs`)가 `.gitignore` 의 `bin/` 에 삼켜져 저장소에 없었고,
게다가 `--check` 는 CRLF 체크아웃에서 **세 데이터셋 전부 «드리프트»** 라고 거짓 보고했다.
🔴 이 티켓의 AC-0 은 그 경우 «손으로 편집된 것» 이라고 적어 두었는데, 그대로 따랐다면 세
파일을 통째로 다시 써서 **거대한 가짜 diff** 를 만들었을 것이다. 640 이 둘 다 고쳤고,
그래서 위 `rc=0` 은 처음으로 의미 있는 측정이다.

## AC-1 — 이커머스: 카테고리별로 볼 만해진다

- 상품 **8 → 24**. 여섯 리프 카테고리가 각각 **정확히 4개**(전에는 1~2개).
- `categories[].productCount` ↔ 실제 공개 상품 수: **6/6 일치**(스크립트로 대조).
- 🔴 **기존 `V8` 을 고치지 않았다.** 새 마이그레이션으로 추가했다:
  `migration/V19__seed_more_sample_data.sql` · `migration-h2/V12__seed_more_sample_data.sql`.
- 🔴 쌍둥이 **둘 다** 갔다. 🔵 컬럼 목록이 서로 다른 것이 **정상**이다 — postgres 트리는
  `V13`(tenant_id NOT NULL)·`V14`(seller_id NOT NULL)를 지나왔고 h2 트리는 `V11` 까지라
  그 컬럼이 없다. 그 사실을 두 파일 머리말에 서로를 가리키며 적었다.
- 상품 이미지는 스토어가 이미 쓰는 방식(Unsplash CDN URL)을 따랐다 — 새 호스트 없음.

### 🔵 실행으로 검증했다 — 문법만 본 것이 아니다

h2 마이그레이션 **12개를 실제 H2 에 순서대로 적용**하고(`org.h2.tools.RunScript`,
`MODE=PostgreSQL`) 결과를 질의했다:

```
V1 … V11 ✔ · V12__seed_more_sample_data.sql ✔     (12/12 적용 성공)

SELECT c.name, COUNT(p.id) FROM categories c LEFT JOIN products p …
  노트북 4 · 상의 4 · 스마트폰 4 · 식품 4 · 전자기기 4 · 하의 4 · 의류 0
SELECT COUNT(*) FROM products         → 24
SELECT COUNT(*) FROM product_variants → 65
```

🔵 `의류 0` 은 부모 카테고리다(리프가 상의/하의). 번들 시드와 **같은 분포**다.

## AC-2 — 팬: 피드와 아티스트가 늘고, 얼굴이 생긴다

- 아티스트 **3 → 6**, 게시물 **4 → 12**. 여섯 명 **전원**이 공개 1 + 잠금 1 을 갖는다
  (전에는 잠긴 글이 루미에게만 있어서, 나머지 카드는 «로그인하면 더 있다» 를 보여 줄 방법이
  없었다).
- 🔴🔴 잠긴 글은 `body`·`bodyPreview` **둘 다 없다** — 스크립트로 전수 확인(누출 0건).
- **`profileImageUrl` 을 여섯 명 전원 채웠다.** 🔵 소유자 결정(2026-09-08): 라이선스
  인물사진(Unsplash). 🔴🔴 **실존 인물을 참고하지 않았다.**
- 이미지 없음/404 폴백은 기존 경로가 이미 처리한다(오늘까지 여섯 명 전원이 `null` 이었으므로
  그 경로가 **오늘의 정상 경로**였다) — 회귀시키지 않았다.

### 실제 시드는 **네 파일**에 걸쳤다 — 형제를 grep 해서 찾았다

`seed-fan.sh` 는 ARTIST_POST 를 **아티스트 본인 로그인으로** 발행한다. 그래서:

| 파일 | 무엇 |
|---|---|
| `infra/demo/seed/seed-fan.sh` | 아티스트 셋 추가 · 여섯 명 `profile_image_ref` · 글 8개 |
| `iam …/account-service …/R__06_…sql` | identities · accounts · account_roles 각 3행 |
| `iam …/auth-service …/R__02_…sql` | credentials 3행 |
| `iam …/auth-service …/FanArtistDemoSeedTest.java` | 🔴 **핀** — `hasSize(3)` ×2 · 정규식 `^ARTIST_([A-C])` |

🔴 그 핀을 안 찾았으면 이 변경은 CI 에서 빨개졌을 것이다. 셋 → 여섯으로 올리면서,
그 수가 «비어 있지 않음» 을 재는 **바닥**이지 «정확히 몇 명인가» 라는 제품 사실이 아니라는
주석을 함께 넣었다.

🔵 **실행으로 검증**: `./gradlew :…:auth-service:test --tests '*FanArtistDemoSeedTest*'`
→ **BUILD SUCCESSFUL**. 그 테스트가 세 시드 파일을 서로 대조하므로, 여섯 아티스트에 대해
자격증명·계정·역할·`seed-fan.sh` 의 id 가 **일치한다는 것이 실행으로 확인됐다.**

## AC-3 — 콘솔: 로그인 없이 보이는 표본 확충

- 표 행 **30 → 65**(8개 표 전부). `overview` 는 도메인 다섯 → **여덟**(실제 도메인 수와 일치).
- 🔴 백엔드 추출 경로가 없는 큐레이션 데이터라는 사실은 `console-sample.mjs` 머리말이
  **이미** 명시하고 있다(§ "여기에는 백엔드에서 온 값이 한 줄도 없다") — 새로 적지 않고 그대로 뒀다.
- `(console)` 가드의 행위는 **한 줄도 안 건드렸다**.

## AC-4 — 두 벌이 갈라지지 않는다

**가드 `scripts/check-seed-catalogue-parity.sh`** — 세 출처의 상품 id 집합을 대조한다
(선례: `(z32)` 의 묶음 표 대조).

```
check-seed-catalogue-parity: OK — 번들 24 · postgres 24 · h2 24 · 세 출처의 상품 id 집합이 같습니다.
```

**하한을 둘 것인가 — 두지 않기로 정했고, 근거를 적는다.** 이 모집단은 «늘어나는 것이 목표»
라 하한이 성립할 것처럼 보이지만, 상품 수는 **제품 결정**이고 줄어들 수도 있다(단종·개편).
거기에 하한을 걸면 그 결정이 도착하는 날 **성공이 고장난다** — 이 저장소가 일곱 번 밟은
함정이다. 🔴 대신 **비공허성**에 걸었다: 세 출처가 각각 0건이 아니어야 한다(0건이면 세 빈
집합이 서로 «일치» 하며 조용히 통과한다).

🔵 카테고리 하한(AC-1 의 «카테고리마다 4개»)은 **이 가드에서 재지 않는다.** 축이 다르고,
섞으면 카테고리를 개편하는 날 이 가드가 빨개지면서 그 빨강이 «갈라졌다» 로 잘못 읽힌다.

🔵 파일 이름을 고정하지 않고 `V*seed*.sql` 을 훑는다 — 다음 사람이 `V20` 을 더할 때 이
스크립트도 고쳐야 한다면, **안 고치는 날 그 파일은 대조에서 빠진 채 초록**이 된다.

**self-test 4칸** — 실트리를 변형해서 증명한다:

| 칸 | 무엇 | 결과 |
|---|---|---|
| (1) 대조군 | 손대지 않은 트리 | ✅ 위반 0 |
| (2) bite | **postgres 시드에만** 상품을 더한다 | ✅ 물었다 |
| (3) bite | **h2 시드에만** 상품을 더한다 | ✅ 물었다 |
| (4) 대조군 | 되돌리면 다시 초록인가(주입 파일이 남지 않았는가) | ✅ 위반 0 |

🔴 (2)만 있으면 h2 축은 **한 번도 안 재어진다.** 두 축을 따로 문다.

## AC-5 — 이미 초과된 이미지 변환 축

**산술** (AC 가 요구한 대로 숫자로 적는다):

- 신규 상품 16개 · 신규 **고유** 이미지 URL **16개**(상품당 1개, 중복 없음)
- `next/image` 의 변환은 (원본 × 너비 × 포맷) 단위이고 **첫 생성 후 캐시**된다
- 렌더 너비를 3종, 포맷을 2종(avif·webp)으로 **보수적으로** 잡으면
  → 일회성 증가 **≤ 16 × 3 × 2 = 96건**

**판단: `TASK-MONO-587` 의 삼지선다를 부르지 않는다.** 월 허용치가 5,000건이고 실측 사용량이
5.6K(112%)인데, 이 변경의 증가분은 **일회성 ≤96건 = 월 허용치의 ≤1.9%** 다. 축을 «더 민다»
는 것은 사실이지만 그 크기가 결정을 바꾸지 않는다. 🔴 그래서 **여기서 조용히 정한 것이
아니라, 재고 나서 «부르지 않는다» 를 판단으로 적는다.**

🔵 실측(2026-09-08)한 세 앱의 차이도 그대로다: `web-store` 만 이 축을 타고
(`next/image` + `remotePatterns`), `fan-platform-web` 은 `images: { unoptimized: true }` 라
**안 탄다** ⇒ 아티스트 사진 6장은 이 축과 무관하다.

## AC-6 — 늘려도 성질이 유지된다

- `infra/demo/public-data` 패키지 시험 **31/31 통과**(`rc=0`).
- 🔴 누출 대조군을 **함께 늘렸다**: fan 13 → **29** · store 2 → 2 · console 17 → **39**.
  모집단이 커지면 «독립 grep 0건» 이 더 약한 증거가 되기 때문이다.
- **독립 grep** — 스냅샷에서 0건, 픽스처에서 20건(**양성 대조군**: 탐지기가 실제로 찾을 수
  있다는 것을 먼저 보인다):

  | 바늘 | 스냅샷 | 픽스처 |
  |---|---|---|
  | `MUST-NOT-LEAK` | **0** | 20 |
  | `realName` · `김하늘` · `accountId` · `HIDDEN-PRODUCT` | **0** | — |

- `coverage` 봉투가 늘어난 수량을 정확히 말한다(빌더가 그 값을 세서 찍는다:
  `artists=6 posts=12`, `products=24 categories=6`, `domains=7`).

## 실행한 게이트

| 게이트 | 결과 |
|---|---|
| `scripts/check-seed-catalogue-parity.sh` (+ `--self-test`, `bash -n`) | ✅ rc=0 · 번들 24 · pg 24 · h2 24 |
| `node bin/build-bundled-snapshots.mjs --check` | ✅ rc=0 |
| `node --test tests/public-data.test.mjs` | ✅ **pass 31 · fail 0** |
| **H2 에 마이그레이션 12개 실제 적용 + 질의** | ✅ 24 상품 · 카테고리마다 4 · 변형 65 |
| **`FanArtistDemoSeedTest` 실행** | ✅ BUILD SUCCESSFUL |
| `bash -n infra/demo/seed/seed-fan.sh` | ✅ rc=0 |
| `check-package-script-targets` · `check-message-backticks` (+ self-test) | ✅ 전부 rc=0 |
| `ci.yml` YAML 파싱 + 배선 사슬 4단계 | ✅ 잡 57개 |
| 스테이지 diff 크기 점검(줄바꿈 손상 여부) | ✅ 전부 실제 변경분에 비례 |

🔵 마지막 칸을 일부러 넣었다: 이 세션의 편집 스크립트가 세 파일을 **CRLF 로** 썼고(파이썬
텍스트 모드의 기본), git 이 스테이지에서 LF 로 정규화했다. 정규화가 안 됐다면 `seed-fan.sh`
같은 파일이 **전체 재작성**으로 보였을 것이다 — `TASK-MONO-640` 이 방금 이름 붙인 함정이다.

🔴 **미측정 — 실제로 기동해서 본 것은 없다.** 아래 셋은 데모 스택이 떠야 잴 수 있고,
소유자가 이번 세션에 EC2 를 켜지 않기로 했다:

1. `seed-fan.sh` 가 새 아티스트 셋으로 **실제 로그인해 글을 발행하는가.**
   🔵 위험은 낮다: 기존 셋과 **복사-동일**한 구조이고, `seed_fail` 은 소프트 실패라
   부팅을 죽이지 않고 요약에 기록된다. 그리고 세 시드 파일의 **일치는 핀 테스트가 실행으로
   확인**했다. 그래도 «로그인이 실제로 되는가» 는 안 쟀다.
2. postgres `V19` 가 **기존 볼륨에** 적용되는가(H2 로만 실행 검증했다).
3. 늘어난 데이터가 세 화면에 **실제로 그려지는가**(머지 → Vercel 배포 뒤 확인 가능).

## CORRECTION — 위 § Verification 이 «검증했다» 고 적은 것 중 둘이 결함을 못 봤다

머지 전 CI 가 **여섯 건**을 잡았고 그중 **둘이 진짜 결함**이었다. 위 절은 그것들을 모르는
상태로 쓰였으므로, 지우지 않고 여기 정정한다.

### 진짜 결함 ① — `V19` 가 `product_variants.tenant_id` 를 안 채웠다

Testcontainers 통합 시험에서 마이그레이션이 죽었다:
`null value in column "tenant_id" of relation "product_variants"`.
`products` 의 `tenant_id`·`seller_id` 는 챙기고 **변형 표를 놓쳤다**.

🔴🔴 **위 절의 「H2 에 12개를 실제로 적용했다」가 이것을 못 잡은 이유**: 두 트리는 **다른
스키마를 지나왔다.** h2 트리는 `V11` 까지라 `product_variants` 에 `tenant_id` 가 **아예
없다** ⇒ 한쪽의 초록은 다른 쪽의 증거가 아니다. 그런데 위 절은 그 실행을 «마이그레이션을
실행으로 검증했다» 로 뭉뚱그렸다 — 실제로 검증된 것은 **h2 트리뿐**이었다.

처방: 변형 INSERT 에 `tenant_id` 추가. Docker 가 없어 실제 postgres 로는 못 돌렸으므로,
마이그레이션 DDL 에서 **NOT NULL(DEFAULT 없는) 컬럼을 뽑아 INSERT 가 전부 덮는지** 기계적으로
검사했다(두 트리 누락 0건). 🔵 그 검사기가 **수정 전 판에서 `tenant_id` 를 잡는다**는 양성
대조군을 함께 돌렸다 — 안 그러면 「누락 없음」이 검사기 고장과 구별되지 않는다.

### 진짜 결함 ② — `R__02` 자격증명 시드가 MySQL 문법 오류였다

`You have an error in your SQL syntax ... near '),\n('`. 행을 추가하는 스크립트가 괄호를
하나 더 넣었다. auth-service 가 컨텍스트를 못 띄워 fan live-trio e2e 두 클래스가
`initializationError` 로 죽었다.

🔴🔴 **위 절의 「`bash -n` rc=0」과 「6/6 일치」가 이것을 못 잡은 이유**: 그 둘은 **결함의
축과 다른 것을 잰다.** `bash -n` 은 셸 문법만 보고(이 파일은 SQL 이다), 행 수 세기는 괄호를
안 센다. 즉 나는 «검증했다» 고 적었지만 그 검사들은 이 결함을 **볼 수 없는 종류**였다.

처방: 괄호 수정 + **H2 를 MySQL 모드로 띄워 두 시드 SQL 을 실제로 실행**했다:
`R__02 ✅ · R__06 ✅ · credentials 6 · identities 6 · accounts 6 · account_roles 12 · ARTIST 6`.
그리고 `FanArtistDemoSeedTest` 를 다시 돌렸다(BUILD SUCCESSFUL).

### 나머지 넷 — 제품 수치를 얼려 둔 핀

| 어디 | 얼려 둔 것 | 바꾼 것 |
|---|---|---|
| `public-pages.test.tsx` | 아티스트 `toHaveLength(3)` · 글 제목 리터럴 둘 | 저장본에서 파생(+ 비공허성 하한) |
| `demo-tour.spec.ts` (콘솔 e2e) | 표 전체 행 `toHaveCount(4)` | 좁히기 **전** 개수를 읽어 «1보다 크다» |
| `ProductRegisterQueryIntegrationTest` ×2 | 페이지 크기 20 을 «시드가 작다» 의 대리로 사용 | 전체 개수를 먼저 물어 그만큼 요청 |

🔵 넷 다 **수치를 올리지 않고 성질로 바꿨다**. 3→6, 4→9 로 올렸으면 다음에 데이터가 바뀔 때
또 깨진다 — 그 시험들의 축은 «몇 개인가» 가 아니었다.

### 🔴 일반화 — 이 티켓이 남기는 교훈

두 결함 모두 **검사가 재는 축이 결함의 축과 달랐다**. 「H2 초록」을 postgres 의 증거로,
「셸 문법」을 SQL 의 증거로 썼다. 두 번 다 검사 자체는 통과했고, 통과가 아무것도 뜻하지
않았다. ⇒ 검사를 적을 때 **«이 검사는 어떤 결함을 볼 수 있는가»** 를 함께 적어야 한다.

### 최종 CI

**60개 체크 · 실패 0.**

# Task ID

TASK-MONO-606

# Title

더 이상 우리 것이 아닌 공인 IP 가 OAuth 콜백으로 **등록된 채 남는다.** 부팅마다 하나씩 늘고 걷어내는 코드가 없다. 🔴 넷 중 하나는 **public 클라이언트**다.

# Status

review

# Owner

monorepo

# Task Tags

- security
- demo
- iam

---

# 🔎 어떻게 나왔나 — `TASK-MONO-605` 의 **판정 밖**에서

`TASK-MONO-605` 가 `TASK-BE-582` 의 「기존 볼륨」 판정을 위해 데모 루트 볼륨 스냅샷을
복원해 `oauth_clients` 를 읽었다. 판정은 통과였는데, 그 행에 **더 이상 우리 것이 아닌
공인 IP** 가 콜백으로 남아 있었다.

## 기전 — 설계된 대로 동작한 결과다. **버그가 아니라 빠진 절반**이다

`infra/demo/seed-demo-domain.sh` 는 `.local/` 을 포함한 등록 URI 를 현재 데모 도메인으로
치환한 사본을 **`JSON_MERGE_PRESERVE` 로 덧붙인다.** 원본을 지우지 않는 것은 **의도**이고
그 헤더가 이유까지 적어 뒀다 — *"같은 DB 를 로컬 `*.local` 로도 쓸 수 있어야 한다."*

🔵 **그 판단은 옳다.** 문제는 **덧붙이기만 있고 걷어내기가 없다**는 것이다:

- 멱등성 가드(`WHERE ... NOT LIKE CONCAT('%', @dom, '%')`)는 **이번 도메인**만 본다.
  부팅마다 `@dom` 이 다르므로 **매번 가드를 통과하고 매번 새로 붙는다.**
- 옛 도메인을 지우는 코드는 이 저장소 어디에도 없다.
- **데모는 EIP 를 안 쓴다**(실측: `describe-addresses` = `[]`, terraform 에 `aws_eip` 없음)
  ⇒ 정지할 때마다 공인 IP 가 **AWS 풀로 반납**되고, 다음 부팅은 **다른 IP** 를 받는다.
- ⇒ **부팅 1회 = 죽은 도메인 1개 추가. 줄어드는 경로 없음.**

---

# ✅ AC-0 실측 (2026-08-30) — **모집단을 세대별로 다시 셌다**

🔴 **이 절이 이 티켓의 첫 판(2026-08-30 오전)을 한 군데 반증한다.** 그 판은
*"그 누적이 **AMI 에 굳어서** 배포된다 ⇒ 재굽기마다 직전 세대의 죽은 IP 를 상속한다"*
고 적었다. **거짓이다.** 데모 예산 0분, 임시 t3.small 하나에서 **AMI 스냅샷 자신을 복원**해
확인했다:

| 볼륨 | docker 볼륨 | `/var/lib/docker` | wtmp 부팅 | 죽은 sslip **IP** | 죽은 URI |
|---|---:|---:|---:|---:|---:|
| **옛 AMI** `ami-08df900798bc99c83` | **0** | 30M | 1 | **0** | **0** |
| **새 AMI** `ami-0caf015f7cd9144fd` | **0** | 36M | 1 | **0** | **0** |
| 옛 인스턴스 루트 (`i-033a3820845432e16`) | 129 | 1.6G | **4** | **3** | **30** (redirect 15 + post-logout 15) |
| **현재 인스턴스** `i-0c4721bdeb335885a` (재굽기 후) | 73 | — | **2** | **1** | **10** (redirect 5 + post-logout 5) |

🔴🔴 **AMI 는 DB 를 굽지 않는다 — 도커 볼륨은 첫 부팅에 생긴다.** 그러므로 누적은
**인스턴스 수명 안의 일**이고, **재굽기 + 인스턴스 교체가 0으로 리셋한다.**
실제로 그렇게 됐다: 옛 인스턴스 3개 → 현재 인스턴스 **1개**.

🔴 **첫 판이 어떻게 틀렸나**: 옛 인스턴스 볼륨의 모든 파일이 `2026-08-22` 각인이라
*"굽는 날 이후로 안 돌았다 ⇒ 굽기의 산물이다"* 로 읽었다. **«언제 쓰였나»에서
«어디서 왔나»를 추론한 것**이고 두 질문은 다르다. 확인법은 하나 — **AMI 를 열어 보는 것**.
[[feedback_verify_claims_against_the_source_itself]]

## 🔵 그래서 「상한 없음」의 의미가 좁아진다 — 그러나 사라지지는 않는다

| 첫 판이 말한 것 | 실측 |
|---|---|
| 세대를 넘어 영원히 자란다 | ❌ 재굽기가 리셋한다 |
| 부팅마다 +1, 걷어내는 코드 없음 | ✅ **그대로 참** — 옛 인스턴스는 4부팅에 3 IP 를 모았다 |

⇒ **위험은 「인스턴스가 오래 살수록」 자란다.** 데모는 온디맨드라 인스턴스가 몇 달 살고
그동안 수십 번 뜰 수 있다. 재굽기는 그 카운터를 리셋하지만 **정기적 사건이 아니다.**

---

# ✅ AC-1 실측 (2026-08-30) — **심각도. 넷이 한 덩어리가 아니다**

🔴 이 티켓의 첫 판은 *"심각도를 미리 정하지 마라"* 라고 적었다. 그대로 쟀다.

| 클라이언트 | ① 인증 방식 | ② PKCE 강제 | 죽은 콜백 |
|---|---|---|---|
| `platform-console-web` | 🔴 **`["none"]` = public** | `true` | 있음 |
| `fan-platform-user-flow-client` | `["client_secret_basic"]` | `true` | 있음 |
| `ecommerce-web-store-client` | `["client_secret_basic"]` | `true` | 있음 |
| `ecommerce-admin-dashboard-client` | `["client_secret_basic"]` | `true` | 있음 |

**③ 그 IdP 에 무엇이 있나**: `credentials` **7행** · `oauth_clients` 16 · `oauth2_authorization` 18 ·
`refresh_tokens` 28. **데모 규모의 시드 계정**이다.

## 🔴🔴 판정 — 「치명적」도 「무해」도 아니다. 그리고 **PKCE 가 이 축을 막지 못한다**

공격의 모양: 공격자가 그 죽은 IP 를 **AWS 에서 할당받고**, 그 호스트로 가는
`redirect_uri` 로 authorize 링크를 만들어 **로그인된 피해자가 클릭**하게 한다.
등록된 주소이므로 IdP 의 exact-match 검증을 **통과한다.**

🔴 **PKCE 는 이 시나리오를 막지 않는다.** PKCE 는 *정당한 클라이언트의* 코드를 가로채는
공격을 막는다 — 여기서는 **공격자가 요청자 자신**이라 `code_challenge` 를 자기가 고르고
`code_verifier` 를 자기가 들고 있다. **넷 다 `true` 인 것은 이 축에 대해 위안이 아니다.**

🔵 **실제로 막고 있는 것은 `client_secret` 이다.** 토큰 교환에 secret 이 필요한
**confidential 3종**(fan · ecommerce ×2)은 공격자가 코드를 받아도 **교환하지 못한다.**

🔴 **그 방어가 없는 것이 `platform-console-web` 하나다.** `["none"]` = public 이라
secret 요구가 없고 PKCE 는 위 이유로 안 막는다 ⇒ **코드를 토큰으로 바꿀 수 있다.**
그리고 그것은 **운영자 콘솔**이다 — 이 데모에서 권한이 가장 큰 표면.

## 🔵 과대주장하지 않는다 — 이 판정이 **재지 않은** 것

- **그 IP 를 실제로 할당받을 수 있는가**는 안 쟀다. 같은 리전에서 반복 할당으로 노리는
  기법이 알려져 있으나 **이 티켓이 시도하지 않았다.**
- **공격 창은 데모가 켜져 있는 동안뿐이다** — IdP 자체가 그 인스턴스에 있고 issuer 도
  IP 파생이다. 지금 데모는 `stopped` 다.
- **피해 범위는 데모 시드 계정 7개**다. 실계정이 아니다.
- 🔴 **그렇다고 「무해」가 아니다** — 운영자 콘솔의 public 클라이언트에 공격자 통제
  콜백이 등록돼 있는 상태는 **그 자체로 결함**이고, 고치는 비용이 낮다.

---

# ✅ AC-4 실측 — 모집단과 **경로 모양**

죽은 sslip 도메인을 든 클라이언트는 **4개**(옛 인스턴스 기준. 현재 인스턴스도 같은 4개).
🔴 **콜백 경로 모양이 다르다** — 한 술어가 넷을 다 덮는다고 가정하면 안 된다:

| 클라이언트 | 경로 |
|---|---|
| `platform-console-web` | `/api/auth/callback` |
| `fan-platform-user-flow-client` | `/api/auth/callback/iam` (호스트 2종: `fan-platform.` · `web.fan-platform.`) |
| `ecommerce-web-store-client` | `/api/auth/callback/iam` |
| `ecommerce-admin-dashboard-client` | `/api/auth/callback/iam` |

🔵 **`post_logout_redirect_uris` 도 같은 수만큼 오염된다** — 실측에서 redirect 와 1:1 이었다
(옛 인스턴스 15 + 15, 현재 5 + 5). **회수는 두 필드 다 해야 한다.**

## ✅ AC-2 술어 리허설 — **실측된 볼륨 위에서 산술을 확인했다**

술어 = 「`sslip.io` 를 포함하면서 **현재 도메인이 아닌 것**」.

| 대상 | 결과 | 뜻 |
|---|---|---|
| 현재 인스턴스, 현재 도메인 = `3-38-176-240` | **0건 삭제** | ✅ 살아 있는 것을 안 지운다 |
| 옛 인스턴스, 최신 도메인만 보존 | **10건 삭제** (15 중) | ✅ 3 도메인 중 2 도메인분만 정확히 지운다 |
| 보존 대상(`.local`/`localhost`/`hubwang.com`) | 6 클라이언트 **14 URI** | ✅ 술어에 안 걸린다 |

🔴 **이 리허설은 SQL 술어의 산술만 확인한 것이다.** 실제 회수 구현·멱등성·부팅 중 중단
안전성은 AC-2 가 여전히 열어 둔다.

---

# Goal

죽은 공인 IP 가 `redirect_uri` / `post_logout_redirect_uri` 로 등록된 채 남지 않게 한다.
🔵 심각도는 **이미 쟀다**(AC-1) — 남은 것은 **조치**다.

---

# Scope

**In:**

- `seed-demo-domain.sh` 에 「옛 데모 도메인 회수」 추가 (AC-2 · AC-3)
- 회수가 안 되면 빨개지는 가드 (AC-3)
- 🔴 `platform-console-web` 이 public 인 것이 **의도인지 확인**하고 적기 (AC-1′)

**Out:**

- `.local` / `localhost` / `hubwang.com` 항목 — **의도된 것**이고 로컬 개발이 쓴다
- 마이그레이션 파일 수정 — 체크섬을 깬다. **런타임 시드의 문제다**
- 데모를 고정 도메인으로 바꾸는 결정 — `ADR-MONO-067` D4 / `TASK-MONO-576`
- `platform-console-web` 의 인증 방식 **변경** — AC-1′ 이 「실수」로 판정하면 **별 티켓**이다

---

# Acceptance Criteria

## AC-0′ — 착수 시 **다시 세라** (verify-then-act)

위 실측은 `2026-08-30` 값이다. 착수 시점에 **현재 인스턴스의 죽은 도메인 수**를 다시 센다.
🔴 **파일 grep 금지 — 행으로.** 🔵 기동 불요: 정지된 인스턴스 루트를 `create-snapshot` →
복원 → `mysql:8.0` 로 읽는다(절차 전문은 에이전트 메모리 `PLAYBOOKS.md`).
🔴 **0 건이면 STOP** — 그 사이 무엇이 고쳤는지부터 찾는다.

## AC-1′ — 🔴 `platform-console-web` 의 `["none"]` 이 **의도인지** 확인하고 적어라

AC-1 이 이 클라이언트 하나를 다른 축에 올려 놨다. 조치 전에 **왜 public 인지**를 확인한다
(시드 마이그레이션과 콘솔 앱의 토큰 교환 경로를 읽는다).

- **의도라면** — 브라우저가 직접 교환한다는 뜻이고, 그렇다면 이 티켓의 회수는 **더 급하다**
  (secret 방어선이 없다).
- **실수라면** — 이 티켓보다 큰 결함이고 **별 티켓**이다. 🔴 여기서 고치지 마라.

## AC-2 — 회수는 **보존해야 할 것을 명시적으로 지키면서** 한다

- 지울 것: **`sslip.io` 이면서 현재 `DEMO_DOMAIN` 이 아닌 것.** `redirect_uris` 와
  `client_settings` 의 `post-logout-redirect-uris[1]` **양쪽**.
- 🔴 **`DEMO_DOMAIN` 이 비었거나 `local` 이면 아무것도 지우지 말고 종료** — 시드 스크립트의
  기존 early-exit 과 같은 규칙. 이 가드가 없으면 로컬 실행이 데모 등록을 날린다.
- 🔴 **넓은 술어 금지.** 「http 이면 삭제」는 `.local`(14 URI)을 같이 지운다.
- 🔵 산술은 위 § AC-2 술어 리허설에서 확인됐다 — **구현이 그 술어를 쓰는지**가 남았다.
- 🔴 **회수 후 원소 수 ≥ 1 을 단언하라.** 배열이 비면 그 클라이언트는 로그인 불가가 된다.

### 🟢 픽스처가 확보돼 있다 — 그리고 **랜딩 시점에 지워야 한다** (2026-08-30, 소유자 결정)

`TASK-MONO-605` 의 스냅샷 `snap-09449008990589c36` 을 **이 AC 를 위해 보존**하기로 소유자가
정했다. 이유는 하나다 — **죽은 sslip 도메인이 3개 쌓인 볼륨은 그것뿐**이고, 현재 인스턴스는
1개뿐이라(재굽기가 리셋했다) 「3개 중 2개만 지운다」를 **다른 데서 만들 수 없다.**

- 🔵 그래서 이 AC 의 검증은 **합성 픽스처가 아니라 실제로 쌓인 데이터** 위에서 할 수 있다.
- 🔴 **AC-3 의 양성 대조군(주입)은 여전히 별도로 필요하다** — 「실제 데이터로 통과했다」와
  「없는 것을 넣으면 무는가」는 다른 질문이다.
- 🔴🔴 **랜딩과 함께 지워라.** AWS 태그가 그 계약을 들고 있다:
  `KeepUntil=TASK-MONO-606-AC-2` · `KeepReason=...` · `DecidedBy=owner 2026-08-30`.
  ```bash
  aws ec2 delete-snapshot --snapshot-id snap-09449008990589c36   # AC-2 랜딩 후
  ```
  🔴 지우기 전에 **`TASK-MONO-605` § AC-3 의 삭제 트리거를 다시 읽어라** — 그 티켓이
  이 스냅샷의 처분을 기록한 정본이다.

## AC-3 — **재발하지 않게** 한다. 1회 청소는 조치가 아니다

- 회수를 **덧붙이기와 같은 트랜잭션**에 넣는다(부팅 중 죽어도 안전하게).
- 🔴 **가드가 필요하다.** 이 결함은 「부팅마다 하나씩」 자라 사람이 볼 때는 이미 커져 있다.
  `verify-demo-wrapper.sh` 계열에 **「현재 도메인이 아닌 sslip 등록이 0인가」** 칸을 넣어라.
- 🔴 **양성 대조군 필수** — 일부러 죽은 도메인 하나를 주입하고 가드가 **무는지** 본다.
  물지 않으면 그 가드는 없는 것이다. [[feedback_why_a_guard_does_not_bite]]
- 🔴 **모집단 ≥ 1 을 단언하라** — 대상이 0개로 계산되면 가드는 rc=0 으로 통과한다.
  [[feedback_a_runner_that_matches_no_package_exits_zero]]

## AC-4′ — 🔴 **양성 대조군은 「다음 부팅」에서 닫힌다. 그것을 명시하고 넘겨라**

*"회수 후 현재 도메인 콜백으로 로그인이 되는가"* 는 **데모가 떠 있어야** 잴 수 있고,
기동은 소유자 승인 + 예산 차감 사안이다.

- 🔴 **이 칸을 「했다」로 적지 마라.** 다음 기동 창에 실측하고, 그때까지는 **미측정**이다.
- 🔵 기동 없이 갈 수 있는 데까지는 간다: 스냅샷 복원 하네스에서 **회수 SQL 을 실제로 돌리고**
  결과 행을 읽는 것은 예산 없이 가능하다. **로그인 왕복만** 기동이 필요하다.
- 🔴 **회수만 하고 「깨끗해졌다」로 닫지 마라** — 이 저장소는 그 모양으로 데인 적이 있다.

## AC-5 — **이 조치가 안 고치는 것**을 적는다

- 죽은 IP 를 **이미 누가 갖고 있었다면** 과거에 무슨 일이 있었는지 이 티켓은 모른다.
  🔴 「없었다」고 적지 마라 — **안 쟀다.**
- 데모 도메인이 부팅마다 바뀌는 **구조 자체**는 그대로다(`ADR-MONO-067` D4 / `576`).
- `platform-console-web` 의 public 여부(AC-1′)는 별 축이다.

---

# Related Specs

- `infra/demo/seed-demo-domain.sh` — 기전이 사는 곳
- `tasks/review/TASK-MONO-605-…md` § 판정 밖에서 나온 것 + 두 번째 `## CORRECTION`
- `projects/iam-platform/apps/auth-service/src/main/resources/db/migration/V0008__create_oauth_tables.sql` — `client_authentication_methods` · `client_settings` 컬럼
- `docs/adr/ADR-MONO-067-…md` § D4 — 부팅마다 도메인이 바뀌는 구조

# Related Contracts

없음. OAuth2 `redirect_uri` 정확 일치는 RFC 6749 §3.1.2.3 / Spring Authorization Server 동작이다.

---

# Edge Cases

- 🔴 **현재 도메인을 「옛 것」으로 오판하면 지금 돌아가는 로그인이 깨진다.** 술어는 실행
  시점의 `DEMO_DOMAIN` 을 참조해야 하고, 비었거나 `local` 이면 **아무것도 안 한다.**
- 🔴 `client_settings` 의 post-logout 목록은 **Jackson default-typing** 이라 배열이 `[1]` 에 있다
  (`[0]` 은 타입 태그 문자열). `[0]` 을 배열로 다루면 조용히 망가진다 — V0016/V0021 의 교훈.
- 🔴 `redirect_uris` 를 **텍스트로** 다루면 MySQL 의 JSON 재직렬화와 싸우게 된다.
  파싱된 트리 위에서 다뤄라 — 시드가 이미 그 이유로 `JSON_TABLE` 을 쓴다.
- 🔵 **재굽기 + 교체가 누적을 리셋한다**(실측). 그러나 그것은 조치가 아니다 — 정기적이지 않다.
- 🔵 스냅샷 복원 하네스로 **예산 없이** AC-0′·AC-2 의 행 판정까지 갈 수 있다.

# Failure Scenarios

| 실패 | 증상 | 방어 |
|---|---|---|
| 술어가 넓어 `.local` 까지 삭제 | 로컬 개발 로그인 전멸 | AC-2 좁은 술어 + 보존 목록(14 URI) |
| `DEMO_DOMAIN` 없이 실행 | 등록이 통째로 날아감 | AC-2 early-exit |
| `post_logout` 을 안 고침 | 절반만 깨끗 | AC-2 양쪽 필드 |
| 1회 청소만 하고 닫음 | 다음 부팅부터 다시 자람 | AC-3 |
| 가드를 넣었는데 안 묾 | 매주 초록인데 목록은 자람 | AC-3 양성 대조군 |
| 대상 0개로 가드 통과 | rc=0, 아무 일도 안 함 | AC-3 모집단 ≥ 1 |
| 기동 없이 「로그인 된다」로 닫음 | 왕복을 안 재고 닫음 | AC-4′ 미측정 명시 |
| PKCE 를 방어선으로 계산 | 공격자가 요청자면 PKCE 는 안 막는다 | AC-1 판정에 명시 |

---

# ✅ 구현 결과 (2026-09-01 UTC) — **회수 + 가드 완료. AC-4′ 만 열려 있다.**

하네스: 데모 인스턴스 `i-0c4721…` 는 **`stopped` 그대로**. 임시 t3.small 1대 + 스냅샷 2개
(현재 세대 / 보존 픽스처)에서 판정하고 **전부 회수**했다. 🔵 **데모 예산 차감 0분.**

## AC-0′ — 착수 시 다시 셌다 ✅ (**0 아님 ⇒ STOP 아님**)

🔴 **파일 grep 이 아니라 행으로.** 현재 인스턴스 루트를 스냅샷 → 볼륨 → `mysql:8.0` 으로 읽었다.

| 잰 것 (현재 세대) | 값 |
|---|---|
| sslip URI | **10** |
| distinct sslip 호스트 | **5** (`console` · `fan-platform` · `web.fan-platform` · `web.ecommerce` · `admin.ecommerce`) |
| 세대 | **1** — 전부 `3-38-176-240` (2026-08-29 부팅) |
| 비-sslip URI (`.local`/`localhost`/`hubwang.com`) | **26** ← 보존 대상 |
| JSON 무효 행 | **0** |

🔴 인스턴스가 `stopped` 이므로 그 공인 IP 는 **반납됐다** ⇒ 10건 전부 **죽은 등록**이다.
🔵 티켓 본문의 08-30 실측(«현재 1세대 / 픽스처 3세대»)과 **일치**한다 — 그 사이 부팅이 없었다
(`LaunchTime=2026-08-29T15:50:37Z` 불변).

## AC-1′ — `["none"]` 은 **의도다** ✅ (repo 실측, 비용 0)

`V0015` 헤더가 이유까지 적어 뒀다 — *"Public-client lineage (ADR-003): … `client_secret_hash = NULL`
(NO client secret — PKCE only) · `client_authentication_methods = ["none"]` · `require-proof-key = true`"*.
`projects/iam-platform/docs/adr/ADR-003-public-client-refresh-token-revoke-converter.md` 도 실재한다.
⇒ **실수가 아니므로 별 티켓은 열지 않는다**(Scope 대로).

🔴 **다만 이 AC 가 그 결과에 붙여 둔 이유절은 틀렸다.** *"의도라면 브라우저가 직접 교환한다는
뜻"* 인데, `console-web` 은 **서버에서 교환한다** — `src/app/api/auth/callback/route.ts:104-110`
이 route handler(서버)에서 `grant_type=authorization_code` + HttpOnly 쿠키의 `code_verifier` 로
`${OIDC_ISSUER_URL}/oauth2/token` 을 부른다. 브라우저는 코드도 verifier 도 못 본다.

🔵 **그래도 § AC-1 의 심각도 판정은 안 바뀐다** — 공격자는 `console-web` 의 런타임을 쓰는 것이
아니라 **자기 요청으로 자기 verifier 를 들고** 교환한다. 서버측 교환도 PKCE 도 이 축을 막지
않고, 막는 것은 `client_secret` 인데 이 클라이언트엔 없다.

## AC-2 — 회수 ✅ (**보존해야 할 것을 지키면서**)

`infra/demo/seed-demo-domain.sh` 에 회수 절을 **덧붙이기와 같은 트랜잭션**으로 넣었다
(`START TRANSACTION … COMMIT`). 술어 = **sslip.io 이면서 현재 `DEMO_DOMAIN` 이 아닌 것**,
`redirect_uris` 와 `post-logout-redirect-uris[1]` **양쪽**.

**보존 픽스처(`snap-09449008990589c36`, 3세대) 위에서 실제 스크립트를 돌린 결과:**

| 지표 | before | after |
|---|---:|---:|
| sslip URI | 30 | **10** |
| distinct sslip 호스트 | 15 | **5** (한 세대만) |
| **비-sslip URI** | 24 | **24 — 하나도 안 지워짐** |
| 원래부터 `[]` 인 클라이언트 | 10 | 10 (무변) |
| `JSON_VALID=0` 인 행 | 0 | **0** |

- 스크립트 `rc=0`, 새 사후조건이 `[seed] OK — 죽은 sslip 등록 0건 (회수 확인)` 출력.
- **멱등**: 다시 돌려도 수치 불변.
- 남은 행(console): `["…console.local/…", "…localhost:3000/…", "…console.54-116-51-195.sslip.io/…"]`.

🔴 **«넓은 술어 금지» 가 실측으로 확인됐다** — *"http 이면 삭제"* 였다면 위 **24건**이 함께 사라졌다.

🔵 **AC-2 의 «원소 수 ≥ 1» 을 그대로 가드로 만들면 첫날부터 빨갛다** — `[]` 인 클라이언트가
**10개** 있다(client_credentials 서비스 클라이언트). 그래서 단언은 «모든 클라이언트» 가 아니라
**«회수 대상이 된 클라이언트»** 에 건다. 구현에서는 `kept_n >= 1` 이 그 역할을 하고, 남길 것이
0개면 JOIN 이 행을 안 만들어 **배열은 손대지 않은 채 남는다**(fail-safe).

## AC-3 — 재발 방지 ✅ + **가드가 무는 것을 증명했다**

**(1) 스크립트 자신의 사후조건** — «덧붙였다» 와 «걷어냈다» 는 다른 주장이므로 따로 단언한다.
🔴 **bite 를 A–B–A 로 증명했다** (주입만으로는 부족했다 — 주입 직후 회수가 지워 버려
단언이 발화할 기회가 없었다):

```
A  회수 술어를 no-op 으로 사보타주 + 죽은 도메인 8-8-8-8 주입
   → SABOTAGED_RC=1 · "[seed] !!! 죽은 sslip 등록이 1 건 남아 있다 — 회수가 동작하지 않았다"
   → 8-8-8-8 잔존 = 1        (사보타주가 실제로 회수를 껐다는 증거)
B  정상 스크립트
   → REPAIRED_RC=0 · "[seed] OK — 죽은 sslip 등록 0건" · 8-8-8-8 잔존 = 0
```

**(2) `verify-demo-wrapper.sh` 칸 `(z18)`** — `--live` 에서 IdP DB 를 읽어 죽은 등록 0건을 잰다.
🔴 **이 가드가 조용히 통과할 수 있는 길 셋을 «초록» 이 아니라 «판정 안 함» 으로 갈랐다**:
① 질의 실패 ② 빈 출력 ③ **모집단 0**(`DEMO_DOMAIN=local` 이면 시드가 early-exit 한다 —
**AMI 를 굽는 중이 정확히 그 상태다**). `--require-coverage` 가 그것을 FAIL 로 승격한다
(가드 (h) 와 같은 규율).

**(3) 칸 `(z18s)`** — 판정 로직을 순수 함수로 분리해 **DB 없이** bite 를 증명한다. **6/6**:
죽은 1건·3건 → 문다 / 깨끗 → 통과 / **질의 실패·빈 출력·모집단 0 → 전부 «판정 안 함»**.

🔴 **라벨 충돌을 잡았다** — 처음 `(z17)` 로 붙였는데 그 라벨은 `TASK-MONO-550` AC-4 가 이미
쓰고 있었다(변수 접두사 `z17_` 까지). `(z18)`/`(z18s)` 로 옮겼고 기존 블록은 무손상이다.

## AC-4′ — ⏸️ **미측정. 다음 기동 창으로 넘긴다** (이 AC 가 지시한 대로)

*"회수 후 현재 도메인 콜백으로 **로그인이 되는가**"* 는 데모가 떠 있어야 잴 수 있다.
🔴 **이 칸을 「했다」로 적지 않는다.** 기동 없이 갈 수 있는 데까지는 갔다 — 회수 SQL 을
**실제 행 위에서** 돌리고 결과를 읽는 것까지(위 AC-2). **로그인 왕복만** 남았다.

**다음 기동 창에서 잴 것:**

1. 부팅 후 시드 로그에 `[seed] OK — 죽은 sslip 등록 0건 (회수 확인)` 이 있는가.
2. `verify-demo-wrapper.sh --live` 의 `(z18)` 이 `ok` 인가 — 🔴 **`skip` 이면 판정된 게 아니다.**
3. 새 도메인으로 **로그인이 끝까지 되는가** — 🔴 회수가 살아 있는 등록을 지웠다면 여기서 죽는다.
4. `oauth_clients` 의 sslip 세대가 **1개뿐**인가.

## AC-5 — 이 조치가 **안 고치는 것** ✅

- 🔴 **죽은 IP 를 이미 누가 갖고 있었는지 이 티켓은 모른다.** 「없었다」고 적지 않는다 — **안 쟀다.**
- 데모 도메인이 부팅마다 바뀌는 **구조 자체**는 그대로다(`ADR-MONO-067` D4 / `ADR-MONO-069`).
  회수는 **창을 줄일 뿐 없애지 않는다** — 데모가 켜져 있는 동안 현재 IP 는 등록돼 있고 정지
  시점에 반납된다. **다음 부팅의 회수까지가 노출 창이다.**
- `platform-console-web` 의 public 여부는 **의도**로 확인됐다(AC-1′) — 바꾸지 않았다.
- `.local`/`localhost`/`hubwang.com` 등록은 그대로 둔다 — 의도이고 로컬 개발이 쓴다.
- 🔵 **AMI 축**: 회수는 런타임 시드가 하므로 이미 구운 AMI 안의 낡은 행도 **다음 부팅에 회수된다**.
  「AMI 를 다시 구워야 고쳐진다」가 **아니다**.

---

## 🔴 부수 발견 — 별 티켓 `TASK-MONO-608`

`(z18s)` 를 `--live` 게이트 **위**에 두었는지 확인하려고 `(z16)` 을 읽다가, **그 가드가 자기
docstring 에 앵커돼 있어 진짜 게이트를 한 번도 본 적이 없다**는 것을 발견했다. 진짜 게이트
안에 칸을 주입해도 **안 문다**(실측). `606` 의 칸 배치는 이와 **무관하게 옳지만** 그것을
강제하는 것이 지금은 없다. ⇒ [`TASK-MONO-608`](TASK-MONO-608-the-guard-that-watches-the-live-gate-anchors-on-its-own-docstring.md).

## 🧹 하네스 회수 (2026-09-01)

| 리소스 | 처분 |
|---|---|
| `i-0b7e4c520b218b49e` (t3.small) | **terminated** |
| `vol-0ab0389f4aa93aa9c` · `vol-09a6bbbbf2e481f35` | **deleted** |
| `snap-05121cdf701f0a9b6` (AC-0′ 임시) | **deleted** |
| **`snap-09449008990589c36`** (보존 픽스처) | 🔴 **살아 있다** — `KeepUntil=TASK-MONO-606-AC-2`. AC-2 랜딩 후 삭제가 계약이고 **삭제는 파괴적이라 소유자 승인 사안**이다. 지우기 전에 `TASK-MONO-605` § AC-3 의 삭제 트리거를 다시 읽어라 |

태그 스윕(`Name=tag:Task,Values=TASK-MONO-606`)에 남은 볼륨·스냅샷 **0건**, 인스턴스는 `terminated` 만.

## CORRECTION (2026-09-01 UTC) — **보존 픽스처를 지웠다. 위 § 하네스 회수 표의 「살아 있다」는 이제 거짓이다.**

위 표는 *"`snap-09449008990589c36` … 🔴 **살아 있다**"* 라고 선언한다. 그 선언이 **더 이상
참이 아니다.** 원문은 고치지 않는다 — 그때는 참이었다. 여기 지금 참인 것을 적는다.

**삭제 트리거가 발화했다.** `TASK-MONO-605` § AC-3 이 처분을 이렇게 못 박아 뒀고, 지우기 전에
그 절을 다시 읽었다:

| | |
|---|---|
| 처분 | **보존** |
| 이유 | `TASK-MONO-606` AC-2 의 유일한 「3 도메인」 픽스처 |
| **삭제 트리거** | 🔴 **`TASK-MONO-606` AC-2 랜딩 시점.** 그때 지운다 |
| 결정자 · 일시 | 소유자, 2026-08-30 |

**트리거 조건을 코드로 확인했다** — «머지됐다» 를 믿지 않고 `origin/main` 의 파일을 열었다:

| 축 | 관측 |
|---|---|
| (a) PR | `#3557` **`state=MERGED`** · `mergedAt=2026-09-01T05:21:30Z` |
| (b) 스쿼시 | `f999b2884` = 검증 시점 `origin/main` tip |
| (c) 머지 시점 비-skip | **6/6 SUCCESS**(`Demo wrapper smoke` 포함) · 실패 0 |
| **AC-2 가 정말 랜딩했나** | `git show origin/main:infra/demo/seed-demo-domain.sh` 에 `kept_n >= 1` **3건** · 사후조건 문구 **1건** ⇒ **회수 절이 main 에 있다** |

그리고 소유자가 삭제를 **명시적으로 다시 승인**했다(2026-09-01).

```
aws ec2 delete-snapshot --snapshot-id snap-09449008990589c36   → rc=0
aws ec2 describe-snapshots --snapshot-ids snap-09449008990589c36
  → InvalidSnapshot.NotFound   (삭제 확인)
```

### 🔴 이 삭제가 되돌릴 수 없게 만든 것 — 정직하게 적는다

**「죽은 sslip 세대가 3개 쌓인 볼륨」은 다시 만들 수 없다.** 현재 인스턴스는 1세대뿐이고
(재굽기가 리셋했다), 3세대를 다시 쌓으려면 **부팅을 세 번** 해야 하는데 그 부팅마다 이제
**회수가 돈다** — 즉 이 픽스처는 **원리적으로 재현 불가능**해졌다.

🔵 그래도 지운 것이 맞다고 보는 근거: 그 픽스처가 답해야 할 질문(«3개 중 2개만 지우고 1개는
남기는가»)은 **이미 답했고 그 수치가 이 티켓에 기록됐다**(§ AC-2: 30→10 · 15→5 · 비-sslip
24 전수 보존 · 멱등 · A–B–A bite). 🔴 **다만 AC-4′ 가 거짓으로 나오면 그때 재현 가능한
것은 「라이브 인스턴스」뿐이지 이 픽스처가 아니다.** 그 축을 잃었다는 것을 여기 적어 둔다.

### 🔵 계정에 남은 스냅샷 둘은 **지우면 안 된다** (다음 사람을 위해)

`TASK-MONO-605` 의 Failure Scenarios 가 *"이유 없이 남은 스냅샷은 다음 사람이 지운다"* 를
경고했으므로, 남은 것들의 정체를 확인해 적어 둔다 — **둘 다 살아 있는 AMI 의 백킹 스냅샷**이다:

| 스냅샷 | 무엇 | 지우면 |
|---|---|---|
| `snap-04ac23e320726cfb2` (08-22) | `ami-08df900798bc99c83` (`portfolio-demo-1787382710`, `available`) | 그 AMI 가 깨진다 |
| `snap-0d7b00004c167a8c3` (08-29) | `ami-0caf015f7cd9144fd` (`portfolio-demo-1788014073`, `available`) | 그 AMI 가 깨진다 |

⇒ **고아가 아니다.** 정리 대상으로 세지 마라.

### 이 CORRECTION 이 **안 바꾸는 것**

- 🔴 **AC-4′ 는 그대로 열려 있다.** 로그인 왕복은 기동이 필요하고, 이 삭제는 그 축과 무관하다.
  이 티켓은 **`review/` 에 남는다.**
- § AC-2 의 실측 수치는 그대로 유효하다 — 픽스처가 사라진 것이지 측정이 사라진 것이 아니다.

## CORRECTION (2026-09-01 UTC) — 같은 시체의 **두 번째 집**이 있었고, 이 티켓은 그 집을 몰랐다

`TASK-MONO-611` 이 `ADR-MONO-069` § R1 을 조사하다가 **`3-38-176-240`** 을 이 티켓이 보는
곳이 아닌 데서 또 만났다:

| 집 | 무엇 | 이 티켓이 보나 |
|---|---|---|
| IdP DB `oauth_clients` | 등록된 `redirect_uri` / post-logout | ✅ 이 티켓의 대상 |
| **Vercel `kanggle-store` 프로덕션 env** | `OIDC_ISSUER_URL=http://iam.3-38-176-240.sslip.io` (08-29 생성) | 🔴 **아니다** — 이 티켓 전문에 `vercel` **0건** |

⇒ 같은 죽은 IP 가 **두 시스템**에 박혀 있었는데, 티켓은 한쪽만 알았다. 발견 경로도 가드가
아니라 **다른 티켓의 곁가지**였다.

### 처분 — 611 이 했다. **이 티켓의 범위는 넓히지 않는다**

- Vercel 쪽 값은 **삭제됐다**(2026-09-01, 소유자 지정. 삭제 전 2개 → 후 1개 확인).
- 원장은 `projects/ecommerce-microservices-platform/apps/web-store/VERCEL.md` § OIDC 축 —
  그 문서가 *"넣은 값과 그 이유를 여기에 적는다"* 라고 **지시하고 있었는데 08-29 의 값이
  한 줄도 없었다.**
- 🔵 **이 티켓의 AC 는 하나도 바뀌지 않는다.** IdP DB 축은 그대로이고 **AC-4′ 도 그대로
  열려 있다**(기동 창 필요). 이 CORRECTION 은 *"저 IP 를 다 치웠다"* 가 **아니라**
  *"이 티켓이 세는 모집단이 전부가 아니었다"* 를 남기는 것이다.

🔴 **다음에 죽은 IP 를 셀 때는 «IdP DB» 가 아니라 «그 문자열을 담을 수 있는 시스템 전부» 로
모집단을 잡아라** — 최소한 IdP DB · Vercel env(4 프로젝트) · `infra/demo/*.env` ·
terraform tfvars. [[feedback_recount_population_dont_inherit_scope]]

---

# ✅🔴 CORRECTION (2026-09-02 UTC) — AC-4′ 를 `TASK-MONO-610` 기동 창에서 쟀다

AC-4′ 가 지시한 네 칸 중 **셋이 닫혔고 하나는 여전히 미측정**이다. `review/` 이므로
표를 고치지 않고 순수 추가한다.

## 🔵 먼저 — 양성 대조군이 라이브에서 재현됐다

창 시작 시점(AMI 판본, 회수 시드가 호스트에 **없던** 상태)의 실측:

```
ecommerce-admin-dashboard-client | … admin.ecommerce.3-38-176-240.sslip.io … admin.ecommerce.43-202-166-3.sslip.io …
ecommerce-web-store-client       | … web.ecommerce.3-38-176-240 …            web.ecommerce.43-202-166-3 …
fan-platform-user-flow-client    | … (양쪽 세대 모두) …
platform-console-web             | … (양쪽 세대 모두) …
```

⇒ **죽은 세대 `3-38-176-240` 이 부팅을 건너 그대로 남아 있었다.** 이 티켓이 고치려는
결함이 라이브에서 관측됐다 — 회수 전 상태의 사진이다.

## 판정

| # | AC-4′ 의 기준 | 판정 | 근거 |
|---|---|---|---|
| ① | 시드 로그에 회수 확인 줄 | 🟢 **PASS** | `[seed] OK — 죽은 sslip 등록 0건 (회수 확인)` — 재기동 1회 + **EC2 부팅 2회 모두** |
| ② | `verify --live` 의 `(z18)` 이 `ok` | 🔴 **미측정** | 아래 |
| ③ | 새 도메인으로 로그인이 끝까지 | 🟢 **PASS** | 세 도메인에서 authorization_code + PKCE 왕복이 **토큰 200** 까지 성립 |
| ④ | `oauth_clients` 의 sslip 세대가 1개 | 🟢 **PASS** | 아래 |

### ④ 의 숫자 — 술어와 대조군을 함께 적는다

부팅 뒤 (도메인 `3-38-132-31`) 기준:

| 질의 | 값 |
|---|---:|
| `redirect_uris like '%3-38-176-240%'` (더 옛 세대) | **0** |
| `redirect_uris like '%43-202-166-3%'` (직전 세대) | **0** |
| `redirect_uris like '%3-38-132-31%'` (현재 세대) | **4** ← 🔵 **양성 대조군**. 0이면 내 술어가 틀린 것이다 |

🔵 처음 셀 때와 **같은 질의 모양**으로 다시 셌다 — 중간에 추출 술어를 바꿨다가
「세대가 줄었다」로 읽을 뻔했고, 그건 추출기를 재는 것이다.
[[feedback_comparing_two_extracts_measures_the_extractors]]

### 🔴 ② 는 왜 미측정인가 — **skip 이 아니라 미도달이다**

`verify-demo-wrapper.sh --live` 가 **(z18) 에 닿기 전에 (w) 에서 중단**됐다:

```
(w) 모든 OIDC 리소스 서버가 자기 JWKS 호스트를 해소할 수 있는가
  FAIL: fan:membership-service → JWKS 호스트 'auth.hubwang.com' 가 이 서비스의 네트워크 어디에도 없습니다
```

이 FAIL 은 `TASK-MONO-610` 의 뒤집기가 만든 **위양성**이다(그 티켓 § 기동 창 실측 원장 ②).
런타임은 반증한다 — 그 컨테이너 안에서 해소 `216.150.16.129` · JWKS **200 433B**.
⇒ **(w) 의 술어가 C2 를 반영하도록 고쳐지기 전까지 (z18) 은 이 경로로 잴 수 없다.**
🔴 AC-4′ 가 *"`skip` 이면 판정된 게 아니다"* 라고 못 박았는데, 여기서는 **skip 조차 아니다.**

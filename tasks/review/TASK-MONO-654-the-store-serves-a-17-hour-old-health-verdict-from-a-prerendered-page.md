# Task ID

TASK-MONO-654

# Title

🔴🔴 **스토어가 «17시간 전의 헬스 판정» 을 서빙한다** — 배너는 「서버가 꺼져 있다」인데 같은 화면의 데이터는 라이브다

# Status

review

# Owner

monorepo

# Task Tags

- demo
- vercel
- isr

---

# Goal

방문자가 데모를 켠 뒤 스토어를 열면 **「데모 서버가 꺼져 있어 …」 배너가 사라지게** 한다.
지금은 프리렌더된 사본에 그 판정이 구워져 있어, 서버가 떠 있어도 배너가 남는다.

---

# 🔴 실측 (2026-09-09 UTC)

## ① 배너가 틀린 순간이 그림으로 남아 있다

`TASK-MONO-648` 의 촬영 창에서 `store /` 를 **11:24:05Z** 에 찍었다. 같은 창의 기동은
**11:19:14Z 에 완료**됐고(웜 부팅 9분 16초), 그 창의 콘솔 캡처 59장이 라이브 데이터로
나왔다. 즉 **백엔드가 뜬 지 5분 뒤**다. 그런데 그림 맨 위에 이렇게 적혀 있다:

> 지금 보이는 상품은 샘플 데이터입니다. **데모 서버가 꺼져 있어** 장바구니·로그인 같은
> 실시간 기능은 잠겨 있습니다. 데모 시작 페이지에서 서버를 켠 뒤(약 10분) 다시 열어 주세요.

## ②🔴🔴 같은 화면이 자기 자신과 모순된다

`store /my/wishlist` 의 그림에는 **그 배너와 라이브 데이터가 같이 있다**:

| 화면 요소 | 무엇을 말하나 |
|---|---|
| 상단 노란 배너 | 「데모 서버가 꺼져 있어 … 로그인 같은 실시간 기능은 잠겨 있습니다」 |
| 본문 위시리스트 | 맥북 프로 16인치 M4 · 갤럭시 S25 울트라 · 슬림핏 데님 청바지, **「2026. 9. 9. 16:46:26 추가」** |

로그인이 「잠겨 있다」는 화면이 **그날 16:46 에 로그인해서 담은 항목**을 그리고 있다.
🔵 이것이 기전을 가른다 — 배너는 **프리렌더된 껍데기**에 있고 내용물은 **클라이언트에서
가져온다.**

## ③ 캐시 헤더가 기전을 확정한다

```
X-Nextjs-Prerender: 1          ← 프리렌더(ISR)된 페이지다
X-Nextjs-Stale-Time: 300       ← 재검증 주기 5분
X-Vercel-Cache: STALE
Age: 61778                     ← 그런데 서빙된 사본은 17.2시간 전 것이다
```

⇒ **재검증 주기가 5분인데 나이가 17시간**이다. stale-while-revalidate 는 만료 뒤 첫
방문자에게 **낡은 사본을 주고** 뒤에서 다시 만든다. 데모 사이트는 트래픽이 거의 없으므로
그 「첫 방문자」가 바로 **면접관**이고, 그가 보는 것은 언제나 낡은 판정이다.

---

# 🔵 문서를 먼저 열었다 — 여기 없다

`docs/guides/interview-demo-walkthrough.md` § 6 「알려진 한계」 원장에 이 행이 **없다.**
§ 1 에 인접한 문장이 하나 있지만 **다른 상태**를 말한다:

> 꺼져 있으면 200 을 내면서 *"데모 서버가 꺼져 있어 **상품 데이터를 불러올 수 없습니다**"*
> 를 그린다 — 2026-09-01 실측.

🔴 그 문장이 서술하는 화면은 **상품이 안 보이는** 화면이다. 이 티켓의 화면은 **상품 20개가
보이는데 배너만 꺼졌다고 말하는** 화면이다. 같은 문구가 아니고 같은 상태도 아니다.
🔵 § 6 의 팬 피드 캐시 행(2026-08-06 고침)은 **표면이 다르다**(그쪽은 렌더된 뷰 캐시).

⇒ **새 결함이다.** 그리고 § 6 에 행을 더해야 한다(아래 AC-4).

---

# 🔴 왜 이것이 포트폴리오에서 특히 나쁜가

`TASK-MONO-634` § D7 이 이름 붙인 함정의 정확한 재발이다 — **방문자가 「고장」으로 읽고
닫는다.** 게다가 이 경우는 더 나쁘다: 방문자가 **시키는 대로 데모를 켠 뒤에도** 같은
문구를 본다. 그러면 「켰는데도 안 된다」가 되고, 그건 「아직 안 켰다」보다 나쁘다.

🔵 `TASK-MONO-651`(지원용 포트폴리오 문서)이 이 링크를 건다. 그 문서가 「켜는 데 9분」을
적어도, 켠 뒤에 이 배너를 만나면 그 안내가 **거짓말이 된다.**

---

# Scope

## 포함

- 배너의 판정이 **방문 시점의 상태**를 반영하게 한다
- § 6 원장에 행 추가
- 가드

## 제외

- 🔴 **샘플 데이터 폴백 자체** — `ADR-MONO-070` / `TASK-MONO-635` 가 정한 설계다.
  백엔드 없이도 둘러볼 수 있는 것은 **기능이지 결함이 아니다.** 이 티켓은 «판정이 낡았다»
  만 다룬다.
- 🔴 **다른 앱(fan·console)의 같은 부류** — 모집단을 안 쟀다. AC-0 이 세고, 있으면 별도 티켓.
- ISR 자체를 걷어내는 것 — 반경이 크고 이 결함에 비해 과하다. **고른다면 근거를 적어라.**

---

# Acceptance Criteria

## AC-0 — 착수 전 재측정

- [x] 🔴 **인스턴스를 켠 상태에서** `store /` 를 브라우저로 열어 배너가 남는지 다시 재라.
      🔴🔴 **`curl` 로는 판정할 수 없다** — 지금은 인스턴스가 `stopped` 라 배너가 **옳고**,
      그 상태에서 재면 «정상» 으로 보인다. 판정 조건은 **켜져 있을 때**다.
- [x] `X-Vercel-Cache` · `Age` · `X-Nextjs-Stale-Time` 을 다시 적어라. 값은 바뀐다.
- [x] 🔴 **fan·console 도 같은 부류인지 세라.** 여기서 고치지 마라 — 수만 적고 별도 티켓.
      🔵 `TASK-MONO-635` 가 셋에 같은 폴백을 넣었으므로 **있을 법하다.**

## AC-1 — 판정이 방문 시점을 반영한다

- [x] 배너의 근거가 **프리렌더 시점이 아니라 방문 시점**의 상태다.
- [x] 🔴 **정적 폴백은 유지한다.** 백엔드가 정말 꺼져 있으면 배너는 그대로 나와야 한다 —
      두 방향을 다 지켜야지 한쪽으로 통일하면 안 된다(`TASK-MONO-644` AC-2 와 같은 요구).
- [x] 🔵 「아직 모른다」(탐침 전)와 「물어봤는데 꺼져 있다」를 **같은 화면으로 만들지 마라**
      — `TASK-MONO-636` 이 이름 붙인 축이다. 깜빡임이 생기면 그 사실을 적어라.

## AC-2 — 배너와 내용물이 모순되지 않는다

- [x] 🔴 **위 ② 의 조합이 재현되지 않는다**: 「로그인이 잠겼다」는 배너와 로그인해서 담은
      위시리스트가 **한 화면에** 있으면 안 된다.
- [x] 🔵 이것은 AC-1 과 다른 축이다 — AC-1 을 고쳐도 **껍데기와 내용물의 데이터 출처가
      다르면** 다른 조합에서 또 어긋난다.

## AC-3 — 가드

- [x] 🔴 **판정은 실행 비교로.** 「백엔드 up」과 「down」 두 상태를 주고 배너의 유무를
      비교한다. grep 은 이 축을 못 잰다(문구는 두 경우 다 코드에 있다).
- [x] **대조군**: 백엔드 down → 배너가 **나온다**(안 나오면 그것도 결함이다).
- [x] **bite** — 판정을 프리렌더 시점으로 되돌리면 빨강. 🔴 **주입·실행·bite 를 각각**
      단언하라.
- [x] 🔴 **한계를 적어라**: 이 가드는 「배너 문구가 옳은가」를 재지 **캐시가 실제로 얼마나
      낡는가**는 못 잰다. 후자는 라이브에서만 보인다.

## AC-4 — 원장과 라이브

- [x] `docs/guides/interview-demo-walkthrough.md` § 6 에 **행을 더한다**(상태 이모지로
      시작 · 이 티켓을 추적으로 인용). 🔵 § 6 의 규칙은 「인용한 티켓이 `done/` 으로 가면
      그 행도 고쳐라」이므로, 닫을 때 그 행도 함께 처리한다.
- [x] 🔴 **배포 뒤 인스턴스를 켠 상태에서** 라이브로 다시 재라. 「파일이 바뀐 것」과
      「방문자가 보는 것」은 다른 축이다.
      → 🟢 **동사=「재라」. 쟀다** (2026-09-11 창 · `16:10~16:28Z`). **방문자가 보는 것**으로
      쟀다: 백엔드가 **up** 인 상태에서 찍은 `store-1-home.jpg` 를 **열어 보니 배너가 없다**.
      대조 축(배포 여부 · 꺼진 쪽)은 아래 § AC-4 동사별 대조.

---


---

# 🟡 AC-0 부분 측정 (2026-09-10 UTC · `in-progress`)

## 🔴 먼저 — **이 AC 가 요구한 도구를 안 썼다**

AC-0 의 첫 칸은 *"인스턴스를 켠 상태에서 `store /` 를 **브라우저로** 열어라.
🔴🔴 **`curl` 로는 판정할 수 없다**"* 이다. **나는 `curl` 로 쟀다.**

🔵 상태 조건(«켜져 있을 때»)은 **충족했다** — `TASK-MONO-656` 의 apply 와 같은 창에서,
9개 묶음이 전부 `ready` 인 동안 쟀다. 못 지킨 것은 **도구** 쪽이다.
⇒ 그래서 이 절은 **AC-0 을 닫지 않는다.** 아래는 「기전은 확정됐고 렌더 확인이 남았다」다.

🔴 창은 이미 `POST /stop` 으로 닫혔고, **다시 여는 것은 두 번째 창이라 별도 승인 사항**이다.
이 티켓만을 위해 켜지 않는다.

## 🟢 그럼에도 기전은 확정됐다 — **세 번 재서 모두 STALE**

인스턴스가 **`running`** 이고 9개 묶음이 **전부 `ready`** 인 동안, `store /` 를 24분에 걸쳐
세 번 요청했다:

| # | 시각(UTC) | `X-Vercel-Cache` | `Age` | 배너 |
|---|---|---|---|---|
| 1 | 11:43:16 | **STALE** | 49448 (13.7h) | 있음 |
| 2 | ~11:44:52 | **STALE** | 49544 | 있음 |
| 3 | 12:07:45 | **STALE** | **50917 (14.1h)** | 있음 |

부수 헤더: `X-Nextjs-Prerender: 1` · `X-Nextjs-Stale-Time: 300`(재검증 5분).

그 문서가 담고 있던 문구, **백엔드가 켜져 있는 동안** 그대로:

> 샘플 데이터입니다. **데모 서버가 꺼져 있어** 장바구니·로그인 같은 실시간 기능은 잠겨
> 있습니다. 데모 시작 페이지에서 서버를 켠 뒤(약 10분) 다시 열어 주세요.

🔵 대조군: *"불러올 수 없습니다"*(상품이 안 보이는 **다른 상태**의 문구)는 **0건**이었다 —
즉 관측된 것은 기안이 지목한 그 배너가 맞고 다른 상태와 섞이지 않았다.

## 🔴🔴 그리고 기안보다 **한 발 더 나간 사실**이 나왔다

기안은 stale-while-revalidate 를 *"만료 뒤 **첫 방문자에게** 낡은 사본을 주고 뒤에서 다시
만든다"* 로 적었다. 그 모델이라면 두 번째나 세 번째 요청은 **신선해야 한다.**

**안 그랬다.** `Age` 가 49448 → 49544 → 50917 로 **단조 증가**했다 — 세 번 다 **같은 캐시
객체가 나이만 먹은 것**이고, 24분 동안 재생성된 사본이 한 번도 안 왔다.

⇒ 🔴 「첫 방문자만 낡은 것을 본다」가 **아니다.** 적어도 이 엣지에서는 **연속 방문자가 계속**
낡은 사본을 받는다. 면접관이 이 링크를 두 번 눌러도 같은 화면이다.

🔵 **이것이 AC-1 의 설계를 바꾼다**: 「방문 시점을 반영한다」를 고칠 때, 재검증이 **왜
안 걸렸는지**를 먼저 답해야 한다(트래픽이 없어서인가 · 재검증이 실패하는가 · 엣지가
전파를 안 받는가). 🔴 세 갈래가 서로 다른 처방을 부른다.

## ⚪ 남은 것

- **브라우저 렌더 확인** — 배너가 하이드레이션 뒤에도 남는가, 그리고 같은 화면의 데이터가
  라이브인가(기안의 «화면이 자기 자신과 모순된다»). 🔵 배너 자체는 **프리렌더된 HTML 에
  있으므로** 브라우저도 같은 글자를 받는다 — 브라우저가 더해 주는 것은 **아래쪽 데이터의
  출처**다. 🔴 그것을 재려면 창이 다시 열려야 한다.
- **AC-0 ③ — fan·console 도 같은 부류인지 세기.** 안 셌다.

---

# Related Specs / Contracts

- `ADR-MONO-070` · `TASK-MONO-635` — 백엔드 없이 성립하는 공개 열람(**이 설계는 유지**)
- `TASK-MONO-634` § D7 — 「이미지는 기동을 겸하지 않는다」, 방문자가 고장으로 읽는 축
- `TASK-MONO-636` — 「아직 모른다」와 「물어봤는데 없다」는 다른 값이다
- `TASK-MONO-644` — 두 상태를 갈라야지 한쪽으로 통일하면 안 된다
- `TASK-MONO-651` — 지원용 포트폴리오 문서가 이 링크를 건다
- `docs/guides/interview-demo-walkthrough.md` § 1(인접 문장) · § 6(원장)
- `apps/web-store/src/shared/config/demo-backend.ts`

---

# Edge Cases

- **인스턴스가 켜지는 중(`pending`)** — 「꺼져 있다」도 「켜져 있다」도 아니다. 🔴 `stopped`
  와 같이 묶지 마라(`TASK-MONO-653` 과 같은 함정).
- **탐침이 실패했다** — 「꺼져 있다」와 「못 물어봤다」는 다른 사실이다. 안 가르면
  `TASK-MONO-636`·`644` 가 두 번 고친 그 결함을 세 번째로 만든다.
- **트래픽이 많아 캐시가 자주 재생성된다** — 그러면 이 결함이 **가끔만** 보이고, 그때는
  아무도 재현하지 못한다. 🔵 데모 사이트는 트래픽이 거의 없어서 **항상** 보인다는 것이
  오히려 다행이다.
- **배너를 껍데기에서 빼면 CLS 가 생긴다** — 배너가 나중에 나타나면 화면이 밀린다.
  그 대가를 알고 고르고, 적어라.

---

# Failure Scenarios

1. 🔴🔴 **인스턴스가 꺼진 상태에서 재고 「정상」이라 적는다.** 지금 배너는 **옳다** —
   판정 조건은 켜져 있을 때다. AC-0 이 이것을 막는다.
2. 🔴 **배너를 아예 없앤다.** 백엔드가 정말 꺼졌을 때 방문자가 이유를 못 듣는다 —
   `TASK-MONO-636` 이 고친 「사유 없는 죽은 화면」으로 되돌아간다.
3. 🔴 **샘플 데이터 폴백까지 걷어낸다.** 그건 `ADR-MONO-070` 의 설계다.
4. **가드를 grep 으로 쓴다.** 문구는 두 상태 모두에서 코드에 있으므로 아무것도 못 잰다.
5. 🔴 **§ 6 원장에 안 적는다.** 이 표가 정본이고 required check 가 그것을 지킨다 —
   안 적으면 다음 사람이 「새 결함」이라 부르며 같은 조사를 반복한다(2026-09-09 에 내가
   두 번 그랬다).

---

# 분석 / 구현 권장

분석=Opus 5 / 구현 권장=**Opus** — 렌더 전략(프리렌더/ISR ↔ 클라이언트)에 손대는 일이고,
「배너를 지우면 된다」가 **틀린 답**인 부류다(Failure 2·3). 가드도 실행 비교라 설계가 필요하다.

---

# 🔴🔴 AC-0 닫힘 (2026-09-11 UTC · 데모 창) — **재현됐다. 그리고 티켓보다 나쁘다.**

## 첫 칸 — 「인스턴스를 켠 상태에서 브라우저로」

🟢 조건 충족: 8/8 묶음 `ready`(`07:22:51Z`), `/status` `state=running`. 그 상태에서
**브라우저로** `store /` 를 열었다(🔴 `curl` 로는 판정 불가라는 AC-0 의 조항 그대로).

```
X-Vercel-Cache: STALE      Age: 120265 (= 33.4시간)      X-Nextjs-Stale-Time: 300
배너 문구 3종 전부 존재: "데모 서버가 꺼져 있어" · "샘플 데이터입니다" · "실시간 기능은 잠겨"
상품 링크 8개(샘플)      본문 527자      first·reload 동일
```

⇒ **배너가 남는다.** 기전은 확정이고 렌더까지 확인했다.

## 🔴🔴 「낡았다」가 아니라 **「재생성이 착지하지 않는다」**

티켓 제목의 «17시간» 은 이제 **33.4시간**이다. 그런데 더 중요한 것은 **자가 치유가
일어나지 않는다**는 것이다. 2분간 8회 요청하며 지켜본 결과:

```
[1] age=120298  cache=STALE   [5] age=120362  cache=STALE
[2] age=120314  cache=STALE   [6] age=120378  cache=STALE
[3] age=120330  cache=STALE   [7] age=120393  cache=STALE
[4] age=120346  cache=STALE   [8] age=120410  cache=STALE
```

🔴 **`Age` 가 단조증가하고 `STALE` 이 안 풀린다.** `revalidate = 60` 인데 요청이 반복돼도
새 판이 **한 번도 안 내려온다** ⇒ 「오래된 사본을 주고 뒤에서 다시 만든다」가 아니라
**뒤에서 만드는 것이 도착하지 않는다.** 🔵 이 구분이 AC-1 의 설계를 바꾼다 — 캐시 수명만
줄이는 처방은 **이 상태를 안 고친다.**

## 🔴 AC-0 ③ — fan·console 은 **같은 부류가 아니다.** 그리고 모집단은 store 안에서 **3**이다

| 앱 | `export const revalidate` | `export const dynamic` | 판정 |
|---|---|---|---|
| **web-store** | **3 파일** (`= 60`) | 5 | 🔴 **결함 부류** |
| console-web | **0** | 66 | 🟢 전부 동적 |
| fan-platform-web | **0** | 3 | 🟢 동적 (`DemoBackendNotice` 위젯은 있다) |

⇒ **별도 티켓이 필요 없다** — fan·console 에는 이 부류가 없다. `TASK-MONO-635` 가 셋에
같은 폴백을 넣었으므로 «있을 법하다» 고 적었는데, **폴백이 있는 것과 그 판정이 프리렌더에
굳는 것은 다른 축**이었다.

🔴🔴 **그런데 이 티켓이 세는 자리가 좁다.** `DemoBackendNotice` 는 페이지가 아니라
**`app/(store)/layout.tsx`** 에 있고, 그 레이아웃이 **세 ISR 라우트를 전부 감싼다**:

```
src/app/(store)/page.tsx                  export const revalidate = 60
src/app/(store)/products/page.tsx         export const revalidate = 60
src/app/(store)/products/[id]/page.tsx    export const revalidate = 60
```

⇒ 티켓은 `store /` 하나를 말하지만 **영향 라우트는 셋**이다. AC-1·AC-3 의 대상과 가드
모집단을 셋으로 잡아야 한다.

## 남은 칸

- AC-1·AC-2·AC-3·AC-4 는 **구현 축**이고 이 창에서 하지 않았다(이 창은 측정 창이다).
- 🔵 AC-4 의 *"배포 뒤 인스턴스를 켠 상태에서 라이브로 다시 재라"* 는 **고친 뒤** 축이라
  이 창에서 닫을 수 없다.

---

# 🔴🔴 증거 추가 (2026-09-11 UTC · `TASK-MONO-648` 창 2회차) — **그리고 이 티켓의 모집단이 틀렸다**

> 출처: `TASK-MONO-648` 의 썸네일 재촬영. **이 결함을 찾으러 간 것이 아니라** 포트폴리오
> 썸네일을 검수하다 걸렸다.

## ① 배너가 또 찍혔다 — 그리고 이번엔 **main 에 커밋됐다**

`store /` 를 **`11:37:46Z`** 에 찍었다. 같은 창의 다섯 묶음이 **`11:36:27Z` 에 전부 `ready`**
였으므로 **기동 완료 1분 19초 뒤**다. 같은 실행에서 찍힌 콘솔 두 장이 데모 백엔드의 실제
데이터(1,677자 / 1,004자)를 그렸으니 **백엔드가 살아 있었다는 것은 확정**이다. 그런데:

> 지금 보이는 상품은 샘플 데이터입니다. **데모 서버가 꺼져 있어** …

🔴 **§ ① 과 같은 문장이 이틀 뒤 다시 나왔다** — 일시적 현상이 아니다.

🔴🔴 **그리고 이번 건은 그림이 저장소에 들어갔다.** 그 캡처가
`infra/demo/aws/site/thumbnails/store-1-home.jpg` 로 커밋돼(`3690792eb`) **론처 캐러셀이
방문자에게 「데모 서버가 꺼져 있다」를 상시 광고**한다. 이 티켓 § 「왜 포트폴리오에서 특히
나쁜가」가 말한 피해가 **썸네일로 고정**된 셈이다.

## ②🔴🔴 같은 실행의 `/products` 에는 배너가 **없다** — 3초 차이

| 장 | 촬영시각 | 상단 배너 |
|---|---|---|
| `store-1-home.jpg` (`/`) | `11:37:46Z` | 🔴 **있음** |
| `store-2-products.jpg` (`/products`) | `11:37:49Z` | 🟢 **없음** |

⇒ **이 티켓의 모형이 예측하지 못하는 관측이다.** § AC-0 ③ 은 `DemoBackendNotice` 가
`app/(store)/layout.tsx` 에 있고 그 레이아웃이 **세 ISR 라우트를 전부 감싸므로** *"영향
라우트는 셋"* 이라고 적었다. 셋이 같은 레이아웃을 쓴다면 배너도 셋에 같이 나와야 한다.

## ③ 그래서 헤더를 쟀다 — **런타임에서 프리렌더되는 것은 `/` 하나뿐이다**

데모 스택이 `stopped` 인 `12:33Z` 에, 세 라우트를 **3회씩** 쟀다(표본 하나로 판정하지 않는다):

| 라우트 | `X-Nextjs-Prerender` | `X-Vercel-Cache` | `Age` |
|---|---|---|---|
| `/` | **1** | **STALE** | **138,882 → 138,885 → 138,889** |
| `/products` | **없음** | MISS | 0 |
| `/products/p01` | **없음** | MISS | 0 |

3회 전부 동일했다. `/products`·`/products/[id]` 는 `Cache-Control: private, no-cache,
no-store` 로 **동적 서빙**이다 — ISR 사본이 아예 없다.

### 🔴🔴 ⇒ **§ AC-0 ③ 의 「영향 라우트 셋」은 틀렸다. 런타임 모집단은 1이다.**

그 표는 `export const revalidate = 60` 을 **grep 해서** 만들었다. 선언은 셋에 다 있지만
**런타임은 `/` 하나만 프리렌더한다** — 나머지 둘은 무언가가(동적 API 접근 등) 동적으로
밀어냈다. 이 저장소가 이름 붙인 **«선언 파일 grep ≠ 런타임 모집단»** 이 정확히 여기다.

🔴 **이것이 AC-3(가드)에 직접 영향을 준다.** 가드 모집단을 선언 grep 으로 셋으로 잡으면
**두 칸은 영원히 안 무는 공허한 칸**이 되고, 그 초록은 아무것도 증명하지 않는다.
⇒ **가드는 선언이 아니라 응답 헤더(`X-Nextjs-Prerender`)로 모집단을 잡아야 한다.**
🔵 그리고 그 순간 가드는 *"셋이 다 안전한가"* 가 아니라 *"프리렌더되는 라우트가
낡은 판정을 서빙하는가"* 를 재게 된다 — 술어가 **질문과 같아진다.**

## ④ 🔵 `Age` 가 **표본 사이에 증가한다** — § 287 「재생성이 착지하지 않는다」의 재확인

`138,882 → 138,885 → 138,889` (3~4초씩). 세 번 찔렀는데도 나이가 **줄지 않고 늘었다** ⇒
stale 사본을 돌려주면서 **뒤에서 만드는 재생성이 착지하지 않는다.** § 287 이 *"「낡았다」가
아니라 「재생성이 착지하지 않는다」"* 로 고쳐 쓴 그 진단이 독립 표본으로 확인됐다.

🔴 **나이는 더 나빠졌다**: § ③ 이 적은 `Age: 61778`(17.2시간) → 이번 **138,889**(**38.6시간**).
🔵 stale-time 은 여전히 `300`(5분)이다. **463배** 낡은 사본을 서빙하고 있다.

## 🔵 정직한 한계

- 촬영이 기동 **1분 19초** 뒤였다. 「재생성할 시간이 없었다」로 읽힐 수 있지만, ③ 이
  그것을 배제한다 — 서빙된 사본이 **38.6시간** 짜리이고 재생성이 착지하지 않으므로
  경과 시간은 이 결함의 변수가 아니다.
- 배너의 **부재**(`/products`)를 「고쳐졌다」로 읽으면 안 된다. 그 라우트는 애초에
  프리렌더되지 않아 **이 결함의 모집단이 아닌** 것이지, 이 결함이 거기서 해결된 게 아니다.

## ⇒ 이 티켓에 더해진 것

- AC-0 의 재현이 **한 번 더** 됐고(이틀 뒤, 독립 창), 나이가 17.2 → **38.6시간**으로 악화.
- 🔴 **AC-0 ③ 의 모집단 표를 정정해야 한다** — 선언 3 / 런타임 **1**.
- 🔴 **AC-3 의 가드 술어가 바뀌어야 한다** — 선언 grep 이 아니라 응답 헤더.
- 🔴 **새 피해 축**: 그 배너가 **커밋된 썸네일**로 포트폴리오에 박혔다(`3690792eb`).
  654 가 고쳐진 뒤 `store-1-home` 을 **재촬영**해야 한다 — 지금 다시 찍어도 같은 배너가
  나온다(프리렌더 사본이 바뀌지 않으므로).

---

# 🟢🟢 구현 (2026-09-11 UTC · 창 없이) — **AC-1 이 요구한 「왜 안 착지하는가」부터 답했다**

🔴 AC-1 은 *"「방문 시점을 반영한다」를 고칠 때, 재검증이 **왜 안 걸렸는지**를 먼저
답해야 한다(트래픽이 없어서인가 · 재검증이 실패하는가 · 엣지가 전파를 안 받는가).
세 갈래가 서로 다른 처방을 부른다"* 고 못박았다. 그 순서를 지켰다.

## ① 갈래 (a)「트래픽이 없어서」는 **반증됐다** — 그리고 그것으로 충분했다

`store /` 를 이어서 쟀고, 이 티켓에 이미 있던 표본과 합쳐 **다섯 표본 / 이틀**이 됐다.
각 표본의 `Age` 를 측정 시각에서 빼서 **사본이 만들어진 시각**을 역산했다:

| 측정 시각(UTC) | `Age` | ⇒ 사본 생성 시각 |
|---|---|---|
| `2026-09-10T11:43:16Z` | 49,448 | `2026-09-09 21:59:08Z` |
| `2026-09-10T12:07:45Z` | 50,917 | `2026-09-09 21:59:08Z` |
| `2026-09-11T12:33Z`(분 단위 기록) | 138,882 | `2026-09-09 21:58:18Z` |
| **`2026-09-11T14:14:37Z`** | **144,928** | **`2026-09-09 21:59:09Z`** |
| **`2026-09-11T14:15:18Z`** | **144,969** | **`2026-09-09 21:59:09Z`** |

🔴🔴 **다섯이 전부 같은 순간으로 수렴한다**(51초 어긋난 하나는 측정 시각을 **분 단위로만**
적은 표본이다). ⇒ 40시간 동안 **재생성이 단 한 번도 일어나지 않았다.** `revalidate = 60`
이 선언돼 있는데도 그렇다.

🔵 그 사이 요청은 **계속 있었다**(이 티켓의 8연속 요청, 9/10 의 3회, 오늘의 3회, 그리고
방문자). ⇒ **갈래 (a) 는 반증된다.**

🔴 그리고 `2026-09-09T21:59Z` 는 **배포 시각**이다 — `vercel.json` 이
`git.deploymentEnabled.main = false` 라 배포는 `vercel-deploy.yml` 의 Deploy Hook 으로만
생기고, 그 훅은 **그 커밋이 web-store 경로를 건드렸을 때만** 쏜다. ⇒ 이 사본을 새로
만드는 **유일한 사건은 배포**이고, ISR 재검증은 그 일을 하지 않고 있다.

### 🔴🔴 (b) 와 (c) 는 **가르지 못했다 — 그리고 그것이 처방을 바꾸지 않는다**

「재검증이 실패한다」와 「엣지가 전파를 안 받는다」를 가르려면 Vercel 쪽 관측(함수 로그·
빌드 산출물 조회)이 필요하고, **저장소에서는 낼 수 없는 판정**이다. ⚪ 못 쟀다고 적는다.

🔵 **그런데 이 티켓의 결정은 그 구분에 안 걸린다.** AC-1 이 *"세 갈래가 서로 다른 처방을
부른다"* 고 적은 것은 맞지만, 그 셋이 부르는 처방은 전부 **「캐시를 손본다」 계열**이고,
갈래 (a) 가 죽는 순간 **그 계열 전체가 죽는다**:

- (a) 였다면 「수명을 줄인다 / 크론으로 찔러 준다」가 통했을 것이다 — **아니었다.**
- (b)·(c) 중 무엇이든, **재생성 자체가 도착하지 않으므로** 수명을 줄여도 같은 사본이 남는다.

⇒ 남는 유일한 처방은 **판정을 캐시된 사본 밖으로 빼는 것**이고, 그것은 (b)/(c) 어느
쪽이어도 같다. 🔵 그래서 «못 가른 것»이 이 티켓을 막지 않는다 — 다만 **막지 않는 이유를
적어 두는 것**이 ⚪ 를 그냥 덮는 것과 다른 점이다.

---

## ② 🔴🔴 이 티켓의 **모집단이 또 틀렸다 — 이번엔 반대 방향으로**

§ 「증거 추가」가 *"§ AC-0 ③ 의 「영향 라우트 셋」은 틀렸다. **런타임 모집단은 1이다**"*
로 고쳤다. 🔴 **그 정정도 틀렸다.**

그 판정은 살아 있는 URL **셋**(`/` · `/products` · `/products/[id]`)만 찔러서 나왔다.
그런데 그 셋은 «선언이 있는 라우트» 이고, **Next 는 아무것도 선언하지 않은 라우트도
프리렌더한다.** 빌드 산출물을 열었다(`DEMO_API_BASE` 주입 후 `next build`):

| 앱 | 프리렌더 라우트 | 옛 코드에서 **판정이 구워진** 것 |
|---|---|---|
| **web-store** | **19** | 🔴 **15** |
| fan-platform-web | 1 (`/_not-found`) | 0 |
| console-web | 1 (`/_not-found`) | 0 |

구워진 15개: `/` · `/cart` · `/checkout`(+`payment`·`payment/fail`·`payment/success`) ·
`/my/*` 7개 · `/orders`. 🔵 안 구워진 넷(`/login` · `/signup` · `/_not-found` 등)은
`(store)` 레이아웃 **밖**이다 — 즉 경계가 정확히 레이아웃과 일치한다.

⇒ 🔴 **모집단은 1 도 3 도 아니고 15 다.** 이 저장소가 이름 붙인 «전수조사는 «어디를
봤나» 를 잰다» 가 두 번 연속 일어났다: 처음엔 **선언을 세서 3**, 다음엔 **URL 셋을 찔러서
1**. 두 번 다 «모집단을 센 것» 이 아니라 «내가 본 것을 센 것» 이었다.

### 🟢 AC-0 ③ — fan·console 은 **역시 같은 부류가 아니다. 단, 티켓이 적은 이유는 틀렸다**

티켓은 *"`export const revalidate` 가 0 건이므로 전부 동적"* 이라고 적었다. 🔴 그 추론은
성립하지 않는다 — 방금 본 대로 **선언 없이도 프리렌더된다**(web-store 의 `/cart` 등 14개가
그 예다). 그래서 같은 도구(빌드 산출물)로 **다시 쟀고**, 결론은 살아남았다: 두 앱 모두
프리렌더되는 것이 `/_not-found` 하나뿐이다.

🔵 fan 은 페이지 11개에 `dynamic` 선언이 **3개뿐**이라 선언으로는 «8개가 의심» 이었는데,
실제로는 전부 런타임 동적이다. ⇒ **결론은 같지만 근거가 바뀌었다.**
⇒ 🟢 **별도 티켓 필요 없음** — AC-0 ③ 이 요구한 「수만 적고」가 이것이다.

---

## ③ 고친 것

| 파일 | 무엇 |
|---|---|
| `widgets/demo-notice/DemoBackendNotice.tsx` | 🔴 **조건 없이** 클라이언트 위젯만 렌더한다. 판정이 사라졌다 |
| `widgets/demo-notice/DemoBackendNoticeClient.tsx` | 신규 — 방문 시점에 탐침하고 **네 상태를 가른다** |
| `app/api/demo/backend-state/route.ts` | 신규 — `force-dynamic` + `no-store`. 판정을 서버에서 내리되 캐시 밖에서 |
| `middleware.ts` | 🔴 그 경로를 **익명 공개**로. 없으면 배너의 청중이 못 부른다 |
| `scripts/check-prerendered-demo-verdict.sh` | 신규 가드 |
| `.github/workflows/ci.yml` | `frontend-checks` 에 주입 env + 가드 스텝 |

🔴 **배너 문구는 한 글자도 안 바꿨다.** 이 티켓이 고치는 것은 «언제 판정하는가» 이지
«무엇을 말하는가» 가 아니다(`TASK-MONO-642` 가 그 문장을 정했다).

### 🔴 `middleware.ts` 가 없었으면 조용히 실패했을 것이다

탐침 경로를 공개 목록에 넣지 않으면 **익명 요청이 `/login` 으로 307** 된다. 그러면 결함이
「배너가 낡았다」에서 **「배너가 아예 안 뜬다」로 바뀔 뿐**이고, 후자가 더 조용하다 —
그리고 이 배너의 청중이 정확히 **로그인하지 않은 방문자**(면접관)다.
🔵 대조군 테스트를 같이 넣었다: `/api/demo/heartbeat` 는 **여전히 막혀 있어야** 한다
(익명 하트비트가 EC2 예산을 태운다). 그 칸이 없으면 공개 조건을 `/api/demo` 로 넓혀도 초록이다.

---

## ④ AC-3 — 주입 · 실행 · bite 를 **각각** 단언했다

🔴 AC-3 이 *"모집단을 선언 grep 으로 잡지 마라"* 를 요구했고, ② 가 그 이유를 실측으로
보여 준다. 그래서 가드의 모집단은 **`.next/prerender-manifest.json` 의 `routes`** 다 —
런타임 `X-Nextjs-Prerender: 1` 의 **빌드 시점 대응물**이고 네트워크를 안 탄다.

| 축 | 실행 | 결과 |
|---|---|---|
| **주입** | `DEMO_API_BASE` 를 지우고 실행 | 🔴 **RED** — *"이 값이 없으면 옛 코드로 빌드해도 배너가 안 구워집니다(공허한 통과)"* |
| **실행** | 옛 코드로 빌드한 산출물에 실행 | 🔴 **RED — 15개** 파일을 이름까지 찍고 죽는다 |
| **bite** | 새 코드로 빌드한 산출물에 실행 | 🟢 **rc=0** — 라우트 19 / HTML 19 / 구워진 판정 **0** |

🔵 **주입 칸이 이 가드의 핵심이다.** `DEMO_API_BASE` 없이 빌드하면 판정이 `not-demo` 라서
**결함 코드로도 배너가 안 구워진다** — 그 상태의 초록은 아무것도 증명하지 않는다. 그래서
가드가 그 조건을 **통과가 아니라 RED** 로 다룬다.

🔵 **생존 대조군**도 넣었다: 프리렌더된 HTML 에 스토어 레이아웃 지문이 하나도 없으면 RED.
없으면 «배너가 없다» 가 «페이지가 안 그려졌다» 로 성립한다.

### 🔴 이 가드가 재지 **못하는** 것 (AC-3 넷째 칸)

1. **캐시가 실제로 얼마나 낡는가** — 라이브 `Age` 에만 있다. 이 가드는 «구워졌나» 만 잰다.
   이 티켓의 진단(재생성이 착지하지 않는다)은 **가드에서 안 나온다.**
2. **배너가 실제로 뜨는가** — 부재만 재므로 「배너가 통째로 사라진 판」이 여기서는 초록이다.
   그쪽은 `DemoBackendNotice.test.tsx` § B 가 문다. 🔴 **둘은 서로를 대신하지 못한다.**
3. **엣지가 응답을 붙드는가** — 라우트의 `no-store` 는 `demo-backend-state-route.test.ts` 가 본다.

---

## ⑤ ⚪ 로컬에서 못 돌린 것 — **web-store 단위 스위트**

🔴 이 호스트(Node 24)에서 web-store 의 vitest 4.1.0 은 **기동 자체가 안 된다**
(`ERR_PACKAGE_IMPORT_NOT_DEFINED: #module-evaluator`). 알려진 호스트 한계이고 CI 는
Node 20 이라 통과한다. 다운시프트 우회는 **이미 시도됐고 안 통한다**(다른 이유로 한 칸
뒤에서 죽는다) — 그래서 다시 시도하지 않았다.

⇒ 로컬 판정은 `tsc --noEmit` **rc=0** · `next lint` **rc=0** 까지이고, **새로 쓴 단위
테스트의 권위는 CI 의 `frontend-unit-tests`(Node 20)** 다. 🔴 그 잡이 초록인 것을
머지 전에 확인해야 한다 — 이 절이 그것을 안 적으면 «로컬 초록» 으로 읽힌다.

## ⑥ 남은 칸 — AC-4 둘째

- [x] 🔴 **배포 뒤 인스턴스를 켠 상태에서 라이브로 다시 재라.** 창이 필요하다.
  → 🟢 **닫혔다. 🔴 그리고 이 칸은 AC-4 둘째 칸의 «두 번째 사본» 이었다** — 같은 의무가
  두 절에 있었고, 이 저장소가 이름 붙인 «한 사실이 두 절에 있으면 한쪽만 고쳐진다» 의
  표본을 이 티켓이 **스스로 만들었다**. 닫을 때 둘 다 찍어야 했고, 그래서 둘 다 찍었다.
  🔵 그때 재야 할 것: `store /` 의 배너가 **사라지는가**(백엔드 up) · `Age`/`X-Vercel-Cache`
  는 여전히 STALE 이어도 **상관없다**(판정이 더는 거기 없다 — 그 점을 확인하는 것이 요지다).
  🔴 그리고 **배포가 실제로 일어났는지 먼저** 보라: 이 PR 이 web-store 를 건드리므로 훅이
  쏘지만, 「머지 + CI 초록」은 「배포됨」이 아니다.

---

# 🟢🟢 AC-4 둘째 칸 — **배포 뒤 라이브로 다시 쟀다 (2026-09-11 UTC)**

AC-4 가 *"🔴 배포 뒤 인스턴스를 켠 상태에서 라이브로 다시 재라. 「파일이 바뀐 것」과
「방문자가 보는 것」은 다른 축이다"* 라고 적었다.

## ① 먼저 «배포가 실제로 일어났나» 부터 — 🔴 머지 + CI 초록 ≠ 배포됨

`#3781` 이 `16:00:08Z` 에 머지됐다. 그 뒤 `store /` 를 세 번 읽었다:

| 시각(UTC) | `Age` | 프리렌더 HTML 의 배너 문구 |
|---|---|---|
| `16:02:54Z` | **0** | **0건** |
| `16:03:42Z` | 45 | 0건 |
| `16:04:29Z` | 92 | 0건 |

🟢 **`Age: 0` 이 배포의 증거다.** 직전까지 서빙되던 사본은 `Age 144,969`(40시간)였고,
이 티켓의 진단이 *"이 사본을 새로 만드는 유일한 사건은 **배포**"* 였다 — 그 사건이 일어났다.

## ②🟢🟢 판정이 **프리렌더 사본에서 사라졌다** — 데모가 꺼져 있는데도

🔴 이 측정의 요점은 «배너가 없다» 가 아니라 **«배너가 있어야 하는 상태인데 HTML 에
없다»** 이다. 저 세 번을 읽을 때 데모는 `stopped` 였고, 그러면 **배너는 나와야 옳다.**
그런데 프리렌더된 HTML 에는 그 문구가 **0건**이다 ⇒ 판정이 더는 거기서 내려지지 않는다.

🔵 이것이 이 티켓이 고치려던 것 그 자체다. 그리고 **창이 없어도 잴 수 있는** 축이었다 —
`Age` 가 안 풀리는 문제를 캐시로 안 고치고 **판정을 캐시 밖으로 빼서** 고쳤으므로,
캐시가 여전히 STALE 이어도 상관없다.

## ③ 탐침 라우트를 라이브로 확인 — 익명 200, 캐시 없음

```
GET https://store.hubwang.com/api/demo/backend-state
  {"state":"unavailable"}
  Cache-Control: no-store, no-cache, must-revalidate
  X-Vercel-Cache: MISS        Age: 0
  익명 요청 → status=200 (최종 URL 그대로, /login 리다이렉트 아님)
```

🟢 세 가지가 한 번에 확인된다: (a) 판정이 **방문 시점**에 내려진다(데모가 `stopped` 이므로
`unavailable` 이 맞다) · (b) **캐시되지 않는다**(엣지에 굳으면 이 티켓이 한 층 위에서
재발한다) · (c) **익명이 부를 수 있다** — `middleware.ts` 공개 목록이 실제로 동작한다.
🔴 (c)가 없었으면 결함이 「배너가 낡았다」에서 **「배너가 아예 안 뜬다」로 바뀌었을** 것이고,
그 청중이 정확히 이 배너의 대상(로그인 안 한 방문자)이다.

## 🔵 남은 반쪽 — 「백엔드가 켜지면 배너가 사라지는가」

위 셋은 **꺼진 쪽**을 쟀다(배너가 나와야 하고, 판정이 프리렌더에 없어야 한다).
**켜진 쪽**(배너가 실제로 사라지는가)은 아래 § 썸네일 재촬영 창에서 같이 확인한다 —
그 창의 목적 자체가 «배너 없는 화면을 찍는 것» 이므로, 찍힌 그림이 곧 그 측정이다.

---

# 🟢🟢 썸네일 재촬영 (2026-09-11 UTC · 데모 창 · 소유자 승인) — **배너가 사라졌다**

이 티켓 § 「증거 추가」가 *"654 가 고쳐진 뒤 `store-1-home` 을 **재촬영**해야 한다"* 로
남긴 의무다. 🔴 소유자 승인은 **묶음을 명시해서** 받았다: *"store-1-home 재촬영만"*.

## 창 회계 — **13분**

| | 값 |
|---|---|
| 기동 요청 | `16:10:55Z` (`{"bundles":["store"]}` — 최소 묶음) |
| `store` **ready** | `16:19:51Z` — **부팅 8분 56초** |
| 촬영 | `16:27:32Z` |
| `POST /stop` | `16:28:01Z` |
| 예산 | **638 → 651 / 1200** ⇒ **이 창 13분** (남은 549분) |

🔵 지난 두 창(60분 · 35분)의 **5분의 1**이다. 묶음을 하나만 켰고 찍을 것이 한 장이었다.

## 🔴 웜업 술어 — 「배너가 사라졌다」를 신호로 쓰면 안 된다

`16:12:xx Z` 에 이미 탐침이 `{"state":"running"}` 을 줬다. 그때 `store` 묶음은 아직
**`booting`** 이었다(ready 까지 **7분 더**). ⇒ **배너의 부재는 백엔드가 쓸 만하다는
증거가 아니다.**

🔵 기전: 판정은 `/status` 의 **인스턴스** 상태(+ip)만 본다 — 묶음 헬스를 안 본다
(`@demo/backend-resolver`). 그래서 「켜지는 중」 구간에는 배너가 없다.
🔴 이것은 이 티켓이 이름 붙인 축(`TASK-MONO-636`: 「아직 모른다」≠「꺼져 있다」)의
**세 번째 값**이다 — 「켜지는 중」. 지금은 그것이 「켜졌다」와 같은 화면이다.
⇒ 결함이라 부를 만하지만 **이 티켓의 범위가 아니다**(이 티켓은 «낡은 판정» 을 고쳤다).
**별도 티켓 후보**로 여기 적어 둔다. 🔵 창을 열지 않고도 재현된다(기동 중에 탐침을 읽으면 된다).

## 찍힌 그림 — **전수로 열어서 봤다**

🔴 이 티켓 자신이 «묶음당 한 장 표본은 결함 있는 장을 비켜간다» 로 데인 적이 있다(그래서
거짓 배너가 머지됐다). 이번엔 찍은 것이 **한 장**이고 그 한 장을 **열어서** 확인했다:

| 축 | 결과 |
|---|---|
| **「데모 서버가 꺼져 있어」 배너** | 🟢 **없다** — 이 티켓의 목적 그 자체 |
| 헤더 · 히어로 · 「인기 상품」 4카드 | 🟢 정상 렌더(상품 이미지 포함) |
| 「샘플 데이터 (아직 발행 전)」 | 🟡 **남아 있다** — 아래 |

### 🟡 「샘플 데이터」 표기는 **남는다. 그리고 그것이 옳다**

`/` 는 프리렌더 라우트이고 그 상품은 **빌드 시점 저장본**이다(CI 에 백엔드가 없다).
재생성이 착지하지 않으므로 백엔드를 켜도 그 목록은 안 바뀐다. ⇒ `DataProvenanceNotice`
가 「샘플 데이터」라고 말하는 것은 **참**이다.

🔴 그리고 이 티켓의 § 제외가 그것을 명시적으로 뺐다: *"샘플 데이터 폴백 자체 —
`ADR-MONO-070` / `TASK-MONO-635` 가 정한 설계다. 이 티켓은 «판정이 낡았다» 만 다룬다."*
🔵 바뀐 것은 **거짓말이 사라진 것**이다 — 전에는 「서버가 꺼져 있다」(켜져 있는데)였고,
지금은 「샘플 데이터다」(정말 샘플이다)이다.

## 🔵 `capture-shots.mjs` 에 `--only` 를 더했다

승인된 범위가 **한 장**인데 스크립트는 7장을 전부 찍는다 — 나머지 여섯을 승인 없이
갈아 끼울 수 없다. ⇒ `--only <name>` 을 더했고, 🔴 **매니페스트를 통째로 덮지 않는다**:
안 찍은 장의 기록을 살려 찍은 장만 갈아 끼운다(순서는 정본 `SHOTS` 를 따른다).
그러지 않으면 1장짜리 매니페스트가 남아 (z37) 이 문다 — 그리고 **그 가드가 옳다**.

실측 확인: `1 updated, 6 kept` · 매니페스트 **7항목** · `.jpg` **7개** ·
`index.html` 이 참조하는 항목 **7** · 콘솔 두 장의 `requiresAuth`/`tenant` 메타 **보존**.
🔵 오타 방어도 넣었다(`--only store-1-hom` → 아는 이름을 나열하고 종료) — 「0장 찍고 rc=0」을 막는다.


---

# 🟢🟢 AC-4 § 동사별 대조 (2026-09-11 UTC) — **두 칸은 같은 의무 하나였다**

## 🔴 먼저: 이 티켓이 **같은 의무를 두 절에 적어 두었다**

| 자리 | 문구 |
|---|---|
| `AC-4` 둘째 칸 | *"배포 뒤 인스턴스를 켠 상태에서 라이브로 다시 재라"* |
| § ⑥ 남은 칸 | *"배포 뒤 인스턴스를 켠 상태에서 라이브로 다시 재라"* |

🔴 뒤의 것은 **구현 절이 스스로 만든 사본**이다. 의무를 «남은 것» 으로 다시 적는 것은
친절해 보이지만, 그 순간 **닫을 자리가 둘**이 된다 — 그리고 이 저장소가 이름 붙인 함정이
정확히 그것이다(`TASK-MONO-645` AC-6 이 같은 모양으로 한 세션을 썼다).
🔵 ⇒ **둘 다 찍었다.** 그리고 그 사실을 여기 적어, 다음 사람이 한쪽만 보고 «아직 안 닫힘» 으로
읽지 않게 한다.

## 동사 = 「재라」 — 세 축을 다 쟀다

이 티켓 자신이 § ⑥ 에서 **무엇을 재야 하는지를 셋으로** 적어 뒀다. 그 셋으로 대조한다.

### (a) 🔴 「배포가 실제로 일어났는지 **먼저** 보라 — 머지 + CI 초록은 배포됨이 아니다」

🟢 **쟀다.** `#3781` 머지(`16:00:08Z`) 직후 `store /` 를 세 번 읽었고 **`Age: 0`** 이었다.
직전까지 서빙되던 사본은 **`Age 144,969`(40시간)** 였고, 이 티켓의 진단이 *"이 사본을 새로
만드는 유일한 사건은 **배포**"* 였다 ⇒ 그 사건이 일어났다.
🔵 독립 확인: 네 머지 커밋 각각에 **`Vercel deploy hooks` = success**.

### (b) 🔴 「`store /` 의 배너가 **사라지는가**(백엔드 up)」 — **이것이 「방문자가 보는 것」이다**

🟢 **쟀다.** 창(`16:10:55Z` 기동 → `store` **ready** `16:19:51Z`)에서 백엔드가 **up** 인
상태로 `16:27:32Z` 에 찍었고, 그 **그림을 열어서** 확인했다: **배너가 없다.**
🔴 «파일이 바뀌었다» 가 아니라 **«렌더된 화면에 없다»** 로 쟀다 — AC-4 가 요구한 구분 그대로.

### (c) 🔵 「`Age`/`X-Vercel-Cache` 는 STALE 이어도 **상관없다**」

🟢 **확인했다.** 데모가 `stopped` 일 때(배너가 **나와야 옳은** 상태) 프리렌더 HTML 의
배너 문구가 **0건**이다 ⇒ 판정이 더는 캐시된 사본에 없다. 그러므로 캐시가 낡든 말든
배너의 정확성에 영향이 없다 — **이 티켓이 고른 처방의 요점이 바로 그것**이다.

## 🔵 부수 — 이 창이 **반증한** 것도 있다

「배너가 사라졌다」를 웜업 신호로 쓰면 안 된다: `16:12` 에 이미 탐침이 `running` 이었는데
`store` 묶음은 `booting` 이었고 ready 까지 **7분 더** 걸렸다. 판정이 인스턴스 상태만 보고
묶음 헬스를 안 보기 때문이다. 🔴 **이 티켓의 결함이 아니라 새로 관측된 축**이고,
`TASK-MONO-636` 이 이름 붙인 축의 **세 번째 값**(「켜지는 중」)이다 — 별도 티켓 후보.

⇒ 🟢 **AC-0 ~ AC-4 의 모든 칸이 닫혔다.** 이 티켓은 `review/` 로 간다.

# Task ID

TASK-MONO-625

# Title

🔴 **마지막 데모-호스트 행을 옮기면 부팅 프로브가 빈다** — `ADR-MONO-067` 단계 3 의 론처 몫

# Status

review

# Owner

monorepo

# Task Tags

- adr
- demo
- launcher
- guard

---

# Goal

`ADR-MONO-067` 단계 3 이 요구하는 **론처의 console 행 이관**(`data-served`: `demo-host` → `vercel`)을,
**부팅 판정을 죽이지 않고** 수행한다.

# 🔴 이 티켓이 «한 단어 변경» 이 아닌 이유 (실측, 2026-09-05 UTC)

ADR 은 단계 3·4 의 론처 몫을 이렇게 적었다:

> *"console·fan 행을 옮길 때 필요한 것은 새 예외가 아니라 그 행의 `data-served` 를 `vercel`
> 로 바꾸는 것뿐이다."*

**형제 둘에서는 그것이 참이었다. console 에서는 거짓이다** — 이 행이 **마지막 데모-호스트 행**
이기 때문이다.

## 현재 세 행 (`infra/demo/aws/site/index.html:96-98`)

```
console    data-served="demo-host"  data-host="console"                      ← 남은 하나
ecommerce  data-served="vercel"     data-url="https://store.hubwang.com/"
fan        data-served="vercel"     data-url="https://fan.hubwang.com/"
```

## 🔴🔴 그 선언을 **부팅 판정도 읽는다**

`infra/demo/demo-up.sh:513-556` 이 같은 마크업에서 «찌를 표면» 을 유도한다
(*"선언의 출처는 여전히 한 벌이다 — 두 벌이면 하나만 고쳐진다"*). 그리고 하한이 있다:

```
SURFACE_ROW_FLOOR=3   # 론처가 약속하는 화면의 총 수 (추출이 깨졌는가)
SURFACE_FLOOR=1       # 부팅 때 실제로 찌를 표면의 수  ← provenance: "console 뿐"
```

**console 행을 옮기면 찌를 표면이 `0` 이 된다.**

| 하는 일 | 결과 |
|---|---|
| 행만 옮기고 `SURFACE_FLOOR=1` 그대로 | `surf_undecidable=("추출:0/1")` → **`final_rc=1`** ⇒ **모든 데모 부팅이 실패**한다. 그리고 그 실패는 *"데모가 안 떴다"* 로 읽힌다 |
| `SURFACE_FLOOR=0` 으로 내린다 | 루프가 **빈 채로 통과**한다 — `surf_ok` 가 비고 `✔ HTTP 표면` 줄이 아예 안 찍히며 `final_rc` 는 **무조건 0**. 🔴 **성공이 고장난 것과 구별되지 않는다** |

🔵 `demo-up.sh` 자신이 이 함정을 **이미 두 번 적어 뒀다**:

> 🔴🔴 `TASK-MONO-618` — **이 값을 억제와 같은 PR 에서 안 내리면 부팅이 영구 실패한다.**
> … `TASK-MONO-604` AC-3 이 *"583 이 없었다면 이 티켓이 부팅을 영구히 못 끝내게 만들었을 것"*
> 이라고 적은 그 자리다.

**이번이 그 계열의 세 번째이고, 앞의 둘과 다른 점은 「N=1 → N=0」이라는 것이다.**
형제들은 옮긴 뒤에도 console 이 남아 있어서 하한을 **내리면** 됐다. 여기서는 내릴 바닥이 없다.

## 🔴 진짜 원인 — **한 선언이 두 질문에 답하고 있고, 여기서 둘이 갈린다**

| 질문 | 지금 답하는 것 |
|---|---|
| «방문자는 어디로 가는가» | `data-served` |
| «부팅이 끝났는지 무엇으로 재는가» | **같은** `data-served` |

console 은 **방문자는 Vercel 로 가지만 데모 호스트도 여전히 서빙한다**(억제 오버레이가 없다 —
`infra/demo/console-vercel.override.yml` 부재, `projects.sh:59` 에 그 항목 없음).
⇒ **두 답이 처음으로 달라진다.** 이 티켓이 고를 것은 그 분기를 어떻게 표현하느냐다.

# Scope

**In**

- `infra/demo/aws/site/index.html` — console 행의 서빙 출처 선언
- `infra/demo/demo-up.sh` — 부팅 표면 판정(하한 · 유도 규칙)
- `infra/demo/verify-demo-wrapper.sh` — `(z14)` 가 새 모양을 물도록
- 필요하면 `infra/demo/aws/site/check-launcher-fresh.sh`

**Out**

- 🔴 **데모의 console 억제**(`console-vercel.override.yml`). 그것은 **별개이고 선행이 다르다** —
  `TASK-MONO-618` 이 fan 억제를 «왕복이 세션까지 성립한 뒤에» 얹은 것처럼, console 억제는
  `TASK-MONO-624`(홉 ②③④⑤) 뒤이고, 게다가 **585 가 기록한 알려진 한계**(`console-bff` 는
  Vercel 에서 안 닿는다)가 «억제해도 되는가» 자체를 여는 질문으로 만든다.
  🔵 **이 티켓은 억제를 전제하지 않는다** — 오히려 «데모 호스트가 아직 서빙 중» 이 이 티켓의
  전제다.
- 방문자 화면 코드. 이 티켓은 **론처와 부팅 판정**만 만진다.

# Acceptance Criteria

- [x] **AC-0 (착수 시 재측정 + 분기 결정)** ✅ 세 축 재측정(§ 구현 기록 0) · 갈래 **A** · B·C 기각 사유 기록
      1. 세 행의 `data-served` 를 **다시 읽는다**(위 표를 상속하지 마라).
      2. `SURFACE_FLOOR` · `SURFACE_ROW_FLOOR` 의 **현재 값과 provenance 주석**을 읽는다.
      3. 🔴 **`console.<DEMO_DOMAIN>` 을 데모 호스트가 아직 서빙하는지 확인한다**
         (`projects.sh` 의 `[console]` 체인에 `*-vercel.override.yml` 이 있는가).
         **있으면 이 티켓의 전제가 무너진 것이므로 멈추고 보고한다.**
      4. 아래 세 갈래 중 하나를 고르고 **왜 나머지 둘이 아닌지** 적는다:

      | 갈래 | 내용 | 🔴 대가 |
      |---|---|---|
      | **A** | 마크업에 **부팅 프로브용 선언을 따로** 둔다(예: `vercel` 행에도 `data-demo-probe="console"`). 한 벌 유지, 파생만 둘 | 속성이 하나 늘고 `(z14)`·`demo-up.sh`·`check-launcher-fresh.sh` 셋이 그 규칙을 알아야 한다 |
      | **B** | 부팅 프로브를 **론처 마크업에서 떼어** `projects.sh`/체인에서 유도한다 | 🔴 «선언이 두 벌» 로 돌아간다 — ADR 이 명시적으로 피한 모양 |
      | **C** | 부팅 완료 판정에서 **HTTP 표면 축을 폐기**하고 도메인 헬스만 쓴다 | 🔴 축이 하나 줄어든다. 「컨테이너는 healthy 인데 화면은 502」를 못 잡게 된다 |

      🔵 **추천은 A** 이지만 **AC-0 이 골라라** — 위 표는 착수 전 추정이다.

- [x] **AC-1 — 행을 옮긴다.** ✅ `data-served="vercel"` + `data-url="https://console.hubwang.com/"`(307 라이브 실측 · 음성 대조군 404) + `data-demo-probe="console"`, `data-host` **제거** `data-served="vercel"` + `data-url="https://console.hubwang.com/"`,
      그리고 🔴 **`data-host` 를 반드시 제거**한다. 마크업 주석이 *"두 속성은 상호 배타"* 라고
      적었고 `(z14)` 가 `vercel` 행의 잔존 `data-host` 를 **실패로 문다**
      (`verify-demo-wrapper.sh:2651`).

- [x] **AC-2 — 부팅 판정이 여전히 무언가를 잰다.** ✅ `SURFACE_FLOOR` **1 그대로**(0 으로 안 내렸다). 지문에 이름이 찍힌다: `HTTP 표면 1/1` · `console=<code>`
      🔴🔴 **하한을 0 으로 내려서 통과시키지 마라.** 빈 루프의 `rc=0` 은 «표면이 정상» 과
      «측정이 죽었다» 를 **구별하지 못한다** — 이 저장소가 이름 붙인 실패다
      (*"줄어드는 모집단에 하한을 걸면 성공이 고장난다"*).
      통과 기준: **부팅 완료 지문에 표면이 하나 이상 이름으로 찍힌다**(예: `console=307`).

- [x] **AC-3 — bite.** ✅ (a) 선언 삭제 → 판정 불가 `(z15)(6)` · (b) 대상 0 → rc≠0 · (c) 정상 → 초록(52칸 rc=0). 주입 단언 3종을 **각각 깨뜨려** 물리는 것까지 확인(① ② ③). 🔴 ④ 는 기대와 다른 셀 `(z4)` 이 먼저 물었다 — § 5 에 그대로 적었다 고른 갈래를 **주입해서** 증명한다:
      (a) console 행의 선언을 지우면 → **판정 불가**(조용한 통과 아님)
      (b) 프로브 대상이 0이 되면 → **빨간다**
      (c) 정상 상태 → **초록**
      🔴 **주입이 실제로 됐는지 먼저 단언하라.** `(z14)` 는 이미
      `verify-demo-wrapper.sh:3082` 에서 프로브 행을 주입하는 선례를 갖고 있다.

- [x] **AC-4 — 하한의 provenance 를 갱신한다.** ✅ 🔴 틀린 것은 «값» 이 아니라 «뺄셈» 이었다 — 문장을 합집합 유도로 갈아 끼우고 2026-09-06 전수 목록을 붙였다 `SURFACE_FLOOR` 주석이 지금
      *"= 1 (console 뿐)"* 이라고 적고 있다. 🔴 값을 바꾸든 안 바꾸든 **그 문장은 거짓이 된다** —
      같은 커밋에서 고쳐라(같은 사실이 두 곳에 있으면 한쪽만 고쳐진다).

- [x] **AC-5 — 부팅 완료 지문이 바뀐 것을 적는다.** ⚪→✅ 🔴 **지문은 «안 바뀌었다»** (`console=307`, 1/1) — 프로브 선언을 서빙 선언에서 뗐기 때문이다. AC 가 «바뀐 것을 적어라» 였으므로 **왜 안 바뀌었는지와 언제 바뀔지**(console 억제 시 1/1 → 0개)를 `demo-up.sh` 주석에 적어 닫았다 `demo-up.sh` 주석이 창 #3 의
      `console=307 web.fan-platform=307`(2/2) → `console=307`(1/1) 변화를 이미 기록했다.
      🔴 **다음 기동 창에서 옛 지문을 기다리면 창이 영원히 안 열린다** — 새 지문을 적어라.

- [x] **AC-6 — `(z14)` 가 새 모양을 문다.** ✅ `z14_floor=3` 그대로. 🔴 **AC-6 의 예고는 맞았고 처방의 위치가 틀렸다** — `z15probe` 행은 `(z15)` 의 임시 트리에만 있어 `(z14)` 모집단이 아니다. `(z14)` 는 자기 픽스처(`z14probe`)가 필요했고 그것을 만들었다. `z15probe` 는 그대로 뒀다(Edge Case ②) `z14_floor=3` 은 **그대로**다(론처가 약속하는 화면
      총 수는 안 줄었다). 🔵 `demo-host` 분기의 bite 는 주입 행이 유지하므로 공허해지지 않는다 —
      **그것을 확인하고 적어라**(모집단이 0이 되는 분기는 조용히 죽는다).

# Related Specs

- [`docs/adr/ADR-MONO-067-demo-surfaces-served-from-vercel.md`](../../docs/adr/ADR-MONO-067-demo-surfaces-served-from-vercel.md)
  § 단계 표(단계 3) · § 단계 2 완료(`(z14)` 축 신설) · § 단계 3·4 는 이 자리도 함께 지난다

# Related Contracts

없음.

# Related Tasks

- `TASK-MONO-583` — `(z14)` 를 뒤집어 `data-served` 를 만든 티켓(단계 2 의 론처 몫)
- `TASK-MONO-603` — 링크가 활성인데 **컨테이너가 숨겨져** 있던 나머지 절반
- `TASK-MONO-604` · `TASK-MONO-618` — store · fan 의 **억제** 몫 (이 티켓의 Out)
- `TASK-MONO-585` — 단계 3 의 앱 몫. `done`. § 알려진 한계에 `console-bff` 미도달 기록
- `TASK-MONO-624` — console 왕복 실측. **억제**의 선행이지 이 티켓의 선행은 아니다

# Edge Cases

① 🔴 **`SURFACE_ROW_FLOOR`(3)와 `SURFACE_FLOOR`(1)는 서로 다른 것을 잰다.** 전자는 «추출이
   깨졌는가», 후자는 «찌를 것이 있는가». 하나를 고치며 다른 하나를 같이 만지면 두 축이 섞인다.

② **`(z14)` 의 self-test 주입 행**(`data-host="z15probe"`)은 `demo-host` 분기를 살려 두는
   유일한 모집단 원소가 될 수 있다. 🔴 그 행을 «이제 안 쓰니 지우자» 로 지우면 분기가
   **공허해진다** — 지우지 마라.

③ **론처는 배포본과 저장소 사본이 따로다.** 저장소를 고쳐도 `hubwang.com` 이 곧바로 바뀌지
   않는다. 🔵 라이브 확인은 배포 뒤에, 그리고 **서빙본**으로 하라
   (`check-launcher-fresh.sh --origin`).

④ 🔴 **데모가 꺼져 있을 때도 이 변경의 효과가 보여야 한다** — 그것이 이관의 목적이다.
   판정: 데모 `stopped` 에서 론처를 열어 console 링크가 **활성**인가.

# Failure Scenarios

① **하한을 0 으로 내리고 초록을 받는다** → 🔴 그것은 통과가 아니라 **계측기 상실**이다.
   AC-2 가 그것을 금지한다. 갈래 C 를 고른 경우에도 «표면 축을 폐기했다» 를 **명시**해야지
   하한만 0 으로 두고 넘어가면 안 된다.

② **`data-host` 를 남긴 채 `data-served="vercel"` 로 바꾼다** → `(z14)` 가 문다(설계대로).
   🔵 이 경우는 실패가 아니라 가드가 일한 것이다.

③ **부팅이 영구 실패한다** → AC-2 를 안 지킨 것이다. `demo-up.sh` 가 이 시나리오를 미리
   적어 뒀으니 그 주석을 읽어라.

# Definition of Done

- [x] 론처 세 행이 전부 `data-served="vercel"` + `data-url`, `data-host` **0개**
- [x] 부팅 완료 지문에 표면이 **이름으로** 찍힌다 (빈 통과 아님)
- [x] `SURFACE_FLOOR` provenance 주석이 새 사실과 일치
- [x] `(z14)` · `demo-up.sh` 양쪽에 bite 가 있고 **주입이 단언됐다**
- [x] 🔵 남은 단계 3 조각(**억제**)이 무엇이고 무엇에 막혀 있는지 ADR 이나 이 티켓에 적혀 있다

---

분석=Opus 5 / 구현 권장=Opus (AC-0 이 **설계 분기**를 고르고, AC-2 가 «하한을 0 으로 내리면
안 되는 이유» 를 지켜야 한다 — 기계적 치환이 아니다).

---

# 📏 AC-0 선실측 (2026-09-05 UTC) — **갈래는 A. 그리고 범위가 이 티켓이 적은 것보다 크다**

🔵 착수해서 마크업·가드를 실제로 고쳐 본 뒤 **되돌렸다.** 되돌린 이유가 아래 ④ 다.
이 절은 다음 사람이 같은 길을 다시 걷지 않게 하려고 적는다.

## ① 세 축 재측정 — 티켓의 전제 그대로

| 축 | 값 |
|---|---|
| 세 행의 `data-served` | console=`demo-host` · ecommerce=`vercel` · fan=`vercel` |
| `SURFACE_ROW_FLOOR` / `SURFACE_FLOOR` | `3` / `1` (provenance 주석: *"console 뿐"*) |
| 데모 호스트가 console 을 아직 서빙하나 | ✅ **서빙한다** — `projects.sh:59` 의 `[console]` 체인에 `*-vercel.override.yml` **없음**, `infra/demo/console-vercel.override.yml` **부재** |

## ② 갈래 = **A**, 그리고 `data-host` 재사용은 안 된다

`(z14)` 가 vercel 행의 잔존 `data-host` 를 **실패로 문다**
(`verify-demo-wrapper.sh` — *"아무도 안 읽는 낡은 값입니다 … 그리고 demo-up.sh 가 그 행을
다시 찌를 위험이 생깁니다"*). 🔵 **그 규칙은 옳다** — 「낡은 값」과 「의도된 선언」은 다른
것이므로 **이름을 달리해야** 한다(`data-demo-probe="<호스트접두사>"`).

🔴 B(체인에서 유도)를 버린 이유: 프로브용 호스트명을 compose 에서 얻으려면 Traefik `Host()`
규칙을 파싱해야 하고, 그건 «선언 두 벌» 을 피하려다 **더 취약한 세 번째 유도**를 만든다.
🔴 C(표면 축 폐기)를 버린 이유: 「컨테이너는 healthy 인데 화면은 502」를 잡는 유일한 축이다.

## ③ 열화는 585 를 읽고 나니 **좁다** — 이관을 미룰 이유가 아니다

Vercel 콘솔에서 죽는 것은 `console-bff` 를 지나는 **레그 3개**(대시보드 개요 · 도메인 상태 ·
알림 인박스)뿐이고, 나머지 6개 도메인 화면은 `ADR-MONO-017` D3.B 대로 **게이트웨이 직결**이라
영향이 없다. 그 셋도 실패를 **상태로 표현**한다(`bffUnavailable` / 502 봉투).
⇒ *"데모 호스트 콘솔이 더 온전하니 옮기지 말자"* 는 **과장이다**(내가 한 번 그렇게 말했다).

## ④ 🔴🔴 **그런데 `(z14)` 자신이 데모-호스트 행 ≥1 을 요구한다** — 여기서 멈췄다

```
[ "$z14_n_demo" -ge 1 ] || fail "(z14) demo-host 정책을 받는 행이 **0개**입니다
                                 — 그 정책은 안 재고 초록이 됩니다."
```

그리고 그 아래 **실행 대조 루프**의 `else`(데모 호스트) 분기 — `(1)` 파생 주소 · `(2)` bite
(DOWN/UNKNOWN/STOPPED 비활성) · `(3)` partial · `(4)` stale — 도 **행이 0이면 한 칸도 안
돈다.** 가드가 그 이유를 자기 주석에 적어 뒀다: *"아래 실행 대조는 존재하지 않는 행에 대해
아무 단언도 하지 않기 때문이다."*

⇒ console 행을 옮기면 **`demo-up.sh` 만이 아니라 `(z14)` 의 정책 커버리지 모델**을 바꿔야
한다: *"진짜 행이 하나 있다"* → *"주입 픽스처가 그 분기를 돌린다"*. 🔵 그 저장소엔 이미
선례가 있다(`verify-demo-wrapper.sh` 가 `data-host="z15probe"` 행을 sed 로 주입한다) —
**없는 것은 기전이 아니라 (z14) 를 그 기전 위로 옮기는 일**이다.

🔴 **이것은 이 티켓의 AC-6 이 예고한 바로 그 지점**이다(*"모집단이 0이 되는 분기는 조용히
죽는다"*). 예고는 맞았고, **크기 추정이 틀렸다** — 「한 단어 + 하한 조정」이 아니라
**가드 커버리지 리팩터**다.

## ⑤ 🔴 실행으로는 못 쟀다 — 그 구별을 지킨다

`verify-demo-wrapper.sh` 를 통째로 돌려 `(z14)` 를 실제로 빨갛게 만들어 보려 했으나
**셀 (h)「참조 이미지가 레지스트리에 실재하는가」에서 멈춘다**(300초 timeout, rc=124 —
로그는 `(h)` 줄에서 끝난다). ⇒ 위 ④ 는 **소스 판정**이지 실행 측정이 아니다.
🔵 단, 술어가 `[ "$z14_n_demo" -ge 1 ]` 라는 **산술 한 줄**이고 그 입력이 마크업 행 수이므로
불확실성은 낮다. 🔴 그래도 구현 시에는 **(z14) 셀만 떼어 돌려 RED 를 먼저 확인**하라 —
이 저장소가 「착수 전 RED」를 요구하는 자리다.

### 🔴🔴 그런데 «셀만 떼어 돌려라» 를 **그대로 할 수 없다** — 래퍼에 셀 선택이 없다

`verify-demo-wrapper.sh` 를 전수로 훑어도 `--only` / `SKIP` / 셀 인자 같은 **선택 기전이
없다**(`case "$1"` 넷은 전부 셀 **내부**의 `-p` 파서다). 셀은 파일 순서대로 **무조건 전부**
돈다 ⇒ `(z14)`(≈ L2546~) 에 닿으려면 그 앞의 `(a)`~`(h)` 를 다 지나야 하고, `(h)` 는
**Docker Hub 레지스트리 조회**라 이 환경에서 멈춘다.

🔵 `(h)` 가 느린 것은 결함이 아니다 — 그 셀은 *"우리 커밋과 무관하게 외부에서 깨지는"*
축(`MONO-353`, `bitnami/kafka:3.7` 태그 삭제)을 재려고 **일부러** 외부를 찌른다.

⇒ 착수자가 고를 수 있는 길 (측정 안 함 — 셋 다 후보다):

| | 방법 | 대가 |
|---|---|---|
| a | `(z14)` 구간을 잘라 임시 스크립트로 실행 | 🔴 **`fail`·`grepq` 등 래퍼의 헬퍼를 함께 가져와야** 한다. 잘라낸 하네스가 원본과 달라지면 **하네스를 재는 것**이 된다 |
| b | 네트워크가 되는 환경(CI)에서 전체 실행 | 🔴 왕복이 느리고 「착수 전 RED」를 로컬에서 못 본다 |
| c | 래퍼에 **셀 선택 인자를 추가**한다 | 🔵 이 티켓 밖이지만 **다음 사람도 같은 벽을 만난다** — 별도 티켓 후보 |

🔴 **a 를 고르면 「하네스가 어느 트리를 재는지」를 먼저 핀하라** — 잘라낸 조각이 원본과
같은 것을 재고 있는지부터 단언해야 한다. 그 단언 없이 얻은 RED 는 **내 하네스가 만든
RED** 와 구별되지 않는다.

## ⇒ 남은 일 (다음 착수자에게)

1. `(z14)` 의 demo-host 정책 커버리지를 **주입 픽스처**로 옮긴다(실행 대조 4칸 포함).
   `z14_n_demo >= 1` 은 «진짜 행» 이 아니라 «그 분기가 돌았나» 를 재게 바꾼다.
2. 마크업: console 행 → `data-served="vercel"` + `data-url="https://console.hubwang.com/"`
   + `data-demo-probe="console"`, `data-host` **제거**.
3. `(z14)`: `data-demo-probe` 규칙(vercel 행 전용 · 호스트 접두사만 · demo-host 행엔 금지).
4. `demo-up.sh`: 프로브 대상 = demo-host 행의 `data-host` **∪** vercel 행의 `data-demo-probe`.
   `SURFACE_FLOOR` 는 **1 그대로**, provenance 주석만 갱신.
5. bite 3종(선언 삭제 → 판정 불가 · 대상 0 → 빨강 · 정상 → 초록), **주입을 먼저 단언**.

---

# 🔨 구현 기록 (2026-09-06 UTC) — 갈래 A 집행

## 0. AC-0 재측정 (상속하지 않고 다시 쟀다)

| 축 | 실측값 | AC-0 기록과 |
|---|---|---|
| 세 행의 `data-served` | console=`demo-host` · ecommerce=`vercel` · fan=`vercel` | 일치 |
| `SURFACE_ROW_FLOOR` / `SURFACE_FLOOR` | `3` / `1` (provenance 주석 *"console 뿐"*) | 일치 |
| 데모 호스트가 console 을 아직 서빙하나 | ✅ `projects.sh` `[console]` 체인 = `projects/platform-console/docker-compose.yml` **단독**, `infra/demo/console-vercel.override.yml` 부재 | 일치 ⇒ **전제 유지, 멈추지 않는다** |

🔵 **`console.hubwang.com` 이 살아 있는지도 쟀다**(AC-1 이 그 주소를 박으라고 하므로 —
죽은 주소를 넣으면 가드는 초록이고 방문자는 404 를 본다):

```
https://console.hubwang.com/            307 → /dashboards/overview
https://store.hubwang.com/              200
https://fan.hubwang.com/                307 → /login?from=%2F
https://nosuch-negative-control...      404      ← 음성 대조군
```

음성 대조군이 404 이므로 그 307 은 **와일드카드가 아니라 이 프로젝트에 실제로 물린 것**이다.

## 1. 🔴🔴 AC-0 ⑤ 의 벽을 넘었다 — 「셀 (h) 에서 멈춘다」는 **오진이었다**

AC-0 은 *"래퍼를 통째로 돌리면 `(h)` 에서 멈춘다(300초 timeout, rc=124)"* 로 기록했고,
그래서 「(z14) 셀만 떼어 돌려라 → 그런데 셀 선택 기전이 없다」는 막다른 골목을 남겼다.

**멈춘 게 아니라 느린 것이었다.** 실측:

| | |
|---|---|
| `docker manifest inspect traefik:v3.1` | 31.9s |
| `docker manifest inspect alpine:3.20` | 34.8s |
| `docker manifest inspect traefik:v3.1` (2회차) | 25.8s |
| (h) 가 조회하는 이미지 수 | 17 |

⇒ 17 × ~30s ≈ **8.5분**. 300초 타임아웃이 그보다 짧았던 것이고, `rc=124` 는 가드가 아니라
**타임아웃의 지문**이었다. 🔵 이 저장소가 이름 붙인 함정 그대로다 — *"딱 떨어지는 초 =
타임아웃 지문(느림 아님)"* 의 거울상으로, 여기서는 **느림을 멈춤으로** 읽었다.

⇒ 그래서 AC-0 이 나열한 a·b·c 중 **아무것도 고르지 않았다.** 네 번째 길이 있었다:

> **`(h)` 만 PATH shim 으로 무력화하고 원본 래퍼를 그대로 돌린다.**
> shim 은 `docker manifest …` 만 즉시 0 을 돌려주고 나머지는 실물 `docker` 로 위임한다.

🔴 **왜 이것이 a(잘라내기)보다 안전한가** — 래퍼 소스가 **바이트 동일**하고 `fail`·`grepq`
등 헬퍼가 전부 실물이며, 개입의 영향이 **내가 판정을 쓰지 않는 셀 하나**에 갇힌다.
a 는 잘라낸 조각이 원본과 달라질 여지를 만들고, 그러면 «내 하네스가 만든 RED» 와
«가드가 문 RED» 가 구별되지 않는다.

🔴🔴 **그 대가를 명시한다: 이 shim 아래서 `(h)` 의 판정은 아무것도 증명하지 않는다.**
아래 모든 로컬 런의 `이미지 실재 검증 커버리지 17/17` 은 **가짜**다. 그 축은 CI 가 진짜로
잰다(그리고 nightly 는 `--require-coverage` 로 무신호까지 잡는다). 🔵 셀 선택 기전이
없다는 AC-0 의 관찰 자체는 여전히 참이고, 그 후속 티켓 후보(c)도 여전히 유효하다.

## 2. 착수 전 RED (AC-3 이 요구하는 자리)

마크업만 옮기고 가드는 안 건드린 트리에서 원본 래퍼 실행:

```
[verify] (z14) 방문자 화면 링크가 전부 있고, 안 뜬 화면은 열리지 않는가
  FAIL: (z14) demo-host 정책을 받는 행이 **0개**입니다 — 그 정책은 안 재고 초록이 됩니다.
```

⇒ AC-0 ④ 의 **소스 판정이 실행으로 재현됐다.** 예측이 맞았고, 이제 근거가 산술 한 줄이
아니라 관측이다.

## 3. 무엇을 고쳤나

### (1) `infra/demo/aws/site/index.html` — AC-1

```
console  data-served="vercel"  data-url="https://console.hubwang.com/"  data-demo-probe="console"
```

`data-host` **제거**(두 속성 상호 배타 · `(z14)` 가 잔존값을 문다). 🔵 **방문자 경로는 한
줄도 안 바뀐다** — `renderSurfaces()` 는 `data-served`/`url`/`host` 만 읽고 새 속성을 안 본다.

### (2) `infra/demo/demo-up.sh` — AC-2 · AC-4 · AC-5

찌를 대상 = **demo-host 행의 `data-host` ∪ vercel 행의 `data-demo-probe`**.
`SURFACE_FLOOR` 는 **1 그대로**(🔴 0 으로 안 내렸다), `SURFACE_ROW_FLOOR` 는 **3 그대로**
(Edge Case ① — 두 하한은 다른 것을 잰다. 하나 고치며 다른 하나를 만지지 않았다).

🔴 **AC-4 — provenance 주석에서 틀린 것은 «값» 이 아니라 «뺄셈» 이었다.** 옛 문장은
*"3 에서 Vercel 로 옮겨간 두 행을 뺀 값"* 이었고, 그 뺄셈은 이제 **0** 을 내놓는데 값은
1 이다. 그래서 문장을 새 유도로 갈아 끼웠다(합집합 · 2026-09-06 전수 목록 포함).

### (3) `(z14)` — 🔴🔴 이 티켓의 실제 크기

- **`z14_n_demo >= 1` 하한을 걷어냈다.** 그 하한은 «정책이 재어졌나» 를 «진짜 행이 있나» 로
  물었고, 두 질문은 **모집단이 줄지 않는 동안만** 같은 답을 낸다.
- 대신 **주입 픽스처**(`wms|demo-host|z14probe||`)가 demo-host 정책 4칸을 돌린다.
  실행 대조 루프의 입력이 `z14_rows_fx`(실제 행 + 픽스처)이고, **vercel 칸은 실제 판정
  (`z14_out`)** 을, **demo-host 칸은 픽스처 판정(`z14_out_fx`)** 을 읽는다 ⇒ 두 축이
  서로의 모집단에 안 묶인다.
- `vercel-0` 대조군의 행 집합도 픽스처에서 온다(안 그러면 그 칸이 통째로 사라진다).
- `data-demo-probe` 규칙을 **순수 술어 `z14_probe_err()` 로 떼어 내고 self-test 9칸**을
  붙였다(통과해야 할 4 + 물어야 할 5 — 🔴 물기만 세면 «전부 문다» 는 술어도 만점이다).
- **`z14_n_boot_probe = z14_n_demo + z14_n_probe`** 를 계산해 (z15)에 넘긴다.

### (4) `(z15)` — AC-3 · AC-6

- 기대 건수의 출처를 `z14_n_demo`(이제 **0**) → `z14_n_boot_probe`(**1**)로 바꿨다.
  안 바꿨으면 이 칸이 `0/0` 을 확인하며 **아무것도 안 보고 초록**이 됐다.
- **bite 대상 `z15_target_host="console"` 은 안 바뀌었다** — 세 번째 이사가 아니었다.
  옮겨야 했던 것은 대상이 아니라 **대상을 고르는 식**이었다.
- 새 칸 **(6)**: `data-demo-probe` 를 지우면 → **판정 불가**(rc≠0 + `HTTP 표면 판정 불가` +
  실제로 아무것도 안 찌름). 🔴 주입 증명 3단언이 앞에 있다 — 원본에 속성이 있었나 ·
  사본에 없나 · **행 수는 그대로인가**(행이 사라졌다면 그 빨강은 `SURFACE_ROW_FLOOR`
  라는 **다른 축**이 낸 것이다).
- 새 칸 **(7)** 대조군: 사본을 **그대로** 돌리면 초록이고 console 을 찌른다 ⇒ (6)의 빨강이
  «선언을 지워서» 지 «사본이라서» 가 아님을 가른다.

## 4. AC-6 — `(z14)` 가 새 모양을 무는가

- `z14_floor=3` **그대로**(론처가 약속하는 화면 총 수는 안 줄었다). ✅
- 🔴 **AC-6 이 «주입 행이 demo-host 분기를 살려 준다» 고 적은 것은 (z15) 의 `z15probe`
  였고, 그것으로는 부족했다** — 그 행은 `(z15)` 의 임시 트리 안에서만 존재하므로
  `(z14)` 의 모집단에는 없다. `(z14)` 는 자기 픽스처가 필요했고, 그것이 이번에 만든
  `z14probe` 다. ⇒ **AC-6 의 예고는 맞았고 처방의 위치가 틀렸다.**
- `(z15)` 의 `z15probe` 주입 행은 **그대로 둔다**(Edge Case ② — 지우면 그 칸이 공허해진다).

## 5. 실측 — 무엇을 돌렸고 무엇이 나왔나

### 정상 (AC-3 (c))

`bash infra/demo/verify-demo-wrapper.sh` (정적 전량, `(h)` shim) → **rc=0 · 52칸 · FAIL 0건**

```
ok: 방문자 화면 링크 3개 — 실제 행: vercel 3행(여섯 상태 전부 활성·정적 주소) · demo-host 0행
    · 부팅 프로브 선언 1건(술어 self-test 9칸) · demo-host 정책 4칸은 **주입 픽스처**
      (wms/z14probe, 주입 3단언)로 집행 · 컨테이너 축(…)
ok: 부팅 판정이 HTTP 표면을 본다 — 1/1 확인(demo-host 행의 data-host ∪ vercel 행의
    data-demo-probe) · 억제된 Vercel 행은 안 찌름 · 표면 하나 죽이면 bite · 안 뜬 도메인은
    미검사 · 목록 0건은 판정 불가 · **프로브 선언 삭제도 판정 불가**(주입 3단언 + 대조군)
    · 목록은 론처 마크업에서 읽음
```

🔴 **AC-2 통과 기준 충족**: 부팅 완료 지문에 표면이 **이름으로** 찍힌다(`1/1`, 빈 통과 아님).

### 리포 가드

`check-index-queue-drift` · `check-task-id-collision` · `check-walkthrough-ledger-drift` ·
`check-public-domains` · `check-demo-resolver-copies` · `check-message-backticks` — **전부 rc=0**
(🔵 `git add` **뒤에** 돌렸다 — 스테이지 전에 돌리면 `git ls-files` 가드가 가짜로 통과한다).

### bite (AC-3) — 고의로 깨뜨려 **무는지** 확인

🔵 하네스는 `git checkout` 을 안 쓴다(이 저장소가 그것으로 작업분을 지운 이력이 있다) —
백업 파일에서 복원하고 `cmp` 로 원상을 단언한다. 주입이 파일을 **실제로 바꿨는지**도
매 칸에서 먼저 단언한다(안 바뀌면 그 칸은 아무것도 시험하지 않는다).

| # | 무엇을 깨뜨렸나 | 기대 | 실측 |
|---|---|---|---|
| ① | 픽스처 행을 **아예 주입 안 함** | 주입 단언 ① | ✅ `FAIL: (z14) 픽스처 주입 실패 — 행이 3 → 3 입니다(기대 4).` |
| ② | 주입 행을 `demo-host` → `vercel` 로 | 주입 단언 ② | ✅ `FAIL: (z14) 픽스처 주입 실패 — demo-host 행이 여전히 0개입니다.` |
| ③ | 드라이버에 픽스처 대신 **실제 행**을 넘김 | 주입 단언 ③ | ✅ `FAIL: (z14) 주입한 픽스처 행 'wms' 이 판정 출력에 없습니다.` |
| ④ | `demo-up.sh` 의 프로브 유도를 **583 시절로 되돌림** | `(z15)` 가 문다 | ⚠️ 빨갛지만 **무는 셀이 다르다** — 아래 참조 |
| ⑤ | (정상) 손 안 댄 트리 | 초록 | ✅ rc=0 · 52칸 · FAIL 0건 |

🔴 **④ 를 정직하게 적는다 — 내 기대가 틀렸고, 결과는 기대보다 낫다.**
`demo-up.sh` 의 vercel-프로브 유도를 지우자 **`(z15)` 가 아니라 `(z4)` 가 먼저 물었다**:

```
FAIL: (z4) 대조군 실패 — 아무 도메인도 실패하지 않았는데 demo-up.sh 가 rc=1 로 끝났습니다.
```

`(z4)` 는 `demo-up.sh` 를 **실제로 실행**하는 셀이고, 프로브 대상이 0 이 되면
`SURFACE_FLOOR=1` 을 못 채워 `surf_undecidable=("추출:0/1")` → `final_rc=1` 이 된다.
⇒ 이것은 **이 티켓 § 표의 첫 행**(*"행만 옮기고 `SURFACE_FLOOR=1` 그대로 → 모든 데모
부팅이 실패한다"*)을 **실행으로 재현한 것**이다. 예고가 맞았다.

🔵 그래서 이 회귀는 **한 셀이 아니라 두 셀**에 잡힌다. `(z4)` 가 `fail()` 로 즉시
종료하므로 `(z15)` 까지 도달하지 않을 뿐이다.
🔴 그러면 `(z15)` 쪽 축은 안 재어진 것인가 — **아니다.** `(z15)` 의 새 칸 **(6)** 이
같은 축을 **자기 서브런**으로 잰다(`(z4)` 의 영향을 안 받는다): 프로브 선언을 지운
마크업으로 `demo-up.sh` 를 돌려 «판정 불가 + 아무것도 안 찌름» 을 확인하고, **(7)** 이
같은 파일을 안 지운 채 돌려 그 빨강이 «선언을 지워서» 지 «사본이라서» 가 아님을 가른다.
그 두 칸은 정상 런에서 통과했다 — 즉 **red 를 실제로 만들어 내고 확인했다.**

## 6. 🔵 남은 단계 3 조각 — Definition of Done 마지막 항목

**억제**(`console-vercel.override.yml`)가 남았고, 이 티켓은 그것을 **전제하지 않는다**
(오히려 «데모 호스트가 아직 서빙 중» 이 전제다). 막고 있는 것:

1. `TASK-MONO-624`(홉 ②③④⑤ 왕복 실측) — `TASK-MONO-618` 이 fan 억제를 «왕복이 세션까지
   성립한 뒤에» 얹은 것과 같은 순서.
2. `TASK-MONO-585` 가 기록한 **알려진 한계** — `console-bff` 는 Vercel 에서 안 닿는다.
   그것이 «억제해도 되는가» 자체를 여는 질문으로 만든다.

🔴🔴 **그 억제 PR 이 반드시 함께 다루어야 할 것들을 여기에 적어 둔다** (그때 흩어져 있으면
한 곳만 고쳐진다):

- `index.html` — console 행의 **`data-demo-probe` 를 지운다**(안 지우면 부팅 판정이 영원히
  안 열리는 주소를 12번 재시도하고 그 실패는 「데모가 안 떴다」로 읽힌다).
- `demo-up.sh` — `SURFACE_FLOOR` 가 **N=0** 이 된다. 🔴 **하한을 0 으로 내리는 것은 답이
  아니다**(빈 루프 rc=0 = 계측기 상실). 필요한 것은 **설계 변경**이며, 그 문단이 이미
  `demo-up.sh` 주석에 «N=1 과 N=0» 으로 적혀 있다.
- `(z15)` — `z14_n_boot_probe >= 1` 이 **그날 빨개지는 파수꾼**이다. 그 빨강을 하한 조정으로
  끄지 마라. 그리고 `z15_vercel_hosts` 에 `console` 을 더하고 바닥을 3 으로 올려라
  (지금 더하면 «찌르면 안 되는데 찔렀다» 로 **거짓** 빨강이 난다 — 주석에 적어 뒀다).
- `(z14)` — 영향 없다. 픽스처가 커버리지를 들고 있으므로 모집단이 어떻게 변하든 안 죽는다.
  **그것이 이 티켓이 산 것이다.**

## 7. ⚪ 못 잰 것 (그리고 왜 그것이 이 티켓의 결함이 아닌지)

| 축 | 상태 | 사유 |
|---|---|---|
| 라이브 론처(`hubwang.com`)에 새 마크업이 서빙되나 | ⚪ **미측정** | Edge Case ③ — 론처는 배포본과 저장소 사본이 따로다. 저장소를 고쳐도 즉시 안 바뀐다. 확인은 **배포 뒤 서빙본**으로(`check-launcher-fresh.sh --origin`) |
| 데모 `stopped` 에서 console 링크가 활성인가 (Edge Case ④) | 🟢 **가드가 잰다** | `(z14)` 의 `STOPPED` 칸이 vercel 행 3개 전부에 «활성 + 정적 주소» 를 단언하고 통과했다. console 이 그 셋에 들어왔다는 것이 이 티켓의 효과다 |
| `(h)` 이미지 실재 | ⚪ **로컬에선 무의미** | 위 §1 의 shim. CI 가 잰다 |
| 실기동(`--live`) | ⚪ **미측정** | docker 데몬이 이 호스트에서 꺼져 있다. `--live` 는 CI/packer 몫이고, 새 축은 전부 정적 칸이라 `(z16)` 이 그것들이 게이트에 안 갇혔음을 확인했다(갇힌 칸 0건) |

# Task ID

TASK-MONO-622

# Title

팬의 해석기 입력이 들어갔다 — 그런데 **그것을 잰 사람도, 낡았을 때 빨개질 것도 없다**

# Status

review

# Owner

monorepo

# Task Tags

- adr
- demo
- ci

---

# 🔎 어디서 왔나 — `TASK-MONO-621` § AC-3 이 **이름을 적어 둔 일**이다

창 #4(2026-09-04)의 칸 5 가 FAIL 했고, 대조군이 원인을 한 칸으로 지목했다:

| Vercel 프로젝트 | `DEMO_API_BASE` (창 #4 시점) |
|---|---|
| `kanggle-store` | ✅ 있다 |
| `kanggle-portfolio` | ✅ 있다 |
| **`kanggle-fan`** | ❌ **없다** |

621 § AC-3 § A 는 이 결함의 소속을 이렇게 적었다:

> 🔵 **새 티켓이 아니라 `TASK-MONO-586` 그 자체다** — 그 티켓의 제목이 문자 그대로
> *"팬은 이미 Vercel 에 있는데 백엔드에 못 닿는다"* 다. 🔴 그런데 586 은
> `review/`(frozen) 이라 AC 를 못 얻는다 ⇒ **후속 티켓 발의 필요**.

**이 티켓이 그 발의다.** 🔴 그리고 발의를 준비하면서 **두 개를 더 찾았다** — 둘 다
「소유자가 env 를 넣었다」는 사실 하나가 저장소를 **거짓말쟁이로 만든** 자리다.

---

# 📐 세 갈래 — 하나는 미측정, 둘은 **오늘 거짓이 된 문장**

## ① 판정이 없다 — 586 의 명제를 아직 아무도 안 쟀다

소유자가 2026-09-04 에 `kanggle-fan` 에 **`DEMO_API_BASE` + `DEMO_PAYMENT_MOCK=1` 을
투입하고 배포**했다. 🔴 **효과는 창 밖에서 원리적으로 못 잰다** — 팬 표면은
`fan-platform-web/src/middleware.ts` 가 fail-closed 라(`TASK-FAN-FE-019` AC-1 의 (A))
공개 경로가 `/login` · `/api/auth/*` · `build-info.json` 뿐이고, 판정 창구 둘이 그 뒤에 있다:

| 재려는 것 | 창구 | 미인증 실측 (2026-09-04, 데모 OFF) |
|---|---|---|
| `DEMO_API_BASE` 발효 | `DemoBackendNotice` — `(main)` 셸의 서버 컴포넌트 | `/` → **307 → `/login?from=%2F`** |
| `DEMO_PAYMENT_MOCK` 발효 | `/api/payment-config` | **307** |

🔵 대조군: 같은 순간 `store.hubwang.com/` 은 **200**(스토어는 공개 셸)이고
`store.hubwang.com/api/store-config` 는 **307** ⇒ 게이트는 팬 전용 성질이 아니라
**라우트별**이며, 팬은 셸까지 닫혀 있다는 것이 차이다.

🔵 부수 확인 — 팬 배포는 코드상 최신이다: `fan.hubwang.com/build-info.json` =
`{"commit":"adcf4c22c0b8bf1840b76b57533acd4916dfd04a","ref":"main"}`. `origin/main` 보다
18 커밋 뒤지만 그 18개 중 `projects/fan-platform/web/fan-platform-web/**` 를 건드린 것은
**0건**(`vercel-ignore.sh` 정상 동작). 🔴 **「코드가 최신」은 「env 가 발효됐다」가 아니다.**

## ② 🔴🔴 가드 `(x2)` 가 **오늘 거짓이 된 실측값**을 박고 있다

`infra/demo/verify-demo-wrapper.sh:1310` (실측 2026-09-04, main `f4cbdd643`):

```
🔴🔴 2026-09-04 실측 기준 이 값은 kanggle-fan 에 **없었다** — 억제로 Vercel 판이
  유일한 팬이 되었으므로, 없으면 프런트가 'PortOne 키 미설정' 으로 구독 요청 자체를
  보내지 않는다(TASK-FAN-FE-015 가 고친 상태가 정확히 그것이다).
```

이 문장은 **쓰인 날 안에 거짓이 됐다**(같은 2026-09-04 에 소유자가 넣었다).
🔴 그리고 **아무것도 그것을 빨갛게 만들 수 없다** — 값은 저장소 밖(Vercel env)에 있고,
`618` 이 *"이 스크립트에는 그것을 빨갛게 만들 수단이 없다"* 를 **명시적으로 수용**했다.

🔴 **그래서 처방은 「값을 갱신」이 아니다.** 갱신해도 다음 소유자 조작에서 또 낡고,
그때도 아무것도 안 문다. [[feedback_a_figure_nothing_can_fail_on_will_drift]]

### 🔴🔴 그리고 그 처방은 **내가 지어낸 것이 아니다 — 형제 칸에 이미 적혀 있다**

같은 파일 `verify-demo-wrapper.sh:1189-1190`, 형제 칸 **(x)**(ecommerce 축)의 주석:

```
# 🔴 **날짜 박힌 실측값을 여기에 넣지 않는다.** 소유자가 값을 넣는 순간 거짓이 되는데
#    이 스크립트에는 그것을 빨갛게 만들 수단이 없다. 실측값은 원장과 티켓이 든다.
```

⇒ **(x2) 는 그 규칙을 어겼다.** 그리고 규칙은 티켓이 아니라 **바로 그 자리의 주석**에
있었다 — 121줄 위, 같은 함수 모양의 형제 칸이다.
[[feedback_grep_the_siblings_before_fixing_it_yourself]]

## ③ 🔴 그 메시지가 지목한 **원장이 비어 있다**

같은 메시지 `verify-demo-wrapper.sh:1308`:

```
원장      : projects/fan-platform/web/fan-platform-web/VERCEL.md
```

**실측** (2026-09-04, main `f4cbdd643`) — 🔵 **형제 원장과 나란히 세면 결함이 한눈에 보인다**:

| 칸 | 원장 | `DEMO_PAYMENT_MOCK` | `DEMO_API_BASE` | 헤더 수 |
|---|---|---|---|---|
| **(x)** ecommerce | `…/apps/web-store/VERCEL.md` | **5건** | **5건** | 18 |
| **(x2)** fan | `…/fan-platform-web/VERCEL.md` | 🔴 **0건** | 🔴 **0건** | 7 |

🔵 양성 대조군은 두 겹이다 — ① 팬 원장 파일 자체는 존재하고 헤더가 7개 있다(빈 파일도,
잘못 지목한 것도 아니다) ② **형제 칸은 같은 지시를 하고 그쪽 원장에는 실제로 있다**
(5건 ×2) ⇒ 「원장에 적는다」는 관행이 지켜지지 **않은 것이 팬 쪽뿐**이다.

⇒ 가드가 사람에게 *"원장을 보라"* 고 시키는데 **원장에 그 항목이 없다.** 지시받은 사람은
빈 파일을 열고 자기 판단으로 메우거나 그냥 넘어간다.
[[feedback_the_unguarded_operation_is_where_the_invariant_breaks]]

---

# Goal

`TASK-MONO-586` 의 명제 — *"팬은 이미 Vercel 에 있는데 백엔드에 못 닿는다"* — 를
**기동 창 #5 에서 판정해 닫고**, 그 판정을 조용히 낡게 만드는 두 자리
(하드코딩된 실측값 · 비어 있는 원장 포인터)를 고친다.

# Scope

**In**

- 기동 창 #5 에서 팬 표면 판정 (`DEMO_API_BASE` 축 · `DEMO_PAYMENT_MOCK` 축)
- `infra/demo/verify-demo-wrapper.sh` (x2) 의 미집행 안내 문구
- `projects/fan-platform/web/fan-platform-web/VERCEL.md` (또는 포인터가 가리킬 실제 원장)
- `tasks/review/TASK-MONO-586` 의 `## CORRECTION` + 그 티켓의 close chore

**Out**

- 🔴 **억제 되돌리기**(`618` 의 fan override 제거). 621 § AC-3 § B 가 *"지금 권하지
  않는다 — env 한 줄이 훨씬 싸다"* 로 판정했고, 이 티켓은 그 판정을 뒤집지 않는다.
  뒤집으려면 **재굽기 1회**가 들고, 그것은 소유자 결정이다.
- `ADR-MONO-067` 단계 3 (console) · `TASK-MONO-585`
- `TASK-MONO-615` ⓐ (rc=124) — 같은 창을 쓸 수는 있으나 이 티켓의 칸이 아니다

---

# Acceptance Criteria

## AC-0 — 🔴 착수 시 **다시 잰다** (verify-then-act)

착수 시점에 아래 넷을 실측하고 **값을 적는다.** 🔴 이 문서의 숫자를 상속하지 마라 —
① ② 는 다른 PR 이 이미 고쳤을 수 있고, ③ 은 확실히 낡는다.

| # | 무엇 | 방법 |
|---|---|---|
| ① | (x2) 의 *"2026-09-04 실측 기준 … 없었다"* 문장이 **아직 있는가** | `grep -n '없었다' infra/demo/verify-demo-wrapper.sh` |
| ② | `VERCEL.md` 의 두 키가 **아직 0건인가** | `grep -c` 두 번 + 🔵 **양성 대조군**(그 파일에 헤더가 ≥1 — 빈 파일을 「0건」으로 오독하지 않게) |
| ③ | 데모 예산·상태 | `/status` 의 `used_minutes` · `state` (🔴 이 문서의 318/600 을 쓰지 마라) |
| ④ | `586` 이 아직 `review/` 인가 | 다른 세션이 닫았으면 이 티켓의 AC-5 가 사라진다 |

🔴 ①이 이미 고쳐져 있으면 **AC-3 을 「해당 없음」으로 닫고 그 사실을 적는다** —
없어진 결함을 고친 척하지 않는다. [[feedback_retract_the_exemption_when_the_defect_is_fixed]]

## AC-1 — 창 #5 에서 팬 표면을 판정한다. 🔴🔴 **유효성 술어가 먼저다**

**🔴 판정 전에 반드시 통과해야 하는 유효성 술어 (세 칸 전부)**

```
① 최종 URL 이 /login 이 아닐 것            curl -w '%{url_effective}'
② 리다이렉트 체인의 Location 에 from= 이 없을 것
③ ①② 를 통과한 뒤에야 도메인 개체를 센다
```

🔴🔴 **왜 이것이 AC 의 첫 칸인가**: 로그인 페이지는 **200 / 10,913 B** 로 돌아오고,
`render.py` 류 추출기에 물리면 「아티스트 링크 0 · 멤버십 링크 0 · 도메인 개체 0」이
나온다 — **창 #4 의 칸 5 가 FAIL 이었을 때와 같은 값**이다. 유효성 술어 없이 재면
「측정했더니 여전히 FAIL」과 「측정을 안 했다」를 **구별할 수 없다.**
[[feedback_measurement_needs_a_validity_predicate]]

🔵 창 #4 의 FAIL 은 유효했다 — 세션 쿠키(`roundtrip.sh` 의 jar)를 들고 쟀으므로 로그인
HTML 이 아니라 에러 문구를 낸 피드 셸을 읽었다. 🔴 그러나 그것은 **적어 두지 않은 전제**
였다. 이 AC 가 그것을 명시적 칸으로 만든다.

**판정 술어**

| # | 술어 | PASS |
|---|---|---|
| a | `fan.hubwang.com/` 의 아티스트/멤버십 링크 수 | **≥ 1** |
| b | *"불러올 수 없습니다"* / *"데모 서버가 꺼져"* 출현 | **0건** |
| c | `/artists` 의 도메인 개체 수 | **≥ 1** |

**대조군 (넷 다)**

| 축 | 대조군 |
|---|---|
| 양성 | 같은 창의 `store.hubwang.com/products` — 창 #3·#4 에서 상품 링크 8개로 PASS 한 기준 |
| 음성 | `fan.hubwang.com/artists-does-not-exist-622` → **404** 여야 한다(200 이면 catch-all 이라 a·c 가 무의미) |
| 시점 | **창 전** 같은 URL 을 같은 술어로 — 🔴 기억된 값이 아니라 **그 자리에서** 잰다 |
| 교대 | 팬·스토어를 **B,A,B,A** 로 번갈아 — 순차 A/B 는 부팅 웜업 드리프트를 잰다 |

**🔴 FAIL 이면 — 다음 가설을 이 순서로, 그리고 창 안에서 고치지 않는다**

1. `DEMO_API_BASE` 값 자체가 틀렸다 → 컨트롤 플레인 URL 을 `terraform output api_base_url`
   와 문자 비교(🔴 끝 슬래시 포함 — `demo-backend.test.ts:142` 가 그 칸을 이미 안다)
2. env 는 있는데 **배포에 안 실렸다** → `build-info.json` 의 commit 이 env 투입 시각
   **이후**인지, 그리고 정적 자산의 `?dpl=` 배포 id 가 바뀌었는지(`610` § AC-4b 후속의 판별자)
3. 해석기가 컨트롤 플레인을 부르는데 실패한다 → 게이트웨이 CORS / `CACHE_TTL_MS` 잔여
4. 백엔드 쪽 → 🔵 창 #4 가 이미 무죄를 실측했다(`actuator/health` 200 · 시드 9건 성공 ·
   왕복 세션까지) ⇒ **가장 마지막에 의심한다**

## AC-2 — 결제 축. 🔵 **AC-1 이 먼저다**

`/api/payment-config` 가 **`{"demoPayment":true}`** 를 내는가.
🔴 **PASS 의 정의는 「값이 `true`」가 아니라 「런타임 값을 읽었다」**이다 —
`612` AC-0 ② 가 그 정의를 안 세웠으면 소유자 대기 항목이 FAIL 로 오기록됐을 자리다.
값이 `false` 면 **AC-2 는 PASS 이고 별 항목이 열린다**(env 는 있는데 배포에 안 실렸다).

🔵 그리고 백엔드 절반은 **극성이 반대**다 — 팬은 목이 기본이고
(`MockPaymentGatewayAdapter` = `@Profile("!portone")`) 실 PG 가 opt-in 이다.
불변식은 「프런트 플래그 ON ⟺ 프로파일에 `portone` **없음**」. (x2) 가 그 절반을 이미 잰다.

🔴 **AC-1 이 FAIL 이면 이 AC 는 ⚪ 미측정으로 닫는다** — 화면이 안 뜨면 결제 축은 잴
수 없고, 「측정 못 했다 + 왜」가 그때의 답이다.

## AC-3 — 🔴🔴 (x2) 메시지에서 **낡을 문장을 없앤다**

🔴 **처방은 「값을 갱신」이 아니다.** 갱신한 값도 다음 소유자 조작에서 낡고, 그때도
아무것도 안 문다. 남길 것은 **계약과 확인 방법**이고, 실측값을 굳이 적을 거면
**「언제 잰 값인지 + 재확인이 필요하다」를 문장 안에** 넣는다.

- (x2) 의 미집행 안내에서 **현재 상태를 단정하는 절**을 제거한다
- 🟢 **형태는 이미 정해져 있다 — 형제 칸 (x) 를 따른다.** (x) 는 같은 구조(누가·언제 /
  명령 / 기대값 / 원장)를 갖되 **실측값 절이 없고**, 왜 없는지를 주석
  (`:1189-1190`)으로 적어 뒀다. ⇒ 이 AC 는 새 형태를 발명하는 일이 아니라
  **형제로 되돌리는 일**이다.
- 🔴 그 주석(*"날짜 박힌 실측값을 여기에 넣지 않는다"*)을 **(x2) 쪽에도 복사한다** —
  없으면 다음 사람이 또 넣는다. 🔵 규칙이 한쪽에만 있는 것이 이 결함의 발생 조건이었다.
- 🔴 **가드를 새로 만들라는 것이 아니다** — 이 축을 저장소가 판정할 수 없다는 것은
  `618` 이(그리고 (x) 쪽은 `612` 가) 근거를 들어 **수용한 사실**이다. 이 AC 는
  *"가드를 만들어라"* 가 아니라 *"거짓을 말하지 마라"* 다. 그 구별을 구현 노트에 적는다.
- 🔵 **부수 확인**: 저장소의 다른 미집행 안내에도 같은 모양의 단정이 있는지 grep 한다
  (착수 시점 실측; 이 문서의 목록을 상속하지 마라).
  [[feedback_grep_the_siblings_before_fixing_it_yourself]]

## AC-4 — 원장 포인터를 **실재하게** 한다

(x2) 가 *"원장: `…/VERCEL.md`"* 라고 시키는데 그 파일에 두 키가 0건이다.

🟢 **선택지를 고를 일이 아니다 — 형제가 답을 갖고 있다.** (x) 가 지목하는
`…/apps/web-store/VERCEL.md` 에는 두 키가 **각각 5건** 있다. ⇒ **팬 원장에 같은 절을
만든다**(포인터를 다른 문서로 돌리는 안은 두 칸의 관행을 갈라놓으므로 버린다 — 그 이유를
구현 노트에 적는다).

🔴 **다만 store 원장을 그대로 베끼지 마라** — 팬은 **극성이 반대**다(목이 기본,
`portone` 이 opt-in). store 문구의 *"백엔드가 `demo-pg` 인 한"* 을 팬에 옮기면
**거짓이 된다**. `618` AC-3 이 (x2) 문구에서 정확히 그 함정을 피했고, 원장에서도 같이
피해야 한다. 🔵 담을 사실 최소 셋: ① 두 키의 **계약**(무엇의 입력인가) ② **확인 명령**
③ **왜 저장소가 판정할 수 없는가**.

🔴 그리고 **이 정합(가드 메시지가 지목한 경로에 그 항목이 실재하는가)을 가드로 지킬 수
있는지**를 이 AC 안에서 판정한다. 🔵 모집단이 2칸뿐이라 값이 낮을 수 있다 —
가능하면 칸을 추가하고, 안 만들기로 하면 **왜인지**를 적는다. 못 만드는 가드를 만든
척하는 것만 금지한다.

## AC-5 — `TASK-MONO-586` 을 닫는다 (또는 **왜 못 닫는지** 적는다)

- AC-1 PASS → 586 에 `## CORRECTION` 순수 추가(frozen) + **별 close chore PR** 로
  `review/ → done/`. 🔴 4-dim (d) 는 586 의 **`# Acceptance Criteria` 절을 열어서** 본다.
- AC-1 FAIL → 🔴 **586 을 옮기지 않는다.** `review/` 에 CORRECTION 만 추가하고,
  AC-1 이 지목한 다음 가설에 **행이 있는지** 확인한다(없으면 이 티켓이 만든다).

🔴🔴 **떠넘김 금지 — 누가 무엇을 닫는지 여기서 한 번만 정한다.** AC-1 의 측정은
`TASK-MONO-618` § AC-0 ① 과 **같은 사실**을 잰다. 그러나 618 의 목표는 억제이고
(AC-5 는 이미 PASS), AC-0 ① 은 그 티켓에서 **전제 칸**이다. ⇒ **판정의 소유자는 이
티켓이고**, 618 § AC-0 ① 은 이 티켓의 판정을 인용해 닫는다. 두 티켓이 서로를 기다리면
일이 조용히 사라진다.
[[feedback_a_completion_claim_with_no_gate_ships_false]]

## AC-6 — 이 티켓이 **안 고치는 것**을 적는다

최소한 아래를 이름으로 적는다:

- 억제 되돌리기(재굽기 1회) — 소유자 결정
- 팬 표면이 fail-closed 라 **밖에서 못 재는 구조** 자체 — 그것은 `TASK-FAN-FE-019` 가
  근거를 들어 고른 (A) 이고, 이 티켓은 그 선택을 재론하지 않는다
- `615` ⓐ · console 단계 3

---

# Related Specs

- [`docs/adr/ADR-MONO-067-demo-surfaces-served-from-vercel.md`](../../docs/adr/ADR-MONO-067-demo-surfaces-served-from-vercel.md) — 단계 4(fan). 집행 순서 `1 → 2 → 4 → 3`(2026-09-04 소유자 지정)
- [`docs/adr/ADR-MONO-068-where-the-demo-backend-resolver-lives.md`](../../docs/adr/ADR-MONO-068-where-the-demo-backend-resolver-lives.md) — § D2: `DEMO_API_BASE` 는 **해석기의 입력**이다(데모 IP 는 부팅마다 바뀌므로 `{DEMO_API_BASE}/status` 를 런타임 조회)
- [`projects/fan-platform/web/fan-platform-web/VERCEL.md`](../../projects/fan-platform/web/fan-platform-web/VERCEL.md) — AC-4 의 대상

# Related Contracts

없음 — API·이벤트 계약 변경이 아니다. 바뀌는 것은 배포 설정의 **문서화**와 가드 문구다.

# Edge Cases

- 🔴 **창 #5 가 안 열릴 수 있다** — 예산(600분)이 소진돼 가면 AC-1·AC-2 는 ⚪ 미측정으로
  닫고 AC-3·AC-4 만 랜딩한다. 🔵 AC-3·AC-4 는 **창이 필요 없다** ⇒ 두 묶음을 같은 PR 에
  넣지 마라. 창을 못 열어서 문구 수정까지 막히면 안 된다.
- 🔴 **AC-1 이 PASS 인데 AC-2 가 FAIL** — 두 env 가 같은 배포에 실렸으므로 이례적이다.
  그 경우 **배포가 두 번 있었는지**를 먼저 본다(`?dpl=` 배포 id).
- 🔵 **`586` 을 다른 세션이 이미 닫았을 수 있다** — AC-0 ④ 가 그것을 본다.
- 🔴 AC-3 을 고치는 PR 이 (x2) 의 **초록 경로 문구**를 건드리면 `check-message-backticks.sh`
  가 물 수 있다(`619` 가 만든 가드) — 백틱을 새로 넣지 마라.

# Failure Scenarios

| 시나리오 | 결과 | 처방 |
|---|---|---|
| 유효성 술어 없이 맨 `curl` 로 잰다 | 로그인 HTML 을 「도메인 개체 0」으로 읽어 **FAIL 을 재현했다고 오보** | AC-1 의 세 칸이 그것을 막는다. 🔴 술어가 통과 못 하면 판정을 적지 말고 **측정 실패**로 적는다 |
| (x2) 의 값만 갱신한다 | 다음 소유자 조작에서 **또 낡는다.** 아무것도 안 문다 | AC-3 이 「갱신 금지, 계약만 남겨라」로 명시 |
| 618 AC-0 ① 과 이 티켓이 서로를 기다린다 | 아무도 안 잰다 — **중복보다 공백이 조용하다** | AC-5 가 소유자를 한 번만 정한다 |
| AC-1 FAIL 인데 586 을 닫는다 | `done/` 은 frozen ⇒ 잔여를 아무도 다시 안 본다 | 4-dim (d) 의 처방 그대로 `review/` 에 두고 CORRECTION |
| 창 안에서 발견한 새 결함을 창 안에서 고친다 | 창이 무한히 커진다(615 창의 실패 모양) | 원인까지 귀속하고 **멈춘다.** 별 티켓 |

---

🔵 **분석=Opus 5 / 구현 권장=Opus** — 창 운영(라이브 호스트 · 순서 제약)과 **유효성 술어
설계**가 본체다. AC-3·AC-4 만 떼면 Sonnet 로 충분하다.

---

# 📏 AC-0 재측정 (2026-09-04 UTC, 착수 시점 · main `469cbbac3`)

🔴 이 문서 위쪽 숫자를 상속하지 않고 다시 쟀다. **넷 다 그대로였다** — 다른 PR 이 고친 것 없음.

| # | 무엇 | 실측 |
|---|---|---|
| ① | (x2) 의 날짜 박힌 단정 | 🔴 **있다** — `verify-demo-wrapper.sh:1310` |
| ② | 팬 원장의 두 키 | 🔴 **0건 · 0건** (헤더 7개 = 양성 대조군: 빈 파일도 오지목도 아니다) · 🔵 형제 store 원장은 **5건 · 5건**(헤더 18) |
| ③ | 데모 | `state=stopped` · **318/600** |
| ④ | `586` 큐 | `review/` — 그대로 |

⇒ AC-3·AC-4 는 **해당 없음이 아니다.** 착수한다.

---

# 🔴🔴 착수하자마자 이 티켓의 **전제 하나가 틀렸다는 것이 드러났다**

이 티켓의 § ② 는 그 처방이 *"형제 칸에 이미 적혀 있다"* 며 **(x) 의 주석**을 인용하고,
INDEX 행은 *"(x2) 는 바로 옆 형제가 명시한 규칙을 어겼다"* 라고 적었다. **틀렸다.**

그 규칙 주석은 **(x2) 에도 이미 있다** — 위반한 메시지 **여섯 줄 위**에
(`verify-demo-wrapper.sh:1299-1300`). 내가 형제 (x) 쪽만 좁은 `sed` 창으로 봤고,
「형제에는 있는데 여기엔 없다」로 유도했다.

🔴 **진단이 바뀐다. 그리고 더 나빠진다:**

| | 내가 적었던 것 | 실제 |
|---|---|---|
| 무엇이 없었나 | 규칙이 (x2) 에 **없었다** | 규칙이 **있었다** |
| 왜 어겼나 | 복사를 빠뜨려서 | 🔴🔴 **주석은 게이트가 아니라서** |
| 처방 | 주석을 복사한다 | 🔴 복사는 **이미 돼 있고 실패했다** ⇒ **실행되는 판정으로 바꾼다** |

🔵 **AC-3 이 «주석을 복사한다» 를 처방했다면 그 PR 은 아무것도 안 고치고 초록이었을 것이다.**
이미 있는 것을 또 넣는 일이니까. AC-0 이 그 자리를 잡았다.
[[feedback_verify_claims_against_the_source_itself]]
[[feedback_a_completion_claim_with_no_gate_ships_false]]

---

# ✅ 구현 (2026-09-04 UTC) — AC-3 · AC-4 · 그리고 새 칸 (z29)

## ✅ AC-3 — (x2) 에서 «현재 상태» 단정을 없앴다

지운 것(4줄) → 넣은 것(6줄). **형제 (x) 와 같은 끝맺음**으로 되돌렸다:

| 지운 것 | 왜 |
|---|---|
| *"🔴🔴 2026-09-04 실측 기준 이 값은 kanggle-fan 에 **없었다**"* | **쓰인 날 안에 거짓**이 됐고, 아무것도 그것을 빨갛게 못 만든다 |

| 넣은 것 | 왜 |
|---|---|
| *"최종 판정은 `/api/payment-config` 가 `{"demoPayment":true}` 를 내는 것이다"* | 형제 (x) 의 끝맺음과 같은 형태 — **계약**이지 상태가 아니다 |
| *"팬은 표면 전체가 로그인 뒤에 있다(middleware 가 fail-closed) … 그 응답은 «값이 false» 와 구별되지 않는다"* | 🔴 이 창구를 쓰려는 사람이 **먼저 알아야 할 것**. AC-1 이 유효성 술어를 요구하는 이유가 여기 있다 |
| *"🔵 **기전(현재 상태가 아니다)**: 이 값이 없으면 프런트가 'PortOne 키 미설정' 으로 …"* | 기전은 **조건문**이라 안 낡는다. 낡는 것은 「지금 없다」였다. 라벨을 붙여 다음 사람이 그 구별을 보게 했다 |

🔴 **`ok` 문구를 통째로 지우지 않았다** — `618` AC-3 이 *"아무것도 안 하고 문구만 지우는 것은
금지"* 라고 못박았고, 미집행 축은 **매 실행마다 이름이 찍혀야** 한다.

## ✅ AC-4 — 팬 원장을 실재하게 했다. 🔵 **고를 일이 아니었다**

선택지 **A**(원장에 절을 만든다). B(포인터 교체)는 버렸다 — 형제 (x) 가 같은 구조로
자기 원장을 채우고 있으므로, 포인터만 돌리면 **두 칸의 관행이 갈라진다.**

`projects/fan-platform/web/fan-platform-web/VERCEL.md` 에 `## 🔴 데모 env 두 개` 신설:
`DEMO_API_BASE`(해석기 입력 · 없으면 조용히 정상 · 끝 슬래시) ·
`DEMO_PAYMENT_MOCK`(결제 절반) · 🙋 사람이 하는 판정(누가·언제/명령/최종 판정) ·
✅ 소유자가 넣은 값(2026-09-04, **날짜와 함께**).

🔴🔴 **store 원장을 베끼지 않았다 — 극성이 반대다.** 코드에서 확인했다:

```
MockPaymentGatewayAdapter        @Profile("!portone")   ← 목이 기본
PaymentGatewayConfig (PortOne)   @Profile("portone")    ← 실 PG 는 opt-in
```

⇒ 불변식은 **프런트 ON ⟺ 프로파일에 `portone` 없음**. store 원장의 *"백엔드가 `demo-pg`
인 한"* 을 옮겼으면 **정반대를 단언**했을 것이다(`demo-pg` 는 팬에 없는 이름이다).

🔵 **실측값이 원장에 있는 것은 규칙 위반이 아니다** — 규칙은 *"실측값은 원장과 티켓이 든다"*
이다. 원장은 낡아도 **날짜가 낡았다고 말해 준다**. 스크립트 메시지는 그러지 못한다.

## 🟢 새 칸 (z29) — 그 주석을 **실행되는 판정**으로 만들었다

| | |
|---|---|
| 자리 | `verify-demo-wrapper.sh` **정적 구간**(LIVE 게이트 앞) — 🔴 (f) 벽 뒤에 두면 영원히 도달하지 않는다(`617` 이 두 창에서 실측) |
| 모집단 | **성질로 선택**: `ok`/`warn` 로 시작하고 *"판정할 수 없다"* 를 담은 **메시지 블록**. 위치·파일명으로 고르지 않았다 — 단계 3(console)이 셋째 축을 더할 때 그 성질이 사라지면 안 된다 |
| 술어 3칸 | (1) 날짜 박힌 값 **없음** (2) 원장을 **지목함** (3) 그 원장이 **실재하고 그 키를 담음** |
| 스캔 | `infra/demo` + `scripts` 의 `.sh` **68파일** → 메시지 블록 **149개** → 미집행 안내 **2건** (🔵 **awk 1회** — 첫 판은 서브셸 1,500개로 fork 고갈, 아래 참조) |

### 🔴🔴 하한은 «발견 수» 가 아니라 «추출기» 에 걸었다

미집행 축은 **줄어드는 것이 목표**다(축이 집행 가능해지면 안내가 사라진다).
발견 수에 하한을 두면 **개선이 실패가 된다** — 이 저장소가 다섯 번 밟은 함정이다.
대신 «메시지 블록이 20개 미만이면 FATAL» 로 잡는다. 0행은 「위반 없음」이 아니라
**「추출기가 깨졌다」**일 수 있고, 둘은 0행으로 구별되지 않는다.
[[feedback_a_non_vacuity_floor_under_a_draining_population]]

### 🔴🔴 예상 못 한 것 넷 — 셋은 **다른 것이 나를 잡았다**

**① 형제 가드 (z25) 가 내 새 코드를 물었다.**
`printf '%s' "$blk" | grep -q PAT` 는 `pipefail` 아래서 앞단이 SIGPIPE(141)로 죽어
**매치했는데 FAIL** 이 된다(그 칸의 실측: 300회 중 295회 오검출). 내 코드에 **4곳** 있었다.
⇒ 전부 `grepq` 로 교체. 🔵 **첫 전체 실행이 (z25)에서 멈춰 (z29)는 실행조차 안 됐다** —
그 rc=1 을 「내 칸이 실패했다」로 읽었으면 엉뚱한 곳을 팠을 것이다. 실패한 칸 이름을 읽어라.

**② 그 첫 실행의 rc 는 `echo` 뒤에 숨을 뻔했다.** 백그라운드 알림은
*"completed (exit code 0)"* 였는데 그것은 파이프라인 마지막 `tail` 의 코드였다.
로그를 열어야 `rc=1` 이 보였다. [[env_bash_pipe_masks_exit_code]]

**③ 첫 (z29) 실행이 거짓 양성 1건을 냈다 — 그리고 그것이 술어를 고쳤다.**
`scripts/check-cross-project-topic-relay.sh:147` 의
`fail "… 정적으로 **판정할 수 없다**."` 가 마커의 **부분문자열**에 걸렸다.
🔴 처방은 마커를 길게 하는 것이 **아니다**(다음 문구에서 또 걸린다) —
**미집행 안내는 정의상 실패하지 않는다**(실패할 수 있으면 그 축은 이미 집행되는 것이다).
⇒ 모집단을 `ok`/`warn` 시작 블록으로 좁히고, **그 거짓 양성을 self-test 의 음성 대조군으로
못박았다**. 🔵 모집단이 2가 아니라 3으로 나온 덕분에 찾았다 — 내가 안다고 생각한 수를
가드가 반증했다. [[feedback_a_guard_that_names_a_cause_needs_a_predicate_only_that_cause_trips]]

**④ 그리고 내 self-test 하나가 「실패할 수 없는 단언」이었다 — 이건 아무도 안 잡았다.**
음성 대조군을 `z29_e="$(z29_scan …)"` 로 받았는데 **명령치환은 서브셸**이라 `z29_scan` 이
올린 전역 `z29_notice` 가 부모로 안 돌아온다 ⇒ `[ "$z29_notice" = "0" ]` 는 블록이 실제로
선택됐어도 **항상 참**이다. 🔴 **이 티켓이 다루는 바로 그 부류(게이트 없는 단언)를
self-test 안에서 저지를 뻔했다.** 코드를 다시 읽다가 찾았고, 셋과 달리 **어떤 가드도
이것을 잡지 않았다** — 그래서 여기 적는다. ⇒ 출력을 파일로 받고, 그 앞에 **「집계기가
정말 세는가」 양성 대조군**(마커를 가진 `ok` 블록을 넣어 `z29_notice` 가 1로 오르는지)을
놓았다. 그 대조군이 없으면 음성 대조군은 또 공허해질 수 있다.
[[feedback_a_figure_nothing_can_fail_on_will_drift]]

### 🔴🔴 첫 구현은 **fork 고갈로 죽었다** — 그리고 그건 성능 문제가 아니다

첫 판은 블록마다 `printf '%s' "$blk" | grep …` 서브셸을 띄웠다. 68파일 × 블록 149개 ×
스캔 3회 ⇒ 프로세스 **1,500개 이상**. msys 에서 그대로 터졌다:

```
dofork: child -1 - forked process died unexpectedly, exit code 0xC0000142, errno 11
infra/demo/verify-demo-wrapper.sh: fork: retry: Resource temporarily unavailable
```

🔴 **이것을 「환경 탓」으로 넘기면 안 되는 이유**가 같은 저장소에 적혀 있다 —
`ci.yml` 의 이 잡 헤더: *"도는 데 오래 걸리는 가드는 언젠가 꺼진다. 그리고 꺼진 가드의
skip 은 초록을 보고한다"*(`MONO-360`). 이 잡의 `timeout-minutes` 는 **15** 다.
느린 가드는 «틀린 가드» 의 한 종류다.

⇒ 추출·판별을 **awk 한 번**에 넣고, 셸은 발견된 안내(오늘 2건)에 대해서만 파일을 읽는다.
프로세스 수천 → **몇 개**. 실측: **분 단위·중단** → **6초·rc=0**, 판정은 동일
(블록 149개 · 안내 2건).

🔵 그리고 이 재작성이 self-test 를 **더 강하게** 만들었다 — `z29_case` 가
기대 종류뿐 아니라 **기대 「안내 수」**도 받는다. 픽스처가 안내로 잡히지 않으면 그 아래
단언들이 전부 공허해지는데, 그 조건을 이제 명시적으로 단언한다.

### ✅ bite — 주입 5칸 + **자연 bite 4칸**

self-test(칸 안에서 매 실행): 🔵 **픽스처가 아니라 실블록을 복사해 변형한다.**

| 칸 | 기대 | 결과 |
|---|---|---|
| 대조군(실블록 그대로) | 위반 0 | ✅ |
| 날짜 주입 | `date` | ✅ |
| 없는 원장 | `ledger-missing` | ✅ |
| 빈 원장(파일 有·키 無) | `ledger-key` | ✅ |
| 🔴 `fail` 블록(마커 有) | **안 잡힘** | ✅ 오늘의 거짓 양성이 회귀 테스트가 됐다 |

🔴 주입 칸마다 **주입이 실제로 됐는지 먼저 단언**한다 — 안 그러면 「주입 안 된 초록」을
「가드가 안 문다」로 오독한다.

**자연 bite** — 오늘 아침 **실재했던** 두 결함을 되돌려 재현했다(별 하네스, 메모리 사본 복원):

| # | 되돌린 것 | rc | 종류 |
|---|---|---|---|
| 0 | 대조군(손 안 댄 트리) | **0** | — |
| A | (x2) 의 날짜 박힌 현재형 단정 | **1** | `date` |
| B | 팬 원장에서 두 키 절 제거 | **1** | `ledger-key` |
| 0b | 복원 대조군 | **0** | — |

🔵 **주입 bite 는 «내가 만든 결함» 을 잡는지 볼 뿐이다.** 이 넷은 **실제로 있었던 것**을
잡는지 본다. 🔴 하네스는 `ROOT` 를 인자로 받고 실재를 단언한다(파킹된 main 을 재고
「안 문다」로 읽는 사고 방지), 셀 본문을 복사하지 않고 **실파일에서 줄 범위로 떠오며**,
`grepq` 정의도 실파일에서 떠온다 — 복사하면 하네스가 재는 것이 「내가 적은 문자열」이 된다.

## 🔴 AC-1 · AC-2 — **남는다. 창이 필요하다**

데모 `stopped`(318/600). 팬 표면이 fail-closed 라 창 밖에서는 원리적으로 못 잰다.
🔵 티켓 Edge Cases 가 예고한 대로 **AC-3·AC-4 만 이 PR 로 랜딩**한다.

## 🔴 AC-5 — `586` 은 **안 옮긴다**

AC-1 이 아직 안 닫혔으므로 close 조건이 성립하지 않는다. 4-dim (d) 의 처방 그대로
`review/` 에 둔다.

## AC-6 — 이 구현이 **안 고치는 것**

- **값 자체**(Vercel env 가 옳은지) — 저장소 밖이고 `612`·`618` 이 판정 불가를 수용했다.
  (z29) 는 *"재라"* 가 아니라 *"거짓을 말하지 마라"* 다.
- **팬이 fail-closed 인 구조** — `TASK-FAN-FE-019` 가 근거를 들어 고른 (A) 다. 재론 안 한다.
- **다른 두 곳의 날짜 박힌 메시지** — `check-suppressed-containers.sh:146`(런타임이
  재검사하는 전제의 출처) · `demo-up.sh:658`(과거형 관측). 🔵 **둘 다 규칙이 허용하는
  형태**라 (z29) 모집단 밖이다. 축은 「날짜가 있나」가 아니라 **「시제 + 게이트 유무」**다.
- `615` ⓐ · console 단계 3 · 억제 되돌리기.

---

# ✅ AC-0 부록 — **기동 창 #5 매니페스트** (2026-09-05 UTC, 창을 열기 전에 작성·커밋)

`TASK-MONO-616` § AC-0 의 절차를 그대로 따른다: **큐 전수 스캔 → 매니페스트 작성 →
커밋 → 그 다음 부팅.** 🔴 켜고 나서 목록을 만들지 않는다.

🔵 **별 번들 티켓을 만들지 않은 이유**: 창 #2(`615`)·#4(`621`)가 전용 티켓을 쓴 것은
그 창들이 **재굽기를 묶었기** 때문이다. 창 #5 는 **재굽기가 없다**(팬 수정은 Vercel env
쪽이고, `622` 가 랜딩한 (z29)는 CI 가드라 부팅 경로에 없다) ⇒ 창의 산출물이 이 티켓의
AC-1·AC-2 뿐이므로 여기에 싣는다.

## AC-0 ① — 큐 전수 스캔 (**모집단부터 다시 셌다**)

`origin/main` `b51e76397` 기준 활성 큐 **7파일**(프로젝트 큐 24 디렉터리 전부 비어 있음):

| 티켓 | 큐 | 창 칸이 있는가 |
|---|---|---|
| `328` | ready | ❌ **DEFERRED 백로그** — 신호 관측 전 착수 금지 |
| `585` | ready | ❌ **BLOCKED** — `ADR-MONO-067` 단계 3(console)이고 집행 순서상 **마지막**. 이 창은 console 을 안 건드린다 |
| `587` | ready | ❌ **⏳ SCHEDULED** — `~2026-09-28` 주기 말. 오늘 읽으면 티켓이 금지한 «중간 표본 외삽» 이다 |
| `615` | in-progress | ❌ **아래 § 정정 참조** — ⓐ 는 닫혀 있다 |
| `618` | in-progress | ❌ **칸이 아니다** — AC-0 ① 은 이 티켓 AC-1 과 **같은 사실**을 재고, 판정 소유자는 `622` 다(§ AC-5). **되돌려 적기** 대상 |
| `622` | in-progress | 🟢 **칸 1 · 칸 2** |
| `586` | review | ❌ **칸이 아니다** — 칸 1 의 결과로 닫힌다. **되돌려 적기 + close chore** 대상 |

### 🔴🔴 정정 — `615` ⓐ 를 칸으로 세려다 티켓이 그것을 금지한 것을 발견했다

착수 시 나는 *"`615` ⓐ 는 부팅 횟수를 먹는 칸이라 같은 창에 태운다"* 로 계획했다.
**틀렸다.** `615` 본문 § *"⚪ ⓐ 닫는다 — 「측정 불가」가 이 칸의 답이다"*(2026-09-04)가
그 칸을 닫았고, 재개 조건 A/B/C 를 적으면서 이렇게 못박았다:

> 🔴 그 전에는 매니페스트에 적지 마라 — **닫힌 칸을 창마다 다시 세면 소화율 분모가
> 거짓이 된다.**

그리고 조건 **C**(부팅이 정상 대역 690~777s 를 크게 넘김)는 *"칸이 아니라 **부수 관측**"*
으로 남기라고 했다 — `/status` 폴링으로 **이미 재고 있으므로 추가 비용이 0** 이기 때문이다.

🔵 **조건 A(SSM)는 그 뒤 참이 됐고 창 #4 가 이미 그것으로 쟀다** — 결과는
*"봤는데 안 걸렸다"*(부팅 713s, 로그에 `매달림` 0건). 🔴 **표본을 하나 더 쌓아도 안 닫힌다** —
「안 걸렸다」는 「안 걸린다」가 아니고, 닫는 것은 **재현**뿐이다.
⇒ 창 #5 에서 ⓐ 는 **칸이 아니다.** 부수 관측으로만 남는다.
[[feedback_recount_population_dont_inherit_scope]]

## AC-0 ②③ — 매니페스트 본표 (**칸 · 술어 · 대조군 · PASS 의 정의**)

### 🔴🔴 칸 0 (판정이 아니라 **전제**) — 세션을 먼저 얻는다

팬 표면은 `middleware.ts` 가 fail-closed 라 **공개 경로가 `/login`·`/api/auth/*`·
`build-info.json` 뿐**이다. 세션 없이 부르면 **로그인 HTML 이 200 / 약 10,913 B** 로 오고,
추출기에 물리면 「도메인 개체 0」 — **창 #4 칸 5 의 FAIL 과 지문이 같다.**

```
유효성 술어 (셋 다 통과해야 칸 1·2 를 잰다)
  ① 왕복이 세션까지 간다            hop5 /api/auth/session 에 accountId
  ② 최종 URL 이 /login 이 아니다     curl -w '%{url_effective}'
  ③ 체인의 Location 에 from= 이 없다
```

🔴 실패하면 **칸 1·2 는 「FAIL」이 아니라 「측정 실패」**로 적는다. 둘은 다른 명제다.
🔴 왕복 curl 에 **`Accept: text/html` 필수**(정본 `infra/demo/seed/lib.sh:159`) ·
IdP 베이스는 **홉①의 `Location` 에서 파생**한다(박아 두지 마라 — 창 #3 의 교훈).

### 칸 1 — `622` AC-1 · 팬 표면이 **실데이터**를 낸다

| | |
|---|---|
| 술어 | (a) `/` 의 아티스트·멤버십 링크 **≥1** · (b) *"불러올 수 없습니다"/"데모 서버가 꺼져"* **0건** · (c) `/artists` 의 도메인 개체 **≥1** |
| 양성 대조군 | 같은 창의 `store.hubwang.com/products` — 창 #3·#4 에서 상품 링크 8개로 PASS 한 기준 |
| 음성 대조군 | `fan.hubwang.com/artists-does-not-exist-622` → **404** (200 이면 catch-all 이라 a·c 가 무의미) |
| 시점 대조군 | **창 전** 같은 URL 을 같은 술어로 — 🔴 기억된 값이 아니라 **그 자리에서** 잰다 |
| 교대 | 팬·스토어를 **B,A,B,A** 로 번갈아 (순차 A/B 는 부팅 웜업 드리프트를 잰다) |
| **PASS 의 정의** | 위 (a)(b)(c) 전부 + 음성 대조군 404. 🔴 **「200 이 왔다」는 PASS 가 아니다** |

### 칸 2 — `622` AC-2 · 결제 축

| | |
|---|---|
| 술어 | `/api/payment-config` 가 **런타임 값을 낸다** |
| **PASS 의 정의** | 🔴 **「값이 `true`」가 아니라 「런타임 값을 읽었다」**. `612` AC-0 ② 가 그 정의를 안 세웠으면 소유자 대기 항목이 FAIL 로 오기록됐을 자리다 |
| `false` 가 나오면 | **칸 2 는 PASS 이고 별 항목이 열린다**(env 는 있는데 배포에 안 실렸다) |
| 백엔드 절반 | 🔵 극성 반대 — `membership-service` 프로파일에 `portone` 이 **없어야** 한다. 칸 (x2) 가 그 절반을 이미 잰다 |

#### 🔵 창 #4 의 처분을 **완화한다** — 그리고 왜인지 적는다

`622` AC-2 는 *"AC-1 이 FAIL 이면 이 AC 는 ⚪ 미측정으로 닫는다 — 화면이 안 뜨면 결제
축은 잴 수 없다"* 라고 적었다. 그것은 **팬 표면이 통째로 죽은 줄 알았을 때** 쓴 문장이다.
지금은 막고 있던 것이 **인증 게이트**임을 알고, `/api/payment-config` 는 **route handler**
라 세션만 있으면 피드 렌더와 **독립적으로** 읽힌다.
⇒ **칸 0 이 통과하면 칸 1 의 결과와 무관하게 칸 2 를 잰다.** 🔴 칸 0 이 실패하면 둘 다
「측정 실패」다. [[feedback_blocked_decision_check_the_shared_premise]]

## AC-0 ④ — 🔴 순서와 상충

```
부팅 성공 판정  →  15s (해석기 CACHE_TTL_MS)  →  칸 0(세션)  →  칸 1  →  칸 2
```

- 🔴 **부팅 지문은 `[demo] ✔ HTTP 표면 1/1: console=307`** 이다 — `618` 이 `SURFACE_FLOOR`
  를 2→1 로 내렸으므로 **옛 지문 `2/2` 를 기다리면 창이 안 열린다**(창 #4 가 실측).
- 🔴 **`state=running` 은 부팅 완료가 아니다**(404→503→200). 위 지문까지 기다린다.
- 🔵 칸 1 과 칸 2 는 **서로 독립**이다(위 완화 참조) — 한쪽 FAIL 이 다른 쪽을 미측정으로
  만들지 않는다.
- 🔴 **상충 없음**: 이 창은 억제를 건드리지 않고 재굽기도 없다. 창 #4 의
  *"FAIL 이면 되돌릴 수 없다"* 같은 비가역 갈래가 **이번엔 없다.**

## AC-0 ⑤ — 🔴 창을 열기 전 **한 번 더 읽을 것** (착수 즉시, 추정 금지)

| # | 무엇 | 왜 |
|---|---|---|
| ① | `/status` 의 `used_minutes` | 🔴 이 문서의 **318 을 상속하지 마라** |
| ② | `state=stopped` **독립 확인 2종** | `/status` + `auth.hubwang.com/.well-known/openid-configuration` = **503** |
| ③ | **`POST /start`** 로 연다 | 🔴 `terraform apply` 는 「핀 갱신 + 기동」이라 창 #4 에서 매니페스트 커밋보다 부팅이 앞섰다. 이번엔 재굽기가 없으므로 apply 를 쓸 이유가 **없다** |
| ④ | 🔴🔴 **heartbeat 를 직접 친다** | `idle_minutes=20`. 론처 탭을 열어 둘 수 없으므로 `POST /heartbeat` 를 30s 주기로 도는 루프를 창 내내 돌린다 — 끊기면 자동 종료된다 |
| ⑤ | 팬 라이브 커밋 확인 | `build-info.json` 이 **env 투입 이후 배포**인지. 🔵 09-05 기준 `e07e9dad1`(=main tip) |

## 🔴 이 창이 **미리 적는 미측정** (창이 열리기 전에 확정)

| 항목 | 사유 |
|---|---|
| `verify --live` 의 **(f) 이후 전부** | 구조적 — (f) 가 `container_name` 고정 때문에 떠 있는 데모에서 못 돈다(`617` 이 두 창에서 실측) |
| `615` ⓐ (rc=124) | **닫힌 칸이다**(위 § 정정). 부수 관측으로만 — 부팅 소요가 690~777s 를 크게 넘으면 그때 사건 자체가 증거다 |
| `615` C3 술어 | 라이브가 필요 없다 — 창 밖 |
| console 단계 3 / `585` | 집행 순서상 마지막. 창 밖 |

## 🔵 부수 관측 (칸이 아니다 — 추가 비용 0)

1. **부팅 소요 시간** — `/status` 폴링으로 이미 잰다. 690~777s 대역 밖이면 `615` 조건 C.
2. **`[suppressed] OK`** — `617` 의 판정자가 부팅마다 찍는다. SSM 으로 로그를 읽는 김에 본다.
3. **`web.fan-platform` 404** — `618` AC-5 가 이미 닫았다. 재확인만.

🔴 **소화율 분모 = 2.** 미측정(사유 기재)은 닫은 칸에 넣지 않는다.

---

# 🟢 AC-1 · AC-2 — **기동 창 #5 (2026-09-05 UTC). 둘 다 PASS. 소화율 2/2**

인스턴스 `i-022ca131b94ac2d1a` · IP `16.184.16.218` · `DEMO_DOMAIN=16-184-16-218.sslip.io`
AMI 8차(재굽기 없음) · 팬 라이브 `e07e9dad1` (= main tip, PR `#3625` 가 트리거한 배포)

## 창 운영 실측

| | |
|---|---|
| 예산 | 창 전 **318/600** → 창 후 **332/600** ⇒ **14분** (창 #3 17분 · #4 20분 — **역대 최저**) |
| 부팅 | `04:20:01 → 04:30:19 UTC` = **618초** · `Result=success` · `ExecMainStatus=0` |
| 여는 법 | 🟢 **`POST /start`** — 창 #4 의 `terraform apply`(핀 갱신+기동)와 달리 재굽기가 없어 쓸 이유가 없었다 |
| 🔴 heartbeat | 론처 탭이 없으므로 **30초 주기 `POST /heartbeat` 루프를 직접 돌렸다** — 창 내내 **31비트**, 자동 종료 없음 |
| 🟢 순서 | **매니페스트가 `main` 에 랜딩한 뒤(`52e8dd15a`) 부팅했다** — 창 #4 가 어긴 616 § AC-0 의 순서가 이번엔 지켜졌다 |
| 창 닫기 | `POST /stop` → **독립 확인 2종**(`/status` + discovery **503**) |

## 판정표 — **2/2**

| 칸 | 소유 | 판정 | 한 줄 |
|---|---|---|---|
| **0** (전제) | 유효성 | 🟢 **PASS** | 왕복이 세션까지 — `accountId=…fa02` · `tenantId=fan-platform` · `roles=["FAN"]` |
| **1** | `622` AC-1 | 🟢 **PASS** | 팬 표면이 **실데이터**를 낸다 — 도메인 개체 `/` **4** · `/artists` **3** · 에러 문구 **0** |
| **2** | `622` AC-2 | 🟢 **PASS** | `/api/payment-config` = **`{"demoPayment":true}`** (200 / 20 B) |

## 칸 1 — 대조군 4축이 전부 섰다

| 축 | 값 |
|---|---|
| **시점** (창 전, 그 자리에서) | 팬 3건 전부 **측정 실패**(로그인 HTML 200/10,913 B) · store `/products` **에러 문구 2건 · 상품 0** |
| **창 안** | 팬 `/` **개체 4** · `/artists` **개체 3** · 에러 문구 **0** |
| **양성** (같은 창) | `store.hubwang.com/products` **상품 8 · 가격 8** — 교대 두 패스 모두 동일 |
| **음성** | `fan.hubwang.com/artists-does-not-exist-622` = **404**(실제 404 페이지) ⇒ 200 은 catch-all 이 아니다 |
| **교대** | **B,A,B,A** 로 쟀다 — store 가 두 패스에서 8·8 로 안정 ⇒ 부팅 웜업 드리프트가 결과를 만들지 않았다 |

🔵 창 #4 의 관측(*"피드를 불러올 수 없습니다 · 백엔드 게이트웨이가 응답하지 않을 수
있습니다 · 도메인 개체 0"*)이 **뒤집혔다.** 보이는 본문에 시드가 그대로 나온다:
*"ARTIST 2026. 9. 4. 프로듀싱 노트를 시작합니다 노아입니다…"* · 아티스트 디렉토리에
**노아 · 루미 · 세아**.

### 🔴 그런데 내 술어가 또 한 번 틀릴 뻔했다 — 이번엔 **개체의 «모양»**이었다

첫 판정에서 `/` 의 개체가 **0** 으로 나왔다. 본문에는 실제 포스트가 보이는데 링크 수는
0 이라 **칸 1 을 FAIL 로 적을 뻔했다.** 원인은 데이터가 아니라 정규식이다:

```
실제 href :  /posts/01a06a84-c198-…  ×4      ← 피드의 개체는 «포스트»다
내 술어   :  /artists/… · /membership/…      ← «아티스트·멤버십» 만 셌다
```

🔴 **넓히는 것이 골대 옮기기가 아님을 음성 기준선으로 증명했다** — `/posts/` 를 추가한
같은 술어를 창 전 파일들에 걸면 **여전히 전부 0** 이다:

| 파일 | `/posts/` | `/artists/<id>` | `/membership/<id>` | 합 |
|---|---:|---:|---:|---:|
| 창 전 `/`(로그인) | 0 | 0 | 0 | **0** |
| 창 전 `/artists`(로그인) | 0 | 0 | 0 | **0** |
| 창 전 404 대조군(로그인) | 0 | 0 | 0 | **0** |
| 창 안 404 대조군(진짜 404) | 0 | 0 | 0 | **0** |
| **창 안 `/`** | **4** | 0 | 0 | **4** |
| **창 안 `/artists`** | 0 | **3** | 0 | **3** |

⇒ 판별력을 유지한 채 고쳐졌다. 창 #3 의 가격 정규식 정정(`59,000 원` 의 공백)과 **같은
모양의 실패**다 — 세 창 연속으로 **내 추출기가 가장 유력한 결함**이었다.
[[feedback_my_verification_predicate_is_the_likeliest_defect]]

🔵 그리고 나머지 정규식은 **잘 설계돼 있었다** — `/membership`·`/artists`(id 없는 내비
링크)는 `<id>` 를 요구한 덕분에 정확히 제외됐다. 틀린 것은 «엄격함» 이 아니라 **모집단**이다.

## 칸 2 — 그리고 **음성 대조군이 이 창의 함정을 눈에 보이게 만들었다**

```
세션 有 : /api/payment-config → 200 /     20 B  {"demoPayment":true}   final=…/api/payment-config
세션 無 : /api/payment-config → 200 / 10,999 B  (로그인 HTML)          final=…/login?from=%2Fapi%2Fpayment-config
```

🔴🔴 **둘 다 `200` 이다.** `code == 200` 만 봤으면 로그인 페이지를 「PASS」로 읽었을 것이다.
칸 0 의 유효성 술어(최종 URL 이 `/login` 이 아닐 것)가 그 자리를 지켰다.
🔵 형제 축도 같다 — `store.hubwang.com/api/store-config` 도 세션 없이는 `/login` 으로 간다.

### 🟢 백엔드 절반까지 같이 쟀다 — **두 절반이 같이 측정된 건 이번이 처음이다**

```
fan-platform-membership  SPRING_PROFILES_ACTIVE = default     ← portone 없음 ⇒ 목
프런트                    demoPayment = true
```

⇒ 불변식 **「프런트 ON ⟺ 프로파일에 `portone` 없음」** 성립. 🔵 (x2) 가 *"프런트 절반은
저장소 밖이라 판정할 수 없다"* 라고 적은 그 절반을, 세션을 들고 밖에서 읽어 채웠다.
🔴 **그것이 (x2) 를 «집행 가능» 으로 만들지는 않는다** — 이 측정에는 **기동 창과 로그인
세션**이 필요하고, CI 에는 둘 다 없다. `618` 의 수용은 그대로 유효하다.

## 🔵 부수 관측 (칸이 아니다 — 추가 비용 0)

| # | 관측 | 값 |
|---|---|---|
| 1 | 부팅 소요 | **618초** · `매달림` 로그 **0건** ⇒ `615` 조건 C **미발화**(그 칸은 닫힌 채로 남는다) |
| 2 | `617` 판정자 | `[suppressed] OK — 억제 대상 2개(검사 2건) 모두 컨테이너가 존재하지 않습니다` |
| 3 | 억제 표면 | `web.fan-platform` **404** · `web.ecommerce` **404** · `console` **307** · 게이트웨이 `fan-platform` **401**(정상) |
| 4 | 팬 시드 | `생성 0 · 기존 9 · 실패 0` — 창 #4 의 데이터가 볼륨에 남아 있다 |

🔴 **관측 1 을 「615 가 고쳐졌다」로 읽지 마라.** 615 본문 그대로다: *"「안 걸렸다」는
「안 걸린다」가 아니다."* 표본이 하나 늘었을 뿐이고, 그 칸은 **재현으로만** 닫힌다.

## ⇒ AC-1 · AC-2 **닫힘**. 남은 것은 AC-5(586 close)뿐이다

---

# 🟢 AC-5 — **`TASK-MONO-586` 을 닫았다** (2026-09-05 UTC)

AC-1 이 PASS 했으므로 AC-5 가 적은 두 갈래 중 첫째를 탄다:

> AC-1 PASS → 586 에 `## CORRECTION` 순수 추가(frozen) + **별 close chore PR** 로
> `review/ → done/`. 🔴 4-dim (d) 는 586 의 `# Acceptance Criteria` 절을 **열어서** 본다.

| 단계 | 결과 |
|---|---|
| CORRECTION 순수 추가 | ✅ (`#3629`) |
| 별 close chore PR | ✅ `#3630` — 스쿼시 `0b1d4c494` |
| (d) — AC 절을 **열어서** | ✅ AC-0~AC-4 전부 닫힘. § *"남은 것 — AC-3"* 은 `#3489` **이전의 기록**이었다 |

## 🔴 (c) 에서 **required 가 비어 있던 시기를 그냥 통과시키지 않았다**

586 의 impl PR 둘(`#3479`·`#3489`)에 **FAILURE 2건**이 있었고, 당시는 `598` 이 required
넷을 등록하기 **전**이라 (c)가 «어떤 상태에서도 참» 이었다. 열어 보니 `ci.yml` 잡 실패
**0건**이고 실패는 **외부 배포 status** 둘뿐이며, 🔵 **같은 날 PR 6건 전수가 똑같이 그
둘만** 빨갰다 ⇒ 기간 한정 사건. 오늘 두 프로젝트 다 정상.

🔴 그 과정에서 **내 필터가 또 틀렸다** — Vercel 은 **status context** 라 `.name` 이 null
이고 `.context` 에 이름이 있다. `.name` 만 보면 이웃 PR 의 **같은 실패가 0건으로** 보이고,
그러면 «이 PR 만 빨갰다» 로 오귀속했을 것이다. 대조군을 세는 술어부터 검증해야 한다.
[[feedback_comparing_two_extracts_measures_the_extractors]]

## ⇒ 이 티켓의 남은 AC

**없다.** AC-0(+부록) · AC-1 · AC-2 · AC-3 · AC-4 · AC-5 · AC-6 전부 닫혔다.

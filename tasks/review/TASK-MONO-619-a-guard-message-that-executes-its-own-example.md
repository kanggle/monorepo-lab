# Task ID

TASK-MONO-619

# Title

🔴🔴 **가드의 실패 메시지가 자기 예시를 «실행»한다.** 문구는 조용히 사라지고,
운이 나쁘면 **그 명령이 진짜로 돈다** — `TASK-MONO-617` 에서 `docker rm` 이 실제로 실행됐다.

# Status

review

# Owner

monorepo

# Task Tags

- demo
- guard
- hygiene

---

# 🔎 어디서 왔나 — **bite 가 잡았다. 초록 경로에서는 안 보인다**

`TASK-MONO-617` 이 새 가드를 만들고 bite 를 돌리자 이런 출력이 나왔다:

```
check-suppressed-containers.sh: line 169: profiles:: command not found
[suppressed] 🔴 억제 선언은 렌더에서만 효력이 있습니다.  로 가려진 서비스는 compose 에게
docker: 'docker rm' requires at least 1 argument          ← 🔴 실제로 실행됐다
```

`warn "… \`profiles:\` …"` 의 백틱이 **명령 치환**으로 해석된 것이다.

🔴 **이 결함은 FAIL 경로에서만 드러난다.** 통과 메시지에는 백틱이 없으므로, 가드를 초록으로만
돌려 본 사람은 **평생 못 본다.** 그리고 물어야 할 순간에는 **자기 처방을 지운 채** 문다.
[[feedback_why_a_guard_does_not_bite]]

🔵 617 은 자기 신규 코드를 고쳤고, **기존 파일의 같은 결함은 이 티켓으로 넘겼다** —
각 메시지가 어느 가드의 처방인지 따로 읽어야 하기 때문이다(617 § AC-6).

---

# 📐 재현 (2026-09-03 실측)

```bash
$ bash -c 'fail(){ echo "MSG: $*"; }; fail "(z16) … (`z16_` 토큰이 들어 있습니다)"'
bash: line 1: z16_: command not found
MSG: (z16) … ( 토큰이 들어 있습니다)          ← 🔴 «`z16_`» 이 통째로 사라졌다

$ bash -c 'fail(){ echo "MSG: $*"; }; fail "(z16) … (\`z16_\` 토큰이 들어 있습니다)"'
MSG: (z16) … (`z16_` 토큰이 들어 있습니다)    ← 이스케이프하면 온전하다
```

## 🔴 피해는 두 층이고, 둘째가 더 나쁘다

| 층 | 무엇 |
|---|---|
| ① | **문구가 조용히 사라진다** — 처방에서 «무엇을 고쳐라» 가 빠진다 |
| ② | **그 토큰이 PATH 에 있으면 진짜로 실행된다** — 617 의 `` `docker rm -f` `` 가 그랬다(인자가 없어 실패했을 뿐이다) |

---

# 📋 모집단 (2026-09-03 실측, main `0fea801a8`)

`infra/demo/*.sh` + `scripts/*.sh` **51개** 스캔 → 메시지 문자열 안 미이스케이프 백틱
**9건, 전부 `infra/demo/verify-demo-wrapper.sh`**:

| # | 줄 | 가드 칸 | **실행될 것** |
|---|---|---|---|
| 1 | 1289 | (y) | `dbexec --why "<사유>"` |
| 2 | 2157 | (z12) | `http://iam.${DEMO_DOMAIN}` |
| 3 | 3058 | (z16) | `z16_` |
| 4 | 3146 | (z17) | `.env` |
| 5 | 3148 | (z17) | `pg_hba` · `127.0.0.1/32 trust` |
| 6 | 3582 | (z23) | `.env` |
| 7 | 3583 | (z23) | `.env` |
| 8 | 3584 | (z23) | `?error=Configuration` |
| 9 | 3789 | (z24) | 🔴 **`demo-boot.sh <name>`** — `<name>` 은 **입력 리다이렉션**이다 |

🔵 **오늘 이 아홉 중 PATH 에 있는 것은 없다** ⇒ 지금은 층 ①(문구 소실)만 실현된다.
🔴 **그것을 「무해하다」로 읽지 마라** — 617 의 `docker` 는 PATH 에 있었고 **실행됐다.**
위험은 문자열의 내용에 달려 있고, 그 내용은 **다음 사람이 메시지를 고칠 때 바뀐다.**

## 🔴 그리고 탐지기가 **거짓 양성 1건**을 냈다 — 술어의 요구사항이 여기서 나온다

`scripts/check-required-check-names.sh:125` 도 걸렸지만 **결함이 아니다**:

```bash
grep -qF '`INDEX queue drift`' "$t/platform/git-workflow-policy.md" || …
```

백틱이 **작은따옴표 안**이라 실행되지 않는다. ⇒ **술어는 인용 문맥을 봐야 한다.**
「메시지 호출 줄에 미이스케이프 백틱이 있다」로만 재면 **멀쩡한 코드를 고발한다.**
[[feedback_my_verification_predicate_is_the_likeliest_defect]]

---

# Goal

가드의 실패 메시지가 **자기 예시를 실행하지 않게** 하고, 같은 결함이 **다시 들어오지 못하게** 한다.

# Scope

- **포함**: 위 9건 수정 · 재발 방지 가드 1칸 · 그 가드의 bite
- **제외**: 메시지 **문구**의 개선(내용은 그대로 두고 이스케이프만)
- **제외**: `infra/demo` · `scripts` 밖의 셸 스크립트 🔵 AC-0 이 모집단을 다시 센다

---

# Acceptance Criteria

## AC-0 — 착수 시 **다시 센다** (verify-then-act)

1. 위 9건이 **여전히 9건인지** 다시 스캔한다. 🔴 그 사이 누가 메시지를 고치면 줄 번호가
   밀린다 — 줄 번호가 아니라 **내용**으로 찾아라.
2. 🔴 **모집단을 상속하지 마라** — `infra/demo/*.sh` · `scripts/*.sh` 밖에도 같은 모양이
   있는지 본다(`.github/workflows/*.yml` 안의 인라인 `run:` 도 셸이다).
3. 🔵 **오늘 PATH 에 있는 토큰이 있는지** 확인한다. 있으면 그 줄이 **우선순위 1**이다.

## AC-1 — 9건을 고친다. **문구는 바꾸지 않는다**

백틱을 `\`` 로 이스케이프한다. 🔴 백틱을 **지우거나** 다른 기호로 바꾸지 마라 —
이 저장소의 메시지는 코드 조각을 백틱으로 감싸는 관례를 따르고, 그것이 처방의 가독성이다.

## AC-2 — 🔴🔴 **재발 방지 가드**. 술어는 **인용 문맥**을 본다

- 무는 조건: **큰따옴표 문자열 안**의 미이스케이프 백틱.
- 🔴 **작은따옴표 안은 안 문다** — 위 거짓 양성이 그 요구사항이다.
- 🔴 **주석은 안 문다** — 이 저장소의 헤더 주석은 백틱을 정상적으로 쓴다.
- 🔴 **가드 자신의 설명 문구에 걸리지 않게** 하라. `TASK-MONO-604` 가 밟은 함정이다
  (판별자가 자기 문서에 매치한다). [[feedback_a_discriminator_can_match_its_own_documentation]]
- 🔵 자리는 **정적 구간**이다(파일을 읽을 뿐 데모가 필요 없다).
  🔴 `verify --live` 의 **(f) 뒤는 금지** — 도달하지 않는다(`TASK-MONO-617` 이 실측).

## AC-3 — bite. **주입을 먼저 단언한다**

- **주입**: 임의의 메시지 줄에 미이스케이프 백틱을 하나 넣는다 → 가드가 **rc≠0**,
  메시지가 **파일·줄·실행될 토큰**을 이름으로 댄다.
- **대조군 ①**: 주입 전 **rc=0**.
- **대조군 ②**: 🔵 **작은따옴표 안의 백틱**을 넣는다 → **rc=0 이어야 한다**(거짓 양성 금지).
- **대조군 ③**: 🔵 **주석 안의 백틱** → **rc=0 이어야 한다**.
- 🔴 셋을 다 넣어야 한다. ①만 보면 「늘 무는 가드」와 구별되지 않고, ②③이 없으면
  **멀쩡한 코드를 고발하는 가드**가 된다 — 그런 가드는 꺼진다.

## AC-4 — 🔴 **모집단 하한**

스캔한 파일 수가 **0이면 FAIL** 이다. glob 이 안 맞아 0개를 스캔하고 「위반 0건」으로
통과하는 것이 이 부류 가드의 표준 고장이다. [[env_empty_detector_output_is_not_absence]]
🔵 위반 수의 하한은 **두지 마라** — 이 모집단은 **비어 가는 것이 목표**다(고치면 0이 된다).
[[feedback_a_non_vacuity_floor_under_a_draining_population]]

## AC-5 — 이 구현이 **안 고치는 것**을 적는다

- 메시지 문구 자체 · `infra/demo`·`scripts` 밖 · `.github/workflows` 인라인 셸(AC-0 이 세고
  범위를 정한다)

---

# Related Specs

- `TASK-MONO-617` § AC-3 bite · § AC-6 — 이 결함의 발견 경위와 범위 밖 선언
- `infra/demo/verify-demo-wrapper.sh` — 9건 전부가 여기 있다
- `TASK-MONO-604` — 판별자가 자기 설명에 매치한 선례((z19) bite 첫 시도)

# Related Contracts

없음.

# Edge Cases

- 🔴 **`$'\n'"…"` 이어붙임 줄** — 이 저장소의 `fail` 호출은 여러 줄로 이어진다. 그 줄들은
  `fail` 로 시작하지 않으므로 「호출 줄」만 보는 술어는 **9건 중 8건을 놓친다**(실측: 9건 중
  8건이 이 모양이다).
- 🔴 한 줄에 백틱이 **홀수 개**면 짝이 안 맞아 다음 줄까지 먹는다 — 그 경우 증상이
  「문구 소실」이 아니라 **구문 오류**이거나 더 넓은 실행이다.
- 🔵 `bash -n` 은 이 결함을 **못 잡는다** — 문법적으로 완전히 정상이기 때문이다.
  그래서 별도 가드가 필요하다.
- 🔴 이스케이프한 뒤 **메시지를 실제로 출력시켜 눈으로 확인**하라. `bash -n` 통과는 증거가
  아니다(위 줄과 같은 이유).

# Failure Scenarios

| 상황 | 잘못된 처리 | 옳은 처리 |
|---|---|---|
| 「지금 PATH 에 없으니 무해」 | 안 고치고 닫는다 | 🔴 617 의 `docker` 는 **있었고 실행됐다**. 내용은 다음 사람이 바꾼다 |
| 백틱을 **지워서** 해결 | 처방의 코드 조각이 평문이 되어 읽기 어려워진다 | 🔴 **이스케이프**하라 |
| 작은따옴표 안까지 고발 | 멀쩡한 코드가 빨개지고 가드가 꺼진다 | 🔴 술어가 **인용 문맥**을 본다 (대조군 ②) |
| 호출 줄만 스캔 | 🔴 **9건 중 8건을 놓친다**(`$'\n'"…"` 이어붙임) | 이어붙임 줄도 모집단에 넣어라 |
| 스캔 0파일로 통과 | 「위반 없음」이 아니라 **안 봤다** | 🔴 **파일 수 하한** |
| 위반 수에 하한을 건다 | 다 고치면 성공이 고장난다 | 🔵 이 모집단은 **비어 가는 것이 목표**다 |

---

---

# ✅ 구현 결과 (2026-09-03 UTC)

## AC-0 — 재측정. **모집단을 넓혔더니 안 늘었다**

| 축 | 값 |
|---|---|
| ① 9건이 여전한가 | ✅ **9건** (main `12a3d0f7d`) |
| ② 모집단을 넓힌다 | `git ls-files '*.sh'` **118개** + `.github/workflows/*.yml` **6개** = **124파일** ⇒ 여전히 **9건, 전부 `verify-demo-wrapper.sh`** |
| ③ PATH 에 있는 토큰 | **없음** (8종: `.env`·`127.0.0.1/32`·`?error=Configuration`·`dbexec`·`demo-boot.sh`·`http://iam.…`·`pg_hba`·`z16_`) |

🔵 **그리고 인용 인식 술어가 거짓 양성을 걸렀다** — 티켓이 예고한 그대로
`scripts/check-required-check-names.sh:125` 가 초판 탐지기에서는 걸렸으나(10건),
문맥을 보는 술어에서는 **안 걸린다**(9건). 그 1건의 차이가 이 가드의 설계 요구였다.

## AC-1 — 9건 이스케이프. **문구는 한 글자도 안 바꿨다**

`verify-demo-wrapper.sh` 9줄 · 백틱 **20개**. 🔵 **증명**: diff 의 `-`/`+` 9쌍을 떠서
`+` 쪽에서 `\` + 백틱 → 백틱 치환을 하면 `-` 쪽과 **완전히 일치**한다
(*"비교 9쌍 · 이스케이프 외 변경 0건"*).

🔵 **그리고 메시지를 실제로 출력시켜 봤다** — Edge Cases 가 요구한 그것이다
(`bash -n` 통과는 증거가 아니다):

```
OUT: (z16) 집힌 구간이 **(z16) 자기 본문**입니다(`z16_` 토큰이 들어 있습니다) — 판정 불가입니다.
… 이 중 하나라도 참이면 **옛 자격으로 초기화된 DB 가 새 `.env` 를 만나는 상태**가 …
… 비-루프백에서 재야 합니다 — `pg_hba` 의 `127.0.0.1/32 trust` 는 …
```

백틱이 **온전히 남았고** `command not found` 가 **한 건도 없다**(고치기 전에는 그 토큰이
실행되고 문구가 사라졌다).

## AC-2 — 가드 `scripts/check-message-backticks.sh`

선례는 `check-public-domains.sh` 다 — **스크립트 + `--self-test` + 전용 CI 잡** 3스텝.

**술어는 문자를 훑으며 인용 상태를 추적한다**:
- 이스케이프(`\x`)는 통째로 건너뛴다
- **큰따옴표 밖의 `'` 만** 홑따옴표 상태를 뒤집는다(큰따옴표 안의 `'` 는 리터럴이다 —
  `warn "it's fine"` 을 오독하지 않기 위해서)
- **홑따옴표 밖**의 이스케이프 안 된 백틱만 위반

🔴 **한계를 헤더에 적었다**: 여러 줄에 걸친 인용은 추적하지 않는다. 이 저장소의 메시지는
줄 단위로 닫히고, 그 이상을 재려면 셸 파서가 필요하다 — **못 재는 것을 재는 척하지 않는다.**

🔵 **모집단 하한**: 스캔 파일 **0개면 FAIL**. glob 이 안 맞아 0개를 훑고 「위반 없음」으로
통과하는 것이 이 부류의 표준 고장이다.
🔵 **위반 수에는 하한을 두지 않았다** — 이 모집단은 **비어 가는 것이 목표**다.

## AC-3 — self-test 4칸. 🔴🔴 **그리고 그것이 내 상수 오류를 잡았다**

| 칸 | 주입 | 기대 | 실측 |
|---|---|---|---|
| (1) 대조군 | 손 안 댄 실파일 | 0 | ✅ 0 |
| (2) bite | 메시지 줄에 미이스케이프 백틱 | 1 | ✅ 1 |
| (3) 거짓 양성 | **홑따옴표 안**의 백틱 | **0** | ✅ 0 |
| (4) 거짓 양성 | **주석 안**의 백틱 | **0** | ✅ 0 |

🔵 픽스처가 아니라 **실파일(`verify-demo-wrapper.sh`)을 복사해 변형**한다.

### 🔴🔴 첫 self-test 는 (2)에서 «안 물었다» 로 실패했고, 원인은 술어가 아니라 **상수**였다

```
bash:  printf '%c' 96  →  9      ← 🔴 %c 는 인자 «문자열의 첫 글자» 를 찍는다
bash:  printf '\140'   →  `
awk :  sprintf("%c",96) → `      ← awk 는 코드포인트다. 둘이 다르다
```

주입에도 그 주입의 **단언에도 같은 틀린 상수**를 썼기 때문에 `grep -qF` 단언이 **통과했다** —
즉 「주입은 됐는데 가드가 안 문다」로 보였고, 실제로는 **백틱을 한 번도 주입하지 않았다.**
🔴 **주입 단언이 주입과 같은 상수를 공유하면 그 단언은 자기 자신을 확인할 뿐이다.**
⇒ self-test 에 **칸 (0) 상수 단언**을 넣었다(`BT` 가 awk 의 것과 같은가).
[[feedback_assert_the_injection_before_reading_the_bite]]
[[feedback_my_verification_predicate_is_the_likeliest_defect]]

## CI 배선 — **네 자리를 자체검사했다**

| 자리 | 확인 |
|---|---|
| `paths-filter` 항목 `message-backticks` | ✅ 순수 positive (`**/*.sh` · `.github/workflows/*.yml` · 가드 자신) |
| `changes` 잡의 **`outputs:` 선언** | ✅ 🔴 이게 없으면 `needs.changes.outputs.…` 가 빈 값이 되어 **`if` 가 항상 거짓** — 배선이 없는데 초록으로 보인다 |
| 잡 정의 `message-backticks` | ✅ 3스텝(`bash -n` · `--self-test` · 본 실행) |
| 잡의 `if` 가 그 출력을 읽는가 | ✅ |

🔴 **`code-changed` 와 AND 하지 않았다** — 도착 경로는 **아무 셸 편집**이고 그중 일부는
`code-changed` 의 모집단 밖이다. AND 로 묶으면 그 편집이 이 잡을 **안 깨운다.**

## AC-5 — 이 구현이 **안 고치는 것**

- 메시지 **문구** 자체(이스케이프만 했다)
- 여러 줄에 걸친 인용(술어의 명시된 한계)
- 🔵 `.github/workflows` 인라인 `run:` 은 **모집단에 넣었고** 오늘 위반 0건이다

## 검증

| 게이트 | 결과 |
|---|---|
| `bash -n` (가드 · verify-wrapper) | **rc=0** |
| `check-message-backticks.sh` 본 실행 | **rc=0** — 124파일, 위반 **0건** |
| `--self-test` | **rc=0** — 대조군 0 / 주입 1 / 홑따옴표 0 / 주석 0 |
| diff 순수성 (문구 불변) | **9쌍 · 이스케이프 외 변경 0건** |
| 메시지 실제 출력 | 백틱 보존 · `command not found` **0건** |
| `ci.yml` YAML 파싱 | **OK · 잡 52개** |
| CI 배선 4자리 자체검사 | **4/4** |
| `check-index-queue-drift` / `task-id-collision` / `walkthrough-ledger` | **rc=0** ×3 |

{
  "name": "federation-hardening-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "TASK-MONO-139 — Phase 8 Federation Hardening cross-product Playwright e2e cohort (ADR-MONO-018 D1/D2/D3).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^20.10.0",
    "kafkajs": "^2.2.4",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  }
}

# -----------------------------------------------------------------------------
# erp-platform notification-service — TASK-ERP-BE-011.
#
# Approval-notification fan-out (first increment). Consumes approval-service's 4
# transition topics, persists in-app notifications + a Category C delivery
# record (IN_APP synchronous DELIVERED), serves a recipient-scoped read-only
# inbox. Terminal consumer: no outbox / no event publication (E5-adjacent).
# -----------------------------------------------------------------------------
spring:
  application:
    name: erp-platform-notification-service

  datasource:
    # Shares the erp_db MySQL instance with masterdata / approval (separate
    # tables: notification / notification_delivery / processed_events).
    url: jdbc:mysql://${MYSQL_HOST:mysql}:${MYSQL_PORT:3306}/${MYSQL_DB:erp_db}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: ${MYSQL_USER:erp}
    password: ${MYSQL_PASSWORD:erp}
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2

  jpa:
    hibernate:
      # Flyway owns the schema — Hibernate validates only.
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        jdbc.time_zone: UTC
    open-in-view: false

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  # Kafka consumer — 4 approval topics. Manual ACK; values read as String and
  # parsed with the service ObjectMapper (blueprint: read-model-service).
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP:kafka:9092}
    consumer:
      group-id: erp-notification-v1
      auto-offset-reset: earliest
      enable-auto-commit: false
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
    listener:
      ack-mode: manual

  # Standard OAuth2 Resource Server backed by GAP's JWKS (RS256), mirrors
  # masterdata / read-model / approval (same erp tenant gate).
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${OIDC_ISSUER_URL:http://iam.local}
          jwk-set-uri: ${OIDC_JWK_SET_URI:${JWT_JWKS_URI:${OIDC_ISSUER_URL:http://iam.local}/oauth2/jwks}}

server:
  port: ${SERVER_PORT:8080}
  forward-headers-strategy: framework
  error:
    include-message: always
    include-binding-errors: always
    include-stacktrace: never

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      probes:
        enabled: true
      show-details: when-authorized
  metrics:
    tags:
      application: ${spring.application.name}
  tracing:
    sampling:
      probability: 1.0
  otlp:
    tracing:
      endpoint: ${OTLP_ENDPOINT:http://localhost:4318/v1/traces}

logging:
  level:
    root: INFO
    com.example.erp.notification: INFO
    org.hibernate.SQL: WARN
  pattern:
    level: "%5p [traceId=%X{traceId:-} requestId=%X{requestId:-} tenantId=%X{tenantId:-} userId=%X{userId:-}]"

erpplatform:
  oauth2:
    # Keep byte-identical to masterdata / read-model / approval allowed-issuers
    # (cross-service issuer drift guard — SAS issuer + legacy
    # iam D2-b).
    allowed-issuers: ${OIDC_ALLOWED_ISSUERS:${OIDC_ISSUER_URL:http://iam.local},iam}
    required-tenant-id: ${OIDC_REQUIRED_TENANT_ID:erp}

  # External-channel delivery (TASK-ERP-BE-020, v2.0). Default OFF = production
  # net-zero: no external delivery is created (enabled=false) and the retry
  # scheduler bean is absent (retry.enabled=false) → v1 IN_APP-only behaviour.
  # mode=slack swaps the no-op stub for the real SlackWebhookChannelAdapter.
  notification:
    external:
      enabled: ${NOTIFICATION_EXTERNAL_ENABLED:false}
      mode: ${NOTIFICATION_EXTERNAL_MODE:noop}
      slack:
        webhook-url: ${NOTIFICATION_SLACK_WEBHOOK_URL:}
        connect-timeout-ms: ${NOTIFICATION_SLACK_CONNECT_TIMEOUT_MS:2000}
        read-timeout-ms: ${NOTIFICATION_SLACK_READ_TIMEOUT_MS:5000}
      retry:
        enabled: ${NOTIFICATION_RETRY_ENABLED:false}
        poll-interval-ms: ${NOTIFICATION_RETRY_POLL_INTERVAL_MS:5000}
        batch-size: ${NOTIFICATION_RETRY_BATCH_SIZE:50}
        initial-backoff-ms: ${NOTIFICATION_RETRY_INITIAL_BACKOFF_MS:1000}
        max-backoff-ms: ${NOTIFICATION_RETRY_MAX_BACKOFF_MS:60000}

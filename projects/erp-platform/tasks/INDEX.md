# Tasks Index — erp-platform

This document defines task lifecycle, naming, and move rules for the **erp-platform** project. Repo-root [tasks/INDEX.md](../../../tasks/INDEX.md) covers monorepo-level (cross-project) tasks; this file covers erp-platform-internal tasks only.

---

# Lifecycle

backlog → ready → in-progress → review → done → archive

Only tasks in `ready/` may be implemented.

---

# Task Types

- `TASK-ERP-BE-XXX`: backend (Spring Boot service implementations)
- `TASK-ERP-INT-XXX`: cross-service integration / E2E (Testcontainers · Docker compose)
- `TASK-ERP-FE-XXX`: frontend — declared for future use, erp v1 is backend-only (platform console renders erp per ADR-MONO-013 §3.3)

---

# Move Rules

## backlog → ready
Allowed only when:
- related specs exist (`specs/services/<service>/architecture.md`, `specs/contracts/...`)
- related contracts are identified
- acceptance criteria are clear
- task template is complete

## ready → in-progress
Allowed only when implementation starts.

## in-progress → review
Allowed only when:
- implementation is complete
- tests are added
- contract / spec updates are completed if required

## review → done
Allowed only after review approval.

### Review Rules
- Tasks in `review/` must not be re-implemented directly.
- If a review reveals a bug or missing requirement, create a new fix task in `ready/` referencing the original task.
- Fix tasks must include the original task ID in their Goal section (e.g. "Fix issue found in TASK-ERP-BE-001").
- Do not modify a task file after it moves to `review/` or `done/`.

### PR Separation Rule (lifecycle ↔ PR boundary)

Each lifecycle transition lands in its own PR. **Never bundle task spec authoring with implementation in the same PR.**

| Stage | Recommended PR shape |
|---|---|
| `(writing) → ready` | **spec PR** — adds the task file to `ready/` + updates this `INDEX.md` ready list. No implementation code. |
| `ready → in-progress → review` | **impl PR** — moves the task file through `in-progress/` to `review/` and lands the implementation. Lifecycle moves and impl commits should be separate commits but live in one PR. |
| `review → done` | **chore PR** — moves merged task file(s) from `review/` to `done/` + updates the `INDEX.md` done list. May batch multiple merged tasks. |

The repo-root [tasks/INDEX.md](../../../tasks/INDEX.md) is the authoritative definition. This summary applies the same rule at the project level.

## done → archive
Allowed when no further active change is expected.

---

# Rule

Tasks must not be implemented from `backlog/`, `in-progress/`, `review/`, `done/`, or `archive/`.

---

# Task List

## backlog

(empty)

## ready

(empty)

## in-progress

(empty)

## review

(empty)

## done

- `TASK-ERP-BE-018-read-model-delegation-scope-projection.md` — **DONE (2026-06-06, 3-dim verified — impl PR #1143 squash `059ccbd6`)**. read-model-service **v1.3** — delegation scope projection (BE-017 의 read-model 짝; **producer-only forward gap 종결**). BE-017 이 `erp.approval.delegated.v1` payload 에 `scope`(GLOBAL|REQUEST)/`scopeRequestId` 를 가산했으나 아무도 project 안 하던 갭을 기존 `DelegationFactEventConsumer`(byte-unchanged 구독) 가 `delegation_fact_proj` 에 투영하도록 확장 → "전체 위임 vs 특정 건 한정" 이 통합조회 노출. **핵심 설계: scope=grant-time 불변 메타데이터** → `validFrom`/`validTo` 와 **동형** 처리 — `DelegationFactProjection.applyGrant` 가 scope/scopeRequestId 를 **sticky-terminal status 가드 밖에서 무조건 (re)stamp**(out-of-order revoke-before-grant 행[scope=null]을 후속 grant 가 status REVOKED 유지하며 scope 채움), `applyRevoke` 는 미restate(보존), `ofRevoked` 는 null(미조작 E5; revoke payload scope 없음). **평행 additive**: `DelegationFactCommand`+`DelegationEnvelopeToCommandMapper`(granted-only 추출)+`ApplyDelegationFactUseCase`+`DelegationFactProjJpaEntity`+repo 매핑+`DelegationFactResponse`(NON_NULL: scope=revoke-only 시 ABSENT, scopeRequestId=GLOBAL 시 ABSENT). **V4 마이그레이션**: scope/scope_request_id **NULLABLE**(approval-service 의 NOT NULL 과 달리 — revoke-only 행 scope 미상) + `ck_delegation_fact_proj_scope CHECK (scope IS NULL OR scope IN ('GLOBAL','REQUEST'))` — **§16 값집합 DB 고정(NULL 허용), coherence CHECK 없음**(producer[BE-017]가 pairing 보장, read-model 은 검증된 데이터만 투영). ACTIVE/REVOKED/sticky-terminal/dedupe(T8)/org_scope/activeAt **byte-unchanged**. terminal consumer(no outbox/re-emit grep 0, 신규 토픽 0). 분석=Opus 4.8 / 구현=backend-engineer(Opus dispatch)+dispatcher 독립 재검증. spec/contract: read-model architecture.md §v1.3 amendment + read-model-subscriptions.md §Delegation + read-model-api.md §Delegation facts + data model. **3-dim**: (a) MERGED+`059ccbd6`; (b) origin/main tip=local HEAD 일치; (c) pre-merge failing required=0(**Integration(erp-platform) Testcontainers pass 2m17s** = `DelegationFactProjectionIntegrationTest` scope 투영[REQUEST→both, GLOBAL→scope_request_id null] + out-of-order revoke→grant scope fill[status REVOKED 유지] + CHECK 거부 + 기존 approval-fact/delegation ACTIVE·REVOKED·org_scope 회귀; Build&Test GREEN). dispatcher 독립 재검증: **applyGrant scope assignment 이 `if(!isTerminal())` 가드 밖**(L121-122 vs L126) + applyRevoke 미변경 + mapper granted-only + V4 nullable+CHECK 직접 확인 + `:read-model-service:check --rerun-tasks` BUILD SUCCESSFUL. **메타: ① BE-017 producer-only forward 필드 소비로 forward gap 종결; ② scope=grant-time 불변→validFrom/validTo 와 동형(applyGrant unconditional outside status guard) 로 out-of-order 에서 scope 미손실; ③ §16 CHECK + §17 IT 단언(`DataAccessException`+제약명) 선반영 → BE-017 의 CI #1 RED 재발 없이 1회 GREEN(교훈 즉시 작동); ④ read-model CHECK=값집합만 NULL 허용(revoke-only 미상), coherence 는 producer 책임.** out-of-scope=콘솔 scope 카드(PC-FE-056)/scope 필터/per-route. 사용자 "BE-018 read-model scope projection" 선택. [[project_monorepo_template_strategy]] [[feedback_spring_boot_diagnostic_patterns]] [[project_platform_console_adr_013]]

- `TASK-ERP-BE-017-per-request-delegation-scoping.md` — **DONE (2026-06-06, 3-dim verified — impl PR #1141 squash `dbb1cd97`)**. approval-service **v2.3** — per-request delegation scoping (architecture.md §v2.2-deferred sub-part #1 의 **per-request 절반**; per-route 는 route-template identity 부재로 계속 deferred). 오늘 `DelegationGrant` 은 **blanket A→D**(대결자가 A 가 결재자인 모든 단계 대행); 이 증분이 **scope 차원** 추가 — `scope=REQUEST` + `scopeRequestId=<approvalRequestId>` 면 그 **한 건만** 대행, `scope=GLOBAL`(기본)은 v2.1 동작 **byte-unchanged**. **domain**: `DelegationScope`(GLOBAL|REQUEST) enum + `DelegationGrant.scope`/`scopeRequestId` + **cross-field coherence 불변식**(REQUEST⟺scopeRequestId non-blank, GLOBAL⟺null; 위반→`DelegationInvalidException` 422) + 순수 `coversRequest(approvalRequestId)`(GLOBAL=any, REQUEST=id 일치). **resolution(핵심 리스크)**: `DelegationResolver.resolve` 가 `approvalRequestId` 추가 → active grant 가 `isActiveAt(now) && coversRequest(rid)` **둘 다** 만족해야 인가; **in-domain `coversRequest` 재검증이 SQL scope predicate 위 authoritative filter**(defense-in-depth, R2 누수 방지); `findActiveGrant` 가 scope 로 narrow(GLOBAL 결정적+limit1); 호출부 `request.getId()` threading; SoD(delegate≠submitter)/withdraw 기안자-only 불변. **api/event**: create body `scope?`/`scopeRequestId?`(unknown→400 VALIDATION_ERROR, coherence 위반→422) + view + `erp.approval.delegated.v1` payload 에 `scope`(항상)/`scopeRequestId`(REQUEST만, NON_NULL); **`publishRevoked`+4 transition 토픽 byte-unchanged**(revoke 는 scope 미restate, validity window 처럼 sticky). **V4 마이그레이션**: scope(backfill GLOBAL)+scope_request_id + **CHECK 2개**(`ck_delegation_grant_scope` 값집합 + `ck_delegation_grant_scope_req` coherence) — **§16 선반영**. **producer-only forward interface**: read-model scope projection(**BE-018**)+콘솔 카드(**PC-FE-056**) 가 새 payload 필드 후속 소비, 그때까지 read-model/notification unknown-field tolerant. 분석=Opus 4.8 / 구현=backend-engineer(Opus dispatch)+dispatcher 독립 재검증. spec/contract: approval-service architecture.md §v2.3 + approval-api.md §v2.3 + erp-approval-events.md §v2.3. **3-dim**: (a) MERGED+`dbb1cd97`; (b) origin/main tip=local HEAD 일치; (c) pre-merge failing required=0(**Integration(erp-platform) Testcontainers pass 1m38s** = `DelegationIntegrationTest` 34 IT: REQUEST grant R1 인가/R2 403 + GLOBAL 회귀 + 422/400 coherence + view scope + CHECK 거부 + 기존 위임/approval 회귀; Build&Test GREEN). dispatcher 독립 재검증: `coversRequest`/resolver filter/V4 CHECK 2개/payload scope/controller 400 경로 직접 + `:approval-service:check` BUILD SUCCESSFUL. **⚠️ CI #1: 34 중 33 PASS, AC-7 CHECK 테스트만 단언 타입 불일치 RED→fix(`5fbfa615`)** — CHECK 제약 자체는 정상(error 3819 로 incoherent row 거부 증명), 단 MySQL CHECK 위반(SQLState HY000)을 Spring 이 `DataIntegrityViolationException` 아닌 `UncategorizedSQLException` 으로 분류 → 공통 부모 `DataAccessException`+제약명 메시지 단언으로 정정. **메타: ① blanket A→D 의 정제(GLOBAL=special case byte-unchanged); ② resolution in-domain 재검증=SQL 위 defense-in-depth; ③ V4 CHECK 선반영(§16) 으로 제품/마이그레이션 무수정·테스트 단언만 1줄 정정; ④ §14 재확인 — Docker-free `:check` GREEN 이나 Testcontainers IT 가 CHECK 거부의 Spring 예외분류까지 권위 적발(MySQL HY000 CHECK→Uncategorized, DataIntegrityViolation 아님).** out-of-scope=read-model scope projection(BE-018)/콘솔 카드(PC-FE-056)/per-route/auto-absence/transitive. 사용자 "per-request 위임 scoping" 선택. [[project_monorepo_template_strategy]] [[feedback_spring_boot_diagnostic_patterns]] [[project_platform_console_adr_013]]

- `TASK-ERP-BE-016-notification-revoke-consumer.md` — **DONE (2026-06-06, 3-dim verified — impl PR #1139 squash `23e389b1`)**. notification-service delegation-revoked 알림 — `erp.approval.delegation.revoked.v1` 소비(ERP-BE-014 grant 알림의 **revoke 짝**; delegation 알림 grant+revoke 대칭 완성). ERP-BE-015 가 발행하게 된 revoke 토픽을 notification-service 가 **6번째 consumer** 로 구독 → 권한 잃는 **delegate**(`delegateId`)에게 "위임 권한 회수됨" in-app 알림. **평행 additive**(기존 5 consumer[4 transition + delegated] + `ApprovalEvent`/`DelegationEvent` **byte-unchanged**): revoke payload 는 validity window 없음 → 별 `DelegationRevokedEvent`(grantId/delegatorId/delegateId/reason?) + `NotifyOnDelegationRevokedCommand` + `RecipientResolver`/`NotificationFactory` overload + `NotificationType.DELEGATION_REVOKED` + `EnvelopeToCommandMapper.mapDelegationRevoked` + `ApprovalEventConsumerSupport.processDelegationRevoked` + `ApprovalDelegationRevokedConsumer`(@RetryableTopic 3-retry+DLT, group `erp-notification-v1`, dedupe T8) + UseCase 공유 `dispatch` helper. **V3 마이그레이션이 `ck_notification_type` CHECK allow-list 에 `DELEGATION_REVOKED` 선반영**(§16 교훈: enum STRING 길이 OK ≠ 마이그레이션 불필요; source_type DELEGATION 은 V2 에서 이미 허용). terminal consumer(no outbox/re-emit, grep 0). 직접 구현(ERP-BE-014 mirror). spec/contract: notification-subscriptions.md(6번째 topic + revoke 노트 supersede) + architecture.md v1.2 amendment + erp-approval-events.md(notification revoke 소비 추가). **3-dim**: (a) MERGED+`23e389b1`; (b) origin/main tip 일치(제목 `--subject`); (c) pre-merge failing required=0(**Integration(erp-platform) Testcontainers pass 1m41s** = `NotificationEndToEndIntegrationTest` revoked→inbox(recipient=delegate, type=DELEGATION_REVOKED) + dedupe + V3 CHECK + 기존 5 consumer 회귀; Build&Test GREEN). 직접 재검증: `:notification-service:check` BUILD SUCCESSFUL(unit incl. revoked resolver/factory/usecase/mapper) · E5 grep 0 · V3 CHECK. **메타: §16 CHECK allow-list 선반영으로 ERP-BE-014 의 CI-RED 재발 없이 1회 GREEN(교훈 즉시 작동).** **= delegation 도메인 완전 종결**(BE-013 grant/revoke producer + 014 grant 알림 + 015 read-model + 016 revoke 알림 / 콘솔 PC-FE-054 관리 + 055 현황 — grant/revoke × producer/notification/read-model/console 전 표면). out-of-scope=외부 채널/digest. 사용자 "revoke 알림 leg" 선택. [[project_monorepo_template_strategy]] [[feedback_spring_boot_diagnostic_patterns]] [[project_platform_console_adr_013]]

- `TASK-ERP-BE-015-delegation-fact-projection.md` — **DONE (2026-06-06, 3-dim verified — impl PR #1135 squash `8fc218d9`)**. delegation-fact read model + revoke 이벤트 **2-leg atomic** — "누가 누구를 대신할 수 있는가" 정확한(ACTIVE/REVOKED) 통합조회. ERP-BE-013 의 revoke 가 audit-only(이벤트 무발행)라 read model 이 취소를 못 보던 갭을 producer+consumer 1 PR 로 해소. **Leg A(approval-service additive)**: 신규 토픽 `erp.approval.delegation.revoked.v1`(`ApprovalEventPublisher.publishRevoked`; payload grantId/delegatorId/delegateId/reason?/tenantId/occurredAt/actor) + `revokeDelegation()` 이 **ACTIVE→REVOKED 전이 시에만** `@Transactional` outbox 경계 안 발행(audit+event atomic, 멱등 re-revoke 무발행); grant-create+4 transition+resolver/SoD byte-unchanged. **Leg B(read-model, ERP-BE-010 청사진 답습)**: `DelegationFactEventConsumer`(delegated+revoked, group `erp-read-model-v1`, @RetryableTopic+DLT+manual ACK+dedupe T8) → `DelegationFactProjection`(**sticky-terminal REVOKED**=applyGrant 이 `!isTerminal()`일 때만 ACTIVE, out-of-order revoke-before-grant, last-event-wins) → Apply/Query usecase(org_scope=delegator 부서 subtree[employee_proj→department_proj 해소, fail-closed, `["*"]`/null=net-zero], activeAt 필터) → `DelegationFactController`(GET /delegations + /{grantId}, 404 MASTERDATA_NOT_FOUND, READ gate) + V3 `delegation_fact_proj`+**`CHECK(status IN('ACTIVE','REVOKED'))`** + repo/JPA. terminal consumer(no outbox/re-emit, grep 0). 분석=Opus 4.8 / 구현=backend-engineer(Opus dispatch)+dispatcher 독립 재검증. spec/contract: erp-approval-events.md v2.2(revoke 토픽)+read-model-subscriptions.md v1.2+read-model-api.md delegation+read-model architecture amendment+approval architecture 노트. **3-dim**: (a) MERGED `8fc218d9`; (b) origin/main tip 일치(`--subject`); (c) pre-merge failing required=0(**Integration(erp-platform) Testcontainers pass 2m34s** = `DelegationFactProjectionIntegrationTest` grant→ACTIVE/revoke→REVOKED/dedupe/org_scope list+404/activeAt + 기존 approval-fact·masterdata 회귀; Build&Test GREEN). dispatcher 독립 재검증: `:read-model-service:check`+`:approval-service:check` BUILD SUCCESSFUL(124+103 unit/slice) · E5 grep 0 · V3 CHECK · revoke emit-on-transition+atomic/projection sticky-terminal 코드 직접 확인. **메타: 읽기모델 정확성=revoke 이벤트 필수(create-only 투영은 수동취소 미반영)→producer+consumer 2-leg 1 atomic PR(모노레포 장점); V3 CHECK allow-list 선반영(ERP-BE-014 교훈, CI 1회 GREEN).** out-of-scope=approval v2.2(per-request/auto-absence/transitive)/revoke notification leg/콘솔 read 카드. 사용자 "delegated-event 소비자 고리"→2-leg 선택. [[project_monorepo_template_strategy]] [[feedback_spring_boot_diagnostic_patterns]]

- `TASK-ERP-BE-014-notification-delegation-consumer.md` — **DONE (2026-06-06, 3-dim verified — impl PR #1133 squash `41c2f88c`)**. notification-service delegation-granted 알림 — `erp.approval.delegated.v1` 소비(**approval→notification delegated leg 완결**). ERP-BE-013 이 발행하나 소비자 0이던 producer-only 토픽을 notification-service 가 **5번째 consumer** 로 구독 → 위임받은 결재자(`delegateId`)에게 "결재 권한 위임됨" in-app 알림. **평행 additive 경로**(기존 4 transition consumer + `ApprovalEvent`/`NotifyOnApprovalCommand`/`map(...)`/`handle(NotifyOnApprovalCommand)` **byte-unchanged**): 신규 `DelegationEvent`(render) + `NotifyOnDelegationCommand` + `RecipientResolver`/`NotificationFactory` overload + `SourceRef.DELEGATION` + `NotificationType.DELEGATION_GRANTED` + `EnvelopeToCommandMapper.mapDelegation` + `ApprovalDelegatedConsumer`(@RetryableTopic 3-retry+DLT, group `erp-notification-v1`, dedupe T8) + UseCase 공유 `dispatch` helper. terminal consumer(no outbox/no re-emit, grep 0). spec/contract: architecture.md §v1.1 amendment + notification-subscriptions.md(5번째 topic) + erp-approval-events.md 소비자 노트. body=delegatorId/validFrom/validTo(없으면 "무기한")/reason(present 시). revoke=이벤트 무발행→무알림. **3-dim**: (a) MERGED+`41c2f88c`; (b) origin/main tip 일치(제목 `--subject`); (c) pre-merge failing required=0(**Integration(erp-platform) Testcontainers pass 1m45s** = `NotificationEndToEndIntegrationTest` delegation→inbox + dedupe + 무기한 렌더 + 기존 4 transition 회귀; Build&Test GREEN). **⚠️ CI #1 RED→fix**: `@Enumerated(STRING)` 컬럼 길이는 맞았으나 **V1 `ck_notification_type`/`ck_notification_source_type` CHECK allow-list 가 enum 값 집합 고정** → 새 `DELEGATION`/`DELEGATION_GRANTED` insert 거부(`ck_notification_source_type violated`) → **V2 마이그레이션으로 두 CHECK 확장**(`7bbfc290`). **메타: `@Enumerated(STRING)` 컬럼 길이 OK ≠ 마이그레이션 불필요 — enum 컬럼 CHECK 제약은 별도 확장 필요; Docker-free `:check` slice 는 DB CHECK 미실행이라 미포착, Testcontainers IT 권위 게이트가 적발** ([[feedback_spring_boot_diagnostic_patterns]] §14/§15 류). **out-of-scope**: read-model delegation projection(ERP-BE-015 후보) / revoke 알림 / 외부 채널. 분석=Opus 4.8 / 구현=Opus(직접). 사용자 "delegated-event 소비자 고리" 선택. [[project_monorepo_template_strategy]] [[project_platform_console_adr_013]]

- `TASK-ERP-BE-013-approval-delegation.md` — **DONE (2026-06-05, 3-dim verified — impl PR #1123 squash `650ae89c`)**. approval-service **v2.1** — 대결/위임 (delegation) (ADR-016 §D3 approval forward-declaration **3번째·마지막 증분**; **단계/대결/위임 완성**: BE-009 단일단계 → BE-012 다단계+IN_REVIEW → BE-013 위임). spec PR #1122 `4ab5c377`(architecture.md §v2.1 amendment + approval-api.md + erp-approval-events.md + error-handling.md `## Approval Workflow` 섹션[`DELEGATION_INVALID`/`DELEGATION_NOT_FOUND` + 기존 approval 코드 consolidate, latent gap 정합] + ADR-016 §D3 amendment). 분석=Opus 4.8 / 구현=backend-engineer(Opus dispatch)+dispatcher 독립 재검증. **domain/delegation**: `DelegationGrant` aggregate(A→D windowed, ACTIVE|REVOKED; create=자기위임/역기간→`DelegationInvalidException`; revoke 멱등; `isActiveAt(now)`) + `DelegationResolver`(direct=actor==approver / active grant A→actor+window / **fail-closed notAuthorized**) + `DelegationResolution` VO. **전이 resolution**: approve/reject 가 `resolveActingApprover`→onBehalfOf; grant 없으면 `APPROVAL_NOT_AUTHORIZED_APPROVER`; **SoD=대결자(D)가 요청 submitter면 거부**(위임 통한 자기결재 차단); aggregate `(actorId,route,onBehalfOf,now)` overload 가 effective approver==현 단계 검증(T4/SoD 보존); audit row 에 onBehalfOf. **events**: 신규 producer-only 토픽 `erp.approval.delegated.v1`(grant create; aggregateType=DelegationGrant); approved/rejected 에 `actingForApproverId` 가산(NON_NULL); 기존 4 토픽 불변; revoke 감사만. **REST**: POST /delegations + /{id}/revoke + GET. **persistence**: `delegation_grant` 테이블+3 인덱스 + Flyway V3(순수-additive + nullable `approval_action.on_behalf_of`). withdraw 기안자-only 불변. **소비자(read-model BE-010/notification BE-011) 무변경**(신규 토픽 미구독+가산 필드 ignore). **3-dim**: (a) MERGED+`650ae89c`; (b) origin/main tip=local HEAD 일치(제목 `--subject`); (c) pre-merge failing required=0(**Integration(erp-platform) Testcontainers pass 1m38s** = `DelegationIntegrationTest` 10/10 + `ApprovalLifecycleIntegrationTest` 19/19 BE-009/BE-012 회귀; Build&Test GREEN). dispatcher 독립 재검증: scope=approval-service only(26파일, 계약/ADR/소비자 drift 0) · DelegationResolver(fail-closed)/전이 SoD(delegate==submitter 거부)/grant create 검증/V3 순수-additive 코드 직접 확인 · `:check --rerun-tasks` 101 unit 재실행 GREEN. 신규 토픽=producer-only forward interface. **v2.2-deferred**: per-request/per-route 위임·자동부재 감지·transitive chaining·delegated 이벤트 소비자·콘솔 위임 UI. **= ADR-016 §D3 approval forward-declaration 3-증분 완성.** 사용자 "approval v2.1 대결/위임(#1)" 선택. [[project_monorepo_template_strategy]]

- `TASK-ERP-BE-012-approval-multi-stage-in-review.md` — **DONE (2026-06-05, 3-dim verified — impl PR #1120 squash `b749c1f9`)**. approval-service **v2.0** — 다단계 결재선(1~N) + `IN_REVIEW` (ADR-016 §D3 approval forward-declaration **2번째 증분**; ERP-BE-009 단일단계의 multi-stage/IN_REVIEW 절반 실현). spec PR #1119 `72c96e7c`(architecture.md §v2.0 amendment + approval-api.md + erp-approval-events.md additive + ADR-016 §D3 amendment). 분석=Opus 4.8 / 구현=backend-engineer(Opus dispatch)+dispatcher 독립 재검증. **domain**: `ApprovalStatus` +`IN_REVIEW`(non-terminal) + `ApprovalStateMachine.next(current,command,isLastStage)` 확장(approve from SUBMITTED|IN_REVIEW → last?APPROVED:IN_REVIEW; 2-arg overload=v1.0; finalized-guard 우선) + `ApprovalRoute` 순서있는 1~N 단계(`singleStage`/`multiStage`, empty/blank/self/`duplicate_stage_approver` validity) + `ApprovalRequest` **per-stage authz**(현 단계 결재자만=순서강제, 이전/이후 단계→`NOT_AUTHORIZED_APPROVER`) + `currentStageIndex`/`totalStages` 전진/finalize. **persistence**: 신규 `approval_route_stage` 테이블+entity/repo + **Flyway V2**(`current_stage_index`/`total_stages` 컬럼, `IN_REVIEW` CHECK 추가, `approval_action.stage`, **기존 요청 1-stage backfill** `CONCAT('ars-',id)`). **application**: create=`approverIds`∨legacy `approverId`; **중간 단계 approve=audit row만 outbox 무발화, 최종 approve=`approved.v1` 발화**(terminal-once 보존); payload `currentStage`/`totalStages` 가산; inbox=현 단계 결재자 status∈{SUBMITTED,IN_REVIEW}. **presentation**: detail/summary `stages`/`currentStage`(NON_NULL)/`totalStages`, status +IN_REVIEW, history `stage`. **하위호환**: 1단계=v1.0(IN_REVIEW 미경유), 콘솔 PC-FE-051 `approverId` 동작. **consumer 무변경**: read-model(BE-010)/notification(BE-011) 가산 필드 ignore + terminal-once 유지. **3-dim**: (a) MERGED+`b749c1f9`; (b) origin/main tip=local HEAD 일치(제목 `--subject`); (c) pre-merge failing required=0(**Integration(erp-platform) Testcontainers pass 1m26s** = 19 IT 실 MySQL+Kafka+WireMock: 2단계 happy[IN_REVIEW 중간·0 중간이벤트·audit·2 stage row] + 1단계 회귀 + per-stage authz 403 + reject/withdraw from IN_REVIEW + finalized + 멱등 + cross-tenant 403 + duplicate 422; Build&Test + 전 Integration GREEN). dispatcher 독립 재검증: scope=approval-service only(계약/ADR/task/타 서비스 drift 0, 25 파일) · 상태기계/이벤트 발화 지점(중간 무발화·최종 approved)/route validity/V2 backfill 코드 직접 확인 · `:check --rerun-tasks` 72 unit 재실행 GREEN. 신규 에러코드 0·신규 토픽 0. **대결/위임(delegation)=v2.1 분리**(작동하는 multi-stage base 위 독립 권한위양 모델). 사용자 "approval v2(#1)" 선택. [[project_monorepo_template_strategy]]

- `TASK-ERP-BE-011-notification-service-bootstrap-first-increment.md` — **DONE (2026-06-05, 3-dim verified — impl PR #1114 squash `7fd1b0eb`)**. notification-service 부트스트랩 first increment — 결재 알림 fan-out(ERP v2 pillar; **approval→notification forward-consumer leg 완결**). spec PR #1113 `6a9b5e5b`(architecture+notification-subscriptions+notification-api+ADR-016 §D3 amendment+NOTIFICATION_NOT_FOUND 등록). 분석=Opus 4.8 / 구현=backend-engineer(Opus dispatch)+dispatcher 독립 재검증. dual-type Hexagonal `apps/notification-service/`(`com.example.erp.notification`, event-consumer + rest-api): `NotificationServiceApplication`(OutboxAutoConfiguration/OutboxMetrics exclude — terminal consumer) + 4 approval `@KafkaListener`(`erp.approval.*.v1`, group `erp-notification-v1`, @RetryableTopic 3-retry+DLT, manual ACK, invalid→즉시 DLT, `processed_events` dedupe T8) + `RecipientResolver`(pure: SUBMITTED·WITHDRAWN→approverId / APPROVED·REJECTED→submitterId) + `Notification` 영속 + `NotificationChannelPort`(IN_APP adapter=영속 즉시 DELIVERED attempt=1 / stub external no-op, green-wash 회피) + `NotificationDelivery`(Category C 구조) + inbox REST(`GET /api/erp/notifications` recipient-scoped + `GET /{id}` 타인→404 `NOTIFICATION_NOT_FOUND` + `POST /{id}/read` 멱등) + READ gate(entitlement-trust) + RS256 + V1 Flyway. deploy 배선 atomic(settings.gradle/ci.yml/docker-compose `/api/erp/notifications`). terminal consumer(publish/outbox 0 grep). **3-dim**: (a) MERGED+`7fd1b0eb`; (b) origin/main tip 일치; (c) pre-merge failing required=0(**Integration(erp-platform) Testcontainers pass 1m53s** = `NotificationEndToEndIntegrationTest` 7/7 실 Kafka+MySQL: 4 recipient 매핑·dedupe·invalid DLT·in-app·inbox scope·mark-read 멱등; Build&Test GREEN). **⚠️ CI IT가 로컬 `:check`(Docker-free)가 못 잡은 정밀도 버그 적발→fix**: `inboxIsRecipientScopedAndMarkReadIdempotent` 가 mark-read 응답 readAt 을 in-memory nanosecond(1st) vs MySQL DATETIME(6) microsecond(re-mark DB re-read) 로 비교해 FAIL(`757127542Z`≠`757128Z`). 멱등성은 정상; fix=`ClockPort.now()` `truncatedTo(MICROS)`(모든 영속 타임스탬프 in-memory==DB 결정성, `7bb8646d`). dispatcher 독립: scope=notification-service+배선 only · `:check` 48 unit/slice GREEN · recipient(withdrawn→approver)·publish grep 0 코드 확인. **메타: nanosecond `Instant.now`↔MySQL DATETIME(6) microsecond round-trip 정밀도 불일치 — clock 을 micros truncate 로 결정화; Docker-free slice 는 DB round-trip 부재라 미포착, Testcontainers IT 가 권위 게이트(§14 재확인).** v2-deferred=외부 채널(Slack/SMTP)+Category C retry scheduler/masterdata·permission·delegation 통지/콘솔 bell. 사용자 "notification-service 부트스트랩" 선택.

- `TASK-ERP-BE-010-read-model-approval-fact-projection.md` — **DONE (2026-06-05, 3-dim verified — impl PR #1111 squash `d1a9d83f`)**. read-model approval-fact 투영 — **approval→read-model 이벤트 루프 완결**(통합조회 v2 first increment; ERP-BE-009 outbox 의 소비자 0 고리 닫음, masterdata→read-model[ERP-BE-007] 미러). spec PR #1110 `f1a8d4aa`(architecture amendment+subscriptions+read-model-api+ADR-016 §D3 amendment). 분석=Opus 4.8 / 구현=backend-engineer(Opus dispatch)+dispatcher 독립 재검증. read-model-service 확장: `ApprovalEventConsumer`(4 `@KafkaListener` `erp.approval.*.v1`, group `erp-read-model-v1` 합류, `@RetryableTopic` 3-retry+DLT, manual ACK, invalid→즉시 DLT, `processed_events` dedupe T8) + `ApprovalFactProjection`(domain, **terminal-once**: `applySubmitted` `if(!isTerminal())` revert 방지, `ofTerminal` out-of-order submittedAt=null no-fabrication, last-terminal-wins never un-finalize) + `ApplyApprovalFactUseCase`(latest-state upsert) + `QueryApprovalFactUseCase`(org_scope subtree 필터: DEPARTMENT subject=자신/EMPLOYEE subject=`employee_proj.department` 경유 fail-closed disjunction; `["*"]`/미설정=net-zero; subject read-time 해소 미해소→null+meta.unresolved) + `ApprovalFactController`(`GET /approvals` list/`/{id}` detail, out-of-scope/missing→404 `MASTERDATA_NOT_FOUND`) + V2 Flyway `approval_fact_proj` + `EmployeeProjectionRepository.findIdsByDepartmentIdIn`(additive). **E5 유지**(approval 경로 publish/outbox/write-back 0 — grep 확인; latest fact 만, history 권위=approval-service REST). **3-dim**: (a) MERGED+`d1a9d83f`; (b) origin/main tip 일치; (c) pre-merge failing required=0(**Integration(erp-platform) Testcontainers pass 1m54s** = `ApprovalFactProjectionIntegrationTest` 실 Kafka+MySQL produce→consume→project→read + terminal-once + out-of-order + org_scope list/404 + net-zero; Build&Test GREEN). dispatcher 독립: scope=read-model-service only · `:check` BUILD SUCCESSFUL 99 unit/slice · terminal-once 코드 확인 · E5 grep 0. **메타: ERP-BE-009 producer→ERP-BE-010 consumer 로 erp 내 두 신규 서비스 이벤트 루프 완결(masterdata→read-model 미러); forward-decl 집행이라 새 ADR 불요. E5 — latest fact 만, history 권위=approval-service.** v2-deferred=business-partner/permission facts/notification/multi-stage. 사용자 "read-model approval-fact 투영" 선택.

- `TASK-ERP-BE-009-approval-service-bootstrap-first-increment.md` — **DONE (2026-06-05, 3-dim verified — impl PR #1106 squash `575bc9bf`)**. approval-service 부트스트랩 first increment — **erp 첫 실 도메인 로직(workflow 상태기계) 서비스**(read-model E5 read-only 와 대비). ADR-016 §D3 forward-declaration 집행(spec PR #1105 `5225e9be`: architecture.md+approval-api.md+erp-approval-events.md+§D3 amendment). 분석=Opus 4.8 / 구현=backend-engineer(Opus dispatch)+dispatcher 독립 재검증. `apps/approval-service/`(75 파일, `com.example.erp.approval`, Hexagonal `rest-api`): `ApprovalStateMachine`(pure, finalized-guard 최우선→`APPROVAL_ALREADY_FINALIZED`, legal-edge→`APPROVAL_STATUS_TRANSITION_INVALID`, 전 전이 `next()` 경유 우회 불가, 직접 status UPDATE 없음) + `ApprovalRequest` aggregate(DRAFT→SUBMITTED→APPROVED|REJECTED|WITHDRAWN; route 자기결재 차단→`APPROVAL_ROUTE_INVALID`, 비권한자→`APPROVAL_NOT_AUTHORIZED_APPROVER` E3/I4 SoD) + `ApprovalApplicationService`(단일 @Transactional: 전이+audit append-only+outbox 원자 A7, audit-fail rollback A10) + DB 멱등(claim-before-execute `idempotency_keys`, same-key replay/payload충돌, E4) + `MasterDataRestAdapter`(E1 subject ref-check, RETIRED/404/unreachable→submit 거부 DRAFT 보존, 날조 금지) + outbox `erp.approval.{submitted,approved,rejected,withdrawn}.v1` + REST 8 + GlobalExceptionHandler(5 erp.md 코드) + RS256+entitlement-trust dual-accept. deploy 배선 atomic: settings.gradle include + ci.yml erp `:check`/`:integrationTest`(+read-model latent-gap 백필) + docker-compose `/api/erp/approval` priority=100. **3-dim**: (a) MERGED+`575bc9bf`; (b) origin/main tip 일치; (c) pre-merge failing required=0(**Integration(erp-platform) Testcontainers pass 1m49s** = `ApprovalLifecycleIntegrationTest` 실 MySQL+WireMock: create→submit→approve happy + E1 RETIRED/404 거부 + 자기결재 422 + 비권한 403 + 불법전이 409 + 종결 409 + 멱등 replay + reason 400 + tenant gate + inbox; Build&Test 신규 서비스 포함 GREEN). dispatcher 독립: scope=approval-service+배선 only(기존 서비스·타 프로젝트·.claude 무변경) · `:check` BUILD SUCCESSFUL 52 unit/slice · `ApprovalStateMachine` 가드순서 코드 확인. **v2-deferred(named, not stubbed)**: multi-stage routing / 대결·위임 / IN_REVIEW / rich inbox 필터 / 콘솔 parity slice / read-model approval-fact 투영 / notification fan-out. **메타: read-model(ERP-BE-007) first-increment 선례대로 forward-declaration 집행(새 architecture.md=HARDSTOP-09 충족, 새 결정 0); 로컬 미실행 IT 가 CI Linux Testcontainers 권위 게이트에서 GREEN — 신규 서비스 부트스트랩은 실 컨텍스트(JPA+Kafka outbox+security) IT 가 기능 게이트.** 사용자 "approval-service 부트스트랩 (ERP v2 pillar)" 선택.

- `TASK-ERP-BE-008-org-scope-subtree-containment-and-read-filter.md` — **DONE (2026-06-05, 3-dim verified — impl PR #1097 squash `1476168d`)**. erp `org_scope` 소비 — masterdata subtree containment + read-model 대칭 read 필터. per-operator org_scope 데이터-스코프 **완성**(BE-338 GAP 출처/전파 + 이 task erp 소비). 분석=Opus 4.8 / 구현=backend-engineer(Opus dispatch)+dispatcher 독립 재검증. masterdata `RoleScopeAuthorizationAdapter` flat `contains`→**subtree-aware `isWithinScopedSubtree`**(target→조상 walk via `DepartmentRepository.ancestors`, depth-bound+cycle-break E1; target==root shortcut; `isPlatformScope()` `["*"]` bypass + empty→`DENY_SCOPE` 불변) + read-model `OrgScope` VO + `ReadAuthorizationGate.orgScope(jwt)`(absent/`"*"`→platform net-zero, explicit `[]`→zero-scope via hasClaim) + `QueryEmployeeOrgViewUseCase` subtree(`findSubtreeIds`) 필터 + `?departmentId=` 합성; out-of-scope detail→404 `MASTERDATA_NOT_FOUND`, 미투영 dept→보수적 제외(E5). write·read 대칭. **3-dim**: (a) MERGED+`1476168d`; (b) origin/main tip 일치; (c) pre-merge failing required=0(19 pass/1 skipping, **Integration(erp-platform) Testcontainers GREEN** = subtree write 200/403 + scoped list 좁힘 + out-of-scope 404 + net-zero IT, first-try). dispatcher 독립 재검증: scope=erp masterdata+read-model only · bypass 보존 · `:check --rerun-tasks` BUILD SUCCESSFUL(62+67, 0 failures). **메타: claim=subtree-root(GAP 이 erp 트리 모름)→erp 가 자기 트리로 조상-walk(target→ancestors, O(depth)); net-zero(`["*"]`/absent) 라 BE-338 과 순서 무관; write·read 데이터-스코프 대칭(쓰는 범위=보는 범위).** 사용자 "per-operator org_scope" 선택.

- `TASK-ERP-BE-007-read-model-service-org-view-first-increment.md` — **DONE (2026-06-04, 3-dim verified)**. erp `read-model-service` 첫 증분 — 마스터 변경 이벤트 전파의 **비어 있던 소비자 고리**를 닫음("v1 consumers = none" 해소). 3-PR: **spec PR #1089** (`04723776`, markdown-only: read-model-service spec 3종 + task→ready + PROJECT.md frontmatter `[rest-api]`→`[rest-api,event-consumer]` + Service Map + ADR-016 §D3 amendment + erp-masterdata-events.md consumer note + masterdata forward-ref) → **impl PR #1090** (`e35d429c`, 신규 `apps/read-model-service` Hexagonal rest-api+event-consumer: 4 `@KafkaListener` + `@RetryableTopic` 3-retry+DLT + `processed_events` dedupe T8 + 4 MySQL projection[`erp_read_model_db`] + 읽기 시점 employee org-view[부서 ancestry path+비용센터+직급] + read-only REST 2종 + entitlement-trust dual-accept + READ fail-closed; erp docker-compose bitnami/kafka KRaft 브로커 신설+masterdata `KAFKA_BOOTSTRAP` 배선+`erp_read_model_db` initdb; **no-outbox/no-publish E5 terminal** — `OutboxAutoConfiguration` exclude) → **close chore (this)**. **구현=backend-engineer(Opus dispatch)+dispatcher 독립 재검증**(BE-001 패턴): scope leak 0 · E5 main publish/outbox grep 0(`KafkaTemplate`=`@RetryableTopic` DLT plumbing) · `:check --rerun-tasks` BUILD SUCCESSFUL(55 unit+slice) · `docker compose config -q` exit 0. **기능 게이트 = CI "Integration (erp-platform, Testcontainers)" 1m23s PASS**(실 Kafka+MySQL+JWKS consume→project→read end-to-end + dedupe/DLT/unresolved/RETIRED+asOf/보안 4분기). **3-dim**: (a) MERGED+`e35d429c`; (b) origin/main tip 일치; (c) pre-merge failing required=0(19 pass/1 skipping). 청사진=scm `inventory-visibility-service`. **메타**: ① producer 이미 완성(BE-001)·강화 지점=비어 있던 소비자(forward-decl 실행이라 신규 ADR 불요) ② OutboxAutoConfiguration exclude(lib↔spec `processed_events` 테이블명 충돌+`ddl-auto=validate` 공존 불가) ③ **라이브 스모크 정직 deferral**(가동 중 e2e 스택 `erp.local` 라우터 충돌 위험 → 미실행; CI IT 가 기능 경로 커버 — green-wash 아님). **follow-up**: ① console "통합 조회" 카드(TASK-PC-FE) ② business-partner+풀 통합조회+per-operator org_scope read 필터(v2) ③ 풀 스택 로컬 라이브 스모크.

- `TASK-ERP-BE-006-masterdata-response-effectiveperiod-contract-conformance.md` — **DONE (2026-06-03, 3-dim verified)**. masterdata-service response **contract conformance**: the 5 master `*View` records emitted FLAT `effectiveFrom`/`effectiveTo`/`createdAt`/`updatedAt`, violating `masterdata-api.md` § Common shapes (and the platform-console PC-FE-010 consumer) which require nested `effectivePeriod: { effectiveFrom, effectiveTo }` (+ detail `audit`). **Surfaced by TASK-MONO-170** live demo: console ERP 운영 got 200 from all 5 masters but every response failed the consumer Zod parse (`erp_ok`→`erp_error`) → degraded. Latent because the console client had only mocked unit tests + the producer slice test asserted only `$.data.code`/`status`. **Fix**: new `EffectivePeriodDto` + `AuditDto`; 5 Views reshaped flat→nested + `from()` factories; `DepartmentControllerSliceTest` constructor + **nested-shape regression assertion** (`$.data.effectivePeriod.effectiveFrom` exists, `$.data.effectiveFrom` doesNotExist); `MasterdataLifecycleIntegrationTest` accessors. **NO domain/persistence/auth/contract/ADR change** (contract already specifies nested; producer brought into conformance). impl PR #1036 squash `466c4903`. **3-dim**: (a) MERGED+`466c4903`; (b) origin/main tip chain; (c) pre-merge failing=0 (19 SUCCESS/1 SKIPPED incl. erp Integration Testcontainers + slice test = real-Jackson nested-shape proof). 라이브 재배포(fed-e2e healthy). **메타: per-domain ops 콘솔은 mock 단위테스트만 있어 producer↔계약 nested-shape drift 잠복 — 회귀 단언이 실 직렬화를 단언해야 재발 차단.** 분석=Opus 4.8 / 구현=Opus(직접).

- `TASK-ERP-BE-005-tenant-gate-entitlement-trust-dual-accept.md` — **DONE** (ADR-MONO-019 § 3.3 step 3 복제 1/N, erp; impl bundled branch + close chore; BE-303 3-dim). 분석=Opus 4.8 / 구현=Opus (dispatch). **impl PR #962** (squash `b75fbed1`). finance 파일럿(FIN-BE-006) blueprint 의 erp masterdata-service 복제. `TenantClaimValidator`(decode)+`TenantClaimEnforcer`(filter, erp 에 신규 EnforcerTest)를 `tenant_id==erp` 고정 → **entitlement-trust dual-accept**: legacy(`tenant_id ∈ {erp,*}`) ∪ 서명 `entitled_domains ∋ erp`, 공유 정적 `isEntitled` 양 지점, **거부=!legacyOk && !entitled**(fail-closed). **net-zero**(claim 부재 시 legacy만). 격리 IT entitled(wms+[erp])→2xx / non-entitled(scm)→403 TENANT_FORBIDDEN. architecture.md § Multi-tenancy + Failure #3. **BE-303 3-dim**: (a) MERGED `b75fbed1`; (b) tip 일치; (c) pre-merge failing=0. **BE-299 re-stage** ✓. **CI 1-pass** erp Integration GREEN 1m39s. **scope-lock** erp masterdata 만. erp validator=finance byte-identical → 복제 near-mechanical(blueprint 일반화 확인). **남은 step 3**: scm/wms(6-svc)/gap+console-bff + GAP `entitled_domains` populate(shared).

- `TASK-ERP-BE-004-masterdata-cross-tenant-http-it.md` — **DONE** (3-PR sequence per erp PR Separation Rule, BE-303 3-dim objectively merge-verified). 분석=Opus 4.7 / 구현=Opus 4.7 (isolation → Opus, ADR-013 § D6 row 8; 2-cycle CI). **Implements ADR-MONO-018 D5 federation isolation regression — erp slice** (lean gap-fill: the D5 audit found wms `OidcAuthIntegrationTest` / scm `MultiTenantIsolationIntegrationTest` / finance `CrossTenantHttpIntegrationTest` / GAP `AdminAuditTenantScopeIntegrationTest` already cover their domains + console-bff slice = TASK-PC-BE-006 DONE; erp had only a `TenantClaimValidator` **unit** + controller **slice** test — no HTTP-layer IT exercising the gate end-to-end). **spec PR #909** (squash `316b8229`, markdown fast-lane). **impl PR #910** (squash `ded56253`, +~130 across 3 files — new IT + task review-move + INDEX; **"Integration (erp-platform, Testcontainers)" CI job PASS 1m25s** = CI-Linux real-execution; Docker unavailable locally → `compileTestJava` only locally, honestly flagged). **`CrossTenantHttpIntegrationTest`** (MockMvc + Testcontainers MySQL/Kafka + JWKS MockWebServer, mirrors finance pattern) on `GET /api/erp/masterdata/employees`: `tenant_id=scm` (foreign) → **403** `$.code=TENANT_FORBIDDEN` (AC-3); `tenant_id=*` → **2xx** (platform-scope wildcard accepted — erp-specific edge, AC-4); `tenant_id=erp` → **2xx** (AC-5); no token → **401**. **AC-6** `git diff origin/main -- 'projects/erp-platform/apps/masterdata-service/src/main/**'` = empty (test-only) ✓. **2 CI cycles**: ① cross-tenant 403 + no-token 401 PASS, but erp/`*` → 403 not 2xx — root cause = token `roles=[ERP_READ]` did not satisfy `RoleScopeAuthorizationAdapter` READ (fail-closed: requires scope `erp.read`/`erp.write`/operator) → 403 PERMISSION_DENIED at the **authorization** layer (the tenant gate passed; scm fails earlier at the decode tenant gate, so its 403 is TENANT_FORBIDDEN); ② fix = mint token with `scope=erp.read` (the employees list passes targetDepartmentId=null → no data-scope check) → 4/4 PASS. **BE-303 3-dim**: (a) state=MERGED + mergeCommit `ded56253`; (b) `git log origin/main` tip `ded56253` 일치; (c) pre-merge `gh pr checks 910` failing=0. **BE-299 re-stage**: staged done/ blob Status=done ✓. **메타**: ① **tenant gate vs authorization layer 분리 진단** — cross-tenant(scm)는 JWT decode 의 TenantClaimValidator 에서 403 TENANT_FORBIDDEN; same/wildcard tenant 는 tenant gate 통과 후 RoleScopeAuthorizationAdapter(fail-closed)가 별도 평가. IT 토큰은 read scope 를 가져야 2xx 도달 — tenant 단언과 authorization 단언의 layering 을 정확히 구분. ② **ADR-018 D5 lean gap-fill 종결** — console-bff(PC-BE-006) + erp(this) 2 신규 IT + wms/scm/finance/GAP 기존 attestation = 6-point isolation surface 충족. **ADR-MONO-018 Phase 8 (D4 observability federation + D5 isolation regression) 전체 종결.** ③ **next** = 외부 trigger (새 federation hardening scope 또는 새 phase).

- `TASK-ERP-BE-002-platform-console-operator-read-consumer-reconciliation.md` — **DONE** (3-PR sequence per erp strict PR Separation Rule, each stage objectively merge-verified). 분석=Opus 4.7 / 구현=Opus 4.7 / 리뷰=Opus 4.7. **spec PR #655** (squash `09d4cb2a`, mergedAt 2026-05-20T05:28:45Z) — task author to `ready/` (185-line spec, FIN-BE-005 2-file finance subset answer, NOT scm 3-file). **impl PR #656** (squash `083c744b`, mergedAt 2026-05-20T05:33:06Z, +27/-3) — (1) `projects/erp-platform/specs/integration/gap-integration.md` 새 `## platform-console Operator Read Consumer (ADR-MONO-013)` 섹션 (Token 검증 규칙↔Error Responses 사이; +23/-0) + (2) `projects/erp-platform/PROJECT.md` § GAP IdP Integration clarifying bullet (+1/-0, frontmatter L1-L10 byte-unchanged 검증 — diff hunk @L79 시작) + lifecycle ready→review. **(B) document/accept of existing capability**: GAP RS256 + JWKS + issuer + `tenant_id ∈ {erp,*}` + `X-Token-Type` chain (§ Token 검증 규칙 #1/#3/#4) 가 이미 허용하던 것을 명시화. *"internal-only 경계"* #6/E7 **CLARIFIED, not weakened** — 경계 = 비-GAP-SSO 트래픽 (raw public internet); GAP-authenticated console routed via internal Traefik 는 SSO 경계 **내부**. #6 byte-identical. **불변 검증**: `masterdata-api.md` + `masterdata-service/architecture.md` + ADR-MONO-016 + § Token 검증 규칙 #1–#6 + `PROJECT.md` frontmatter (domain=erp / traits=[internal-system,transactional,audit-heavy] / service_types=[rest-api] / data_sensitivity=confidential) 모두 byte-unchanged. **read surface**: 10 v1-live GETs (5 masters × {list, detail}: `/api/erp/masterdata/{departments,employees,job-grades,cost-centers,business-partners}`) with `?asOf=<ISO-8601>` point-in-time (architecture.md E3 half-open `[from, to)`). **excluded**: 16 mutation endpoints (5×create/5×patch/5×retire/1×move-parent) + v2 `approval-service`/`read-model-service`/future `admin-service` (ADR-MONO-016 § D3). **erp internal-system producer obligations cross-ref** (신규 erp 요구 아님): confidential `data_sensitivity` + audit-heavy + E1 (reference integrity)/E2 (effective dating)/E3 (asOf point-in-time)/E8 (append-only audit) — 콘솔이 producer-authoritative 사실을 충실히 렌더. NO new OAuth client / NO new gateway-service route / NO auth-model change / NO erp ADR. **finance FIN-BE-005 vs erp ERP-BE-002 shape 일치 (honest)**: 두 task 모두 2-file (`gap-integration.md` + `PROJECT.md`); 동일 shape 의 이유 = 둘 다 `gateway-public-routes.md` 미존재 (finance gateway v1-deferred / erp gateway v1-IN 선언이나 architecture spec 미작성). scm 3-file 답습 force-fit 거부 — 정직 2-file. **close chore (this PR)** — `git mv review/ → done/` + Status `review → done` + INDEX move; BE-299 re-stage check (`git show :<done-path>` reads `done`) verified before commit; BE-303 객관 머지 검증 (impl PR #656 mergeCommit `083c744b` + `git log origin/main` tip 일치) before close chore start. Scope across 3 PRs = `projects/erp-platform/specs/integration/gap-integration.md` + `PROJECT.md` + task lifecycle/INDEX only (no code under apps/, no other project, no other ADR). **erp-side ADR-MONO-013 Phase 6 prerequisite SATISFIED** — TASK-PC-FE-010 (platform-console erp console section) 의 `backlog → ready` move 가 이제 unblocked. closed via this close chore PR.
- `TASK-ERP-BE-001-masterdata-service-bootstrap.md` — **erp-platform 첫 도메인 서비스 `masterdata-service` Hexagonal 구현 (spec-first 2-PR + close chore — erp INDEX 엄격 PR Separation Rule)**. 분석=Opus 4.7 / 구현=backend-engineer(Opus dispatch) / 리뷰=Opus 4.7 (dispatcher 독립 재검증). **spec PR #649** (squash `16a5d1fe`, mergedAt 2026-05-20T04:02:36Z): architecture.md(Hexagonal, ADR-MONO-012 canonical, HARDSTOP-09/10; Service Type=`rest-api` single — outbox≠event-consumer, finance-account / scm-procurement 선례·platform/service-types INDEX L48) + masterdata-api.md(26 endpoints + flat error envelope + `?asOf=` point-in-time read + `Idempotency-Key` on every mutation + data-scope contract) + erp-masterdata-events.md(5 `erp.masterdata.*.changed.v1` topics, libs/java-messaging BaseEventPublisher envelope, v1 PUBLISH 결정 build.gradle 증거 backing) + platform/error-handling.md erp section(HARDSTOP-03 clean, additive only — 6 unique erp codes + cross-refs to Platform-Common aliases). 4 files / 1449+ / 0−. **impl PR #650** (squash `b110e03f`, mergedAt 2026-05-20T04:48:59Z, 99 file/5996+/37−): domain(16 — 5 aggregate Department/Employee/JobGrade/CostCenter/BusinessPartner + EffectivePeriod VO half-open `[from, to)` + MasterStatusMachine + AuditLog append-only + AuthorizationDecision + DomainErrors, framework-free per architecture.md "JPA-on-domain exception") / application(14 — `MasterdataApplicationService` 단일 @Transactional 경계 authorize→repo→audit→event 순서; MasterdataEventPublisher extends BaseEventPublisher; AuthorizationPort/ClockPort/IdempotencyStore ports) / infra(19 — JPA adapters per aggregate + outbox + **DbIdempotencyStore FIN-BE-004 final form** (non-tx orchestrator catches `DataIntegrityViolationException` OUTSIDE REQUIRES_NEW, `IdempotencyKeyTx` 4 single-statement REQUIRES_NEW methods — cycle-1 broken form ABSENT) + **RoleScopeAuthorizationAdapter fail-CLOSED default** (denyRole/denyScope before allow, E6) + RS256 JWT chain `tenant_id ∈ {erp,*}` + JpaConfig/ClockConfig) / presentation(13 — 5 controllers ≡ masterdata-api.md + GlobalExceptionHandler maps every domain exception to platform/error-handling.md erp codes + TenantClaimEnforcer + IdempotentExecution + PublicPaths; **0 @Transactional on controllers** 실코드 grep 확인) / Flyway V1 MySQL InnoDB utf8mb4 9-table + **per-field `@JdbcTypeCode(SqlTypes.VARCHAR)` on every `@Enumerated`** (7↔7 paired, FIN-BE-002 cycle-2 form; cycle-1 global-config broken form ABSENT) + 2 `@JdbcTypeCode(SqlTypes.JSON)` for audit / tests(9 — 4 domain unit + 2 infra unit + 1 application unit + 1 slice + 3 IT `@Tag("integration")` Testcontainers MySQL+Kafka, H2 forbidden) + Dockerfile + docker-compose `erp.local` Traefik label active + backing service expose-only. **dispatcher 독립 재검증** (agent report 불신, 재실측): scope=99 files all under projects/erp-platform/ (0 leak: specs/platform/rules/ADR/libs/other-projects all byte-stable) · `getOperatorToken`/`getAccessToken` 부재 · `@Transactional` in presentation 부재(1 string=JavaDoc) · enum-paring 7↔7 · `actionExecuted.isEqualTo(1)` EXACT · `DataIntegrityViolationException` caught at DbIdempotencyStore L58/73/93 OUTSIDE any tx · `AuditLogRepository` port=`append()` ONLY 구조적 append-only · `RoleScopeAuthorizationAdapter` denyRole/denyScope-before-allow · `./gradlew :check --rerun-tasks` BUILD SUCCESSFUL 25s 36/0/0/0 (7 XML 파싱) · `docker compose config -q` exit=0. **⚠️ 정직 gap (green-wash 금지)**: erp ci.yml 은 Docker-free `:check` 만 wire — 3 Testcontainers IT 클래스 (`AbstractMasterdataIntegrationTest`+`MasterdataLifecycleIT`+`IdempotencyConcurrencyIT`, 실 MySQL+Kafka)는 CI 미실행 (scm MONO-048 / finance MONO-115 동형 부재). IT 는 compile-clean + E1-E8 구조 독립검증됨이나 행위 증명은 CI 미수행 → **follow-up = TASK-MONO-XXX candidate: "Integration (erp-platform, Testcontainers)" CI job 신설** (`.github/workflows/ci.yml` monorepo-level, scm MONO-048 / finance MONO-115 analog). 이는 finance v1 BE-001 의 동일 시나리오 (finance v1 honest gap-then-closure chain: BE-001→MONO-115→BE-002→003→004 가 패턴). erp v1 같은 cycle 가 follow-up 될 수 있음. closed via this close chore PR.

## archive

(empty)

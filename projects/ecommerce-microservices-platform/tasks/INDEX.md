# Tasks Index

This document defines task lifecycle, naming, and move rules.

---

# Lifecycle

backlog → ready → in-progress → review → done → archive

Only tasks in `ready/` may be implemented.

---

# Task Types

- `TASK-BE-XXX`: backend
- `TASK-FE-XXX`: frontend
- `TASK-INT-XXX`: integration

---

# Move Rules

## backlog → ready
Allowed only when:
- related specs exist
- related contracts are identified
- acceptance criteria are clear
- task template is complete

## ready → in-progress
Allowed only when implementation starts.


## in-progress → review
Allowed only when:
- implementation is complete
- tests are added
- contract/spec updates are completed if required

## review → done
Allowed only after review approval.

### Review Rules
- Tasks in `review/` must not be re-implemented directly.
- If a review reveals a bug or missing requirement, create a new fix task in `ready/` referencing the original task.
- Fix tasks must include the original task ID in their Goal section (e.g. "Fix issue found in TASK-BE-002").
- Do not modify a task file after it moves to `review/` or `done/`.

## done → archive
Allowed when no further active change is expected.

---

# Rule

Tasks must not be implemented from `backlog/`, `in-progress/`, `review/`, `done/`, or `archive/`.

---

# Task List

## backlog

| ID | Title | Service | Tags |
|---|---|---|---|
| TASK-BE-077 | 쿠폰/프로모션 시스템 — 할인 쿠폰 발급, 사용, 프로모션 관리 기능 | promotion-service (신규) | code, api, event |
| TASK-BE-078 | 알림 서비스 — 이메일/SMS/푸시 알림 발송 기능 | notification-service (신규) | code, api, event |
| TASK-BE-079 | 리뷰/평점 시스템 — 상품 리뷰 작성, 평점 관리, 평균 평점 집계 | review-service (신규) | code, api, event |
| TASK-BE-080 | 위시리스트/찜 기능 — 사용자별 관심 상품 목록 관리 | user-service | code, api |
| TASK-BE-081 | 배송 추적 서비스 — 주문 배송 상태 관리 및 추적 | shipping-service (신규) | code, api, event |
## ready

| ID | Title | Service | Tags |
|---|---|---|---|
| TASK-BE-390 | **READY — ⏳ 2026-08-01 게이트 (그 전 구현 금지)**. D2-b deprecation window(~2026-08-01) 종료 후 gateway `allowed-issuers`에서 레거시 `iam` issuer 제거 + 테스트 정리. AC-0 verify-then-act(live `iss=iam` 토큰 0 확인) 선행. | gateway-service | code, security, test |
| TASK-BE-548 | **NEW (READY)** — 배송 상태 동시 전이의 **root 경합 제거**(`@Version` 낙관적 락 + 409 계약). `TASK-BE-537` 이 재현하고 `TASK-BE-547`(done)이 후속으로 명시한 그 작업. BE-547 은 **피해**(중복 이벤트/알림)를 발행측 결정적 event_id 로 종결했지만 두 동시 트랜잭션이 `shippings` 에 **무해한 중복 UPDATE**(같은 값)를 쓰는 root 경합 자체는 남았다. 이 티켓은 `shippings.@Version` 으로 두 번째 커밋을 `ObjectOptimisticLockingFailureException` 으로 거부하고 **500 누출(BE-537 지적)을 409 로** 매핑. **⚠️ AC-0 이 "할 가치"부터 판정** — BE-547 이 이미 고객 피해 종결 + 경합 패자에 409(아웃박스 PK→DIVE)를 주므로 순가치는 "중복 UPDATE 제거 + 더 정확한 실패 모드"뿐(버그 아닌 견고성 개선). **won't-do 도 유효한 결과**(BE-537 음성결과 틀). **🔴 매개자 상호작용**: `@Version` 이 UPDATE 에서 먼저 터져 BE-547 아웃박스-PK 경로가 동시 케이스에서 도달불가가 될 수 있음(실측·명시). **에러코드 일관성**(같은 충돌에 409 둘 금지, F2). BE-547 결정적 event_id 는 소비자 idempotency 키로 **유지**(상보, 대체 아님). 로컬 Testcontainers FLAKY, **CI Linux 권위**. 분석·구현=Opus 4.8. [[project_deterministic_event_id_outbox_pk_collapses_dupes]] [[env_test_fixture_impossible_input_proves_nothing]] | shipping-service | concurrency, optimistic-lock, contract |
| TASK-BE-549 | **NEW (READY)** — auth-service DIVE 핸들러를 fleet 표준 헬퍼로 수렴(message-string 매칭 제거). `TASK-MONO-450`(done, #2814)이 `DataIntegrityViolations.isUniqueViolation()`(SQLSTATE 23505) 로 fleet 을 selective 매핑(unique→409, FK/NOT NULL→500)으로 재배선했고 **17개 표현계층 DIVE 핸들러 중 16개가 수렴**했으나, **auth-service 만 `message.contains("uq_users_email")` brittle 문자열 매칭 잔존**(`GlobalExceptionHandler.java:71-82`). 헬퍼 Javadoc 이 경고하는 vendor/driver 의존 패턴 — 깨지면 중복 이메일 동시등록이 409 대신 **500 누출**. **⚠️ blind 교체 금지**: 현재 `uq_users_email` 만 409 EMAIL_ALREADY_EXISTS 로 매핑하므로 naive `isUniqueViolation→EMAIL_ALREADY_EXISTS` 는 **다른 unique 위반을 오분류**. **AC-0 이 "email 이 백스톱 도달 유일 unique 제약인가"를 재측정**(코드가 이긴다) 후 형태 판정. 동시 중복이메일 race IT 로 동작 보존. `EmailAlreadyExistsException` 정상경로 핸들러는 불변(상보). auth 는 MONO-450 scope 밖 **선재 갭**(회귀 아님). 로컬 Testcontainers FLAKY, **CI Linux 권위**. 분석=Opus 4.8/구현=Sonnet. [[feedback_workaround_becomes_the_contract]] [[project_enforcement_straggler_sibling_parity]] | auth-service | code, error-contract, refactor |

## in-progress

_(없음)_

## review

_(없음)_

## done

| ID | Title | Service | Tags |
|---|---|---|---|
| TASK-BE-552 | **DONE (2026-07-22, 3-dim verified — impl PR #2862 squash `7c372b92e`; `state=MERGED`, `origin/main` 편입, non-SUCCESS 체크 **0** — doc-only 이므로 코드 레인은 경로필터로 SKIPPED, `changes` gate SUCCESS).** gateway `public-routes.md` 가 이미 제거된 `/api/auth/**` 3개 public route + Sensitive rate-limit tier 를 여전히 광고하던 **doc-only stale** 정정. **AC-0 재측정**: 스펙엔 여전히 3행+tier 존재, `application.yml` 엔 제거 주석만(라우트 0 → `/api/auth/**` 404) — 코드 정상, routing 결함 아님. **AC-1**: 3행 + Sensitive-tier auth 참조 제거, auth→GAP/IAM OIDC 노트 추가(토큰은 IdP `/oauth2/authorize` 에서 발급·gateway 는 RS256 JWT 검증, `/oauth/**` 가 permit 표면). **AC-2 결정=defer(명시)**: `SecurityConfig` PUBLIC_PATHS≈34 / `RouteService`≈9 / integration-test yml 의 dead `/api/auth/**` 참조는 무해(라우트 0→404)하고, 제거하면 doc 정합화를 gateway Testcontainers IT 표면으로 확장(무기능 변경) → 범위 밖 유지, 별도 cleanup 티켓 후보. 분석=Opus 4.8/구현 권장=Sonnet. | gateway-service | docs |
| TASK-BE-554 | **DONE (2026-07-21 audit round-2, merged #2848).** 이커머스 계약 문서 정합: review-api `POST /api/reviews` 요청에 누락된 `productName` 필드, order-api 미문서화 admin 엔드포인트 2종(`/summary`, `/insights`), events README § 6 인덱스 테이블에 누락된 `fulfillment-events.md` 행. Doc-only, 행동 변경 없음. | review-service, order-service | docs |
| TASK-BE-547 | **DONE (2026-07-21, 3-dim verified — impl PR #2811 squash `5bd1490f7`; `state=MERGED`, `origin/main` tip 일치, non-SUCCESS 체크 **0** — `Build & Test JDK21` + ecommerce 통합 A·B·C(Testcontainers) 실제 실행).** 동시 이중 `PUT /shippings/{id}/status`(→SHIPPED) 가 `ShippingStatusChanged` 아웃박스 2행 → 고객 배송 알림 2회(`TASK-BE-537` 이 재현한 유일한 실사용 피해). 원인: `SpringShippingEventPublisher` 가 발행마다 `UUID.randomUUID()` 채번 ⇒ 동시 중복이 서로 다른 event_id 로 두 소비자 dedup 을 무력화. **판정 = (A) 발행측 결정적 채번**: event_id 를 `(shippingId,newStatus)` 에서 `UUID.nameUUIDFromBytes` 로 유도. 선형·forward-only 상태기계라 한 shipment 은 각 status 한 번만 진입 ⇒ 같은 키 재발 = 경합/재시도 중복일 때뿐(정당한 전이 불침). **🔵 매개자 = event_id 가 곧 `shipping_outbox` 행 PK** ⇒ 두 번째 동시 발행이 PK(23505) 충돌 → 트랜잭션 롤백 → 이벤트가 토픽 미도달(부수효과 1회). 보존되는 outbox 행이 영구 idempotency 키가 되고, read-then-write 앱 검사가 아니라 DB 강제 유니크라 **동시성에서 성립**(AC-4). **🔵 409 판정**: 경합 패자는 **기존 BE-542 DIVE 백스톱**(`GlobalExceptionHandler` SQLSTATE 23505)으로 이미 409 — (C) `@Version` OptimisticLock 이 낼 클라이언트 계약과 **동일**하되 version 컬럼·마이그레이션·새 예외 없이 등록된 에러코드 재사용. ⇒ (C) 의 무거운 계약 작업을 안 끌어옴. `ManualShipConfirmRequested`(wms 이중 차감 위험)도 패자 전체 롤백으로 함께 접힘(BE-537 이 남긴 덤 피해 해소). **(B) 기각**: order 소비자에 동일 event_id 결함 방치(상태기계가 우연히 가림 — 다음 소비자에서 재발, workaround-becomes-contract). **(C) 기각**: 가장 옳지만 가장 비쌈, (A) 가 같은 계약을 더 싸게 달성 — root 이중 UPDATE(같은 값, 무해)는 남기나 피해는 완전 종결, root 제거가 진짜 필요하면 별 티켓. **계약 선행 갱신**(AC-5): `shipping-events.md` § event_id semantics(채번 규약/idempotency 키) + `shipping-api.md` PUT status 409 DATA_INTEGRITY_VIOLATION on concurrent conflict. **테스트**: `SpringShippingEventPublisherTest` 결정론 단위(같은 전이→같은 id, 다른 newStatus/다른 shipment→다른 id) + `ConcurrentStatusTransitionIntegrationTest`(BE-537 특성화)를 **회귀 가드로 전환** — 이중 발행 단언 **2→1**, 정확히 한 스레드가 유니크 위반 롤백(XOR) 단언. notification-service 무변경(기존 event_id dedup 이 결정적 id 를 받으면 그대로 작동, F4 회귀 없음). **머지 treadmill**: 고빈도 main 으로 INDEX rebase 2회(BE-545/546 close 와 충돌 — 매번 마커·배치·큐디렉토리 대조로 해소, INDEX queue drift CI 통과). 로컬 Windows Testcontainers 는 FLAKY(권위 아님) — 컴파일+결정론 단위까지만 로컬 GREEN, 동시성 IT 는 **CI Linux 가 권위**. 분석·구현=Opus 4.8. [[feedback_workaround_becomes_the_contract]] [[project_deterministic_event_id_outbox_pk_collapses_dupes]] [[project_enforcement_straggler_sibling_parity]] [[env_ci_retarget_reopen_index_churn]] | shipping-service | concurrency, idempotency, notification |
| TASK-BE-545 | **DONE (2026-07-21, 3-dim verified — impl PR #2810 squash `dbad5f504`; `state=MERGED`, `origin/main` 조상 확인, non-SUCCESS 체크 **0** — SKIPPED 24(경로필터)/SUCCESS 13, ecommerce 전 레인 `Build & Test`+통합 A·B·C+프런트 실제 실행).** **정정 티켓 — `TASK-BE-533`(PR #2798)에 동시 세션이 끼워 넣은 거짓 주장을 반증·정정.** 그 커밋은 order-service `UserWithdrawnEvent` 에 `@JsonIgnoreProperties` 를 추가하며 *"tenant_id 때문에 실 `UserWithdrawn` 메시지 전량이 `user.user.withdrawn.dlq` 직행"* 이라 단정하고 감사 티켓(구 BE-545)까지 파일했다. **🔴 실 Spring 컨텍스트 bean 으로 반증**: 컨슈머 주입 mapper 는 `FAIL_ON_UNKNOWN_PROPERTIES=false`(Testcontainers 로 `OrderServiceApplication` 부팅해 주입 bean 직접 측정 — feature off, tenant_id envelope ACCEPTED). 근거 테스트가 `new ObjectMapper()`(strict)를 쓴 **픽스처 artifact** 였고 프로덕션 경로는 항상 역직렬화, DLQ 직행 0. 어노테이션은 형제(`PaymentRefundedEvent`·`AccountDeletedEvent`) 일관성·미래 strict-mapper 대비로 **유지**, 서사만 정정. **회귀 테스트 재구성**: 기본 mapper=어노테이션 없이도 GREEN(=장애 없었음 증거) / strict mapper=어노테이션 있어야 GREEN(=어노테이션이 지키는 유일한 경우), **mutation 으로 후자만 정확히 RED** 확인(F3 가드). 헛수고 티켓(구 BE-545)을 감사→정정으로 재작성, BE-533 done 노트의 거짓 문단 교체. **F1 자기적용**: 내 "장애 없음" 도 가설로 두고 AC-0 에서 실 bean 재측정 선행. order-service 385 tests 0F 0S(XML). INDEX treadmill(동시 close-chore 연속)로 rebase 3회, 매번 마커·배치·중복 실측 검증. 분석·구현=Opus 4.8. [[env_test_fixture_impossible_input_proves_nothing]] [[feedback_guard_predicate_wrong_verify_the_artifact]] [[feedback_recount_population_dont_inherit_scope]] [[env_concurrent_session_task_id_collision]] | order-service (docs/test) | correction, correctness |
| TASK-BE-546 | **DONE (2026-07-21, 3-dim verified — impl PR #2808 squash `c001e3646`; `state=MERGED`, `origin/main` ff 확인, non-SUCCESS 체크 **0** — `Build & Test` + ecommerce 통합 A·B·C(Testcontainers) 실제 실행).** `wishlist_items` 유니크 제약을 `(user_id, product_id)` → `(tenant_id, user_id, product_id)`(V6). V4 가 tenant_id 를 격리축으로 넣으며 제약을 *"unchanged"* 로 남겨 선체크(`...AndTenantId`, `TenantContext` 주입)와 축이 어긋나 있었다. **🔴 AC-0 도달불가를 산문 아닌 구조로 확정**: `wishlist_items.user_id` FK → `user_profiles(user_id)`(globally unique) ⇒ 한 wishlist 행=한 프로필=한 tenant, **두 컬럼 키와 세 컬럼 키가 이 테이블에서 정확히 같은 집합을 거부** ⇒ 런타임 사고 0, **표현력/정합성 결함**(제약 밖 user_id 발급 규칙이 바뀌면 조용히 틀림; 형제 review-service 는 V7 로 이미 정렬). AC-1=무손실 가정 금지(`RAISE EXCEPTION` 선행). **🔴 AC-2 정직하게** — 진짜 불변식(동일 테넌트 중복 거부 + 다른 상품 허용)만 IT 로 고정. **교차 테넌트 허용은 의도적 미테스트**: FK 로 그 행을 못 만드니 픽스처가 곧 impossible input, 그 불가능성 자체가 AC-0 도달불가 논거 ⇒ IT Javadoc 에 사유 기록(거짓 픽스처로 없는 성질을 증명하지 않는다). **로컬 전체 IT 는 호스트 도커 데몬 포화(`ContainerLaunchException` 전 클래스, 데몬 buffer 고갈)로 실패했으나 내 IT 단독 2/2 GREEN + CI Linux A·B·C GREEN 이 회귀 무증명의 권위**(AC-4). 계약 변경 0(도달 불가 경로라 관측 거동 0). `TASK-BE-543` AC-2 에서 분리, `TASK-BE-540` V7 선례. 분석·구현=Opus 4.8. [[env_test_fixture_impossible_input_proves_nothing]] [[project_enforcement_straggler_sibling_parity]] [[project_testcontainers_docker_desktop_blocker]] | user-service | tenancy, constraint, migration |
| TASK-BE-533 | **DONE (2026-07-20, 3-dim verified — impl PR #2798 squash `9ba5a0534`; `state=MERGED`, `origin/main` 조상 확인, non-SUCCESS 체크 **0** — `Build & Test` + ecommerce 통합 A·B·C + 프런트 레인 실제 실행).** ADR↔구현 드리프트 감사 산물 ②. ADR-006 이 **자기 ACCEPT 의 게이트로 선언한** 4개 mitigation 을 실제로 배선. **AC-0 재측정: 4건 전부 여전히 미이행 확정**(각 "부재" grep 을 `user-service/UserMetrics.java` known-positive 로 자기검증 후 판정). user-service `event_publish_failure_total` tap 은 **존재** — 비대칭 보존됨. **한 population 원칙**: `notification_sent_total{channel}`·`notification_failed_total{channel,reason}` 을 **저장되는 notification row 당 정확히 1회** 증가(둘 다 `NotificationSendService#renderAndSend`)시켜 `failed/(failed+sent)` 를 진짜 비율로 만듦 — sender 별 시도 단위 집계 금지. `reason` 은 **bounded enum**(카디널리티 상한 = channels x reasons). **🔴 fail-soft 함정을 실측으로 잡음**: `WebPushSender` 는 구독별 예외를 의도적으로 삼켜서, **아무에게도 도달 못 한 push 가 정상 리턴 → row 는 SENT, 카운터는 0** 이었다 — 서비스 계층만 계측했으면 원 결함을 한 겹 안쪽에서 재생산했을 것. 전건 실패일 때만 escalate(부분 성공=발송, 만료 prune=housekeeping). **🔴 알림은 ADR 문자열을 복원하지 않았다**: ADR-006 은 `user_event_publish_failure_total` 이라 적지만 그런 시계열은 없다 — 실제 emitter 는 공유 `event_publish_failure_total{service,event_type}` 이고, ADR 문자 그대로 썼으면 **BE-532 가 치운 바로 그 no-data 결함을 재생산**했을 것. **🔵 `promtool check rules` 는 파싱만 증명한다**(제거된 룰들도 몇 달간 잘 파싱됐다) → **신규 `alert-rules.test.yml`**: 앱이 emit 하는 이름·라벨 그대로의 합성 시계열로 **실제 발화까지 단언**(양쪽 rename 이 조용한 no-data 대신 테스트 RED 를 냄). **🔴 runbook 작성 중 ADR 오류 1건 발견**: ADR-006 은 `user.user.withdrawn` 소비자를 *"order + auth-service"* 라 하지만 ecommerce auth-service 는 TASK-BE-132 로 폐기(settings.gradle·compose 양쪽 제외) — 소비자 그룹이 없는 걸 확인하라는 단계를 제거하고 order-service 단독으로 정정. 검증: 3서비스 133/229/383 테스트 0 failed·**0 skipped**(XML 실측), **mutation 2건이 의도한 신규 테스트만 정확히 RED**, `promtool check`(17 rules)+`test` SUCCESS(compose 가 핀한 **v2.54.1** 로 실행). **🔵 범위 밖 잔여(BE-532 가 넘긴 입력, 확인함)**: `observability.md` 의 `notification_retry_total`·`event_consumed_total`·`event_duplicate_total` 은 여전히 emitter 0 — 참조 알림 룰이 없어 본 티켓 범위 밖이나 **스펙은 live 처럼 적고 있어 같은 선언↔진실 축**, 후속 티켓 후보. **🔴 (동시 세션이 추가한 order-service 변경 + 그 주장은 `TASK-BE-545` 에서 반증·정정됨)**: 이 PR 에는 동시 세션이 `order-service` `UserWithdrawnEvent` 에 `@JsonIgnoreProperties(ignoreUnknown=true)` 를 추가한 커밋을 함께 넣으며 *"tenant_id 때문에 실 `UserWithdrawn` 메시지 전량이 `user.user.withdrawn.dlq` 로 직행했다"* 는 주장을 붙였다. **그 주장은 거짓이다** — 근거 테스트가 `new ObjectMapper()`(strict)를 썼을 뿐, 컨슈머가 실제 주입받는 Spring 자동설정 mapper 는 `FAIL_ON_UNKNOWN_PROPERTIES=false`(실 `OrderServiceApplication` 컨텍스트 bean 으로 실측: feature off, tenant_id envelope ACCEPTED). 실 메시지는 어노테이션 유무와 무관하게 항상 역직렬화됐고 DLQ 직행은 없었다. 어노테이션 자체는 형제 일관성·미래 strict-mapper 대비로 **유지**하되, 서사·회귀 테스트·이 노트는 `TASK-BE-545`(정정 티켓)가 바로잡았다. 분석·구현=Opus 4.8. [[feedback_guard_predicate_wrong_verify_the_artifact]] [[feedback_recount_population_dont_inherit_scope]] [[env_test_fixture_impossible_input_proves_nothing]] [[env_concurrent_session_task_id_collision]] | notification-service, user-service, order-service (infra/docs) | code, observability, docs |
| TASK-BE-537 | **DONE (2026-07-20, 3-dim verified — impl PR #2802 squash `9ce8b5271`; `state=MERGED`, `origin/main` 조상 확인, non-SUCCESS 체크 **0** — `Build & Test` + ecommerce 통합 A·B·C 실제 실행).** **🔴 재현됨 — 상태 기계는 순차 재생만 막는다. 동시 전이는 통과한다.** 조사 티켓(수정 티켓 아님). 실제 Testcontainers Postgres 에서 동시 `SHIPPED` 2건이 **둘 다** 인메모리 가드를 통과하고 **둘 다 커밋**, `ManualShipConfirmRequested` 아웃박스 **2행** 관측(`ConcurrentStatusTransitionIntegrationTest`, 4 tests/0 skipped/0 failures). **AC-0 재측정이 티켓 숫자를 정정했다** — `@Version` 은 shipping-service **0건**이 맞지만 저장소 나머지는 티켓의 *"10개 파일"* 이 아니라 **103개 파일**(known-positive `AsnJpaEntity` 포함 자기검증 완료). **AC-1 측정값**: 유효 격리 = `read committed` — 서비스 전체에 격리 설정이 0건이라 Postgres 기본값이 그대로이고, 이를 추론이 아니라 `updateStatus` 가 실제로 도는 트랜잭션 안에서 `SHOW transaction_isolation` 으로 직접 측정. 조회 경로에 `@Lock`/`FOR UPDATE` **0건** ⇒ 읽기가 잠금을 안 잡으므로 창(window)은 트랜잭션 전 구간. **F2(가짜 동시성) 방어**: 겹침을 sleep 이 아니라 **래치**로 보장 — 이미 주입돼 있던 `Clock` 심에 훅을 걸어 T2 가 `canTransitionTo` 를 통과한 **바로 그 시점**(`Shipping.java:87` 검사 → `:104` 시계 호출)에 래치를 내리므로, **래치가 내려갔다는 사실 자체가 "T1 미커밋 상태에서 T2 가 PREPARING 을 읽고 가드를 통과했다"는 증거**다. 직렬화됐다면 타임아웃 RED. 교착 불가(T1 은 래치만 대기하고 그 래치는 T2 가 차단 가능한 작업보다 먼저 내림). **🔵 피해 범위는 티켓 가정보다 작다 — wms 재고 이중차감은 일어나지 않는다**: 두 중복은 `partition_key` null → `aggregate_id`(=`orderId`)가 Kafka 키(`AbstractOutboxPublisher.java:116`) ⇒ 같은 파티션·순서 보장 ⇒ 두 번째는 saga 가 이미 `SHIPPED` 라 `ManualShipConfirmConsumer.java:181-184` terminal no-op(3차 방어 `ConfirmShippingService.java:176` `PACKED` 불변식). **⚠️ 이것을 막는 건 dedupe 가 아니다** — 두 발행이 각각 새 `UUID.randomUUID()`(`SpringShippingEventPublisher.java:100`)라 wms 의 `eventId` 중복제거는 **이 경우를 못 잡는다**; 실제로 막는 건 **saga 상태 가드 + 파티션 순서**. **🔴 방어 안 되는 쪽 = `ShippingStatusChanged`** — 역시 2건 발행되고 `ShippingStatusChangedEventConsumer` 는 그대로 발송으로 넘기며, 두 `event_id` 가 다르므로 어떤 `eventId` dedupe 도 못 잡는다 ⇒ **고객 배송 알림 2회**가 이 창의 유일한 실사용 피해. **의도적으로 고치지 않음** — `@Version` 은 경합을 없애는 게 아니라 실패 모드를 바꾼다(2번째 커밋 → `OptimisticLockException` → 지금은 **500 으로 누출**) ⇒ 최소 가드가 아니라 **에러 응답 계약 변경(409+에러코드 등록)+마이그레이션** 동반이고 계약이 구현보다 선행해야 함. 후속 티켓 필요. **테스트는 CHARACTERIZATION(2 를 고정)이지 회귀 가드가 아니다** — 가드 도입 티켓이 2→1 로 **뒤집어야** 하며 그때의 RED 가 곧 가드가 물었다는 증거. 소견 기록=`specs/services/shipping-service/architecture.md` § Domain Constraints. **권위 주의**: 로컬 Windows Testcontainers 는 권위 아님 — 같은 런에서 `ShippingIntegrationTest`/`OutboxRelay` 가 `ContainerLaunchException` 으로 떨어졌다가 격리 재실행 시 전건 GREEN(호스트 포화). **CI Linux 가 권위.** 분석·구현=Opus 4.8. [[env_test_fixture_impossible_input_proves_nothing]] [[feedback_recount_population_dont_inherit_scope]] [[project_testcontainers_docker_desktop_blocker]] | shipping-service | investigation, concurrency |
| TASK-BE-543 | **DONE (2026-07-20, 3-dim verified — impl PR #2801 squash `3122a4da2`; `state=MERGED`, `origin/main` 조상 확인, non-SUCCESS 체크 **0** — `Build & Test` + ecommerce 통합 A·B·C(Testcontainers) 실제 실행).** `payments.order_id`: 제약이 옳고 선체크가 틀렸다. 그리고 409 는 정답이 아니었다.** **AC-0 을 산문이 아니라 배선으로 확정** — F2 가 금지한 `ShippingRepositoryImpl:70`(*"orderId 는 전역 유일"*) 주석을 채택하지 않고 생성 지점을 따라갔다: `Order.java:83` 이 `UUID.randomUUID()` 로 **유일하게** 발급하고 테넌트 분할 스킴이 없으며, `:118 reconstitute` 는 인자를 받는 재수화라 생성 지점이 아니고, `OrderJpaEntity:21` 은 할당식 `@Id` 라 DB 가 값을 만들지 않는다 ⇒ **전역 UNIQUE(`V1:3`)가 옳고 테넌트 스코프 선체크가 좁았다.** **🔴 AC-1 = 409 기각, 404 채택** — Edge 1 이 옳았다. BE-542 백스톱이 이 경로를 409 로 만들지만 원인이 *남의 테넌트 주문 참조* 라면 **409 는 교차 테넌트 존재를 확인해 주는 응답**이다(M3 는 은닉 요구). 404 `PAYMENT_NOT_FOUND` 는 이 엔드포인트가 *"그런 결제 없음"* 에 이미 쓰는 코드라 **두 경우가 호출자에게 구별되지 않는다.** **🔴 선체크만으로는 가드가 아니다(동시성)** — 두 요청이 모두 전역 선체크를 통과한 뒤 한쪽이 먼저 커밋하면 패자는 제약에 걸려 **409 를 받는다 = 404 로 숨기려던 존재가 그대로 샌다.** 진짜 중재자는 제약이므로 생성 경로가 위반을 **직접 같은 404 로 번역**한다. **`saveAndFlush` 가 필수** — `PaymentJpaEntity:17-19` 는 할당식 `@Id` 라 plain `save()` 의 INSERT 가 커밋까지 지연돼 **catch 가 죽은 코드**가 된다(`TASK-BE-541` 카탈로그). 포트에 `saveAndFlush` 신설. **부수 변경**: `incrementPaymentCreated()` 를 저장 성공 이후로 이동 — 새 거부 경로가 생겨 그대로 두면 거부된 요청이 "생성됨" 으로 계수된다. **선행 상태 정정**: 티켓 본문이 BE-540 을 *"review/ · close chore 대기"* 로 적었으나 **`eaed87080` 로 머지돼 `done/` 에 있다** ⇒ AC-0·F3 대로 done 기록이 우선. 대조 결과 `user_id` 축에서 **두 기록은 모순 없음** — F1·F3 미발생. **AC-2 = `wishlist_items` 분리 → `TASK-BE-546` 신규 filing**(도달 불가라 우선순위 낮음 + PR 을 payment 범위로 유지 + 다른 task 가 `user-service` 동시 편집 중이라 직렬화 규율). **AC-3**: 이 판정이 payment 백스톱 **결정적 경로를 1개 줄인다** — `MONO-450` 입력으로 기록. **로컬 실측**: payment 단위 **212 tests / 0F 0E 0S**(`--rerun-tasks`, XML 대조), 신규 2건이 XML 에 **실제 실행 기록으로** 존재함을 개별 확인. **가드 물림 확인**: 선체크 제거 → `never()).saveAndFlush` 파손 / catch 제거 → DIVE 가 그대로 올라와 단언 파손. **🔴 정직한 한계**: 동시 케이스는 DIVE 를 스텁한 **단위** 테스트라 *제약이 실제로 그 시점에 그 예외를 낸다* 를 증명하지 않는다 — 그 전제는 BE-542 가 CI Linux 에서 확인한 23505 전달에 기댄다. 로컬 Windows Testcontainers 는 FLAKY, **CI Linux 가 권위.** 분석=Opus 4.8 / 구현=Sonnet+Opus 4.8. [[feedback_guard_predicate_wrong_verify_the_artifact]] [[env_shared_issuer_authenticated_is_not_authorized]] [[env_test_fixture_impossible_input_proves_nothing]] | payment-service, order-service (측정) | tenancy, constraint, adjudication |
| TASK-BE-538 | **DONE (2026-07-20, 3-dim verified — impl PR #2800 squash `ab72509ef`; `state=MERGED`, `origin/main` 조상 확인, non-SUCCESS 체크 **0**).** ADR-002 D3 판정 완료: (A)+(C), (B) 기각.** **AC-0 전수 재측정이 이 티켓의 제목 전제를 반증했다** — 모집단은 20 이 아니라 **27**(컨트롤러 `**/*Controller.java` 전수 열거, 키워드 검색 아님), 분포는 `0/1/13/6` 이 아니라 **`4 ENFORCED / 2 OPTIONAL / 19 NATURAL-KEY / 2 NONE`**. **문자적 만족이 0 이 아니라 4 다**(payment refund · coupon issue · `POST /admin/products` · `PATCH .../stock` — 전부 BE-535/536 산물). 넷 다 **컨트롤러 바인딩은 `required=false`** 이고 서비스가 `IdempotencyKeyRequiredException` 으로 하드 거부 ⇒ **탐지 지점을 컨트롤러 애노테이션에 두면 전건 OPTIONAL 로 오분류**한다. **🔴 § Edge 3 의 중심 주장이 무효화됐다** — Edge 3 은 *"중복 거부의 종착지가 대부분 500, ecommerce 9개 서비스가 DIVE 핸들러 미보유"* 라 적었으나 **지금은 10개 서비스 전부 보유**(`TASK-BE-542` 배선) ⇒ **27건 중 500 유출 0건**. Edge 3 표의 `LEAKS-500`/`RACE-ONLY→500` 판정은 전부 stale. 중간 조사가 `PATCH /variants/{id}` 를 500 LEAK 로 보고했으나 `product-service GlobalExceptionHandler:197` 에 핸들러가 존재함을 직접 확인해 **NATURAL-KEY→409 로 정정** — `saveAndFlush`+catch 대 plain `save` 의 실제 차이는 *409 vs 500* 이 아니라 **도메인 특화 409 대 일반 409** 다. **(B) 기각 근거**: BE-535/536 의 선택에 이미 기준이 있다 — 키는 **서버가 재시도와 정당한 반복을 구별 못 할 때만**(부분환불·재고 증감) 사고, 신원이 결정되면 자연키(정산 윈도우), 전이가 결정되면 상태기계(주문취소·결제승인·쿠폰사용). `POST /orders/{id}/cancel` 에 키를 붙여도 얻는 게 0 ⇒ 19건 중 최소 17건에 순수 비용. **(C) 채택 근거(Edge 4 적중)**: 20→27 변화에 **엔드포인트 추가는 0** — 순전히 *"금전·재고 변경 API"* 경계 해석 차이이고, 두 독립 열거가 **같은 부류**(요율·가격 설정, 할인 규칙 CRUD, `POST /payments`, 배치 스윕)에서 판단을 유보했다. 범위를 안 고치면 다음 인구조사도 또 다른 수를 센다. **AC-2 제안 문장 = 확정형**: 메커니즘 **4종 대등**(상태기계 / 자연키 UNIQUE **단 응답 시점 4xx 변환 필수** / 전체 치환 — **증분 연산 불가** / 클라이언트 키 — **구별 불가 시 필수, 선택적 키는 불만족**) + 적용 범위를 (a) 잔액·지급·환불 (b) 재고·예약 (c) 쿠폰 발급·사용 (d) **이들을 산출하는 파라미터**로 열거, **컨슈머는 D1·D2 로 이관**. **AC-3 = 27건 전부 대조, 3건 거부**(`POST /orders` 선택적 키 · carrier-webhook blank-key 우회 · `POST /promotions` 재요청이 두 번째 할인 규칙 생성) ⇒ **Edge 2 통과: 서술이 아니라 규칙이고, 옳은 코드를 영구 위반으로 남기지 않는다.** `PATCH /admin/products/{id}` 는 절대값 치환이라 의도적 통과(거부하면 정상 REST 갱신 전부가 위반 = Edge 2 의 반대편 과오). **AC-4**: 개정 task 는 **ACCEPT 게이트 — 맨 "진행" 불가, self-ACCEPT 금지**, 사용자 정확형 의사표시 선행. 문장이 확정형이라 개정은 재작성이 아니라 **적용**. **파생 4건**(promotion 생성 무방비 · `POST /orders` 키 선택적 · webhook blank-key · **`PaymentRefundService.refundPayment(String)` 컨슈머 자금유출**). **🔴 이 재측정의 사각지대 = 컨슈머 쓰기 경로 0건 측정** — `product-service` 재고 예약은 HTTP 엔드포인트가 **아예 없고** 컨슈머만 쓴다. 재고 멱등성 감사는 REST 층이 아니라 컨슈머 층. 코드·ADR 본문 diff **0**. **🔴 AC-4 정정(커밋 후 발견)** — 티켓 본문 § AC-4 가 후속 개정 task 를 `TASK-BE-544`(가칭) 로 적었으나 **그 번호는 이미 점유됐다**(ADR-MONO-052 후속, PR #2797 / 브랜치 `chore/file-mono455-be544`). 개정 task 는 **착수 시점에 미사용 번호를 재배정**하고, 번호 확정 전 `gh pr list --state all` + `tasks/**` 를 함께 확인할 것. `review/` 는 HARDSTOP-05 로 동결이라 본문이 아니라 이 행에 기록한다. 분석·구현=Opus 4.8. [[feedback_recount_population_dont_inherit_scope]] [[env_empty_detector_output_is_not_absence]] [[project_adr_accept_gate_exact_intent]] | (문서/ADR 판정) | adr, idempotency, governance |
| TASK-BE-534 | **DONE (2026-07-20, 3-dim verified — impl PR #2799 squash `2e7cb9327`; `state=MERGED`, squash commit 이 `origin/main` 조상임 확인, 머지 시점 non-SUCCESS 체크 **0**).** ADR↔구현 드리프트 감사 산물 ③. 프로젝트 ADR-003(frontend dual strategy)이 `Accepted` 인 채 방치 — ADR-MONO-031(ACCEPTED·집행완료)이 admin-dashboard 를 sunset 시켰고 스펙엔 RETIRED 마커가 있으나 **ADR 레벨 마커만 없어 SoT 사슬이 한 링크 끊김**. + ADR-004 의 `multi-tenant` 미선언 주장이 `PROJECT.md` 와 불일치(ADR-MONO-030 결과). AC-0 재검증 완료(4전제 전부 유지) — ADR-003 `Superseded by ADR-MONO-031`(admin-dashboard 절반만, web-store FSD 유효) + README 행 갱신 + ADR-004 trait 예시 정정(ADR-MONO-030 인용). | docs/adr | docs |
| TASK-BE-539 | **DONE (#2770, `47746f658`) — 인덱스가 코드의 쓰기 모양과 어긋나 정상 최초 전송이 거부됐다.** `uq_notifications_event_id`(`V4:3`)는 `event_id` 당 1행을 강제하는데 `NotificationSendService:60` 은 **발송 가능 채널마다 1행**을 같은 `event_id` 로 쓴다 ⇒ 커밋 flush 에서 2번째 채널이 위반 → 전체 롤백 → 재시도마다 `existsByEventId`(:52) 가 여전히 empty → 3회 소진 → **DLQ, 발송 기록 없이 유실**. 수정=`V8` 이 `(tenant_id, event_id, channel)` 로 재정의. **AC-0 재측정이 티켓 서술을 두 곳에서 바꿨다**: (a) enum 은 **3개**(EMAIL·SMS·PUSH)지만 센더는 2개라 SMS 는 `:74` 에서 스킵 — 오늘 2행인 이유는 열거 크기가 아니라 **센더 등록 여부**다; **(d) 티켓은 "관리자가 만들 때까지 잠재" 로 봤으나 fed-e2e DB 실측 결과 `PAYMENT_COMPLETED` 가 이미 EMAIL+PUSH 로 시드**돼 있고, 33행 전부 EMAIL·PUSH 0행 — 발송가능 채널이 둘인 유일한 타입이 알림 0인 유일한 타입. **단 이것을 "이미 발화했다"로 읽지 않았다** — 해당 로그창에 DIVE 0건·PAYMENT_COMPLETED 소비 0건이라 "이벤트가 안 왔다"로도 똑같이 설명된다. 확정된 것은 **발화에 필요한 설정이 이미 갖춰져 있고 부족한 것은 관리자 조치가 아니라 트래픽뿐**이라는 더 좁은 명제. **AC-2 RED 실측**(run 29725001878, ecommerce A): 신규 3건이 전부 `duplicate key value violates unique constraint "uq_notifications_event_id"` 로 실패했고 **그 외 실패 0건**. AC-4 의 RED 가 F2(교차 테넌트 충돌)를 논증이 아니라 **측정**으로 확인했다. **스펙이 자기모순이었다**: overview invariant 2 는 *"channel-별 독립 적용"* 으로 팬아웃을 전제하는데 invariant 1 의 *"한 번만 발송"* 은 행 하나로 읽힌다 — **V4 는 틀린 쪽 절반을 구현**한 것. 두 문장 모두 `(tenant_id, event_id, channel)` 로 명시 정정. **AC-5**: 무손실을 가정하지 않고 V8 이 중복 그룹을 먼저 세어 `RAISE EXCEPTION` (옛 인덱스로부터의 연역이지 이 DB 에 대한 관측이 아니므로). Edge 4: notification 에 `migration-h2` **없음**. 로컬(여유 0.84 GiB·컨테이너 43) 은 판정 자격 없음 — **CI Linux 가 권위**. 출처=`TASK-BE-538` Edge 3. 분석·구현=Opus 4.8. [[env_empty_detector_output_is_not_absence]] [[project_guard_reachability_not_just_bite]] **머지 검증 3차원**: `state=MERGED` / `origin/main` tip=`47746f658` / 머지 전 실패 체크 0(12/12 pass). **🔴 마지막 RED 는 결함이 아니라 내 테스트였다** — 2차 런에서 3건 중 2건이 GREEN 으로 전환되고 AC-4 만 남았는데 실패 종류가 `DataIntegrityViolationException` → `AssertionError: expected 1 but was 2` 로 **바뀌었다**(=제약 위반은 이미 사라짐, V8 은 작동). 원인은 275행이 `otherTenant` 가 아니라 **default 테넌트** 단언이었고 같은 클래스의 형제 테스트가 default 에 PUSH 템플릿을 이미 시드해 2행이 된 것 — **픽스처가 실행 순서에 의존**했다. 기대값을 2로 바꾸는 건 여전히 형제 의존이므로, 두 테넌트에 EMAIL+PUSH 를 명시 시드하고 **채널 집합**을 단언하도록 교체 — 순서 무관이고 원래보다 강한 것(팬아웃이 전역 1회가 아니라 **테넌트별**로 일어남)을 증명한다. **최종 확인**: `Migrating schema "public" to version "8 - fix event id unique index shape"` 가 런 로그에 존재(=적용 실증), 실패 0.  | notification-service | data-loss, index, kafka |
| TASK-BE-540 | **DONE (#2780, `9112d5ff8`) — AC-0 이 두 사례를 갈랐다: A 도달가능(수정), B 도달불가(정합성으로 강등).** **A(notification push 구독)**: endpoint 는 (브라우저, origin, VAPID키)의 함수인데 실측상 VAPID 키페어가 `app.notification.push.vapid.*` **전역 1개**, 스토어프런트 origin 이 `web.ecommerce.local` **1개**, 테넌트는 호스트명이 아니라 **JWT 클레임** ⇒ **같은 브라우저가 다른 테넌트 사용자로 로그인하면 endpoint 가 동일**. 전역 `findByEndpoint` 가 남의 행을 찾아 `updateKeys` 로 **A 의 키를 회전**시키고 200 반환, INSERT 분기 미도달이라 `uq_push_subscriptions_tenant_endpoint` 는 영영 안 걸린다. **AC-1 판정=테넌트별(제약이 옳고 조회가 틀림)** — 브라우저가 두 테넌트 신원으로 각각 구독하는 건 정당하므로 `(tenant, endpoint)` 당 1행이 맞다. 전역 UNIQUE 로 되돌리면 **지금 버그를 사양으로 승격**시키는 것. **B(review)**: 도달 불가 — `product_id`=제품별 랜덤 UUID(한 테넌트 소유), `user_id`=전역 유일 + `V4:9` 가 *"tenant_id 는 격리 컬럼이지 식별 키의 일부가 아니다"* 명시. 500(BE-542 이후 409)을 낸 적이 없다 ⇒ 티켓 지시대로 **정합성 결함으로 재분류**, `V7` 로 정렬만. **🔴 AC-2 mutation 이 내 주장을 반증했다** — RED 2건 예측, **1건만** 물었다. 해지 IT 는 수정 전에도 통과 = **물 기회 없는 가드**. 이유는 `unregister` 의 **소유자 필터**가 이미 막고 있어서 ⇒ *"테넌트 B 의 해지가 A 의 행을 지운다"* 는 **HTTP 경로에 대해 거짓**. 진짜 삭제 결함은 **소유자 필터가 없는 send-path prune**(`WebPushSender`) — `deleteByEndpoint` 가 그 endpoint 의 **모든 테넌트 행**을 지웠다. 이 절반은 **테넌트 스코프로도 못 고친다**(prune 은 Kafka 스레드라 `TenantContext` 없음 → 기본 테넌트로 해석) ⇒ **정체성 기반 삭제**. 해지 IT 는 삭제 않고 **재는 것으로 재명명**(본문은 소유권을 재면서 이름이 테넌트 격리를 주장하는 게 더 비싼 실수), prune 은 `same(dead)` 로 **행 정체성** 단언(endpoint 문자열 단언이면 endpoint 삭제도 통과했다). **정직한 한계**: prune 테스트는 단위이고 현 시그니처상 endpoint 삭제는 컴파일 불가라 mutation 으로 물림 증명 못 함 — **재도입 방지 회귀 가드로만** 주장. **포트 javadoc 이 없는 스코핑을 주장**했다(*"register/unregister 는 TenantContext 로 스코프된다"* — 둘 다 안 씀). **BE-542 백스톱 인계 처리**: 저쪽이 javadoc 에 *"BE-540 이 인덱스를 재구축하면 이 테스트는 바뀌어야 한다"* 고 남긴 대로 트리거를 **불가능한 교차 테넌트 상태 → 실제 경합 창**(구매검증 목 안에서 경쟁 행 커밋=선체크와 save 사이)으로 교체, 목적(실제 23505→409)은 보존. **AC-4 훑기(고치지 않고 목록)**: 신규 2건 — `payment-service payments`(선체크 `findByOrderIdAndTenantId` ⊂ 제약 `order_id` 전역 `V1:3`) · `user-service wishlist_items`(선체크 3열 ⊂ 제약 `(user_id, product_id)` `V3:8`; `V4:6` 이 *"unique constraints are unchanged"* 라고 **적어두고** 방치). **둘 다 도달성 미측정** — 마이그레이션 주석의 *"충돌 불가"* 는 증거가 아니라 채택 안 함. 무근거 선체크 2건 — `product-service sellers.account_id` · `settlement-service commission_accrual`. BE-539 인덱스는 선체크보다 넓지만 **결함 아님**(선체크=채널 루프 앞 이벤트 게이트, 제약=채널당 1행 — 다른 불변식). **곁가지(후속 후보)**: `OrderServiceClient:24` 구매 검증이 `X-Tenant-Id` 를 **전파하지 않아** 비-기본 테넌트에서 기본 테넌트 주문을 조회한다. **AC-5**: `migration-h2` 는 product-service 에만 — 해당 없음. 마이그레이션 2개 모두 무손실 가정 없이 중복 그룹을 세어 `RAISE`. 로컬(0.70 GiB) 판정 불가, CI Linux 권위. 분석·구현=Opus 4.8. [[project_guard_reachability_not_just_bite]] [[env_test_fixture_impossible_input_proves_nothing]] [[feedback_guard_predicate_wrong_verify_the_artifact]] | notification-service, review-service | tenancy, constraint, adjudication |
| TASK-BE-532 | **DONE (2026-07-20, 3-dim verified — impl PR #2783 squash `cb933ed7f`; 머지 전 failing **0** / pass **10**, `state=MERGED`, `origin/main` tip 대조 완료, done/)** — `notification_delivery` 그룹의 **영구 no-data 알림 2건 제거**(거짓 커버리지). **AC-0 실측: 그룹 3룰 중 2룰 무-emitter 확정 삭제, 1룰(`NotificationSenderDown`) 존치** — 후자는 앱 코드가 아니라 **Prometheus 자신의 `up{}` 스크레이프 지표** 기반이라 no-data 가 아니다(§Edge Cases). **빈 grep 을 부재의 증거로 승격시키지 않았다**: 같은 패턴을 `user-service/UserMetrics.java` 에 걸어 known-positive 자기검증(F2 가드). Grafana 대시보드 4종·`infra/prometheus/**` 역참조 0건 확인 후 삭제. `observability.md` 는 두 지표를 **planned(TASK-BE-533)** 로 정정(F3 가드). `promtool` 부재 → PyYAML `safe_load` 파싱 검증(6그룹, `notification_delivery`=1룰). **🔴 삭제가 남긴 거짓 문장 1건 추가 수정**: 그룹 헤더 주석이 *`notification_failed_total{...} 기반`* 이라 계속 선언 — 정작 그 지표를 쓰던 룰이 사라져 **산출물은 옳고 문장만 틀린** 상태였고, 이 축을 보는 CI 술어는 없다. **🔵 범위 밖 관찰(BE-533 입력)**: `observability.md` 의 `notification_retry_total`·`event_consumed_total`·`event_duplicate_total` 도 emitter 0 — 참조하는 알림 룰이 없어 이 티켓 범위 밖. 분석=Opus 4.8 / 구현=Sonnet 4.6. [[feedback_guard_predicate_wrong_verify_the_artifact]] | notification-service (infra) | docs, observability |
| TASK-BE-542 | **DONE (2026-07-20, 3-dim verified — impl PR #2775 squash `7de3a637b`; 머지 전 failing **0** / pass **12**, `state=MERGED`, `origin/main` tip 일치).** **🔵 CI 가 로컬이 증명 못 한 것을 증명했다** — ecommerce 통합 A·B·C 전건 통과에 Layer-2 IT 가 포함되므로, SQLState `23505` 가 Hibernate → Spring → 핸들러까지 실제로 전달된다는 **판별식 전체가 기대던 가정**이 CI Linux 에서 확인됐다(로컬 통과는 FLAKY 호스트의 단일 표본이었다). `Error code registry` 도 통과 = 8곳 신규 방출이 레지스트리와 정합. **[선별 매핑 8곳 배선]** `platform/error-handling.md:137` 이 `DATA_INTEGRITY_VIOLATION`(409)을 함대 catch-all 로 선언하는데 ecommerce **8개** 서비스가 미구현이었다(티켓의 "9곳"은 오산 — `search-service` 는 JPA 리포지터리 0 이라 필요 없음). **🔴 AC-0 가 이 티켓의 프레이밍을 반증했다 — "ecommerce 만 straggler" 가 아니라 표준이 아예 없다**: 홀더 5곳이 코드 **3종**(wms·scm·fan 4개 = `CONFLICT` / finance = `CONCURRENT_MODIFICATION` / ecommerce user = `DATA_INTEGRITY_VIOLATION`)이고, 레지스트리가 정경으로 적은 코드는 **5곳 중 1곳만** 낸다. **선별 방식은 함대에 전무했다.** **🔴 첫 판별식 안이 물 수 없는 가드였다**: `spring-orm 6.2.1` 바이트코드상 Hibernate `ConstraintViolationException` → **평범한 DIVE**(오프셋 267→278)이고 `DuplicateKeyException` 은 `NonUniqueObjectException` 에서만 생성(380→386) ⇒ **예외 타입은 판별식이 될 수 없다.** 채택 = cause 체인의 **SQLState `23505`**(웹 계층에 Hibernate 미유입 + 메시지 문자열 매칭 회피). 배선 = unique → **409 `DATA_INTEGRITY_VIOLATION`** / FK·NOT NULL·CHECK → **500 유지 + `log.error`**(각 서비스 기존 catch-all 과 바이트 동일 ⇒ 비-unique 거동 불변). **AC-5 두 층**: Layer1 = `MockMvc`+`setControllerAdvice` 8곳, 애노테이션 임시 제거로 **가드가 무는지 실측** — 단 `23503→500` 케이스는 핸들러가 없어도 통과하므로 **배선 증거가 아니라 과잉매핑 방지 non-regression** 이다. Layer2 = 실 DB 1건. **🔴 백스톱의 정직한 범위 = 결정적 경로 1개 + 나머지는 경합 시에만** — 7개 서비스는 지역 catch·선체크가 이미 선점한다. 실질 수확은 `JpaWebhookDeliveryStore` 의 동시중복 500 → 409(`TASK-BE-541` 이 "이 계층에서 못 고친다"고 명시한 건). **🔴 이 배선이 `TASK-BE-540` 을 조용하게 만든다**: Layer2 의 결정적 경로가 **BE-540 사례 B 그 자체**라 그 티켓의 "도달성 미측정"이 **도달 가능으로 확정** 이라고 내가 적었으나 **과장이었다(정정: `TASK-BE-543`)** — 픽스처가 인위적이라 **23505 전달만 증명**됐고 프로덕션 도달성은 미측정, 증상이 500→409 로 바뀌어 **알림에서 사라진다** — BE-540 §F1 이 경고한 시나리오를 알면서 밟은 것이다. **백스톱이 540 을 가려 준다고 읽지 말 것.** 540 이 인덱스를 테넌트화하면 이 IT 는 바뀌고 함대의 결정적 백스톱 경로는 **0** 이 된다. **AC-4**: 선별은 `error-handling.md:137` 의 "Generic DB constraint violation" 과 어긋나 공유 파일을 여기서 고치지 않고 루트 **`TASK-MONO-450`** 을 별도 spec PR 로 세웠다(코드 3종 단일화 + 판별식 8벌 `libs` 승격 판정 포함). 로컬 XML: 8서비스 **1,600** 단위 F0 E0 S0 + IT 2건, **8/8 신규 Layer-1 클래스가 `tests=2` 로 존재함을 개별 확인**(레인 초록만으로는 내 파일이 실행됐다는 증거가 못 된다). **한계**: Layer2 는 **단일 로컬 표본**이고 호스트 Testcontainers 는 FLAKY — **CI Linux 가 권위**. ecommerce 는 수렴 전까지 **가장 올바른 상태가 아니라 가장 일관되지 않은 상태**이며 `TASK-MONO-450` 이 그 부채의 소유자다. 분석·구현=Opus 4.8. [[project_enforcement_straggler_sibling_parity]] [[feedback_guard_predicate_wrong_verify_the_artifact]] [[feedback_recount_population_dont_inherit_scope]] | ecommerce 8개 서비스 | error-handling, sibling-parity, adjudication |
| TASK-BE-541 | **DONE (2026-07-20, 3-dim verified — impl PR #2766 squash `b6e46d442`; 머지 전 failing **0** / pass **12**(ecommerce 통합 A·B·C Testcontainers 포함), `state=MERGED`, `origin/main` tip 일치).** **[죽은 중복 방어 catch 제거 + HTTP 도달 2건 수정]** **AC-0 실측: 13 지점(티켓 추정 "6곳 이상"의 두 배), 죽음 7 / 삶 6.** **🔴 판별식이 티켓과 달랐다** — `save` 대 `saveAndFlush` 라는 *이름*이 아니라 **`@Id` 생성 전략**이 공동 결정 인자다(`IDENTITY` 면 plain `save` 라도 catch 가 산다). 대상 dedupe 엔티티는 전부 **할당식 `@Id`** 라 지연 확정, 엔티티를 직접 열어 확인. **🔴 훑기가 티켓의 처방을 뒤집었다**: "죽은 곳을 전부 살린다"는 대부분 틀린 답 — **catch 후 `throw`(중단)** 은 살릴 수 있지만 **catch 후 `return`(계속 진행)** 은 못 살린다(flush 된 위반이 세션을 rollback-only 로 만들어 커밋에서 다시 실패). ⇒ `OrderPlacementService` 만 `saveAndFlush` 로 **살려** 동시 동일 멱등키 500 → **409** 복구(`OrderRepository.saveAndFlush` 포트 신설), dedupe 5곳 + `JpaWebhookDeliveryStore` 는 **catch 를 제거**하고 진짜 중재자(**재시도**)를 문서화 — 결과는 이미 옳았고 catch 만 거짓이었다. `REQUIRES_NEW` 는 각 클래스가 의도한 원자성을 깨서 불채택. **별건 수정**: `POST /admin/products` 가 요청 내 동일 `optionName` 을 **저장 전에** 409 로 거부(충돌이 페이로드에 다 보이므로 DB 에 맡길 이유 없음; 술어는 제약과 같게 대소문자 구분). **거짓 주석 정정**: `VariantManagementService` 가 존재하지 않는 *"in-memory addVariant() check"* 를 근거로 삼고 있었다. **불가능 전제 제거(AC-4)**: `OrderPlacementServiceTest` 는 `save`→`saveAndFlush` 스텁으로 전제를 실재화, `EventDeduplicationCheckerUnitTest` 동시-INSERT 케이스는 **삭제**(트랜잭션 경계는 Mockito 로 표현 불가 — 대체 유닛 테스트는 같은 거짓 확신 재생산). **AC-2 수정 전 RED 확인**: 가드 비활성 상태에서 정확히 1건만 실패, 대소문자 테스트는 그때도 통과(술어 과잉 아님 동시 확인). 로컬 단위 XML: product **364** / order **381** / shipping **185** / settlement **131**, F0 E0 S0, `--rerun-tasks`. **🔴 정직한 한계**: 동시성 IT 없음(로컬 Testcontainers FLAKY) — 409 복구는 논리로 확인했지 실 DB 경합으로 증명 못 했다. `JpaWebhookDeliveryStore` 의 500 은 **그대로**이고 제거한 건 그걸 막는다는 *주장*뿐(4xx 번역=`TASK-BE-542`). 분석·구현=Opus 4.8. [[env_test_fixture_impossible_input_proves_nothing]] [[feedback_guard_predicate_wrong_verify_the_artifact]] [[feedback_recount_population_dont_inherit_scope]] | order-service, product-service, shipping-service, settlement-service | concurrency, dead-code, test-integrity |
| TASK-BE-536 | **DONE (2026-07-20, 3-dim verified — impl PR #2754 squash `65e50b29b`; 머지 전 failing **0** / SUCCESS **27**, `state=MERGED`, `origin/main` tip 일치, 두 마이그레이션 UNIQUE 제약을 main 에서 read-back).** **[재고/쿠폰 4개 중복요청 가드 — ADR-002 D3 인구조사 NONE 6/6 종결]** **🔴 AC-0 재측정이 티켓 표를 반증했다** — 티켓은 `POST /variants` 를 *"no uniqueness on optionName"* 이라 적었으나 `V5__add_fk_on_delete_and_unique_constraints.sql:14` 에 `uq_product_variants_option UNIQUE (product_id, option_name)` 이 **이미 있었다.** 진짜 결함은 가드 부재가 아니라 **가드가 깨끗한 에러로 배선되지 않은 것**(plain `save()` → child INSERT 가 커밋으로 밀려 catch 를 지나침 → **409 아닌 raw 500**). ⇒ 마이그레이션 추가 없이 `saveAndFlush` + DIVE→409 `DUPLICATE_VARIANT_OPTION` 번역만. **티켓을 믿었으면 있는 제약 위에 두 번째를 얹었다.** **메커니즘(AC-5, 혼합)**: variants=natural key / `PATCH /stock`·`POST /products`·`coupons/issue`=`Idempotency-Key` 필수. 재고는 *"+10 두 번"이 둘 다 정당한 창고 이벤트*라, 상품은 *동명이 정당*이라, 쿠폰은 *같은 유저 재발행이 정당하고 `validateCanIssue` 가 총량만 캡*이라 natural key 불가. 헤더는 함대 표준 `Idempotency-Key`(신설 0), 코드도 기존 `IDEMPOTENCY_KEY_REQUIRED`/`CONFLICT` 재사용. dedupe 3종은 BE-535 `payment_refund_request` 형태(**`saveAndFlush`** + 선점 insert 를 변경 이전 = AC-4 arbiter + TTL 없음). **AC-3(F2)**: 리플레이는 mutating 경로를 건너뛰어 `StockChangedPayload` 재발행 0. **크로스프로젝트 원자**(콘솔 12파일 동일 커밋). **🔴 CI 가 로컬이 못 본 결함을 잡았다** — 로컬 IT 는 `postgres:16-alpine ContainerLaunchException` 으로 전건 실패해 *인프라*로 정확히 분류했는데, **그 인프라 장애가 진짜 코드 결함을 가리고 있었다**: CI Linux 에서 `MultiTenantIsolation`·`SellerScopeIsolation` 9건이 `expected:<201> but was:<400>` — 두 IT 헬퍼가 새 필수 헤더를 안 보냈다. **단위 레인 362/0/0 은 이걸 못 잡는다**(가드가 서비스 계층이라 서비스를 목킹하는 슬라이스는 통과). 수정 시 **실패한 둘만 고치지 않고 모집단 5개를 전수 재측정**(1개는 이미 처리됨, 2개는 슬라이스라 불필요). **⇒ 'flake=인프라' 는 가설이지 결론이 아니다 — 인프라 실패 아래 코드 결함이 있을 수 있다.** **🟡 알려진 한계(후속)**: 콘솔이 키를 호출 지점마다 새로 만들어 **더블클릭은 키가 둘이라 둘 다 통과** — 티켓이 주 위협으로 지목한 경로다. `pending` 비활성화가 순진한 더블클릭은 막고 백엔드 가드는 at-least-once 잡·안정 키 클라이언트에 도달하므로 '물 기회 0' 은 아니나, 키를 폼 단위로 올리려면 리셋 시점 설계가 필요해 범위 밖으로 **명시**했다. 최종 실측: product **362**/promotion **88** 단위 0F0S, 콘솔 vitest **2852**, lint clean, registry GREEN, CI 27 SUCCESS. 분석=Opus 4.8 / 구현=Sonnet 4.6. [[feedback_recount_population_dont_inherit_scope]] [[env_ci_flake_is_a_hypothesis_not_a_verdict]] [[project_guard_reachability_not_just_bite]] | product-service, promotion-service, console-web | idempotency, stock, coupon |
| TASK-BE-535 | **DONE (2026-07-20, 3-dim verified — impl PR #2747 squash `090b2dd46`; 머지 전 failing **0** / SUCCESS **27**, `state=MERGED`, `origin/main` tip 일치, 두 마이그레이션 제약을 main 에서 직접 read-back).** **[중복 요청이 실제 돈을 두 번 움직이던 money 엔드포인트 2건 폐쇄]** ADR-002 D3 멱등 인구조사(20 중 NONE 6)의 money 2건. **① 부분환불 이중지급** — `Payment.refund(long)` 이 검증 후 `refundedAmount += amount` 누적. 핵심 난점 = 중복이 **정당한 두 번째 부분환불과 바이트 단위로 구분 불가**라 서버측 natural key 로는 원리적으로 못 푼다 ⇒ 클라이언트 `Idempotency-Key` **필수화**(없으면 400 `IDEMPOTENCY_KEY_REQUIRED`). order-service 의 `required=false` 는 인구조사가 OPTIONAL(=구멍)로 분류한 그 선택이라 funds-out 에서 반복하지 않았고, **전수 grep 으로 in-repo 호출자 0 을 확인한 뒤** 필수화했다. 무인자 `refund()`(OrderCancelled) 는 무변경 — REFUNDED 조기반환으로 이미 멱등. **그 비대칭이 진짜 함정이었다: 그 메서드를 읽은 리뷰어는 "환불은 멱등"이라 결론내고 자기가 읽은 메서드에 한해 옳다.** **② 정산기간 중복 OPEN** — `close` 가 accrual 을 **변경 없이 read-only fold** 하므로 같은 윈도우 OPEN 둘 → 셀러당 이중 지급. **V6 부분(partial) unique** `(tenant_id, period_from, period_to) WHERE status='OPEN'` → 409 `PERIOD_ALREADY_OPEN`. **부분인 이유** = 닫힌 뒤 같은 윈도우 재개방(정정 재실행)은 정당. **클라이언트 키가 아니라 DB 제약인 이유** = 라이브 콘솔 호출자가 키를 안 보내고 윈도우 자체가 좋은 natural key(호출자 무변경). **진짜 overlap 은 의도적 미가드** — Javadoc 이 "a tenant may run overlapping windows" 로 설계임을 명시 ⇒ 뒤집기는 ADR 게이트(HARDSTOP-09). 사용자 결정으로 exact-dup 만 채택, IT `overlapping_but_not_identical_window_is_still_allowed()` 가 경계를 단언. **동시성(AC-5) = DB 제약이 심판** — read-then-write 는 둘 다 통과시키므로 unique 제약이 arbiter 이고, **키 선점 insert 를 PG 호출·금액변경보다 먼저** 배치해 레이스 패자가 돈에 도달 못 한다. **`saveAndFlush`(plain `save` 아님)** — 지연 insert 면 위반이 커밋 시점에 터져 **catch 와 PG 호출을 지나친다**. **fail-closed** = dedupe 저장소가 같은 트랜잭션 안 자기 Postgres(Redis 아님)라 fail-open degrade 모드가 없다. **retention 없음**(만료가 구멍을 다시 연다). **🔴 형제 대조가 외톨이 이름을 잡았다** — 신설 코드를 `IDEMPOTENCY_KEY_REUSED` 로 냈다가, 같은 의미를 **fintech·saas·erp 3도메인 4섹션이 이미 `IDEMPOTENCY_KEY_CONFLICT`** 로 쓰는 것을 확인해 함대 이름으로 개명(새로 도입되는 참 = 고치기 가장 싼 시점). **🔴 `BUILD SUCCESSFUL` 을 네 번 믿지 않았다** — ①`UP-TO-DATE` ②`FROM-CACHE` ③메서드명 grep 0건(실제로는 `@DisplayName` 이라 XML `name` 이 한국어) ④or-연산자를 파이프라인에 붙여 known-true 자기검증이 무효화. **매번 산출물은 옳았고 술어가 틀렸다.** 최종 실측(`--rerun-tasks --no-build-cache` + test-report XML): payment 단위 **208**/IT **22**, settlement 단위 **131**/IT **20**, 전부 0 실패 0 스킵. **정산 IT 5클래스 전건 실패를 XML 이 오진에서 구했다** — V6 가 Flyway 를 깬 줄 알았으나 진짜 메시지는 `Could not find a valid Docker environment`(Flyway 실행 *전*, 알려진 Windows Testcontainers flake), 재시도 통과. 계약 선행 갱신 + 3코드 등록, registry GREEN. **후속(별 티켓)**: `platform/error-handling.md:413` 이 `PERIOD_WINDOW_INVALID` 를 *"or it overlaps an existing period"* 로 설명하나 **코드는 겹침 허용** — layer-5 선언↔코드 모순이고, registry 검사는 *방출 코드의 등록 여부*만 보지 *설명이 참인지*는 안 봐서 CI 가 구조적으로 못 잡는다. 범위 밖 유지: NATURAL-KEY 13·재고/쿠폰 4(BE-536)·ADR-002 D3 본문·`libs` 필터·`platform-console`. 분석·구현=Opus 4.8. [[project_enforcement_straggler_sibling_parity]] [[feedback_guard_predicate_wrong_verify_the_artifact]] | payment-service, settlement-service | money, idempotency, saga |
| TASK-BE-531 | **DONE (2026-07-19, 3-dim verified — impl PR #2710 squash `a3eb80f94`; 머지 전 `mergeState=CLEAN`, failing 0, `state=MERGED`, origin/main tip 일치 — Build & Test(JDK21) + Integration(ecommerce, Testcontainers) + Package boot jars(ecommerce) 전부 SUCCESS).** **[12-서비스 백엔드 리팩토링 스캔 Phase 3 — Tier C triage, 5후보 중 1건만 실질 refactor 확정]** **실행**: payment `PaymentConfirmService.confirm()`의 4-depth 중첩 post-capture escalation → `escalateStrandedRefund(orderId, latest, paymentKey, cause)` 추출(throw는 caller 잔류, recorder=별도 REQUIRES_NEW bean→self-invocation 없음, 호출순서·metric·F1 fail-open 로그 불변, `PaymentConfirmServiceTest` 무수정). **재검증 후 미실행 4건(근거)**: order cancel-tail=false-dup(`cancel`/`OrderCancelledEvent.of` 오버로드·라벨·로그 갈라짐+outbox med-risk) / settlement domain↔JPA split=**`SettlementPeriod` javadoc이 "JPA=allowed exception" 명문화**→위반 아님·`@Version` 사안·ADR 필요 / product WmsReconciliation clamp=behavior 아님(`@Version`/emitted-delta 변경)·설계 결정 / gateway swagger 데이터화=prod-off·이득 modest·`RouteLocator` 테스트 선례 0→test-first 비용 과다. **이커머스 백엔드 리팩토링 스윕(Tier A/B/C) 종료** — 대부분 서비스는 이미 잘 팩토링된 참조 구현이었고, 하위 후보 다수가 false-dup/marginal/설계사안으로 판명→churn 대신 근거와 함께 미실행. 분석·구현=Opus 4.8. [[feedback_repo_knows_what_it_does_not_say]] | payment | code, refactor |
| TASK-BE-530 | **DONE (2026-07-19, 3-dim verified — impl PR #2707 squash `d957262df`; 머지 전 `mergeState=CLEAN`, failing 0, `state=MERGED`, origin/main tip 일치 — Build & Test(JDK21) + Integration(ecommerce, Testcontainers) + Package boot jars(ecommerce) 전부 SUCCESS).** **[12-서비스 백엔드 리팩토링 스캔 Phase 2 — Tier B, 진짜 수렴 중복만 추출, 단일 PR/7서비스 커밋]** batch-worker `RestClients.timed`(4 client) · settlement `OperatorRoleGuard`(2 컨트롤러) · review `ReviewSortFields`(controller 400↔repo Sort 2층 allow-list) · promotion `OperatorRoleGuard`(3 서비스)+`handleMissingRequestHeader` dead-branch collapse · shipping `saveAndPublishStatusChange` tail+`OperatorRoleGuard`(3 클래스) · order/product `TenantContext.runWithTenant`(각 3 consumer, set→run→clear-in-finally byte-동치). **회의적 제외**: notification 3-consumer 템플릿(same-shape/different-specifics=과잉추상화, 공통 인터페이스 없음) / product dedupe-헬퍼(MANDATORY 전파·DIV med-risk)·uuid·image tail(marginal) / product AccountStatusChanged(try/**catch**/finally=순수 try/finally 헬퍼 부적합) / cross-service admin-role(libs HARDSTOP-03, R-12/R-20). **로컬 compile이 "삭제가 남긴 소비자"를 포착**(shipping `previousStatus`가 updateStatus 로그 사용→추출 경계를 save+publish로 좁힘). 단위테스트로 authz 403·sort 400·tenant bind/clear 보존 확인. 분석·구현=Opus 4.8. **후속: Tier C(payment confirm() extract / order cancel-tail / settlement domain-JPA split / product clamp / gateway swagger)=Phase 3 개별 안전망 필요, 보류.** [[project_enforcement_straggler_sibling_parity]] [[feedback_deletion_leaves_survivors_grep_the_consumers]] | batch-worker, settlement, review, promotion, shipping, order, product | code, refactor |
| TASK-BE-529 | **DONE (2026-07-19, 3-dim verified — impl PR #2705 squash `53f252e49`; 머지 전 `mergeState=CLEAN`, failing 0, `state=MERGED`, origin/main tip 일치 — Build & Test(JDK21) + Integration(ecommerce, Testcontainers) + Package boot jars(ecommerce) 전부 SUCCESS).** **[12-서비스 백엔드 리팩토링 전수 스캔 Phase 1 — Tier A, 최저위험 배치]** 순수 dead-code 삭제 4 + lib-alignment 1, category-separated 6커밋. **review** `ReviewJpaRepository` non-tenant query 7개 삭제(BE-403/멀티테넌시 전 잔재, `…AndTenantId` 형제가 대체, 콜러 0 grep) · **gateway** `RateLimiterConfig.ipKeyResolver`(BE-405가 고아화, yml 미참조)+`RouteService.isPublicRoute`(실제 판정=SecurityConfig, prod 콜러 0) 삭제+미사용 field/import 정리 · **payment** `Payment.isPending/isCompleted` 삭제(콜사이트 0) · **settlement** `QuerySettlementPeriodUseCase.getPeriod` 삭제(콜러 0)+import/javadoc 정리 · **user** `UserProfileService`/`WishlistService` 인라인 clamp→`java-common PageQuery.of`(byte-동치, straggler). **로컬 compile이 `WishlistService` clamp-local 다운스트림 재사용을 포착→`pageQuery.page()/size()`로 수정**(삭제가 남긴 소비자). Testcontainers IT 호스트 차단→CI-Linux 권위. 분석·구현=Opus 4.8. **후속: Tier B 서비스별 추출(Phase 2), Tier C 민감 항목(Phase 3).** [[project_enforcement_straggler_sibling_parity]] | review, gateway, payment, settlement, user | code, refactor |
| TASK-FE-094 | **DONE (2026-07-19, 3-dim verified — impl PR #2699 squash `8c589eecb`; 머지 전 `mergeState=CLEAN`, failing 0, `state=MERGED`, origin/main tip 일치 — Frontend unit(vitest)/lint&build/E2E smoke + Build & Test + Integration(ecommerce, Testcontainers) 전부 SUCCESS).** **[TASK-FE-092 스캔의 Tier 3(컴포넌트 구조) 후속]** 정밀 조사 결과 **ReviewCard 하나만 실질 가치** → `ReviewCard`의 13 flat props를 `review`(rating/title/content/createdAt) + `actions`(onEdit/onDelete/onUpdate/onCancelEdit) 객체로 그룹, **본문 미사용 죽은 `reviewId`(구 `_reviewId`) 제거**. 3파일(ReviewCard + 호출부 ReviewList·MyReviews), 본문은 상단 구조분해로 무변경, **테스트 무수정**(ReviewCard 직접 테스트 없음 — 부모 review-list/my-reviews 테스트가 review 객체 먹여 **출력** 단언 → 출력 불변이면 통과). **🔵 Tier 3 나머지 3종 DEFERRED**: OrderItemsSection(checkout↔order = 아이템 타입·레이아웃·요약 상이 = false-dup) / AddressSection(9-props지만 호출부 1곳+혼합 출처 = 그룹핑이 destructuring만 이동) / AddressForm `useAddressFormFields` 훅(field-hook↔validation-hook 결합+엣지 = net 개선 불확실). 로컬 vitest 불가(Node24↔vitest4)→CI(Node20) 권위; 로컬 tsc0+lint0. 분석·구현=Opus 4.8. [[feedback_repo_knows_what_it_does_not_say]] | web-store | code, refactor |
| TASK-FE-093 | **DONE (2026-07-19, 3-dim verified — impl PR #2695 squash `44318199`; 머지 전 `mergeState=CLEAN`, non-pass 0, `state=MERGED`, origin/main tip 일치 — Frontend unit(vitest)/lint&build/E2E + Build & Test + Integration(ecommerce) 전부 SUCCESS).** **[TASK-FE-092 스캔 후속 — 사용자 결정: Tier 4 + ProductDetail + route 통합]** 실질 산출 2건: (1) **ProductDetail dead-code 삭제**(컴포넌트+CSS+테스트+배럴; 소비자=자기 테스트뿐, 프로덕션은 ProductDetailWithCart 대체) (2) **`/orders` → `/my/orders` route 통합**(URL/UX 변경, 별도 커밋): Footer 링크 갱신 + `/orders`·`/orders/[id]` 영구 redirect(북마크 흡수, `[id]` 전달) + **orders-page/order-detail-page 테스트를 `/my/orders` 페이지로 이관**(두 테스트가 OrderHistory/OrderDetailView의 **유일 커버리지** — 직접 컴포넌트 테스트 없음; 콘텐츠 테스트 전량 보존, page-level unauth-redirect 케이스만 제거→게이팅은 my/layout 책임). **🔵 Tier 4 폴리시(#1~#5) 전량 DEFERRED — 정밀 조사 결과 실질 가치 없음**: 중복 useRequireAuth(제거 시 리다이렉트 커버리지 소실, my/layout 테스트 부재) / 미사용 export·barrel 트림(tsc·lint 에러 0 순수 표면, 배럴 결합) / ReviewCard reviewId(Tier 3 props 리팩토링과 결합) / skeleton primitive(**진짜 중복 아님** — 공유 primitive 이미 추출, 각 loading 은 자기 페이지 레이아웃 미러링). **applyCoupon=KEEP**(실 백엔드 엔드포인트 래퍼), PriceDisplay 채택=시각 변경이라 out. 로컬 vitest 불가(Node24↔vitest4)→CI 권위. 분석·구현=Opus 4.8. [[feedback_repo_knows_what_it_does_not_say]] | web-store | code, refactor |
| TASK-FE-092 | **DONE (2026-07-19, 3-dim verified — impl PR #2691 squash `c57a53b58`; 머지 전 `mergeState=CLEAN`, failing 0, `state=MERGED`, origin/main tip 일치 — Frontend unit(vitest)/lint&build/E2E smoke + Build & Test + Integration(ecommerce, Testcontainers) 전부 SUCCESS).** **[web-store 전수 스캔(FSD/dead-code+dup/complexity+naming 3-축)에서 발굴한 동작 무변경 리팩토링 Tier 1+2, 카테고리별 7커밋]** #1 cross-feature `useAuth` 4곳→`@/shared/lib/auth-context`(정경 출처; 테스트가 이미 shared origin mock→무수정) · #2 dead-code 5종 삭제(oauth icons dir·`isAuthenticated()`+미사용 auth import·auth/search model types·product 배럴 type re-export; src+__tests__ grep 잔여 0) · #3 `withMockFallback`(address CRUD 4×) · #5 `createListQueryKeys`(`const` 튜플로 키 배열 byte-identical) · #7 `updateParams` 다중키 · #4 `mapQueryError`(order2+notification1) · #6 `decodeServerJwt`(session↔logout, getToken 인자 동일). **의도적 inline 유지**: use-shipping-tracking(3-분기 ACCESS_DENIED→2-분기 헬퍼 부적합). **#8 Pagination 통합 DEFERRED** — shared/ui(Link/URL+번호+ellipsis) vs review(버튼/콜백+카운터)는 진짜 중복 아닌 설계상 다른 두 패턴, 통합=동작 변경 → 미실행("중복이 곧 병은 아니다"). **로컬 vitest 불가(Node24↔vitest4 `#module-evaluator`)→CI(Node20)가 테스트 권위**; 로컬은 tsc0+lint0. features/ 내 cross-feature import 잔여 0. Out-of-scope(후속): applyCoupon/ProductDetail test-only, /orders vs /my/orders 중복 route, PriceDisplay 채택, Tier3/4. 분석·구현=Opus 4.8. [[feedback_repo_knows_what_it_does_not_say]] | web-store | code, refactor |
| TASK-BE-506 | **DONE (2026-07-18, 3-dim verified — impl PR #2669 squash `c5b4db0eb`; 머지 전 **9 SUCCESS / 0 failing**, `state=MERGED`, mergeState CLEAN — 결정적 `Integration (ecommerce, Testcontainers)` + Build & Test + Frontend(unit/E2E/lint) SUCCESS).** **[finance-forbidden onion 의 sibling-parity 확장 — TASK-FIN-BE-050 감사가 낳은 ecommerce straggler, 사용자 정책결정=열어라(finance/erp 정합)]** 게이트웨이 account-type 평면(`AccountTypeEnforcementFilter`, GlobalFilter order −2)이 플랫폼 super-admin wildcard 토큰(`tenant_id='*'`, roles 없음)을 `/api/admin/**` 에서 403 → 콘솔 operator-overview ecommerce 카드 `forbidden`. **수정 = safe-method(GET/HEAD) AND `tenant_id='*'` 한정 admit** — write(POST/PUT/PATCH/DELETE)→403 유지, operator 무회귀(불변식: READ 가시성만 확대). 공유 `TenantClaimValidator.WILDCARD_TENANT` 사용(layer-1 tenant gate 와 drift 없음), path-scope `/api/admin/**` 불변(blast radius 유지). account-type 평면 bypass 지만 게이트웨이가 유일 집행점(downstream header-trust). **RED-before/GREEN-after**(short-circuit 제거→GET/HEAD admit 2건 RED, write/non-wildcard/operator 무변) + mutation. 분석·구현=Opus 4.8. [[project_enforcement_straggler_sibling_parity]] [[project_finance_forbidden_onion_sweep_2026_07_18]] | gateway-service | code, security, gateway |
| TASK-BE-505 | **DONE (2026-07-17, 3-dim verified — impl PR #2627 squash `df4a91827`; 머지 전 CI 실패 0, 결정적 `Build & Test` + `Integration (ecommerce, Testcontainers)` SUCCESS).** **[declared-vs-enforced sweep 산물 · 보안 authz]** order-service `/api/internal/orders/**` 가 **선언된 시스템 자격이 아니라 아무 유효 IAM 토큰이나 수락**했다. 체인은 signature+timestamps+issuer+(기본 blank)audience 만 검증하고 `.anyRequest().authenticated()` — 그런데 ecommerce 는 시스템 `client_credentials` 와 일반 CUSTOMER 토큰을 **같은 SAS issuer** 로 발급하므로 "authenticated" 로는 구별 불가. 유효한 CUSTOMER 토큰이 통과해 내부 mass-confirm sweep + **cross-tenant operator-cancel**(`findByIdAcrossTenants`, ownership 체크 0)을 실행 가능. 계약(`order-confirm-paid-stale.md`)은 "valid **system** credential 없이는 NEVER 실행"을 약속. 심각도 MED — `/api/internal/**` 는 게이트웨이 미라우팅(내부망 발판 필요). **discriminator 는 auth-service `TenantClaimTokenCustomizer` 로 확정**: cc 토큰 `sub`=client-id(authorization_code/refresh 만 account UUID 로 override), CUSTOMER `sub`=account UUID. **수정(Option A)**: `SystemClientSubjectValidator` 를 `internalJwtDecoder` validator 체인에 추가 — `sub ∈ order.internal.oauth2.allowed-client-ids`(기본 `ecommerce-internal-services-client`) 아니면 401(기존 entrypoint, 계약 401 프레이밍 일치). empty 허용목록=fail-closed. 크로스레포 IAM seed 변경 0. **초록 CI 가 가린 이유**: `ConfirmPaidStaleIT` 가 모든 토큰을 `sub=ecommerce-internal-services-client` 로 발급 → 다른 sub 토큰 거부를 한 번도 시험 안 함. **테스트**: `SystemClientSubjectValidatorTest`(6, 빠른 레인, **mutation=no-op validator→5/6 RED**) + `ConfirmPaidStaleIT`(+2: CUSTOMER-sub→401·sweep 미실행 / 타클라이언트 sub→401, pre-change 에선 200+sweep). 계약 § Authentication 에 sub-pin 명문화. 분석·구현=Opus 4.8. [[env_ci_flake_is_a_hypothesis_not_a_verdict]] | order-service | code, security, bug |
| TASK-BE-504 | **DONE (2026-07-16, 3-dim verified — impl PR #2606 squash `d616ed530`; 머지 전 CI GREEN 실패 0, 결정적 `Integration (ecommerce, Testcontainers)` = 10개 서비스 전부 통과).** **[monorepo `TASK-MONO-415` 자매 — 그 착수 재측정이 낳은 후속]** ecommerce 서비스는 공유 `libs/java-web-servlet` `CommonGlobalExceptionHandler` 를 상속하지 않고 **각자 독립 `GlobalExceptionHandler` 복사본**에 catch-all `@ExceptionHandler(Exception.class)`→500 버그를 전파 → Spring 6.1+ `NoResourceFoundException`(없는 경로=404)을 **500 INTERNAL_ERROR** 로 삼킴. **🔴 AC-0 재열거가 스냅샷을 두 번 정정**: MONO-415 서브에이전트가 적은 "7" → grep 후보 **11** → 실제 버그 **10**(폐기된 ecommerce `auth-service` 제외 — `TASK-BE-132`, Gradle 빌드 제외, 죽은 코드 수정 회피). 각 핸들러에 `handleNoResourceFound`+`handleNoHandlerFound`→404 `NOT_FOUND`(MONO-415 공유 핸들러와 바이트 동일 semantics/code; `NOT_FOUND` platform 레지스트리 기등록). 방식 A(공유 base 통합 안 함). `ErrorResponse.of` 균일, 반환 스타일 매칭(ResponseEntity 8 / `@ResponseStatus` 2). 신규 단위테스트 20(2/서비스), "없는 경로→500" 화석 0. **라이브 베이스라인**: 배포된 데모 user-service 가 없는 경로에 500 반환 확인(버그 실재); 수정 패턴은 MONO-415(account-service)에서 이미 라이브 404 검증됨. 분석·구현=Opus 4.8. | 10 services (notification/order/payment/product/promotion/review/search/settlement/shipping/user) | code, bug | 
| TASK-FE-091 | **DONE (2026-07-15, 3-dim verified — spec PR #2569 squash `3fcb84882` · impl PR #2571 squash `6c35599d2`; 머지 전 **2 SUCCESS / 31 SKIPPED / failing 0**)**. `TASK-PC-FE-241` AC-4 잔여 — web-store 가 콘솔 날짜·시간 규약의 *구현만* 복제하고 규칙은 복제하지 않았다(cross-project import 불가). **doc-only, 코드 0줄.** **AC-1 대조 = 갈라지지 않았다**(`hourCycle:'h23'` · pinned `Asia/Seoul` · placeholder `'-'` · 파싱불가→verbatim 전부 일치) ⇒ 정합화 티켓 불필요. **🔴 포인터를 어디 두느냐가 유일한 실질 결정이었고, 그럴듯한 후보 대부분이 0바이트 스텁이었다**(`coding-standards.md`·`build-test-run.md`·`repository-map.md`·`release-process.md`) — 거기 넣었으면 **티켓이 스스로 경고한 Edge Case**("아무도 안 여는 문서에 넣으면 규칙은 여전히 도달 불가")를 그대로 밟았다. 살아있는 문서 **2곳**에 배치: `PROJECT.md`(= `CLAUDE.md` 가 착수 시 읽으라고 규정한 **SoT #1** — `TASK-PC-FE-242` 가 콘솔에 쓴 것과 **같은 레버**) + `docs/onboarding/local-dev.md`(사람 경로 — 콘솔이 자기 컨벤션 문서를 링크한 것과 **대칭**). 링크·앵커 해석 실측 확인. **AC-3**: 두 포인터 모두 규칙을 **재서술하지 않는다** — *무엇을 쓰라*(헬퍼)와 *왜 여기 없는가*(cross-project)만 말하고 근거는 **가리키기만** 한다(복사본은 갈라진다 = PC-FE-241 AC-4). 🔵 **부수 기록(고치지 않음)**: `frontend-ui.md` §4 는 web-store 주석이 *"근거 없이 mirrors 만 적혀 있다"* 고 적었으나 **실제로는 근거 3줄이 있다 — 빠진 건 링크였다.** 그 3줄은 존치(호출부 요약이지 경쟁하는 규칙 서술이 아니며, 다음 사람이 `h23` 을 "간소화" 하는 것을 막는 마지막 방어선). 분석=Opus 4.8 / 구현=Opus 4.8. | web-store | code, onboarding |
| TASK-FE-090 | **DONE (2026-07-13, 3-dim verified — impl PR #2514 squash `3f413e616`; 머지 전 22 SUCCESS / 9 SKIPPED / failing=0). 자진 신고 — `account-type-guard.spec.ts` 의 120s timeout override 제거. 그 근거를 CI 가 반증했다.** `TASK-MONO-381`(root, `8dabdb38d`)이 커밋 `556ff90e0` 으로 이 스펙에만 120s 를 주며 주석에 *"레인의 첫 GAP 로그인이라 cold-SAS 비용을 떠안는다 ⇒ 60s 는 진짜 예산이 아니다"* 라고 단언했다. **머지 직후 push-트리거 nightly(런 `29244678278`)가 그 단언을 반증했다 — 그 "첫 GAP 로그인" 은 CI 에서 `4.0s`** 다(golden-flow 3.8s · cart 6.9s · logout 17.6s). **기본 60s 에 15배 여유** ⇒ cold-SAS 이론은 성립하지 않는다. **원인: 오염된 호스트에서 잰 시간을 인과로 승격시켰다** — 로컬 측정 당시 다른 세션의 컨테이너 19개가 CPU 를 먹는 중이었다(`env_webstore_local_lighthouse_host_saturation` 가 이미 기록한 함정). **🔴 진짜 결함은 숫자가 아니라 유보를 둔 위치였다**: MONO-381 은 **PR 본문엔** *"호스트 경합과 분리하지 못해 근본 원인은 주장하지 않았다"* 라 적고도 **코드 주석엔 확신만 남겼다.** 리뷰어는 PR 본문을 한 번 읽고 다음 구현자는 주석을 계속 읽는다 — **불확실성은 토론에 남고 확신이 코드로 갔다.** 이 저장소가 반복해 대가를 치른 **선언↔진실 드리프트**를, 그것을 잡는 task 를 하면서 스스로 하나 심은 것. **구현 = 10줄 순삭**(override + 근거 주석). 스펙은 나머지 5개와 같은 기본 60s. **AC-1/AC-4 ✅**(`tsc` 0 · `next lint` 0 · 성능 인과 주장 0). **✅ AC-2/AC-3 — 머지 후 push-트리거 nightly(런 `29257699024`)에서 *표를 직접 읽어* 확인**(full-stack 레인은 PR 에서 안 돌므로 impl PR 의 초록 22개는 이 두 AC 에 대해 아무 말도 못 한다 — **이 비대칭이 MONO-381 을 물린 자리다**): `assert-specs-ran` = **`account-type-guard.spec.ts ran=1 / skipped=0`**, `OK — all 5 required specs ran`; 스펙 **3.6s**, **12 passed / 43.2s, flaky 0, failure 0**(재시도가 실패를 덮는 것이 MONO-381 에서 실제로 일어난 실패 모드다). **직전 4.0s 와 재현 일치 ⇒ 로컬 28~60s = 호스트 경합이라는 결론이 두 번째 독립 런으로 확정.** **가드 로직·크로스테넌트 시드·REQUIRED 등록은 손대지 않았고, 같은 런이 정상 동작을 계속 실증한다** — 스펙은 돌고, 물고, 회귀 없다. **교훈: 기능은 로컬이 증명할 수 있지만 성능은 아니다. 그리고 PR 본문에 유보를 적었다면 코드 주석에도 적어라.** 분석=Opus 4.8. | web-store | test, cleanup |
| TASK-BE-503 | **DONE (2026-07-11, 3-dim verified — impl PR #2414 squash `c175c80e3`)**. **[미티켓 발굴 — BE-498 이 남긴 "재차 RED 시 착수" 신호가 도착(2회 독립 발생: PR #2402·#2408, 둘 다 ecommerce 파일 0개)]** `CrossTenantRateLimitIsolationIntegrationTest` AC-1 의 간헐 RED 는 **인프라 flake 가 아니라 테스트 논리 결함 2건**이었다. 어설션 본문이 처음부터 답을 말하고 있었다 — `a1`(fresh 사이클의 *첫* 요청)이 이미 429 = 타이밍이 아니라 **상태 누수**. **근인 1**: `cleanRateKeys()` 가 `rate:ecommerce-gw:*` 로 glob 했으나 그건 `tenantRouteKeyResolver` 반환값 = SCG 에 넘기는 **`id`** 이지 Redis 키가 아니다. SCG 4.2.0 `RedisRateLimiter.getKeys` 의 레시피(jar 상수풀 `#401` = `request_rate_limiter.{.}.`)대로 실제 키는 **`request_rate_limiter.{<routeId>.<id>}.tokens/.timestamp`** — 리터럴 `request_rate_limiter.{` 로 시작하므로 **매치 0건 = 정리가 완전한 no-op**. "retry with fresh keys" 가 fresh keys 를 한 번도 얻지 못했다. **근인 2**: Awaitility 게이트가 `failOpenDelta==0 && a2==429` 만 요구하고 **`a1` 을 제약하지 않아**, 드레인된 버킷에서 시작한 재시도 사이클(`a1=429, a2=429`)이 **게이트를 만족시켜 채택**되고 곧바로 `a1 != 429` 단언이 터진다 — **재시도 루프가 하필 실패를 만드는 상태에서 종료**. BE-497 은 거울상 구멍(refill 로 `a2≠429` 인 느린 사이클 폐기)만 막았다. **간헐성 설명**: 첫 사이클은 진짜 빈 Redis 라 항상 통과 → **버그는 재시도에서만 도달 가능**, 포화는 첫 사이클이 게이트를 놓치게 만드는 **방아쇠일 뿐 원인이 아니다**. **수정**: 정리를 **접두어-무관**(전용 컨테이너 전체 비움 — SCG 키 형태를 추측하는 것이 바로 이 버그였으므로 추측하지 않는다) + 게이트에 `a1 != 429` + `classifyTimeout` 에 "정리 실패" 분기(다음 사람이 세 번째로 포화 오분류하지 않도록) + **정리 계약 가드 신설**(probe→키 존재→정리→0개). 가드는 **Awaitility 루프 밖**에 둔다 — `ignoreExceptions()` 가 `Throwable` 을 삼켜 루프 안 단언은 타임아웃으로 둔갑한다. **운영 코드 0줄**(`FailOpenRateLimiter`·`OverrideAwareRateLimiter`·`TenantRouteRateLimitConfig` 는 옳고 fail-open 은 ADR 설계 TASK-BE-405). **mutation-check(실측 증명)**: 옛 glob 복원 시 실제 Redis 에서 `removed == 0L` 로 가드 FAIL — 가드 선행조건(`liveKeyCount()>0`)이 먼저 통과하므로 **키가 존재하는데 0개를 지웠다**는 뜻(추론 아님). 그 상태에서 **AC-1 은 여전히 통과** = 간헐성 메커니즘 재현. **검증**: 로컬 실 Redis `tests=2 skipped=0 failures=0`(XML), CI Linux **`Integration (ecommerce, Testcontainers): SUCCESS`**(권위 레인 — 두 번 이 PR 을 빨갛게 만들던 바로 그 레인). **교훈**: **"flake=인프라" 는 가설이지 결론이 아니다** — 재실행-GREEN 은 인프라를 증명하지 않고, 진짜 메시지는 어설션 본문에 있다. | gateway-service | bug, test, flaky |
| TASK-BE-501 | **DONE (2026-07-11, 3-dim verified — impl PR #2409 squash `b83adf4c3`)**. **[게이트웨이 전수 진단 산물 · 보안]** gateway 결함 2건. **① `X-Actor-Id` 미-strip = `platform/api-gateway-policy.md` L74 정면 위반.** `IdentityHeaderStripFilter` 는 이 헤더를 안 지우고 `JwtHeaderEnrichmentFilter` 도 안 세팅 → **strip ✗ + enrich ✗ = 클라이언트 위조값이 백엔드까지 그대로 통과**. **"enrich 가 덮으니 괜찮다" 는 세 겹으로 성립 안 함**: (i) ecommerce enrich 집합에 이 헤더가 **아예 없다**(덮어쓸 코드가 없음), (ii) 있었더라도 enrich 는 **JWT 없으면 no-op** — public 라우트(`GET /api/products/**`, `GET /api/reviews/products/**`, `PUBLIC_PATHS`, 캐리어 웹훅)엔 JWT 가 없어 **strip 이 유일 방어선**, (iii) 정책이 strip 을 순서까지 못박아 요구한다(enrich 는 보강이지 대체재 아님). **폭발 반경(정직)**: ecommerce `src/main` 리더 **0건 = 장전 안 된 총**. 그러나 **wms 는 이미 12개 컨트롤러가 이 헤더를 감사 주체로 신뢰**(admin-service ×5 + master-service ×7 의 `ACTOR_HEADER = "X-Actor-Id"`; `AdminEventEnvelopeBuilder` 가 감사 이벤트 actor 로 문서화) → **패턴이 이 저장소의 확립된 관행**이라 리더가 하나 생기는 순간 **감사로그 위조**. 리더 0인 지금이 가장 싸게 막을 시점. **② `tenant_mismatch` → 401 (자기 javadoc 은 403 을 약속)** — `TenantClaimValidator` javadoc 이 작성 시점부터 **403 `TENANT_FORBIDDEN`** 을 약속했으나 `SecurityConfig.unauthorizedEntryPoint()` 에 분기가 없어 모든 인증실패가 401, **약속된 코드는 코드베이스 어디서도 방출 안 됨**(scm 엔 그 분기가 있다). 인증 우회는 아니지만 **비용 있는 계약 위반**: 401 은 cross-tenant 클라이언트에게 "토큰이 낡았으니 새로 받아라" 는 **거짓말**(재발급해도 동일 거절 → 성실한 클라이언트가 갱신 루프에 빠진다). 403 = "토큰은 멀쩡, 엣지가 틀림" 이 참이고 행동가능. 부수: cross-tenant 거절이 `reason="invalid"` 로 집계돼 **인가 신호가 인증 카운터 안에 숨어 있었음** → `reason="tenant_mismatch"` 분리. **AC-4 mutation check (진짜 증거)**: 테스트 통과는 픽스의 증거가 아니다 — **결함을 되돌렸을 때 무는지**가 증거. 두 결함 재주입(테스트 무손상, `src/main` 한정 `git diff` 로 확인) → 이 프로젝트에서 **5건 RED**(strip 3 + 403 매핑 2), **그 외 무손상**(표적 정확). 되돌린 뒤 최종 GREEN 을 **XML 로 `0 skipped` 확인**(`BUILD SUCCESSFUL` 만 믿지 않음). 신규 테스트는 **JWT 없는 라우트**를 일부러 찌른다 — 거기가 "enrich 가 덮는다" 방어가 무너지는 지점. **스코프 규율**: scm 의 10-헤더 집합을 통째 복사하지 않고 **정책이 이름 붙인 것만**. **3-dim**: (a) MERGED `b83adf4c3`; (b) origin/main tip 일치; (c) pre-merge failing=0 — **22 SUCCESS / 3 SKIPPED, rerun 없이 attempt 1**. 자매=`TASK-BE-502`(wms), 동일 PR. **진단 부산물(오탐 1건 기록)**: 서브에이전트가 `entitled_domains` 참조 카운트(wms 3 / scm 5 / **fan 0**)로 "fan 누락" 을 보고했으나 **코드 재검증 결과 오탐** — fan 이 0 인 건 방치가 아니라 **아키텍처**(`ProductCatalog.ENTRIES` 에 `fan` 부재 = 구독 불가 도메인, V0019 는 `wms/scm/erp/finance` 만 시드, `omni-corp` 도 fan 미구독, `fan-platform`=`B2C_CONSUMER`). 심었다면 **프로덕션 보안 필터에 죽은 코드**. 서브에이전트 보고는 코드로 재검증해야 한다. **후속**: `libs/java-gateway` 추출 — 이제 네 게이트웨이 동작이 일치하므로 추출 PR 이 "행동 불변" 을 정직하게 주장 가능. 분석=Opus 4.8 / 구현=Opus 직접. | gateway-service | code, security, gateway, contract |
| TASK-BE-497 | **DONE (2026-07-11, 3-dim — PR #2397 `c28f6f8c9`)**. `CrossTenantRateLimitIsolationIntegrationTest`(M7 AC-1) 가 CI 러너 포화 시 간헐 `429 기대 → 404`. 근본 원인=**테스트↔설계 불일치**: `FailOpenRateLimiter` 는 BE-405 대로 Redis 지연 시 의도적 fail-open(허용, `X-RateLimit-Remaining:-1`)인데 테스트는 강제집행(정확히 429)을 단정 → 포화 시 limiter 가 열려 repoint 된 JWKS-mock 라우트가 404 반환 → 로직 실패로 오인. 약한 1번째 assertion(`≠429`)이 같은 fail-open 을 가림. **해법(프로덕션 무변경)**: `runCycle` 이 A-A-B 를 fresh 키로 돌며 `gateway_ratelimit_redis_unavailable_total` **델타**(>0=fail-open=인프라)와 A 두 요청 간 시차를 기록; Awaitility 가 **`델타==0 && a2==429`(강제집행 실증)** 사이클이 나올 때까지 재시도(warm-up 흡수 + replenish 창 회피 — 리필된 느린 사이클은 폐기). 그 사이클 안에서만 격리 속성 단언. timeout 시 `classifyTimeout` 3분기: fail-open(인프라)/시차≥900ms refill 창(인프라)/빠른데 미거부(진짜 강제집행 실패=로직). 두 함정 다 처리(①1번째 fail-open→델타 게이트가 보장 ②replenish→시차 측정). `Thread.sleep` 0(AC-4), 실 Redis 격리 유지(AC-2), `src/main` byte-unchanged(AC-3). **검증**: 컴파일 clean + **CI `Integration (ecommerce)` 레인 rerun 없이 1차 GREEN**(AC-5, 22 SUCCESS). 같은 PR 의 WMS `LocationIntegrationTest` seed-POST flake 는 내 diff 와 인과 무관(wms/libs 0 파일)·rerun GREEN 으로 분리 확정 — **같은 클래스(seed-time 인프라-취약 IT)가 저장소에 더 있음, 미티켓 관찰**. MONO-343(머지됨)은 doc-only 노출만 차단, 이 task 가 코드-PR 노출의 원인 제거. 분석=Opus 4.8 / 구현=Opus 직접. | gateway-service | test, flake, gateway |
| TASK-FE-087 | **DONE (2026-07-03, 3-dim — PR #2142 `0c3e86d7f`)**. FE-086 후속 — 알림 설정 PushOptIn 에서 제목·설명 제거하고 버튼만 푸시 토글 아래 표시(미지원/차단 안내·색상·기기목록 유지). 분석=Opus 4.8 / 구현=Opus 직접. | web-store | code, ui |
| TASK-FE-086 | **DONE (2026-07-03, 3-dim — PR #2137 `10afe5a75`)**. 알림 설정 UI — 푸시 채널 활성화 시 "이 브라우저에서 푸시 받기"+"푸시 수신 기기"를 푸시 토글 아래 한 영역으로 그룹화(`pushEnabled` 게이팅), 옵트인 버튼 글자색을 좌측 사이드바 선택 메뉴 색(`--color-on-primary`)과 정합. 분석=Opus 4.8 / 구현=Opus 직접. | web-store | code, ui |
| TASK-FE-085-fix-001 | **DONE (2026-07-03, 3-dim — PR #2133 `26422a408`)**. FE-085 후속 — PushOptIn 구독/해지 시 "푸시 수신 기기" 목록이 즉시 갱신 안 되던 버그(서로 다른 React Query 캐시, 목록 미invalidate). `usePushSubscription` 이 구독/해지 성공 시 `pushDevices` 쿼리 invalidate. 라이브 fed-e2e 발견(백엔드 정상, 프런트 캐시 갭). 분석=Opus 4.8 / 구현=Opus 직접. | web-store | code, bug, test |
| TASK-FE-085 | **DONE (2026-07-02, 3-dim — PR #2119 `87ebff41a`)**. 알림 설정 "푸시 수신 기기" 목록 — 백엔드 `GET /me/push-subscriptions`(목록, 키 미노출) + `user_agent` V7 캡처 + `listByUser`, 프런트 `PushDeviceList`(UA→기기명 라벨·등록일·`이 기기` 배지·기기별 해지). 교차-레이어 원자 PR. 백엔드 IT + 프런트 lint/unit/e2e GREEN. 분석=Opus 4.8 / 구현=BE 위임+FE 직접. | notification-service, web-store | code, api, migration, ui, push |
| TASK-FE-084 | **DONE (2026-07-02, 3-dim — PR #2111 `d9e840819`)**. web-store 주문 내역 `OrderCard` 3줄→2줄 압축 — 1줄=날짜+상태 배지, 2줄=`상품명 외 N건 · 총액원`(span 분리, ellipsis). 순수 UI, 기존 테스트 무수정 통과. 분석=Opus 4.8 / 구현=Opus 직접. | web-store | code, ui |
| TASK-FE-083-fix-002 | **DONE (2026-07-02, 3-dim — PR #2103 `da9e656a7`)**. FE-083 후속 버그픽스 — web-store BFF 프록시(`api/bff/[...path]`)가 백엔드 204/205/304 응답에 (빈) 본문을 실어 `Response` 생성자 `TypeError`→500 으로 변질(모든 백엔드 204 가 클라 500). 라이브 fed-e2e push 구독 해지에서 발견(백엔드는 204 정상 삭제, UI 만 실패 오인). null-body-status 시 본문 `null` 전달 + 프록시 204/304/200 테스트. 분석=Opus 4.8 / 구현=Opus 직접. | web-store | code, bug, test |
| TASK-FE-083-fix-001 | **DONE (2026-07-02, 3-dim — PR #2097 `dc9077608`)**. FE-083 후속 버그픽스 — web-store 인증 미들웨어가 `/sw.js` 를 /login 307 리다이렉트해 SW 등록이 막히던 것 수정(공개 allowlist + matcher 제외). 라이브 fed-e2e 기동으로 발견(앱 실행 전 미적발). 신규 middleware 테스트. 분석=Opus 4.8 / 구현=Opus 직접. | web-store | code, bug, test |
| TASK-FE-083 | **DONE (2026-07-02, 3-dim verified — PR #2090 `6f514106e`)**. web-store Web Push 구독 — `public/sw.js`(push 배너·notificationclick) + `use-push-subscription`(권한→SW→VAPID subscribe→백엔드 등록/해지) + `PushOptIn`(3-way 권한 UX, NotificationSettings 통합) + `@repo/types`/`@repo/api-client` push 메서드. 테스트 3종(CI Node20 GREEN). 라이브 배너=fed-e2e 수동 스모크. **Web Push E2E(BE-463→BE-464→FE-083) 완결.** 분석=Opus 4.8 / 구현=Opus 직접. | web-store | code, push |
| TASK-BE-464 | **DONE (2026-07-02, 3-dim verified — PR #2088 `517d28660`)**. notification-service Web Push(VAPID) 실연동 — V6 `push_subscriptions` + 등록/해지 API + `GET vapid-public-key`(200/503) + `WebPushSender`(BE-463 stub 대체·404/410 lazy prune·per-endpoint 실패 격리) + `MartijndwarsWebPushGateway`(web-push:5.1.1 + BouncyCastle 1.78.1 명시추가·미설정 skip graceful). 단위 5종 + notification-service Testcontainers IT(풀 wiring/Flyway/엔티티) GREEN. 실 브라우저 발송=후속 FE-083. 분석=Opus 4.8 / 구현=Opus 직접. | notification-service | code, api, migration, push |
| TASK-BE-463 | **DONE (2026-07-01, 3-dim verified — PR #2076 `daf928de3`)**. notification-service PUSH 채널 stub/log `PushNotificationSender`(@Component, supportedChannel=PUSH) — sender 부재로 조용히 drop되던 갭 해소, senderMap[PUSH] 자동 충전으로 render→send 경로 연결(발송 서비스 무변경, 계약 무변경). notification-service Testcontainers IT PASS 로 wiring 검증. 실 provider=후속 BE-464(Web Push/VAPID)→FE-083. 분석=Opus 4.8 / 구현=Opus 직접. | notification-service | code, test |
| TASK-BE-459 | **DONE (2026-06-29, impl PR #2022 squash `4b699e48f`, 3-dim verified)**. Authored `ADR-008 PROPOSED` — `/api/internal/**` 내부 엔드포인트 인증 경계를 네트워크-단독(현행 filter `permitAll`) → **앱-레이어 방어심층**(네트워크경계 유지 + NetworkPolicy 명시 승격 + 공유시크릿 토큰 게이트 constant-time + prod fail-closed + 감사). `BE-118-fix-002` deferred-but-untickected TODO 정식화. 버린 대안=네트워크-단독·mTLS/메시·IAM-JWT(각 기각사유). doc-only(ADR + README). **3-dim**: (a) MERGED+`4b699e48f`; (b) origin/main tip 일치; (c) pre-merge failing=0 (doc fast-lane). ACCEPTANCE + per-service 구현(auth/order/batch-worker)=user-gated 후속. 분석=Opus 4.8 / 구현=Opus 직접. | auth-service (정책=ecommerce 전반) | adr, docs |
| TASK-BE-449 | **DONE (2026-06-27, 3-dim verified — impl PR #1992 squash `e57e7f1ef`)**. ecommerce outbox v1→v2 스윕 **6/6 (마지막, money-critical)**. payment-service: `V8 payment_outbox`(UUID PK) + `PaymentEventOutboxWriter` v2 행 직접 영속 + `PaymentOutboxPublisher extends AbstractOutboxPublisher`(@Profile !standalone + @ConditionalOnProperty `outbox.polling.enabled` 게이트명 유지). 4토픽(`payment.payment.{completed,refunded}`+`payment.alert.refund.{stranded,unresolved}`) verbatim·payload byte-identical·key=paymentId. 커스텀 `event_publish_failure_total{service=payment-service}` = lib OutboxMetrics 래핑으로 보존. lib OutboxAutoConfiguration 유지(v1 테이블 validate). **CI IT 레인이 적발한 latent 버그 fix**: JpaConfig 가 `adapter.out.persistence` 만 스캔→`adapter.out.event` 의 PaymentOutboxRepository NoSuchBean 부팅실패→basePackages 에 `adapter.out.event` 추가(§27 패턴: mock-repo 단위 green, 풀부팅만 적발). **3-dim**: (a) MERGED+`e57e7f1ef`; (b) origin/main tip=`e57e7f1ef`; (c) pre-merge `Integration (ecommerce, Testcontainers)` GREEN(재검증) + 全 required pass(fails=0). 분석=Opus 4.8 / 구현=Opus 직접. | payment-service | code, refactor, event, outbox, money-safety |
| TASK-BE-448 | **DONE (2026-06-27, 3-dim verified — impl PR #1991 squash `16e6ecac6`)**. ecommerce outbox v1→v2 (saga-critical). order-service: `V11 order_outbox`(UUID PK) + `SpringOrderEventPublisher` v2 행 직접 영속 + `OrderOutboxPublisher extends AbstractOutboxPublisher`(@Profile !standalone). 4토픽(`order.order.{placed,confirmed,cancelled}`+`order.alert.saga.recovery.exhausted`) verbatim·payload byte-identical·key=orderId. 커스텀 실패 메트릭=OutboxMetrics 래핑 보존. CI IT 레인 ITs(ConfirmPaidStaleIT/OrderStuckRecoveryIT/MultiTenant) outbox→order_outbox 갱신. **CI IT 적발 fix**: 신규 마이그레이션을 `V10`로 만들었으나 기존 `V10__add_order_idempotency_key.sql` 와 충돌(`Found more than one migration with version 10` 부팅실패)→**V11 로 rename**(`:test` 는 Flyway 미실행이라 미적발). **3-dim**: (a) MERGED+`16e6ecac6`; (b) origin/main tip=`16e6ecac6`; (c) pre-merge ecommerce IT GREEN(재검증)+全 required pass. 분석=Opus 4.8 / 구현=Opus 직접. | order-service | code, refactor, event, outbox, saga |
| TASK-BE-447 | **DONE (2026-06-27, 3-dim verified — impl PR #1990 squash `6f7abd505`)**. ecommerce outbox v1→v2. settlement-service: `V4 settlement_outbox`(UUID PK, `infrastructure.persistence` 패키지=@EnableJpaRepositories base) + `SpringSettlementEventPublisher`(@Profile !standalone, standalone=Noop 유지) v2 행 직접 영속 + `SettlementOutboxPublisher extends AbstractOutboxPublisher`. 단일토픽 `settlement.period.closed.v1` verbatim·key=periodId. `settlement.outbox.*` 를 기존 `settlement:` yml 블록에 병합(중복키 회피). build.gradle test 태스크에 `excludeTags 'integration'` 추가. **CI IT 레인 없음**→권위=단위+master 패턴 미러. **3-dim**: (a) MERGED+`6f7abd505`; (b) origin/main tip=`6f7abd505`; (c) pre-merge 全 required pass(fails=0). 분석=Opus 4.8 / 구현=Opus 직접. | settlement-service | code, refactor, event, outbox |
| TASK-BE-446 | **DONE (2026-06-27, 3-dim verified — impl PR #1989 squash `4dc2d892c`)**. ecommerce outbox v1→v2. shipping-service: `V9 shipping_outbox`(UUID PK) + `SpringShippingEventPublisher` v2 행 직접 영속(3메서드) + `ShippingOutboxPublisher extends AbstractOutboxPublisher`. **혼합 토픽 컨벤션 verbatim**: `shipping.shipping.status-changed`(무접미)+`ecommerce.fulfillment.requested.v1`+`ecommerce.shipping.manual-confirm-requested.v1`. 구조화 envelope 2종=자체 event_id 재사용, opaque pre-serialized FulfillmentRequested=fresh UUID PK(wms 컨슈머가 payload eventId dedupe→안전). `shipping.outbox.*` 를 기존 `shipping:` 블록 병합(중복키 부팅실패 1회 적발·수정). **CI IT 레인 없음**→권위=단위+미러. **3-dim**: (a) MERGED+`4dc2d892c`; (b) origin/main tip=`4dc2d892c`; (c) pre-merge 全 required pass. 분석=Opus 4.8 / 구현=Opus 직접. | shipping-service | code, refactor, event, outbox |
| TASK-BE-445 | **DONE (2026-06-27, 3-dim verified — impl PR #1988 squash `7146846a3`)**. ecommerce outbox v1→v2. review-service: `V6 review_outbox`(UUID PK) + `OutboxReviewEventPublisher` v2 행 직접 영속(`toMessage()` 보존) + `ReviewOutboxPublisher extends AbstractOutboxPublisher`. 3토픽 `review.review.{created,updated,deleted}` verbatim·key=reviewId. build.gradle 에 `spring-boot-starter-actuator` 추가(micrometer-core 컴파일·MeterRegistry 빈). v1 ReviewOutboxPollingScheduler+테스트 삭제. **CI IT 레인 없음**→권위=단위+미러. **3-dim**: (a) MERGED+`7146846a3`; (b) origin/main tip=`7146846a3`; (c) pre-merge 全 required pass. 분석=Opus 4.8 / 구현=Opus 직접. | review-service | code, refactor, event, outbox |
| TASK-BE-444 | **DONE (2026-06-27, 3-dim verified — impl PR #1987 squash `d93979388`)**. ecommerce outbox v1→v2 스윕 **첫 서비스**(in-platform v2 레퍼런스 확립, 최저 blast-radius — CouponUsed/Expired 라이브 컨슈머 없음). promotion-service: `V7 promotion_outbox`(UUID PK) + `SpringPromotionEventPublisher` v2 행 직접 영속(lib OutboxWriter 미사용) + `PromotionOutboxPublisher extends AbstractOutboxPublisher`. 2토픽 `promotion.coupon.{used,expired}` verbatim·payload byte-identical·key=couponId·행 event_id=envelope event_id. lib OutboxAutoConfiguration 유지. build.gradle actuator 추가. **ADR-MONO-004 §6 편집은 이 브랜치에서만**(교차PR 충돌 회피; 종결 chore 에서 6/6 일괄 reconcile). **CI IT 레인 없음**→권위=단위+master 패턴 미러. **3-dim**: (a) MERGED+`d93979388`; (b) origin/main tip=`d93979388`; (c) pre-merge 全 required pass. 분석=Opus 4.8 / 구현=Opus 직접. | promotion-service | code, refactor, event, outbox |
| TASK-BE-443 | **DONE (2026-06-26, 3-dim verified — impl PR #1977 squash `a41fce8c`)**. **Money-safety** — BE-442 검증이 노출한 잠복 제품 결함: confirm() post-capture re-read(`findByOrderId`, 동일 `@Transactional` Hibernate Session)가 **stale L1-managed PENDING** 엔티티를 반환·동시 커밋 VOIDED 컬럼 폐기(세션 엔티티 동일성=DB 격리수준 위) → 동시 void 미감지 → 자동역환불·stranded 스킵 → `@Version` 없어 last-writer-wins로 **취소주문에 COMPLETED+PaymentCompleted**(자금 보유, BE-435/437 가드가 그 race에서 dead). 단위는 mock-repo 2-stub(PENDING→VOIDED)라 가짜 green—실 Hibernate 세션 미반영. 수정=`PaymentRepository.findByOrderIdFresh`(impl `entityManager.refresh` fresh 재SELECT)+L88 swap, **성공경로 byte-equivalent**(no-race refresh=no-op), L82–87 주석 정정. durability IT(BE-442 흡수) 해제=REQUIRES_NEW로 VOIDED-during-capture 결정론 주입(`confirmPayment` Answer가 별도 커넥션 `handleOrderCancelled` 커밋, AtomicInteger 게이트·sleep 0), pre=PENDING·capture 1회·post-rollback=VOIDED·stranded outbox 1+stranded_refund 1 생존·PaymentCompleted 0 단언. **3-dim**: (a) MERGED+`a41fce8c`; (b) origin/main tip=`a41fce8c2`; (c) pre-merge `Integration (ecommerce)` GREEN(1m50s, **durability IT 실제 실행·통과**=fix 실증)+全 required pass. 단위 `:payment-service:check` GREEN(PaymentConfirmServiceTest 2-stub 분리). supersedes BE-442. 분석=Opus 4.8 / 구현=Opus backend-engineer. | payment-service | code, bug, payment, money-safety |
| TASK-BE-442 | **SUPERSEDED → done (2026-06-26) by TASK-BE-443**. The stranded-durability IT rework was found **infeasible test-only**: confirm()'s post-capture re-read returns the stale L1-managed PENDING entity (same Session) and discards the concurrently-committed VOIDED columns → the stranded path is unreachable → that is a latent **production** money-safety defect, not a test gap. The IT rework folds into TASK-BE-443 (the production fresh-read fix + this IT as its regression proof). No separate implementation. 분석=Opus 4.8. | payment-service | code, test, payment, money-safety |
| TASK-BE-441 | **DONE (2026-06-26, 3-dim verified — impl PR #1975 squash `41e44930`)**. TASK-MONO-307이 격리한 order IT 잔여 5건(systemic 2버그 BE-439/440 외) triage — **전부 test-expectation drift, 제품 회귀 0, 제품코드 무변경**. (1) 403 error-code: `$.code` `UNAUTHORIZED`→`ACCESS_DENIED`(계약 order-api.md §GET/§cancel가 `403\|ACCESS_DENIED` 명시; UNAUTHORIZED=401), OrderQuery/Cancellation 2건. (2) seller-scope ABAC: `OrderJpaEntity.items` `@OneToMany`에 `@OrderBy` 부재→배열 순서 미보장이라 `$.items[0]/[1]` 위치 단언을 순서무관으로(EXISTS 귀속은 정상). (3) `@Version`: repo `save()` 업데이트 경로가 현재 엔티티 로드+`updateFrom`이 `@Version` 미복사(last-writer-wins)라 repo API로 충돌 구성 불가→실 동시충돌로 재작성(managed 로드→native `version+1`→stale dirty-flush). (4) saveAll: `toEntities`가 `version!=null`+id부재 시 throw(경로 정상). (5) refund-on-PENDING: `PaymentRefundConfirmationService`가 `InvalidOrderException` catch+skip(`markRefunded`는 CANCELLED 필요)=의도된 no-op. **CI round 2**: repo 예외 2건은 변환 레이어가 한 단계 어긋남(@Version=직접 flush→raw `jakarta OptimisticLockException`; saveAll=@Repository 변환→`InvalidDataAccessApiUsageException` 래핑) → 사후변환 타입으로 단언 교정. **3-dim**: (a) MERGED+`41e44930`; (b) origin/main tip=`41e449304`; (c) pre-merge `Integration (ecommerce)` GREEN(4m59s)+全 required pass. 격리 6 메서드 전부 해제. 분석=Opus 4.8 / 구현=Sonnet backend-engineer + Opus 직접(예외타입 교정). | order-service | code, test, triage |
| TASK-BE-440 | **DONE (2026-06-26, 3-dim verified — impl PR #1972 squash `14e92d3f`)**. Money-safety: TASK-MONO-307 ecommerce IT 레인이 노출한 latent 버그 — `OrderCancelled`→환불/void consumer(`PaymentRefundService.handleOrderCancelled`, BE-435)가 활성 tx 없이 persist(`InvalidDataAccessApiUsageException`). 근본원인=진입 메서드 `handleOrderCancelled`에 `@Transactional` 누락 + 내부 `refundPayment`/`voidPayment`는 **self-invocation으로 Spring AOP 우회** → consumer 스레드에 tx 미시작 → 캡처된 결제가 이벤트 경로로 환불 안 됨(머니 손실). 수정=진입 메서드에 `@Transactional`(REQUIRED, confirm 경계 미러; self-invoke가 join). 멱등/상태분기 가드 + StrandedRecorder REQUIRES_NEW 무변경. **3-dim**: (a) MERGED+`14e92d3f`; (b) origin/main tip=`14e92d3f8`; (c) pre-merge `Integration (ecommerce)` GREEN(1m52s)+全 required pass. consumer-tx IT 2건(PaymentRefundIntegrationTest) 해제·CI 검증. durability IT는 별개 원인(BE-440 fix가 VOIDED 선커밋→pre-capture 가드가 stranded 경로 차단)으로 **BE-442 분리·재격리**. 단위 `:payment-service:check` GREEN. 분석=Opus 4.8 / 구현=Opus backend-engineer. | payment-service | code, bug, payment, money-safety |
| TASK-BE-439 | **DONE (2026-06-26, 3-dim verified — impl PR #1973 squash `0f77a860`)**. TASK-MONO-307 IT 레인이 노출한 latent 버그 — order saga sweep/read 경로가 detached 엔티티를 tx 밖에서 매핑해 lazy `OrderJpaEntity.items` 접근 시 `LazyInitializationException`; `OrderStuckDetector.sweep()`(@Scheduled, OSIV 없음)의 catch가 삼켜 **stuck 주문을 영영 복구 안 함**(silent). 두 원인 수정: (1) production sweep — `findStuckPaymentPending`/`findStalePaidUnconfirmed`를 **two-step id-then-fetch**(스칼라 id 페이징 → `LEFT JOIN FETCH items` 무-Pageable)로 items를 쿼리에서 초기화(세션 경계 무관·HHH90003004 회피); (2) ITs(OrderStuckRecoveryIT/ConfirmPaidStaleIT)의 검증 read가 off-thread `findById`를 bare 호출(tx 없음)→`reload()`로 read-only tx 안에서 매핑. **3-dim**: (a) MERGED+`0f77a860`; (b) origin/main tip=`0f77a8605`; (c) pre-merge `Integration (ecommerce)` GREEN(5m10s, 3라운드 진단 끝)+全 required pass. 격리 IT(OrderStuckRecoveryIT 클래스 4 + ConfirmPaidStaleIT 2메서드) 해제. 단위 `:order-service:check` GREEN·sweep 효과(상태전이·attempt·outbox) 단언 유지=가리지 않음. 분석=Opus 4.8 / 구현=Sonnet backend-engineer + Opus 직접(test-read fix). | order-service | code, bug, saga |
| TASK-BE-438 | **DONE (2026-06-26, 3-dim verified — impl PR #1964 squash `d7e4f598`)**. BE-437 후속 — 묶인 환불(stranded refund) 자동 재조정 sweeper(Category-A saga, ADR-MONO-005 §2.3 D3, §2.6 D6 payment stranded-refund 행 추가). `stranded_refund` 테이블(Flyway V7) + `StrandedRefund` 도메인/상태머신(`STRANDED→RESOLVED`\|`UNRESOLVED`). `PaymentRefundStrandedRecorder`가 같은 `REQUIRES_NEW`에 행 영속(dedupe=open당 1행 partial-unique). `StrandedRefundSweeper`(@Scheduled, !standalone) + `StrandedRefundReconciler`(REQUIRES_NEW per-record, 별도 빈=AOP 경계)가 **PG 상태 선조회**(신규 `PaymentGatewayPort.fetchStatus`, 이미 CANCELED면 재취소 없이 RESOLVED=이중환불 방지 F1) 후 재취소; bounded-retry+지수backoff(Clock 주입, max 8)→terminal `UNRESOLVED`+`PaymentRefundUnresolved`(`payment.alert.refund.unresolved`) 에스컬레이션 공동커밋. 메트릭 resolved/unresolved 카운터 + `payment_refund_stranded_open` gauge. **3-dim**: (a) MERGED+`d7e4f598`; (b) origin/main tip=`d7e4f598b`; (c) pre-merge 20 pass/1 skip/**0 fail**(payment unit GREEN; 신규 reconciler/sweeper/domain/recorder/metrics 테스트 AC-2..AC-6). ⚠️AC-8 재조정 IT=`@Tag(integration)`→MONO-307까지 compile-only(단위 권위). 분석=Opus 4.8 / 구현=Opus 직접. | payment-service | code, event, payment, money-safety, saga |
| TASK-BE-437 | **DONE (2026-06-26, 3-dim verified — impl PR #1959 squash `b4ce981c`)**. Money-safety: payment-service HTTP `confirm()` 캡처-후 자동역환불 `cancelPayment` 실패 무방비 수정(BE-435 Edge Case L109/F1 후속). `PaymentRefundStrandedRecorder`(별도 빈 `REQUIRES_NEW`)가 `PaymentRefundStranded` 아웃박스 escalation(`payment.alert.refund.stranded`)을 confirm 롤백 견디게 발행 + `payment_refund_stranded_total` 메트릭 + ERROR(F1 내부 try/catch) → confirm reject. 성공경로 무변경. **3-dim**: (a) MERGED+`b4ce981c`; (b) origin/main tip=`b4ce981c3`; (c) pre-merge 20 pass/1 skip/**0 fail**(payment unit GREEN, PaymentConfirmServiceTest +3). ⚠️AC-2 durability IT=`@Tag(integration)`→MONO-307까지 compile-only. follow-up=auto-reconciliation sweeper. 분석=Opus 4.8 / 구현=Opus backend-engineer + Opus 검수. | payment-service | code, event, payment, money-safety |
| TASK-BE-435 | **DONE (2026-06-26, 3-dim verified — impl PR #1954 squash `ce4d5f6c`)**. Stuck payment-pending 주문 종결을 `STUCK_RECOVERY_FAILED` → **`CANCELLED(PAYMENT_TIMEOUT)`** 자동취소로 전환(ADR-MONO-005 §2.3 D3 auto-resolving refinement, 선행 TASK-MONO-306 #1952 머지 후 amend-before-implement). order: `CancelReason` enum + `Order.cancel(reason)` + `OrderCancelledEvent.cancelReason`(additive, legacy→OPERATOR) + stuck-handler cancel+OrderCancelled+informational alert 공동커밋(R3/R4). payment: `PaymentStatus.VOIDED` + consumer `handleOrderCancelled`(COMPLETED→환불/PENDING→void/terminal→no-op) + confirm 전·후 가드(캡처후 re-read 자동역환불)로 양쪽 순서 money-safe·멱등. product: 무변경(NEW release no-op, AC-4 테스트). **3-dim**: (a) MERGED+`ce4d5f6c`; (b) origin/main tip=`ce4d5f6ca`; (c) pre-merge 20 pass/1 skip/**0 fail**(Build&Test=ecommerce `:check` order 87/payment 68/product 10 unit GREEN). **⚠️caveat**: AC-2/3 money-safety IT·OrderStuckRecoveryIT는 `@Tag(integration)`이라 **CI·로컬 어디서도 미실행**(ecommerce `:check` excludeTags + ecommerce IT CI 레인 부재 + 로컬 Docker 차단; 컴파일만). 분기/가드 **로직은 unit으로 CI 커버**, 풀 Spring/Postgres 순서증명만 미자동화 — follow-up 후보=ecommerce `:integrationTest` CI 잡 추가. 분석=Opus 4.8 / 구현=2×Opus backend-engineer + Opus 검수. | order-service, payment-service | code, event, saga, payment, adr |
| TASK-FE-076 | **DONE (2026-06-18, 3-dim verified — impl PR #1823 squash `703470451`)**. Restored the **Nightly E2E → Frontend E2E full-stack (web-store, Playwright)** job to GREEN. Root cause = assertion drift, not a product defect: the `account_type_mismatch`→`role_denied` copy in `LoginForm.tsx` renders "operator…운영자 콘솔…" (no `admin`); the vitest unit test (`login-form-error-vocab.test.tsx`, `ROLE_DENIED`) was updated but `e2e/auth-redirect.spec.ts:50` still asserted `toContainText('admin')`. Fix = assert stable `'운영자 콘솔'` substring (1-line e2e, product code unchanged). Slipped past PR-gate because the spec lives in `e2e/` (full, nightly-only) not `e2e-smoke/` — the "un-tagged/full runs nightly, never in PR fast lane" gap (`platform/testing-strategy.md`). Surfaced by a 2026-06-18 CI conformance check; pre-existing across multiple main pushes (≤`074adc8`). **3-dim**: (a) MERGED + `703470451`; (b) origin/main tip = `703470451`; (c) pre-merge failing required = 0 (one Playwright-smoke timeout flake — browser-install hung 20m — reran GREEN; smoke non-required, all real test jobs PASS). 분석=Opus 4.8 / 구현=Opus 직접. | web-store | code, test |
| TASK-BE-388 | **DONE (2026-06-15, 3-dim — PR #1653 `5b31584fc`)**. Re-point the IAM→ecommerce account-lifecycle event bridge (ADR-MONO-037). M1 onboarding off the decommissioned `auth.user.signed-up` → IAM `account.created` (minimal profile, Flyway V5 nullable email/name; notification WELCOME de-PII'd); M2/M3 `account.deleted` two-phase (grace→withdraw idempotent, post-grace→profile PII anonymize, TASK-BE-258 obligation); M4 subscription contract + producer-retirement + spec re-point. order-PII cascade = documented-deferred boundary (P3). 20/20 CI SUCCESS. 분석=Opus 4.8 / 구현=Opus 4.8. | user-service, notification-service | code, test, migration, event-driven |
| TASK-FE-073 | **DONE (2026-06-13, 3-dim verified — impl PR #1461 squash `dd279d4aa`)**. 운영자 admin-dashboard 배송관리 **"택배사 동기화" (refresh-tracking) 버튼** — 무인 auto-collect 스케줄러(BE-360) 안 기다리고 운영자가 즉시 aggregator(Delivery Tracker, ADR-007) 상태 pull. 기존 admin 엔드포인트 `POST /api/shippings/{id}/refresh-tracking`(BE-293, ADMIN-only, best-effort) wiring만. FE-only: `@repo/api-client` `refreshTracking`(POST no-body→UpdateShippingStatusResponse) + admin `use-refresh-tracking` 훅(`useInvalidatingMutation`, no optimistic) + `RefreshTrackingButton`(gate=`{SHIPPED,IN_TRANSIT}`∧non-blank trackingNumber; PREPARING=waybill없음·DELIVERED=terminal) + ShippingList(actions 컬럼, combined isPending). 테스트 2(use-refresh-tracking success+fail / 버튼 gating×7). **계약/백엔드 무변경**(엔드포인트 published). UX/latency affordance(correctness=스케줄러가 이미 커버, BE-365 thin-ping 보류와 동일 논리). **3-dim**: (a) MERGED+`dd279d4aa`; (b) origin/main tip=`dd279d4aa`; (c) pre-merge 20 checks pass/0 fail — **Frontend unit tests(ecommerce vitest @Node20) 4m0s PASS**(신규 테스트 검증)+Frontend lint&build(ecommerce) 2m24s+web-store E2E smoke+全 Integration GREEN. ⚠️**로컬 vitest 불가**(host Node24 vs 프로젝트 Node20=vitest4/vite8 `ERR_PACKAGE_IMPORT_NOT_DEFINED`)→tsc/lint만 로컬, vitest는 CI(Node20) authoritative. 전용 worktree `mlab-fe073`+직접 구현(subagent 상대경로 함정 회피). 분석=Opus 4.8 / 구현=Opus 4.8(직접). | admin-dashboard, api-client | code, test |
| TASK-BE-356 | **DONE (2026-06-12)**. ecommerce Multi-Tenancy & Marketplace **spec foundation** (ADR-MONO-030 **Step 1**, Source-of-Truth-first). NEW `specs/features/multi-tenancy-and-marketplace.md` (슬라이스 SoT: 바깥 `tenant_id`=ADR-019 D5 entitlement-trust 진화 재사용 + 안쪽 `seller_id`=net-new 셀러 + `account_type` 평면 재사용; M1-M7 채택·degradation default-seed·M6 회귀·계약영향·보류) + product/order architecture.md 「Multi-Tenancy & Marketplace」 섹션. **정정된 ADR-030 기준**(D4=one IdP+account_type 평면; 게이트=고정슬러그→entitlement-trust; scm/erp=row-level 참조). Spec-only(코드·마이그·PROJECT.md trait 없음). 후속: BE-357(Step2 바깥축 코드+trait) → BE-358(Step3 셀러). 분석=Opus 4.8 / 구현=Opus 직접. | product-service, order-service | spec, multi-tenant, marketplace |
| TASK-FE-072 | **DONE (2026-06-02, 3-dim verified)**. ecommerce 프런트(`web-store`+`admin-dashboard`) Docker 빌드 최적화 (build-infra; bundled spec-less per `feedback_pr_bundling`). `# syntax=docker/dockerfile:1` + BuildKit 캐시 마운트 2건(`deps`: pnpm store, `builder`: 앱별 `.next/cache` `/app/apps/<app>/.next/cache`). runner 스테이지 무변경(standalone/static/public만, `.next/cache` 미복사 → 이미지 크기 불변). **실측 2026-06-02 (web-store, buildx --provenance=false): cold 445s → warm(소스 1줄) 79s (~82%↓, 366s 절감) → 무변경 4s.** platform-console TASK-PC-FE-035(console-web, 60%↓) 검증 패턴의 이식. **impl PR #1027** (squash `38ffebdb`). **3-dim 검증**: (a) state=MERGED + mergeCommit `38ffebdb` + mergedAt 2026-06-02T05:54:06Z; (b) `git log origin/main` tip=`38ffebdb` 일치; (c) pre-merge failing required=0 (**Frontend E2E smoke incl web-store 3m48s** = 새 Dockerfile 로 web-store 빌드+Playwright PASS / **Frontend lint & build (ecommerce) 2m9s** = 두 프런트 빌드 PASS / 全 Integration·E2E·Build·Package GREEN; Observability footprint=skipping). diff scope=Dockerfile×2 + task lifecycle + INDEX only (앱 src/producer/contract/spec/ADR grep NONE). AC-1..AC-6 ALL PASS. provenance off는 빌드-명령 플래그라 문서화만(미커밋). 분석=Opus 4.8 / 구현=Opus 4.8(직접; build-infra 기계적 이식) / 리뷰=Opus 4.8(직접 3-dim). **메타**: ecommerce 두 프런트는 console-web 보다 cold 가 커(445s vs 229s, turbo workspace 의존성 多) `.next/cache` 증분 절감 비율도 더 큼(82% vs 60%). 후속 후보=공유 frontend base Dockerfile 통합(console-web+ecommerce, root TASK-MONO 성격). | web-store, admin-dashboard | code, deploy, test |
| TASK-INT-024 | **DONE (2026-06-02)**. ADR-MONO-021 §3.3 step 3(D4 step3) — web-store GAP e2e 가 `account_type=CONSUMER` 클레임의 full OIDC 왕복(GAP id_token→NextAuth profile→jwt→session→`/api/auth/session`)을 단언. `account-type-claim.spec.ts`(`shouldSkipGap()` 게이트→기존 CI skip, nightly GAP suite 실행) + `iam-consumer-seed.sql` account_type='CONSUMER' 명시(+구식 "미발급" 주석 정정). 정적검증: tsc clean + compose config valid. 클레임 발급은 BE-329 `FormLoginIntegrationTest`(실 MySQL, 실제 SAS 토큰 디코드)로 결정론적 증명 — 본 spec 은 NextAuth 전파 레이어 추가(AC-1 라이브=nightly GAP CI 위임; 실행 중 GAP 이미지가 account_type 이전이라 로컬 재빌드는 호스트 fragility 대비 비례성 낮음). impl PR #1014 squash `aa3ec383`. 3차원 ✓. **ADR-MONO-021 §3.3 steps 1–3 완료.** 분석=Opus 4.8 / 구현=Opus. | web-store | test, e2e, auth, multi-tenant |
| TASK-INT-023 | **DONE (2026-06-01)**. web-store real-GAP e2e — RP-initiated logout AC-1 자동화. TASK-FE-070 라이브 검증 갭 종결. **lean** GAP 스택(mysql+redis+kafka-placeholder+auth-service, **account-service 불필요** — 로그인은 `auth_db.credentials`만; account_type 클레임 미발급도 signIn 통과) + consumer 시드 1행 + `loginAsSeededConsumer`(실 Spring `#username`/`#password` 폼) + `rp-initiated-logout.spec.ts`(`shouldSkipGap()` 게이트→기존 CI 무회귀) + CI 핸드오프 문서(`.github` classifier 차단). **라이브 실증** 실행 중 federation-e2e GAP(V0012+BE-328) 대상 `1 passed (51.0s)`. compose config+tsc clean. impl PR #1004 squash `73b7b01c`. 3차원 ✓. 공유 federated-logout(fan/admin/console) transitively 검증. 분석=Opus 4.8 / 구현=Opus. | web-store | test, e2e, infra, auth |
| TASK-FE-071 | **DONE (2026-06-01)**. admin-dashboard RP-initiated OIDC logout (GAP `end_session`) — web-store(FE-070) client-driven 패턴 기계적 미러(client=`ecommerce-admin-dashboard-client`, port 3001, OPERATOR). id_token 캡처 + `federated-logout.ts`(server-only `getToken`→end_session URL) + route `/api/auth/end-session-url` + `auth-context` logout=fetch→`signOut({redirect:false})`→navigate(fallback `/login`). 테스트 갱신 불필요(AuthGuard/Sidebar가 `useAuth` 모킹). **BE-328**로 V0012 admin URI effective. tsc+lint clean. impl PR #1002 squash `25e7fff7`. 3차원 ✓. **= 모든 GAP-OIDC 프런트(console/fan/web-store/admin) 로그아웃 패리티 sweep 완결.** AC-1 라이브는 후속 스모크((A) 결정). 분석=Opus 4.8 / 구현=Opus. | admin-dashboard | code, security, auth |
| TASK-FE-070 | **DONE (2026-06-01)**. web-store RP-initiated OIDC logout (GAP `end_session`) — 콘솔(PC-FE-033)·fan(FAN-FE-002) 동일 silent-relogin 결함 패리티. id_token 캡처(jwt callback) + `federated-logout.ts`(server-only `getToken`→end_session URL) + route `/api/auth/end-session-url` + client `logout()`=URL fetch→`signOut({redirect:false})`→navigate; cart/token-bridge cleanup 보존; 2 vitest 갱신(CI green). **depends on BE-328**(매퍼 post-logout→RegisteredClient → V0012 web-store URI effective). tsc+lint clean. impl PR #1000 squash `158253bf`(fan+ecommerce 묶음). 3차원 ✓. **AC-1 라이브 브라우저는 후속 스모크**(GAP 수락 BE-328 일반증명 + fetch-fail→local-only fallback; 사용자 (A) 결정). 분석=Opus 4.8 / 구현=Opus. | web-store | code, security, auth |
| TASK-BE-144 | impl PR #928 (squash `fb2a0786`) + this close chore. user-service `RestProductInfoProvider` 의 `RestTemplate` → `RestClient` 마이그레이션 — 외부 prototype 이관 잔재였던 monorepo 마지막 production `RestTemplate` 사용처를 표준(Spring 6 `RestClient`)으로 정렬. `RestTemplateConfig`→`RestClientConfig`(`wishlistRestClient` bean, Boot 3.4 `ClientHttpRequestFactoryBuilder`, connect 3s/read 5s 보존) + provider(`getForObject`→`get().uri().retrieve().body()`, 병렬 fan-out·`DELETED` fallback 무변경) + unit test(`RestClient` fluent-chain mock, 5 시나리오 보존) + `dependencies.md` spec 1줄. HTTP wire 동작 byte-identical. CI 그린(0 fail), 3-dim 머지 검증 통과. 분석=Opus 4.7 / 구현=Opus 4.7 — small refactor. 2026-05-29. | user-service | code, test, refactor |
| TASK-BE-292 | **REVIEWED → approved** (2026-05-16, `/review-task` single-task, review-checklist Spec/Arch/Quality/Security PASS · Perf N/A · Testing PASS via task Verification; fix task 0; on-disk 재검증: 0 envelope-field change + feature/use-case non-orphan + dead-ref 0). impl `task/spec-drift-cohort-2026-05-16` (spec-only, no `apps/`, decision-bearing). **WI-1 (E11) 결정 = (B) accept-inline + remove false promise**: 근거 = 5개 portfolio 프로젝트 0/5 가 shared-schema 미이행 (GAP `.gitkeep` placeholder, wms/scm/fan `schemas/` 부재) → de-facto 관행 = inline-per-contract. `schemas/README.md` 를 "intentionally unused / inline-by-choice" 로 rewrite (19 inline copy 무변경, envelope field diff 0 = semantically inert). **ADR 불요** — (B) 는 새 portfolio convention 미설정·기존 현실 비준 (shared-schema model 채택 시에만 ADR 필요 → README 에 명시 deferred). **WI-2 (E22) 결정 = (A) Anchor**: wishlist 실재 (`user-service/architecture.md` Domain Scope + `dependencies.md` `wishlist_items` 테이블 + product-service enrich + 4-endpoint 완전 계약) → Edge Case "real but lacks feature/use-case → (A)". `specs/features/wishlist-management.md` (English, user-management.md 구조) + `specs/use-cases/wishlist.md` (Korean, user-profile-and-address.md 구조 4 UC) authoring + `wishlist-api.md` Overview 에 upstream traceability (envelope 무변경). (B) retire 면 user-service live 모델과 모순 (Failure Scenario). dead-ref 4-file 0, apps/ 0. 분석=Opus 4.7 / 구현=Opus 4.7 / 리뷰=Opus 4.7. | contracts/schemas, wishlist | api, event, adr |
| TASK-BE-291 | **REVIEWED → approved** (2026-05-16, `/review-task` single-task, review-checklist Spec/Arch/Quality/Security PASS · Perf N/A · Testing PASS via task Verification). impl `task/spec-drift-cohort-2026-05-16` (spec-only, no `apps/`). G6/E6 deprecated in-tree auth-service 잔재 reconcile. **rewrite-to-GAP** (live feature): `features/user-management.md` Profile-Creation/Withdrawal flow + Business Rule + Related Events 표 → GAP `AccountSignedUp` 발행/credential-invalidation GAP-internal (L12 guard 와 정합) · `use-cases/user-profile-and-address.md:200` → GAP 세션 무효화 · `services/user-service/overview.md:46-47` → GAP owner. **guard** (historical): `features/authentication.md` body steps + Related Events 표 strikethrough (자체 L6-18 배너와 정합, Failure Scenario 표 미guard 회피) · `use-cases/signup-and-login.md` Korean deprecation 배너(authentication.md 미러) + actor 토큰 strike · `services/batch-worker/overview.md:21` REMOVED guard · `user-service/overview.md:66` 동파일 L21/70 형식 정합 guard(내부 일관성). 리뷰 재검증: AC grep unguarded current-actor=0 (`signup-and-login.md:5` 는 DEPRECATED 배너 내부 = guarded, false-positive), dead-ref 6-file 0, apps/ 0. Edge Case(consumer behavior 보존·언어 유지) 준수. fix task 0. 분석=Opus 4.7 / 구현=Opus 4.7 / 리뷰=Opus 4.7. | features/use-cases | adr |
| TASK-BE-143 | spec PR #528 (6c1fca14) + impl PR #529 (f8cf59ef) + this chore. product-service `ProductImageService` 의 `infrastructure.storage.StorageProperties` 직접 import 제거 — `domain/port/ProductImageBucketResolver` interface + `S3ProductImageBucketResolver` adapter 추출 (3 call site port 경유). CHERRY-2 trivial bundling: `ProductImageRepository.saveAll` dead-code 제거 (0 production caller). 9 files (+70 / -20) — 3 신규 prod + 1 신규 test + 4 modified + 1 lifecycle move. 205/205 unit pass (16 ProductImageServiceTest signature 갱신 + 1 S3ProductImageBucketResolverTest 신규). HTTP API / Kafka event / S3 bucket name byte-identical. CI 15/15 PASS + 1 SKIP. **메타**: day-one DDD cohort 4번째 검증 (ecommerce/order BE-140 + wms/master BE-141 + scm/procurement DEFER + ecommerce/product 본 task). sweep gain = retrofit era specific 가설 확정. 분석=Opus 4.7 / 구현=Sonnet 4.6. 2026-05-15. | product-service | code, test, refactor |
| TASK-BE-142 | spec PR #475 (1fa73ae5) + impl PR #476 (578ba7b2) + this chore. BE-141 deferred polish 직접 후속. ecommerce 10 backend service overview.md (order / payment / product / promotion / review / search / shipping / user / notification / batch-worker) 일괄 enhancement — fan-platform sibling-equivalent depth (Service identity table + Public surface + 5 Key invariants + Out of scope (v1) 4 신규 섹션). 기존 7 섹션 lower-half (Owned Data / Published Interfaces / Dependent Systems) 보존. 12 files / +624 / -255. 완료 시 ecommerce 13 service overview.md 전체 일관성 완성 (auth-service-deprecated 1개 제외, deprecation). CI markdown-only path-filter 15 SKIP + 1 PASS. production code 0. BE-141 / FAN-BE-006 / MONO-084 같은 same-day single-PR closure 패턴 답습. 분석=Opus 4.7 / 구현=Opus. 2026-05-14. | 10 services | spec, overview, be |
| TASK-BE-141 | spec PR #472 (2bb0591b) + impl PR #473 (5dbc2c9d) + this chore. `/refactor-spec all --dry-run` 2026-05-14 ecommerce HIGH-B closure. 3 frontend-edge service overview.md (gateway-service 33 → ~70 / web-store 34 → ~85 / admin-dashboard 32 → ~80 line) enhancement to fan-platform sibling-equivalent depth — Service identity table + Public surface (routes\|pages) + 6 Key invariants + Out of scope (v1) 4 신규 섹션. 기존 7 섹션 (Service / Responsibility / Owned Data / Published Interfaces / Dependent Systems) 보존. 나머지 10 service overview.md = sibling consistency 유지, deferred polish. CI markdown-only path-filter 15 SKIP + 1 PASS. production code 0. TASK-FAN-BE-006 같은 same-day single-PR closure 패턴 답습. 분석=Opus 4.7 / 구현=Opus. 2026-05-14. | gateway-service / web-store / admin-dashboard | spec, overview, be, fe |
| TASK-BE-140 | PR #380 머지 (2026-05-12, commit cfe27566). order-service `AdminOrderController` domain 모델 직접 import 제거 — `AdminOrderStatusService.changeStatus()` 가 raw `Order` 대신 application DTO 반환. dry-run 2026-05-11 finding A1 single-PR closure (B/C polish 는 DEFER). architecture.md § Forbidden Dependencies + § Boundary Rules 위반 1건 해소. spec/impl bundled PR. CI 15/15 PASS. 분석=Opus 4.7 / 구현=Opus. | order-service | code, test |
| TASK-BE-138 | order-service choreographed-saga stuck-detector — ADR-MONO-005 § D6 ecommerce order 행 **Gap → Compliant**. PR #366 (spec) + PR #367 (impl) + this chore. `OrderStuckDetector` (60 s `@Scheduled`) + `OrderStuckRecoveryHandler` (REQUIRES_NEW per-order, AOP-split mirror of wms `SagaSweeper` + `SagaRecoveryHandler` from TASK-BE-050) sweep `orders WHERE status='PENDING' AND payment_id IS NULL AND created_at < NOW() - 1800 s`. attempt cap 5 → terminal `STUCK_RECOVERY_FAILED` + `OrderSagaRecoveryExhausted` outbox event (T3 co-commit) → Kafka topic `order.alert.saga.recovery.exhausted`. Flyway V7 (2 cols + composite `idx_orders_status_created_at`). 3 metrics (run / recovery.fired{from_state} / exhausted{from_state}). All knobs externalised under `order.saga.stuck-detector.*`. **Test verification:** unit baseline 5+5 PASS via `./gradlew :order-service:check` (no regression on existing 30+ unit). New IT (`OrderStuckRecoveryIT`, 4 methods) authored — local Testcontainers npipe blocker per `project_testcontainers_docker_desktop_blocker` memory; ecommerce IT excluded from CI baseline by project convention. ADR-MONO-005 § D6 + § 5 + History updated; ADR § 1.1 audit row updated; service `architecture.md` Saga row Gap → Compliant; `order-events.md` adds `OrderSagaRecoveryExhausted` section. 분석=Opus 4.7 / 구현=Opus. 2026-05-11. | order-service | code, event, saga, adr |
| TASK-BE-139 | `TossPaymentsAdapter` Resilience4j wrap — Category B compliance per ADR-MONO-005 § D6. PR #363 (spec, f806bc00) + PR #364 (impl, d483f396). **CI 15/15 PASS** (cycle 3 — cycle 1 BulkheadFullException API + cycle 2 HttpServerErrorException subclass simple-name 2 fix). `@CircuitBreaker(toss-payments)` + `@Retry` + `@Bulkhead` on `confirmPayment` + `cancelPayment` (mirror procurement reference, bulkhead 10 concurrent). HTTP client connect 5s + read 10s timeouts. 4xx → `PgConfirmFailedException` (payment row → FAILED, 502); 5xx/timeout/CB-OPEN/bulkhead-full → fallback → `PgGatewayUnavailableException` (row unchanged, 503 `PG_GATEWAY_UNAVAILABLE`). `PaymentConfirmService` + `PaymentRefundService` distinguish two exception kinds. R4j `ignore-exceptions = [HttpClientErrorException, PgConfirmFailedException]` so 4xx bypasses retry. **ADR-MONO-005 status: PROPOSED → ACCEPTED 2026-05-11** (gate 2/2 satisfied; gate 1/2 = TASK-MONO-055 PR #361). 분석=Opus 4.7 / 구현=Opus. 2026-05-11. | payment-service | code, adapter, resilience4j, integration |
| TASK-BE-137 | payment-service outbox housekeeping — BE-136 self-review W3 (PaymentEventPublishIntegrationTest 에 Refunded 풀 round-trip IT method 추가) + W4 (JpaConfig 가 config/ 에 있는 이유 javadoc 추가, Hexagonal layout 정당화; file move 0). CI 15/15 path-filter 적용 (5 PASS + 10 skipped). PR #354 머지 2026-05-11. | payment-service | code, test, housekeeping |
| TASK-BE-136 | payment-service transactional outbox migration — ADR-006 Scenario A impl (PaymentCompleted/PaymentRefunded → outbox). Producer-side silent-loss 가능성 제거; at-least-once 그룹 합류. PR #345 머지 2026-05-11. | payment-service | code, event |
| TASK-BE-135 | ecommerce at-least-once delivery consistency audit + ADR-006 (per-service decision; payment=Scenario A→TASK-BE-136, user/notification=Scenario B). PR #344 머지 2026-05-11. | 7 services | spec, adr |
| TASK-BE-134 | `user.user.withdrawn` topic alignment — production silent-loss bug fix + canonical Topics table 정합. PR #338 머지 2026-05-11. | user-service | code, event, bug |
| TASK-BE-133 | ecommerce 7 service `dependencies.md` spec backfill — Consumes From / Publishes To / Notes 표준화. PR #337 머지 2026-05-11. | 7 services | spec |
| TASK-FE-069 | web-store 상품 상세/홈 LCP·TTI 개선 — Next.js Image 최적화 활성화(`remotePatterns` 화이트리스트: unsplash + placehold.co + localhost/127.0.0.1 + env-driven `NEXT_PUBLIC_OBJECT_STORAGE_HOSTNAME`) + ReviewList `next/dynamic` 코드 스플리팅 + Suspense boundary(향후 PPR 진입점). 컴포넌트 측 `unoptimized={...placehold.co \|\| localhost}` opt-in 패턴은 fallback 안전망으로 유지. specs/services/web-store/architecture.md § Rendering Strategy + § Image Strategy 갱신. 직접 push (PR 미경유), commit a1260d80. CI green: CI 3m58s + Nightly E2E (full-stack web-store) 8m3s. 분석=Opus 4.7 / 구현 권장=Sonnet 4.6. 2026-05-09. | web-store | code, perf |
| TASK-FE-068 | admin-dashboard SSR `API_URL_INTERNAL` fallback — web-store 와 정합성. PR #238 머지. axios baseURL 결정 로직 SSR/CSR 분기 (`typeof window === 'undefined'` 가드): SSR=`API_URL_INTERNAL ?? NEXT_PUBLIC_API_URL ?? localhost:8080`, CSR=`NEXT_PUBLIC_API_URL ?? localhost:8080`. web-store 와 1:1 일치. docker-compose admin-dashboard env 에 `API_URL_INTERNAL=http://gateway-service:8080` 추가. `.env.example` 주석 갱신. vitest 4-case 양쪽 (admin-dashboard + web-store drift 회귀 방지) 추가. CI green: frontend lint+build 2m9s, unit tests 2m25s, e2e smoke 2m42s, ecommerce boot jars 53s. 2026-05-06. | admin-dashboard, web-store | code, bug |
| TASK-BE-132 | ecommerce auth-service 컴포넌트 폐기 — docker-compose / k8s (services + network-policies + secrets + gateway env) / settings.gradle / CI (build-and-test + boot-jars + frontend-e2e) / .env / spec (services rename + auth-api/auth-events/features deprecated). PR #150 (`chore(ecommerce): TASK-BE-132`) 머지. apps/auth-service/ 소스는 보존 (settings.gradle 에서만 제외). follow-up: TASK-MONO-028 candidate — sync-portfolio.sh standalone v1 freeze policy + AUTH_SECRET grep + done task 깨진 spec 경로 cleanup. 2026-05-04. | auth-service (제거), gateway-service, infra | code, security |
| TASK-FE-067 | web-store + admin-dashboard 의 GAP OIDC cutover — NextAuth v5 신규 도입 + 자체 auth flow 폐기 + sync-portfolio.sh exclusion. PR #148 (`feat(ecommerce)!: TASK-FE-067`) 머지. signIn callback 으로 account_type_mismatch URL 전파 + sync exclusion 7건. follow-up: TASK-BE-132 (auth-service 컴포넌트 폐기), 별도 admin-dashboard SSR baseURL fallback (TASK-FE-068 candidate). 2026-05-04. | web-store, admin-dashboard, packages/api-client | code, security, api, breaking |
| TASK-BE-131 | ecommerce gateway-service JWT 검증 방식을 HS256 → JWKS/RSA(RS256)로 교체 | gateway-service | code, security, test |
| TASK-BE-130 | [Security P2] ReviewController sort 파라미터 허용 값 화이트리스트 검증 추가 | review-service | code, security, test |
| TASK-BE-129 | [Security P1] SearchAdminController admin role check 누락 수정 | search-service | code, security, test |
| TASK-BE-126-fix-002 | TASK-BE-126-fix-001 리뷰 수정 — register / addVariant / updateVariant / deleteVariant role 검증 테스트 누락 추가 | product-service | code, test, security |
| TASK-BE-126-fix-001 | TASK-BE-126 리뷰 수정 — 기존 테스트 X-User-Role 헤더 누락 및 role 검증 신규 테스트 추가 | product-service | code, test, security |
| TASK-BE-126 | [Security P0] AdminProductController / AdminProductImageController admin role check 누락 수정 | product-service | code, security, test |
| TASK-BE-128 | [Security P0] POST /api/payments userId 요청 바디 신뢰 취약점 수정 | payment-service | code, security, api, test |
| TASK-BE-127 | [Security P0] AdminAccountSeeder prod 프로파일 노출 수정 — 하드코딩 관리자 계정 씨딩 제거 | auth-service | code, security, test |
| TASK-FE-066 | admin-dashboard 상품 등록/수정 폼 이미지 업로드 UI — 다중 업로드, 미리보기, 순서 변경, 삭제 | admin-dashboard | code, api |
| TASK-BE-125-fix-001 | TASK-BE-125 리뷰 수정 — 계약 위반 13건 + 최적화 4건 | product-service, search-service | code, api, event |
| TASK-BE-125 | product-service 상품 이미지 업로드/삭제 API + ProductImagesUpdated 이벤트 | product-service, search-service | code, api, event |
| TASK-INT-022-fix-001 | TASK-INT-022 리뷰 수정 — MinIO /tmp 마운트, .env.example, infra NSP PSA, minio-init 리소스 제한 | infra | deploy, code |
| TASK-INT-022 | 상품 이미지 객체 스토리지 인프라 구성 — MinIO(local/dev) + S3(staging/prod) 배선 | infra | deploy, code |
| TASK-FE-065-fix-001 | TASK-FE-065 리뷰 fix: 테스트 파일 3개 widgets import 경로 kebab-case 갱신 | web-store | fix, naming, test |
| TASK-FE-065 | web-store widgets 디렉토리 kebab-case 네이밍 적용 | web-store | code, naming |
| TASK-BE-124-fix-001 | TASK-BE-124 리뷰 지적 수정 — wishlistItemId 미구현 전면 보완 | user-service | code, test |
| TASK-BE-124 | GET /api/wishlists/me/check 응답에 wishlistItemId 추가 — 콘트랙트와 구현 불일치 수정 | user-service | code, test |
| TASK-FE-064-fix-001 | TASK-FE-064 리뷰 이슈 수정: 로그아웃 카트 클리어 테스트 누락 및 토큰 만료 시 카트 즉시 삭제 | web-store | code, test |
| TASK-BE-123 | TASK-BE-122 리뷰 fix: DATA_INTEGRITY_VIOLATION 에러 코드 등록 및 wishlist-api 컨트랙트 갱신 | user-service | code |
| TASK-FE-063-fix-001 | TASK-FE-063 리뷰 fix: 대시보드 위젯 테스트 3종 추가 및 집계 한계 경고 표시 | admin-dashboard | code, test |
| TASK-FE-063 | admin-dashboard 홈 화면 1순위 위젯 구현 — 주문/매출/재고 KPI 및 최근 주문 | admin-dashboard | code, api, test |
| TASK-FE-062-fix-001 | TASK-FE-062 리뷰 fix: cross-feature import 제거 + 에러/로딩 fallback 테스트 추가 | admin-dashboard | code, test |
| TASK-FE-062 | admin-dashboard 주문관리 목록/상세에 주문자 이메일 표시 | admin-dashboard | code, test |
| TASK-BE-119-fix-003 | TASK-BE-118-fix-001 리뷰 fix: RepublishSignupEventsIntegrationTest AdminAccountSeeder 충돌 수정 | auth-service | code, test |
| TASK-BE-119-fix-002 | TASK-BE-119 리뷰 fix: 통합 테스트 파일명 컨벤션 + Kafka producer acks/idempotence 설정 추가 | auth-service | code, test |
| TASK-BE-119 | auth-service AuthEvent → Kafka 발행 브리지 구현 — 인메모리 이벤트를 실제 Kafka로 전송 | auth-service | code, event, test |
| TASK-BE-118-fix-002 | TASK-BE-118-fix-001 리뷰 수정 — SecurityConfig TODO 주석에 연결된 태스크 ID 추가 | auth-service | code |
| TASK-BE-118-fix-001 | TASK-BE-118 리뷰 수정 — 재발행 엔드포인트 통합 테스트 누락 추가 및 보안/로깅 보완 | auth-service | code, test |
| TASK-BE-118 | auth-service 가입 이벤트 재발행 내부 엔드포인트 — user-service user_profiles 누락 복구 수단 제공 | auth-service | code, api, event, test |
| TASK-BE-116-fix-001 | TASK-BE-116 리뷰 수정 — payment-service AmountMismatchException HTTP 상태 코드 422 → 400 수정 | payment-service | code, api |
| TASK-BE-114 | auth-service Google OAuth 2.0 로그인 구현 — Authorization Code Flow, 사용자 find-or-create, JWT 발급 | auth-service | code, api, test |
| TASK-BE-098 | order-service API 컨트랙트 불일치 수정 — POST 요청 필드 차이 및 GET 목록 status 필터 미구현 | order-service | code, api, test |
| TASK-BE-097 | gateway-service 통합 테스트 user-service 라우트 누락 수정 | gateway-service | code, test |
| TASK-BE-096 | gateway-service rate_limited 메트릭 실제 연동 — RequestRateLimiter 429 응답 시 gateway_rate_limited_total 기록 | gateway-service | code, test |
| TASK-BE-095 | gateway-service 외부 클라이언트 X-User-Id / X-User-Email 헤더 스푸핑 방어 — 모든 경로에서 수신 헤더 제거 | gateway-service | code, test |
| TASK-BE-105 | order-service ErrorResponse 컨트랙트 위반 수정 — timestamp 필드 제거 및 IllegalArgumentException 핸들러 추가 | order-service | code, api, test |
| TASK-FE-061 | web-store product API mock 폴백 제거 — non-UUID mock id가 쓰기 API로 흘러 들어가는 경로 차단 | web-store | code, test |
| TASK-BE-116 | 전 서비스 GlobalExceptionHandler HttpMessageNotReadableException 핸들러 추가 — 잘못된 JSON/UUID 본문 시 500 → 400 VALIDATION_ERROR | auth-service, user-service, product-service, order-service, payment-service, shipping-service, review-service, promotion-service, notification-service, search-service | code, api, test |
| TASK-FE-047 | TASK-FE-040에서 발견된 LoginForm oauth_failed 에러 메시지 테스트 누락 수정 | admin-dashboard | code, test |
| TASK-BE-113-fix-001 | TASK-BE-113 리뷰 수정 — order-service 회원 탈퇴 시 주문 취소 배치 저장 실제 구현 | order-service | code, event, test |
| TASK-BE-113 | order-service 회원 탈퇴 시 주문 취소 배치 저장 최적화 | order-service | code, event |
| TASK-BE-109 | order-service 이벤트 발행 Transactional Outbox 패턴 적용 | order-service | code, event |
| TASK-BE-110 | order-service 이벤트 컨슈머 event_id 기반 중복 처리 구현 | order-service | code, event |
| TASK-BE-103 | order-service OrderMetrics 취소 reason 매핑 누락 수정 — user_withdrawn 카운터 추가 | order-service | code, test |
| TASK-BE-099 | order-service Application 레이어 Infrastructure 직접 의존 제거 — OrderMetrics, KafkaTemplate 포트 인터페이스 분리 | order-service | code, test |
| TASK-BE-099-fix-001 | TASK-BE-099 리뷰 수정 — OrderEventPublisher 포트 인터페이스 및 SpringOrderEventPublisher 구현체 추가 | order-service | code, test |
| TASK-BE-092 | gateway-service 스펙 헤더 불일치 수정 — X-User-Role vs X-User-Email 명확화 | gateway-service | code |
| TASK-BE-093 | gateway-service 미사용 RateLimiter Bean 및 Properties 제거 | gateway-service | code, test |
| TASK-BE-094 | gateway-service public 라우트 요청 라우팅 메트릭 누락 수정 | gateway-service | code, test |
| TASK-BE-091 | gateway-service RequestLoggingFilter resolveTargetService 중복 제거 — RouteService 위임 | gateway-service | code, test |
| TASK-INT-021 | Kubernetes 보안 강화 — SecurityContext, NetworkPolicy, secrets 관리 개선 | infra | deploy, code |
| TASK-INT-021-fix-002 | TASK-INT-021 리뷰 수정 2차 — Ingress server-snippets 대체, batch-worker NetworkPolicy kubelet probe, web-store/admin-dashboard SSR egress | infra | deploy, code |
| TASK-INT-021-fix-001 | TASK-INT-021 리뷰 수정 — default-deny Egress, seccompProfile, automountServiceAccountToken, PSS 레이블, Ingress 보안 헤더, PDB, 검증 스크립트 보완 | infra | deploy, code |
| TASK-BE-088 | search-service, payment-service Kafka DLQ 설정 추가 — DeadLetterPublishingRecoverer 적용 | search-service, payment-service | code, event, test |
| TASK-BE-088-fix-001 | TASK-BE-088 리뷰 수정 — payment-service catch 블록 특정 예외 변경 및 DLQ 라우팅 테스트 보강 | search-service, payment-service | code, event, test |
| TASK-FE-030 | isApiError 타입 가드 및 ERROR_MESSAGES 상수 공유 모듈 통합 — web-store 5곳 중복 제거 | web-store, packages/types | code, test |
| TASK-FE-031 | web-store, admin-dashboard 중복 공유 UI 컴포넌트 @repo/ui 통합 — LoadingSpinner, EmptyState, ErrorMessage | web-store, admin-dashboard, packages/ui | code, test |
| TASK-FE-032 | 대형 프론트엔드 컴포넌트 분해 — ProductForm(352줄), AddressForm(339줄), AddressList(263줄) | admin-dashboard, web-store | code, test |
| TASK-FE-033 | 프론트엔드 인라인 스타일 정리 — 스타일 객체 추출 및 CSS Modules 전환 | admin-dashboard, web-store | code, test |
| TASK-BE-084 | auth-service, user-service 이메일 검증 중복 제거 — 서비스별 Email Value Object 추출 | auth-service, user-service | code, test |
| TASK-BE-084-fix-001 | TASK-BE-084 리뷰 수정 — Email VO 정규식 불일치, 정규화 불일치, 최대 길이 검증 누락 | auth-service, user-service | code, test |
| TASK-INT-020 | E2E 테스트 커버리지 확대 — 핵심 비즈니스 흐름 전체 시나리오 추가 | infra | test, deploy |
| TASK-INT-019 | 모니터링 알림 규칙 세분화 — Prometheus AlertManager 알림 정책 구성 | infra | deploy, code |
| TASK-INT-018 | 성능/부하 테스트 구성 — 전체 서비스 부하 테스트 스크립트 및 기준 수립 | infra | test, deploy |
| TASK-INT-017 | specs/use-cases/ 유즈케이스 스펙 정의 — 핵심 사용자 시나리오 문서화 | all | code |
| TASK-FE-029 | admin-dashboard React key 수정 및 api-client 하드코딩 설정 외부화 | admin-dashboard, api-client | code, test |
| TASK-BE-076 | product-service 통합 테스트 Kafka 설정 수정 | product-service | code, test |
| TASK-BE-075 | LoginRateLimitFilter 인터페이스 사용 수정 | auth-service | code |
| TASK-BE-074 | gateway-service CORS ConfigMap 누락 수정 | gateway-service | code |
| TASK-BE-073 | auth-service application→infrastructure 직접 의존 제거 및 gateway CORS 외부화 | auth-service, gateway-service | code |
| TASK-BE-072 | payment-service, batch-worker 도메인 모델 프레임워크 의존성 분리 | payment-service, batch-worker | code |
| TASK-BE-071 | payment-service 이벤트 envelope snake_case 수정 및 토픽명 설정 외부화 | payment-service | code, event |
| TASK-BE-069 | order-service DTO 네이밍 수정 및 gateway-service 서비스 레이어 도입 | order-service, gateway-service | code, test |
| TASK-BE-068 | search-service Elasticsearch 응답 null 안전성 및 예외 처리 개선 | search-service | code, test |
| TASK-FE-028 | 프론트엔드 타입 안전성 강화 및 에러 바운더리 추가 | web-store, admin-dashboard | code, test |
| TASK-BE-067 | product-service 패키지명 수정 — interfaces → presentation | product-service | code, test |
| TASK-BE-066 | 전 서비스 이벤트 소비 실패 처리 개선 — 예외 로깅 강화, 메트릭 추가 | all | code, event, test |
| TASK-BE-065 | 전 서비스 입력 검증 강화 — @Valid 누락, 금액/헤더 검증 보완 | all | code, test |
| TASK-INT-013 | Docker 보안 강화 — Java 서비스 non-root 사용자, OTEL JAR 체크섬 검증 | infra | deploy, code |
| TASK-BE-064 | batch-worker 아키텍처 완성 — 빈 설정 클래스 제거, 도메인 검증 추가 | batch-worker | code, test |
| TASK-BE-063 | product-service soft-delete 쿼리 버그 수정 — findById에서 삭제된 상품 조회 가능 | product-service | code, test |
| TASK-BE-062 | auth-service, user-service PII 로그 노출 제거 | auth-service, user-service | code |
| TASK-BE-061 | 전체 서비스 하드코딩 시크릿 제거 — DB 크레덴셜, JWT 시크릿 설정 외부화 | all | code |
| TASK-INT-016 | specs/features/ 기능 스펙 정의 — 구현 완료된 핵심 기능별 feature 스펙 문서화 | all | code |
| TASK-INT-015 | 누락된 서비스 overview.md 스펙 작성 — user-service, gateway-service, batch-worker, admin-dashboard | all | code |
| TASK-INT-015-fix-001 | admin-dashboard overview.md 포맷 수정 — `## Application` → `## Service` | admin-dashboard | fix |
| TASK-INT-014 | 프론트엔드 Docker 이미지 빌드 및 docker-compose 통합 | infra | deploy, code |
| TASK-INT-013 | Docker 보안 강화 — Java 서비스 non-root 사용자, OTEL JAR 체크섬 검증 | infra | deploy, code |
| TASK-INT-011 | 모니터링 대시보드 — Grafana 프로비저닝 및 Prometheus 메트릭 시각화 | infra | deploy, code |
| TASK-INT-010 | Kubernetes 매니페스트 — 프로덕션 배포 Deployment, Service, Ingress 구성 | infra | deploy |
| TASK-INT-009 | 프론트엔드 ↔ 백엔드 통합 검증 — web-store, admin-dashboard API 연동 확인 | infra | deploy, test |
| TASK-INT-008 | CI/CD 파이프라인 구성 — GitHub Actions 빌드, 테스트, 이미지 빌드 워크플로우 | infra | deploy, code |
| TASK-INT-007 | E2E 통합 테스트 — docker compose 환경 전체 흐름 검증 스크립트 | infra | deploy, test |
| TASK-BE-070 | 전 서비스 한국어 로그 메시지 영어 통일 — 운영 일관성 확보 | all | code |
| TASK-BE-070-fix | TASK-BE-070 리뷰 수정 — order-service, product-service 잔여 한국어 에러 메시지 영어 전환 | order-service, product-service | code |
| TASK-INT-012 | 코드 품질 크로스 리뷰 — 전체 서비스 기술 부채 점검 및 개선 태스크 도출 | all | code |
| TASK-INT-006-fix | TASK-INT-006 리뷰 이슈 수정 — Dockerfile 빌드 실패, restart 정책 누락, NEXT_PUBLIC 환경변수 | infra | deploy, code |
| TASK-INT-006 | docker-compose 리소스 제한 및 프론트엔드 헬스체크 추가 | infra | deploy |
| TASK-BE-060 | product-service Metrics Counter 메모리 누수 테스트 추가 | product-service | code, test |
| TASK-BE-057-fix | 이벤트 발행 실패 메트릭 수정 | all | code, event |
| TASK-BE-057 | 전 서비스 이벤트 발행 실패 모니터링 — 실패 메트릭 및 로그 레벨 통일 | all | code, event, test |
| TASK-BE-056 | search-service ProductUpdated 재고 초기화 버그 수정 및 컨슈머 테스트 추가 | search-service | code, event, test |
| TASK-BE-055 | auth-service 클라이언트 IP 해석 로직 통일 — Rate Limit 우회 방지 | auth-service | code, test |
| TASK-BE-054 | order-service, gateway-service Metrics Counter 등록 메모리 누수 수정 | order-service, gateway-service | code, test |
| TASK-BE-053 | order-service, product-service 동시성 제어 — Optimistic Locking 적용 | order-service, product-service | code, test |
| TASK-FE-026 | web-store, admin-dashboard 인증 컨텍스트 및 API 설정 중복 코드 공통 패키지 추출 | web-store, admin-dashboard | code, test |
| TASK-FE-027 | web-store 메모리 누수 수정 및 에러 핸들링 개선 | web-store | code, test |
| TASK-BE-059 | gateway-service 테스트 컴파일 오류 수정 및 Rate Limiter 설정 외부화 | gateway-service | code, test |
| TASK-BE-058 | user-service 주소 관리 동시성 수정 및 입력 검증 강화 | user-service | code, test |
| TASK-FE-023 | admin-dashboard 사용자 관리 기능 구현 — 사용자 목록, 상세 조회 | admin-dashboard | code, api |
| TASK-FE-024 | TASK-FE-021 리뷰 수정 — 토스트 알림 미구현 및 메시지 auto-dismiss 누락 | web-store | code, test |
| TASK-FE-025 | TASK-FE-022 리뷰 수정 — 빈 상태 UI 중복 렌더링 수정 | web-store | code, test |
| TASK-FE-022 | web-store 배송지 관리 UI 구현 — 목록 조회, 추가, 수정, 삭제 | web-store | code, api |
| TASK-FE-021 | web-store 사용자 프로필 페이지 구현 — 프로필 조회 및 수정 | web-store | code, api |
| TASK-BE-052 | TASK-BE-051 리뷰 수정 — invalidate 보조 인덱스 제거 누락 및 Redis 키 spec 반영 | auth-service | code |
| TASK-BE-051 | TASK-BE-050 리뷰 수정 — 탈퇴 사용자 토큰 무효화 누락 보완 | auth-service | code, event |
| TASK-BE-050 | auth-service UserWithdrawn 이벤트 소비 — 탈퇴 사용자 인증 정보 무효화 | auth-service | code, event |
| TASK-BE-049 | batch-worker 부트스트랩 — 프로젝트 구조, DB 스키마, Spring Batch 설정, 초기 잡 구성 | batch-worker | code |
| TASK-BE-048 | TASK-BE-045 리뷰 수정 — 예외 처리 패턴 통일, 로그 언어 일관성 | order-service | code, event |
| TASK-BE-047 | TASK-BE-043 리뷰 수정 — 서비스 네이밍, CANCELLED 주문 예외 처리, 테스트 보완 | order-service | code, event |
| TASK-BE-046 | order-service Kafka DLQ 설정 추가 — 전 컨슈머 DeadLetterPublishingRecoverer 적용 | order-service | code, event |
| TASK-BE-045 | order-service UserWithdrawn 이벤트 소비 — 탈퇴 사용자 활성 주문 취소 처리 | order-service | code, event |
| TASK-BE-044 | order-service PaymentRefunded 이벤트 소비 — 환불 완료 시 주문 환불 상태 반영 | order-service | code, event |
| TASK-BE-043 | order-service PaymentCompleted 이벤트 소비 — 결제 완료 시 주문 결제 상태 반영 | order-service | code, event |
| TASK-BE-042 | user-service 리뷰 수정 2차 — Kafka 발행 실패 처리, 불필요 이벤트 방지, 쿼리 최적화, 예외 위치 수정 | user-service | code |
| TASK-BE-041 | user-service 리뷰 수정 — 이벤트 트랜잭션 분리, DLQ 설정, Admin 권한 체크, 쿼리 최적화 | user-service | code, event |
| TASK-BE-040 | 배송 주소 관리 API — 목록 조회, 추가, 수정, 삭제 | user-service | code, api |
| TASK-BE-039 | 사용자 프로필 조회/수정 API + UserProfileUpdated 이벤트 발행 | user-service | code, api, event |
| TASK-BE-038 | user-service 부트스트랩 — 프로젝트 구조, DB 스키마, 도메인 모델, UserSignedUp 이벤트 소비 | user-service | code, event |
| TASK-FE-020 | 주문 목록 status 필터 미동작 수정 — 계약 업데이트 및 클라이언트 필터 연동 | admin-dashboard | code, api |
| TASK-FE-019 | admin-dashboard 주문 관리 기능 구현 — 주문 목록, 상세, 취소 | admin-dashboard | code, api |
| TASK-FE-018 | FilterBar searchValue prop 동기화 버그 수정 | admin-dashboard | code, test |
| TASK-FE-017 | 상품 목록 name 필터 구현 — API 계약 업데이트 및 프론트엔드 연동 | admin-dashboard | code, api |
| TASK-FE-016 | TASK-FE-015 리뷰 수정 — 버그 수정, 타입 안전성, 누락 테스트 추가 | admin-dashboard | code, test |
| TASK-FE-015 | admin-dashboard 기반 구성 및 상품 관리 기능 구현 | admin-dashboard | code, api |
| TASK-FE-014 | 주문 상세 페이지 결제 정보 표시 | web-store | code, api |
| TASK-FE-013 | TASK-FE-011 주문 플로우 누락 테스트 추가 | web-store | code, test |
| TASK-FE-012 | TASK-FE-011 주문 플로우 구현 수정 — FSD 위반, 페이지네이션, 계약 불일치, 코드 품질 | web-store | code, api |
| TASK-FE-011 | web-store 주문 플로우 구현 — 주문 생성, 주문 내역, 주문 상세 | web-store | code, api |
| TASK-FE-010 | web-store 장바구니 기능 구현 — 클라이언트 상태 관리 및 UI | web-store | code |
| TASK-FE-009 | web-store 인증 플로우 구현 — 로그인, 회원가입, 세션 관리 | web-store | code, api |
| TASK-FE-008 | api-client 테스트 보완 — 토큰 갱신 재시도 및 엣지 케이스 | frontend | code, test |
| TASK-FE-007 | web-store 컴포넌트 및 페이지 테스트 추가 | web-store | code, test |
| TASK-FE-006 | @repo/api-client 및 @repo/types 테스트 추가 | frontend | code, test |
| TASK-FE-005 | web-store 가격 필터 버그 및 UX 개선 수정 | web-store | code |
| TASK-FE-004 | api-client Payment API X-User-Id 헤더 누락 수정 (불필요 — Gateway 자동 주입) | frontend | code, api |
| TASK-FE-003 | web-store 상품 목록 및 검색 페이지 구현 | web-store | code, api |
| TASK-FE-002 | @repo/api-client 및 @repo/types 구현 — Gateway API 연동 기반 | frontend | code, api |
| TASK-FE-001 | 프론트엔드 모노레포 부트스트랩 — Turborepo + 공유 패키지 초기 구성 | frontend | code |
| TASK-BE-037 | 전 서비스 비즈니스 메트릭 구현 — Micrometer Counter/Histogram 등록 | all | code |
| TASK-BE-036 | docker-compose OpenTelemetry Java Agent 설정 — 트레이싱 전파 및 Jaeger 연동 | infra | code, infra |
| TASK-BE-035 | auth-service 비즈니스 메트릭 구현 — Micrometer Counter 6종 | auth-service | code |
| TASK-BE-034 | 전 서비스 Observability 적용 — 트레이싱, 메트릭 노출, Health Check 표준화 | all | code, infra |
| TASK-BE-033 | libs/java-observability 공통 라이브러리 — 구조화 로깅, MDC 필터, Micrometer 공통 설정 | libs | code |
| TASK-BE-032 | docker-compose 전체 구성 — 인프라 + 전체 서비스 컨테이너 | infra | code, infra |
| TASK-BE-031 | gateway-service 부트스트랩 — Spring Cloud Gateway, JWT 검증 필터, 라우팅, Rate Limiting | gateway-service | code, api |
| TASK-INT-005 | payment-service Kafka 전환 — 컨슈머 @KafkaListener, 프로듀서 KafkaTemplate | payment-service | code, kafka |
| TASK-INT-004 | order-service Kafka 전환 — 프로듀서/컨슈머 전환 | order-service | code, kafka |
| TASK-INT-003 | search-service Kafka 컨슈머 전환 — @KafkaListener + 이벤트 envelope | search-service | code, kafka |
| TASK-INT-002 | product-service Kafka 프로듀서 전환 — KafkaProductEventPublisher | product-service | code, kafka |
| TASK-INT-001 | Kafka 인프라 구성 — docker-compose, build.gradle, application.yml | infra | code, kafka |
| TASK-BE-030 | 결제 조회 API + OrderCancelled 소비 — 환불 처리 + PaymentRefunded 이벤트 | payment-service | code, api, event |
| TASK-BE-029 | payment-service OrderPlaced 이벤트 소비 — 결제 생성 + 처리 + PaymentCompleted 이벤트 | payment-service | code, event |
| TASK-BE-028 | payment-service 부트스트랩 — 프로젝트 구조, DB 스키마, 도메인 모델 | payment-service | code |
| TASK-BE-027 | order-service StockChanged 이벤트 소비 — ORDER_RESERVED 시 PENDING → CONFIRMED 전이 | order-service | code, event |
| TASK-BE-026 | 주문 취소 API — POST /api/orders/{orderId}/cancel + OrderCancelled 이벤트 | order-service | code, api, event |
| TASK-BE-025 | 주문 조회 API — GET /api/orders, GET /api/orders/{orderId} | order-service | code, api |
| TASK-BE-024 | 주문 생성 API — POST /api/orders + OrderPlaced 이벤트 | order-service | code, api, event |
| TASK-BE-023 | order-service 부트스트랩 — 프로젝트 구조, DB 스키마, 도메인 모델 | order-service | code |
| TASK-BE-022 | 상품 검색 API — GET /api/search/products (키워드, 필터, 팩싯) | search-service | code, api |
| TASK-BE-021 | 상품 이벤트 소비 및 인덱스 동기화 — ProductCreated/Updated/Deleted/StockChanged | search-service | code, event |
| TASK-BE-020 | search-service 부트스트랩 — 프로젝트 구조, Elasticsearch 설정, 도메인 모델 | search-service | code |
| TASK-BE-019 | 재고 조정 API + StockChanged 이벤트 | product-service | code, api, event |
| TASK-BE-018 | 상품 수정 + 삭제 API + ProductUpdated/ProductDeleted 이벤트 | product-service | code, api, event |
| TASK-BE-017 | 상품 등록 + 조회 API + ProductCreated 이벤트 | product-service | code, api, event |
| TASK-BE-016 | product-service 부트스트랩 — 프로젝트 구조, DB, 도메인 모델 | product-service | code |
| TASK-BE-015 | auth-service 동시 세션 제한 및 비활성 타임아웃 — 사용자당 최대 세션 수 제한, 비활성 세션 자동 만료 | auth-service | code |
| TASK-BE-014 | auth-service 도메인 이벤트 발행 — UserSignedUp, UserLoggedIn, UserLoggedOut, TokenRefreshed, LoginFailed | auth-service | code, event |
| TASK-BE-013 | auth-service 감사 로그 구현 — 로그인 시도, 토큰 갱신, 로그아웃 이력 기록 | auth-service | code, api |
| TASK-BE-012 | auth-service Token Rotation 원자성 보강 및 SHA-256 해시 중복 제거 | auth-service | code |
| TASK-BE-011 | auth-service 누락 테스트 추가 — LoginRateLimitFilter, RedisAccessTokenBlocklist, JsonAuthenticationEntryPoint, DTO 검증 | auth-service | code, test |
| TASK-BE-010 | auth-service 보안 및 에러 처리 수정 — Authorization 헤더 검증, AccessDeniedException 처리, 에러 코드 등록 | auth-service | code, api |
| TASK-BE-009 | auth-service refresh API 계약 수정 — 토큰 로테이션 응답 명시 | auth-service | code, api |
| TASK-BE-008 | auth-service 계약 준수 및 DB 최적화 — 타임스탬프 타입 수정, 중복 인덱스 제거, 트랜잭션 설정 | auth-service | code, api |
| TASK-BE-007 | auth-service 보안 및 데이터 정합성 수정 — 비밀번호 최대 길이 제한 및 Redis 원자적 연산 | auth-service | code |
| TASK-BE-006 | auth-service 레이어 의존성 위반 수정 — application→presentation DTO 제거 및 infrastructure config 추상화 | auth-service | code |
| TASK-BE-005 | auth-service refresh API 계약 준수 및 아키텍처 개선 | auth-service | code, api |
| TASK-BE-004 | auth-service 보안 버그 수정 — DUMMY_HASH, JWT secret 검증 | auth-service | code |
| TASK-BE-003 | 토큰 갱신 + 로그아웃 API | auth-service | code, api |
| TASK-BE-002 | 회원가입 + 로그인 + JWT 발급 API | auth-service | code, api |
| TASK-BE-001 | auth-service 부트스트랩 — 프로젝트 구조, DB/Redis, 도메인 모델 | auth-service | code |

apiVersion: v1
kind: Service
metadata:
  name: batch-worker
  namespace: ecommerce
  labels:
    app: batch-worker
spec:
  type: ClusterIP
  selector:
    app: batch-worker
  ports:
    - port: 8088
      targetPort: 8088
      protocol: TCP

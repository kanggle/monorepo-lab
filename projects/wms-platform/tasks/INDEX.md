# Tasks Index

This document defines task lifecycle, naming, and move rules.

---

# Lifecycle

backlog → ready → in-progress → review → done → archive

Only tasks in `ready/` may be implemented.

---

# Task Types

- `TASK-BE-XXX`: backend
- `TASK-INT-XXX`: integration

(`TASK-FE-XXX` is reserved but not used in this backend-only project.)

---

# Move Rules

## backlog → ready
Allowed only when:
- related specs exist
- related contracts are identified
- acceptance criteria are clear
- task template is complete

## ready → in-progress
Allowed only when implementation starts.

## in-progress → review
Allowed only when:
- implementation is complete
- tests are added
- contract/spec updates are completed if required

## review → done
Allowed only after review approval.

### Review Rules
- Tasks in `review/` must not be re-implemented directly.
- If a review reveals a bug or missing requirement, create a new fix task in `ready/` referencing the original task.
- Fix tasks must include the original task ID in their Goal section (e.g. "Fix issue found in TASK-BE-002").
- Do not modify a task file after it moves to `review/` or `done/`.

## done → archive
Allowed when no further active change is expected.

---

# Rule

Tasks must not be implemented from `backlog/`, `in-progress/`, `review/`, `done/`, or `archive/`.

---

# Task List

## backlog

(empty)

## ready

(empty)

## in-progress

(empty)

## review

- `TASK-BE-394-wms-tier-d-dedup-and-drift.md` — **REVIEW**. Closes the **Tier D** finding from TASK-BE-385 (identical-recap dedup, "verify identity per file first"). Per-file verification showed most flagged "duplication" is **legitimate** (architecture.md event/topic tables = summary + service-local trigger/effect + already cross-ref full schemas; admin error codes already cross-ref `rules/domains/wms.md`; notification DDL = self-disclaimed sketch) — left intact with recorded rationale. The one genuine verbatim duplication, `admin_event_dedupe`, was **triplicated with 2 drifts**: (D1a) admin domain-model §15 `outcome` enum listed **3** values, missing `IGNORED_DUPLICATE_LATE` (admin's LWW signal per the canonical) → added 4th + xref; (D1b) admin idempotency §2.2 inline DDL had a drifted index name (`admin_event_dedupe_processed_at_idx` vs canonical `idx_admin_event_dedupe_processed_at`) + comment-instead-of-CHECK → replaced the dup DDL block with a cross-ref to the canonical `database-design.md` §6, keeping the outcome-semantics prose; (D2) notification domain-model sketch's stale "BE-043 PR" pointer → retargeted to `database-design.md`. Canonical = admin `database-design.md` §6 (4-outcome named CHECK + `idx_*`). Doc-only — `git diff apps/**` empty; D1a adds a missing enum value to a stale doc copy to match the canonical (no schema change). 분석=Opus 4.8 / 구현=Opus.

## done

- `TASK-BE-392-wms-tier-c-saga-semantics-drift.md` — **DONE (2026-06-16, 3-dim verified)**. Code-verified **Tier C** drift-fix (config / state-machine semantics) from TASK-BE-385 § Findings, the tier TASK-BE-387 deferred. Five fixes, each anchored to production code/config: (1) outbound-saga.md §9 sweeper-cap "starts at 10"→resolved `5` (per `application.yml` `max-attempts: 5` + the doc's own §4.2); (2) added terminal `STUCK_RECOVERY_FAILED` to the saga-status.md ASCII+Mermaid and outbound domain-model.md §6 ASCII+terminal-list (present in `SagaStatus` enum + States table but absent from diagrams) + marked the stale domain-model "Open Items" list resolved (architecture.md backfill audit = all ✅); (3) removed the phantom "operator force-completes" from saga-status.md (no v1 force-complete; transition table lists only `inventory.confirmed`/`:retry-tms-notify`); (4) MANUAL reservation release reason_code `ADJUSTMENT_RECLASSIFY`→`PICKING_CANCELLED` in reservation-saga.md + reservation-status.md (per `ReleaseReservationService.reasonCodeFor`: `MANUAL→PICKING_CANCELLED` unconditional); (5) master domain-model.md lot ASCII label `EXPIRED (terminal)`→`(terminal for reactivation)` (matches the already-correct prose + `Lot`/`LotStatus` code: `EXPIRED→INACTIVE` permitted). Doc-only — `git diff apps/**` empty; no enum value / topic / Flyway / contract change. Deferred: Tier D (recap dedup) / Tier E (notification authoring). impl PR #1689 squash `a422339ff`. **3-dim**: (a) MERGED+`a422339ff`; (b) origin/main tip `a422339ff`; (c) pre-merge failing=0 (doc-only — all code/e2e jobs path-filter-skipped, `changes` pass). 분석=Opus 4.8 / 구현=Opus. [[project_refactor_sweep_status]]

- `TASK-BE-391-wms-exception-handler-status-map-collapse.md` — **DONE (2026-06-15, 3-dim — PR #1656 `be0a264b2`)**. Collapsed the per-entity `@ExceptionHandler` boilerplate in all 5 WMS `GlobalExceptionHandler`s (master/admin/inbound/outbound/inventory) into a single domain-exception dispatcher backed by a `Class→HttpStatus` map. Behavior-preserving (exact status + body; two default variants 500-warn vs 422 preserved; special framework handlers untouched; inventory redundant-422 handlers collapsed). **Net −153 LOC**, no domain/contract change. 20/20 CI SUCCESS (incl. master+outbound Testcontainers IT). F3 finding from the 2026-06-15 WMS refactor recon (F1/F4 deferred=shared-lib, F2 declined=HARDSTOP-03). 분석=Opus 4.8 / 구현=Opus 4.8.

- `TASK-BE-387-wms-spec-drift-fix.md` — **DONE (2026-06-15, 3-dim verified)**. Closes the **code-verified Tier A/B drift** flagged by TASK-BE-385 § Findings. Each fix direction confirmed against production code/migration/rule before edit (not agent claims): `inventory.adjusted`→`inventory.reserve.failed` reserve-failure reply (5 inventory/outbound-events files, per `InventoryReserveFailedEvent`+MONO-196) · `priorStatus`→`previousStatus` (3 notification files + notification-subscriptions contract, per `OrderCancelledEvent`) · notification overview 6 real subscribed topics (per `AlertConsumer`) · dedupe CHECK 2→4 values + `delivery.failed`→`delivered` topic (per `V1__init.sql`) · `SENT`→`SUCCEEDED` (per `DeliveryStatus`) · `inbound.asn.created`→`received` (per `AsnReceivedEvent`) · `outbound.picking.confirmed`→`completed` (per `PickingCompletedEvent`) · master location uniqueness within-warehouse→global (per W3 + `V4__init_location.sql`). 15 spec files, doc-only meaning-preserving (apps/ diff=0); canonical `inventory-events.md` unedited. Deferred: Tier C–E (config/dedup/authoring). impl PR #1638 squash `a2cb21c94`. **3-dim**: (a) MERGED+`a2cb21c94`; (b) origin/main tip `a2cb21c94`; (c) pre-merge failing=0 (Frontend E2E smoke pass 4m10s + `changes` pass). 분석=Opus 4.8 / 구현=Opus. [[project_refactor_sweep_status]]
- `TASK-BE-385-wms-spec-refactor-pass.md` — **DONE (2026-06-15, 3-dim verified)**. Full `/refactor-spec` pass over all wms specs (7 services + contracts + integration) via 3 parallel discovery agents + per-finding re-verification. **Applied (meaning-preserving, verified)**: `WMS_SUPERVISOR`→`WMS_SUPERADMIN` (admin database-design seed-doc typo, confirmed vs the actual V99 seed) + `ShippingRecord`→`Shipment` ×3 (outbound overview term drift) + `## Public surface (routes)`→`## Public surface` (gateway, anchor-safe) + gateway external-integrations orphan cross-ref. **Headline**: wms specs structurally healthy (0 dead-refs / 521 links, consistent terms). **Findings (flagged, NOT auto-fixed — drift requiring a verified follow-up)**: Tier A service-doc↔canonical-contract value divergences (`inventory.adjusted`↔`inventory.reserve.failed` residue, notification wrong subscribed-topics/enum/`priorStatus`↔`previousStatus`), Tier B event/aggregate name drift in overviews, Tier C saga config/state semantics, Tier D identical-recap dedup, Tier E notification domain-model missing sections (authoring). Recommend follow-up TASK-BE-386 (verified drift-fix). impl PR #1629 squash `dfeff0b52`. **3-dim**: (a) MERGED+`dfeff0b52`; (b) origin/main tip `dfeff0b52`; (c) pre-merge failing=0 (doc-only — code/e2e jobs path-filter-skipped, `changes` pass). No code / contract / Flyway change. 분석=Opus 4.8 / 구현=Opus. [[project_refactor_sweep_status]]
- `TASK-BE-383-wms-roles-only-operator-spec-alignment.md` — **DONE (2026-06-15, 3-dim verified)**. Doc-only, net-zero spec alignment: brought the wms service specs into conformance with the roles-only identity model (ADR-MONO-032 D5 step 4/5 `account_type` removal + ADR-MONO-035 O1/4a operator domain-role derivation at assume-tenant, `wms → WMS_OPERATOR`). Unlike erp (ERP-BE-021), wms specs **never carried** the literal `account_type=OPERATOR` framing and were already roles-based — the drift was a **documentation gap**: no wms spec recorded where an operator's `WMS_OPERATOR` role originates post-ADR-035. Edits: admin-service architecture § Security (operator domain-role source bullet + related the entitlement-trust `WMS_VIEWER` synthesis to the new operator-role path, net-zero) + iam-integration.md (assume-tenant derivation). No code / contract / Flyway / ADR-decision change. impl PR #1615 squash `d80d05496`. **3-dim**: (a) MERGED+`d80d05496`; (b) origin/main tip `d80d05496`; (c) pre-merge failing=0 (all code/e2e jobs path-filter-skipped, `changes` pass). 분석=Opus 4.8 / 구현=Opus (doc-only). [[project_adr032_unified_identity_roles]]
- `TASK-BE-334-outbound-securityconfig-web-conditional.md` — **DONE (2026-06-03, 3-dim verified)**. BE-333 follow-up — made outbound `SecurityConfig` `@ConditionalOnWebApplication(type = SERVLET)`. After BE-333 fixed the bean-override + Flyway blockers, the outbound `@SpringBootTest(webEnvironment=NONE)` ITs unmasked the next context-load layer: `securityFilterChain(HttpSecurity, …)` needs `HttpSecurity` (servlet-web only) → `No qualifying bean of type HttpSecurity` in NONE. Fix = web-conditional (production servlet web app → loads; NONE ITs → skip). `:check` green (web-slice/`@WebMvcTest` unaffected; production security unchanged); the securityFilterChain context-load failure eliminated (SagaSweeperIT/TmsClientAdapterIT now load context). impl PR #1047 squash `fe9607b6`. **3-dim**: (a) MERGED+`fe9607b6`; (b) origin/main tip `fe9607b6`; (c) pre-merge failing=0 (19 checks pass). **Deferred (user decision 2026-06-03 — low ROI, undeployed service)**: full outbound `integrationTest` green + CI wiring. Fixing the context-load layers (BE-333 bean+flyway, BE-334 security) unmasked further independent per-test issues (the suite was never run): `SagaSweeperIT`/`TmsClientAdapterIT` insert `outbound_saga`/`shipment` rows with no parent `outbound_order` → FK violation; `IdempotencyFilterRedisIT` asserts 409 gets 201. outbound-service is undeployed (absent from federation/demo/CI E2E) → full test-by-test IT repair + CI-gating is a dedicated effort if/when outbound is deployed. **메타: 컨텍스트-로드 결함 다층(bean-override→flyway→security→test-data FK→functional); 미배포·미-CI 서비스는 전 레이어 미검증 → "green 만들기"=IT 재작성. ROI 판단 후 production-correct 전제층(BE-333 startup+migration, BE-334 security)만 incremental landing, 나머지 정직하게 deferred — 무리한 자율 grind 금지.** 분석=Opus 4.8 / 구현=Opus(직접).
- `TASK-BE-333-outbound-non-standalone-startup-and-tms-dedupe-migration-repair.md` — **DONE (2026-06-03, 3-dim verified)**. Repaired two latent breakages that stopped `outbound-service` starting in any non-`standalone` profile (surfaced by the BE-332 "wire outbound IT into CI" follow-up; latent because outbound is undeployed in every running stack + its `integrationTest` is not CI-gated): **(1)** `outboxPublisher` **bean-name collision** — outbound's `@Component OutboxPublisher` (`@Profile("!standalone")`, default name `outboxPublisher`) vs libs `OutboxAutoConfiguration` `@Bean outboxPublisher()`; the auto-config's `@ConditionalOnMissingBean` is **by-type** and outbound's bean is a different type (`extends AbstractOutboxPublisher`) → not suppressed → two `outboxPublisher` definitions → `BeanDefinitionOverrideException` (every `@SpringBootTest` IT + non-standalone runtime). Fix = `@SpringBootApplication(exclude = OutboxAutoConfiguration.class)` (outbound is outbox-self-contained — uses no libs auto-config bean, verified). **(2)** `tms_request_dedupe` **Flyway conflict** — V4 created it as `(shipment_id, idempotency_key, tms_status, requested_at)` but V13 + the runtime entity `TmsRequestDedupeEntity` use `(request_id, sent_at, response_snapshot)`; V13's `CREATE TABLE IF NOT EXISTS` no-ops then its `sent_at` index fails (`column "sent_at" does not exist`). Fix = reconcile V4 to the canonical (entity/V13) schema (safe in place — no deployed outbound DB → no applied checksum). **AC verified**: `:outbound-service:check` green (CI Build & Test 2m44s); the post-fix IT run eliminated the bean-override + Flyway errors (6 ITs now load+pass). Diff confined to `OutboundServiceApplication.java` + `V4__…sql`. impl PR #1045 squash `23de0560`. **3-dim**: (a) MERGED+`23de0560`; (b) origin/main tip `23de0560`; (c) pre-merge failing=0 (20 checks pass). **Deferred (separate task)**: full outbound `integrationTest` suite green + **CI wiring** — fixing (1)+(2) unmasked further independent pre-existing web-env IT failures (`securityFilterChain` needs `HttpSecurity` under `@SpringBootTest(webEnvironment=NONE)`; idempotency servlet filter inactive in `NONE`; `TmsClientAdapterIT`) = a web-environment IT overhaul. **메타: ① `@ConditionalOnMissingBean`=by-TYPE — 공유 auto-config 빈과 서비스 자체 빈 이름이 겹치고 타입이 다르면 충돌 → exclude/distinct-name. ② 컨텍스트-로드 결함은 다층(startup→flyway→security→filter); 한 층 고치면 다음이 드러남 — 승인 범위 초과 발견 시 정직하게 재스코핑(전제층만 복구, web-env overhaul 분리). ③ 미배포 서비스 마이그레이션=적용상태 없어 in-place 교정 안전.** 분석=Opus 4.8 / 구현=Opus(직접).
- `TASK-BE-332-wms-readmodel-nullable-temporal-filter-pg-type-inference-class-cleanup.md` — **DONE (2026-06-03, 3-dim verified)**. BE-331 audit follow-up — the audit swept every `@Query` repository and found **5 more** wms-platform PostgreSQL read-model queries with the same nullable-**temporal** `:p IS NULL OR …` 42P18 pattern (each 500s on an unfiltered search): admin `ShipmentSummaryRepository`(shippedAt)/`OrderSummaryRepository`(requiredShipDate)/`AsnSummaryRepository`(expectedArriveDate), master `JpaLotRepository`(expiryDate), outbound `OrderJpaRepository.findFiltered`+`countFiltered`(requestedShipDate+createdAt). Fix = `CAST(:p AS string)` on each temporal IS-NULL guard (query text only; `>=`/`<=`/`<`/`>` keeps temporal typing). **Audit ruled out (verified, NOT bugs)**: global-account(security/admin/account)+erp+finance = **MySQL** (42P18 is PostgreSQL-specific; proven by `AdminActionJpaRepositoryTest` on MySQLContainer calling `search(null,…)` GREEN — the `:from`/`:to` matches there are false positives), scm procurement = no temporal filter, Throughput/Notification/Reservation = required non-null params. **6 regression tests (real PostgreSQL 16, all-null path)**: admin `ReadModelPersistenceIntegrationTest` +3 (`:integrationTest` 14/14, CI-gated via the master+notification Integration job 2m24s) + master `LotRepositoryImplTest` +1 + outbound `OrderJpaRepositoryFilterIT` (new `@DataJpaTest` PG slice, `ddl-auto=create-drop` to sidestep a pre-existing outbound test-infra issue: V13 tms_request_dedupe Flyway + `@SpringBootTest` outboxPublisher bean-override) +2 — all green locally. impl PR #1043 squash `02f3eef0`. **3-dim**: (a) MERGED+`02f3eef0`; (b) origin/main tip `02f3eef0`; (c) pre-merge failing=0 (20 checks pass). No live redeploy (these are dashboard endpoints other than the console WMS 운영 inventory+alerts page, which BE-331 already fixed). **메타: ① audit 의 핵심 가치 = 가정 말고 검증 — 동일-패턴 매치 중 MySQL 서비스는 false positive(42P18=PostgreSQL 전용); DB 타입을 실 테스트로 확인. ② read-model `@Query` 의 nullable temporal `:p IS NULL` 은 PostgreSQL CAST 필수, round-trip 테스트만으론 못 잡으니 all-null 쿼리-실행 테스트 동반. ③ 선재 테스트-인프라 결함(outbound)은 `@DataJpaTest`+`create-drop` 슬라이스로 우회 — full-context IT 불가 시 쿼리만 격리 검증.** 별건 follow-up: outbound `integrationTest` CI 배선 + outbound 테스트-인프라(V13/bean-override) 정리. 분석=Opus 4.8 / 구현=Opus(직접).
- `TASK-BE-331-admin-dashboard-nullable-temporal-filter-pg-type-inference.md` — **DONE (2026-06-03, 3-dim verified)**. Fixed the PostgreSQL `42P18 could not determine data type of parameter` **500** on the unfiltered `GET /dashboard/alerts` (+ identical latent defect on `/dashboard/adjustments`). `AlertLogRepository.search` / `AdjustmentAuditRepository.search` guarded nullable **temporal** bounds (`detectedAtFrom/To`, `occurredAtFrom/To`) with a bare `:p IS NULL` → an untyped null PostgreSQL cannot type → `InvalidDataAccessResourceUsageException` → 500. The platform-console WMS 운영 section degraded as a whole (the alerts leg 500 drops `Promise.all([inventory, alerts])`); the inventory leg itself was 200. Real stacktrace `PSQLException 42P18 at AlertDashboardController.list:62`. Fix = `CAST(:p AS string)` on the temporal IS-NULL guards (query text only — the `>=`/`<=` keeps temporal typing, cast-to-string preserves IS-NULL semantics); UUID/String/Boolean bounds resolve fine and are untouched. **Undetected** because the WebMvc slice **mocks** the repository, and the real-PostgreSQL `ReadModelPersistenceIntegrationTest` (which DOES run in CI — the "Integration (master+notification)" job invokes `admin-service:integrationTest`) only **round-tripped** these entities (save/findById) and never called `search()` with all-null filters → a **test-coverage gap**, not a CI-wiring gap. (Corrected: an earlier framing here called integration "excluded from CI"; it is excluded from `:check` but runs via the dedicated Integration job.) **AC-1…5 verified**: 2 regression tests added (all-null filters, real PostgreSQL 16) → `:integrationTest` 11/11 green locally + `:check` green; diff confined to 2 `@Query` strings + 2 tests. AC-6: admin-service rebuilt (bootJar) + redeployed into the demo stack (healthy, clean Hibernate `@Query` validation at startup) — browser render = user smoke. impl PR #1041 squash `f3d1c6e1`. **3-dim**: (a) MERGED+`f3d1c6e1`; (b) origin/main tip `f3d1c6e1`; (c) pre-merge failing=0 (20 checks pass). **메타: MONO-170 데모가 노출한 4번째(마지막) drift — producer 의 실제 PostgreSQL 쿼리 버그. read-model 쿼리의 nullable temporal `:p IS NULL` 은 PostgreSQL 에서 CAST 필요; mock-only WebMvc + integration-excluded-from-CI 조합이 sender. 별건 follow-up: admin-service integrationTest 를 CI 에 배선(real-PG 게이트 부재가 근인).** 분석=Opus 4.8 / 구현=Opus(직접).
- `TASK-BE-323-tenant-gate-entitlement-trust-dual-accept-wms.md` — **DONE (2026-05-31)**. ADR-MONO-019 § 3.3 step 3 복제 3/N (wms, finance→erp→scm→**wms**). 6 `TenantClaimValidator`(gateway/admin/inbound/inventory/master/outbound)를 `tenant_id==wms` strict → **entitlement-trust dual-accept**(legacy strict equals ∪ 서명 `entitled_domains ∋ wms`; 거부=둘 다 불충족, fail-closed). **wildcard `*` 미도입**(blueprint `WILDCARD_TENANT` 의도적 제외 — wms net-zero). 서비스별 로컬 isEntitled(모듈 경계). admin `TenantClaimValidatorTest` 신규. gateway overview.md 갱신. impl PR #966 squash `0e20b02a`. 3차원 ✓ (MERGED / tip 일치 / pre-merge 0 non-success). CI 1차 `LocationIntegrationTest:142` 선재 flake(`shortSuffix()` 충돌, validator 무관) → `--failed` 재실행 GREEN. 남은 step 3=gap+console-bff + GAP `entitled_domains` populate(shared). 분석=Opus 4.8 / 구현=Opus.
- `TASK-BE-308-outbound-packing-qty-aggregator-dedup.md` — **DONE (F-L6-1 single-finding cross-class duplication closure — wms cluster 7번째 (마지막) service sweep, TRUE 0 7/7 milestone 도달)**. 분석/리뷰=Opus 4.7 / 구현=Sonnet 4.6 (worktree refactoring-engineer agent). outbound-service strategic-sampling dry-run (258 main file scope — wms cluster largest surface, saga orchestrator): L0/L1/L2/L3/L4=0, L6=1 (본 finding). base rate ≈ 0.004/file = wms cluster mature-implementation 신기록 (master 0.005 + inbound 0.005 와 동급). 7 intentional preservation 명시 (verify exemption — InventoryConsumerSupport admin BE-300 lesson applied / MasterEventConsumer single-dispatcher admin BE-300 lesson / 6 MasterXxxProjector strategy admin BE-300 lesson / EventDedupeRepositoryImpl Propagation.MANDATORY BE-027 pattern / 3 DataIntegrityViolationException 다른 outcome BE-307 동일 conclusion / MasterReadModelRepositoryImpl per-aggregate native SQL / 5 application service 모두 5+ helper method-extracted). **spec PR #885** (squash `0ba865b9`, mergedAt 2026-05-27 KST, markdown fast-lane). **impl PR #886** (squash `35c0fb69`, mergedAt 2026-05-27 KST, +55/-20 net +35 LOC across 3 files: 신규 PackingQtyAggregator 1 + PackingService modify + ConfirmShippingService modify; **CI 19 SUCCESS + 1 SKIPPED + 0 FAILURE** incl. wms IT/E2E + outbound IT + saga IT + GAP IT + scm IT + finance IT + erp IT + console-bff IT + 4 E2E job authoritative GREEN — Hexagonal port/adapter + 4-step saga state machine + TMS adapter + ERP webhook + outbox event 패턴 보존 검증; **명시적 git add 으로 `.clone/` leak 회피** = BE-307 lesson applied). **Implementation**: 신규 `com.wms.outbound.application.service.PackingQtyAggregator` (`final class` package-private + `private constructor` + `static Map<UUID, Long> sumByOrderLine(List<PackingUnit>)` — 4-line `Map.merge` body byte-identical transcribe). **3 callsite 1-line 치환**: (a) `PackingService.allUnitsCoverAllLines` L211-216 → `Map<UUID, Long> sumByOrderLine = PackingQtyAggregator.sumByOrderLine(units);` (boolean-return logic 보존) (b) `PackingService.validatePackingCompleteness` L309-314 → same 1-line (throw-exception logic 보존) (c) `ConfirmShippingService.buildShippingEventLines` L231-236 → `Map<UUID, Long> packedSumByOrderLine = PackingQtyAggregator.sumByOrderLine(packingUnits);` (caller-specific variable name `packedSumByOrderLine` 보존). 불필요한 import 정리 (`HashMap` from PackingService + `PackingUnitLine` from ConfirmShippingService). byte-identical preservation: HashMap iteration order + Long::sum aggregation + `(long) PackingUnitLine.qty` widening + 5 PackingService methods (create/seal/confirm/completePackingFromSeal/emitPackingCompleted/toResult) + 6 ConfirmShippingService methods (confirm/assertVersionAndStatus/loadShippingAggregates/buildShipmentRecord/emitShippingOutbox/generateShipmentNo) unchanged + all exception types + messages + Order/Shipment/Saga aggregate transitions + all outbox event constructor calls byte-identical. **AC verify**: AC-1 PackingQtyAggregator shape (package-private final + private ctor + static method, 6-line body byte-identical) ✓ / AC-2 PackingService.allUnitsCoverAllLines 1-line 치환 + boolean-return logic 보존 ✓ / AC-3 PackingService.validatePackingCompleteness 1-line 치환 + throw-exception logic 보존 ✓ / AC-4 ConfirmShippingService.buildShippingEventLines 1-line 치환 + 변수명 `packedSumByOrderLine` 보존 ✓ / AC-5 PackingService 5 method + ConfirmShippingService 6 method unchanged ✓ / AC-6 import 정리 (`HashMap`/`PackingUnitLine` 사용처 없음 → 제거) ✓ / AC-7 worktree agent BUILD SUCCESSFUL 200 tests baseline + after ✓ / AC-8 CI 19/20 GREEN authoritative incl. outbound IT (Testcontainers Postgres + Kafka) + saga IT + TMS WireMock IT ✓ / AC-9 cross-service drift = 0 (only outbound-service 3 files modified, explicit git add for INDEX + task rename — no `.clone/` leak per BE-307 lesson) — **43회째 zero-retrofit invariant** (`projects/{scm,global-account,erp,fan,ecommerce-microservices,finance,platform-console}-platform/` + `libs/` + 다른 6 wms service byte-unchanged) ✓ / AC-10 domain method + port interface + API contract + event payload + DB schema 변경 0 ✓ / AC-11 3 callsite Map result byte-identical (HashMap iteration order + Long widening 보존) ✓. BE-297/299/300/301/302/303/304/305/306/307 회귀 0. **dispatcher 독립 재검증** (BE-303 3-dim): **spec PR #885**: (a) state=MERGED + mergeCommit=`0ba865b9`; (b) `git log origin/main` tip `0ba865b9` Fast-forward `0fd1c07e..0ba865b9`; (c) markdown fast-lane — 3-dim ALL GREEN. **impl PR #886**: (a) state=MERGED + mergeCommit=`35c0fb69` + 19 SUCCESS + 1 SKIPPED + 0 FAILURE; (b) `git log origin/main` tip `35c0fb69` Fast-forward `0ba865b9..35c0fb69`; (c) pre-merge failing=0 — 3-dim ALL GREEN. **close chore (this PR)** — `git mv review/ → done/` + Status `review → done` + INDEX move; BE-299 re-stage check verified; BE-303 3-dim 객관 머지 검증 완료 before close chore start. **메타**: ① **wms cluster 7/7 TRUE 0 (100%) milestone 도달** — admin (BE-297/299/300, 3 cohort) + inventory (BE-301/302/303, 3 cycle) + gateway (BE-304, single finding) + notification (BE-305, single finding) + master (BE-306, single finding) + inbound (BE-307, single finding) + **outbound (BE-308, single finding)**. 단일 cluster 의 7 service sequential sweep 완전 closure = depth-first sweep cohort 의 신기록. **6-cluster TRUE 0** (admin + inventory + scm + erp + global-account + **wms whole cluster**) — multi-service mature-implementation 영역 확장. ② **helper extraction hexology + cross-class utility 5번째 instance** — BE-300 ProjectionConsumerSupport (admin, infra/messaging/) + BE-301 JwtHelper (inventory, adapter/in/web/) + BE-304 ReactiveJwtAccess (gateway, security/) + BE-306 PageableFactory (master, adapter/out/persistence/) + **BE-308 PackingQtyAggregator (outbound, application/service/)** = 5 cross-class utility + BE-305 markPermanentAndPersist (notification, same-class private method) + BE-307 ReceiveAsnService method extraction (inbound, in-class long-method polish) = **7 helper extractions** wms cluster 전체. wms cluster 의 helper trilogy → quadrilogy → pentology → hexology → **heptology** evolution. ③ **strategic-sampling dry-run mature-implementation 신기록** — outbound-service 258 file scope 의 base rate 0.004/file = wms cluster mature-implementation 신기록. master 0.005 + inbound 0.005 + outbound 0.004 = top 3 mature service. outbound = wms cluster largest surface (saga orchestrator + TMS integration + 6 application service + 9 RepositoryImpl) 임에도 mature implementation. cluster-wide depth-first sweep 가 깊어질수록 base rate ↓ + intentional preservation framework ↑. ④ **7 intentional preservation framework 정립** — BE-307 6-카테고리 hierarchy 발전 (7번째 = "already method-extracted services" 추가). outbound 의 5 application service 모두 5+ helper method-extracted 상태 = wms cluster sweep 시 미리 적용된 BE-307 ReceiveAsnService 패턴. 미래 sweep 시 "intentional preservation 7-카테고리 framework" 가 verify exemption 표준. ⑤ **wms cluster TRUE 0 7/7 (100%) milestone 의 다음 trigger** — wms 모든 service sweep 완료 → 다음 자연 trigger = **다른 cluster** (finance / erp / fan / ecommerce-microservices) base rate 측정 OR **libs v2** (≥ 2026-06-10 cadence lock) OR **BE-309 cross-service spec sweep**. 단일 cluster 의 7-service sequential sweep cohort 완전 종결 = depth-first sweep methodology 의 reference implementation.

- `TASK-BE-307-inbound-receive-asn-long-method.md` — **DONE (F-L3-1 single-finding long-method polish closure — wms cluster 6번째 service sweep, 6/7 coverage 도달)**. 분석/리뷰=Opus 4.7 / 구현=Sonnet 4.6 (worktree refactoring-engineer agent). inbound-service strategic-sampling dry-run (199 main file scope, ~15 file Read + grep verify): L0/L1/L2/L4/L6=0, L3=1 (본 finding) + 1 borderline (F-L3-2 ConfirmPutawayLineService Out 명시). 6 intentional preservation 명시 (verify exemption — 6 MasterXxxProjector strategy admin BE-300 lesson applied / EventDedupeRepositoryImpl Propagation.MANDATORY BE-027 pattern / ErpWebhookInboxProcessor self-injection 회피 / MasterReadModelRepositoryImpl 6 per-aggregate SQL / ErpAsnWebhookController 4 private method 이미 분리 / InstructPutawayService + InspectionService + CloseAsnService 이미 method-extracted). **spec PR #882** (squash `6352b4c1`, mergedAt 2026-05-27 KST, markdown fast-lane). **impl PR #883** (squash `0564da3b`, mergedAt 2026-05-27 KST, +46/-30 net +16 single file `ReceiveAsnService.java` (+ 사용자 Option A cleanup commit `7ee263b4` 으로 `.clone/worktrees/agent-a0ab6e4ea8e0f33ff/` stray scm-platform file 2 제거 — agent worktree leak hazard 42회째 invariant 회복); **CI 19 SUCCESS + 1 SKIPPED + 0 FAILURE** incl. wms IT/E2E + GAP IT + scm IT + finance IT + erp IT + console-bff IT + 4 E2E job authoritative GREEN — Hexagonal port/adapter + ASN state machine + master read-model snapshot + outbox 패턴 보존 검증). **Implementation**: 4 신규 private method 추출 (`resolveActiveWarehouseOrThrow(UUID) → void`, `resolveSupplierPartnerOrThrow(UUID) → PartnerSnapshot`, `resolveAsnNoOrThrow(String) → String`, `buildAsnLines(List, UUID) → List<AsnLine>`). `receive()` body **58 → 29 LOC** (~50% reduction). byte-identical preservation: exception type (WarehouseNotFoundInReadModelException × 2-throw / PartnerInvalidTypeException × 2-message variant "not found in read model" + "status=X type=Y" / AsnNoDuplicateException × 1 / SkuInactiveException × 2-throw) + Asn 13-arg constructor + AsnReceivedEvent 10-arg constructor + log.info format ("asn_received asnId={} asnNo={} source={}") + `isSystemActor`/`buildEventLines`/`toResult` static (InspectionService 외부 사용 surface 보존) + Authorization guard L59-61 위치 + `@Override @Transactional` annotation. **AC verify**: AC-1 4 신규 private method shape (signature + byte-identical body) ✓ / AC-2 receive() ~29 LOC 축소 + WarehouseSnapshot 변수 제거 (void return) + 3 변수 (partner / asnNo / lines) 보존 ✓ / AC-3 isSystemActor + buildEventLines + toResult static 3 method unchanged + InspectionService.start() `ReceiveAsnService.toResult(saved)` 외부 사용 surface 보존 ✓ / AC-4 @Override @Transactional annotation 위치 unchanged ✓ / AC-5 CI 19/20 GREEN authoritative (wms IT 포함, 회귀 0) ✓ / AC-6 cross-service drift = 0 (only `ReceiveAsnService.java` modified, .clone/worktrees/ leak 사용자 Option A 으로 회복) — **42회째 zero-retrofit invariant** (`projects/{scm,global-account,erp,fan,ecommerce-microservices,finance,platform-console}-platform/` + `libs/` + 다른 6 wms service byte-unchanged) ✓ / AC-7 AsnReceivedEvent payload 10 field byte-identical + log.info format string byte-identical ✓ / AC-8 Asn 13-arg constructor + 5 port interface (AsnPersistencePort.save / existsByAsnNo + AsnNoSequencePort.nextAsnNo + MasterReadModelPort.findWarehouse/findPartner/findSku) unchanged ✓ / AC-9 test 변경 = mechanical only ✓. BE-297/299/300/301/302/303/304/305/306 회귀 0. **dispatcher 독립 재검증** (BE-303 3-dim): **spec PR #882**: (a) state=MERGED + mergeCommit=`6352b4c1`; (b) `git log origin/main` tip `6352b4c1` Fast-forward `64d0921c..6352b4c1`; (c) markdown fast-lane — 3-dim ALL GREEN. **impl PR #883**: (a) state=MERGED + mergeCommit=`0564da3b` + 19 SUCCESS + 1 SKIPPED + 0 FAILURE; (b) `git log origin/main` tip `0564da3b` Fast-forward `6352b4c1..0564da3b`; (c) pre-merge failing=0 — 3-dim ALL GREEN. **close chore (this PR)** — `git mv review/ → done/` + Status `review → done` + INDEX move; BE-299 re-stage check verified; BE-303 3-dim 객관 머지 검증 완료 before close chore start. **메타**: ① **wms cluster 6번째 service sweep = 6/7 coverage (~86%)** — admin + inventory + gateway + notification + master + **inbound**. 잔존 = outbound 1 service. wms cluster TRUE 0 (7/7) 도달까지 1 service. 단일 cluster 의 6 service sequential sweep = depth-first sweep cohort 의 신기록 단일-cluster 실행. ② **strategic-sampling dry-run 의 mature-implementation hierarchy** — inbound-service 199 file scope 의 base rate ≈ 0.005/file (master 0.005/file 와 동일 수준). gateway 0.07/file (small surface) + notification 0.018/file (event-consumer) + admin/inventory 0.16-0.27/file (high cohort) 와 비교 시 **inbound = master-tier mature**. wms cluster 의 hexagonal-architecture-uniform service surface 중 sweep coverage 가 깊어질수록 base rate ↓ 경향. ③ **6 intentional preservation hierarchy 정립** — BE-301 9th-instance / BE-302 10th-instance 의 발전형. 본 task 의 6 intentional preservation: (a) cross-class strategy pattern 의 admin BE-300 lesson applied (`MasterEventConsumer` javadoc L27-32 명시) — Spring AOP self-invocation hazard 회피 (b) Propagation.MANDATORY pattern 의 BE-027 reference (`EventDedupeRepositoryImpl` javadoc 명시) (c) self-injection 회피 의 dedicated bean 패턴 (`ErpWebhookInboxProcessor` + `WebhookInboxStatusUpdater` javadoc 명시) (d) per-aggregate native SQL 의 over-abstraction 회피 (`MasterReadModelRepositoryImpl` 6 upsert SQL) (e) 이미-extracted method 의 추가 extraction 회피 (`ErpAsnWebhookController.receive` 4 method) (f) 이미-extracted service method 의 추가 extraction 회피 (`InstructPutawayService` + `InspectionService` + `CloseAsnService`). 미래 sweep 시 "intentional preservation 6-카테고리 hierarchy" 가 verify exemption 표준 framework. ④ **`.clone/worktrees/` leak hazard recovery via 사용자 Option A** — `git add -A` 가 prior agent worktree leak `.clone/worktrees/agent-a0ab6e4ea8e0f33ff/` 의 untracked file 2 (scm-platform 영역) 까지 스테이지 → 의도하지 않은 cross-project drift 발생. impl PR #883 의 첫 push (commit `4789ffd2`) 에 포함됨. 사용자 가 Option A (`git rm -r .clone/worktrees/` + commit `7ee263b4` + push) 으로 cleanup → squash-merge 시 의도된 1-file scope 만 main 으로 들어감 (42회째 zero-retrofit invariant 회복). **메타 lesson**: `git add -A` 사용 시 untracked-but-conflicting paths 검토 의무. 명시적 file path 사용 권장 (`git add <file1> <file2> ...`). Future close chore 시 `git status --short` 으로 untracked listing 먼저 확인. ⑤ **single-task scope conservative + dry-run finding granularity 정립** — BE-303 cohort B (3 finding) 가 inventory 의 cohort scope, BE-305/306/307 single finding 이 small-surface OR mature-implementation 의 conservative scope. base rate 0.005-0.018/file = single-task 충분. base rate ≥0.1/file (admin BE-297/299/300, inventory BE-301) = cohort scope 가능. **inbound 6/7 milestone 의 wms cluster TRUE 0 도달 trigger** — 잔존 outbound 1 service 만 sweep 완료 시 wms cluster 7/7 TRUE 0 도달.

- `TASK-BE-306-master-pageable-factory.md` — **DONE (F-L6-1 cross-class duplication closure — wms cluster 5번째 helper extraction, cross-class utility 4번째 instance)**. 분석/리뷰=Opus 4.7 / 구현=Sonnet 4.6 (worktree refactoring-engineer agent). master-service strategic-sampling dry-run: 214 main file scope (6 aggregate × Hexagonal), L0/L1/L2/L3/L4 = 0 (Hexagonal 깨끗, AggregateVersionGuard 이미 추출), L6=1 대규모 cross-class. **5번째 wms service sweep** → 5/7 coverage (admin + inventory + gateway + notification + **master**). **spec PR #879** (squash `c286bb3b`, mergedAt 2026-05-27 KST, markdown fast-lane). **impl PR #880** (squash `3ff5d28c`, mergedAt 2026-05-27 KST, **+49/-150 net -101 LOC** across 7 files; **CI 19 SUCCESS + 1 SKIPPED + 0 FAILURE** incl. wms IT/E2E + GAP IT + scm IT + finance IT + erp IT + console-bff IT + 4 E2E job authoritative GREEN — Spring Data JPA + paging/sort + 6 aggregate Hexagonal repository pattern 보존 검증). **Implementation**: 신규 `com.wms.master.adapter.out.persistence.PageableFactory` (`package-private final class` + `private constructor` + `static Pageable from(PageQuery)` + `private static Sort resolveSort(String, String)` + `private static Sort.Direction parseDirection(String)` — 6 RepositoryImpl 의 `toPageable`/`resolveSort`/`parseDirection` 3 private method byte-identical transcribe). **6 RepositoryImpl** (Warehouse/Zone/Location/Sku/Partner/Lot) 동일 mechanical update: (a) `DEFAULT_SORT_FIELD = "updatedAt"` 상수 제거, (b) `DEFAULT_SORT_DIRECTION = Sort.Direction.DESC` 상수 제거, (c) `toPageable + resolveSort + parseDirection` 3 private method 제거, (d) `findPage` body 의 `Pageable pageable = toPageable(pageQuery);` → `Pageable pageable = PageableFactory.from(pageQuery);` 1-line 치환, (e) 불필요한 `org.springframework.data.domain.PageRequest` + `org.springframework.data.domain.Sort` import 제거. `findPage` body 의 `PageResult<>(stream + 4 page fields)` 6-line ctor pattern 은 **libs/common 영역 (`com.example.common.page.PageResult`) 변경 필요** = 별 task BE-307 후보 명시 (zero-retrofit invariant 보호). **AC verify**: AC-1 PageableFactory shape (package-private final + private ctor + static method, byte-identical body) ✓ / AC-2 6 RepositoryImpl `DEFAULT_SORT_FIELD` grep = 0 + `toPageable\(` grep = 0 + `PageableFactory.from\(` grep = 6 (각 RepositoryImpl 1번) ✓ / AC-3 PageResult ctor byte-unchanged ✓ / AC-4 application/service layer 변경 0 (`git diff main -- .../application/service/` empty) ✓ / AC-5 worktree agent BUILD SUCCESSFUL 17 tasks ✓ / AC-6 CI 19/20 GREEN authoritative ✓ / AC-7 cross-service drift = 0 (only master-service main) ✓ — **41회째 zero-retrofit invariant** (`projects/{scm,global-account,erp,fan,ecommerce-microservices,finance,platform-console}-platform/` + `libs/` + master 외 6 wms service byte-unchanged) / AC-8 domain method + port interface + API contract + event payload + DB schema 변경 0 ✓ / AC-9 `PageRequest.of(page, size, sort)` + `Sort.by(direction, field)` argument 순서 byte-identical ✓. BE-297/299/300/301/302/303/304/305 회귀 0. **dispatcher 독립 재검증** (BE-303 3-dim): **spec PR #879**: (a) state=MERGED + mergeCommit=`c286bb3b`; (b) `git log origin/main` tip `c286bb3b` Fast-forward `96adbc6b..c286bb3b`; (c) markdown fast-lane — 3-dim ALL GREEN. **impl PR #880**: (a) state=MERGED + mergeCommit=`3ff5d28c` + 19 SUCCESS + 1 SKIPPED + 0 FAILURE; (b) `git log origin/main` tip `3ff5d28c` Fast-forward `c286bb3b..3ff5d28c`; (c) pre-merge failing=0 — 3-dim ALL GREEN. **close chore (this PR)** — `git mv review/ → done/` + Status `review → done` + INDEX move; BE-299 re-stage check verified; BE-303 3-dim 객관 머지 검증 완료 before close chore start. **메타**: ① **wms cluster 5번째 service sweep = 5/7 coverage (~71%)** — admin + inventory + gateway + notification + **master**. 잔존 = inbound + outbound 2 service. wms cluster TRUE 0 (7/7) 도달까지 2 service. 단일 cluster 의 5 service sequential sweep = depth-first sweep cohort 의 가장 큰 단일-cluster 실행. ② **cross-class utility extraction 4번째 instance + helper extraction trilogy → pentology** — BE-300 ProjectionConsumerSupport (admin, infra/messaging/) + BE-301 JwtHelper (inventory, adapter/in/web/) + BE-304 ReactiveJwtAccess (gateway, security/) + **BE-306 PageableFactory** (master, adapter/out/persistence/) = 4 cross-class utility + BE-305 markPermanentAndPersist (notification, same-class private method) = **5 helper extraction**. wms cluster 의 helper trilogy → quadrilogy → pentology evolution. ③ **largest single-task LOC reduction = -101 net** — BE-306 의 6 RepositoryImpl 모두 동일 mechanical update + 1 utility 도입 = +49/-150 = -101 LOC net. BE-303 (3 finding, +120/-70 = +50 LOC) + BE-301 (3 finding, +145/-111 = +34 LOC) + BE-304 (3 file, +39/-20 = +19) + BE-305 (1 file, +12/-8 = +4) + BE-300 (4 consumer 1 utility, +71/-69 = +2) 와 비교 시 **압도적 dedup magnitude** — cross-class instance 수 (6) × 동일 line 수 (~20) = 120 LOC × dedup ratio. 6 instance × 동일 패턴은 단일 PR closure 의 dedup sweet-spot. ④ **strategic-sampling dry-run 의 효율** — 214 main file 전체 scan 대신 representative 2 service (Warehouse + Zone) + 1 RepositoryImpl (WarehouseRepositoryImpl) sampling + 6 RepositoryImpl grep verify = ~15 file Read 로 base rate 측정. 큰 service surface 의 sweep 시 전체 scan 불요 — pattern identification 후 grep verify 가 효율적. Future large-service (master / inbound / outbound / 다른 cluster 의 erp/finance) sweep 시 동일 strategic-sampling 패턴 적용 가능. ⑤ **worktree subagent dispatch 의 single-utility 대규모 cross-class refactor 적합성** — Sonnet 4.6 agent 가 1 utility + 6 mechanical update + import 정리 + BUILD verify 모두 self-contained 단일 cycle 안에서 closure. agent local BUILD SUCCESSFUL 2회 (baseline + after) + cherry-pick 으로 main session 으로 merge. agent 의 1.27M tokens duration ~21 min 으로 6-file mechanical update + grep verify + test 모두 수행. Future mechanical multi-file refactor (≥5 file × 동일 패턴) 시 동일 worktree dispatch 패턴 적용 가능.

- `TASK-BE-305-notification-permanent-failure-dedup.md` — **DONE (F-L6-1 single-finding closure — wms cluster 4번째 service sweep, same-class private method extraction 4번째 패턴 카테고리)**. 분석/리뷰=Opus 4.7 / 구현=Sonnet 4.6 (직접; mechanical extract). notification-service 의 `/refactor-code` dry-run base rate = 55+ main file scope, L0/L1/L2/L3/L4 = 0 (BE-043 bootstrap 깨끗), L6=1 (본 task) + 6 intentional preservation 명시 (AlertConsumer co-location + DeliveryExecutor 4 test seam + 2-constructor pattern 등 documented intent, verify exemption). **spec PR #876** (squash `32148005`, mergedAt 2026-05-27 KST, markdown fast-lane). **impl PR #877** (squash `f63886b0`, mergedAt 2026-05-27 KST, +12/-8 single file = DeliveryDispatchPerRow.java only; **CI 19 SUCCESS + 1 SKIPPED + 0 FAILURE** incl. wms IT/E2E + GAP IT + scm IT + finance IT + erp IT + console-bff IT + 4 E2E job authoritative GREEN — Testcontainers Postgres + Kafka + reservation saga + outbox event payload + delivery state machine 보존 검증). **Implementation**: 신규 `DeliveryDispatchPerRow.markPermanentAndPersist(NotificationDelivery delivery, String reasonPrefix, Exception cause, String outboxStatus)` private method (4-line body: `delivery.markFailedPermanent(reasonPrefix + ": " + cause.getMessage(), clock.instant()); deliveries.update(delivery); outbox.writeDeliveryCompleted(delivery, outboxStatus); attemptsFailed.increment();` byte-identical 순서). 2 permanent-failure catch block 단일 호출 치환: `ChannelNotConfiguredException` → `markPermanentAndPersist(delivery, "CHANNEL_NOT_CONFIGURED", permanent, "FAILED_CHANNEL_NOT_CONFIGURED")` + `ChannelPermanentFailureException` → `markPermanentAndPersist(delivery, "VENDOR_4XX", permanent, "FAILED_PERMANENT")`. prefix + outboxStatus mapping byte-identical. success path (`markSucceeded + update + outbox("SUCCEEDED") + attemptsSucceeded`) + retryable `RuntimeException` catch (`handleRetryable` 호출) + `DeliveryRetryExhaustedException` catch (inside handleRetryable) + MDC.put/remove + Timer.record(deliveryDuration) + `@Transactional(propagation = REQUIRES_NEW)` 모두 byte-unchanged. **AC verify**: AC-1 markPermanentAndPersist 4-line body (L120-126) / AC-2 2 catch 단일 호출 치환 (L107/L109) + prefix/outboxStatus 매핑 byte-identical / AC-3 success/retryable/retry-exhausted path + REQUIRES_NEW + MDC + Timer 모두 unchanged / AC-4 domain method + port interface signature unchanged / AC-5 CI 19/20 GREEN authoritative incl. notification IT (Testcontainers Postgres + Kafka) + outbox event payload + delivery state machine / AC-6 cross-service drift = 0 (only notification-service main) — **40회째 zero-retrofit invariant** (`projects/{scm,global-account,erp,fan,ecommerce-microservices,finance,platform-console}-platform/` + `libs/` byte-unchanged) / AC-7 counter call 순서 + 횟수 unchanged. BE-297/299/300/301/302/303/304 회귀 0. **dispatcher 독립 재검증** (BE-303 3-dim): **spec PR #876**: (a) state=MERGED + mergeCommit=`32148005`; (b) `git log origin/main` tip `32148005` Fast-forward `366c5385..32148005`; (c) markdown fast-lane — 3-dim ALL GREEN. **impl PR #877**: (a) state=MERGED + mergeCommit=`f63886b0` + 19 SUCCESS + 1 SKIPPED + 0 FAILURE; (b) `git log origin/main` tip `f63886b0` Fast-forward `32148005..f63886b0`; (c) pre-merge failing=0 — 3-dim ALL GREEN. **close chore (this PR)** — `git mv review/ → done/` + Status `review → done` + INDEX move; BE-299 re-stage check verified; BE-303 3-dim 객관 머지 검증 완료 before close chore start. **메타**: ① **wms cluster 4번째 service sweep** — admin (BE-297/299/300, 3 cohort, 8/8 TRUE 0) + inventory (BE-301/302/303, 3 cycle, 8/8 effective sweep, 5번째 cluster TRUE 0) + gateway (BE-304, single finding, 1/1) + **notification (BE-305, single finding, 1/1)**. 4/7 wms service sweep coverage = ~57%. 잔존 = master + inbound + outbound 3 service. cluster-wide depth-first sweep 의 ⁴⁄₇ milestone. ② **same-class private method extraction = 4번째 패턴 카테고리** — BE-300 ProjectionConsumerSupport (admin, cross-class utility, `infra/messaging/`) + BE-301 JwtHelper (inventory, cross-class utility, `adapter/in/web/`) + BE-304 ReactiveJwtAccess (gateway, cross-class utility, `security/`) + **BE-305 markPermanentAndPersist** (notification, **in-class private method**) = 4번째 패턴. cross-class utility (public final + private ctor + static method) 와 다른 in-class private method 패턴 — 동일 bean 안 같은 책임 (catch block dedup) 이라 Spring AOP self-invocation 함정 무관. 같은 `@Transactional(REQUIRES_NEW)` annotation 안에서 호출되므로 outer TX scope 보존. ③ **Catch handler dedup sweet-spot** — 2-3 catch block 의 동일 4-5 line block 이 typical refactor candidate. 본 case 는 2 catch + clean parameter mapping (prefix + outboxStatus 만 다름). RuntimeException catch + DeliveryRetryExhaustedException catch 의 3-line `update + outbox + counter` 패턴은 부분 overlap 으로 scope 외 명시 — over-extension 회피. Future catch block dedup 시 동일 sweet-spot heuristic (2-3 catch + 동일 4-5 line + clean parameter mapping). ④ **6 intentional preservation 명시 + verify exemption** — BE-301 9th-instance (BE-302 의 10번째 instance) 의 발전형 = 본 task 에서 6 intentional preservation 모두 main session 검증 (AlertConsumer co-location + DeliveryExecutor 4 test seam + 2-constructor pattern + DeliveryDispatchPerRow @Transactional 위치 + libs/java-messaging 비-extend 의도 + JSONB-payload 정당화). 미래 sweep 시 "intentional preservation list" = task body 명시 의무 → over-zealous refactor 사전 차단. ⑤ **service base rate hierarchy 정립** — gateway 0.07/file (BE-304) + notification 0.018/file (BE-305, 1 finding / 55+ file) = service surface 가 클수록 base rate ↓ (BE-043 bootstrap 의 충실한 implementation 효과). 반면 inventory + admin 의 base rate 0.16-0.27/file 가 더 높은 이유 = 더 많은 cohort + 더 많은 file = aggregate finding ↑. **small-surface OR mature-implementation service 의 base rate < 0.1/file = single PR closure 충분 신호**.

- `TASK-BE-304-gateway-jwt-access-dedup.md` — **DONE (F-L6-1 single-finding closure — wms cluster 3번째 utility class extraction)**. 분석/리뷰=Opus 4.7 / 구현=Sonnet 4.6 (직접; mechanical extract). gateway-service 의 `/refactor-code` dry-run base rate = 14 main file / 792 LOC scope, L0/L1/L2/L3/L4 = 0, L6=1 (본 task). **spec PR #873** (squash `ab4ea813`, mergedAt 2026-05-27 KST, markdown fast-lane). **impl PR #874** (squash `ad695ddf`, mergedAt 2026-05-27 KST, +39/-20 across 5 files; **CI 19 SUCCESS + 1 SKIPPED + 0 FAILURE** incl. wms IT/E2E + GAP IT + scm IT + finance IT + erp IT + console-bff IT + 4 E2E job authoritative GREEN — Spring Cloud Gateway reactive WebFlux + JWT validation 보존 검증). **Implementation**: 신규 `com.wms.gateway.security.ReactiveJwtAccess` (`public final class` + `private constructor` + `public static Mono<JwtAuthenticationToken> currentToken()` 4-line cast chain + `public static Mono<Jwt> currentJwt()` = `currentToken().map(JwtAuthenticationToken::getToken)`; utility 자체는 raw empty Mono 반환, fallback 책임은 caller 의 `.defaultIfEmpty(...)`). 2 GlobalFilter caller 의 5-line cast chain 단일 호출로 치환: (a) `JwtHeaderEnrichmentFilter.filter` L29-33 → `ReactiveJwtAccess.currentJwt().map(jwt -> enrich(exchange, jwt))` (b) `AccountTypeValidationFilter.filter` L32-36 → `ReactiveJwtAccess.currentJwt().map(jwt -> "OPERATOR".equals(jwt.getClaimAsString("account_type")))` (간소화: 기존 `auth.getToken().getClaimAsString(...)` 2-step 에서 `currentJwt()` 직접 사용으로 1-step). 2 caller 의 import 정리: `ReactiveSecurityContextHolder` + `JwtAuthenticationToken` 제거, `ReactiveJwtAccess` 추가. 2 caller 의 `.defaultIfEmpty(...)` fallback (exchange / Boolean.TRUE) + 최종 `.flatMap(...)` chain + filter ordering (-1 / -2) 모두 byte-unchanged. **AC verify**: AC-1 utility class shape (public final + private ctor + currentToken + currentJwt) / AC-2 JwtHeaderEnrichmentFilter caller updated / AC-3 AccountTypeValidationFilter caller updated (simplified 1-step) / AC-4 filter ordering byte-unchanged / AC-5 header injection + claim validation logic byte-identical / AC-7 CI 19/20 GREEN authoritative incl. wms IT + GAP IT + 4 E2E jobs / AC-8 cross-service drift = 0 (only gateway-service main) — **39회째 zero-retrofit invariant** (`projects/{scm,global-account,erp,fan,ecommerce-microservices,finance,platform-console}-platform/` + `libs/` byte-unchanged). AC-6 (local build) = CI authoritative substitute (gradlew main 디렉토리 absent — worktree 또는 가상 env 사용 시 가능). BE-297/299/300/301/302/303 회귀 0. **dispatcher 독립 재검증** (BE-303 3-dim): **spec PR #873**: (a) state=MERGED + mergeCommit=`ab4ea813`; (b) `git log origin/main` tip `ab4ea813` Fast-forward `b7ebbf77..ab4ea813`; (c) markdown fast-lane — 3-dim ALL GREEN. **impl PR #874**: (a) state=MERGED + mergeCommit=`ad695ddf` + 19 SUCCESS + 1 SKIPPED + 0 FAILURE; (b) `git log origin/main` tip `ad695ddf` Fast-forward `ab4ea813..ad695ddf`; (c) pre-merge failing=0 — 3-dim ALL GREEN. **close chore (this PR)** — `git mv review/ → done/` + Status `review → done` + INDEX move; BE-299 re-stage check verified; BE-303 3-dim 객관 머지 검증 완료 before close chore start. **메타**: ① **wms cluster 3번째 utility class extraction — universal helper boundary pattern 확정** — admin-service `ProjectionConsumerSupport` (BE-300, `infra/messaging/`) + inventory-service `JwtHelper` (BE-301, `adapter/in/web/`) + gateway-service `ReactiveJwtAccess` (BE-304, `security/`) = wms cluster 3 service × 1 utility = consistent layer-correct placement 패턴 정립. **Layered + Hexagonal architecture 둘 다 universal** (admin=Layered / inventory=Hexagonal / gateway=Layered + reactive). Future utility extract 시 동일 패턴 적용 가능 (utility 가 layer-correct location 에 위치, public final + private ctor + static method). ② **Reactive (WebFlux) utility 의 fallback 책임 boundary** — utility 가 raw empty `Mono` 반환 + caller 의 `.defaultIfEmpty(...)` 가 fallback 책임. utility 내부에 default 도입 시 caller 의 의도 (exchange / Boolean.TRUE 등 context-specific fallback) 무시. 본 task 의 ReactiveJwtAccess 가 이 분리 boundary 명시화 — utility 는 "access" 의 책임만, fallback 은 caller. Future reactive utility 추가 시 동일 boundary 적용 가능. ③ **gateway-service base rate 측정 = small-surface service 의 정상 metric** — dry-run 14 main file / 792 LOC scope, L0/L1/L2/L3/L4=0 + L6=1 = base rate 0.07/file. inventory (8 finding / 50+ file) + admin (8 finding / 30+ file) 의 cluster 평균 0.16-0.27/file 의 약 ¼. **Layered + stateless edge 의 자연 결과** — domain 무, aggregate 무, 단순 filter chain 만. small-surface service 의 sweep 효율 = base rate verification + single closure. ④ **simplification opportunity in AccountTypeValidationFilter** — 기존 코드는 `auth -> { String accountType = auth.getToken().getClaimAsString(...); return Mono.just(...); }` 의 2-step lambda + return Mono.just. 새 코드는 `jwt -> "OPERATOR".equals(jwt.getClaimAsString(...))` 의 1-step map. utility extract 가 사용자 인지 부담 줄여 자연스럽게 code simplification 유발. Future utility extract 시 caller 의 follow-on simplification 검토 의무. ⑤ **CI authoritative verify with reactive WebFlux + Spring Cloud Gateway depth** — wms IT + GAP IT + scm IT + finance IT + erp IT + console-bff IT + 4 E2E job = production-equivalent baseline 으로 reactive Mono chain (`.map`, `.defaultIfEmpty`, `.flatMap`) + `ReactiveSecurityContextHolder` integration + JWT decode + claim extract 모두 verify. WebFlux 의 lazy + non-blocking semantics 가 cast chain extraction 후에도 byte-identical 동작 보장. Future reactive code refactor 시 CI 가 authoritative final gate.

- `TASK-BE-303-inventory-cohort-b-long-method-polish.md` — **DONE (Cohort B applied — 3 L5 long-method finding closure)**. 분석/리뷰=Opus 4.7 / 구현=Sonnet 4.6 (worktree refactoring-engineer agent). **BE-302 spec amend + BE-303 = inventory-service 8/8 effective sweep coverage → 5번째 cluster TRUE 0 도달** (GAP + PC console-bff + SCM + wms admin + **wms inventory**). **spec PR #870** (squash `19e9930a`, mergedAt 2026-05-27 KST, markdown fast-lane). **impl PR #871** (squash `209b435e`, mergedAt 2026-05-27 KST, +120/-70 across 3 files; **CI 19 SUCCESS + 1 SKIPPED + 0 FAILURE** incl. wms IT/E2E + GAP IT + scm IT + finance IT + erp IT + console-bff IT + 4 E2E job authoritative GREEN — Extract Method 패턴 + W1 transfer atomicity + SQL byte-identical 검증). **Implementation**: (1) **F-L5-1** `AdjustStockService.persistAdjustmentResult` 12 → 6 arg shrink — 6 redundant param (`delta`, `bucket`, `reasonCode`, `reasonNote`, `actorId`, `createdAt`) 모두 `StockAdjustment` entity 에서 derive 으로 method body 안에서 lookup; signature = `(StockAdjustment adjustment, InventoryMovement movement, Inventory inventory, MovementType movementType, boolean reducedAvailable, Counter counter)`. caller `doAdjust` L122-125 도 6-arg 호출로 갱신. (2) **F-L5-2** `TransferStockService.transfer()` 82 → 59 LOC — 3 private method extraction (`loadOrCreateTarget` 14 LOC + `applyLegs` 9 LOC + `buildTransferredEvent` 15 LOC) + 2 private static nested record (`TargetResolution(Inventory target, boolean wasCreated)` + `LegPair(Inventory first, Inventory second, InventoryMovement firstLeg, InventoryMovement secondLeg)`). id-ascending lock order (`source.id().compareTo(target.id()) <= 0`) + side-effect order (persistInventory first → second → 2 movements → transfer → outbox → counter → low-stock → log) **byte-identical** 보존. (3) **F-L5-3** `InventoryRepositoryImpl.listViews` WHERE/bind dual-walk 8-conditional → `Filter[6]` 정적 배열 + 단일 loop — `private record Filter(String whereFragment, String paramName, Function<InventoryListCriteria, Object> valueSupplier, Predicate<InventoryListCriteria> isEnabled)` + `private static final Filter[] FILTERS` (6 entry: warehouseId → locationId → skuId → lotId → hasStock → minAvailable). `where` builder + `bindFilters` 모두 단일 loop 로 치환. SQL output (WHERE 부절 순서 + parameter binding 순서) + `hasStock` paramName=null 처리 모두 byte-identical 보존. **AC verify**: AC-1 6 params (signature L176-181) / AC-2 transfer() = 59 LOC (target ≤ 60, lines 79-137) / AC-3 Filter[] + 2 loops (L31/35/151/178) / AC-4 worktree agent BUILD SUCCESSFUL (baseline 2m9s + after 56s) / AC-5 CI 19/20 GREEN authoritative (Testcontainers 5 IT/E2E job + Hexagonal layer verify) / AC-6 cross-project drift = 0 (only inventory-service main) / AC-7 domain layer untouched (`git diff main -- .../domain/` empty diff). **38회째 zero-retrofit invariant** (`projects/{scm,global-account,erp,fan,ecommerce-microservices,finance,platform-console}-platform/` + `libs/` byte-unchanged). BE-297/299/300/301/302 회귀 0. **dispatcher 독립 재검증** (BE-303 3-dim): **spec PR #870**: (a) state=MERGED + mergeCommit=`19e9930a`; (b) `git log origin/main` tip `19e9930a` Fast-forward `f1c82043..19e9930a`; (c) markdown fast-lane — 3-dim ALL GREEN. **impl PR #871**: (a) state=MERGED + mergeCommit=`209b435e` + 19 SUCCESS + 1 SKIPPED + 0 FAILURE; (b) `git log origin/main` tip `209b435e` Fast-forward `19e9930a..209b435e`; (c) pre-merge failing=0 — 3-dim ALL GREEN. **close chore (this PR)** — `git mv review/ → done/` + Status `review → done` + INDEX move; BE-299 re-stage check verified; BE-303 3-dim 객관 머지 검증 완료 before close chore start. **메타**: ① **wms inventory-service 8/8 effective sweep coverage = 5번째 cluster TRUE 0** — BE-301 Cohort A 3 closure (duplication dedup) + BE-302 spec amend resolution-via-spec 2 finding (F-L0-1/F-L1-1 systemic recognition) + BE-303 Cohort B 3 closure (L5 long-method polish) = **3+2+3 = 8/8 effective sweep coverage**. **5번째 cluster TRUE 0**: GAP / PC console-bff / SCM / wms admin / **wms inventory**. 단일 service 의 multi-cycle (Cohort A → spec amend → Cohort B) 완성 패턴 정립. ② **worktree-isolated subagent dispatch 의 효율** — Sonnet 4.6 agent (worktree-agent-a309d137940fccb4f) 가 3 finding 모두 단일 dispatch 안에서 closure. agent local BUILD SUCCESSFUL 2회 (baseline 2m9s + after 56s) + cherry-pick 으로 main session 으로 merge. agent 가 isolated env 으로 baseline → impl → verify 단 cycle 의 self-contained. ③ **Extract Method (Low risk) refactoring policy 100% 준수** — 3 finding 모두 Extract Method 카테고리 + behavior 보존 (byte-identical 검증 in 모든 invariant: W1 id-ascending / SQL output / outbox payload / counter order / log emission). domain layer untouched + API/event contract unchanged + DB schema unchanged. ④ **CI 19/20 GREEN authoritative verify** — 5+ IT/E2E job (Testcontainers 4 IT + 4 E2E job + GAP/scm/finance/erp/console-bff IT) 가 production-equivalent baseline 으로 검증. W1 transfer atomicity (id-ascending lock + 2-row transaction) + reservation saga (TransferStockService 가 saga state-transition 경로 아님 확인) + SQL semantics + outbox payload 모두 cross-project E2E 까지 cover. ⑤ **Method extraction 의 sweet-spot: shrink 가능한 3 phase 추출** — 82 LOC body 의 9 phase 중 3 phase (load + apply + event-build) 만 추출, 나머지 6 phase 는 inline 유지 (validate + persist + side-effect 등 small 한 phase 는 method 만들면 over-abstraction). 결과 59 LOC (target ≤60 정확히 충족). Future long-method 작업 시 동일 sweet-spot 적용 가능.

- `TASK-BE-302-inventory-architecture-spec-systemic-patterns.md` — **DONE (Tier 2 spec-correction; 0 production code change)**. inventory-service architecture.md L165 (Layer Rules #3) + L433 (Security #2) 두 위치 amend, 7-consumer + 5-controller × 17-endpoint systemic + BE-027/028 APPROVED layered convention 명시화. **spec PR #867** (squash `abd902ce`, mergedAt 2026-05-27 KST, markdown fast-lane). **impl PR #868** (squash `01c7ab2e`, mergedAt 2026-05-27 KST, +16/-3 single file = architecture.md only; markdown fast-lane). **Amendment scope**: (1) L165 — "Adapters depend inward" 문구에 inbound adapter 가 TX-join 요구 시 outbound port (EventDedupePort, Propagation.MANDATORY 등) 호출이 합법 패턴임을 명시 + BE-027 cross-ref; (2) L433 — Security bullet 2 를 layered auth pattern 명시 = (a) controller-level `@PreAuthorize` = role baseline (contract role table mirror, `AdjustmentController.java:47` + `ReservationController.java:42` inline-doc), (b) application-layer programmatic check = invariant-tied authorization (예: bucket=RESERVED → INVENTORY_ADMIN), `jwt.getClaim("role")` 직접 parsing 금지 + BE-028 cross-ref. **AC verify**: AC-1/2 amend 적용 / AC-3 production code byte-unchanged (`git diff main -- apps/inventory-service/src/` empty) / AC-4 cross-ref drift 없음 (master-service/architecture.md L122 + L258 동일 문구는 out-of-scope by design, BE-303 후보) / AC-5 test byte-unchanged / **37회째 zero-retrofit invariant** (실제 0 production code 변경). **dispatcher 독립 재검증** (BE-303 3-dim): **spec PR #867**: (a) state=MERGED + mergeCommit=`abd902ce`; (b) `git log origin/main` tip `abd902ce` Fast-forward `1a9a5211..abd902ce`; (c) markdown fast-lane — 3-dim ALL GREEN. **impl PR #868**: (a) state=MERGED + mergeCommit=`01c7ab2e`; (b) `git log origin/main` tip `01c7ab2e` Fast-forward `abd902ce..01c7ab2e`; (c) pre-merge failing=0 — 3-dim ALL GREEN. **close chore (this PR)** — `git mv review/ → done/` + Status `review → done` + INDEX move; BE-299 re-stage check verified (`git show :done-path` reads `Status: done`); BE-303 3-dim 객관 머지 검증 완료 before close chore start. **메타**: ① **BE-301 9th-instance "agent finding category trust ≠ verify exemption" 의 10번째 instance** — `/refactor-code wms inventory C2` 호출 시 main session verify 가 F-L0-1 + F-L1-1 두 finding 을 **isolated → systemic + reviewed-and-approved** 재분류. 단일 site refactor 시도가 uniformity 깨고 BE-027/028 APPROVED 결정 뒤집는 격임을 catch. dry-run finding catalog 가 **시간에 따라 outdated 됨** (BE-027/028 의 APPROVED 가 finding category 의 인지 시점 이전) 도 동시 학습. ② **refactor-code → refactor-spec redirect 패턴 정립** — dry-run finding 이 spec ↔ impl conflict 일 때, **impl 변경이 아닌 spec 명시화** 가 본질적 작업. 본 task = 그 패턴의 첫 explicit 적용. 미래 동일 conflict 발견 시 동일 redirect 가능 (refactor-code → refactor-spec). ③ **Tier 2 spec-correction = 0 production code change 의 가능성** — refactor-spec policy 가 강조하는 "no requirement change / no contract change / no scope change" 가 layered convention 명시화에서도 완벽히 성립. spec amend 가 established convention 을 retrofit-document 하는 것이지 새 rule 도입이 아님. ④ **Cross-service spec drift = BE-303 후보** — master-service/architecture.md L122 + L258 의 동일 문구가 동일 conflict 가능 (5/5 controller × N endpoint × ? consumer). Out-of-scope by design 으로 본 task 에서 분리, BE-303 cross-service spec sweep 후보로 명시. inventory-service 한정 정착 후 sibling service 별도 task. ⑤ **markdown fast-lane 의 spec-only PR 효율** — spec PR + impl PR + close-chore PR 3-PR sequence 가 모두 markdown fast-lane (∵ 변경된 file 이 .md only) = 각 PR 의 CI 시간 < 10 sec (path-filter changes job 만 실행). 7-minute spec-only 3-PR sequence (BE-301 / BE-302 의 spec-only 부분과 동일 pattern). 분석=Opus 4.7 / 구현=Sonnet 4.6 (직접; 2-line mechanical spec amend) / 리뷰=Opus 4.7.

- `TASK-BE-301-inventory-duplication-sweep.md` — **DONE (Cohort A applied — L6 duplication 3 finding)**. 분석/리뷰=Opus 4.7 / 구현=Opus 4.7 (직접; 2 helper class 신설 + 2 service + 3 controller dedup + 2 test fixture). wms admin 8/8 TRUE 0 직속 후속, **wms cluster 의 2번째 service sweep 시작** (inventory-service). **spec PR #864** (squash `7bd15cbf`, mergedAt 2026-05-27 KST, markdown fast-lane). **impl PR #865** (squash `96b88af0`, mergedAt 2026-05-27 KST, +145/-111 across 10 files; **CI 19 SUCCESS + 1 SKIPPED + 0 FAILURE** incl. wms IT + GAP IT + scm IT + finance IT + erp IT + console-bff IT + 4 E2E job authoritative GREEN — Hexagonal layer direction + Testcontainers IT 검증). **Implementation**: 신규 (a) `application/service/MasterRefValidator.java` (`@Component`, constructor inject `MasterReadModelPort`, 단일 `validate(UUID locationId, UUID skuId, UUID lotId)` 메서드 — Location active + SKU active + Lot active/expired 24-line byte-identical block 정확한 transcribe; `MasterRefInactiveException` factory 호출 invariant 보존; lotId null skip 보존), (b) `adapter/in/web/JwtHelper.java` (`public final class` + `private JwtHelper()` + `public static String actorId(Jwt)` — 기존 4-line null guard + subject/claim fallback 정확한 transcribe). 2 application service dedup: AdjustStockService L232-256 `validateMasterRefs` 제거 + `masterReadModel` field → `masterRefValidator` field 교체 (validateMasterRefs 만 사용처) + 5 unused imports cleanup (MasterReadModelPort + LocationSnapshot + LotSnapshot + SkuSnapshot + MasterRefInactiveException + Optional 모두 제거); ReceiveStockService L140-164 `validateMasterRefs` 제거 + `masterRefValidator` field 추가 (`masterReadModel` field 보존, L107 findLocation locationCode lookup 사용처 있음) + 2 unused imports cleanup (LotSnapshot + SkuSnapshot 제거; LocationSnapshot 보존). 3 controller dedup: AdjustmentController 3 endpoint guard (L72-77/L101-106/L126-131 각 6-line) → `requireIdempotencyKey(String)` + `requireReasonNote(String)` private static methods (3 endpoint body 각 2-line `requireIdempotencyKey(...); requireReasonNote(...);` 호출); AdjustmentController + ReservationController + TransferController 의 private static `actorId(Jwt jwt)` method 모두 제거 + 호출 site 모두 `JwtHelper.actorId(jwt)` 로 교체. **Test fixture update** (BE-297 mechanical pattern 2 site): AdjustStockServiceTest L82-85 + ReceiveStockServiceTest L58-61 의 service constructor call 에 `new MasterRefValidator(masterReadModel)` 또는 `new MasterRefValidator(masterRefs)` 추가. **TransferStockService.validateSku byte-unchanged** (requiresLot 추가 로직 = scope 외 명시; agent dry-run over-zealous 분류 main session verify 후 정정). **AC verify**: grep `validateMasterRefs` in `apps/inventory-service/src/` = 0 (2 service 모두 제거 완료, MasterRefValidator 가 새 owner) / grep direct `idempotencyKey == null` in `AdjustmentController` = 0 (helper 로 추출됨) / grep `private static String actorId\(Jwt` in 3 controller = 0 (모두 제거) / Local `:check --rerun-tasks` BUILD SUCCESSFUL 1m 10s (16/16 actionable) / CI 19/20 GREEN authoritative incl. wms IT (Testcontainers + Hexagonal layer verify) + 4 E2E jobs / MasterRefInactiveException throw site invariant 보존 (4 throw type 모두 유지) / Idempotency-Key + reasonNote 메시지 byte-identical / actorId fallback byte-identical / HTTP API + outbox event payload byte-identical / **36회째 zero-retrofit invariant** / BE-297 + BE-299 + BE-300 회귀 0 (admin-service 패턴 보존). **dispatcher 독립 재검증** (BE-303 3-dim): **spec PR #864**: (a) state=MERGED + mergeCommit=`7bd15cbf`; (b) `git log origin/main` tip `7bd15cbf` Fast-forward `b8c6bb35..7bd15cbf`; (c) markdown fast-lane — 3-dim ALL GREEN. **impl PR #865**: (a) state=MERGED + mergeCommit=`96b88af0` + 19 SUCCESS + 1 SKIPPED + 0 FAILURE; (b) `git log origin/main` tip `96b88af0` Fast-forward `7bd15cbf..96b88af0`; (c) pre-merge failing=0 — 3-dim ALL GREEN. **close chore (this PR)** — `git mv review/ → done/` + Status `review → done` + INDEX move; BE-299 re-stage check verified (`git show :done-path` reads `Status: done`); BE-303 3-dim 객관 머지 검증 완료 before close chore start. **메타**: ① **wms cluster 2번째 service sweep 시작** — admin-service 8/8 TRUE 0 (BE-297/299/300) 직속 후속, inventory-service 가 wms 의 두 번째 sweep target. dry-run 8 finding 중 3 finding (Cohort A) closure. C2 (L0+L1 architecture compliance saga review) + C3 (L5 long-method) 잔존 후보. depth-first cluster sweep 패턴 연장 (BE-300 ㉜ 메타 lesson 답습). ② **Helper class boundary: Hexagonal 에서도 자연스러움** — wms admin-service (Layered) 의 `AssignmentEventHelper` + `ProjectionStatusService` 패턴이 wms inventory-service (Hexagonal) 에서 그대로 적용. `MasterRefValidator` = application/service/ (application layer), `JwtHelper` = adapter/in/web/ (inbound adapter layer) — 두 helper 모두 layer-correct. Hexagonal 의 strict direction 이 helper extraction 의 placement 를 더 명확히 강제 (Layered 의 약간 자유로움과 대조). ③ **TransferStockService.validateSku 보존 = agent dry-run over-zealous 정정** — agent 가 F-L6-1 의 3번째 instance 로 TransferStockService.validateSku 를 분류했으나 main session verify 결과 requiresLot 추가 로직 포함 (다른 책임). scope 정정 후 2 service 만 dedup. **agent finding category 신뢰 ≠ verify 면제** 의 BE-301 패턴 9회째 확정 (BE-316 메타 ⑨ 답습). ④ **Multi-instance test fixture mechanical pattern 안정화** — BE-297 (3 test fixture constructor 업데이트) + BE-301 (2 test fixture 업데이트) 가 동일 패턴: 새 helper class 의 mock 또는 real instance constructor 추가. service constructor signature 의 superset 변경 시 test fixture 업데이트 = mechanical chain. ⑤ **Hexagonal direction 보존 검증** — Hexagonal architecture override 없는 service 의 sweep 에서 layer direction 위반 가능성 검증. F-L0-1 (ShippingConfirmedConsumer 의 ReservationRepository 직접 inject) 는 본 task scope 외 (saga review 필요, 별 task BE-302 후보). MasterRefValidator + JwtHelper 도입은 layer-correct (application → port-out, adapter.in → adapter-local utility) 으로 Hexagonal direction 위반 없음. 분석=Opus 4.7 / 구현=Opus 4.7 (직접) / 리뷰=Opus 4.7.

- `TASK-BE-300-admin-projection-consumer-dispatch-dedup.md` — **DONE (Cohort C2 applied — F-L6-2 4-Kafka-consumer dispatch dedup; wms admin-service 8/8 sweep TRUE 0 도달)**. 분석/리뷰=Opus 4.7 / 구현=Opus 4.7 (직접; static utility + functional interface 패턴, AbstractProjectionService precedent 회피, Spring AOP self-invocation 함정 회피). BE-297 (Cohort C1) + BE-299 (Cohort C3) 직속 후속, **wms admin-service 마지막 finding closure → 8/8 sweep TRUE 0 도달** (3-cluster TRUE 0 + wms admin = **4번째 cluster TRUE 0**). **spec PR #861** (squash `9043223b`, mergedAt 2026-05-27 KST, markdown fast-lane). **impl PR #862** (squash `32886dbf`, mergedAt 2026-05-27 KST, +71/-69 across 6 files; **CI 19 SUCCESS + 1 SKIPPED + 0 FAILURE** incl. `Integration (master-service + notification-service, Testcontainers)` + `Integration (iam-platform, Testcontainers)` + `E2E (gateway-master live-pair smoke, Testcontainers)` + `E2E (scm-platform v1 cross-service smoke, Testcontainers)` + `E2E (fan-platform v1 live-trio smoke, Testcontainers)` 5 IT/E2E jobs authoritative GREEN — Spring AOP self-invocation 함정 회피 + `@Transactional` proxy 정상 적용 보존 검증). **Implementation**: 신규 `infra/messaging/ProjectionConsumerSupport.java` (`public final class`, private constructor, 단일 static `public static void dispatch(ConsumerRecord<String, String>, ProjectionEnvelopeParser, ProjectionMetrics, java.util.function.Consumer<ProjectionEnvelope>)` 메서드 — 4 consumer 의 기존 `onMessage` 16-line body 의 정확한 transcribe). 4 ProjectionConsumer (`InventoryProjectionConsumer` + `InboundProjectionConsumer` + `MasterProjectionConsumer` + `OutboundProjectionConsumer`) 의 `onMessage` body 16-line × 4 = 64 LOC dup → 각 1-line `ProjectionConsumerSupport.dispatch(record, parser, metrics, projectionService::project);`. 4 consumer 의 `org.slf4j.MDC` + `com.wms.admin.application.projection.ProjectionEnvelope` import 제거 (utility 가 쓰임). `@KafkaListener` annotation (topics + groupId) + `@Component` + `@Profile("!standalone")` + class javadoc 모두 byte-unchanged. **Spring AOP self-invocation 함정 회피 패턴**: (1) `ProjectionConsumerSupport` = Spring bean 아님 (private constructor + final class — 인스턴스 생성 불가, Spring DI 우회); (2) `projectionService::project` method reference = 4 consumer 의 Spring 주입 인스턴스 의 method reference → utility 가 invoke 시 정상 Spring proxy 통해 호출 = `@Transactional` proxy 정상 적용 보존; (3) SKILL.md AbstractProjectionService precedent 의 위험 (`this.project(...)` self-invocation 시 Spring proxy 우회되어 `@Transactional` 무시) 완전 회피. **AC verify**: grep `ProjectionConsumerSupport.dispatch` = 4 caller sites (4 consumer) + 1 definition / grep `MDC` in `infra/messaging/` 4 consumer = 0 (utility 안에서만 사용) / grep `ProjectionEnvelope` in 4 consumer = 0 (utility 가 쓰임) / `@KafkaListener` annotation 4 consumer 모두 byte-unchanged (topics + groupId 보존) / Local `./gradlew :projects:wms-platform:apps:admin-service:check --rerun-tasks` BUILD SUCCESSFUL 1m 23s (7/7 actionable) / CI 19/20 GREEN authoritative incl. 5 IT/E2E jobs (Kafka real Testcontainers verify) / `admin.action.*` outbox event payload byte-identical / MDC eventId/sourceTopic put/remove invariant 보존 / RuntimeException propagate (metrics.recordError + throw) 보존 / 18 topic dispatch (7 inventory + 3 inbound + 6 master + 2 outbound) 모두 동일 / **35회째 zero-retrofit invariant** (`projects/{scm,global-account,erp,fan,ecommerce-microservices,finance,platform-console}-platform/` + `libs/` byte-unchanged) / BE-297 + BE-299 회귀 0. **dispatcher 독립 재검증** (BE-303 3-dim): **spec PR #861**: (a) state=MERGED + mergeCommit=`9043223b`; (b) `git log origin/main` tip `9043223b` Fast-forward `a5d3a5d5..9043223b`; (c) markdown fast-lane — 3-dim ALL GREEN. **impl PR #862**: (a) state=MERGED + mergeCommit=`32886dbf` + 19 SUCCESS + 1 SKIPPED + 0 FAILURE; (b) `git log origin/main` tip `32886dbf` Fast-forward `9043223b..32886dbf`; (c) pre-merge failing=0 — 3-dim ALL GREEN. **close chore (this PR)** — `git mv review/ → done/` + Status `review → done` + INDEX move; BE-299 re-stage check verified (`git show :done-path` reads `Status: done`); BE-303 3-dim 객관 머지 검증 완료 before close chore start. **메타**: ① **wms admin-service 8/8 sweep TRUE 0 도달 (4번째 cluster)** — dry-run 8 finding 모두 closure (C1=3 + C3=4 + C2=1 = 8). 3-cluster TRUE 0 (GAP / PC console-bff / SCM) 에 이은 **4번째 cluster TRUE 0**. 2026-05-26 BE-017 sweep tail 이후 ~36 hour 동안 6 task closure (SCM-BE-017 + BE-316 + SCM-BE-018 + wms BE-297 + BE-299 + **BE-300**). 동일 cluster (wms admin-service) 의 3 cohort sequential closure (C1 → C3 → C2) = 단일 cluster sweep 의 "depth-first" 패턴 정립. ② **Spring AOP self-invocation 함정 회피 = static utility + functional interface 패턴** — SKILL.md AbstractProjectionService precedent 의 위험 (abstract superclass 가 `this.project(...)` self-invocation 시 Spring proxy 우회 → `@Transactional` 무시) 회피의 정답 패턴: (a) utility class = Spring bean 아님 (final + private constructor) (b) projectionService::project method reference = caller 가 Spring 주입 받은 proxied instance 의 method reference → utility invoke 시 proxy 통해 호출 = `@Transactional` 정상 적용. Future Kafka consumer / event handler dedup 시 동일 패턴 적용 가능 (abstract superclass 회피, static utility + method reference 채택). ③ **Functional interface = `java.util.function.Consumer`** — java stdlib 의 `Consumer<T>` 사용 = 별도 custom `ProjectionEnvelopeHandler` interface 도입 불요 (over-engineering 회피). `Consumer<ProjectionEnvelope>` 가 의미를 정확히 전달 + Java stdlib 의 표준 functional interface 선호. ④ **5 IT/E2E job authoritative verify** — CI 가 Kafka real Testcontainers (master+notification IT) + GAP IT + 3 E2E job (gateway-master live-pair / fan-platform live-trio / scm-platform cross-service smoke) = 5 jobs 가 본 변경의 권위적 verify. wms admin-service 변경이 cross-project E2E 까지 cover = path-filter 가 wms 변경의 fan-out 정확 인식. ⑤ **Sweep depth-first vs breadth-first 패턴** — 2026-05-26~27 의 5 cycle = 3 cluster (SCM / GAP-spec / wms) × 2 cohort 평균. 동일 cluster 의 3 cohort sequential closure (BE-297→BE-299→BE-300) = depth-first; 직전 SCM-BE-017→BE-316→BE-018 = cluster 다양 = breadth-first. dry-run base rate 가 meaningful backlog (8 finding+) 일 때 depth-first 가 효율 (cluster TRUE 0 도달 명확 목표); base rate 적을 때 breadth-first 가 효율 (cluster 다양 maturity 측정). 분석=Opus 4.7 / 구현=Opus 4.7 (직접) / 리뷰=Opus 4.7.

- `TASK-BE-299-admin-mixed-hygiene.md` — **DONE (Cohort C3 applied — L1+L2+L3+L5 mixed hygiene, 4 finding closure)**. 분석/리뷰=Opus 4.7 / 구현=Opus 4.7 (직접; mechanical 4-finding mixed hygiene + 1 new application service + 1 test fixture update). BE-297 (Cohort C1) 직속 후속, **wms admin-service 2번째 cohort closure** — dry-run 8 finding 중 7 closure (잔여 = F-L6-2 4-Kafka-consumer dispatch dedup, Cohort C2 medium risk). **spec PR #858** (squash `7516ee26`, mergedAt 2026-05-27 KST, markdown fast-lane). **impl PR #859** (squash `1142be12`, mergedAt 2026-05-27 KST, +156/-110 across 6 files; **CI 19 SUCCESS + 1 SKIPPED + 0 FAILURE** incl. `Integration (master-service + notification-service, Testcontainers)` + `Integration (iam-platform, Testcontainers)` 양쪽 authoritative GREEN). **Implementation (4 finding)**: (F-L1-1) `OperationsController` thin: `application/projection/ProjectionStatusService` 신설 (controller 의 4 field `AdminEventDedupeRepository dedupeRepository` + `KafkaListenerEndpointRegistry registry` + `KafkaLagProbe lagProbe` + `String consumerGroup` + computation logic 모두 service 로 이동); controller body = 1-line delegation (`return projectionStatusService.computeStatus()`). controller 의 `application.repository.AdminEventDedupeRepository` + `infra.observability.KafkaLagProbe` import = 0 (architecture.md § Layer Rules rule 1 "controllers thin, call application services" 부합). (F-L2-1) `RoleService.deserialisePermissions` 의 silent catch (`catch (Exception e) { return List.of(); }`) → `log.warn("Failed to deserialise permissions json — returning empty list: json={}", permissionsJson, e); return List.of();` — 빈 list 반환 behavior-neutral 보존 + `org.slf4j.Logger` field + SLF4J imports 추가. (F-L3-1) `InventoryProjectionService.applySnapshot:327` `boolean lowStock = availableQty <= 10` → `<= DEFAULT_LOW_STOCK_THRESHOLD` + class top 에 `private static final int DEFAULT_LOW_STOCK_THRESHOLD = 10;` (javadoc: "v1 default low-stock alert threshold; recomputed at projection time"). (F-L5-3) `InventoryProjectionService.onAdjusted` (50 LOC, 두 책임) → main body 4-line (`appendAuditRowIfAbsent(envelope, p, occurredAt); return updateSnapshotFromAdjustment(p, occurredAt);`) + 2 private method 추출: `appendAuditRowIfAbsent(ProjectionEnvelope, JsonNode, Instant)` (~25 LOC, early return guard 패턴) + `updateSnapshotFromAdjustment(JsonNode, Instant): DedupeOutcome` (~15 LOC, inventory object 부재 시 early return APPLIED). 동작 byte-identical. **Test fixture update**: `OperationsControllerWebMvcTest` 의 `@MockitoBean AdminEventDedupeRepository dedupePort` → `@MockitoBean ProjectionStatusService projectionStatusService` (controller 가 service 만 의존, mock 1-arg). 기존 3 test case (admin 200 + viewer 403 + unauthenticated 401) 모두 보존 — `when(projectionStatusService.computeStatus()).thenReturn(new ProjectionStatusResponse(List.of(), 0.0d, 120, 3, 1, 0))` stubbing 으로 같은 jsonPath assertion 그대로 PASS. **AC verify**: grep `ProjectionStatusService` in main = 4 (definition + controller field/constructor/method) / grep `DEFAULT_LOW_STOCK_THRESHOLD` = 2 (definition + applySnapshot use) / grep `appendAuditRowIfAbsent | updateSnapshotFromAdjustment` = 4 (2 definitions + 2 onAdjusted calls) / Local `./gradlew :projects:wms-platform:apps:admin-service:check --rerun-tasks` BUILD SUCCESSFUL 31s (7/7 actionable) / CI 19/20 GREEN authoritative / GET /api/v1/admin/operations/projection-status response shape byte-identical / `admin.role.*` outbox event payload byte-identical / inventory snapshot lowStock 임계 동일 (10) / **34회째 zero-retrofit invariant** (`projects/{scm,global-account,erp,fan,ecommerce-microservices,finance,platform-console}-platform/` + `libs/` byte-unchanged) / BE-297 회귀 0 (cascade-revoke dedup 패턴 보존). **dispatcher 독립 재검증** (BE-303 3-dim): **spec PR #858**: (a) state=MERGED + mergeCommit=`7516ee26`; (b) `git log origin/main` tip `7516ee26` Fast-forward `dbeb6551..7516ee26`; (c) markdown fast-lane — 3-dim ALL GREEN. **impl PR #859**: (a) state=MERGED + mergeCommit=`1142be12` + 19 SUCCESS + 1 SKIPPED + 0 FAILURE; (b) `git log origin/main` tip `1142be12` Fast-forward `7516ee26..1142be12`; (c) pre-merge failing=0 — 3-dim ALL GREEN. **close chore (this PR)** — `git mv review/ → done/` + Status `review → done` + INDEX move; BE-299 re-stage check verified (`git show :done-path` reads `Status: done`); BE-303 3-dim 객관 머지 검증 완료 before close chore start. **메타**: ① **wms admin-service 2-cohort sequential closure (single-day)** — BE-297 Cohort C1 직후 BE-299 Cohort C3 closure. 8 finding 중 7 closure (C1=3 + C3=4 = 7). 잔여 = F-L6-2 (Cohort C2 4-Kafka-consumer dispatch dedup, medium risk Kafka IT 의존). dry-run base rate 의 high-ROI portion (C1+C3 = low risk mechanical) 가 single-day closure cycle 에 적합. ② **Layered service 도입 = api/dto/ import borderline acceptable** — Layered architecture override 에서 application service 가 api/dashboard/dto/ProjectionStatusResponse 를 직접 반환. Hexagonal 의 inbound port + DTO mapping 비용 회피. controller 의 layer-violation 정정이 service 의 dto-import 도입 trade-off 으로 균형 — Layered 에서는 acceptable boundary (Hexagonal 이라면 application/dto/ 분리 필요). ③ **WebMvcTest mock 변경 = thin controller pattern 의 자연스러운 결과** — controller 가 service 만 inject 받으니 WebMvcTest 도 service 만 mock. 기존 4-dependency stubbing (dedupePort + registry + lagProbe + groupId) 부담 → 1-dependency `projectionStatusService.computeStatus()` stubbing. 기존 jsonPath assertion (lifetimeApplied = 120, etc.) 그대로 PASS = mock return 값으로 보존. service 의 computation logic 자체 unit test 는 별 `ProjectionStatusServiceTest` 후보 (본 task scope 밖, future). ④ **Silent catch → log.warn = behavior-neutral + observability gain** — `catch (Exception e) { return List.of(); }` → `catch (Exception e) { log.warn(...); return List.of(); }`. 사용자 표면 동작 (empty list 반환) 보존, ops 측 observability 만 추가. `coding-rules.md § Exceptions` 의 silent swallow 금지 원칙 부합. SLF4J Logger field 추가 = mechanical (RoleService 에 기존 Logger 부재 verify 후 추가). ⑤ **early-return refactor pattern = behavior-identical method split** — onAdjusted 의 두 phase 가 사실상 independent (audit row 가드 + snapshot upsert 가드). private method 추출 시 if-block 의 negation 으로 early return 변환 (`if (!auditRepo.existsById(auditId)) { ... }` → `if (auditRepo.existsById(auditId)) return;`). main onAdjusted = 두 method 호출만, return 값 두 번째 method 의 결과. 동작 byte-identical, readability gain. 분석=Opus 4.7 / 구현=Opus 4.7 (직접) / 리뷰=Opus 4.7.

- `TASK-BE-297-admin-cascade-revoke-dedup.md` — **DONE (Cohort C1 applied — L5+L6 deactivate-cascade dedup)**. 분석/리뷰=Opus 4.7 / 구현=Opus 4.7 (직접; mechanical Extract Method + 2 caller site dedup + 3 test fixture constructor 업데이트). `/refactor-code wms admin-service` (2026-05-26) dry-run의 첫 cohort closure (F-L5-1 + F-L5-2 + F-L6-1). **spec PR #855** (squash `097689e7`, mergedAt 2026-05-27T+09KST, markdown fast-lane). **impl PR #856** (squash `2411b0d9`, mergedAt 2026-05-27T+09KST, +54/-25 across 7 files; **CI 19 SUCCESS + 1 SKIPPED + 0 FAILURE** incl. `Integration (master-service + notification-service, Testcontainers)` + `Integration (iam-platform, Testcontainers)` 양쪽 authoritative GREEN). **Implementation**: `AssignmentEventHelper` 에 `cascadeRevoke(List<UserRoleAssignment>, String, String, Instant): List<UUID>` 신규 메서드 + `AssignmentRepository` field/constructor inject. for-each 루프 안에서 (a) `a.revoke(occurredAt, actorId)` (b) `assignmentRepository.save(revoked)` (c) `revokedIds.add(saved.id())` (d) `appendAssignmentRevokedEvent(saved, cascadeReason, actorId, occurredAt)` — 7-line byte-identical loop 의 정확한 transcribe. `UserService.deactivate` (L131-138, 7-line) + `RoleService.deactivate` (L137-144, 7-line) 모두 단일 `assignmentEventHelper.cascadeRevoke(...)` 호출로 대체. cascadeReason 두 값 (`"USER_DEACTIVATED"` + `"ROLE_DEACTIVATED"`) 보존, 인자로 전달. **two-phase clock pattern 보존**: cascade instant (`cascadeNow = clock.instant()`) 와 후속 deactivate instant (`now = clock.instant()`) 분리 보존 (기존 동작 byte-identical — javadoc 명시). **3 test fixture constructor 업데이트**: `UserServiceTest.java:44` + `UserServiceAuthzTest.java:51` + `RoleServiceTest.java:45` 의 `new AssignmentEventHelper(outbox, envelopeBuilder)` → `new AssignmentEventHelper(assignmentRepo, outbox, envelopeBuilder)` (3-arg). **AC verify**: grep `cascadeRevoke` in main: 2 caller sites (UserService:L133 + RoleService:L139) + helper L61 definition + helper internal L68 `appendAssignmentRevokedEvent` 호출 / direct `appendAssignmentRevokedEvent` 호출 in callers = 0 (모두 cascade-revoke loop dedup) / `./gradlew :projects:wms-platform:apps:admin-service:check --rerun-tasks` BUILD SUCCESSFUL 1m 6s (7/7 actionable) / CI 19/20 GREEN authoritative / `admin.assignment.revoked` event payload byte-identical (assignmentId/userId/roleId/warehouseId/revokedAt/revokedBy/cascadeReason) / **33회째 zero-retrofit invariant** (`projects/{scm,global-account,erp,fan,ecommerce-microservices,finance,platform-console}-platform/` + `libs/` byte-unchanged). **dispatcher 독립 재검증** (BE-303 3-dim): **spec PR #855**: (a) state=MERGED + mergeCommit=`097689e7`; (b) `git log origin/main` tip `097689e7` Fast-forward `54d048bc..097689e7`; (c) markdown fast-lane — 3-dim ALL GREEN. **impl PR #856**: (a) state=MERGED + mergeCommit=`2411b0d9` + 19 SUCCESS + 1 SKIPPED + 0 FAILURE; (b) `git log origin/main` tip `2411b0d9` Fast-forward `097689e7..2411b0d9`; (c) pre-merge failing=0 — 3-dim ALL GREEN. **close chore (this PR)** — `git mv review/ → done/` + Status `review → done` + INDEX move; BE-299 re-stage check verified (`git show :done-path` reads `Status: done`); BE-303 3-dim 객관 머지 검증 완료 before close chore start. **메타**: ① **First wms admin-service refactor sweep cycle since service inception** — 직전 SCM (BE-016/017/018) + GAP (BE-288~303/+) + PC console-bff cluster TRUE 0 도달 후 wms admin-service 가 first new-target sweep. dry-run 결과 = meaningful backlog (L5=3 / L6=2 / L1=1 / L2=1 / L3=1 = 8 net finding). Cohort C1 (L5+L6 dedup) 만 본 task scope. C2 (L6 4-Kafka-consumer dispatch, medium risk) + C3 (L1+L2+L3+L5 mixed hygiene) 잔존 후보. ② **Layered architecture override 에서 helper boundary 의 자연스러움** — wms admin-service 가 `Layered` (architecture.md `## Overrides`) 라 Hexagonal port/adapter 부재. `AssignmentEventHelper` 가 이미 `application/assignment/` 위치 = cascade-revoke 책임 추가의 정확한 boundary. inter-layer 변경 0 = pure intra-application dedup. ③ **Test fixture constructor update = 3 caller site mechanical** — 새 constructor signature 의 3-arg 형태가 기존 2-arg 형태의 자연스러운 superset (assignmentRepo prepend). 3 test 모두 동일 `assignmentRepo` 변수 사용 = 같은 패턴 mechanical 일괄 edit. SCM-BE-018 의 `git add -A` `.clone/` leak hazard 재출현 없이 specific paths 로 stage (env_git_worktree_verify_windows 패턴 준수). ④ **two-phase clock pattern preservation** — 기존 코드의 `Instant now = clock.instant()` 가 cascade loop 내부 + deactivate event 외부 두 site 에서 호출 = 두 distinct instant. spec 의 초기 AC-5 ("동일 instant") 는 잘못된 해석 — 실제 코드는 의도된 distinct. impl 단계에서 정정해서 cascadeNow (cascade loop 공유) + now (deactivate event) 두 변수로 분리 보존. helper javadoc 에 명시. ⑤ **CI authoritative verify post-Windows-local: Java compile error catch** — local `:check --rerun-tasks` 가 1차에서 compile fail (test 의 2-arg constructor mismatch). 3 test fixture site 발견 + 1-line edit 각 → 2차 `:check` GREEN 1m 6s. local fail 의 fast feedback 가 CI authoritative verify 의 효율 enhance. 분석=Opus 4.7 / 구현=Opus 4.7 (직접) / 리뷰=Opus 4.7.

- `TASK-BE-293-wms-spec-drift-gateway-routes-openitems-idempotency.md` — **REVIEWED → approved** (2026-05-16, `/review-task` single-task, review-checklist Spec/Arch/Quality/Security PASS · Perf N/A · Testing PASS via task Verification; fix task 0; on-disk 재검증: route 정합 + ✅ Glob 재확인 + EXTERNAL_TIMEOUT 부재 재확인(⚠️ 정직) + dead-ref 8-file 0). impl `task/spec-drift-cohort-2026-05-16` (spec-only, no `apps/`). **WI-1 (W9)**: `gateway-service/architecture.md` route 표에 `/api/v1/outbound/** → outbound-service` + HMAC-only `/webhooks/erp/order` 추가 (overview.md canonical 정합; outbound = `outbound-service-api.md` base `/api/v1/outbound` + `erp-order-webhook.md` 실재 → gateway-exposed 확정; `/webhooks/erp/asn` + notification no-REST note 보존). 비차단 관찰: 표의 per-service host port(8081-8086) vs overview.md `:8080` 선존 drift 는 W9 scope 외 (신규 row 는 overview.md canonical `:8080`). **WI-2 (W18)**: master(6 item, 전부 ✅) + outbound(12 item) `architecture.md` Open Items → `inventory-service` retrospective ✅/⚠️/❌ format 답습; "must be completed before" framing 제거; master:186 "to be authored" stale line 정정. **정직 마킹**: outbound #11 = ⚠️ (`EXTERNAL_TIMEOUT` platform/error-handling.md 미등록 — "already global" claim stale → 별도 TASK-MONO-* 후보), #12 = ✅ (WI-1 본 PR 해소). Glob 전수 검증, canonical Identity/H3 무손상(append/reframe only). **WI-3 (W15) 결정 = (B) document divergence (NOT normalize)**: master idempotency.md 에 단일 authoritative `## Cross-Service Idempotency Key Conventions` 섹션(5-svc shape/cap 비교표 + per-service rationale: master hashed-flat-64 = key-length safety, inventory/inbound/admin raw-128 = debuggability, outbound raw-255 = saga-flow accommodation) + inventory/inbound/admin/outbound idempotency.md 4 pointer. notification(Postgres event-dedupe) = out-of-set 명시. **ADR 불요** — (B) 는 새 convention 미설정·기존 의도적 상태 기록 (normalize 면 5-svc code change + portfolio convention → 미래 TASK-MONO-*+ADR, README/섹션에 deferred 명시). dead-ref 8-file 0, apps/ 0. 분석=Opus 4.7 / 구현=Opus 4.7 / 리뷰=Opus 4.7.

- `TASK-BE-287-outbox-shape-divergence-mono-049-cross-ref.md` — spec commit `a84b0bc0` + impl PR #524 (squash `6bfe2afa`) + close chore (BE-286 직속 후속). **APPROVED (inline self-review)** — `/refactor-spec all --dry-run` non-deadref **Tier 2 F-06+F-07 spec-only closure**. master vs 5 sibling outbox schema divergence (master = `libs/java-messaging` BIGSERIAL+TEXT+`status` enum / 5 sibling = wms-specific UUID+JSONB+`partition_key`+TIMESTAMPTZ) 의 cross-reference + **TASK-MONO-049 § 6 follow-up #1 deferred per ADR-MONO-003 D2 cadence (≥ 2026-06-10)** pointer 명시. 6 file edit (+35 / -2 / 0 production code): master § 2 existing divergence paragraph augmented + 5 sibling (admin/inbound/inventory/notification/outbound) outbox section reverse cross-ref. **Implementation 본 task 범위 외** — D2 freeze 존중, MONO-049 § 6 existing decision 유지. **검증**: 6/6 file `grep "MONO-049"` + `grep "ADR-MONO-003"` 양쪽 hit. **CI**: 1 PASS (`changes`) / 16 SKIPPED / 0 fail. branch = `task/be-287-outbox-shape-divergence-mono-049-cross-ref` (main 분기, BE-286 chore 머지 commit `26958054` 직후). lifecycle = ready → review → done. D4 OVERRIDE: ADR-MONO-003a § D1.1 (project-internal governance polish, refactor-spec follow-up). **refactor-spec cycle 완전 종결**: 8 task / 누적 75 fix (54 deadref + 6 + 1 + 8 sections + 6 file cross-ref). **non-deadref Tier 2 backlog = 0** (모든 audit finding closure 또는 SKIP rationale 문서화). Implementation migration 은 별 cycle (D2 freeze ≥ 2026-06-10 이후). 분석=Opus 4.7 / 구현=Opus 4.7 / 리뷰=Opus 4.7 (inline self-review, 6 file governance pointer + 기존 deferred decision 존중).

- `TASK-BE-286-inventory-reservation-saga-status-shape-backfill.md` — spec commit `7064b91d` + impl PR #522 (squash `78b65d6b`) + close chore (BE-285 / MONO-091 직속 후속). **APPROVED (inline self-review)** — `/refactor-spec all --dry-run` non-deadref **Tier 2 F-01+F-02 paired closure**. inventory-service saga + state-machine spec sibling-emulated backfill: (a) `reservation-status.md` 5 → **11** H2 (+6 신규: Transition Rules / Guard Conditions / Concurrency / Reverse Flows v1: forbidden / Error-Code Mapping / Test Requirements) + 기존 Invariants/Forbidden Patterns/References 보존; (b) `reservation-saga.md` 8 → **10** H2 (+2 신규: § 8 Test Matrix 7 sub-section + § 9 Open Questions) + § References renumber → § 10 + renames 보존 (Operations per-event / Compensation / Concurrency Guarantees / Failure Modes — event-driven choreography architectural reality vs orchestrated sibling). 2 file / +307 / -1 / 0 production code change. **Sibling emulation 조정**: outbound-saga § 8 9 sub-section 중 sweeper/TMS/contract-test 는 reservation 의 Category D TTL 으로 architectural difference 흡수 (§ 8.4 TTL folded) + REST Idempotency § 8.6 추가 (MANUAL release 만 의 sibling 없는 case). § 9 = "no open questions for v1" + 4 v2 candidate. **검증**: `grep -cn "^## "` reservation-status = 11 (target ≥ 10 PASS), reservation-saga = 10 (sibling 10 match). **CI**: 1 PASS (`changes`) / 16 SKIPPED / 0 fail. branch = `task/be-286-inventory-reservation-saga-status-shape-backfill` (main 분기, MONO-091 chore 머지 commit `2818873d` 직후). lifecycle = ready → review → done. **agent dispatch**: backend-engineer subagent (Sonnet) sibling content emulation 위임 (Test Matrix + Open Questions content 작성), main context verification. D4 OVERRIDE: ADR-MONO-003a § D1.1 (project-internal spec polish, refactor-spec follow-up). **refactor-spec cycle progression**: 7th task / 누적 69 fix (54 deadref + 6 + 1 + 8 sections). **Tier 2 backlog 마지막 잔여**: F-06+F-07 outbox/processed_events schema authority (HIGH risk, libs/java-messaging cross-check + ADR-level decision). 분석=Opus 4.7 / 구현=Sonnet 4.6 (subagent) / 리뷰=Opus 4.7.

- `TASK-BE-285-wms-spec-mechanical-naming-clarity-batch.md` — spec commit `e8f765e5` + impl PR #518 (squash `eb82d7be`) + close chore (BE-165 + refactor-spec promotion #517 직속 후속). **APPROVED (inline self-review)** — `/refactor-spec all --dry-run` **non-deadref Tier 1 mechanical batch closure**. 6 fix / 5 file / 7 line edit / 0 production code: (F-04) notification/architecture.md L23 `### Service Type — single` → `### Service Type Composition` (7 WMS sibling canonical); (F-05a/b/c) admin/gateway/notification architecture.md `## Architecture Style: X` / `Rationale` suffix drop → canonical `## Architecture Style` (body 가 style 정당화 보존); (F-09) admin/domain-model.md L138 drop `; specific code TBD` hedge, `(reused)` 보존; (F-10) inventory/database-design.md L235-236 drop `if needed` hedge. **F-03 SKIP** (zero-state vs active-integration intentional structural divergence — numbered vendor catalog vs narrative, not author drift) § Out of Scope 기록. **검증**: 7/7 WMS architecture.md `### Service Type Composition` + `## Architecture Style` canonical aligned + `grep "specific code TBD"` / `grep "shape if needed"` = 0 hit. **CI**: 1 pass (`changes`) / 16 SKIPPED / 0 fail. branch = `task/be-285-wms-spec-mechanical-naming-clarity-batch`. lifecycle = ready → review → done. D4 OVERRIDE: ADR-MONO-003a § D1.1 (project-internal spec polish). **refactor-spec cycle progression** = 4 deadref task (BE-165/283/SCM-BE-013/BE-284) + 본 5번째 (non-deadref Tier 1). **Tier 2 backlog (별 task 후보)**: F-01+F-02 reservation shape (sibling emulation, judgment) / F-06+F-07 outbox/processed_events schema authority (HIGH risk, libs/java-messaging cross-check) / F-08 glossary Service Type pointer (governance drift 차단). 분석=Opus 4.7 / 구현=Opus 4.7 / 리뷰=Opus 4.7 (inline self-review, 4th refactor-spec mechanical batch + F-03 re-classification).

- `TASK-BE-165-wms-spec-dead-ref-mechanical-batch.md` — single-PR closure (2026-05-14, same-day BE-164 직속 후속). **APPROVED (inline self-review)** — `/refactor-spec all --dry-run` (BE-153~164 portfolio gap 완전 종결 직후 audit) Tier 1 mechanical batch closure. 5 fix / 3 spec file / 5 line edit / 0 production code: (1+2) `tms-shipment-api.md` L390+L393 `rules/traits/integration-heavy.md` + `platform/security-rules.md` depth `../../../../` → `../../../../../` (5-level deep at `projects/wms-platform/specs/contracts/http/`); (3) `master-service/database-design.md` L195 `ZoneType.java` package `domain/zone/` → `domain/model/` (production code 의 실제 package, `find` 검증); (4+5) `outbound-service/database-design.md` L793 state-machine basename `outbound-saga-status.md` → `saga-status.md` + "(if present)" hedge 제거. 모두 NEW WMS author artifact (BE-147 tms-shipment-api.md + BE-161 master DB design + BE-163 outbound DB design — 모두 2026-05-14 머지). TASK-MONO-085/086 mechanical batch + post-merge re-verification precedent 답습. **검증**: 3 modified file 의 dead-ref checker = 0 broken. **CI**: 2 pass (`changes` 6s + `Frontend E2E smoke` 2m54s) / 15 SKIPPED (path-filter 의도) / 0 fail. branch = `task/be-165-wms-spec-dead-ref-batch` (main 분기, BE-164 머지 commit `411ce1e0` + MONO-promote `c2d73dd7/55862a46` 직후). lifecycle = ready → review → done (2-commit task author + impl + close chore). D4 OVERRIDE: ADR-MONO-003a § D1.1 (project-internal spec polish, refactor-spec follow-up). **Tier 3 잔존** (별 task 후보, 별 author origin): GAP 53 `libs/*` pre-existing depth bug (pre-import era artifact, MONO-085 scope 누락) + SCM 1 `scm-procurement-events.md:44` ADR-MONO-004 path. 분석=Opus 4.7 / 구현=Opus 4.7 / 리뷰=Opus 4.7 (inline self-review, refactor-spec audit → Tier 1 single-PR closure + post-author re-verification).

- `TASK-BE-164-admin-service-database-design.md` — single-PR closure (2026-05-14, same-day BE-163 직속 후속). **APPROVED (inline self-review)** — **portfolio-wide database-design.md gap 완전 종결 (6/6 service)** + BE-157/160/161/162/163 답습 sixth + final-of-portfolio. `admin-service/database-design.md` 신규 897 line, 16 section: Schema Overview ASCII (21 table = portfolio 최대, write-side 6 vs read-model 15 분리) / **write-side V1** 6 table (admin_user case-insensitive email partial-expression unique LOWER(email) + admin_role permissions_json + is_builtin / admin_user_role_assignment **partial unique COALESCE sentinel UUID** WHERE status='ACTIVE' / admin_setting composite PK with sentinel + scope CHECK constraint / admin_outbox wms-specific shape + aggregate_id VARCHAR(120) divergence / admin_event_dedupe **4-outcome enum incl. IGNORED_DUPLICATE_LATE** unique to admin LWW context) / **§ 7 Read-Model Projection Pattern (LWW upsert)** SQL formula + No FK between read-model tables 정당화 + **PK=eventId natural-dedupe** (trigger-free 대안) / **read-model V2** 15 table (6 master ref + ASN/inspection summary + Order/shipment summary + **admin_inventory_snapshot composite PK sentinel lot_id** + **4 qty columns** + **partial low_stock_flag index** / **admin_adjustment_audit + admin_alert_log append-only via PK=eventId** + **alert_log conditional mutability** ack columns only per architecture.md § 1.6 + partial unacked index / 2 daily throughput counter atomic UPSERT compound PK date+warehouse) / § 14 V99 dev seed narrative (4 built-in role stable UUID + bootstrap admin + 7 seed settings, profile-gated) / Indexing summary 70-row catalog / Migration History V1+V2+V99. **3 sentinel UUID instance** (admin_setting + admin_inventory_snapshot + admin_user_role_assignment COALESCE) 일관 패턴. architecture.md L327 cross-link 정정 (`domain-model.md — Open Items` → `database-design.md`) + L330 추가 cross-link. HARDSTOP-10 hook clean path (admin architecture.md L23 `### Service Type Composition` 이미 존재, outbound 와 동일). **검증**: 16/16 reference path resolution PASS, marker regression 0 (admin 6 spec file), 897 line (AC target 1000-1200 slight under, V2 master ref 6 narrative-only 약식 표현). production code 변경 0, Flyway SQL 변경 0 (markdown only + architecture.md 2-line edit). CI = path-filter (TASK-MONO-074/075) → markdown-only wms-spec → ~1 PASS + 15 SKIP 예상. branch = `task/be-164-admin-service-database-design` (main 분기, BE-163 머지 commit `ceadcd0c` 직후). lifecycle = ready → done (single-PR closure 25번째 entry, BE-141~163 precedent). D4 OVERRIDE: ADR-MONO-003a § D1.1 (project-internal spec backfill). **portfolio-wide database-design.md gap 완전 종결** — 6/6 service 모두 보유 (gateway N/A schemaless). external-integrations.md (7/7) + database-design.md (6/6) **두 portfolio gap 모두 종결**. 분석=Opus 4.7 / 구현=Opus 4.7 / 리뷰=Opus 4.7 (inline self-review, BE-157~163 답습 final-of-portfolio + 21 table 최대 + CQRS Layered exception + LWW upsert formula + PK=eventId natural-dedupe + 3 sentinel UUID + 4-outcome IGNORED_DUPLICATE_LATE + conditional mutability alert_log).

- `TASK-BE-163-outbound-service-database-design.md` — single-PR closure (2026-05-14, same-day BE-162 직속 후속). **APPROVED (inline self-review)** — **portfolio-wide database-design.md gap 2 service 중 #1 closure** (outbound, BE-157/160/161/162 답습 fifth-of-portfolio). `outbound-service/database-design.md` 신규 804 line, 11 section: Schema Overview ASCII (15 table dependency graph) / Master Read Model (V1, 6 snapshot 동일 inbound 4-enum CARRIER) / **Order Aggregate (V2 + V10 schema_align)** — order_no partial unique + zero-downtime legacy column 유지 / **Picking (V3 + V11 align)** saga_id cross-ref + UNIQUE 1:1 / **Packing+Shipping (V4 + V11 + V12)** carton_no per-order partial unique + shipment_no global partial unique + V12 created_by BE-040 / **OutboundSaga (V5 + V14 BE-050)** re_emit_count + sweeper-candidates partial index (`WHERE status IN ('REQUESTED','CANCELLATION_REQUESTED','SHIPPED')`) / **Outbox+EventDedupe (V6 + V9 align)** status+retry_count publisher 컬럼 unique to outbound + wms-specific shape completion / ERP Order Webhook (V7) / **TMS Request Dedupe (V4 → V13 BE-049)** vendor idempotency I4 fallback dual-shape evolution / Append-Only Enforcement (V8, 4 trigger 동일 inbound 패턴) / Indexing summary 48-row catalog / Migration History 14-row evolution table (9 init + 4 align + 2 feature). architecture.md L494 cross-link 정정 (`domain-model.md` → `database-design.md`). **15 table + 4 trigger + 14 migration = portfolio 최대 evolution count** (inbound 18 table 보다 작지만 migration count 1.75배). HARDSTOP-10 hook clean path (outbound architecture.md 가 이미 `### Service Type Composition` 보유). **검증**: 16/16 reference path resolution PASS (sibling 5 spec + 4 sibling database-design + V1/V14 SQL + 4 rules/platform + ADR-MONO-005 saga-timeout-escalation-dead-letter-policy 정확 파일명 fix), marker regression 0 (outbound 6 spec file), 804 line (AC target 850-1000 slight under, V1 master read-model narrative 약식 표현으로 절약). production code 변경 0, Flyway SQL 변경 0 (markdown only + architecture.md 1-line edit). CI = path-filter (TASK-MONO-074/075) → markdown-only wms-spec → ~1 PASS + 15 SKIP 예상. branch = `task/be-163-outbound-service-database-design` (main 분기, BE-162 머지 commit `25c06915` 직후). lifecycle = ready → done (single-PR closure 24번째 entry, BE-141~162 precedent). D4 OVERRIDE: ADR-MONO-003a § D1.1 (project-internal spec backfill). **portfolio-wide database-design.md gap 잔존 1 service** = admin (예상 ~1170 line, BE-164 single task 후보). 분석=Opus 4.7 / 구현=Opus 4.7 / 리뷰=Opus 4.7 (inline self-review, BE-162 답습 fifth-of-portfolio + evolution-rich 14-migration narrative + TMS I4 V4/V13 dual-shape + saga sweeper V14 + outbox V6/V9 shape completion).

- `TASK-BE-162-inbound-service-database-design.md` — single-PR closure (2026-05-14, same-day BE-161 직속 후속). **APPROVED (inline self-review)** — **portfolio-wide database-design.md gap 3 service 중 #1 closure** (inbound, BE-157/160/161 답습 fourth-of-portfolio). `inbound-service/database-design.md` 신규 746 line, 10 section: Schema Overview ASCII (18 table dependency graph) / Master Read Model (V1, 6 snapshot — **partner_snapshot 4-enum CARRIER 추가** vs master-service 3-enum) / ASN Aggregate (V2, 7-status state machine + 2-enum source MANUAL/WEBHOOK_ERP) / Inspection Aggregate (V3, 1:1 ASN UNIQUE + 3 qty bucket + partial index `WHERE acknowledged=FALSE`) / Putaway Aggregate (V4, immutable putaway_confirmation + planned vs actual location 분리) / Outbox+EventDedupe (V5, wms-specific UUID+JSONB+partition_key vs master libs BIGSERIAL+TEXT 차이 명시) / ERP Webhook (V6, status FSM + dual partial index pending/failed + 80-char event_id) / **Append-Only Enforcement V7 = 4 trigger function** (inventory V5 2-trigger pattern 답습 + 확장: dedupe + webhook_dedupe + putaway_confirmation full-block + **inbound_outbox DELETE-only** publisher UPDATE 허용) / ASN Number Sequence (V8, per-day atomic UPSERT `INSERT...ON CONFLICT...DO UPDATE...RETURNING`) / Indexing Strategy Summary 56-row catalog + Migration History (V1-V8). architecture.md L389 cross-link 정정 (`domain-model.md` → `database-design.md`). **18 table = portfolio 최대 schema** (master 8 / inventory 11 / notification 4 / inbound 18). HARDSTOP-10 hook clean path (BE-150 이미 closure). **검증**: 14/14 reference path resolution PASS (sibling 4 spec + 3 sibling database-design BE-157/160/161 + V1+V8 SQL + 4 rules/platform), marker regression 0 (inbound 6 spec file), 746 line (AC target 700-900 PASS). production code 변경 0, Flyway SQL 변경 0 (markdown only + architecture.md 1-line edit). CI = path-filter (TASK-MONO-074/075) → markdown-only wms-spec → ~1 PASS + 15 SKIP 예상. branch = `task/be-162-inbound-service-database-design` (main 분기, BE-161 머지 commit `5b3dfe52` 직후). lifecycle = ready → done (single-PR closure 23번째 entry, BE-141~161 precedent). D4 OVERRIDE: ADR-MONO-003a § D1.1 (project-internal spec backfill). **portfolio-wide database-design.md gap 3 service 중 #1 closure** — 잔존 2 service (outbound/admin) 별 single task 후보 (BE-163/164, 예상 ~940/1170 line). 분석=Opus 4.7 / 구현=Opus 4.7 / 리뷰=Opus 4.7 (inline self-review, BE-157/160/161 답습 fourth-of-portfolio + byte-identical SQL transcribe + 18 table largest scope + V7 4-trigger pattern + atomic UPSERT V8 + partner snapshot 4-enum divergence).

- `TASK-BE-161-master-service-database-design.md` — single-PR closure (2026-05-14, same-day BE-160 직속 후속). **APPROVED (inline self-review)** — **portfolio-wide database-design.md gap 4 service 중 #1 closure** (master, BE-157/BE-160 답습 third-of-portfolio). `master-service/database-design.md` 신규 489 line, 9 section: Schema Overview ASCII (8 table dependency graph) / Warehouse (V1) / Outbox+ProcessedEvent (V2, libs/java-messaging shared, BIGSERIAL+TEXT 다른 wms service 의 wms-specific UUID+JSONB shape 와 차이 명시) / Zone (V3, FK warehouse + compound unique + 6-enum zone_type) / Location (V4, **W3 globally unique location_code**, FK warehouse+zone + denormalized warehouse_id + 5-enum location_type) / SKU (V5, **UPPERCASE check + partial unique barcode**) / Lot (V6, FK sku + compound unique sku_lotno + **supplier_partner_id intentional no-FK** + partial expiry index) / Partner (V7, 3-enum partner_type + internal B2B contact) / Indexing summary 26-row catalog / Migration history (V1-V7) + References. **HARDSTOP-10 hook trigger 발생 → BE-150/BE-154 답습으로 해소**: master-service architecture.md L14 `Service Type \`rest-api\`` cell + standard sub-section 부재 → CRLF→LF normalize (PowerShell `[System.IO.File]::WriteAllText`, 321 line) + `### Service Type Composition` sub-section 추가 (3-row table, single rest-api path) + L14 cell `(single; see Service Type Composition below)` 정정. **세 번째 HARDSTOP-10 instance** (BE-150 inbound + BE-154 gateway + BE-161 master). architecture.md L228 `master_outbox` 명칭 정정 (실제 V2 table 이름 = `outbox` libs schema) + L230-231 cross-link 정정 (`migration scripts ... domain-model.md` → `database-design.md ... domain-model.md`). **검증**: 13/13 reference path resolution PASS, marker regression 0 (master 6 spec file), 489 line (AC target 440-500 PASS). production code 변경 0, Flyway SQL 변경 0 (markdown only + architecture.md 3-block edit: Service Type Composition + L228 outbox cell + L230-231 cross-link). CI = path-filter (TASK-MONO-074/075) → markdown-only wms-spec → ~1 PASS + 15 SKIP 예상. branch = `task/be-161-master-service-database-design` (main 분기, BE-160 머지 commit `3c5eaacf` 직후). lifecycle = ready → done (single-PR closure 22번째 entry, BE-141/142/FAN-BE-006/MONO-084/BE-281/BE-145~160 precedent). D4 OVERRIDE: ADR-MONO-003a § D1.1 (project-internal spec backfill). **portfolio-wide database-design.md gap 4 service 중 #1 closure** — 잔존 3 service (inbound/outbound/admin) 별 single task 후보 (BE-162/163/164, 예상 ~900/940/1170 line). **HARDSTOP-10 hook portfolio 잠재 잔여**: outbound/admin/notification architecture.md 도 동일 standard header 부재 가능성 (notification 은 이미 BE-160 으로 정찰 시 hook trigger 안 함 = standard pattern 통과; outbound/admin 미확인). 분석=Opus 4.7 / 구현=Opus 4.7 / 리뷰=Opus 4.7 (inline self-review, BE-157/160 답습 + W3 W6 invariant cross-reference + libs/java-messaging shared schema 차이 명시 + HARDSTOP-10 inline 해소).

- `TASK-BE-160-notification-service-database-design.md` — single-PR closure (2026-05-14, same-day BE-159 직속 후속). **APPROVED (inline self-review)** — **portfolio-wide database-design.md gap 5 service 중 #1 closure** (notification, BE-157 inventory 답습 second-of-portfolio). `notification-service/database-design.md` 신규 323 line, 7 section: Schema Overview ASCII / NotificationRoutingRule (V1 + partial unique `WHERE enabled=true` T6) / NotificationDelivery (V1 + state machine SQL constraint PENDING→SUCCEEDED|FAILED + delivery_idempotency_key UNIQUE + version optimistic-lock + retry scheduler hot-path partial index) / NotificationEventDedupe (V1 T8, outcome enum QUEUED|FILTERED|NO_RULE|ERROR) / NotificationOutbox (V1 T3 audit-only + pending-publisher partial index) / V2 Seeded Routing Rules (6 row UUID v7 prefix table) / Indexing Strategy Summary (11-row catalog) / Migration History (V1 schema + V2 data) + References. **architecture.md L314 cross-link 정정**: `(full layout in domain-model.md — Open Items)` → `(full layout in [database-design.md](database-design.md))`. byte-identical Flyway V1+V2 (194 line, 4 table + 6 seed row) reflection. **검증**: 12/12 reference path resolution PASS (sibling 4 spec + V1/V2 SQL + 3 rules/platform + inventory database-design.md BE-157 답습 reference). `grep -rn "(Open Item" projects/wms-platform/specs/services/notification-service/` = 0 (regression 0). production code 변경 0, Flyway SQL 변경 0 (markdown only, architecture.md 1 line edit). CI = path-filter (TASK-MONO-074/075) → markdown-only wms-spec → ~1 PASS + 15 SKIP 예상. branch = `task/be-160-notification-service-database-design` (main 분기, BE-159 머지 commit `c04e29d1` 직후). lifecycle = ready → done (single-PR closure 21번째 entry, BE-141/142/FAN-BE-006/MONO-084/BE-281/BE-145~159 precedent). D4 OVERRIDE: ADR-MONO-003a § D1.1 (project-internal spec backfill). **portfolio-wide database-design.md gap 5 service 중 #1 closure** — 잔존 4 service (master/inbound/outbound/admin) 별 single task 후보 (BE-161/162/163/164). 분석=Opus 4.7 / 구현=Opus 4.7 / 리뷰=Opus 4.7 (inline self-review, BE-157 답습 retrospective pattern + byte-identical SQL transcribe + JSONB @JdbcTypeCode regression-guard cross-ref).

- `TASK-BE-159-portfolio-external-integrations-zero-state-batch.md` — single-PR closure (2026-05-14, same-day BE-158 직속 후속). **APPROVED (inline self-review)** — **portfolio-wide external-integrations.md gap 완전 종결** (BE-156 inventory + BE-158 notification + BE-159 3 service batch = 5 service 누적, **wms 7 service 모두 external-integrations.md 보유** — 7/7 종결). **3 신규 file batch** (BE-156 zero-state pattern 답습, sibling reverse 패턴 정형화 후 3 service 동시 적용): (1) `master-service/external-integrations.md` 98 line (upstream anchor zero-state, v2 ERP/PIM sync placeholder); (2) `admin-service/external-integrations.md` 105 line (CQRS read-side projector + Layered architecture exception, v2 external IdP SAML/SCIM placeholder); (3) `gateway-service/external-integrations.md` 103 line (edge gateway + OAuth2 Authorization Server = `iam-platform` sibling project = **infrastructure 분류**, v2 external SaaS IdP swap placeholder). 3 file 모두 7 section 구조 (헤더 / Catalog Summary vendor 0 + sibling reference / Why Zero / Internal-vs-External Boundary / Required-Artifact Compliance Map N/A / Evolution Paths / References). **사전 검증 PASS**: 3 service 의 `find apps/<svc>/src/main/java -path "*/adapter/out*" -type d` 결과 모두 vendor adapter 0 (master = idempotency/messaging/persistence 만, admin/gateway = adapter/out dir 자체 부재). 5 file / +307 / -0 (3 신규 spec + task file 신규 + INDEX entry 1 줄). **검증**: line range per file ✓ (98/105/103, target 80-110), marker regression 0 (14 spec file across 3 service 모두 0), reference resolution per file: master/admin 10/10 PASS + gateway 8/8 PASS (gateway 는 domain-model/idempotency 부재 fact 라 cross-link 안 함, 정확). production code 변경 0, application.yml / Flyway / Java 변경 0 (markdown only). CI = path-filter (TASK-MONO-074/075) → markdown-only wms-spec → ~1 PASS + 15 SKIP 예상. branch = `task/be-159-portfolio-external-integrations-zero-state-batch` (main 분기, BE-158 머지 commit `87e3a058` 직후). lifecycle = ready → done (single-PR closure 20번째 entry, BE-141/142/FAN-BE-006/MONO-084/BE-281/BE-145~158 precedent + **첫 batch authoring entry** — `feedback_pr_bundling.md` 메모리 적용). D4 OVERRIDE: ADR-MONO-003a § D1.1 (project-internal spec backfill batch). **portfolio-wide external-integrations.md gap 완전 종결** — 잔존 gap = database-design.md 5 service (master/inbound/outbound/admin/notification; gateway N/A schemaless), BE-157 답습 batch 또는 single 별 task 후보. 분석=Opus 4.7 / 구현=Opus 4.7 / 리뷰=Opus 4.7 (inline self-review, BE-156 zero-state pattern 답습 batch + service-specific 차별점 정밀 보존 + OAuth2 AS infrastructure 분류 inventory § Internal-vs-External Boundary 답습 + 사전 vendor adapter audit).

- `TASK-BE-158-notification-service-external-integrations.md` — single-PR closure (2026-05-14, same-day same-session BE-157 직속 후속). **APPROVED (inline self-review)** — BE-156/157 메모리 surfaced "portfolio-wide 4 service external-integrations.md gap" 중 **#1 closure** (notification-service, **gap 중 유일하게 real external vendor surface 보유**). `notification-service/external-integrations.md` 신규 533 line, 9 section: 헤더+intent / Catalog Summary (Slack + Kafka + Postgres + Secret Manager 4-row) / **§ 1 Slack Incoming Webhooks (marquee, 13 sub-section)** / Kafka cluster / PostgreSQL / Secret Manager (env-var v1 primary) / Aggregated Resilience Policy / Observability / Test Suite I10 / Per-Vendor Runbook Pointers / Not In v1 / References. sibling answering pattern = `outbound-service/external-integrations.md` (767 line TMS marquee) 의 mid-scale 변형 + `inventory-service/external-integrations.md` (97 line zero-state, BE-156) 의 inverse — single vendor marquee + 3 infrastructure. **byte-identical 검증 11/11 PASS** (adapter `SlackChannelAdapter.java` CONNECT/READ_TIMEOUT 3s/5s + `application.yml` R4j slack circuit TIME_BASED/10/5/50%/10s + retry 3/500ms/exp×2.0/ignoreExceptions + outer delivery max-attempts 5 + backoff [1,5,30,120,600]). 16/16 reference path resolution PASS (`ls` 으로 sibling 3 spec + adapter source + application.yml + notification-events/subscriptions + 5 platform/rules). `grep -rn "(Open Item" projects/wms-platform/specs/services/notification-service/` = 0. production code 변경 0, application.yml 변경 0 (markdown only). CI = path-filter (TASK-MONO-074/075) → markdown-only wms-spec → ~1 PASS + 15 SKIP 예상. **AC line range 250-350 초과 (533, BE-157 동일 패턴: byte-identical preservation + full I1-I10 mapping + 4xx HTTP status table + Aggregated Resilience Policy table 등 spec depth 가 주 요인, sibling outbound 767 line 의 70% scope)** — portfolio-quality 보존 우선. branch = `task/be-158-notification-service-external-integrations` (main 분기, BE-157 머지 commit `4d929f27` 직후). lifecycle = ready → done (single-PR closure 19번째 entry, BE-141/142/FAN-BE-006/MONO-084/BE-281/BE-145~157 precedent). D4 OVERRIDE: ADR-MONO-003a § D1.1 (project-internal spec backfill). **portfolio-wide gap 4 service 중 #1 closure** — **잔존 3 service** (master/admin/gateway external-integrations.md, 모두 zero-state 가능 — BE-156 답습 batch 후보). 분석=Opus 4.7 / 구현=Opus 4.7 / 리뷰=Opus 4.7 (inline self-review, byte-identical adapter source 정합 + integration-heavy I1-I10 sibling pattern 답습).

- `TASK-BE-157-inventory-service-database-design.md` — single-PR closure (2026-05-14, same-day same-session sibling of BE-156). **APPROVED (inline self-review)** — BE-152 audit § "candidate inventory" **#2 closure (마지막)** + BE-152 surfaced 4 candidate 전체 종결 (#1 BE-156 external-integrations / #2 BE-157 database-design / #3 MONO-087 LOT_INACTIVE / #10 BE-153 gateway route + BE-156 status sync). `inventory-service/database-design.md` 신규 retrospective spec authoring (650 line, 10 section: Schema Overview ASCII / Inventory aggregate / InventoryMovement append-only / InventoryOutbox T3 / EventDedupe T8 / Reservation aggregate + reservation_line / StockAdjustment + StockTransfer / Master Read Model 3 snapshot / Append-Only Enforcement Strategy V5 trigger + REVOKE / Indexing Strategy Summary 38-row catalog / Migration History) — Flyway V1-V5 (398 line, 11 table + 2 trigger + 1 function) byte-identical reflection. **portfolio-wide 첫 database-design.md** (sibling 답습 패턴 없음, master/inbound/outbound/admin/notification/gateway 모두 미존재 — portfolio audit candidate surfaced). 동시 closure: **domain-model.md § Open Items** database-design.md row ❌ → ✅ + link 활성화 + BE-157 attribution + "future V6+ migrations must update this file in the same commit" retrospective contract 명시. 3 file / +662 / -7 (신규 1 + domain-model.md 6 line + task file 신규 + INDEX.md 1 entry). **검증**: 13/13 reference path resolution PASS (`ls` 으로 V1-V5 + 4 sibling spec + rules/domains/wms.md + rules/traits/transactional.md + platform/architecture.md). section numbering 1-10 연속 (mid-write 7→9 skip 발견 → renumber). `grep -rn "(Open Item" projects/wms-platform/specs/services/inventory-service/` = 0 (regression 0). production code 변경 0, Flyway SQL 변경 0 (markdown only). CI = path-filter (TASK-MONO-074/075) → markdown-only wms-spec → ~1 PASS + 15 SKIP 예상. **AC line range 280-380 초과 (650, sibling outbound external-integrations.md 767 line 답습 범위, byte-identical SQL preservation + 38-row indexing summary catalog 가 주 요인)** — portfolio-quality 보존 우선. branch = `task/be-157-inventory-service-database-design` (main 분기, BE-156 머지 commit `8d52e16d` 직후 분기). lifecycle = ready → done (single-PR closure 18번째 entry, BE-141/142/FAN-BE-006/MONO-084/BE-281/BE-145~156 precedent). D4 OVERRIDE: ADR-MONO-003a § D1.1 (project-internal spec backfill). 분석=Opus 4.7 / 구현=Opus 4.7 / 리뷰=Opus 4.7 (inline self-review, byte-identical SQL transcribe + first-of-portfolio pattern + cross-reference dense scope).

- `TASK-BE-156-inventory-service-external-integrations-zero-state.md` — single-PR closure (2026-05-14, same-day BE-155 직속 후속). **APPROVED (inline self-review)** — BE-152 audit § "candidate inventory" #1 closure (잔여 #2 = database-design.md retrospective). `inventory-service` 의 `integration-heavy` Required Artifact 1 mandatory file = `external-integrations.md` 신규 zero-state 작성 (97 line, 7 section: 헤더+intent / Catalog Summary (vendor 0 + sibling reference) / Why Zero Direct Integrations / Internal-vs-External Boundary / Required-Artifact Compliance Map / Evolution Paths / References). sibling 답습 pattern = `outbound-service/external-integrations.md` (~767 line TMS marquee) + `inbound-service/external-integrations.md` (~423 line ERP webhook) 의 reverse — inventory 는 "no direct external vendor surface" 명시 + Kafka/Postgres/Redis 가 infrastructure 분류 (not vendor) 정확화 + I9 bulkhead N/A 명시. 동시 closure 2종: (1) **architecture.md § Open Items #8 ❌ → ✅** + link 활성화 + BE-156 attribution; (2) **§ Open Items #9 ⚠️ → ✅** 정정 — "5/6 registered" → "6/6 모두 등록", MONO-087 (commit `58a344d3`) closure attribution; (3) **§ Open Items #10 ❌ → ✅** 정정 — BE-153 (commit `7e14a218`, PR #496) closure attribution + portfolio-wide gap historical context 보존. domain-model.md § Open Items 도 LOT_INACTIVE ⚠️ → ✅ sync. 4 file / +99 / -23 (신규 1 + architecture.md 17 line block + domain-model.md 1 row + task file). **검증**: 9 sibling reference 모두 `ls` 실재 PASS (inbound/outbound external-integrations.md + idempotency.md + inventory-events.md + integration-heavy.md + error-handling.md + observability.md + architecture.md + domain-model.md). `grep -rn "(Open Item" projects/wms-platform/specs/services/inventory-service/` = 0 matches (regression 0). production code 변경 0 (markdown only). CI = path-filter (TASK-MONO-074/075) → markdown-only wms-spec → ~1 PASS + 15 SKIP 예상. branch = `task/be-156-inventory-service-external-integrations-zero-state` (main 분기, BE-155 머지 commit `6c53216a` 직후 분기). lifecycle = ready → done (single-PR closure 17번째 entry, BE-141/142/FAN-BE-006/MONO-084/BE-281/BE-145~155 precedent). D4 OVERRIDE: ADR-MONO-003a § D1.1 (project-internal spec backfill). **BE-152 surfaced follow-up #1 closure + #9/#10 stale sync** — 잔여 BE-152 surfaced candidate = #2 (database-design.md retrospective, ~200-400 line, 별 task). 분석=Opus 4.7 / 구현=Opus 4.7 / 리뷰=Opus 4.7 (inline self-review, sibling pattern adaptation + factual verification scope).

- `TASK-BE-155-service-port-spec-align.md` — single-PR closure (2026-05-14, same-day same-session sibling of BE-153/154). **APPROVED (inline self-review)** — TASK-BE-153 commit `7e14a218` surfaced follow-up 2/2 closure. Audit 시 **notification 도 drift 추가 surfacing** (prometheus.yml 8085 vs code 8086) — scope 확장 → 3 service simultaneous spec align (단독 변경 시 collision: inventory 8082↔inbound 8082 / admin 8086↔notification 8086). **Spec source-of-truth 2개 align** (prometheus.yml + gateway architecture.md § Routes): inventory=8083, notification=8085, admin=8086. **Drift fix code side** (spec edit 0): (1) `inventory-service/application.yml` SERVER_PORT 8082 → 8083, Dockerfile EXPOSE/HEALTHCHECK 동기; (2) `notification-service/application.yml` 8086 → 8085, Dockerfile 동기; (3) `admin-service/application.yml` 8087 → 8086, Dockerfile 동기; (4) `README.md` inventory `:8082` → `:8083` (2 occurrences: architecture diagram L70 + bash quick-start L338+L368). 7 file / +7 / -7. **검증**: 3 service compile PASS separately (`gradlew :projects:wms-platform:apps:<svc>:compileJava` BUILD SUCCESSFUL 21s/15s/49s — daemon crash 시 동시 compile = JVM 메모리 부족, 분리 실행). test 측 port reference 0 사전 audit ✓ (`grep -rn "808[0-9]" projects/wms-platform/apps/<svc>/src/test/` = 0 matches in all 3). production code 변경 0 (yml default + Dockerfile + markdown only). **post-fix port matrix**: 8080 gateway / 8081 master / 8082 inbound / 8083 inventory / 8084 outbound / 8085 notification / 8086 admin (7 service host bootRun 동시 기동 collision-free). CI = path-filter (TASK-MONO-074/075) → 3 service trigger. branch = `task/be-155-service-port-spec-align` (main 분기, BE-153/154 와 독립). lifecycle = ready → done (single-PR closure 16번째 entry, BE-141/142/FAN-BE-006/MONO-084/BE-281/BE-145~154 precedent). D4 OVERRIDE: ADR-MONO-003a § D1.1 (project-internal infrastructure spec drift fix). **BE-153 surfaced follow-up 2/2 closure** — BE-153 + BE-154 + BE-155 의 BE-153-driven 후속 시리즈 완전 종결. 분석=Opus 4.7 / 구현=Opus 4.7 / 리뷰=Opus 4.7 (inline self-review, factual verification scope).

- `TASK-BE-154-gateway-service-type-composition-and-paragraph-fix.md` — single-PR closure (2026-05-14, same-day same-session sibling of BE-153). **APPROVED (inline self-review)** — TASK-BE-153 impl commit `7e14a218` 직속 follow-up, HARDSTOP-10 hook block 해소 + closing paragraph stale 정정. **3 edit** in `specs/services/gateway-service/architecture.md`: (1) line ending CRLF → LF normalize (269 lines, PowerShell `[System.IO.File]::WriteAllText` BE-150 절차 답습); (2) `### Service Type Composition` sub-section 신규 (6 line, ## Identity table 직후 / ## Role 직전, inventory + inbound 패턴 답습, gateway = single rest-api 적합 short body); (3) ## Identity L14 cell 정정 `(edge gateway role)` → `(edge gateway role; see Service Type Composition below)` (inbound L14 답습); (4) ## Routes (v1) closing paragraph 정정 — BE-153 머지 후 stale 된 "Routes for outbound and notification-service arrive in subsequent TASK-INT-* tickets." 를 outbound (BE-153 완료) + notification (event-consumer no REST surface, file link) 정확 분류. 3 Edit 모두 HARDSTOP-10 hook PASS (Composition sub-section 등록 후 hook standard header pattern detect). production code = 0 (markdown only). CI = markdown-only path-filter (TASK-MONO-074/075) → ~1 PASS + 15 SKIP 예상. branch = `task/be-154-gateway-service-type-composition-and-paragraph-fix` (BE-153 와 독립, main 분기). lifecycle = ready → in-progress → done (single-PR closure 15번째 entry, BE-141/142/FAN-BE-006/MONO-084/BE-281/BE-145/146/147/148/149/150/151/152/153 precedent). D4 OVERRIDE: ADR-MONO-003a § D1.1 (project-internal spec drift fix). **BE-153 surfaced follow-up 1/2 closure** — 잔여: inventory-service + admin-service `application.yml` SERVER_PORT default 정합 (TASK-BE-155 후보). 분석=Opus 4.7 / 구현=Opus 4.7 / 리뷰=Opus 4.7 (inline self-review, factual verification scope).

- `TASK-BE-153-gateway-service-routes-backfill.md` — impl commit `7e14a218` + this review→done chore (2026-05-14). **APPROVED (inline self-review)** — factual verification scope (4 routes parse + KeyResolver bean lookup + spec port alignment + notification 제외 근거 + rate-limit tier values byte-identical) + production code 변경 0 (yml + markdown only). `gateway-service/application.yml` + `application-standalone.yml` 에 **4 service routes** 등록 (inbound/inventory/outbound/admin), master route 패턴 답습. **notification-service = event-consumer → route 제외** (PROJECT.md Service Map L44 + `specs/contracts/http/notification-service-api.md` 미존재). rate-limit: inbound/inventory/outbound = standard 100/200 (master 답습), admin = **60/120** (`specs/services/gateway-service/architecture.md § Routes` 답습 — bucket은 `clientIpKeyResolver` 의 `(clientIp, routeId)` 키로 자연 isolated). 4개 URI env var 추가 (default = spec 답습 port 8082/8083/8084/8086, CLAUDE.md § Source of Truth Priority layer 7 services > 14 existing code). spec align: § Routes table outbound 행 backfill ✓ — closing paragraph 정정은 **HARDSTOP-10 hook block** (gateway-service architecture.md의 line 14 Service Type cell 이 `### Service Type Composition` standard header pattern 미준수 — BE-150 inbound 사례 동일). 검증: `gradlew :projects:wms-platform:apps:gateway-service:compileJava :test` BUILD SUCCESSFUL 26s (application context startup = 4 routes yml parse + KeyResolver bean wiring valid). e2e (Docker required) = CI Linux Integration job 위임 (Rancher blocker memory). 4 file / +72 / -3. lifecycle = ready → in-progress → review → done. BE-152 audit § "#10 gateway route" portfolio-wide gap finding **closure** — wms-only gap 청산 (ecommerce/fan/gap 3 project 이미 정상). **2 follow-up candidates surfaced**: (1) TASK-BE-154 gateway-service architecture.md `### Service Type Composition` standard header backfill + closing paragraph 정정 (HARDSTOP-10 해소); (2) inventory-service `application.yml` SERVER_PORT default `8082` (spec 8083 + inbound 8082 collision) + admin-service SERVER_PORT default `8087` (spec 8086) 정합 task. D4 OVERRIDE: ADR-MONO-003a § D1.1 (project-internal infrastructure). same-day single-PR closure 14번째 entry (BE-141/142/FAN-BE-006/MONO-084/BE-281/BE-145/146/147/148/149/150/151/152 precedent). 분석=Opus 4.7 / 구현=Opus 4.7 / 리뷰=Opus 4.7 (inline self-review, factual verification scope).

- `TASK-BE-152-inventory-service-open-items-audit-and-list-correction.md` — spec commit `3930a523` + this review→done chore (2026-05-14). **APPROVED (inline self-review)** — list correction audit 는 byte-identical / dense claim 없고 factual verification (file 존재 / error code 등록) 만으로 risk 작아 self-review 충분. inventory-service `architecture.md § Open Items` (L577-602) + `domain-model.md § Open Items` (L675-689) audit + list 정정. BE-151 후 stale 상태였던 list 본문을 retrospective backfill audit framing 으로 재구성 (section 머리말 "Before First Implementation Task" → "Retrospective Backfill Audit" + BE-152 attribution). 각 항목 status icon 부여 — architecture.md: ✅×7 (#1-7 done, BE-030 era for #1-5 + BE-151 for #6-7) + ❌ #8 (external-integrations.md) + ⚠️ #9 (5/6 error codes registered, LOT_INACTIVE 미등록) + ❌ #10 (gateway route portfolio-wide gap fact 인용); domain-model.md: ❌ database-design.md (Flyway migration de-facto schema) + ✅×3 + ⚠️ error codes + TRANSFER_CROSS_WAREHOUSE 의도된 VALIDATION_ERROR fold 보존 명시. **4 outstanding items 는 별 task 후보로 list 본문 inline 명시만 (file authoring 0)**: TASK-BE-N external-integrations.md zero-state + TASK-BE-N+1 database-design.md retrospective + TASK-MONO-N LOT_INACTIVE 등록 + TASK-MONO-N+1 portfolio-wide gateway routes audit. 검증 `grep -rn "(Open Item" projects/wms-platform/specs/` = 0 (BE-151 outcome 유지). cross-ref 검증 — 신규 활성화 link 모두 dead-reference 0. HARDSTOP-03 PASS. production code 변경 0 — list correction only. lifecycle = ready → review → done (same-day single-PR closure 13번째 entry — BE-141/142/FAN-BE-006/MONO-084/BE-281/BE-145/146/147/148/149/150/151 precedent). 분석=Opus 4.7 / 구현=Opus 4.7 / 리뷰=Opus 4.7 (inline self-review, factual verification scope).

- `TASK-BE-151-inventory-service-reservation-saga-and-state-machine-spec.md` — direct main push commit `c917a2da` (2 신규 spec file + 4 modified) + follow-up dead-link depth fix commit + this review→done chore (2026-05-14). **APPROVED with one warning closed** — code-reviewer (sonnet) independent dispatch verdict: spec content drift 0 / scope discipline 0 / WMS `(Open Item)` marker count 0 / 7 dead reference (depth 5-up vs required 6-up) for `docs/adr/` + `rules/traits/` + `rules/domains/` in § References sections of both new files. Fix: `replace_all` 의 substring trap (5-up 이 6-up 의 substring) 으로 L176 의 § 3 이미 정상 6-up link 가 7-up 으로 over-upgrade — 별 fix Edit 으로 복원. 최종 검증 `ls` 통해 docs/adr/ADR-MONO-005.md + rules/traits/transactional.md + rules/domains/wms.md 3 target 모두 sagas/ + state-machines/ 두 디렉토리에서 6-up resolve PASS. **2 신규 spec file**: `sagas/reservation-saga.md` (~250 line, 9 section: Saga Overview / 3 Operations per-event Reserve+Release 3-trigger+Confirm / TTL Expiry ADR-MONO-005 Category D reference / Compensation single-rule / Concurrency-Idempotency 6-layer / Failure Modes 8-row / Observability 6-counter / References) + `state-machines/reservation-status.md` (~190 line, 6 section: States 3-row / Transitions ASCII+mermaid+per-transition rules table / Forbidden Transitions 3-row / Invariants 7-rule / Forbidden Patterns 6-rule / References). **3 marker 제거**: `architecture.md:512-513` + `:554-555` + `domain-model.md:621-622` markdown link 활성화 + § Open Items #6 / #7 + domain-model § Open Items 2 row 모두 ✅ TASK-BE-151 attribution. byte-identical 검증 (architecture.md L494-510 / L517-525 / L529-547 + domain-model.md L251-353 inline declaration vs 신규 spec 의 event 이름 + saga state + TTL 파라미터 + 3-value released_reason enum + RESERVATION_QUANTITY_MISMATCH 모두 정합). **WMS specs `(Open Item)` marker count = 0 종결** (`grep -rn "(Open Item" projects/wms-platform/specs/` exit 1). cleanup 시리즈 (MONO-085/086 + BE-147/148/149/150 + BE-151) **자연 완전 종결**. lifecycle = ready → review → done (same-day single-PR closure 12번째 entry, BE-141/142/FAN-BE-006/MONO-084/BE-281/BE-145/146/147/148/149/150 precedent). 분석=Opus 4.7 / 구현=Opus 4.7 / 리뷰=Sonnet 4.6 (code-reviewer agent independent dispatch).

- `TASK-BE-150-inbound-architecture-hook-fix-and-marker-cleanup.md` — PR #494 머지 (2026-05-14, squash commit `d4bcbfa0`). `inbound-service/architecture.md` 의 Service Type cell formatting fix + hook navigation 통한 5 stale `(Open Item)` marker cleanup (BE-149 audit 단계에서 HARDSTOP-10 hook trigger 로 cleanup 불가했던 file). Root cause: hook 의 `$existing.Contains($oldString)` 가 file CRLF vs Edit tool 의 oldString LF mismatch 로 false → simContent = unchanged existing → hook 가 existing file 에 `### Service Type` header / `**Service Type**:` bold 부재 detect → fail. Fix 절차: (1) PowerShell `[System.IO.File]::WriteAllText` 으로 file line ending CRLF → LF normalize. (2) `### Service Type Composition` sub-section 신규 (10 line, `rest-api` primary + `event-consumer` consumer path). (3) line 14 cell 정정 — `(primary). Webhook receiver also rides on the REST surface` → `(primary; see Service Type Composition below)` (inventory-service 패턴 답습). (4) 5 marker cleanup — 188/268/307/340/379 BE-149 Pattern 답습. post-merge verify — wms specs `(Open Item)` 잔존 = **3** (모두 inventory-service intentional placeholder, referenced file 미존재). **Combined outcome (BE-147 + BE-148 + BE-149 + BE-150)**: wms specs `(Open Item)` 잔존 ~74 → **3** (intentional only). cleanup 시리즈 자연 종료. branch `task/be-150-inbound-architecture-hook-fix-and-cleanup` 삭제. base = main (e06ed540, no stack). 분석=Opus 4.7 / 구현=Opus 4.7.

- `TASK-BE-149-services-open-item-marker-batch-cleanup.md` — PR #492 머지 (2026-05-14, squash commit `f2d9c0b4`). wms `specs/services/` 디렉토리 의 stale `(Open Item)` marker batch cleanup Phase 2 (per-marker audit). 54 marker × 18 file audit 결과 = 46 stale cleanup / 3 intentional placeholder 보존 (inventory-service `sagas/reservation-saga.md` + `state-machines/reservation-status.md` 미존재 — spec gap honest signal) / 5 hook-blocked 분리 (`inbound-service/architecture.md` 의 line 14 Service Type cell formatting 때문에 HARDSTOP-10 hook trigger, 별 task TASK-BE-150 후보). 17 file × 46 marker 정리 (admin 2 + inbound 9 + inventory 4 + notification 6 + outbound 25). 6 Pattern 별 differentiated edit policy 적용 — trailing `(Open Item)` 단순 제거 / `Full X: ... (Open Items).` trailing period 유지 / `(Open Items, per ...)` → `(per ...)` (rule reference 보존) / `§ "Section" (Open Item).` → `§ "Section".` (3 section 사전 실재 검증: erp-order-webhook.md § Failure-mode Test Cases + § Security Notes, outbound-service-api.md § Idempotency Semantics) / `(Open Items):` → `:` (colon 유지) / `(Open Item; cross-link)` → `(cross-link)` (information-preserving). post-merge verify — `grep -rn "(Open Item" projects/wms-platform/specs/services/` → 정확 8 (3 intentional + 5 hook-blocked). semantic 변경 0. BE-148 (PR #491) 직속 carry-out — contracts/ 20 marker + services/ 46 marker = wms specs `(Open Item)` marker 잔존이 stale → 8 (intentional + hook-blocked) 로 축소. **머지 conflict 1회**: task/be-149 가 main 의 BE-147/BE-148 squash merge 후 INDEX.md + external-integrations.md 양쪽 conflict — stack 순서 보존 + 인접 marker resolution 으로 fix, merge commit `eeb0ee4f` push 후 머지 성공. branch `task/be-149-services-open-item-cleanup` 삭제. 분석=Opus 4.7 / 구현=Opus 4.7.

- `TASK-BE-148-contracts-open-item-marker-batch-cleanup.md` — PR #491 머지 (2026-05-14, squash commit `6011c20d`). wms `specs/contracts/` 디렉토리 의 stale `(Open Item)` marker batch cleanup Phase 1 (events + http + webhooks). 20 marker × 8 file 정리 — `inbound-events.md` (2) + `outbound-events.md` (2) + `inventory-events.md` (2 parenthetical) + `inbound-service-api.md` (1) + `outbound-service-api.md` (4) + `erp-asn-webhook.md` (4 = intro 2 + References 2) + `erp-order-webhook.md` (4 = intro 2 + References 2) + `notification-subscriptions.md` (1). 모든 referenced spec 사전 실재 검증 완료 — 각 marker stale 확정 (intentional placeholder 0). semantic 변경 0 (marker 제거만, backtick reference 형태 / link 활성화 상태 그대로 유지). BE-147 §2.1 case (PR #490) 의 단일 case 확장. **머지 conflict 1회**: task/be-148 가 main 의 BE-147 squash merge 후 INDEX.md `## review` section 의 BE-147/BE-148 entry stack 순서 conflict — stack 순서 보존 resolution 으로 fix, merge commit `91018038` push 후 머지 성공. branch `task/be-148-contracts-open-item-cleanup` 삭제. 분석=Opus 4.7 / 구현=Opus 4.7.

- `TASK-BE-147-tms-shipment-api-vendor-wire-spec.md` — PR #490 머지 (2026-05-14, squash commit `a16eb683`). outbound-service TMS vendor wire-level HTTP contract authoring (`specs/contracts/http/tms-shipment-api.md` 신규 ~350 line + `external-integrations.md` §2.1 / § References 의 "(Open Item — vendor-controlled)" marker 제거). production code 변경 0 — BE-049 (PR #315) 의 머지된 `TmsClientAdapter` / `TmsShipmentRequest` / `TmsShipmentResponse` / `TmsShipmentMapper` wire 를 retrospective 로 spec 화. 12 section 표준 (Endpoint / Authentication / Request Headers / Request Body / Response 2xx + status enum classification / Response 4xx table (409 vendor-honoured Idempotency-Key success-equivalent) / Response 5xx + transport / Idempotency Semantics (vendor-side + `tms_request_dedupe` V13 client fallback) / Vendor Schema Versioning / Out of Scope (v1) / References). cross-ref 검증 — 6 production code path + 5 sibling spec link 의 dead-reference 0 (sibling spec 의 § Vendor Idempotency / § notify failure path 명칭 부재 finding 발견 → spec References 정정 후 closure). `project_mono_085_dead_reference_batch.md` 메모리의 "남은 backlog" 단일 entry (wms tms-shipment-api.md spec authoring) 해소. branch `task/be-147-tms-shipment-api-spec` 삭제. 분석=Opus 4.7 / 구현=Opus 4.7.

- `TASK-BE-146-wms-7-service-overview-skeleton.md` — spec PR #478 (squash `2ab2e502`) + impl PR #479 (squash `1dc95822`) + this chore. **post-/refactor-spec portfolio-wide structural finding (2026-05-14) 직접 해소** — WMS 7/7 service overview.md MISSING 상태 종결. 7 신규 file (~70-85 line each, +524 / -14): `master-service` (Hexagonal, master data anchor, W3+W6+T3) / `inventory-service` (Hexagonal dual REST+event, W1+W2+W4+T3+T8) / `inbound-service` (Hexagonal REST+webhook, T3+T4+I6) / `outbound-service` (Hexagonal saga orchestrator, T3+T4+T8 + ADR-MONO-005 § D6) / `notification-service` (Hexagonal pure event-consumer, T8 + idempotency.md cross-link to BE-145) / `admin-service` (Layered exception CQRS read-side, projection idempotency) / `gateway-service` (Layered edge gateway, JWT + fail-open rate limit). 7-section template 답습 (TASK-FAN-BE-006 / TASK-BE-141/142 sibling pattern): Service identity table + Responsibilities + Public surface + Key invariants + Owned Data + Published Interfaces + Dependent Systems + Out of scope (v1). 본 PR 머지로 portfolio 5 운영 프로젝트 중 4/5 overview.md 일관성 완성 (SCM 3 service 후속 별 task 후보). CI markdown-only path-filter 15 SKIP + 1 PASS. production code = 0. lifecycle = ready → review 직접 (in-progress 우회, BE-141/142 / FAN-BE-006 / MONO-084 same-day single-PR closure precedent — 본 task 가 8번째 entry). 분석=Opus 4.7 / 구현=Opus 4.7. 2026-05-14.

- `TASK-BE-145-notification-service-idempotency-spec-and-dlt-replay-runbook.md` — spec PR #469 (squash `5792fbc7`) + impl PR #470 (squash `ee376e6e`) 머지 (2026-05-14). **`/refactor-spec all --dry-run` (2026-05-13~14) WMS audit critical #1+2 finding closure** — notification-service architecture.md § Open Items #4 (`idempotency.md`) + #5 (`runbooks/dlt-replay.md`) 가 미존재 file 가리키던 backfill (TASK-BE-043 bootstrap PR #276 머지 2026-05-08 후 누락). **`idempotency.md`** (~200 line, 10 section) = consumer-pattern 명세 — Inbound `notification_event_dedupe(event_id PK, source_topic, processed_at, outcome)` + Outbound `delivery_idempotency_key = sha256(eventId+channelId+recipient)` + retry budget (max_attempts=5 + exponential backoff 30s/60s/2m/5m/10m + DELIVERY_RETRY_EXHAUSTED 종결) + Concurrency (JPA @Version + SKIP LOCKED + 60s routing rule cache) + 7 failure modes + 5 Micrometer metrics + 8 testing scenarios + ADR-MONO-005 Category C reference. **`runbooks/dlt-replay.md`** (~150 line, 8 section, `runbooks/` directory 신설) = operator playbook for 2 retry layers (Kafka consumer retry vs application delivery retry) — Identify (DLT inspection + SQL) / Diagnostic classification (5 patterns) / Step 3a-c (DLT drain via kafka-console-consumer + replay script / Manual delivery retry via UPDATE notification_delivery / DLT message removal for poison pill) / Verification (Grafana metric watch) / Rollback safety / Escalation. architecture.md § Open Items #4/#5 → ✅ marker + link to 신규 file. 5 file / +514 / -14 (2 신규 spec + architecture.md 정정 + task spec lifecycle + INDEX). production code = 0 (BE-043 merged production 의 spec backfill only). **Impl PR CI = 15 SKIP + 1 changes PASS** (markdown-only path-filter, TASK-MONO-075 자연 검증). lifecycle = ready → review 직접 (in-progress 우회, spec authoring single-PR closure 패턴 4번째 적용 — TASK-BE-281/MONO-084/FAN-BE-006 precedent). Sibling 답습 패턴 = TASK-MONO-083 / TASK-BE-280/281 / TASK-SCM-BE-011 / TASK-MONO-084 / TASK-FAN-BE-006 — 모두 same-day single-PR closure (본 task 가 7번째 entry). 분석=Opus 4.7 / 구현=Opus 4.7.

- `TASK-BE-144-notification-events-eventversion-int-string-drift-fix.md` — impl PR #451 머지 (2026-05-13, squash commit `e72bb691`). `/refactor-spec all --dry-run` (2026-05-13) WMS audit Top 1 critical finding closure (cross-service consumer wire-format incompatibility). 5 sibling event contracts 가 `"eventVersion": 1` integer 사용, notification-events.md 만 `"v1"` string + `OutboxWriterAdapter.java:35` `EVENT_VERSION = "v1"` String 도 동일 drift. **Fix**: spec 2 site (envelope JSON + schema versioning prose) → int 1, `OutboxWriterAdapter.java` `EVENT_VERSION` int 도입 + DB row storage 는 `EVENT_VERSION_DB = String.valueOf(EVENT_VERSION)` 로 string repr 유지 (DB column VARCHAR(10) migration 회피, minimal scope). production code 영역: OutboxWriterAdapter.java 만 (JpaEntity + V1__init.sql 무변경). 2 file / +11 / -5. **CI 15 PASS + 1 SKIP** (Observability 정상 skip — code change observability flag 무관). **Integration (master + notification) 1m16s PASS** — production code 변경 IT regression 0. DB column type ALTER (VARCHAR → INT) + shared envelope schema 추출 (6 contract byte-identical envelope) 은 follow-up 후보. 분석=Opus 4.7 / 구현=Opus 4.7.

- `TASK-BE-143-master-service-partner-aggregate-bootstrap.md` — PR #398 머지 (2026-05-12, squash commit 44b5736a). master-service Partner aggregate v1 producer-side bootstrap, 49 file / +3797 / -4. 6 layer 신규 (domain Partner+PartnerType + 4 events + 2 exception / application 2 use-case+port+service+4 cmd+query+result / adapter-in PartnerController 6 endpoint + 4 request DTO + response / adapter-out JpaPartnerRepository+entity+mapper+adapter / migration V7+V103 seed / outbox `EventEnvelopeSerializer` Partner 4 event 등록) + master-service `GlobalExceptionHandler` Partner exception 매핑 + `MasterOutboxPollingScheduler` 의 `master.partner.*` → `wms.master.partner.v1` 기존 매핑 활용. **field model = spec wins** (task line 66 의 5 필드 → spec § 5 의 7 필드 채택, CLAUDE.md layer 6 contracts > layer 10 tasks). **status = WarehouseStatus 재사용**, **AggregateVersionGuard** (BE-141 utility) 활용. Tests 신규 ~70 (domain 30 + service 15 + controller 15 + persistence Postgres+H2 10) + 4 event JSON schema + 1 HTTP response schema. **CI 15/15 SUCCESS** (Integration master-service+notification Testcontainers + E2E gateway-master live-pair 포함). Sibling regression 0. Spec drift 0. **Production risk closure** — 다운스트림 (inbound `findPartner` / outbound `validateCustomerPartner` / admin `MasterPartnerProjector`) 가 staging/prod 에서 영원히 empty 되는 잠재 위험 해소. Out of scope: Lot.supplier_partner_id hard FK = v2 / V99 dev seed = 자연 deprecated / MasterEntityStatus rename = 별도 평가 / MasterPartnerProjector no touch. D4 churn freeze 영향 0. **BE-142 audit Branch B (= 누락 확정) closure**. 분석=Opus 4.7 / 구현=Opus 4.7 (backend-engineer agent dispatch).

- `TASK-BE-141-master-service-aggregate-version-guard-extraction.md` — PR #381 머지 (2026-05-12, commit 737ab718). master-service 5 application service (`Warehouse`/`Zone`/`Location`/`Sku`/`Lot`) 의 `requireVersionMatch(UUID, long, long)` + `saveWithOptimisticLock(T)` private helper 5쌍 복붙 → `AggregateVersionGuard` 정적 utility 1개 통합. ~-40 LOC. dry-run 2026-05-11 Finding B1 cherry-pick (B2/B3/B4 는 DEFER). HTTP/event contract 변경 0. **CI 15/15 PASS** (cycle 2 — cycle 1 `LocationIntegrationTest.globalLocationCode_collision` flake via `shortSuffix()` 890-value collision, rerun green). BE-142 와 동일 PR 묶음. 분석=Opus 4.7 / 구현=Opus.

- `TASK-BE-142-master-service-partner-aggregate-spec-iam-audit.md` — PR #381 머지 (2026-05-12, commit 737ab718). master-service spec § Responsibility 6 aggregate 선언 중 **Partner production code 부재 audit** — Branch B (누락 확정) 결론, follow-up `TASK-BE-143` backlog filing (PR #382, commit f38575ff). architecture.md spec drift 의 production risk 명시 (ASN/Order 의 `findPartner` 검증 staging/prod 에서 영원히 empty). v2 Lot.supplier_partner_id hard FK 활성화는 별 task. spec-only audit, code 변경 0. 분석=Opus 4.7 / 구현=Opus.

- `TASK-BE-140-inventory-reservation-expiry-swept-counter.md` — PR # — 머지 (2026-05-11). inventory-service `ReservationExpiryJob` 에 `inventory.reservation.expiry.swept.total` Micrometer counter 추가. ADR-MONO-005 § D5 Category D minimal compliance — per-tick `released` 카운트 만큼 increment, `released > 0` 일 때만 호출 (Counter API no-op 회피). 생성자에 `MeterRegistry` 주입, counter 빌더 1개 등록. `ReservationExpiryJobTest` 4 method × counter assertion (no-expired / N-row release / mid-batch failure / disabled flag — disabled 케이스는 별도 SimpleMeterRegistry). ADR § 1.1 audit row 7 + § D6 inventory 행 + § 5 outstanding follow-up + History 갱신 + `architecture.md` Saga 행 cosmetic note 제거 + § Business metrics 라인 신설. **ADR-MONO-005 outstanding follow-ups 0** — saga timeout/escalation/dead-letter 정책의 substantive + cosmetic 모두 closure. 분석=Opus 4.7 / 구현=Sonnet 4.6 (single counter + test tweak + spec rows).

- `TASK-BE-053-inventory-service-master-event-coverage-audit.md` — PR #343 머지 (2026-05-11). inventory-service master event consumer 커버리지 audit — Warehouse/Zone/Partner 의 의도된 미참조 명시 (architecture.md `## Notes` 섹션 + dependencies.md `Consumes From` 표 컬럼). drift 가 아닌 v1 intentional gap 으로 closure. spec-only.

- `TASK-BE-052-master-lot-cross-service-consumer-audit.md` — PR #336 머지 (2026-05-11). `MasterLotConsumer` 가 outbound-service 에만 등록된 사실 audit — Scenario B (intentional, "Lot tracking is outbound-only in v1") 으로 결정. inbound + inventory architecture.md 양쪽 explicit note + dependencies.md Consumes From 표 갱신. /refactor-spec all (PR #326) Finding [WMS 4] closure.

- `TASK-BE-051-outbound-idempotency-key-e2e.md` — PR #324 머지 (2026-05-11). outbound-service 의 8 mutation endpoint 에 `Idempotency-Key` end-to-end 발효. `OutboundIdempotencyFilter` (`HIGHEST_PRECEDENCE + 20`, Spring Security 후 / DispatcherServlet 전, `POST/PATCH/PUT/DELETE /api/v1/outbound/**` 적용, `/webhooks/**` 제외) + `BodyHashUtil` (canonical-JSON SHA-256, whitespace 무관 / key 순서 무관) + `CachedBodyHttpServletRequestWrapper` (request body re-readable). Storage key namespace = `(method, sha256(URI), idempotencyKey)` — endpoint 또는 method 별 독립 dedupe. 같은 key + 같은 body → cached response byte-identical replay (no side-effect), 같은 key + 다른 body → **409 `DUPLICATE_REQUEST`**. TTL 24h (Redis `SET NX EX`) + 30s lock window 으로 동시 same-key race 차단. Standalone profile 폴백 = 기존 `InMemoryIdempotencyStore`. 메트릭 3개 (`outbound.idempotency.lookup.count{result=hit|miss|conflict}` / `lookup.duration.seconds` / `store.failure.total`). 신규 unit 26 (`OutboundIdempotencyFilterTest` 14 + `BodyHashUtilTest` 12) — 156 → **182 unit pass**. IT 7 (`IdempotencyFilterRedisIT`, Testcontainers Redis) — 6/7 PASS local, 1 flaky (Rancher Desktop docker-java `MalformedChunkCodingException`, 같은 path unit `bodyMismatch_returns409` PASS). CI Linux Integration job 검증 위임. spec 갱신: `idempotency.md` Open Item closure (impl banner / 키 길이 128→255 / namespace 명시 / Redis layout 정확화 / metric+error code 표). **D4 churn freeze 영향 0** — 초안 commit 의 `platform/error-handling.md` 1줄 alias 변경은 amend revert (impl 은 canonical `DUPLICATE_REQUEST` emit, alias 는 project-internal idempotency.md banner 에서 처리). 분석=Opus 4.7 / 구현=Opus 4.7 (backend-engineer agent dispatch).

- `TASK-BE-050-outbound-saga-sweeper.md` — PR #323 머지 (2026-05-11). outbound-service 의 1분 주기 saga 자기 회복 루프. `SagaSweeper` (@Scheduled fixedDelay 60_000ms, @ConditionalOnProperty `outbound.saga.sweeper.enabled`) 가 `REQUESTED` / `CANCELLATION_REQUESTED` / `SHIPPED` (>5분) 사가를 grace 초과 시 outbox 통해 재발신 (idempotent at inventory consumer via `outbound_event_dedupe`). `SagaRecoveryHandler` per-saga `REQUIRES_NEW` cross-bean (Spring AOP self-invocation 회피, `feedback_refactor_code_baseline_it.md` 패턴). DB clock (`now()`) 사용 — replica 간 clock skew 회피. saga `version` optimistic-lock 으로 race-safe. `OutboxReEmitterAdapter` 가 원본 outbox row clone + fresh UUIDv7 envelope `eventId` rewrite + `actorId=system:saga-sweeper` (consumer dedupe 30d TTL race 회피). `re_emit_count` cap 5회 → `STUCK_RECOVERY_FAILED` terminal state 신설 + `outbound.alert.saga.recovery.exhausted` v1 알림 이벤트 동일 TX 발신. Flyway V14: `re_emit_count INT NOT NULL DEFAULT 0` + sweeper-friendly partial index. 메트릭 4개 (`run.count` / `recovery.fired{from_state}` / `exhausted.count{from_state}` + alert event 자체). standalone profile 비활성. 신규 unit 18 (`OutboundSagaSweeperTransitionTest` 8 + `SagaSweeperTest` 10) + IT 5 (`SagaSweeperIT`, Testcontainers Postgres) — 156 → **174 unit pass** / 0 FAIL. local IT skip (Rancher Desktop blocker), CI Linux Integration job 검증. spec 갱신: `admin-events.md` § Alert Topics 신설 + §A1 v1 schema, `state-machines/saga-status.md` `STUCK_RECOVERY_FAILED` row. **3 spec deviations** (cap 5 vs sagas/outbound-saga.md §4.2 cap 10 / `STUCK_RECOVERY_FAILED` terminal 신설 vs metric-only "STUCK" 서술 / fresh envelope eventId vs `outbound-events.md § Saga Sweeper Re-emission` 의 "원본 eventId 유지") — task 명시 따름, 향후 spec consolidation follow-up. **D4 churn freeze 영향 0** (project-internal). 분석=Opus 4.7 / 구현=Opus 4.7 (backend-engineer agent dispatch).

- `TASK-BE-049-tms-real-adapter.md` — PR #315 머지 (2026-05-10). outbound-service `StubTmsClientAdapter` → 실제 `TmsClientAdapter` 교체 완료. Spring `RestClient` + Apache HttpClient 5 (dedicated `PoolingHttpClientConnectionManager` size 10) + Resilience4j (`@CircuitBreaker` 50%/20/60s + `@Retry` 3 attempts 1s/2s/4s + jitter `randomizedWaitFactor=0.2` + `@Bulkhead` SEMAPHORE 10) + `tms_request_dedupe` Flyway V13 + REQUIRES_NEW dedupe persistence adapter + `POST /api/v1/outbound/shipments/{id}:retry-tms-notify` (OUTBOUND_ADMIN, Idempotency-Key required) + `OutboundSaga.recoverFromNotifyFailed` 신규 도메인 전이 (SHIPPED_NOT_NOTIFIED → SHIPPED) + `ExternalServiceUnavailableException` (503 매핑) + `TmsRetryNotAllowedException` + 4 Micrometer 메트릭 (`outbound.tms.request.count{result}` / `request.duration.seconds` / `circuit.state{vendor=tms}` gauge / `retry.count{attempt}` + `dedupe.cache_hit.count` 보너스). standalone profile fallback `StubTmsClientAdapter` 를 `@Profile("standalone")` 로 분리 — 실제 어댑터는 `@Profile("!standalone")`. 신규 unit 18 (TmsClientAdapter 9 + RetryTmsNotificationService 5 + OutboundSaga recoverFromNotifyFailed 3 + ShipmentNotificationListenerTest 1 sig fix) — 138 → **156 unit pass**. WireMock IT 6 시나리오 (success / timeout 3-retry / 5xx 3-retry / 4xx no-retry / circuit open fast-fail / manual retry success) — Rancher Desktop blocker 로 로컬 IT skip, CI Linux runner Integration job 으로 검증 위임 (memory: project_testcontainers_docker_desktop_blocker.md). spec 갱신: `external-integrations.md` §2.3 (adapter layout), §2.4 (RestClient 채택 + sync 정당화), §2.5/§2.6 (adapter-internal `TmsTransientException`/`TmsPermanentException` exception 사용), §2.8 (SEMAPHORE bulkhead + ConnectionPool 정당화). 분석=Opus 4.7 / 구현=Opus 4.7 (backend-engineer agent dispatch).

- `TASK-BE-048-admin-polish-bundle.md` — PR #285 (spec) + PR #286 (impl) 머지 (2026-05-09). Self-verdict: **APPROVED**. 42 file / +1815 / -87 lines / **admin-service v1 minor deviations 7건 묶음 (#2-#8) 완전 청소**. (#2) 5 dashboard controller 의 date-range 필터 (Order/Shipment/ASN/Adjustment/Alert) — `requiredShipDateFrom/To`, `expectedArriveDateFrom/To`, `shippedAtFrom/To`, `occurredAtFrom/To`, `detectedAtFrom/To`. (#3) `X-Read-Model-Lag-Seconds` 응답 헤더 — `worstLagSeconds > 5` 시 emit, `ReadModelLagHeaderInterceptor` + MeterRegistry 연동. (#4) `OperationsController` per-topic Kafka lag — `KafkaAdmin` / `AdminClient.listConsumerGroupOffsets` 통합 (`KafkaLagProbe`). (#5) Throughput counter raw SQL upsert (`INSERT ... ON CONFLICT DO UPDATE WHERE last_event_at < EXCLUDED`) 로 cross-pod race window 방지. (#6) `runbooks/db-role-grants.md` 신규 + column-level GRANT (alert_log.acknowledged_*) ops 절차. (#7) `inbound-events.md` ↔ `admin-events.md` ↔ `outbound-events.md` split topic drift cross-reference (spec docs only). (#8) `admin.query.cache.hit.rate` 메트릭 v1 always 0 javadoc 명시 (v2 Redis cache 활성). admin-service v1 follow-up 카탈로그 완전 종결. D4 churn freeze 면제 (project-internal). 분석=Opus 4.7 / 구현=Sonnet 4.6 (7 minor wiring tweaks routine).
- `TASK-BE-047-admin-projection-kafka-testcontainers-it.md` — PR #284 (spec) + PR #287 (impl) 머지 (2026-05-09). Self-verdict: **APPROVED**. 8 file / +1303 lines / **admin-service v1 read-side Kafka 신뢰도 production-ready 확보 — BE-046 deviation #1 closure**. 4 IT class (`MasterProjectionKafkaIT` / `InboundProjectionKafkaIT` / `OutboundProjectionKafkaIT` / `InventoryProjectionKafkaIT`) + 2 공유 테스트 인프라 (`KafkaTestSupport` / `ProjectionKafkaIntegrationBase`) × Testcontainers (Postgres + Kafka) 로 18 source topic round-trip + dedupe-hit + LWW-stale + DLT routing (non-retryable: Json/UnknownEventType/IllegalArg) + replay scenario 검증. `${random.uuid}` consumer group-id + `ContainerTestUtils.waitForAssignment` + Awaitility. 메트릭 `projection.dropped.count{reason=duplicate|stale}` + `projection.error.count{topic}` 검증. Production code 변경 0 (test-only, `AdminEventDedupePersistenceAdapter` +24 line touch 제외). D4 churn freeze 면제 (`src/test/**` project-internal). 분석=Opus 4.7 / 구현=Opus 4.7 (backend-engineer agent dispatch).
- `TASK-BE-046-admin-service-readmodel-projection.md` — PR #278 (spec) + PR #282 (impl) 머지 (2026-05-08). Self-verdict: **APPROVED**. 89 file / +6374 lines / **CQRS read-side** — 4 *ProjectionService (Master 6 / Inbound 3 / Outbound 2 / Inventory 7 = 18 topic) + AdminEventDedupePort + last_event_at LWW guard + 3 mutation 패턴 (append-only PK=eventId / upsert with LWW / throughput counter) + 15 read-side table + Flyway V2 + 8 dashboard controller + Operations endpoint + alert acknowledge mutation + 5 Micrometer metric + DLT routing + JSONB JdbcTypeCode 회귀가드. **127 tests** (BE-045 누적 74 + BE-046 신규 53) PASS. bootJar SUCCESS. CI Integration job admin-service 14 IT (BE-045 5 + BE-046 9) PASS. Sibling regression 0 (master + inventory + inbound + outbound + notification + gateway). **Deviations 정직 보고 8건**: Kafka Testcontainers IT 미포함 (TASK-BE-047 후보) / dashboard date-range 필터 / X-Read-Model-Lag-Seconds 헤더 / OperationsController per-topic lag / Throughput counter JPA upsert / V2 DB role grants ops 책임 / inbound-events split topics drift / spec 변경 0. **admin-service v1 종결** — portfolio 7 service 운영 진입 (gateway+master+inbound+inventory+outbound+notification+admin). 분석=Opus 4.7 / 구현=Opus 4.7 (backend-engineer agent dispatch).
- `TASK-BE-045-admin-service-bootstrap.md` — PR #277 (spec) + PR #280 (impl + IT fix) 머지 (2026-05-08). Self-verdict: **APPROVED**. 124 file / +7021 lines / **Layered architecture (deliberate exception, declared override of sibling Hexagonal)**. 4 write-side aggregate (User/Role/UserRoleAssignment/Setting) + AdminDomainException sealed root + 11 subtype + 11 outbox event types + Spring Security 4-role 매트릭스 + force cascade revoke (SUPERADMIN-only, AOP-proxy 검증) + GAP OIDC JwtDecoder + tenant_id=wms claim + Idempotency-Key Redis 24h cache + JSONB JdbcTypeCode 회귀가드 + Flyway V1 (6 table) + V99 seed. **82 unit/slice/REST + 5 IT** (Testcontainers Postgres) PASS. bootJar SUCCESS. CI 15/15 SUCCESS. Sibling regression 0. **Deviations 정직 보고 4건**: Setting GLOBAL scope sentinel UUID (API invisible) / service-local outbox (libs TEXT-payload 비호환) / WebMvcTest authz 분리 검증 / Outbox Kafka end-to-end IT BE-046 와 함께. **Fix 1**: IT context standalone profile + Redis autoconfig exclude → CI Linux Integration job PASS 확보. 후속 = TASK-BE-046 admin-service-readmodel-projection. 분석=Opus 4.7 / 구현=Opus 4.7 (backend-engineer agent dispatch).
- `TASK-BE-043-notification-service-bootstrap.md` — PR #269 (spec) + PR #276 (impl) 머지 (2026-05-08). Self-verdict: **APPROVED**. 92 file / +5360 lines / 4 layer Hexagonal + 6 source-topic Kafka consumer + Slack adapter (Resilience4j circuit/retry + bounded WebClient) + outbox + Flyway V1 (4 table) + V2 (6 routing rule seed) + JSONB JdbcTypeCode 회귀가드. **73 tests** (62 unit/slice + 11 IT) — 로컬 unit 62/62 PASS, IT 는 Rancher env blocker 로 cold-start 스킵 (CI Linux 검증). bootJar SUCCESS (78MB). sibling regression 0 (master + inventory PASS). **Deviations 정직 보고**: (1) libs/java-messaging outbox subclass 대신 service-local outbox (JSONB schema 호환), (2) routing-rule TTL cache 미적용 (v1 6 row, follow-up), (3) CB open-state IT 미추가 (Resilience4j 라이브러리 단), (4) compose 컨테이너 entry 미추가 (host JVM bootRun sibling 패턴). Follow-up: runbooks/dlt-replay.md + idempotency.md (architecture.md Open Items #4-#5), routing TTL cache, CB open-state IT, v2 REST + email/push channel + per-user preference. 분석=Opus 4.7 / 구현=Opus 4.7 (backend-engineer agent dispatch).
- `TASK-BE-044-admin-service-spec-open-items-closure.md` — **retroactive filing** (옵션 a, 2026-05-09). PR #273 머지된 admin-service Open Items 8/8 close 작업의 traceability artifact. 4 신규 spec (admin-service-api.md / admin-events.md / idempotency.md / read-model-rebuild runbook) + 4 기존 보강 (`platform/error-handling.md` Admin 섹션, `rules/domains/wms.md` cross-ref, `gateway-service` `/api/v1/admin/**` 라우트 + admin tier rate limit 60 rpm, `PROJECT.md § Overrides` Layered 예외 mirror). admin-service 첫 BE impl task 의 ready/ 진입 게이팅 해소. **D4 churn freeze 영향**: shared 영역 추가형 카탈로그 등록 → 시계 1일 reset (재평가 ≥ 2026-06-08). 분석=Opus 4.7 / 구현=Opus 4.7 (spec 합성).
- `TASK-BE-042-gateway-account-type-validation.md` — WMS gateway-service JWT 클레임 검증 강화: `account_type: OPERATOR` 강제(CONSUMER 접근 차단), `aud: wms` 검증, `X-Account-Type` 헤더 주입 추가. 27 unit tests pass. Self-verdict: **APPROVED**.
- `TASK-BE-041-fix-TASK-BE-040.md` — Documentation-only follow-up: declare `Shipment.shipment_no` format `SHP-YYYYMMDD-NNNN` in `domain-model.md` §5. Review verdict 2026-04-29: **APPROVED**.
- `TASK-BE-040-fix-TASK-BE-038.md` — Follow-up fix for TASK-BE-038: PATCH seal now drives packing-completed (Order PACKING→PACKED, Saga PICKING_CONFIRMED→PACKING_CONFIRMED, outbox row) when last unit sealed AND qty covers all order lines (AC-01/AC-02); ShipmentNotificationPersistence component holds REQUIRES_NEW markNotified/markFailed (AC-03); PickingController/PackingController consume QueryPickingRequestUseCase/QueryPackingUnitUseCase in-ports only — out-port injection removed (AC-04); EventEnvelopeSerializer drops non-contract pickingRequestId from picking.requested/cancelled (AC-05); shipment_no format aligned to SHP-YYYYMMDD-NNNN per contract §4.1 (AC-06 partial — implementation/contract aligned but domain-model.md §5 spec doc not yet updated); Shipment.createdBy populated through to ShipmentResponse (AC-07); 130 tests pass including 3 new seal-completion tests + 4 ShipmentNotificationListener/Persistence tests. Review verdict 2026-04-29: FIX NEEDED → follow-up in TASK-BE-041 (1 warning: shipment_no format documented in contract example + implementation but not yet declared in domain-model.md §5).
- `TASK-BE-038-outbound-pick-pack-ship-domain.md` - PickingRequest/Confirmation/PackingUnit/Shipment aggregates + ConfirmPicking/Packing/Shipping use cases + REST controllers + TMS post-commit wiring (V11 schema align). Review verdict 2026-04-29: FIX NEEDED -> follow-up in TASK-BE-040 (2 critical: PATCH seal endpoint does not emit packing.completed per outbound-service-api.md section 3.2; ShipmentNotificationListener Transactional(REQUIRES_NEW) annotations bypassed by self-invocation; 3 warnings: PickingController/PackingController inject out-ports directly violating Hexagonal layer rule, EventEnvelopeSerializer adds non-contract pickingRequestId field, shipment_no format inconsistent across spec/task/impl).
- `TASK-BE-039-fix-TASK-BE-037.md` — Added OrderQueryService/InventoryReleased/InventoryConfirmed consumer unit tests (port fakes only); cancel response now includes previousStatus/cancelledReason/cancelledAt/cancelledBy via OrderResult cancel-aware mapper + extended OrderResponse; list endpoint N+1 eliminated via SagaPersistencePort.findSagaStatesByOrderIds + OrderLineRepository.findLineSummariesByOrderIds bulk queries; CancelOrderService raises ObjectOptimisticLockingFailureException early on stale expectedVersion; SecurityConfig dead-code path matchers removed (role enforcement happens in application services); OrderMapper relocated to adapter package and made package-private. 100 tests pass. Review verdict 2026-04-29: **APPROVED**.
- `TASK-BE-037-outbound-order-domain-receive-cancel-query.md` — Order aggregate + OutboundSaga real transitions + ReceiveOrder/Cancel/Query use cases + saga consumers (inventory-reserved/released/confirmed) + real OutboxPublisher. 91 unit tests pass. Review verdict 2026-04-29: FIX NEEDED → follow-up in TASK-BE-039 (2 critical: missing AC-16 unit tests for OrderQueryService/InventoryReleasedConsumer/InventoryConfirmedConsumer, cancel response missing previousStatus/cancelledReason/cancelledAt/cancelledBy required by api §1.4; 4 warnings: N+1 in OrderQueryService.list, expectedVersion never compared, SecurityConfig dead path matchers, OrderMapper public visibility).
- `TASK-INT-009-fix-TASK-INT-008.md` — Fix silent catch (WARN log added), rename burst method to burstFrom800Requests, update class Javadoc, ThreadLocalRandom. Review verdict 2026-04-29: **APPROVED**
- `TASK-INT-008-e2e-rate-limit-burst-parallelise.md` — E2E rate-limit scenario 3 parallelised (virtual thread executor, 800 requests). Review verdict 2026-04-29: FIX NEEDED → follow-up in TASK-INT-009 (1 critical: silent catch; 2 warnings: method name/Javadoc out of sync with 800-request count).
- `TASK-BE-036-fix-TASK-BE-035.md` — Populate outcome column in EventDedupePersistenceAdapter (4-arg constructor, outcome="APPLIED"); ArgumentCaptor assertion added in test. Review verdict 2026-04-29: **APPROVED**
- `TASK-BE-035-fix-TASK-BE-034.md` — Fix webhook processing order (secret→timestamp→HMAC), outbound_outbox schema alignment (aggregate_type/event_version/partition_key via V9), outbound_event_dedupe outcome column (via V9). Review verdict 2026-04-29: FIX NEEDED → follow-up in TASK-BE-036 (1 warning: EventDedupePersistenceAdapter does not populate outcome column despite V9 adding it).
- `TASK-BE-034-outbound-service-bootstrap.md` — outbound-service Hexagonal skeleton, V1–V8 Flyway migrations, 6 MasterReadModel consumers, ERP order webhook ingest, saga/TMS/outbox stubs, Redis IdempotencyStore, JWT wiring. Review verdict 2026-04-29: FIX NEEDED → follow-up in TASK-BE-035 (1 critical: webhook processing order; 2 warnings: outbound_outbox/outbound_event_dedupe schema gaps vs domain-model.md).
- `TASK-BE-033-fix-TASK-BE-032.md` — GlobalExceptionHandlerTest MethodArgumentNotValidException 테스트 추가 + InMemoryIdempotencyStore.tryAcquireLock() ConcurrentHashMap.compute() 원자적 구현. Review verdict 2026-04-29: **APPROVED**
- `TASK-BE-032-fix-TASK-BE-031.md` — inbound-service 에러 코드 세분화(16개 도메인 예외 → 계약 정의 code 문자열) + REST Idempotency-Key 필터 구현(InboundIdempotencyFilter: Redis lookup/cache/lock, body hash 비교, DUPLICATE_REQUEST 409). Review verdict 2026-04-29: FIX NEEDED → follow-up in TASK-BE-033 (2 warnings: MethodArgumentNotValidException 테스트 누락, InMemoryIdempotencyStore 비원자적 경쟁조건).

- `TASK-BE-001-master-service-bootstrap.md` — Warehouse CRUD vertical slice. Review verdict 2026-04-20: FIX NEEDED → follow-up in TASK-BE-008
- `TASK-INT-001-gateway-master-service-route.md` — gateway route + JWT + rate-limit + header enrichment. Review verdict 2026-04-20: FIX NEEDED → follow-up in TASK-INT-003
- `TASK-BE-002-zone-aggregate.md` — Zone CRUD vertical slice. Review verdict 2026-04-20: FIX NEEDED → follow-up in TASK-BE-009
- `TASK-BE-003-location-aggregate.md` — Location CRUD + Zone guard. Review verdict 2026-04-20: FIX NEEDED → follow-up in TASK-BE-010
- `TASK-DOC-001-library-boundary-cleanup.md` — Javadoc sweep in libs/. Review verdict 2026-04-20: **APPROVED**
- `TASK-BE-004-sku-aggregate.md` — SKU CRUD. Review verdict 2026-04-20: FIX NEEDED → follow-up in TASK-BE-011
- `TASK-BE-007-master-service-integration-tests.md` — integration suite + contract harness. Review verdict 2026-04-20: **APPROVED** (2 non-blocking warnings noted)
- `TASK-INT-002-gateway-master-e2e.md` — live-pair e2e. Review verdict 2026-04-20: FIX NEEDED → follow-up in TASK-INT-004
- `TASK-INT-004-e2e-scenario2-guard-and-kafka-port.md` — scenario-2 guard + Kafka port + outbox smoke. Review verdict 2026-04-20: FIX NEEDED → follow-up in TASK-INT-006
- `TASK-BE-010-reference-integrity-violation-exception.md` — ReferenceIntegrityViolationException + Zone deactivate guard. Review verdict 2026-04-20: FIX NEEDED → follow-up in TASK-BE-014
- `TASK-BE-008-error-envelope-compliance.md` — error envelope `timestamp` + `STATE_TRANSITION_INVALID` → 422. Review verdict 2026-04-20: **APPROVED** (2 non-blocking suggestions)
- `TASK-BE-009-persistence-adapter-cleanup.md` — removed `existsById` pre-checks, narrowed catch, Javadoc drift. Review verdict 2026-04-20: **APPROVED** (2 non-blocking suggestions)
- `TASK-BE-011-sku-test-coverage-followup.md` — `SkuControllerTest` + `SkuPersistenceAdapterTest` Testcontainers variant. Review verdict 2026-04-20: **APPROVED** (2 non-blocking suggestions)
- `TASK-INT-003-gateway-rate-limit-and-fail-open.md` — `(ip, routeId)` key + fail-open decorator + empty role header. Review verdict 2026-04-20: **APPROVED** (2 non-blocking warnings noted — metric emission + blank-list filtering)
- `TASK-BE-014-warehouse-deactivate-active-zones-guard.md` — warehouse deactivate active-zones guard + hasActiveZonesFor port/adapter. Review verdict 2026-04-20: **APPROVED**
- `TASK-INT-006-drain-destructive-in-awaitility.md` — accumulate drain() across Awaitility retries; fixes masked field-mismatch failures. Review verdict 2026-04-20: **APPROVED** (1 pre-existing UUID.fromString nit noted)
- `TASK-BE-006-lot-aggregate.md` — master-service v1 final aggregate (Lot), scheduled expiration, SKU reverse-guard upgrade. Review verdict 2026-04-20: FIX NEEDED → follow-up in TASK-BE-015 (5 missing test classes + contract harness). 2 non-blocking warnings on LotService (WarehouseStatus comparison, expireBatch transaction scope)
- `TASK-BE-015-lot-test-coverage-followup.md` — Lot test classes (5/5 confirmed). Review verdict 2026-04-20: FIX NEEDED → follow-up in TASK-BE-016 (contract harness Lot wiring only). All 5 test classes high-quality; contract harness missing Lot cases.
- `TASK-BE-016-lot-contract-harness-wiring.md` — Lot cases wired into EventContractTest + HttpContractTest (3 new tests). Review verdict 2026-04-20: **APPROVED** (1 non-blocking note — master-lot-created.schema.json not asserted alongside generic envelope, can be added in future if envelope-depth parity desired)
- `TASK-BE-017-integration-test-flakiness.md` — Kafka consumer offset-reset + AccessDeniedException handler + OutboxMetrics TransactionTemplate + new integration-tests CI job. Review verdict 2026-04-21: **APPROVED** — 2 of 5 tests verified fixed; 3 remain CI-flaky on infra, deferred to TASK-BE-019
- `TASK-BE-018-non-blocking-suggestions-cleanup.md` — batch of 10 cleanup items from BE-008..BE-016 reviews (Sku.isActive, LotExpirationBatchProcessor REQUIRES_NEW, GatewayErrorHandler JSON escape, ApiError requireNonNull, etc.). Review verdict 2026-04-21: **APPROVED** (all 10 items verified)
- `TASK-BE-019-remaining-integration-test-flakiness.md` — bounded producer timeouts (integration profile), key-filtered `KafkaTestConsumer.pollOneForKey`, Awaitility-wrapped Prometheus scrape. Review verdict 2026-04-21: **APPROVED** (3 non-blocking warnings: buffer-on-discard in pollOneMatching, bare pollOneForKey in PublisherResilience, tight 5s counter window)
- `TASK-BE-021-inventory-service-bootstrap.md` — Hexagonal skeleton, V1–V5 Flyway schema (incl. W2 trigger), MasterReadModel consumers (Location/SKU/Lot) with version guard, EventDedupe adapter + outbox table, Redis IdempotencyStore, JWT/security wiring, Kafka DLT error handler. Review verdict 2026-04-28: FIX NEEDED → follow-up in TASK-BE-025 (3 blockers: DLT routing test absent — AC-11 unmet; `@WebMvcTest`/smoke 401-JWT test absent — AC-13 unmet; Redis prefix `inventory:idem:` diverges from spec `inventory:idempotency:` — latent risk for caller-built keys).
- `TASK-BE-022-inventory-core-receive-query.md` — Inventory + InventoryMovement domain (W2 structural invariant), ReceiveStockUseCase + service (master-ref validation, mutation counter), PutawayCompletedConsumer (EventDedupe + per-event TX), OutboxWriter + EventEnvelopeSerializer + OutboxPublisher (@Scheduled, exp backoff, pending/lag/failure metrics), 5 GET query endpoints, GlobalExceptionHandler. 45 unit tests pass; integration tests for putaway → inventory.received and dedupe authored. Review verdict 2026-04-28: FIX NEEDED → follow-up in TASK-BE-026 (3 issues: dead `isFreshlyCreated` + stale "thread-local flag" comment block in ReceiveStockService; `InventoryMovement.restore` bypasses W2 structural guard with no policy doc; `existingInventoryRowGetsIncremented` unit test does not distinguish insert vs update branch).
- `TASK-BE-023-reservation-lifecycle.md` — W4/W5 two-phase: Inventory.reserve/release/confirm + Reservation aggregate state machine, picking_request_id unique-constraint idempotency, ReserveStockService (3-retry optimistic lock, id-ascending order, 100–300ms jitter), ConfirmReservationService (exact shippedQty check), ReleaseReservationService, ReservationExpiryJob (@Scheduled per-row TX), 3 events wired, REST + role guards, GlobalExceptionHandler 4 new codes, gauge + retry counter. 81 unit tests pass; integration tests for picking flow + TTL job. Review verdict 2026-04-28: FIX NEEDED → follow-up in TASK-BE-027 (1 blocker: `PickingRequestedConsumer`, `PickingCancelledConsumer`, `ShippingConfirmedConsumer` all bypass `EventDedupePort` in favour of pickingRequestId-/state-based short-circuits — violates T8 + architecture.md §Consumer Rules + AC-12; `inventory_event_dedupe` table is never written by these three consumers).
- `TASK-BE-024-adjustment-transfer-alert.md` — Final inventory-service v1 mutation surface: Inventory.adjust/transferOut/transferIn/markDamaged/writeOffDamaged + StockAdjustment/StockTransfer aggregates, AdjustStockService (3 operations + RESERVED-bucket admin guard), TransferStockService (W1 atomic id-ascending lock order, target upsert with wasCreated, cross-warehouse rejection, low-stock evaluation), LowStockDetectionService (threshold + 1h Redis SETNX debounce, fail-open), 3 new events into sealed serializer, 8 REST endpoints with role guards, 4 new exceptions, 4 mutation counters. 122 unit tests pass (41 new); integration test covers adjusted/transferred/low-stock end-to-end on Kafka. Review verdict 2026-04-28: FIX NEEDED → follow-up in TASK-BE-028 (1 blocker: RESERVED-bucket admin guard implemented in `AdjustmentController` via raw `jwt.getClaim("role")` parsing — violates architecture.md:396 "authorization in the application layer, not in controllers"; must move into `AdjustStockService`).
- `TASK-BE-025-inventory-bootstrap-test-gaps.md` — Redis idempotency-key prefix aligned to spec (`inventory:idempotency:`), `RedisIdempotencyStoreTest` pins literal Redis key shape, `MasterLocationDltRoutingIntegrationTest` exercises poison-record → DLT path. JWT 401 AC-13 confirmed already covered by existing `unauthenticatedRequestReturns401` in `InventoryQueryControllerTest:46-50` and `MovementQueryControllerTest:83-85` — original BE-021 review flagged a false positive. Build green. Self-verdict 2026-04-28: **APPROVED**.
- `TASK-BE-026-receive-query-hardening.md` — `ReceiveStockService.isFreshlyCreated` + stale "thread-local flag" comment removed, `InventoryMovement.restore` documented as Policy B (trust persisted data) with class-level Javadoc cross-referencing sibling `restore` factories, `ReceiveStockServiceTest.existingInventoryRowGetsIncremented` tightened with `insertCalls`/`updateCalls` counters on `FakeInventoryRepo` so a buggy "always-insert" implementation now fails deterministically. Build green. Self-verdict 2026-04-28: **APPROVED**.
- `TASK-BE-027-eventid-dedupe-on-outbound-consumers.md` — `EventDedupePort` wired into all three outbound-saga consumers (`PickingRequestedConsumer`, `PickingCancelledConsumer`, `ShippingConfirmedConsumer`); each consumer is now `@Transactional` with the dedupe write joining the TX via `Propagation.MANDATORY`. Layered-idempotency Javadoc explains outer (eventId) + inner (terminal-state / pickingRequestId) guards. The third consumer (`ShippingConfirmedConsumer`) was completed manually after the implementing agent hit the rate limit; pattern mirrored from `PickingCancelledConsumer`. Existing `PickingFlowIntegrationTest` covers the wiring end-to-end. Build green. Self-verdict 2026-04-28: **APPROVED** (1 non-blocking note: dedicated unit tests for the three consumers were not added; integration test provides coverage. Future cleanup task can split unit-level dedupe assertions out).
- `TASK-BE-028-reserved-bucket-admin-guard-relocation.md` — Pattern B chosen: `AdjustStockCommand` extended with `Set<String> callerRoles`, populated in `AdjustmentController` from `Authentication.getAuthorities()`, consumed in `AdjustStockService.doAdjust()` which throws `AccessDeniedException` (mapped to 403 by GlobalExceptionHandler) when bucket=RESERVED and roles lack `ROLE_INVENTORY_ADMIN`. Raw `jwt.getClaim("role")` inspection removed from the controller (residual `jwt.getSubject()` / `jwt.getClaimAsString("actorId")` calls retain actor-identification only — not authorization). Build green. Self-verdict 2026-04-28: **APPROVED**.
- `TASK-BE-029-inbound-service-bootstrap.md` — Hexagonal skeleton, V1–V7 Flyway schema (incl. webhook inbox/dedupe + role-grant W2 invariants), 6 master snapshot consumers (Warehouse/Zone/Location/SKU/Lot/Partner) with version guard + EventDedupePort, ErpAsnWebhookController (HMAC + timestamp + dedupe + inbox write), inbox processor stub, Redis IdempotencyStore (`inbound:idempotency:` prefix pinned), JWT/security wiring, Kafka DLT error handler. Self-verdict 2026-04-29: **APPROVED**.
- `TASK-BE-030-inbound-asn-receive-inspect.md` — Full ASN domain (Asn/AsnLine/Inspection/InspectionLine/InspectionDiscrepancy), ReceiveAsn/StartInspection/RecordInspection/AcknowledgeDiscrepancy/CancelAsn/QueryAsn use cases + services, JPA persistence adapters, AsnController + InspectionController REST endpoints, OutboxWriterAdapter (real) + OutboxPublisher (@Scheduled), ErpWebhookInboxProcessor full implementation. Events: inbound.asn.received, inbound.asn.cancelled, inbound.inspection.completed. Self-verdict 2026-04-29: **APPROVED**.
- `TASK-BE-031-inbound-putaway-close.md` — PutawayInstruction/PutawayLine/PutawayConfirmation domain, InstructPutaway/ConfirmPutawayLine/SkipPutawayLine/CloseAsn use cases + services, PutawayController REST endpoints + AsnController:close, PutawayPersistenceAdapter. Events: inbound.putaway.instructed, inbound.putaway.completed (cross-service ⚠️), inbound.asn.closed. Asn.instructPutaway/completePutaway/close state transitions added. 143 unit/slice tests pass. Review verdict 2026-04-29: **APPROVED** (2 non-blocking warnings: error code granularity pre-existing; idempotency-key replay cache not wired — domain uniqueness constraints serve as substitute).

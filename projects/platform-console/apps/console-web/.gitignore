node_modules
.next
.env.local
.env*.local
*.tsbuildinfo

# Playwright e2e harness (TASK-PC-FE-019) — global-setup persists the
# SUPER_ADMIN session here; never check in test tokens.
tests/e2e/fixtures/.storage-state.json
playwright-report/
test-results/

# Playwright PR-time smoke harness (TASK-PC-FE-021) — outputs distinct from
# the nightly full-stack outputs above so both can coexist on the same dev
# machine.
playwright-report-smoke/
test-results-smoke/

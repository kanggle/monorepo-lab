# Task ID

TASK-PC-FE-276

# Title

🔴 **마스터 표가 이름 자리에 UUID 원문을 찍는다 — 바로 옆 화면은 같은 데이터를 이름으로 그린다**

# Status

review

# Owner

platform-console

# Task Tags

- erp-ops
- ui
- readability

---

# Goal

`/erp/masters` 와 그 상세 화면들이 **참조 컬럼에 raw UUID 를 렌더**하는 것을 고친다.

---

# 🔴 어떻게 발견됐나 — 포트폴리오용 캡처를 «눈으로 보다가»

`TASK-MONO-648` 이 취업 자료용으로 콘솔 전 페이지를 찍었다. `/erp/masters` 는 그중
**론처 카드 4장에 뽑힌 장**(2026-09-09 소유자 승인)이라 실제로 열어 봤고, 거기서 나왔다.

🔴🔴 **자동 판정은 이것을 못 잡았다.** 648 의 캡처 스크립트는 본문에 `empty`/`degraded`
표지를 붙이는데, 이 화면은 **둘 다 아니다** — 글자가 가득 차 있고 표도 5개가 다 찼다.
표지는 「글자가 있나」를 묻고 「무엇이 그려졌나」는 못 묻는다. 🔵 **큐레이션에 사람 눈이
필요한 이유가 이 티켓이다.**

## 무엇이 보이는가

`/erp/masters` 의 「부서」 표 — 「상위 부서」 칸:

```
DEPT-FIN  재무팀    ACTIVE  01a085a1-bc98-741e-ba56-21b896de17f4   active (2026-01-01~∞)
DEPT-HQ   본사      ACTIVE  —                                      active (2026-01-01~∞)
DEPT-OPS  운영본부  ACTIVE  01a085a1-bc98-741e-ba56-21b896de17f4   active (2026-01-01~∞)
```

「직원」 표의 「부서」 칸도 같다.

## 🔴🔴 데이터가 없어서가 아니다 — **바로 옆 화면이 이름으로 그린다**

같은 도메인 · 같은 테넌트 · 같은 세션에서 찍은 `/erp/orgview`:

```
EMP-0002  이운영  ACTIVE  DEPT-HQ · 본사 › DEPT-OPS · 운영본부  CC-OPS · 운영 코스트센터  JG-G2 · 대리
EMP-0003  박재무  ACTIVE  DEPT-HQ · 본사 › DEPT-FIN · 재무팀    CC-FIN · 재무 코스트센터  JG-G2 · 대리
```

**코드 + 이름 + 계층 경로**가 전부 나온다. 즉 백엔드가 이름을 해석할 수 있고, 이 프로젝트는
그 표현(`CODE · 이름`)을 **이미 정해 두었다.** 마스터 화면만 그것을 안 쓴다.

## 모집단 — **8곳 · 6파일 · 4개 참조 종류** (형제부터 grep 했다)

| 파일 | 줄 | 무엇을 |
|---|---|---|
| `DepartmentList.tsx` | 138 | `{d.parentId ?? '—'}` |
| `DepartmentDetail.tsx` | 94 | `{d.parentId}` |
| `EmployeeList.tsx` | 160 | `{e.departmentId ?? '—'}` |
| `EmployeeDetail.tsx` | 128 · 150 · 172 | `departmentId` · `jobGradeId` · `costCenterId` |
| `CostCenterList.tsx` | 138 | `{c.departmentId ?? '—'}` |
| `CostCenterDetail.tsx` | 86 | `{c.departmentId}` |

전부 `projects/platform-console/apps/console-web/src/features/erp-ops/components/` 아래다.

🔴 **한 곳만 고치면 나머지 일곱이 남는다.** 그리고 남은 것들은 목록 화면보다 **덜 열리는
상세 화면**에 있어서 다음 사람이 「고쳐졌다」고 믿기 쉽다.

---

# 🔴 아직 안 정한 것 — 구현자가 고르지 말고 **근거를 적고** 고른다

이름을 어디서 얻을 것인가. 셋 다 성립하고 대가가 다르다:

| | 방법 | 좋은 점 | 나쁜 점 |
|---|---|---|---|
| **A** | **같은 페이지에 이미 로드된 목록에서 조회** | 🔵 부서의 상위 부서는 **같은 표 안에 있다.** 네트워크 0, 계약 변경 0 | 🔴 페이지네이션을 넘어가면 못 찾는다(부서 3건이라 오늘은 안 보이는 함정) · 비용센터→부서처럼 **다른 표**를 참조하는 칸엔 그 표가 로드돼 있어야 한다 |
| **B** | 마스터 API 응답에 `parentName` 등을 **추가** | 화면이 단순해진다 | 🔴 **계약 변경** — `specs/contracts/` 를 먼저 고쳐야 한다(CLAUDE.md) |
| **C** | `orgview` read-model 을 마스터 화면에서도 소비 | 이미 이름을 준다 | 🔴 **eventually-consistent** 다(화면에 배너가 그렇게 적혀 있다). 마스터는 «권위» 화면이라 성질이 다르다 — `/erp/orgview` 안내문이 *"권위 데이터는 「마스터」 화면에서 조회합니다"* 라고 못 박는다 |

🔴🔴 **C 는 함정으로 보인다** — 제품이 이미 「권위 vs read-model」을 갈라 놓았는데 그것을
되섞는 것이다. **A 가 가장 작고 B 가 가장 옳아 보이지만, 판단은 계약을 열어 본 뒤에 한다.**
(분석=Opus 5; 이 표는 코드를 읽고 만든 것이고 계약 파일은 **아직 안 열었다**.)

---

# Scope

## 포함

- 위 8곳의 참조 컬럼이 사람이 읽을 수 있는 값을 그리게 한다
- 표현은 `orgview` 의 관례(`CODE · 이름`)를 **따른다** — 새로 짓지 않는다
- 이름을 못 찾은 경우의 표시(아래 Edge Cases)
- 회귀 가드

## 제외

- 🔴 **`/erp/orgview` 를 건드리는 일** — 거기는 이미 옳다
- 🔴 다른 도메인(wms·scm·finance·ecommerce)의 같은 부류 — **모집단을 안 쟀다.**
  🔵 세는 것은 이 티켓 AC-0 이 하고, 있으면 **별도 티켓**으로 낸다(범위가 터진다)
- 계약(`specs/contracts/`) 변경 — 방법 B 를 고른 경우 **선행 티켓**이 된다

---

# Acceptance Criteria

## AC-0 — 착수 전 재측정

- [x] 🔴 위 **8곳 표**를 다시 세라. 줄 번호는 낡는다 — 술어로 세라(참조 id 필드를 셀에
      그대로 렌더하는 곳).
- [x] 🔴 **다른 도메인도 세라.** wms·scm·finance·ecommerce·iam 의 컴포넌트에 같은 부류가
      있는가. **있어도 이 티켓에서 고치지 마라** — 수만 적고 별도 티켓을 낸다.
      🔵 안 세면 다음 사람이 「erp 만 그랬다」고 믿는다.
- [x] 방법 A/B/C 중 하나를 고르고 **왜 그것인지 본문에 적어라.** B 면 계약이 선행이다.

## AC-1 — 고친다

- [x] 8곳 전부. 🔴 **목록 화면만 고치고 상세를 남기지 마라** — 상세는 덜 열려서 남은 것이
      오래 산다.
- [x] 표현은 `CODE · 이름`. 🔴 `orgview` 가 쓰는 문자열을 **재사용**하라(같은 개념에 두
      포맷이 생기면 다음엔 한쪽만 고쳐진다).
- [x] 🔴 **`data-testid` 를 바꾸지 마라** — `erp-department-parentid` 등을 기존 e2e 가
      잡고 있다. 값이 바뀌는 것은 의도지만 **선택자가 바뀌면 그건 다른 변경**이다.

## AC-2 — 회귀 가드

- [x] 🔴 **UUID 모양이 셀에 나오면 빨강.** 술어를 「UUID 정규식이 렌더된 표 셀에
      있는가」로 두면 8곳 전부를 한 번에 지킨다 — 파일마다 단언을 복제하지 마라.
- [x] 🔴 **비-공허성**: 그 가드가 **아무 셀도 안 보고** 초록일 수 있다(픽스처가 비면).
      최소 행 수 하한을 두되 하한의 대상은 «렌더된 참조 셀의 수» 다.
- [x] **bite** — 한 곳을 `{d.parentId}` 로 되돌리면 빨강. 🔴 **주입·실행·bite 를 각각**
      단언하라(변형이 문법을 깨서 난 빨강은 «문 것»이 아니다).
- [x] **대조군** — 이름이 진짜 없는 행(최상위 부서, `parentId = null`)은 `—` 로 남고
      가드가 **안 문다.**

## AC-3 — 포트폴리오 쪽으로 돌려주기

- [x] 🔴 머지되면 **`TASK-MONO-648` 에 알려라.** `/erp/masters` 는 론처 카드 4장 중
      1번으로 승인된 장이고, 지금 찍힌 그림에는 UUID 가 박혀 있다. **다시 찍어야 한다.**
      🔵 648 AC-3 의 낡음 가드는 «경로가 사라졌나» 만 묻고 «내용이 바뀌었나» 는 못 잡는다
      — 그래서 이 칸이 사람 손으로 그 자리를 대신한다.

---

---

# 🟢 착수 기록 (2026-09-10 UTC)

## AC-0 ① — 8곳을 **술어로** 다시 셌다

줄 번호가 아니라 술어(`참조 id 필드를 셀에 그대로 렌더`)로 셌고, **기안의 표와 정확히
일치**했다 — 8곳 · 6파일 · 4종류. 🔵 줄 번호도 아직 맞았다.

## AC-0 ② — 다른 도메인: **48곳 / 12 도메인**. 🔴 그러나 그것은 «결함 수» 가 아니다

술어: `{x.<something>Id}`(및 `?? '…'` 형)를 **보이는 텍스트 자리**에 렌더하는 줄,
`key=`/`data-testid=`/`value=`/prop 전달 제외.

```
ecommerce-ops 11 · ledger-ops 10 · scm-replenishment 4 · scm-ops 4 · finance-ops 4 ·
wms-ops 3 · tenants 3 · org-hierarchy 3 · wms-outbound-ops 2 · operator-groups 2 ·
audit 1 · accounts 1        합계 48
```

🔴🔴 **이 48을 「고칠 목록」으로 읽으면 안 된다.** 술어가 두 가지를 못 가른다:
① **자기 식별자 vs 참조**(`OrdersTable` 의 `{o.orderId}` 는 그 행 자신의 id 이고 정상이다) ·
② **런타임 값이 UUID 인가 읽을 수 있는 코드인가**. ②는 선언으로 못 판정한다 — 실측:
콘솔 zod 스키마에 **`.uuid()` 가 한 건도 없다**(전부 `z.string()`).
⇒ **`TASK-PC-FE-277`** 을 냈다. 그 티켓의 본체는 고치기가 아니라 **가르기**다.

## AC-0 ③ — 방법은 **A**. 그리고 그 근거는 **코드에 이미 있었다**

| | 왜 아닌가 / 왜 맞나 |
|---|---|
| **B**(API 에 `parentName` 추가) | 🔴 계약 변경이라 `specs/contracts/` 가 선행이다. **필요 없었다** — 아래 |
| **C**(`orgview` read-model 소비) | 🔴 제품이 갈라 둔 «권위 vs eventually-consistent» 를 되섞는다. `/erp/orgview` 안내문이 명시적으로 반대한다 |
| 🟢 **A** | **데이터가 이미 그 컴포넌트 안에 있었다** |

🔵 **A 를 고른 결정적 근거 둘** (기안 시점엔 «가장 작아 보인다» 였고, 코드를 열자 **가장
옳은** 것이 됐다):

1. **상세 3종은 참조를 이미 해석해 두고 있었다.** `DepartmentDetail` 은
   `useDepartment(d?.parentId)`, `EmployeeDetail` 은 부서·직급·비용센터 셋을,
   `CostCenterDetail` 은 부서를 — **RETIRED 배지를 그리려고** 이미 부른다.
   즉 `code`·`name` 을 손에 쥔 채 **id 를 찍고 있었다.** 새 요청이 0건이다.
2. **목록 2종은 이미 `optionSources` 를 받고 있었다.** `EmployeeList` · `CostCenterList` 가
   쓰기 다이얼로그 드롭다운용으로 `{id, code, name}` 목록을 **이미** 받는다. `DepartmentList`
   에만 같은 모양(`parentOptions`)을 더했다.

🔴 **페이지네이션 함정의 처리**(기안이 명시를 요구한 것): 조회원 밖의 참조는
**`이름 확인 불가`** 로 그린다 — **id 로 되돌아가지 않는다.** 그래서 부서가 늘어 부모가
페이지 밖으로 나가는 날 그것은 «조용한 퇴행» 이 아니라 **보이는 상태**가 된다.
🔵 상세 화면은 개별 GET 으로 해석하므로 이 함정 자체가 없다.

🔵 **`asOf` 과거 조회**(기안이 «진짜 질문» 이라 부른 것): 이름은 **그 조회가 이미 가져온
응답의 이름**이다 — 목록은 같은 `asOf` 로 받은 목록에서, 상세는 같은 훅이 받은 엔티티에서
온다. 즉 **별도 결정이 필요 없었다**: 시점 축은 이미 화면 전체에 하나로 흐르고 있고,
이 변경은 그 흐름 안의 값을 **읽기만** 한다.

## AC-1 — 8곳 전부. 그리고 **인라인 복제 11곳을 통일했다**

- 목록 3 + 상세 5 = **8곳 전부**. 🔴 상세를 남기지 않았다(기안 Failure 1).
- 표현은 `CODE · 이름`. 🔴🔴 **새로 짓지 않았다** — 그런데 그 문자열은 이미 **11곳에
  인라인으로 복제**돼 있었다(`EmployeeOrgViewCard` 3 · `BusinessPartnerList` 2 ·
  `CostCenterList` 2 · `JobGradeList` 2 · `MasterWriteDialog` 1 · `OrgScopeDialogBody` 1).
  거기에 9·10번째 사본을 더하는 것이 Failure 2 이므로, **한 벌**
  (`lib/master-ref-label.ts` 의 `codeName`)을 만들고 **기존 사본도 그것을 쓰게 했다.**
- 🔴 **`data-testid` 는 한 글자도 안 바꿨다** — `erp-department-parentid` 를 기존 e2e 가
  잡고 있다. 바뀐 것은 **값**이다.

### 🔴 `OrgScopeDialogBody` 는 결국 **손대지 않았다** — 콘솔 가드가 물었다

그 파일도 같은 인라인 복제를 갖고 있어 함께 통일했는데, 전체 스위트가 **둘을** 떨어뜨렸다:

```
FAIL tests/unit/layer-dependency-rules.test.ts
     > no feature imports another feature (deep OR through its barrel)
FAIL tests/unit/OperatorsScreen.test.tsx
```

`features/operators/` 가 `features/erp-ops/lib/` 를 import 한 것이고, **콘솔의 아키텍처
규칙 위반**이다. 🔵 **가드가 옳다.** 되돌리자 둘 다 통과했다. ⇒ 그 파일의 처리(포맷 통일 +
🔴 **id 폴백 제거**)는 헬퍼를 `shared/` 로 옮기는 선행이 있어야 하므로
**`TASK-PC-FE-277` AC-2** 로 넘겼다.

## AC-2 — 회귀 가드 `tests/unit/erp-master-ref-names.test.tsx`

🔴 **술어는 하나다**: 「**렌더된** 참조 셀의 텍스트에 UUID 정규식이 걸리는가」. 파일마다
단언을 복제하지 않았다 — 아홉 번째 자리가 생겨도 같은 술어가 잡는다.

🔵 **모집단을 선언으로 만들었다** — 참조 칸에 `data-master-ref="<entity>.<field>"` 마커를
달았고(8곳), 가드는 그 마커로 셀을 고른다. 🔴 마커를 안 단 참조 칸은 **가드 밖**이므로,
그 사실을 가드 머리말과 `277` AC-1 에 적었다.

**🔴 비-공허성 — 하한의 대상은 «렌더된 참조 셀의 수» 다.** 「UUID 0건」에 하한을 걸면
«없다» 와 «안 봤다» 가 같아진다.

**대조군 둘** (기안이 요구한 것 + 하나 더):

| | 상태 | 기대 |
|---|---|---|
| ① | `parentId: null`(최상위 부서) | `—` · 가드가 **안 문다** |
| ② 🔴🔴 | 조회원 **밖**의 참조(페이지네이션 함정) | `이름 확인 불가` · **UUID 로 안 돌아간다** |

**bite 2종 — 주입·실행·물기를 각각 단언했다:**

```
① DepartmentList 를 {d.parentId ?? '—'} 로 되돌림
   → 2 failed / 7 passed   「부서 목록: 참조 셀에 UUID 원문이 그려졌습니다」
② 부서 픽스처를 0건으로   (비-공허성)
   → 3 failed / 6 passed   「부서 목록: 참조 셀이 0개 — 하한 3」
```

🔵 둘 다 **정확히** 물었다 — 무차별 빨강이 아니다.

🔴 **픽스처의 id 는 실제 화면에서 본 UUID 모양**(`01a085a1-…`)이다. 읽기 쉬운 가짜 id 를
쓰면 이 가드는 **결함을 재현할 수 없는 입력 위에서** 초록이 된다.

⚪ **이 가드가 못 재는 것**: 컴포넌트 렌더를 잰다. 실제 화면에서 백엔드가 무엇을 주는지,
그 값이 정말 UUID 인지는 **라이브 축**이다 — 648 의 자동 판정이 이 결함을 놓친 이유와 같다
(표지는 「글자가 있나」를 묻고 「무엇이 그려졌나」는 못 묻는다).

## AC-3 — `TASK-MONO-648` 에 돌려주기

- [x] 🔴 **`/erp/masters` 를 다시 찍어야 한다.** 지금 론처 카드 1번으로 승인된 그림에는
      UUID 가 박혀 있다. 648 의 `in-progress` 본문에 이 사실을 적었다 — 그 티켓의 재촬영
      묶음(ecommerce 테넌트로 콘솔 재촬영)에 이 장이 **추가**된다.
      🔵 648 AC-3 의 낡음 가드는 «경로가 사라졌나» 만 묻고 «내용이 바뀌었나» 는 못 잡으므로,
      이 칸이 사람 손으로 그 자리를 대신한다.

---

# 🔴 임플 PR 이 이 파일을 `review/` 로 안 옮겼다 (2026-09-10 UTC · `in-progress` 에서 적는다)

`#3731`(squash `dc827d94d`)이 구현과 AC 체크를 전부 실었는데 **파일은 `in-progress/` 에
남았다.** `tasks/INDEX.md` § PR Separation Rule 이 impl PR 에 요구하는
*"moves the task file through `in-progress/` to `review/`"* 중 **마지막 칸이 빠졌다.**
🔵 그래서 이 기록을 여기 적는다 — 같은 문서 § Review Rules 가 `in-progress/` 를
「수정하지 마라」 목록에서 **일부러 뺐고**(*"that file is the working document until it moves
to `review/`"*), `review/` 에 올린 뒤에는 이것을 적을 수 없다.

## 리뷰 단계에서 잰 것

**AC-0 ①②③ · AC-1 · AC-2 · AC-3 — 열린 칸 0.** 체크박스가 아니라 § 착수 기록의 근거를
읽어 대조했다.

🔴 그리고 이 티켓이 **임시로 들고 있던 남의 의무 둘**이 실제로 집을 받았는지 확인했다.
받지 않았으면 `done/` 은 frozen 이라 그 의무가 다시 읽히지 않는다:

| 넘긴 것 | 받은 곳 | 실측 (2026-09-10 UTC) |
|---|---|---|
| `/erp/masters` **재촬영** | `TASK-MONO-648` (`in-progress`) | AC-2 에 한 칸으로 살아 있다 — *"🔴🔴 `/erp/masters` 도 재촬영 대상이 됐다 (2026-09-10, `TASK-PC-FE-276` 이 알림)"* 이고 *"위 `/ecommerce/*` 3장과 **같은 창에서 함께** 찍는다"* 로 **묶음까지** 지정돼 있다 |
| 48곳 **가르기** | `TASK-PC-FE-277` (`ready`) | § 실측 표(48곳/12도메인)와 «이 48은 결함 수가 아니다» 가 그대로 옮겨져 있다 |
| `data-master-ref` **마커 밖은 가드 밖** | `TASK-PC-FE-277` AC-1 | *"참조 칸에는 `data-master-ref` 마커를 단다 — 276 의 회귀 가드가 그것으로 모집단을 만든다. 🔴 마커를 안 달면 그 칸은 **가드 밖**이다"* |
| `OrgScopeDialogBody` **id 폴백** | `TASK-PC-FE-277` AC-2 | 헬퍼 `shared/` 이동이 **선행**임과, 276 이 실측으로 밟은 `layer-dependency-rules.test.ts` 빨강이 근거로 적혀 있다 |

🔵 넷 다 **열려 있는 큐**(`in-progress` · `ready`)에 있다. `done/` 에 넘긴 것은 없다.

# Related Specs / Contracts

- `projects/platform-console/specs/` — ERP 마스터 화면 계약(방법 B 를 고르면 **선행**)
- `TASK-PC-FE-010` — `/erp/masters` 표면
- `TASK-PC-FE-232` — `/erp` 개요 분리, 마스터가 `/erp/masters` 로 이동
- `TASK-PC-FE-076` — drill-in 분할
- `TASK-MONO-648` — 이 결함이 발견된 경로 (AC-2 론처 카드 4장)

---

# Edge Cases

- **`parentId = null`(최상위 부서)** — 지금도 `—` 다. 그대로 둔다.
- **이름을 못 찾았다** — 🔴 **id 로 조용히 되돌아가지 마라.** 그러면 결함이 «가끔»
  되살아나고 그때는 아무도 안 본다. 못 찾았다는 것을 보이게 하라.
- **페이지네이션 밖의 참조**(방법 A 의 함정) — 부서가 3건이라 **오늘은 안 보인다.**
  🔴 부서가 늘면 나타나는 부류이므로, A 를 고르면 이 경우의 처리를 명시하라.
- **폐기된(retired) 참조 대상** — 이름이 있지만 유효기간이 지났다. `orgview` 가 이것을
  어떻게 표시하는지 **먼저 보고** 같게 한다.
- **`asOf` 과거 조회** — 그 시점의 이름이어야 하는가, 현재 이름이어야 하는가.
  🔴 E3 effective-dating 화면이므로 **이것은 진짜 질문이다.** 답을 적어라.

---

# Failure Scenarios

1. 🔴🔴 **목록 화면 3곳만 고치고 상세 5곳을 남긴다.** 상세는 덜 열려서 오래 산다.
2. 🔴 **`orgview` 와 다른 포맷을 새로 만든다.** 같은 개념에 두 표현이 생기고 다음엔
   한쪽만 고쳐진다.
3. 🔴 **read-model(`orgview`)을 마스터 화면에서 소비한다.** 제품이 갈라 둔 «권위 vs
   eventually-consistent» 를 되섞는다 — `/erp/orgview` 안내문이 명시적으로 반대한다.
4. **가드가 공허하게 초록이다** — 픽스처에 참조 셀이 없으면 아무것도 안 보고 통과한다.
5. 🔴 **고치고 나서 `TASK-MONO-648` 에 안 알린다.** 론처에 걸릴 그림에 UUID 가 박힌 채
   남는다 — 하필 취업 자료의 첫 화면이다.

---

# 분석 / 구현 권장

분석=Opus 5 / 구현 권장=**Sonnet** — 8곳의 표현 교체가 본체다.
🔴 단, **AC-0 의 방법 선택(A/B/C)과 AC-2 의 가드 설계는 Opus** — 전자는 계약·아키텍처
경계에 닿고, 후자는 이 저장소가 반복해서 틀린 축이다.

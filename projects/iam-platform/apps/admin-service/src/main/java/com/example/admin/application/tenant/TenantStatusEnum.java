package com.example.admin.application.tenant;

/**
 * TASK-BE-250: Local copy of the tenant status enum for admin-service validation.
 * Mirrors {@code account-service}'s {@code TenantStatus}. Keep in sync.
 */
public enum TenantStatusEnum {
    ACTIVE,
    SUSPENDED
}

# E2E profile — docker-compose.e2e.yml driven overrides.
spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:mysql}:${DB_PORT:3306}/${DB_NAME:account_db}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: ${DB_USERNAME:account_user}
    password: ${DB_PASSWORD:account_pass}
    # TASK-BE-045: see auth-service/application-e2e.yml for rationale.
    hikari:
      connection-timeout: 60000
      initialization-fail-timeout: 60000
      maximum-pool-size: 5
  flyway:
    enabled: true
    # TASK-MONO-160: migration-dev carries the e2e-only globex-corp demo customer
    # (V0021) so the ADR-020 D4 A↔B switch proof has a second customer with
    # complementary [scm,erp] subscriptions, present at startup via Flyway
    # (matching the acme-corp V0020 path). migration-dev is NOT loaded in
    # production — globex-corp never reaches a real DB.
    locations: classpath:db/migration,classpath:db/migration-dev
    baseline-on-migrate: false
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:kafka:9092}

logging:
  level:
    root: INFO
  file:
    name: ""

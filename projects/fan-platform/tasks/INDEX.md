# Tasks Index — fan-platform

This document defines task lifecycle, naming, and move rules for the **fan-platform** project. Repo-root [tasks/INDEX.md](../../../tasks/INDEX.md) covers monorepo-level (cross-project) tasks; this file covers fan-platform-internal tasks only.

---

# Lifecycle

backlog → ready → in-progress → review → done → archive

Only tasks in `ready/` may be implemented.

---

# Task Types

- `TASK-FAN-BE-XXX`: backend
- `TASK-FAN-FE-XXX`: frontend (Next.js)
- `TASK-FAN-INT-XXX`: cross-service integration / E2E

---

# Move Rules

## backlog → ready
Allowed only when:
- related specs exist (`specs/services/<service>/architecture.md`, `specs/contracts/...`)
- related contracts are identified
- acceptance criteria are clear
- task template is complete

## ready → in-progress
Allowed only when implementation starts.

## in-progress → review
Allowed only when:
- implementation is complete
- tests are added
- contract / spec updates are completed if required

## review → done
Allowed only after review approval.

### Review Rules
- Tasks in `review/` must not be re-implemented directly.
- If a review reveals a bug or missing requirement, create a new fix task in `ready/` referencing the original task.
- Fix tasks must include the original task ID in their Goal section (e.g. "Fix issue found in TASK-FAN-BE-001").
- Do not modify a task file after it moves to `review/` or `done/`.

## done → archive
Allowed when no further active change is expected.

---

# Rule

Tasks must not be implemented from `backlog/`, `in-progress/`, `review/`, `done/`, or `archive/`.

---

# Task List

## backlog

(empty)

## ready

- `TASK-FAN-BE-047-api-lets-a-fan-publish-a-gated-post.md` — **🔴 BLOCKED (ADR-003 PROPOSED) — 착수 불가.** API 가 팬의 유료 글 발행을 허용한다: `PublishPostUseCase` 는 `postType` 만 검사하고 `visibility` 는 **아무도 검사하지 않아** `FAN` 역할만으로 `PREMIUM` 글이 201(2026-08-06 재확인). 🔴🔴 **이 티켓이 스스로 적었던 전제가 틀렸다** — *"티어는 아티스트 수익화를 위해 존재한다(`architecture.md` § Visibility Tiers)"* 는 **그 절에 없는 문장**이다(그 절은 읽기 게이팅 메커니즘만 기술). `TASK-FAN-FE-016` 과 같은 세션에서 **추론이 인용처럼** 적힌 것 ⇒ 삭제·정정. 🔴 그리고 **스펙은 침묵이 아니라 반대를 규정한다**: `specs/integration/v1-e2e-scenarios.md` § Scenario 3 이 팬의 `FAN_POST` 를 **PREMIUM·MEMBERS_ONLY 로 발행하는 시나리오를 명시**하고 `VisibilityTierE2ETest` 가 그대로 구현하고 있다(문서가 아니라 **산출물로 확인**). ⇒ 좁히기는 드리프트 교정이 아니라 **새 제약 도입**이고, 티켓 자신의 판정 기준에 따라 **ADR 필요**. **AC-0 4항목 전부 실측 완료**: 비-PUBLIC `FAN_POST` **0건** · `UpdatePostUseCase` 는 `visibility` 를 **받지 않아** 발행 후 불변(⇒ Failure Scenario "나중에 PREMIUM 으로 고친다" 는 **도달 불가**였다) · 운영자 대리 작성 경로 **없음** · ADR 필요. 판정·선택지·권고는 [[ADR-003]] `docs/adr/ADR-003-fan-post-visibility-authoring-rule.md` (**Proposed**, 권고=B 명시 허용 — 지금 다치는 사람이 없고 A 는 명시된 e2e 를 깨며 되돌리기 어렵다). 🔴 **A 를 고르면 `VisibilityTierE2ETest` + `v1-e2e-scenarios.md` 동반 수정 필수** — 안 하면 머지 즉시 e2e RED. **해제 조건**: `ADR-FAN-003 ACCEPTED` 정확형 intent(일반 "진행" 불가, self-ACCEPT 금지). 분석=Opus 5 / 구현 권장=Sonnet(ACCEPT 후, B 면 계약 문서 PR 하나).



## in-progress

## review

- `TASK-FAN-INT-006-membership-gate-still-ships-switchable.md` — **🟣 REVIEW (impl PR, 2026-08-12) — 마지막 끌 수 있는 게이트를 지웠다.** membership-service 를 라이브 스택에 넣고 `community.membership-service.enabled` + `AlwaysAllowMembershipChecker` 를 **삭제**했다. 🔴 **AC-0 의 답이 티켓의 전제를 뒤집었다** — 티켓은 ①(제품 경로)의 비용으로 *"빌링키(PortOne) 앞에 결제 평면을 세워야 한다"* 를 들었는데, `MockPaymentGatewayAdapter` 가 `@Profile("!portone")` 라 **이미 그 스택의 결제 어댑터**다. 세울 것이 없었고, 그러면 ①이 ②(Flyway 시드)·③(직접 INSERT)과 같은 비용에 **증명 범위만 넓다**(구독 경로 + 게이트 둘 다) ⇒ ① 채택. 🔴 **AC-1 을 셋으로 만들었다** — 티켓이 요구한 "있는 독자 200 / 없는 독자 403" 은 **한 방향으로 고장난 게이트로도 만족된다**(base-url 이 틀리면 fail-closed 전건 403). 그래서 **티어 거부**를 넣었다: MEMBERS_ONLY 구독자가 PREMIUM 글에 403 이면서 **같은 계정이 자기 티어는 200** — 도달 불가한 서비스는 이 대조군을 만들 수 없으므로, 이 한 쌍만이 *게이트가 답한다* 와 *게이트에 닿는다* 를 가른다. 🔴 **AC-2 의 술어는 그대로 성립할 수 없어 고쳐 적용했다** — "grep 0건"을 강제하면 남는 선택지가 *삭제 사실을 안 적는 것*뿐이고 그게 재발 조건이다. 대신 *"어떤 활성 파일도 그것이 존재한다고 말하지 않는다"* 로 잡아 스테일 문서 **6곳**을 고쳤다(그중 하나는 아키텍처 문서의 **파일 트리에 삭제된 파일**). 🔵 **AC-3 bite 재현** — 탈출구를 *다른 키* 뒤로 되살리자 7건 중 **구조 케이스 1건만 RED**, 속성 기반 6건 전부 GREEN(INT-005 가 artist 쪽에서 잰 것과 동일). 배선: e2e 5서비스 + `MEMBERSHIP_SERVICE_URI`/`MEMBERSHIP_SERVICE_BASE_URL`(기본값이 이 네트워크에서 안 풀리는, INT-005 와 **같은 함정**) + ci/nightly 부트잘·서비스 목록·아티팩트. [[project_externalised_seam_with_no_counterpart]] [[feedback_guard_predicate_wrong_verify_the_artifact]] [[env_conditional_bean_default_must_be_the_safe_side]]

## done

- `TASK-FAN-INT-005-live-trio-has-no-iam-so-two-gates-ship-switchable.md` — **✅ DONE (2026-08-12, 3-dim verified — impl PR [#3278](https://github.com/kanggle/monorepo-lab/pull/3278) squash `8325acd2d`; state=MERGED · `origin/main` tip 일치 · 머지 전 failing **0** / 33 SUCCESS · 8 SKIPPED · `mergeStateStatus=CLEAN`) — live-trio 에 실제 iam 을 넣고 팔로우 게이트의 탈출구를 삭제했다.** 🔴 **AC-0 이 범위를 둘이 아니라 셋으로 쪼갰다**: artist 쪽만 이 티켓(iam+MySQL 이면 닫힌다) · membership 쪽은 [[TASK-FAN-INT-006]](서비스가 아니라 **데이터**가 비용 — ACTIVE 멤버십 행은 `POST /api/fan/memberships` + **빌링키(PortOne)** 로만 생긴다) · 공유 재사용 워크플로는 [[TASK-MONO-523]](root, 같은 PR). 🔴🔴 **읽기만으로 아낀 CI 왕복 3회**: auth-service 도커 빌드 컨텍스트는 서비스 디렉터리가 아니라 **프로젝트 루트** · ENTRYPOINT 가 `getent hosts mysql/kafka/redis` 로 **DNS 를 하드코딩해 JVM 실행 전에 블로킹**(트리오 별칭 `fan-e2e-*` ⇒ 영원히 대기, 증상은 **Spring 로그 0줄의 평범한 타임아웃**) · 프로덕션 이미지에 **SAS 서명키가 없다**(체크인된 건 `.example` 뿐, 실 키는 `src/test/resources/keys` 라 `bootJar` 가 안 담는다). 🔵 **두 토큰 평면은 설정만으로 분리**됐다(엔드유저=목 JWKS / 워크로드=실 iam, JWKS 병합 불필요) — 단 artist-service 가 `fanplatform.internal.jwt.*` 를 **yml 에 선언하지 않아** 형제 membership 이 문서화한 `INTERNAL_JWT_*` env 가 **아무것에도 닿지 않았다**(parity 추가). 🔴 **게이트를 끄고 있어서 틀린 값이 무해하게 앉아 있었다** — `ARTIST_SERVICE_BASE_URL` 기본값은 이 네트워크에서 해석 불가라 켜는 순간 전 팔로우 fail-closed 였을 값(같은 함정이 membership 쪽에 **그대로 살아 있다** → INT-006). 🔴🔴 **첫 CI 런의 RED 가 이 티켓의 최고 증거였다** — 팔로우 422. 배선 결함이 아니라 **게이트가 옳게 동작한 것**이고, `ArtistAndPostFlowE2ETest` 가 *합성 UUID* 를 팔로우하며 *"v1 has no enforcement that artistAccountId resolves to a real artist"* 라고 적어 둔 전제를 `FAN-BE-045` 가 **이미 무효화**했는데 **탈출구가 그 낡은 주석을 참인 것처럼 가려 왔다**. 판정은 **로그의 부재**로 확정(대리지표 아님) — checker 는 어떤 예외에도 WARN 을 남기는데 1,861줄에서 **0건** ⇒ 토큰·인증·왕복 전부 성공, `200 {exists:false}` 였다(즉 **AC-1 은 그 RED 런에서 이미 증명**). 수정 = 등록한 계정을 팔로우 + 🔴 **거부 케이스 신설**(통과만 단언하면 허용 checker 와 구별 불가). **AC-3 bite**: 다른 키 뒤로 허용 빈 주입 → **속성 기반 7건 전부 초록, 신설 구조 단언 1건만 RED**(복원 후 8/8) ⇒ 반전만으로는 못 잡는다. **AC-4**: e2e 잡 2m41s(n=3) → **3m25s**(n=2), 델타 ≈ +44s — 🔴 before 범위가 이미 49초 폭이라 **상수가 아니라 표본**으로 기록. boot-jars 잡은 jar 3→4 인데 측정 증가 **없음**(n=1 이라 '빨라졌다'고는 안 함). 최종 런 **4/4 PASSED · 0 skipped**, 같은 런에서 **201(등록됨) + 422(미등록)** 동시 성립 ⇒ 게이트가 *판별*한다는 강한 형태. [[project_guard_reachability_not_just_bite]] [[feedback_retract_the_exemption_when_the_defect_is_fixed]] [[env_authservice_entrypoint_hardcoded_dns_gate]]

- `TASK-FAN-BE-045-artist-posts-have-no-author-any-caller-can-be.md` — **✅ DONE (2026-08-11, 3-dim verified — impl PR #3270 squash `0414d4d03`; state=MERGED · `origin/main` tip 일치 · 머지 전 failing **0** / 41체크 · `mergeStateStatus=CLEAN`) — 아티스트에 계정을 주고(`artists.account_id`), 팔로우 대상을 fail-closed 로 검증한다.** `ADR-MONO-059` A(신원 평면) + `ADR-004` A(동기 internal 이음매). 계약을 코드보다 먼저 2회 랜딩(#3270 이전 2커밋). AC-1b V3 `account_id NOT NULL` + `UNIQUE(tenant_id, account_id)` · 항등 백필(시드가 조인 양쪽에 엔티티 id 를 쓰고 있어 다른 값이면 시드 자신의 팔로우가 거절된다) · 필수·불변(PATCH 불가) · IAM 실재검증 안 함(명시 결정). AC-6 artist `/internal/artists/exists` Order(1) 워크로드 체인 신설 + community `ArtistAccountChecker` → 422 `UNKNOWN_ARTIST_ACCOUNT`. 🔴 **워크로드 스코프를 `artist.read`(machine 계열)로 둔 것이 이 표면의 보안 성질 전부** — `fan-platform.artist.read` 는 **엔드유저** 스코프(V0030, 모든 팬의 로그인 토큰에 실린다)라 그걸로 키를 잡으면 로그인한 팬 전원이 `ROLE_INTERNAL` 을 얻는다. 크로스프로젝트: iam `V0032` 를 같은 PR 에 실음(스코프 없으면 SAS 가 `invalid_scope` → checker fail-closed → **모든 팔로우 거절**, 나눠 내면 main 이 그 상태로 머문다). 🔴🔴 **AC-7 답이 착수 노트와 반대로 뒤집혔다** — 1회차 근거("artist-service 는 live-trio 에 떠 있으니 탈출구 불필요")가 **틀린 것을 재고 있었다**: `FanPlatformE2ETestBase` 가 적은 부재 항목은 하나 더 있고(**iam — 워크로드 토큰 발급처**) 탈출구가 지키는 것은 피호출자가 아니라 **토큰**이다. ⇒ ADR 의 두 번째 선택지(**두되 기본값이 거부**)로 답하고, 배선을 `AlwaysAllowMembershipChecker` 의 **역방향**으로 두었다(허용 빈이 명시 조건, 진짜 checker 가 `@ConditionalOnMissingBean` 폴백) ⇒ 예상 못 한 설정은 전부 검증 ON. 🔴 **CI 가 로컬이 잡을 수 없던 것을 잡았다** — 로컬 `integrationTest` 는 rc=0 이지만 Docker 미기동으로 **전부 SKIPPED** 였고, CI 에서 핵심 4건이 RED: 픽스처 `"artist-acct-" + UUID`(48자)가 `@Size(max=36)` 에 걸려 **검증이 호출되지도 않았다**. 위험한 것은 실패가 아니라 **거절 케이스가 422 만 보면 그대로 통과할 수 있었다는 점** — 게이트에 대해 아무것도 증명하지 않으면서. 맨 UUID 로 교체 후 넷 다 실제 실행·PASSED. AC-2/AC-3 는 **한 메서드 안**(팔로우→발행→조인 두 끝 같음 명시 단언→팔로워 피드에 있음→비팔로워 피드에 없음, 글은 DB 에 여전히 존재). AC-6 fail-closed 는 **소켓을 실제로 닫고** 재고(성공 케이스와 유일한 차이가 소켓), 세 케이스 모두 *진짜 checker 가 물렸는지* 를 먼저 확인한다. AC-4 해당없음(B 배제) · **AC-5 는 안 옳긴다** — 남은 차단은 로그인 가능한 계정 + `ARTIST` 역할이고 **둘 다 [[TASK-MONO-512]]** 임을 시드 `--why` 에 기록. 부수적 발견: 프런트 `revalidatePath('/artists/${artistAccountId}')` 가 **계정 id 로 라우트 경로를 만들고 있었다**(라우트는 엔티티 id 키) — 데모에선 두 값이 같아 증상이 없다. 분리함. 🔵 사람 리뷰 없음 — 소유자 스탠딩 자동머지 승인(CI 완전 green)에 따른 self-merge 라 `review/` 를 거치지 않고 `ready/ → done/`.

- `TASK-FAN-FE-016-no-fan-post-compose-screen.md` — **✅ DONE (2026-08-05, 3-dim verified — impl PR #3217 squash `65851441b`; state=MERGED · `origin/main` tip 일치 · 머지 전 failing **0** / pass 14) — 팬 작성 화면 + 조회 경로 신설.** `/compose`(작성) · `/me/posts`(내 글) · 헤더 진입점 2개(**authed 분기 안에만**) + 백엔드 `GET /api/community/posts/mine`. 🔴🔴 **AC-0 이 스코프를 다시 잡았다** — 작성자 기준 조회가 **존재하지 않았다**(읽기는 `GET /{postId}` 와 팔로우 피드뿐). 작성 후 상세로 보내는 것만으로는 **AC-4 가 성립 불가**(시드된 글은 그렇게 못 본다) ⇒ 백엔드 목록 추가가 전제였다. 티켓의 "백엔드 추가가 전제면 스코프를 다시 잡을 것" 이 그대로 발동. 🔵 **두 공백이 서로를 가리고 있었다** — 쓸 수도 없고, 썼어도 찾을 수 없다(피드가 팔로우 기반이라 자기 글이 자기 피드에 안 뜬다). 작성만 붙였으면 두 번째 절반을 그대로 남겼을 것. **AC-2 결정 = UI 가 PUBLIC 고정**: 계약이 두 postType 모두에 세 값을 허용한다고 적고 있어 백엔드를 조이면 계약 변경이므로, 티켓의 Failure Scenario 가 지목한 UI 쪽에서 막고 **바디 전체를 단언**해 고정(부분 단언은 visibility 파라미터화를 통과시킨다). API 측 공백은 `TASK-FAN-BE-047` 로 분리 기재. 🔴 **로컬 3종(tsc·lint·vitest)이 전부 초록인데 `next build` 만 잡은 결함** — `'use server'` 파일은 **async 함수만 export** 가능한데 상수를 export 했다(→ `lib/post-limits.ts` 분리). **라이브 실측**: FAN_POST 행 **1→2**, 작성 후 상세에 제목·본문 렌더, `/me/posts` 에 방금 쓴 글 + **시드된 "첫 콘서트 후기"** 둘 다 보임(AC-4), DB 의 새 행 `visibility=PUBLIC` 확인. 🔵 **JPQL 은 유닛으로 증명 안 된다**(리포지터리가 목) ⇒ `MyPostsQueryIntegrationTest` 6건 신설 — 타인 글 부재·DELETED 제외/DRAFT·HIDDEN 유지·자기 PREMIUM 글 비검열·페이지네이션. **검증**: unit 167 / integrationTest 34 / 프런트 vitest 128, 실패 0 스킵 0, lint 0.  [[project_single_account_all_domain_demo]]

- `TASK-FAN-BE-046-feed-cache-serves-gated-content-after-membership-ends.md` — **✅ DONE (2026-08-05, 3-dim verified — impl PR #3215 squash `80420f1ad`; state=MERGED · `origin/main` tip 일치 · 머지 전 failing **0** / pass 13) — 피드 캐시가 자격 판정을 담지 않게 했다(B안).** 캐시는 `FeedItemSnapshot`(게시물 사실)만 담고 `locked`/`title`/`bodyPreview` 는 **나갈 때** 계산한다 — 히트/미스 경로 모두 같은 한 메서드가 판정. 🔵🔵 **A(이벤트 무효화)를 고르지 않은 이유 = 실패 방향**: 이벤트가 늦거나 유실되면 **fail-open**(유료 글이 열린 채 남는다), 재계산은 `MembershipChecker` 가 fail-closed 라 **fail-closed**. 이 저장소엔 이벤트가 조용히 도달하지 않은 전례(`TASK-MONO-511`)가 있다. 🔵 **BE-019 가 이미 같은 성질을 봤다** — "구버전으로 캐시된 `locked=false` 가 5분간 노출" 을 **배포 절차 노트**로 닫았다; 같은 기전이 배포 때만이 아니라 **자격이 바뀔 때마다** 작동한다는 것을 그때는 보지 않았다. **AC-0 재현**: detail 403 / feed `locked=false` + 제목·본문 200자 그대로(TTL 244초 잔여), redis DEL 하면 즉시 `locked=true`. **라이브 양방향(같은 키를 살려둔 채)**: 구독→즉시 열림(detail 200) / 해지→즉시 잠김(detail 403), 그동안 **TTL 이 296→292→278 로 단조 감소** — 엔트리를 지우거나 다시 쓴 적이 없다는 증거다. **AC-3**: 연속 5회 TTL 단조감소(전부 HIT) + 음성 대조로 키를 지우면 TTL 299 로 복귀(MISS 가 구별됨). 🔴 actuator 메트릭은 401 이라 **빈 값을 결과로 쓰지 않고** 술어를 TTL 단조성으로 바꿨다. **비용**: 게이트는 페이지당 **티어당 1회·최대 2회**(공개 글만 있으면 0회) — 종전 미스 경로는 **게시물당 1회**(페이지당 최대 50)였으므로 미스 경로는 오히려 싸진다. **키 버전 v2**: 옛 JSON 은 잠긴 항목의 title 이 이미 null 이라 조용히 잘못 읽힌다 ⇒ 도달 불가로 만들어 BE-019 가 남긴 **수동 flush 절차가 불필요**해졌다. IT 는 리터럴 복사 대신 `FeedCacheRepository.key(...)` 를 호출한다. **검증**: unit 158 / integrationTest 28, 실패 0 스킵 0. 음성 대조 2방향을 **실제로 실행** — 게이트 무력화(fail-open) → 11 RED(AC-1 포함), 항상 잠김 → 14 RED(AC-2·소유자 단축 포함). **AC-4 는 해당 없음**(A안 전용).  [[project_single_account_all_domain_demo]]

- `TASK-FAN-FE-015-membership-subscribe-blocked-without-demo-pg-switch.md` — **✅ DONE (2026-08-05, 3-dim verified — impl PR #3213 squash `896807f22`; state=MERGED · `origin/main` tip 일치 · 머지 전 failing **0** / pass 12) — 데모 결제 스위치를 팬에도 넣는다(런타임 조회).** 🔵🔵 **술어가 ecommerce 와 반대다** — 팬은 목이 기본(`MockPaymentGatewayAdapter` = `@Profile("!portone")`)이고 `portone` 이 opt-in 이라 불변식이 **“프런트 ON ⟺ portone OFF”**. BE-572 가드 (x) 를 복사하면 **정반대를 단언**한다 ⇒ 뒤집힌 술어로 `(x2)` 를 새로 씀. **형제 파리티는 같은 코드가 아니라 같은 성질이다.** 🔴🔴 **고치자 아래에서 두 번째 결함**: `crypto.randomUUID` 는 **secure context 전용**이라 평문 HTTP `*.local` 에선 아예 없다(`TypeError`). 잠복이었을 뿐 실 PortOne 경로도 HTTP 에선 같은 벽 — 키 가드가 먼저 리턴해 도달한 적이 없었을 뿐이다 ⇒ `randomUuid()`(getRandomValues v4). **티켓의 “막힌 건 사전 가드 하나뿐” 진단은 절반만 맞았다.** **AC-0 재현**: 변경 전 클릭 → 행 2→2 + “PortOne 키 미설정”, 그런데 화면엔 “모의 PG” 가 그대로 → **문구 술어였다면 거짓 통과**. **AC-1**: 행 **2→3**(PREMIUM/ACTIVE 생성). **AC-3/AC-4 음성 대조를 같은 이미지로**(env 만 바꿔 재기동): ON=모의PG문구 true·PortOne문구 false / OFF=정반대 ⇒ **표 자체가 런타임 조회의 증명**. 하드코딩 문구가 헤더 말고 `SubscribePanel` 에도 하나 더 있었다. **AC-5 네거티브 3종 전부 CAUGHT** — 🔴 그중 하나는 **계측기 결함으로 처음에 통과처럼 보였다**(`set -a; source demo.env` 가 커맨드라인 env 를 덮어씀 + 가드 (x) 가 먼저 죽어 (x2) 미실행). 115 테스트·lint·tsc 통과, 실키 0건. 🔴 범위 밖 기록: `projects/fan-platform` 에 **`.dockerignore` 부재** — 로컬 `pnpm install` 후 이미지 빌드가 Windows `node_modules` 복사로 깨진다(실측). impl PR #3213. [[feedback_measurement_needs_a_validity_predicate]]

- `TASK-FAN-FE-014-web-containerize-for-integrated-demo.md` — **✅ DONE (2026-08-05, 3-dim verified — impl PR #3195 squash `2586b455d`; state=MERGED · `origin/main` 포함 확인 · 머지 전 failing **0** / pass 17 · BE-573 과 한 PR)** — 팬 웹 컨테이너화 완료, 라이브 로그인 왕복 14/14 통과.** 통합 데모에 팬 고객 화면이 아예 없었다(형제 파리티 결손 — ecommerce `web-store` 는 이미 compose 서비스 + Traefik). **산출물**: `web/fan-platform-web/Dockerfile` 신설(멀티스테이지 + standalone, 컨텍스트=pnpm 워크스페이스 루트) + compose `fan-platform-web` 서비스(Traefik `Host(web.fan-platform.${DEMO_DOMAIN:-local})` · IPv4 헬스체크 · 256M 상한) + 루트 `infra/traefik` alias 1행 + **`TASK-BE-573`(iam) `V0031` 콜백 등록을 같은 atomic PR 로**. 🔴 **AC-0 판정이 태스크의 가정을 뒤집었다** — "런타임 시드가 데모 콜백을 등록하니 IAM 수정 대개 불필요" 는 **새 호스트명에 대해 거짓**(`seed-demo-domain.sh` 는 *이미 등록된* URI 의 `.local/` 을 치환할 뿐 `web.` 서브도메인을 만들지 못한다) → BE-573 분리 기재 후 구현. **`output:'standalone'` 은 옵트인**(`NEXT_OUTPUT_STANDALONE=1`) — 기존 주석의 "Windows 심볼릭 링크 제약" 주장을 **물려받지 않고 다시 쟀고 오늘도 참**이었다(플래그 ON → `EPERM: symlink`, rc=1 / OFF → rc=0). 무조건 켜면 컨테이너 이득을 로컬 빌드 회귀와 맞바꾼다. **라이브 실측**(15컨, `DEMO_DOMAIN=local`, `demo@demo.com`): 익명 307 게이팅 → next-auth signin → IAM authorize → 자격증명 → 콜백 → 세션(`tenantId=fan-platform` `roles=["FAN"]`, `__Secure-` 아님) → **SSR 5화면 전부 200**. 🔴 **"200 이면 됐다" 로 끝내지 않았다** — `FAN-FE-008` 회귀(로그인은 되는데 SSR 이 bearer 없이 나가 전 페이지 401)는 200 만으로 구분되지 않으므로, `/artists` 의 **ErrorState/EmptyState 문자열이 다르다**는 점을 이용해 **둘 다 부재**(= fetch 200 + `content.length>0`)를 단언했고 DB 에 `artists` 6행이 있다 — **아티스트 6명이 실제 SSR 렌더**. 가드: `verify-demo-wrapper.sh` 정적 20건 전량 + **(i) 가 무는 것 확인**(alias 제거 시 `web.fan-platform.local` 을 정확히 지목). **AC-6 메모리 45.0 MiB / 이미지 309 MB**(단일 표본·무시드). **부수 발견 3건(미수정)**: 🔴 **Flyway 는 주석 안의 달러-중괄호도 치환한다** — V0031 헤더 주석 하나가 auth-service 부팅을 막았고, **고치며 그 패턴을 설명하느라 두 번째로 죽었다**; `demo.env` 의 제네릭 `CORS_ALLOWED_ORIGINS` 가 팬 게이트웨이 기본값을 ecommerce 오리진으로 덮는다(현재는 무해 — 모든 fetch 가 server-only); 세션 `user.email` 이 `undefined`. **범위 밖**: 데모 데이터 시드(`MONO-506`), 브라우저 실렌더(hosts 에 `web.fan-platform.local` 추가는 관리자 필요 — `web.ecommerce.local` 도 현재 hosts 에 없다), AWS 도달(AMI 재굽기 전). 분석·구현=Opus 5. [[env_traefik_skips_unhealthy_and_ipv6_localhost]] [[env_next_public_inlined_at_build_time]]

- `TASK-FAN-BE-042-adr058-d7-event-dedupe-port-adoption.md` — **DONE (2026-08-01, 3-dim verified — impl PR #3154 squash `b75b63ff6`; state=MERGED · origin/main tip ancestor 확인 · 머지 전 실패 체크 0건).** ADR-MONO-058 § D7 (`EventDedupePort` sub-pattern only, per § 1.1's audit table) — notification-service (fan-platform's only event consumer) replaces its hand-rolled check-then-act `ProcessedEventStore`/`JpaProcessedEventStore` (`alreadyProcessed`/`markProcessed`, existence-pre-check-based) with the shared `libs/java-messaging.EventDedupePort` single-call `process(eventId, eventType, work)` contract (already a declared dependency — pure adoption gap, no new dependency). New `EventDedupePortJpaAdapter` implements the port via `INSERT … ON CONFLICT (event_id) DO NOTHING` over the existing `processed_events` table (PK-violation-safe duplicate detection, not existence-pre-check — closes the TOCTOU window the old shape had under concurrent redelivery), `@Transactional(propagation = MANDATORY)` so the dedupe row commits/rolls back atomically with the caller's `Notification` row. `HandleCommunityEventUseCase`/`HandleMembershipEventUseCase` convert `eventId` `String → UUID` at the boundary (both fan-platform producers mint it via `UuidV7.randomUuid()`, confirmed at the outbox publishers before assuming). Old `ProcessedEventStore`/`JpaProcessedEventStore` deleted (0 grep hits under `src/main`, 2 remaining javadoc historical-context mentions only). Test fixtures using non-UUID literals (e.g. `"evt-c1"`) converted via new deterministic `EventIds.uuid(label)` test helper (name-based UUIDv3) — same `UUID.fromString(...)` boundary real envelopes hit, not a synthetic-only path; `DlqRoutingIntegrationTest`'s poison-envelope literal intentionally left untouched (schemaVersion gate throws before the eventId ever reaches the use case). New real-Postgres `EventDedupePortJpaAdapterIntegrationTest`: atomicity-rollback (work throws mid-way → neither dedupe row nor `Notification` row persisted), redelivery no-op, and a genuine two-thread concurrent-race test (proves the TOCTOU fix, not just sequential-delivery parity). Guard mutation-check performed (hardcoded `inserted = 1` → 3/4 adapter unit tests RED, reverted, re-confirmed GREEN byte-identical). `ResilienceClientFactory` explicitly untouched (fan not in that sub-pattern's audit-table row). Test counts: unit/slice 120→126 (+6, additive only), integration 15→18 (+3, additive only), 0 fail/error/skip both lanes. `:notification-service:check` GREEN locally; `:notification-service:integrationTest` (Testcontainers) also GREEN locally against Docker Desktop on this host (18/18) — CI `Integration (fan-platform, Testcontainers)` remains authoritative per policy, to be confirmed on the PR. `TASK-MONO-495` 분할 산물. 분석=Sonnet 5(세션) / 구현=backend-engineer(sonnet).

- `TASK-FAN-BE-043-adr058-d3-pagination-carrier-adoption.md` — **DONE (2026-08-01, 3-dim verified — impl PR #3143 squash `48c71556a`; state=MERGED · origin/main tip ancestor 확인 · 머지 전 실패 체크 0건).** ADR-MONO-058 § D3 — community-service (collapsed two local shapes, `domain.post.PageResult` + `application.FeedPage`, into one `com.example.common.page.PageResult<FeedItemView>` usage at the `PostRepository` port boundary; `hasNext` convenience moved to the `FeedResponse.from(...)` call site), artist-service (`ArtistRepository.findPublishedDirectoryPage` now returns `PageResult<Artist>` instead of the local `DirectoryPage` record; `PageMeta`/`ArtistDirectoryController` untouched), and notification-service (`domain.notification.NotificationPage` replaced by `PageResult<Notification>`; `ApiEnvelope.ofList(...)` gained an additive `totalPages` `meta` field — the exact "some hand-rolled shapes omit `totalPages`" gap `§ D3` names by example) adopt `libs/java-common.PageResult` in place of their hand-rolled carriers. New contract `projects/fan-platform/specs/contracts/http/notification-api.md` documents the `totalPages` addition (no prior HTTP contract existed for this endpoint). `membership-service`/`gateway-service` re-verified untouched (zero diff — no hand-rolled pagination carrier exists there). `artist-api.md`/`community-api.md` left unedited (wire shape verified byte-identical by unmodified contract tests). Test counts unchanged (no `@Test` added/removed): community 149 unit + 28 integration, artist 149 unit + 15 integration, notification 120 unit + 15 integration, all 0 fail/error/skip. `./gradlew :community-service:check :artist-service:check :notification-service:check` GREEN locally; `integrationTest` (Testcontainers) also GREEN locally against Docker Desktop; CI `Integration (fan-platform, Testcontainers)` lane to be confirmed on the PR. `TASK-MONO-495` 분할 산물. 분석=Sonnet 5(세션) / 구현=backend-engineer(sonnet).

- `TASK-FAN-BE-044-adr058-d4-security-chain-assembly-adoption.md` — **DONE (2026-07-31, 3-dim verified — impl PR #3135 squash `cb2b33bb3`; state=MERGED · origin/main tip ancestor 확인 · 머지 전 실패 체크 0건).** ADR-MONO-058 § D4 (fan-platform only) — 4개 서블릿 서비스(community/artist/membership/notification)가 손으로 쓰던 `NimbusJwtDecoder`+validator 체인 조립과 `SecurityConfig`의 제네릭 tail(CSRF/session/authorize/resource-server 호출 순서)을 `libs/java-security-servlet`의 `ResourceServerChainAssembler`(선행 `TASK-MONO-500`, **`done` 직접 확인 후 착수**) 호출로 대체. **auto-config 아님** — 라이브러리에 실제 애노테이션 0건, `META-INF/spring/…imports` 부재를 grep으로 확인, 호출부는 각 서비스 자기 `@Configuration`. **`anyRequest()` tail은 서비스별 실측**(ADR이 fleet 14/19 `denyAll` vs 5/19 `authenticated`라 균일 가정 금지) — 4개 전부 `denyAll()`, 이제 `.anyRequestDenied()`로 명시. **session/CSRF posture도 편집 전 4개 전부 판독 → 완전 동일(divergence 0건)**, 강제 통일한 곳 없음. **검증 방식이 핵심**: 기존 `SliceTestSecurityConfig`는 프로덕션을 *닮은 자체 체인*을 선언해서 D4 리팩터가 뭘 바꿔도 초록으로 남음 → 신규 `SecurityChainAssemblySliceTest`(서비스별)는 **실제 프로덕션 `SecurityConfig` 빈 + 실제 `ServiceLevelOAuth2Config` validator 체인**을 import하고 서명 검증원만 로컬 RSA 키로 치환, **리팩터 전 트리에서 먼저 GREEN 확인 후 리팩터 뒤 무수정 재실행**(= "동작 불변"이 주장이 아니라 측정). 무토큰 401 / 교차테넌트 403 `TENANT_FORBIDDEN` / 미허용 issuer 401 / public path 무토큰 도달 / denyAll tail(유효토큰+미등록경로=403) / CSRF 비활성(405·422·400 — `CsrfFilter`가 낼 수 없는 상태코드) / STATELESS(`getSession(false)==null`). **membership 이중 체인**: `@Order(1)` `/internal/**` 체인·`WorkloadIdentityAuthoritiesConverter`·`internalJwtDecoder` **byte-unchanged**, 신규 `Ordering` 네스트 4케이스가 **상태코드가 아니라 메시지로** 응답 체인을 식별(두 체인의 401/403 문구가 다름 → swap이 조용히 통과 불가) + workload 토큰이 `ROLE_INTERNAL` 통과해 디스패처 도달하는 **양성 케이스**까지. D1(`TASK-FAN-BE-040`) 배선은 **재사용**: 각 체인의 `new ActorContextJwtAuthenticationConverter<>(ActorContext::new)` 식을 빌더 파라미터로 **이동만**, 컨버터 신규 생성·`…servlet.actor` 편집 0건. 테스트 community 140→149 / artist 137→149 / membership 139→153 / notification 111→120, **양쪽 0 fail·0 error·0 skip, 순수 additive**(삭제·약화 0건). 🔴 **자진 공개 2건**: (1) mutation-calibration(체인 약화 후 프로브 RED 확인) **미수행** — 프로덕션·테스트 전용 양쪽 다 auto-mode classifier가 하드 차단, 우회하지 않고 준수. 대신 프로브 2개는 내부 컨트롤 보유(같은 체인이 admitted-but-unmapped 요청에 404를 내는 것이 증명돼 있어 미등록 경로의 403은 인가 판정임 / `CsrfFilter`는 403만 낼 수 있음), session 프로브는 내부 컨트롤 없는 순수 characterization pin. (2) 공유 빌더가 `jwtDecoder(jwkSetUri)`로만 진입 가능해 **validator 체인 조립이 JWKS URI에 결합** — 손으로 쓴 사본은 안 읽던 필드라, 4개 `FanTenantGatePolicyTest.wiredConfig()`에 해당 필드 1줄씩 추가 필요(프로덕션 무영향: `@Value` 기본값 없음 → 미설정이면 이미 컨텍스트 실패). 보존한 기존 divergence 2건도 정리 안 하고 유지: artist의 access-denied 코드는 `FORBIDDEN`(형제 3개는 `PERMISSION_DENIED`), community/artist는 단일 `JwtDecoder` 빈 컨텍스트 해석 vs membership/notification은 `.jwtDecoder(...)` 명시 핀. wire-visible 변화 0건(계약·yml·build.gradle·컨트롤러·DTO 무편집), `gateway-service`(reactive) diff 비었고 `libs/` 무편집. 4개 `:check` 로컬 GREEN, CI `Integration (fan-platform, Testcontainers)`가 권위(PR에서 대기). `TASK-MONO-495` 분할 산물.

- `TASK-FAN-BE-041-adr058-d6-iam-client-credentials-token-provider-adoption.md` — **DONE (2026-07-31, 3-dim verified — impl PR #3123 squash `dfc8a6a70`; state=MERGED · origin/main tip ancestor 확인 · 머지 전 실패 체크 0건).** ADR-MONO-058 § D6 (fan-platform only) — deletes community-service's local `IamClientCredentialsTokenProvider` (the ADR's cited best-of-breed parameterized-`scope` shape, but still carrying platform-default-charset Basic-auth + no connect/read timeouts) and adopts the shared, already-fixed `libs/java-security` class (`TASK-MONO-501`, confirmed `done` before starting). `MembershipCheckerAutoConfig` gained a `@Bean` factory method constructing the shared POJO directly (it carries no `@Component`), threading community's four existing `iam.internal-client.*` keys through unchanged plus two new `connect-timeout-ms`/`read-timeout-ms` keys (default 2000/3000ms, matching the sibling `community.membership-service.*` convention already in the same file — no prior timeout value existed to preserve). New `CommunityIamTokenAcquisitionTest` (call-site-level, MockWebServer) proves the UTF-8 Basic-auth header + `scope=membership.read` URL-encoding for community's real config values (byte-equivalent to the deleted copy's request — the exact shape `TASK-FAN-BE-030` fixed after a live 403), plus zero-timeout construction rejection; `MembershipCheckerAutoConfigTest` gained a test proving the configured timeout property actually flows into the shared class's constructor (override to `0` fails context refresh). `HttpMembershipCheckerTest`'s fail-closed contract passes unmodified. Test count 138→140 (+2, additive only, 0 failures both sides). No `build.gradle` change (`libs:java-security` was already declared). `TASK-MONO-495` 분할 산물. 분석=Sonnet 5(세션) / 구현=backend-engineer(sonnet) 위임 + 리드 diff·CI 검증.
- `TASK-FAN-BE-040-adr058-d1-actor-jwt-claim-cluster.md` — **DONE (2026-07-31, 3-dim verified — impl PR #3109 squash `be15315442a638d356c36259b6ee01f5541636e2`; state=MERGED · origin/main tip 포함 확인 · 머지 전 실패 체크 0건).** ADR-MONO-058 § D1 (fan-platform only, ACCEPTED 2026-07-30) — 4개 서비스(community/artist/membership/notification)의 actor/JWT-claim 추출 클러스터(`ActorContextJwtAuthenticationConverter`, `ActorContextResolver`, `@CurrentActor` + argument-resolver)를 `libs/java-security-servlet`의 새 `…servlet.actor` 패키지로 승격. 4벌 메커니즘이 **byte-identical**(`git diff --no-index` 전 인접쌍 = package/import/javadoc만 차이)임을 선행 확인 — 동작 분기 0건. `ActorContext` 레코드와 그 편의 메서드(`isOperator`/`isAdmin`/`owns`/`hasRole`, `FAN_OPERATOR` 등 롤 리터럴)는 **서비스에 그대로 잔류**(§ D1 Ownership Rule); 공유측은 `ActorContextFactory<A>` 하나로만 서비스 타입을 받는다. **설계 결정**: 역할 집합이 `Set.copyOf`가 아닌 `Collections.unmodifiableSet(HashSet)` — `hasRole()`이 `roles.contains(role)`을 직접 호출하는데 `Set.copyOf(...).contains(null)`은 NPE를 던지고 `HashSet`은 `false`를 반환, "더 안전해 보이는" 선택이 실제로는 인증 경로 동작을 바꿨을 것. **검증**: 실 필터체인 + 실 RSA 서명 JWT로 서비스별 신규 `ActorContextAuthPathSliceTest` — array-form `roles`/delimited-string `role`(공백·콤마) 양쪽, `@CurrentActor` 바인딩, 무토큰 401, 교차테넌트 403, artist `hasAnyRole` 403, membership `/internal/**` 403/401. 테스트: community 130→138 / artist 129→137 / membership 130→139 / notification 104→111 / lib 35→77, 전부 신규 테스트만(0 failure). Mutation-check(ROLE_ 접두사 제거·문자열 split 제거)로 가드 물림 확인. 타 프로젝트(erp×4/finance×2/scm×4) `compileTestJava` 전부 GREEN, 관측 가능한 동작 변화 0건. **재시도 경위**: 최초 시도 600초 정체로 실패, 재개 후 API 연결 오류(ECONNRESET)로 재중단 — 두 경우 모두 worktree 변경사항은 온전히 보존되어 세션 리드가 상태 확인 후 재개 지시, artist-service의 중간 미완료 상태(옛 파일 미삭제)를 명시적으로 지적해 최종적으로 12개 파일(서비스당 3개) 전부 정상 삭제 확인. shared 경로(`libs/java-security-servlet`) 포함 원자 PR — TASK-FIN-BE-064/TASK-FAN-BE-038/039와 동일한 project-task 판단 기준 적용. 선행=BE-038(D2)·BE-039(D5) ✓. 분석=Sonnet 5(세션) / 구현=backend-engineer(opus) 위임 + 리드 diff·CI 3-dim 검증(재개 2회, 각 재개 전 worktree 상태 직접 확인).

- `TASK-FAN-BE-039-adr058-d5-public-paths-shared-value-type.md` — **DONE (2026-07-30, 3-dim verified — impl PR #3107 squash `f9d21ea5600427dc9870ed8830ccd8b873ed0d36`; state=MERGED · origin/main tip 포함 확인 · 머지 전 실패 체크 0건).** ADR-MONO-058 § D5 (fan-platform only) — `PublicPaths`의 `isPublic()` 매칭 메커니즘(EXACT/PREFIXES 컨테이너 + 매칭 로직)만 `libs/java-security-servlet.PublicPathSet`으로 승격, 실제 public 경로 데이터는 4개 서비스 각자 소유 그대로(정책 불변). 서비스별 before/after 경로 목록 byte-identical 확인(community/artist/notification `{health,info,prometheus}`+`{health/}`, membership은 추가로 `/webhooks/portone`). 서비스별 classification parity 테스트로 회귀 없음 검증. 테스트: community 127→130 / artist 126→129 / membership 127→130 / notification 101→104 / lib 24→35, 전부 신규 테스트만 추가(0 failure). shared 경로(`libs/java-security-servlet`) 포함 원자 PR — TASK-FIN-BE-064/TASK-FAN-BE-038과 동일한 project-task 판단 기준 적용. 분석=Sonnet 5(세션) / 구현=backend-engineer(sonnet) 위임 + 리드 diff·CI 3-dim 검증.

- `TASK-FAN-BE-038-adr058-d2-error-envelope-shared-handler-adoption.md` — **DONE (2026-07-30, 3-dim verified — impl PR #3105 squash `8fd1d484fba3e5efc28c86948cf63800e242b411`; state=MERGED · origin/main tip 포함 확인 · 머지 전 실패 체크 0건).** ADR-MONO-058 § D2 (fan-platform only): 4개 서비스(`community`/`artist`/`membership`/`notification`)가 각자 들고 있던 `AbstractDomainExceptionHandler` 사본(115~124줄 × 4, 그 중 7~9개 arm 이 공유 base 와 동등)을 `libs/java-web-servlet.CommonGlobalExceptionHandler` 상속으로 대체, `details` 없는 arm 은 `libs/java-web.ErrorResponse` 로 전환. **D2 유보 블로커 2건 해소**: (1) wire-shape — `ErrorResponse` 확장 **거부**(소비자 39파일/타 프로젝트 2개), `platform/error-handling.md` "permitted to extend" 조항 근거로 `details` 운반 arm 4개만 로컬 `ApiErrorBody` 유지(notification 은 0건 → 타입 삭제); (2) status — 공유 handler 에 `protected HttpStatus validationFailureStatus()`(default 400) 훅 추가, fan 만 422 override(iam 4개 소비자 무편집·무영향, 하위호환 테스트로 확인). 🔴 **AC-6 이 가정을 반증**: `Instant`→ISO-8601 은 Jackson/spring-web 기본값이 아니라 Boot `JacksonAutoConfiguration` 만의 것이고, artist 는 `RedisCacheConfig` 의 `ObjectMapper` 빈이 Boot 것을 밀어내 **에러 `timestamp` 를 숫자로 내보내고 있었음**(`platform/error-handling.md`·`artist-api.md`·FE 타입 전부 string 이라고 명시) → `ApiErrorBody.timestamp` 를 `String` 선포맷으로 변경해 mapper 무관하게 `ErrorResponse` 와 동일. 관측 변화 2건(500→400 missing-param/header/malformed; artist timestamp 숫자→ISO 문자열) 모두 **기존 계약 쪽으로의 수정**이라 contract 편집 없음. 로컬 GREEN — community 115→127 / artist 116→126 / membership 118→127 / notification 93→101 / lib 40→43, fail 0. iam account 502·auth 640·security 240 GREEN(무편집). shared 경로(`libs/java-web-servlet`) 포함이라 lib+4서비스 **원자 PR** — TASK-FIN-BE-064와 동일하게 project task로 진행(ADR-MONO-058 §5가 이미 execution scope를 ACCEPT에 포함, CI/diff 검토 후 세션 리드 병합 결정). **후속(미티켓)**: artist-service `RedisCacheConfig`의 ObjectMapper shadowing은 에러 envelope 외 다른 DTO/Redis 캐시 페이로드에도 영향 — 별도 범위(캐시 payload 포맷 변경 수반)라 이 task에서 미수정, follow-up 후보로 기록. 분석=Sonnet 5(세션) / 구현=backend-engineer(opus) 위임 + 리드 diff·CI 3-dim 검증.
- `TASK-FAN-BE-037-jwks-uri-default-path-mismatch.md` — **DONE (2026-07-30, impl PR #3045 squash `315052832`, 3-dim verified).** 5개 서비스(gateway/community/artist/membership/notification) 전부 `JWT_JWKS_URI` 기본값이 `http://iam.local/.well-known/jwks.json`인데, IAM `auth-service`의 실제 엔드포인트는 `/oauth2/jwks`(ecommerce/scm/erp/finance는 전부 올바름 — fan-platform만 straggler). fan-platform 자신의 스펙 내에서도 갈라져 있었음(`iam-integration.md`·`membership-service/dependencies.md`는 정답 `/oauth2/jwks`, `community`·`artist`·`gateway-service` 스펙은 오답 — 정답 쪽에 맞춰 정정). 테스트가 전부 `/.well-known/jwks.json`을 서빙하는 자체 mock으로 실제 IAM 배선을 우회해 지금까지 은폐됨. 스펙 3 + compose 5 + application.yml 5 + env.example 1 + mock server 6(membership 이중서빙 단일화 포함) + 단언 테스트 2 수정. `grep "well-known/jwks"` 0건(tasks/ 문서 제외) + 5개 서비스 `:check` 전부 GREEN, 테스트 개수 무회귀(gateway 39/community 107/artist 116/membership 118/notification 93). **3-dim**: (a) MERGED `315052832`; (b) origin/main tip 편입(ancestor); (c) pre-merge failing=0(전부 pass/skip, 0 fail/0 pending). 분석=Sonnet 5 / 구현=backend-engineer(sonnet) 위임 + 리드 diff 직접 재검증. [[project_fan_platform_local_bringup]]
- `TASK-FAN-BE-035-notification-envelope-parser-dedup.md` — **DONE (2026-07-29, impl PR #3032 squash `d45c7369`, 3-dim verified).** Extracted `EventEnvelope` (envelope preamble + schemaVersion gate) + `JsonFields` (`requireText`/`requireInt`/`optionalText`/`optionalTextArray`/`requireInstant`) as package-private static helpers in `application.consumer`, shared by `MembershipEventParser`/`CommunityEventParser` — collapses the ~47-of-125-line verbatim duplication `TASK-FAN-BE-026` created when `CommunityEventParser` was cloned from `MembershipEventParser`. Zero test-file diff (both parser tests construct via `new XEventParser(new ObjectMapper())`, unaffected); `notification-service:test` unchanged at 83 tests before/after; `grep -c 'Envelope is not a JSON object'` = 1 (single preamble occurrence); `architecture.md` § Package Layout updated. **3-dim**: (a) MERGED `d45c7369`; (b) origin/main tip 편입(ancestor, ff-synced); (c) pre-merge failing=0 — Build&Test·**Integration(fan-platform, Testcontainers)**·E2E(fan-platform live-trio)·Frontend lint/unit/E2E·Package boot jars·INDEX drift·Task ID collision 전부 GREEN (rebase-triggered rerun after a same-project INDEX.md merge conflict with BE-036, all green). 선행=BE-026 ✓. 후속=없음. 분석=Sonnet 5(세션) / 구현=backend-engineer(sonnet) 위임 + 리드 diff·CI 3-dim 검증.
- `TASK-FAN-BE-036-remove-dead-tenant-context.md` — **DONE (2026-07-29, impl PR #3031 squash `6f2bb961`, 3-dim verified).** Deleted the unreferenced `TenantContext` class from `notification-service`/`community-service`/`artist-service` (already removed from `membership-service` by an earlier sweep — these three were stragglers). Repo-wide grep confirmed zero live consumers of the class in any of the three services (only self-references within each file, plus an unrelated same-named test constant); `architecture.md` § Package Layout updated in all three. `notification-service:check`/`community-service:check`/`artist-service:check` GREEN, identical test counts (93/115/116), no test file modified. **3-dim**: (a) MERGED `6f2bb961`; (b) origin/main tip 편입(ancestor, ff-synced); (c) pre-merge failing=0 — Build&Test·**Integration(fan-platform, Testcontainers)**·E2E(fan-platform live-trio)·Frontend lint/unit/E2E·Package boot jars·INDEX drift·Task ID collision 전부 GREEN, first-pass. 선행=없음(standalone dead-code sweep). 후속=없음(`membership-service`는 이미 정리됨, 재작업 불필요). 분석=Sonnet 5(세션) / 구현=backend-engineer(sonnet) 위임 + 리드 grep·diff·CI 3-dim 검증.

- `TASK-FAN-BE-026-v2-notification-wiring-community-events.md` — **DONE (2026-07-29, 3-dim verified — impl PR #3026 squash `420fa564f`; state=MERGED · origin/main tip=ancestor · 머지 전 required 0 failing, incl. `Integration (fan-platform, Testcontainers)`+`E2E (fan-platform v1 live-trio smoke, Testcontainers)`).** `community.comment.added.v1`/`community.reaction.added.v1`에 라우팅 필드(`postAuthorAccountId`, `mentionedAccountIds`) 추가(additive) → notification-service `CommunityEventConsumer`가 REPLY/MENTION/REACTION_BADGE로 소비(no-sync-coupling 유지 — 신규 코드는 RestClient/WebClient/Feign 0건; AC-1 원문 grep 술어는 TASK-FAN-BE-016/017 의 이메일/푸시 어댑터로 이미 stale, 무관함을 diff 대조로 확인). `mentionedAccountIds`는 멘션 파싱/유저네임 디렉토리 부재로 항상 빈 배열 발행(계약 필드만 additive로 존재, 의도적 범위 제한 — 임의 파싱 로직 발명 안 함). `Notification` 스키마 V3 마이그레이션(membership_id nullable + post_id 컬럼 + CHECK 확장 + idempotency 유니크를 `(source_event_id, account_id, type)`로 확장 — 한 이벤트가 여러 수신자로 fan-out 가능해서 필요, 원 지시에 없던 에이전트의 자체 발견). **독립 검증**: 에이전트 diff를 계획과 라인 단위 대조(필드명·시그니처·자기알림 억제 로직) + community-service(115)/notification-service(93) 유닛·슬라이스 테스트 JUnit XML 직접 재집계(0 failures/errors/skips) + CI Linux Testcontainers 레인 GREEN(로컬 Windows Docker 미의존). 분석=Sonnet 5(세션) / 구현=backend-engineer(Opus dispatch)+독립 재검증.
- `TASK-FAN-BE-034-portone-profile-context-boot-test.md` — **DONE (2026-07-29, 3-dim verified — impl PR #3020 squash `ee8e87ff3`; state=MERGED · origin/main tip 편입(ancestor) · 머지 전 required 0 failing).** `PortOneProfileContextBootIntegrationTest` — `portone` 프로파일로 membership-service 전체 컨텍스트를 부팅만 하는 CI-gated 가드(기존 `Integration (fan-platform, Testcontainers)` 레인에 `@Tag("integration")`으로 자연 배선, 신규 CI YAML 불요). **첫 실행(무-mutation)에 실결함 발견+동일 PR 내 수정**: `SubscribeUseCase`/`RenewMembershipUseCase`/`WebhookReconcileUseCase`가 Lombok `@RequiredArgsConstructor` + FIELD-레벨 `@Qualifier("paymentGateway")`를 썼는데, Lombok이 그 애노테이션을 생성 생성자 파라미터로 복사하지 않음(javap 바이트코드로 확인 — `RuntimeVisibleParameterAnnotations` 0개) → fix-02가 "수정"했다고 기록한 4개 소비 유스케이스 중 3개가 실제로는 `portone` 하 여전히 unqualified였고, 이 가드의 무-mutation 첫 로컬 실행이 `RenewMembershipUseCase`에서 실제 `NoUniqueBeanDefinitionException`으로 즉시 RED. 3개 클래스를 명시적 생성자(파라미터-레벨 `@Qualifier`, 이미 올바르던 `AutoRenewMembershipsUseCase` 패턴과 동일)로 전환해 수정 — 동작 변화 없음, 순수 DI 배선. **AC-2 mutation-check(수정 후)**: `PaymentGatewayConfig.portOnePaymentGateway()`의 `@Qualifier("paymentGateway")`를 `@Qualifier("recurringBillingGateway")`로 역전 → 로컬 재현 RED(`NoSuchBeanDefinitionException` for `portOneRecurringBillingGateway`, mock 취소 후 즉시 GREEN 재확인, 커밋 안 됨). **로컬 검증**: `:membership-service:check`(unit+slice, Docker-free) GREEN, `:membership-service:integrationTest`(Testcontainers 전체 12클래스, 신규 가드 포함) 전부 GREEN(로컬 Windows Docker 자원경합으로 Kafka/Postgres 컨테이너 기동 타임아웃 3회 재시도 필요 — 인프라 노이즈, project memory `env_hyperv_socket_buffer_exhaustion_docker_churn`/`project_testcontainers_docker_desktop_blocker` 패턴과 일치, CI Linux 가 권위). 분석=Sonnet 5 / 구현=Sonnet 5.
- `TASK-FAN-BE-033-fix-02-portone-ambiguous-bean.md` — **DONE (2026-07-28, 라이브 검증으로 직접 확인 — 브라우저+실카드 결제 성공 → `memberships` 행 `payment_ref=pay-38912e68-...`, 실 PortOnePaymentAdapter 경로 확인, `pgmock_` 아님).** ADR-001 라이브 결제 검증(membership-service `portone` 프로파일 실기동) 중 발견: `PaymentGatewayConfig`의 두 `@Bean`이 같은 `PortOnePaymentAdapter` 인스턴스를 `PaymentGatewayPort`/`RecurringBillingGateway` 양쪽으로 노출 — Spring eager 타입매칭이 두 빈 다 양쪽 포트의 candidate로 판정해 `portone` 프로파일에서만 `NoUniqueBeanDefinitionException`(mock 프로파일은 단일-인터페이스 클래스라 무영향; 어떤 테스트도 `portone` 풀 컨텍스트를 기동 안 해 CI 미포착). Fix=안정적 `@Qualifier`("paymentGateway"/"recurringBillingGateway")를 mock/portone 양쪽 빈 + 4개 소비 유스케이스 생성자 파라미터에 동일 부여(7파일, 순수 DI 배선). `./gradlew :membership-service:test`(mock 프로파일) GREEN 확인. 분석·구현=Opus(라이브 검증 중 자체 발견·수정). [[project_fan_platform_portone_pg_integration]]
- `TASK-FAN-FE-013-billing-key-issuance-ui.md` — **DONE (2026-07-28, 3-dim verified — impl PR #3001 squash `5a17225cd`; state=MERGED · origin/main tip=ancestor · 머지 전 0 failing, 전체 vitest/lint/build 통과).** [ADR-002](../docs/adr/ADR-002-billing-key-auto-renewal.md) — **이로써 정기결제 전체 스택 완결**(ADR-MONO-057 lib → BE-033 백엔드 → FE-013 프런트). 빌링키 발급 UI(`AutoRenewToggle`) — `requestIssueBillingKey`(`portone-checkout.ts` 패턴 재사용, 구매자 identity 전달) → `enrollBillingKey`/`cancelBillingKeyEnrollment` 서버 액션(정정된 외부경로 `/api/v1/memberships/billing-key`, fix-01 이후). 백엔드 발급목록 조회(GET) 엔드포인트 없어 낙관적 로컬 상태 UI로 설계(새 엔드포인트 발명 안 함). `billingKey`는 단일 호출로 소비, 컴포넌트 상태·로그·응답에 절대 미보존. **독립 검증**: diff=fan-platform-web만 · `gatewayFetch` flat-body 래핑 실제 코드로 확인 · 경로/계약 일치 · tsc/vitest(105/105)/lint/build 전부 직접 재실행 GREEN. 분석=Opus / 구현=frontend-engineer(Opus dispatch)+독립 재검증. [[project_fan_platform_portone_pg_integration]]
- `TASK-FAN-BE-033-fix-01-gateway-path.md` — **DONE (2026-07-28, 3-dim verified — impl PR #2998 squash `b5f33221c`; state=MERGED · origin/main tip=ancestor · 머지 전 0 failing).** TASK-FAN-BE-033 fix — `BillingKeyController`가 단수 `/api/fan/membership/billing-key`로 매핑돼 게이트웨이(복수 `/api/v1/memberships/**`만 리라이트, TASK-FAN-BE-009)로 도달 불가했던 라우팅 결함. 복수 `/api/fan/memberships/billing-key`로 정정(게이트웨이 무변경으로 커버). `MembershipController` 기존 매핑과 충돌 없음 확인. IT+계약스펙 경로 동기화. TASK-FAN-FE-013 착수 전 자체 발견.

- `TASK-FAN-BE-033-billing-key-auto-renewal.md` — **DONE (2026-07-28, 3-dim verified — impl PR #2996 squash `59a07ab2f`; state=MERGED · origin/main tip=ancestor · 머지 전 0 failing — 전체 Build&Test·Integration(iam/ecommerce)·E2E·Frontend 통과, 무관 Maven-403 인프라 flake 1건 재실행 후 GREEN 확인).** [ADR-002](../docs/adr/ADR-002-billing-key-auto-renewal.md) — `BillingKeyEnrollment`(AES-GCM 암호화, `libs/payment-portone`의 `RecurringBillingGateway`+`PortOneWebhookVerifier` 소비) + 스케줄러(`AutoRenewMembershipsUseCase`) + 웹훅(`/webhooks/portone`, `PublicPaths` 추가) + 발급/해지 API. **핵심 설계 수정**: renew가 새 row를 만들 뿐 옛 row status를 안 바꾸므로, 후보선정은 flat WHERE 대신 `findActiveByAccount`+tier필터+max(validTo)로 "현재" 멤버십만 선택(무한 재갱신 버그 회피, 테스트로 고정). 실패정책: declined=자연 재시도(캘린더가 상한), ambiguous=`verify()` 재조정 후에만 갱신. `RenewMembershipUseCase` 완전 무변경. 마이그레이션에 부분 유니크 인덱스(활성 enrollment 티어당 1개, DB 레벨 레이스 방지). **독립 검증**: diff=fan-platform만 · 기존 회귀테스트(Renew/Subscribe/Slice) GREEN · 신규 테스트 9종 실제 실행 확인(XML 카운트 대조) · 모노레포 전체 컴파일 GREEN. CI가 실제로 잡은 결함 1건(`BILLING_KEY_ENROLLMENT_NOT_FOUND` 미등록 → `platform/error-handling.md` 등록 후 GREEN). 🔴 **잔여 위험(공개, 미차단)**: 응답유실+PG eventual-consistency 동시발생 시 드물게 이중청구 가능(연결테이블 없음, v1 범위 밖) — 테스트 PG 자금이라 실 피해 없음, 갱신은 최대 1회만 부여됨. 웹훅은 로컬에서 라이브 테스트 불가(공개 URL 없음). 분석=Opus / 구현=backend-engineer(Opus dispatch)+독립 재검증. [[project_fan_platform_portone_pg_integration]]

- `TASK-FAN-FE-012-portone-buyer-identity.md` — **DONE (2026-07-24, 3-dim verified — impl PR #2972 squash `8e08ffdb9`; state=MERGED · origin/main tip=ancestor · 머지 전 0 failing / Frontend unit·E2E·lint&build + fan Integration 통과).** fan-platform-web: PortOne `requestPayment`에 `customer`(구매자 이메일/이름/전화) 전달. 데모 채널=KG이니시스 V2 일반결제인데 FE-010/011 checkout이 `customer` 미전달 → SDK가 창 열기 전 거부("이니시스 V2 일반 결제의 경우 구매자 이메일은 필수 입력입니다"). 세션 실제 이메일(`FanSession.email`/`displayName`, OIDC email/profile scope)→`page.tsx`→`SubscribePanel`/`RenewPanel`→checkout `customer`(누락 시 데모 폴백, IAM email claim 부재에도 결제 안 막힘). thrown SDK 에러 시 PG 자체 메시지 표면화. 서버 검증경계(ADR-001) 불변=구매자 이메일은 status/amount/currency 재검증 대상 아님(client→PG only). vitest 98/98(신규 forwarding 2)+tsc+lint+build GREEN. 분석·구현=Opus 4.8. [[project_fan_platform_portone_pg_integration]]

- `TASK-FAN-BE-032-membership-tier-upgrade-proration.md` — **DONE (2026-07-24, 3-dim verified — impl PR #2968 squash `25a225702`; state=MERGED · origin/main tip=ancestor · 머지 전 0 failing / CI Integration(fan) 통과).** membership-service tier 가격 + MEMBERS_ONLY→PREMIUM 업그레이드 프로레이션(FE-011과 원자적). flat 9,900 → **tier별**(MEMBERS_ONLY 7,900/PREMIUM 17,900, `MembershipPricing`). 프리미엄 구독 시 활성 멤버스전용 보유=**업그레이드**: 멤버스 supersede(`SUPERSEDED_BY_UPGRADE`) + 일할 크레딧 차감(`17,900×M − 잔여일×7,900/30`, 0 floor; 0원=PG 스킵). `UpgradeQuoter` 하나가 `GET /upgrade-quote`와 subscribe charge를 **동일 공식**(클라 결제액=서버 재검증액). supersede+create+양 이벤트=1 TX. `UpgradeProration`(pure)·`SubscribeUseCaseTest` 업그레이드·**upgrade IT(supersede 실 DB 상태전이)=CI Integration 통과**. 분석·구현=Opus 4.8. [[project_fan_platform_portone_pg_integration]]
- `TASK-FAN-FE-011-membership-upgrade-quote-ui.md` — **DONE (2026-07-24, 3-dim verified — impl PR #2968 squash `25a225702`; state=MERGED · ancestor · 머지 전 0 failing).** fan-platform-web tier 금액 + 업그레이드 quote UI(BE-032와 원자적). flat `MONTHLY_CHARGE_KRW` → `TIER_MONTHLY_KRW`(7,900/17,900). 멤버스전용 보유 중 프리미엄 카드=**업그레이드**: `getUpgradeQuote`(useEffect)→크레딧 표시 + "프리미엄으로 업그레이드" CTA + 프로레이션 금액 결제창(0원=스킵). FE-009 상위집합 보존. vitest 96/96(업그레이드 포함)+tsc+lint+build GREEN. 분석·구현=Opus 4.8. [[project_fan_platform_portone_pg_integration]]

- `TASK-FAN-BE-031-portone-payment-adapter.md` — **DONE (2026-07-24, 3-dim verified — impl PR #2963 squash `22c4d7105`; state=MERGED · `22c4d7105` origin/main tip=ancestor · 머지 전 0 failing required).** membership-service 실 PG 어댑터(ADR-001 Phase 1, FE-010과 원자적 머지) — `PortOnePaymentAdapter`(`@Profile("portone")`) + PortOne REST 서버측 검증(`status==PAID` AND 결제금액==청구금액 변조가드 AND KRW, 클라이언트 성공신호 불신, 실패=fail-closed 422). mock은 `@Profile("!portone")` 기본값=CI/키없음 자동 폴백(실 PG 호출 없음). API 필드 `paymentToken→paymentId`(DTO/command/controller/contract), 포트 파라미터 `paymentReference` 중립화(subscribe+renew 공유, 도메인·유스케이스 불변). API secret=env only(portone 하 fail-fast), 미커밋·미CI. **로컬**: `compileJava`+`compileTestJava` + `PortOnePaymentAdapterTest`(MockWebServer fail-closed 7케이스+auth 헤더) GREEN. **CI Integration(fan-platform) Testcontainers 통과=권위**(paymentId JSON·rename 검증). 분석·구현=Opus 4.8. [[project_fan_platform_seed_recipe_and_two_gaps]]

- `TASK-FAN-FE-010-portone-checkout-window.md` — **DONE (2026-07-24, 3-dim verified — impl PR #2963 squash `22c4d7105`; state=MERGED · ancestor · 머지 전 0 failing required).** fan-platform-web `@portone/browser-sdk@^0.1.9` 실 결제창(ADR-001 Phase 1, BE-031과 원자적) — "결제 토큰" 필드 제거, "카드로 결제"→`requestPortOnePayment()`(공유 헬퍼=subscribe+renew)로 **실 PortOne 결제창**→`paymentId`를 검증 액션에 전달. `totalAmount=9900*planMonths`(백엔드 flat charge 일치=서버 금액검증 통과). `NEXT_PUBLIC_PORTONE_*`(빌드타임 인라인, 반공개; 시크릿 브라우저 미유출). FE-009 상위집합 단언 보존. **로컬**: vitest 95/95(SDK mock: cancel→미호출·decline inline)+tsc+lint+`next build` GREEN. **결제창 배선 검증**: 키와 재빌드 시 실 Store ID가 번들 인라인 확인(=결제창 실 키 초기화). 🔴 잔여(사용자): 브라우저 실 테스트결제·API secret 회전·backend portone 프로파일 전환·ADR-001 ACCEPT 승격. 분석·구현=Opus 4.8. [[project_fan_platform_seed_recipe_and_two_gaps]]

- `TASK-FAN-FE-009-tier-superset-subscribe-ui.md` — **DONE (2026-07-24, 3-dim verified — impl PR #2959 squash `dde47309c`; state=MERGED · `dde47309c` origin/main 편입(ancestor) · 머지 전 0 failing required).** 티어 상위집합 UI — `PREMIUM ⊇ MEMBERS_ONLY`(membership `AccessPolicy.tierGrants`)인데 구독 UI가 프리미엄 보유자에게도 MEMBERS_ONLY "구독하기"를 라이브로 제공 → 아무것도 안 주는 중복 구매+중복 멤버십 행. Fix=`SubscribePanel`이 PREMIUM 보유 시 MEMBERS_ONLY 카드에 "프리미엄에 포함됨" 비활성 표기; 역방향(MEMBERS_ONLY 보유→PREMIUM 제공)은 업그레이드 경로로 보존. 순수 프런트(백엔드·계약·access-policy 무변경). vitest 3케이스(`[]`/`['PREMIUM']`/`['MEMBERS_ONLY']`)+tsc+lint green; `heldActiveTiers` prop에 완전 결정적이라 라이브 전용 실패모드 없음. 2026-07-24 데모 UX 리뷰 중 발굴. 분석·구현=Opus 4.8. [[project_fan_platform_seed_recipe_and_two_gaps]]

- `TASK-FAN-FE-008-server-session-access-token.md` — **DONE (2026-07-24, 3-dim verified — impl PR #2951 squash `d72830d7b`; state=MERGED · `d72830d7b` origin/main 편입(ancestor) · 머지 전 10 pass / 0 failing required).** 🔴 fan-platform-web의 잠복 제품버그 — 모든 Server-Component 데이터 fetch가 **bearer 없이** 게이트웨이로 나가 401 → 전 페이지 "불러올 수 없습니다" 배너. 원인=`session.ts getFanSession()`이 **세션 객체**에서 토큰을 읽는데 `sessionCallback`은 F2로 **토큰을 세션에서 제거**(유닛테스트가 `accessToken undefined` 단언) → 항상 null → 14페이지가 `getX(null,…)`. Fix=`getToken()`(next-auth/jwt)으로 **raw 세션 JWT에서 직접** 디코드(F2 유지, 세션엔 여전히 토큰 없음) + 순수 `selectAccessToken` 추출·유닛테스트 4건. 브라우저 로그인이 지금껏 완주된 적 없어 잠복. **라이브 검증**: headless next-auth 로그인→SSR `/artists`·`/`가 실제 아티스트·피드 렌더(배너 사라짐). 🔴 verify-seed(직접 토큰)는 200이었으나 실 브라우저 SSR 경로는 깨져 있던 것 — **라이브만 포착**. green-wash 회피=세션에 토큰 붙이는 손쉬운 오답(테스트된 F2 약화) 대신 raw JWT 읽기. 분석·구현=Opus 4.8. [[project_fan_platform_seed_recipe_and_two_gaps]]

- `TASK-FAN-BE-030-community-requests-membership-scope.md` — **DONE (2026-07-24, 3-dim verified — impl PR #2945 squash `d0446f2f1`; state=MERGED · `d0446f2f1` origin/main 편입(ancestor) · 머지 전 13 pass / 0 failing required).** BE-029 완결 후속 — **post-merge 라이브 검증**이 BE-029 단독으론 데모가 여전히 403임을 노출. community `IamClientCredentialsTokenProvider`가 `grant_type=client_credentials`만 보내고 **scope를 요청 안 함** → SAS가 scope 클레임을 아예 안 붙임(라이브: no-scope→`scope=undefined`, `&scope=membership.read`→`["membership.read"]`) → 실제 community 토큰에 `membership.read`가 없어 BE-029 converter가 정당하게 거부. Fix=요청 본문에 `scope=membership.read` 추가(config `iam.internal-client.scope`, default, URL-encoded) + 테스트 2곳 교정. **라이브 E2E 검증**: membership+community 재빌드·재배포 후 멤버→PREMIUM 게시물 **200**(이전 403), `/internal` `{"allowed":true}`. 🔴 **green-wash 교훈**=BE-029 교정 테스트가 scope 토큰을 발급해 converter만 통과시켰으나 실제 community 요청과 불일치 → CI green인데 미작동, **라이브 검증만이 포착**(feedback_guard_predicate_wrong_verify_the_artifact 계열). 분석·구현=Opus 4.8. [[project_fan_platform_seed_recipe_and_two_gaps]]

- `TASK-FAN-BE-029-membership-workload-identity-positive-discriminator.md` — **DONE (2026-07-24, 3-dim verified — impl PR #2939 squash `4147c389a`; state=MERGED · `4147c389a` origin/main 편입(ancestor) · 머지 전 13 pass / 0 failing required).** membership `/internal` workload-identity 결함 — `WorkloadIdentityAuthoritiesConverter`가 **tenant_id 있으면 거부**(부정 판별자)했으나 실제 IAM cc 토큰은 tenant-scoped라 `tenant_id="fan-platform"` 운반(TenantClaimTokenCustomizer fail-closed 스탬프, jwt-standard-claims.md가 모든 grant에 tenant_id 강제) → **모든 멤버의 MEMBERS_ONLY/PREMIUM 읽기 403**(프로덕션 잠복; 테스트가 tenant_id 없는 가짜 토큰으로 green-wash). Fix=**긍정 판별자**(필수 scope `membership.read`, tenant_id 무관; `security-rules.md` "exactly one of subject/scope" + order-service `SystemClientSubjectValidator` 선례). 테스트 4파일을 실제 IAM shape(tenant_id 있음)으로 교정 — **fan Integration Testcontainers CI GREEN 2m17s가 권위**(교정된 AccessCheck/InternalAuth IT가 실제 토큰 shape 검증; 로컬 gradle은 호스트 자원고갈 JVM 크래시로 불가). ⚠️ 라이브 데모 멤버→200은 membership 재빌드+재배포 필요(호스트 여유 시). 라이브 진단이 **INT-004 전제(로컬 env 배선) 반증** → INT-004 supersede(done). 분석·구현=Opus 4.8. [[project_fan_platform_seed_recipe_and_two_gaps]]

- `TASK-FAN-INT-004-local-s2s-membership-workload-identity-wiring.md` — **⛔ SUPERSEDED (2026-07-24) by TASK-FAN-BE-029.** 라이브 진단으로 전제 반증: "로컬 docker-compose S2S 자격증명 미프로비저닝"이 아니라(자격증명 정상·토큰 발급됨), 실제 원인은 **토큰-shape 계약 충돌**(IAM cc 토큰이 tenant_id 운반 vs membership converter가 tenant_id 있으면 거부). 수신측 제품 결함=BE-029로 수정, 로컬 오버레이 변경 불필요. 미구현 종결(no impl PR).

- `TASK-FAN-BE-028-artist-directory-null-query-cast.md` — **DONE (2026-07-24, 3-dim verified — impl PR #2933 squash `2418c112e`; state=MERGED · `2418c112e` origin/main 편입(ancestor) · 머지 전 13 pass / 0 failing required).** artist 디렉터리 `GET /api/v1/artists`가 `q` 없이 호출되면 500(기본 "browse all" 랜딩이 도달=프로덕션 결함) — untyped `:q`가 null일 때 PostgreSQL이 `LOWER(('%'||?||'%'))` 서브트리를 못 typing해 `lower(bytea)`로 해석(42883). Fix=`ArtistJpaRepository.searchPublished`의 `:q` **두 occurrence 모두** `CAST(:q AS string)`(Hibernate가 named param 2회 바인딩=IS NULL 가드+CONCAT, 둘 다 캐스팅 필요; wms 형제 관용구). non-null `q` 경로 byte-보존. 신규 `ArtistDirectoryBrowseIntegrationTest`(PostgreSQL Testcontainers)가 null-`q`/null-`q`+type/non-null-`q` 3경로 잠금 — **fan Integration Testcontainers CI 레인 GREEN 2m27s가 권위**(Docker-less Windows SKIP, H2는 버그 마스킹). 2026-07-24 로컬 데모 시드 중 발굴. ⚠️ 인접 잠복 쌍둥이=wms `AdminUserJpaRepository.java`(별건 티켓). 분석·구현=Opus 4.8. [[project_fan_platform_seed_recipe_and_two_gaps]]

- `TASK-FAN-BE-027-refactor-spec-fan-polish.md` — **DONE 2026-07-22 (3-dim verified — impl PR #2879 squash `7e1fa2120`; 전 PR MERGED · main 편입 · 머지 전 required 0 failing, done/).** refactor-spec fan artist 이벤트계약 타이틀 정규화(`# artist-events (artist-service Kafka contract)`→`# artist-events — Kafka contract`, community/fan-membership 형제 정렬); 29 스펙 dead-ref 0. HTTP 3타이틀 세트 일관·eventId JSON 예시 내 한글 상태주석은 report-only. 분석=Opus 4.8.
- `TASK-FAN-BE-025-current-actor-authorship-channel-dedup.md` — **DONE (2026-07-22, impl PR #2865 squash `d92d7af38`; 3-dim verified — state=MERGED · origin/main tip 일치 · 머지 직전 0 failing required).** N1 `@CurrentActor` 인자 리졸버(서비스별 self-registering `WebMvcConfigurer`, 각 서비스 `currentOrThrow()` 위임)로 호출부 31× 대체(artist 12/community 12/membership 5/notification 2) — 401/403 실패경로 byte-보존. N2 `ActorContext.owns(authorAccountId)` 로 authorship 6× 통합(PublishPost role-check 제외). N3 `ChannelDeliveryMetrics` 로 채널 METRIC+recording 4× 단일화. 4서비스 compile GREEN + slice/adapter/use-case 테스트 무변경 GREEN(리졸버가 @WebMvcTest slice 에서 등록 확인). 분석·구현=Opus 4.8.

- `TASK-FAN-BE-024-outbox-publisher-classname-doc-drift.md` — **DONE (2026-07-21 audit round-2, merged #2848).** 은퇴한 v1 `*OutboxPollingScheduler` 클래스명을 v2 `*OutboxPublisher` 로 정정 — event contracts 2건 + service architecture Forbidden-Dependencies 절 3건 (doc-only).
- `TASK-FAN-BE-023-notification-inbox-shape-conformance.md` — **DONE (2026-06-28, 3-dim verified)**. ADR-MONO-043 **P2/fan** conformance — **REST §1 shape (Axis 1)** only: `NotificationResponse` += `sourceDomain="fan"` (aggregator attribution) + nullable `deepLink` (NON_NULL omit); `status`(UNREAD/READ) + `membershipId` extensions preserved (contract §1.2, normative read signal = `read` boolean). Controller += normative `unread` boolean query param (contract §2.1) mapping onto the status enum (existing `status=` kept for back-compat, used when `unread` absent). Unblocks P3 aggregator. **Lib engine/dedupe adoption NOT pursued** (same finding as ERP-BE-027 — wms-derived lib not net-zero across domains; engine stays fan-owned, ADR-038 M3 precedent). Net-zero for CI (additive JSON + additive query param). `:notification-service:test` GREEN (slice asserts sourceDomain + deepLink-absent + unread→UNREAD mapping); Integration(fan-platform, Testcontainers) authoritative. 분석=Opus 4.8 / 구현=Opus 직접. [[ADR-MONO-043]]
- `TASK-FAN-BE-022-artist-service-outbox-v2-migration.md` — **DONE (2026-06-27, 3-dim verified — impl PR #2001 squash `1c7b70570`)**. artist-service dual-axis v1→v2 (Postgres). `V2 artist_outbox` (`extends OutboxRowEntity`, UUID PK, partial pending index; KEEP-auto-config — v1 `outbox`/`processed_events` retained for lib EntityScan validate). artist was ALREADY ports/adapters — the `ArtistEventPublisher` port kept UNCHANGED, `ArtistEventPublisherAdapter` rewritten (drops `extends BaseEventPublisher`, keeps the write-side `artist_registered_total` counter + the conditional `reason`/`debutDate` omissions, builds the canonical 7-field envelope byte-identical). `ArtistOutboxPublisher extends AbstractOutboxPublisher` (6-topic resolve, wrapped `artist_outbox_publish_failures_total`, pending gauge); v1 scheduler deleted; `OutboxConfig` adds TransactionTemplate (Clock present). Relay stays ACTIVE (@EnableScheduling already on); no @ConditionalOnProperty gate (v1 had none). **3-dim**: (a) MERGED+`1c7b70570`; (b) origin/main tip 일치; (c) pre-merge failing=0 (Integration(fan-platform, Testcontainers) GREEN). 분석=Opus 4.8 / 구현=backend-engineer(Opus dispatch) + dispatcher 직접 wire 검증.
- `TASK-FAN-BE-021-community-service-outbox-v2-migration.md` — **DONE (2026-06-27, 3-dim verified — impl PR #2001 squash `1c7b70570`)**. community-service dual-axis v1→v2 (Postgres, same pattern). `V2 community_outbox`; `CommunityEventPublisher` → port + `OutboxCommunityEventPublisher` adapter (4 events, `base()` helper + envelope byte-identical); `CommunityOutboxPublisher extends AbstractOutboxPublisher` (4-topic resolve, wrapped `community_outbox_publish_failures_total`, gauge); v1 scheduler deleted; OutboxConfig adds TransactionTemplate. CI IT lane (authoritative): `OutboxRelayIntegrationTest` + `CommunityServiceIntegrationTest`/base re-pointed v1 `outbox`/lib repo → `community_outbox`/`CommunityOutboxJpaRepository` (+truncate). **3-dim**: (a) MERGED+`1c7b70570`; (b) tip 일치; (c) pre-merge failing=0 (fan Testcontainers GREEN). 분석=Opus 4.8 / 구현=backend-engineer(Opus) + dispatcher 검증.
- `TASK-FAN-BE-020-membership-service-outbox-v2-migration.md` — **DONE (2026-06-27, 3-dim verified — impl PR #2001 squash `1c7b70570`)**. membership-service dual-axis v1→v2 (Postgres, same pattern). `V3 membership_outbox`; `MembershipEventPublisher` → port + `OutboxMembershipEventPublisher` adapter (3 events, envelope byte-identical); `MembershipOutboxPublisher extends AbstractOutboxPublisher` (3-topic resolve, wrapped `membership_outbox_publish_failures_total`, gauge); v1 scheduler deleted; OutboxConfig adds TransactionTemplate. **JpaConfig @EntityScan EXTENDED** with the `infrastructure.jpa` outbox package (payment §27 — entity must be scanned or the IT boot fails). CI IT lane (authoritative): the relay IT + 4 lifecycle ITs (Subscribe/Cancel/ExpirySweep/PaymentDecline + base) re-pointed lib `OutboxJpaRepository`/v1 `outbox` → `MembershipOutboxJpaRepository`/`membership_outbox` (+truncate). **3-dim**: (a) MERGED+`1c7b70570`; (b) tip 일치; (c) pre-merge failing=0 (fan Testcontainers GREEN — validated the re-pointed ITs + v2 schema). 분석=Opus 4.8 / 구현=backend-engineer(Opus) + dispatcher 검증. ADR-MONO-004 §6 reconcile deferred to a single end-of-series pass (iam remains).
- `TASK-FAN-BE-011-community-artist-it-ci-wiring.md` — **DONE (2026-06-09, 3-dim verified — impl PR #1209 squash `3cb8aafb`)**. community-service + artist-service `integrationTest` 를 `fan-integration-tests` CI job 에 배선 — per-service `@SpringBootTest` Testcontainers 스위트가 **CI 첫 실행**. FAN-BE-010 직접 follow-up(그 게이트 IT 가 로컬로만 증명됐던 이유 해소). **why latent**: `fan-platform-e2e` job 은 별도 `:tests:e2e:e2eSmokeTest` 모듈(FAN-INT-001)을 돌릴 뿐 per-service 스위트 아님 + `:check`=Docker-free + `disabledWithoutDocker=true`→Docker-less dev 호스트서 SKIP=실행이력 0(memory §19/§20). 분석=Opus 4.8 / 구현=Opus 4.8(로컬 IT 먼저 돌려 전체 실패 surface 파악 후 fix→CI 배선). **노출+수정(test-infra only, production code 0)**: (a) **community §19c** — `CommunityServiceIntegrationBase` 에 `JdbcTemplate truncateAll()`(outbox/processed_events 포함 TRUNCATE), `CommunityServiceIntegrationTest`+`OutboxRelayIntegrationTest` cleanup `outboxJpaRepository.deleteAll()`(no default tx→`TransactionRequiredException`)→`truncateAll()`(§19a static start 는 FAN-BE-010 서 기착); (b) **artist §19a** — `ArtistServiceIntegrationBase` 컨테이너 start `@BeforeAll`→`static {}` 블록("Mapped port" fix); (c) **artist YAML** — `application-test.yml` `artist.cache.directory.namespace` 값(콜론 다수+말미 콜론)을 quote — unquoted plain scalar 가 `ScannerException: mapping values are not allowed here` 로 context-load 실패(test-profile 전용 파일이라 IT 미실행 동안 미파싱 잠복; main application.yml 은 `${...}` 래핑이라 파싱됨). artist ITs 는 cleanup 불요(고유 데이터 자연격리)라 truncateAll 미추가. **3-dim**: (a) MERGED+`3cb8aafb`; (b) origin/main tip 일치; (c) pre-merge failing required=0(**Integration(fan-platform) Testcontainers pass 1m34s = community 7 IT + artist 8 IT + membership 전부 CI 첫 green**; Build&Test pass 2m40s). 선택 staging(데모 working-tree 무오염). 로컬 사전검증: 세 스위트 1-invocation BUILD SUCCESSFUL. **메타: §20(b) 실증 — disabledWithoutDocker IT 를 CI 첫 투입 시 1 cycle 안에 §19a/§19c + YAML latent 3종 동시 노출, 로컬-먼저-진단으로 CI RED 0 회 달성.** 멤버십 슬라이스 부수 테스트부채 청산. 사용자 "FAN-BE-011" 선택. [[feedback_spring_boot_diagnostic_patterns]] [[project_fan_platform_v1_complete]] [[project_testcontainers_docker_desktop_blocker]]
- `TASK-FAN-FE-003-membership-subscribe-gate-ui.md` — **DONE (2026-06-09, 3-dim verified — impl PR #1207 squash `95798df6`)**. fan-platform-web 멤버십 구독/취소/상태 UI — 정적 `/membership` 안내 페이지를 membership-service(FAN-BE-009) 백킹 실동작 화면으로 전환(gateway `/api/v1/memberships/**`). **수직 슬라이스 사용자-가시 표면 완성**(백엔드 leg 008→009→010, post-gate 403 ErrorState 는 기존). 분석=Opus 4.8 / 구현=Opus 4.8 + 독립 재검증. **산출(13 파일)**: `entities/membership`(Membership/Tier/Status/ListItem read-time `active`) + `features/membership/api`(getMemberships server-only + currentActive; **actions `'use server'`**: `subscribe(tier,planMonths,paymentToken)` 서버측 `Idempotency-Key`(randomUUID) 생성 + **discriminated result 반환**[422 PAYMENT_DECLINED/MEMBERSHIP_TIER_INVALID 를 throw 아닌 `{ok:false}` → 클라이언트 inline 렌더, auth/transport 는 throw], `cancelMembership(id)` 404/already-canceled=멱등 no-op, 양쪽 revalidatePath) + `features/membership/ui`(SubscribePanel `'use client'` 티어카드+결제토큰 demo[`tok_decline` 거절]+plan-months; MembershipStatusCard confirm-gated 해지) + `app/(main)/membership/page.tsx` Server Component(현재상태+패널, `?tier=` 하이라이트) + posts/[id] 게이트 ErrorState deep-link `/membership?tier=<requiredTier>`(`details.requiredTier` 판독) + `shared/api/client.ts` `RequestOptions.headers`(Idempotency-Key). **보안 경계**: accessToken 서버측 유지(server-only read + `'use server'` write, 클라이언트 컴포넌트는 plain data만) — `next build` 가 `/membership`=동적 SSR 2.52 kB client JS 로 확인(토큰 번들 미유출). **3-dim**: (a) MERGED+`95798df6`; (b) origin/main tip 일치; (c) pre-merge failing required=0(**Frontend lint&build pass 2m24s + Frontend unit pass 3m49s(vitest 33/33, 신규 멤버십 6) + Frontend E2E smoke pass 3m42s** = MONO-166 게이트 3종 green). 로컬 사전검증: tsc --noEmit clean · next lint clean · vitest 33/33 · next build OK. 선택 staging(데모 working-tree 무오염). **잔여 slice 후속**: 멤버십 히스토리 페이지·notification-bell(notification v2)·FAN-BE-011(community IT→CI) — 본 task 범위 밖. 사용자 "FAN-FE 멤버십 구독/게이트 UI" 선택. [[project_fan_platform_v1_complete]] [[project_platform_console_adr_013]] [[project_monorepo_template_strategy]]
- `TASK-FAN-BE-010-community-http-membership-checker.md` — **DONE (2026-06-09, 3-dim verified — impl PR #1205 squash `7ac26e6a`)**. community-service `HttpMembershipChecker` 어댑터 교체 — FAN-BE-009 가 가동한 membership-service internal access-check 를 실제 호출해 v1 `AlwaysAllowMembershipChecker` stub 대체 → `MEMBERS_ONLY`+**`PREMIUM`** 게이트 실강제. **수직 슬라이스 백엔드 leg 완성**(spec 008 → impl 009 → **어댑터 교체 010**). 분석=Opus 4.8 / 구현=Opus 4.8(직접, cross-service 인증·fail-closed) + 독립 재검증. **adapter**: `IamClientCredentialsTokenProvider`(hand-rolled client_credentials, admin-service ADR-005 패턴 — `RestClient`+Jackson, lazy+cached refresh-skew; 사전시드 `community-service-client` IAM V0009 scope `membership.read`) + `HttpMembershipChecker`(`GET /internal/membership/access?accountId&tier&tenantId` → `allowed` verbatim on 2xx, **fail-closed**=timeout/connection-error/non-2xx/malformed→false; Bearer=RestClient interceptor, 토큰획득 실패→catch→deny). **bean 결정성(§19)**: `MembershipCheckerAutoConfig` 에서 real `@Bean` **먼저** 선언 + AlwaysAllow stub **나중** `@ConditionalOnMissingBean` → 단일 `@Configuration` 내 top-to-bottom 순서 보장으로 stub 항상 백오프(컴포넌트-스캔 빈 대상 `@ConditionalOnMissingBean` 비결정성 회피; stub 은 inert fallback escape-hatch 로 보존, fail-OPEN 시 WARN). **PREMIUM 하드닝**: `PostAccessGuard` PREMIUM always-pass+TODO 제거 → 동일 checker 로 hard fail-close(required tier=PREMIUM, 티어위계 PREMIUM⊇MEMBERS_ONLY=membership-service 서버측 `AccessPolicy` 해소; author/operator 는 HTTP 호출 전 short-circuit). config: `community.membership-service.*`+`iam.internal-client.*` 안전기본값. **수신측 인프라 완비 확인**: membership `WorkloadIdentityAuthoritiesConverter` 가 client_credentials 토큰(tenant_id 없음+scope/client_id)→ROLE_INTERNAL(FAN-BE-009 AccessCheckIT 증명). **3-dim**: (a) MERGED+`7ac26e6a`; (b) origin/main tip 일치; (c) pre-merge failing required=0(**Build & Test pass 2m23s** = community-service:check 신규 단위테스트 포함; Integration(fan-platform) pass 50s — membership IT 무영향). 선택 staging(데모 working-tree 무오염). **tests**: `HttpMembershipCheckerTest`+`IamClientCredentialsTokenProviderTest`(MockWebServer, Docker-free `:check` — allowed verbatim·500/401/malformed/connection-error fail-closed·Bearer+Basic-auth+cache/refresh) + `PostAccessGuardTest`(PREMIUM deny/allow/author-bypass) + `MembershipGateIntegrationTest` `premium_v1AlwaysPasses`→**`premium_denyForNonMember`**(403 MEMBERSHIP_REQUIRED). **§19 base fix**: `CommunityServiceIntegrationBase` 컨테이너 start `@BeforeAll`→`static {}` 블록(`@DynamicPropertySource` context-load 가 `@BeforeAll` 보다 먼저 → Mapped-port 실패; `disabledWithoutDocker=true` 라 그간 Windows 네이티브서 SKIP=실행이력 0) → 게이트 IT 가 **실제로 실행**됨(로컬 Docker 3/3 PASS). **⚠️ follow-up FAN-BE-011**: `community-service:integrationTest`(7 IT class) 를 `fan-integration-tests` CI job 에 배선 — 첫 CI 투입 surface 가 본 task 범위 밖(OutboxRelayIT 등이 §19 outbox-deleteAll TransactionRequiredException 가능 → JdbcTemplate truncate 동반 필요); FAN-BE-010 adapter+하드닝 로직은 `:check` 단위로 CI-gated. 사용자 "FAN-BE-010 community 어댑터 교체" 선택. [[project_fan_platform_v1_complete]] [[project_adr005_workload_identity_complete]] [[feedback_spring_boot_diagnostic_patterns]] [[project_monorepo_template_strategy]]
- `TASK-FAN-BE-009-membership-service-implementation.md` — **DONE (2026-06-09, 3-dim verified — impl PR #1203 squash `674c244e`)**. membership-service v2 **구현** — FAN-BE-008 spec(architecture.md SoT) 의 신규 서비스 부트스트랩(92 파일). Layered + 명시적 상태기계(community/artist 컨벤션), Postgres `fanplatform_membership`, Kafka outbox. community-service 가 stub(`AlwaysAllowMembershipChecker`)으로 두던 게이트의 실강제 백엔드 가동(어댑터 교체=FAN-BE-010). 분석=Opus 4.8 / 구현=backend-engineer(Opus dispatch)+dispatcher 독립 재검증. **domain**: `Membership` aggregate(상태기계 ACTIVE/CANCELED via `MembershipStateMachine`, read-time 만료, 티어위계 PREMIUM⊇MEMBERS_ONLY via `AccessPolicy`) + 결정적 `PaymentGatewayPort` mock(`tok_decline` sentinel) + idempotency. **api**: subscribe/cancel/list/detail(Idempotency-Key T1, `{data,meta}`) + **internal `GET /internal/membership/access`**(community `MembershipChecker.hasAccess(accountId,tier,tenantId)→boolean` 와 1:1, fail-closed=deny-as-200). **security**: 2 ordered chain — Order(1) `/internal/**` workload-identity(client_credentials JWT, ROLE_INTERNAL; end-user→403/no-token→401, ADR-005) + Order(2) `/api/fan/**` tenant 3-layer. **outbox**: `fan.membership.activated/canceled.v1`(유일 producer 경로); `expired.v1` forward-declared(read-time 만료, scheduler 없음). **V1**: memberships(tier/status CHECK §16, micros TIMESTAMPTZ §15) + idempotency_keys + outbox + processed_events; ddl-auto=validate, Postgres-only. **인프라**: settings/package.json/Dockerfile/docker-compose+`fanplatform_membership` DB/gateway route(`/api/v1/memberships/**`→`/api/fan/memberships/**`, `/internal/**` 미노출)/**신규 `fan-integration-tests` Testcontainers CI job**(pure-positive `fan` 필터). **3-dim**: (a) MERGED+`674c244e`; (b) origin/main tip=local HEAD 일치; (c) pre-merge failing required=0(**Integration(fan-platform) Testcontainers pass 1m33s** = 8 IT: subscribe→ACTIVE→cancel/decline→422-no-row/멱등/access 티어위계·만료·취소·cross-tenant·DB-down fail-closed/multi-tenant/outbox-relay/internal-auth 401·403/health; Build&Test GREEN). 선택 staging(데모 working-tree 무오염). **⚠️ fan 최초 전용 Testcontainers IT job 이 3 latent 패턴 연쇄 적발(모두 §6/§14 — Docker-free `:check` GREEN 뒤 숨음, IT 만 적발) → 3 fix**: (1) IT base 컨테이너 `@BeforeEach` start(=`@DynamicPropertySource` context-load 보다 늦음)→`static {}` 블록 start(`6673afbe`, CI-green erp read-model 패턴); (2) `MockPaymentGatewayAdapter` `@Component`+`@ConditionalOnMissingBean`(컴포넌트-스캔 빈에선 비신뢰→포트 미등록)→plain `@Component`(`35640c2f`); (3) IT cleanup `outboxJpaRepository.deleteAll()`(java-messaging `OutboxJpaConfig enableDefaultTransactions=false`→tx 없이 `em.merge` 실패)→base `JdbcTemplate.truncateAll()`(`ca3d308b`, masterdata 패턴, 싱글톤 컨테이너 cross-class idempotency 누수 동시 해소). 메타: §15/§16 data-model 선반영했으나 부트스트랩 latent(컨테이너 lifecycle·DI·repo-tx)는 spec 으로 못 막음 — 전용 IT job 이 첫 실행에서 노출. **수직 슬라이스 2/3**(spec 008 → **impl 009** → community 어댑터 교체 010 → FE). 사용자 "FAN-BE-009 구현" 선택. [[project_fan_platform_v1_complete]] [[project_adr005_workload_identity_complete]] [[feedback_spring_boot_diagnostic_patterns]] [[project_testcontainers_docker_desktop_blocker]] [[project_monorepo_template_strategy]]
- `TASK-FAN-BE-008-membership-service-spec.md` — **DONE (2026-06-06, 3-dim verified — spec PR #1147 squash `8b371c88`)**. membership-service v2 **bootstrap SPEC/design** — fan-platform PROJECT.md § Service Map v2 가 forward-declare 한 `membership-service`(rest-api, 멤버십/구독/프리미엄 접근제어 PG mock)의 아키텍처+계약 확정. community-service `MEMBERS_ONLY`/`PREMIUM` 티어가 현재 `AlwaysAllowMembershipChecker`(항상 통과+WARN) stub 으로 미강제 → 이 spec 이 실강제의 설계 토대. **spec/docs only(production code 0, 신규 ADR 0** — fan-platform 컨벤션: service architecture.md 가 결정 담지, PROJECT.md § v2 = forward-declaration 권위). 분석=Opus 4.8 / 설계=architect(Opus dispatch)+dispatcher 독립 재검증. 산출: `specs/services/membership-service/{architecture,overview,data-model,dependencies,observability}.md`(Layered+명시적 상태기계: subscribe→ACTIVE→cancel→CANCELED + **read-time 만료**[delegation `isActiveAt` 선례], **티어 위계 PREMIUM⊇MEMBERS_ONLY**, PG mock 결정적 경계[`tok_decline` 거절 sentinel], tenant 3-layer, outbox) + `contracts/http/membership-api.md`(4 public[subscribe/cancel/list/detail, Idempotency-Key, PG mock] + **1 internal** `GET /internal/membership/access` → community `MembershipChecker.hasAccess(accountId,tier,tenantId)→boolean` 와 **1:1**[fail-closed, deny=200 `{allowed:false}`, GAP client_credentials workload identity ADR-005, gateway 미노출]) + `contracts/events/fan-membership-events.md`(`fan.membership.activated/canceled.v1` outbox, notification v2 consumer forward; `expired.v1` forward-declared 미발행[read-time 만료, scheduler 없음]) + PROJECT.md § v2/gateway routing(`/api/v1/memberships/**`→`/api/fan/memberships/**`, community 컨벤션 일관)/iam-integration 노트. **3-dim**: (a) MERGED+`8b371c88`; (b) origin/main tip=local HEAD 일치; (c) docs-lane failing required=0(changes path-filter, code gate 미해당). dispatcher 독립 재검증: internal access-check ↔ MembershipChecker 포트 1:1(파라미터/fail-closed/deny-as-200) · gateway RewritePath community/artist 일관 · 11 파일 전부 markdown(production code 0) · §15(micros clock)+§16(CHECK allow-list) 가드 data-model 선반영. **메타: 신규 서비스=spec 먼저**(HARDSTOP-09/source-of-truth; erp spec-PR→impl-PR 선례 동형). 3-task 수직 슬라이스: **spec FAN-BE-008(이 task) → impl FAN-BE-009 → community `HttpMembershipChecker` 어댑터 교체 FAN-BE-010**; 멤버십 게이트 UI=FAN-FE 후속; notification-service(membership 이벤트 소비)=PROJECT § v2 별건. 사용자 "fan membership-service v2"(비-erp 다양화) 선택. [[project_fan_platform_v1_complete]] [[project_adr005_workload_identity_complete]] [[project_monorepo_template_strategy]]
- `TASK-FAN-FE-002-rp-initiated-logout.md` — **DONE (2026-06-01)**. fan-platform-web RP-initiated OIDC logout (GAP `end_session`) — 콘솔(PC-FE-033) 동일 silent-relogin 결함 패리티. id_token 캡처(jwt callback) + `federated-logout.ts`(server-only `getToken`→GAP `/connect/logout` URL, post_logout_redirect_uri=app origin+`/` exact-match V0011) + Header server-action `signOut({redirectTo: endSession ?? '/login'})`. **depends on BE-328**(매퍼가 post-logout→RegisteredClient 복사 → V0011 fan URI effective). tsc+lint clean, CI green. impl PR #1000 squash `158253bf`(fan+ecommerce 묶음). 3차원 ✓. **AC-1 라이브 브라우저는 후속 스모크**(잔여 리스크 낮음: GAP 수락 BE-328 일반증명 + null→local-only fallback; 사용자 (A) 결정). 분석=Opus 4.8 / 구현=Opus.
- `TASK-FAN-BE-007-e2e-path-and-oauth-client-name-drift.md` — **REVIEWED → approved** (2026-05-16, `/review-task` single-task, review-checklist Spec/Arch/Quality/Security PASS · Perf N/A · Testing PASS via task Verification; fix task 0). impl `task/spec-drift-cohort-2026-05-16` (spec-only, GAP V0011 read-only, no `apps/`). **WI-1 (F16)**: `v1-e2e-scenarios.md` step 1/2 `/api/v1/artist/artists`·`/api/v1/artist/artists/{id}/status` off-by-one → canonical `/api/v1/artists`·`/api/v1/artists/{id}/status` (artist-api.md L16-19 + gateway-service/architecture.md L146-149 route 표 = authority; 두 step 모두 positive valid-call 201/200 확인 — Edge Case negative-test 아님). **Edge-Case 편집 제외**: `gateway-service/architecture.md:152` 의 `/api/v1/artist/**` 는 회피한 catch-all glob 을 설명하는 prose (route 표는 이미 canonical) — task Scope("e2e doc 가 유일 edit target") + Edge Case + Failure Scenario("정당한 다른 endpoint blind replace 금지") 에 따라 미편집·문서화. AC grep 의도 "(no extra-segment off-by-one form remains)" 충족. **WI-2 (F21)**: `fan-platform-web/architecture.md:150` `fan-platform-realm-internal-services-client` → `fan-platform-internal-services-client` (client_id 확인, Keycloak realm 아님; GAP V0011 seed L84 = canonical; iam-integration.md:48/103 이미 정합). `grep realm-internal-services-client`=0, GAP/apps diff 0, dead-ref 0. 분석=Opus 4.7 / 구현=Opus 4.7 / 리뷰=Opus 4.7.

- `TASK-FAN-BE-006-gateway-and-web-overview-skeleton.md` — spec PR #466 (squash `0fff6dc1`) + impl PR #467 (squash `bf83b4a7`) 머지 (2026-05-14). **`/refactor-spec all --dry-run` (2026-05-13~14) fan-platform audit critical #1+2 finding closure** — 4 service 중 sibling 2 (artist-service + community-service) 는 `overview.md` 보유, gateway-service + fan-platform-web 미존재 → sibling 7-section skeleton 답습으로 2 file 신규 authoring. **gateway-service/overview.md** (~60 line) = edge gateway role (rest-api, Layered, Spring Cloud Gateway reactive) + 4 RewritePath routes table (TASK-FAN-BE-005) + 6 key invariants (JWT validation 강제, tenant fail-closed, stateless, fail-open rate limit 등). **fan-platform-web/overview.md** (~80 line) = frontend-app (FSD lite, Next.js 15 App Router + next-auth v5 PKCE + TanStack Query) + 7-page table (5 pages + login + next-auth handler) + 6 key invariants (HttpOnly cookie only, server-only accessToken via `getFanSession()`, single fetch boundary, tenant locked, MEMBERS_ONLY v1 stub, cross-feature isolation). content source = 각 service architecture.md 의 Identity + Role + Routes + Style Rationale 발췌. 4 file / +132 / -10 (2 신규 overview + task spec lifecycle + INDEX). production code = 0. **Impl PR CI = 15 SKIP + 1 changes PASS** (markdown-only path-filter, TASK-MONO-075 자연 검증). lifecycle = ready → review 직접 (in-progress 우회, mechanical skeleton fill single-PR closure 패턴 3번째 적용 — TASK-BE-281 / TASK-MONO-084 precedent). Sibling 답습 패턴 = TASK-MONO-083 / TASK-BE-280/281 / TASK-SCM-BE-011 / TASK-MONO-084 (모두 same-day single-PR closure, 본 task 가 6번째 entry). 분석=Opus 4.7 / 구현=Opus 4.7.

- `TASK-FAN-BE-001-gateway-service-bootstrap.md` — gateway-service Spring Boot 부트스트랩 (OIDC + Traefik). PR #115 머지 2026-05-03 (review fix 포함: JWKS startup probe + FailOpenRateLimiter narrowing).
- `TASK-FAN-BE-002-community-service-bootstrap.md` — community-service Spring Boot 부트스트랩 (Layered + post 상태 기계 + outbox). PR #116 머지 2026-05-03 (review fix 8건 포함: Critical status_changed 이벤트, FeedCache read-through, etc.). follow-up: TASK-FAN-BE-004 (prometheus rate-limit), TASK-MONO-025 (UUID v7 마이그레이션).
- `TASK-FAN-BE-003-artist-service-bootstrap.md` — artist-service Spring Boot 부트스트랩 (Hexagonal ports/adapters + outbox). PR #125 머지 2026-05-03 (review fix 6건 포함: 인덱스 tenant_id prefix, FANDOM POST/PATCH 분리, error envelope 일관, ARTIST_ARCHIVED 정책 문서화, AddRole 전용 enum, outbox 테스트 race fix).
- `TASK-FAN-BE-004-prometheus-rate-limit.md` — gateway 의 `/actuator/prometheus` 네트워크 격리 (option c). PR #128 머지 2026-05-03. spec 정정 + 신규 ops guide + 통합 테스트 2건. follow-up: Prometheus 컨테이너 docker-compose 추가 시 `fan-platform-net` join 필요.
- `TASK-FAN-BE-005-gateway-rewrite-path-fix.md` — gateway `application.yml` 에 4 routes (community + artists + artist-groups + fandoms) `RewritePath` 추가. 외부 `/api/v1/...` → 내부 `/api/...` 매핑. 5 통합 테스트. PR #136 머지 2026-05-03. follow-up: PR #131 머지 후 e2e SPRING_APPLICATION_JSON 우회 제거 cleanup.
- `TASK-FAN-INT-001-v1-services-e2e.md` — fan-platform v1 3 backend service (gateway/community/artist) cross-service E2E 스위트. JUnit 5 + Testcontainers (postgres/redis/kafka/wiremock JWKS) + 3 시나리오 (artist+post happy path / multi-tenant isolation / visibility tier). 새 CI job `fan-platform-e2e`. SPRING_APPLICATION_JSON 우회는 production 측 FAN-BE-005 fix 후 cleanup PR 로 정리. PR #131 머지 2026-05-03.
- `TASK-FAN-FE-001-frontend-bootstrap.md` — fan-platform-web Next.js 15 App Router + Tailwind + next-auth v5 + GAP OIDC PKCE. 4 페이지 + 27 vitest + 4 playwright smoke 통과. FSD lite 아키텍처. CI 3 job 확장 (frontend-checks/unit-tests/e2e-smoke). OIDC client 는 TASK-MONO-026 으로 등록됨. PR #132 머지 2026-05-03.

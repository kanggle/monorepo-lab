# Tasks Index — finance-platform

This document defines task lifecycle, naming, and move rules for the **finance-platform** project. Repo-root [tasks/INDEX.md](../../../tasks/INDEX.md) covers monorepo-level (cross-project) tasks; this file covers finance-platform-internal tasks only.

---

# Lifecycle

backlog → ready → in-progress → review → done → archive

Only tasks in `ready/` may be implemented.

---

# Task Types

- `TASK-FIN-BE-XXX`: backend (Spring Boot service implementations)
- `TASK-FIN-INT-XXX`: cross-service integration / E2E (Testcontainers · Docker compose)
- `TASK-FIN-FE-XXX`: frontend — declared for future use, finance v1 is backend-only (platform console renders finance per ADR-MONO-013 §3.3)

---

# Move Rules

## backlog → ready
Allowed only when:
- related specs exist (`specs/services/<service>/architecture.md`, `specs/contracts/...`)
- related contracts are identified
- acceptance criteria are clear
- task template is complete

## ready → in-progress
Allowed only when implementation starts.

## in-progress → review
Allowed only when:
- implementation is complete
- tests are added
- contract / spec updates are completed if required

## review → done
Allowed only after review approval.

### Review Rules
- Tasks in `review/` must not be re-implemented directly.
- If a review reveals a bug or missing requirement, create a new fix task in `ready/` referencing the original task.
- Fix tasks must include the original task ID in their Goal section (e.g. "Fix issue found in TASK-FIN-BE-001").
- Do not modify a task file after it moves to `review/` or `done/`.

### PR Separation Rule (lifecycle ↔ PR boundary)

Each lifecycle transition lands in its own PR. **Never bundle task spec authoring with implementation in the same PR.**

| Stage | Recommended PR shape |
|---|---|
| `(writing) → ready` | **spec PR** — adds the task file to `ready/` + updates this `INDEX.md` ready list. No implementation code. |
| `ready → in-progress → review` | **impl PR** — moves the task file through `in-progress/` to `review/` and lands the implementation. Lifecycle moves and impl commits should be separate commits but live in one PR. |
| `review → done` | **chore PR** — moves merged task file(s) from `review/` to `done/` + updates the `INDEX.md` done list. May batch multiple merged tasks. |

The repo-root [tasks/INDEX.md](../../../tasks/INDEX.md) is the authoritative definition. This summary applies the same rule at the project level.

## done → archive
Allowed when no further active change is expected.

---

# Rule

Tasks must not be implemented from `backlog/`, `in-progress/`, `review/`, `done/`, or `archive/`.

---

# Task List

## backlog

(empty)

## ready

(empty)

## in-progress

(empty)

## review

(empty)

## done

- `TASK-FIN-BE-053-converter-test-structure-refactor.md` — **DONE (2026-07-18, 3-dim verified — impl PR #2674 squash `f9a2f1b51`; 머지 전 전부 SUCCESS/0 failing — Build & Test (JDK 21, Linux) + **Integration (finance-platform, Testcontainers) SUCCESS** + Error code registry; `state=MERGED`).** **[test-only, 프로덕션·커버리지 무변경 — behaviour-preserving 구조 리팩토링]** 두 finance `ActorContextJwtAuthenticationConverterTest`(account+ledger) 를 대칭 리팩토링: `// ----` 섹션 주석 → JUnit5 `@Nested` 5그룹(ScopeLifting/EntitlementTrust/SuperAdminWildcard/ConvertGuards/RoleShapes, 리포트 그룹화) · guard 테스트 인라인 빌더 중복 → 공유 `base()` 헬퍼(header+고정 timestamp, sub/tenant 없음) · `Instant.now()` → 고정 `IAT`/`EXP`(컨버터가 만료 미검증) · `VIEWER_ROLE`/`SUPERADMIN_READ_ROLE` static import · 클래스 Javadoc 4개 관심사로 갱신. **각 테스트 여전히 15개, 0 skipped/failures/errors** — 이제 그룹당 5개 리포트로 출력. **의도적 배제**: 모듈 간 dedup(컨버터=의도적 per-service 중복, test만 공유는 비대칭 — `feedback_repo_knows_what_it_does_not_say`) / 파라미터화(보안-속성 DisplayName 명료성 우선). 사용자 "파일 내부 정리" 선택. 분석=Opus 4.8. [[feedback_repo_knows_what_it_does_not_say]] [[project_console_web_godfile_split_series]]
- `TASK-FIN-BE-052-converter-error-path-and-role-shape-test-parity.md` — **DONE (2026-07-18, 3-dim verified — bundled impl PR #2671 squash `a1a64d95d`; 머지 전 전부 SUCCESS/0 failing — Build & Test (JDK 21, Linux) + **Integration (finance-platform, Testcontainers) SUCCESS** + Error code registry; `state=MERGED`).** **[test-only, 프로덕션 무변경 — FIN-BE-051 의 out-of-scope 노트가 명명한 잔여 갭]** 두 finance `ActorContextJwtAuthenticationConverter` 테스트가 `convert()` 가드절과 `extractRoles` 두 shape 를 0건 커버 → 양 서비스(account+ledger)에 4개씩 추가: `sub` 누락→`IllegalStateException`(메시지 단언으로 sub 가드 고정, tenant_id 존재시킴) · `tenant_id` 누락→ISE(sub 존재) · `role` 단수 alias fallback 분기 · `roles` 구분자 문자열→다중 `ROLE_*` split 분기. 각 컨버터 테스트 11→15, XML `skipped=0 failures=0 errors=0`. FIN-BE-051 과 같은 브랜치 직렬화(양쪽 account 테스트 수정→stacked-PR 0-checks 회피). 분석=Opus 4.8. [[project_enforcement_straggler_sibling_parity]]
- `TASK-FIN-BE-051-account-converter-scope-shape-test-parity.md` — **DONE (2026-07-18, 3-dim verified — bundled impl PR #2671 squash `a1a64d95d`; 머지 전 전부 SUCCESS/0 failing — Build & Test (JDK 21) + **Integration (finance-platform, Testcontainers) SUCCESS**; `state=MERGED`).** **[test-only, 프로덕션 무변경 — 형제 parity 갭 (test 축)]** account `extractScopes` 는 3 shape(JSON array / RFC-6749 공백구분 String / `scp` alias)를 받는데 account 테스트는 array 만 검증, ledger 형제는 이미 3개 다 커버 → account 가 자기 코드의 `String.split`·`scp` fallback 분기를 미검증. `spaceDelimitedScopeLifted` + `scpFallbackLifted` 추가로 account 를 ledger 와 동급으로. account 테스트 9→11. **최근 FIN-BE-046~049 컨버터 작업의 단위테스트 리뷰에서 발굴** — "N-1 배선 형제 straggler" 패턴이 test 축에도 성립. 분석=Opus 4.8. [[project_enforcement_straggler_sibling_parity]] [[feedback_recount_population_dont_inherit_scope]]
- `TASK-FIN-BE-050-superadmin-wildcard-read-sibling-parity-audit.md` — **DONE (2026-07-18, 감사 완료 + 발굴 straggler 전부 수정/CI확정 — 조사 티켓).** **[FIN-BE-049 의 후속 sibling-parity 감사 — super-admin wildcard READ 축을 전 도메인에 전수 대조]** finance 를 고친 뒤 "형제 straggler 를 남겼는가"를 물어 erp/scm/ecommerce(전부 `.allowSuperAdminWildcard()` 로 layer-1 wildcard 를 여는 도메인)의 layer-2 READ 게이트를 감사. **판정**: **scm=FINE**(3서비스 read gate 가 `.authenticated()` — read 층을 조인 적 없어 wildcard 통과, **반증 수확**) · **erp=STRAGGLER**(masterdata+read-model+notification, 도메인 게이트가 scope∨isOperator∨entitled 요구 → wildcard 403 → **TASK-ERP-BE-031 수정·머지 #2668**) · **ecommerce=STRAGGLER**(다른 축 — 게이트웨이 account-type 평면이 `/api/admin/**` 를 operator role 로 막음; 사용자 정책결정=열어라 → **TASK-BE-506 수정·머지 #2669**) · **wms=N/A**(strict equality, wildcard 가 layer-1 서 거부). **감사는 static**(게이트 predicate + 이미 런타임 확정된 토큰 프로필)이었고, 각 fix 의 RED-before/GREEN-after 테스트가 판정을 런타임 증명으로 전환. **메타**: 하나의 축(super-admin wildcard read)이 4개 플랫폼에 걸쳐 있었고, finance 만 고치면 3개 도메인이 straggler 로 남았다 — sibling-parity 규율이 정확히 이걸 잡음. 분석=Opus 4.8. [[project_enforcement_straggler_sibling_parity]] [[project_finance_forbidden_onion_sweep_2026_07_18]]
- `TASK-FIN-BE-049-finance-superadmin-wildcard-read-authority.md` — **DONE (2026-07-18, 3-dim verified — impl PR #2665 squash `f9b541dbc`; 머지 전 **7 SUCCESS / 0 failing**, `state=MERGED` — **Integration (finance-platform, Testcontainers) Linux SUCCESS** + Build & Test (JDK 21) + iam 레인 *실제 실행* 통과).** finance super-admin **wildcard-read** authority parity (account + ledger) — FIN-BE-048 의 wildcard 형제(한 축 옆). 플랫폼 super-admin 의 base OIDC domain-facing 토큰(`tenant_id='*'`, finance scope 없음, domain role 없음 [ADR-033 S2 / ADR-034 U5 가 `SUPER_ADMIN` 을 domain 토큰서 제외], `entitled_domains=[]`)이 layer-1(tenant gate `allowSuperAdminWildcard()`) 통과하나 layer-2(`readAuthorities`) 에서 403 → console super-admin 페르소나 finance 카드 `forbidden`. **런타임 확정**(nightly-e2e run `29635409302` vs main `de8edcae4`: RSC `reason=PERMISSION_DENIED` — 실제 403). **수정**(FIN-BE-048 미러): 양 converter 가 `TenantClaimValidator.WILDCARD_TENANT.equals(tenant_id)` 시 READ-only `ROLE_FINANCE_SUPERADMIN_READ` 합성(감사 분리 위해 `ROLE_FINANCE_VIEWER` 와 별개 role); `readAuthorities` 에만 추가, `writeAuthorities` 불변. **불변식=wildcard 는 READ 만 확대**(HTTP IT: wildcard GET 통과+POST→403; non-wildcard scopeless→여전히 403, grant 는 `tenant_id='*'` 에 엄격 키잉). mutation RED-on-removal 양 서비스. auth-service 무변경(ADR-033/034 plane disjointness — `ROLE_SUPER_ADMIN` mint 금지). 후속 sibling-parity 감사=**FIN-BE-050**(ready/, erp `ReadAuthorizationGate`/scm/ecommerce). **iam BE-518 super-admin 페르소나 종결** — nightly console e2e 그린 확인 후 BE-518 최종 close. 분석·구현=Opus 4.8(security-critical). [[project_enforcement_straggler_sibling_parity]] [[project_finance_forbidden_onion_sweep_2026_07_18]] [[env_shared_issuer_authenticated_is_not_authorized]]

- `TASK-FIN-BE-048-finance-read-entitlement-trust-authority-parity.md` — **DONE (2026-07-18, 3-dim verified — impl PR #2663 squash `bc9e5fb2c`; 머지 전 **7 SUCCESS / 0 failing**, `state=MERGED` — **Integration (finance-platform, Testcontainers) Linux SUCCESS** + Build & Test (JDK 21) + iam 레인 *실제 실행* 통과).** **[finance-forbidden onion 스윕의 최종 층 — layer-2 authority straggler]** federation `entitlement-trust-crossdomain` 이 자격 있는 acme operator(`entitled_domains=[finance,wms]`, scope/role 없음)의 finance 카드를 `forbidden` 시킴. **런타임 확정**(DIAG run `29632072800`): 토큰은 layer-1 tenant gate(`trustEntitledDomains`) 통과하나 finance-account/ledger `SecurityConfig` 의 layer-2 read gate(`SCOPE_finance.read`/operator-role)에서 403 `PERMISSION_DENIED`. 형제 wms 는 통과(`ROLE_WMS_VIEWER` 자동부여). **원인=finance 가 entitlement-trust 를 layer-1(FIN-BE-006 파일럿)에만 적용, layer-2 미적용 → FIN-BE-046/047 read-scope 하드닝이 straggler 를 만듦.** **수정**: 양 서비스 `ActorContextJwtAuthenticationConverter` 가 `TenantClaimValidator.isEntitled(jwt,"finance")` 시 READ-only `ROLE_FINANCE_VIEWER` 합성(wms `ROLE_WMS_VIEWER` 미러, TASK-MONO-162 / ADR-MONO-019 §D5 / ADR-MONO-020 D4); `readAuthorities` 에만 추가, `writeAuthorities` 불변. **불변식 = READ 가시성만 확대, mutation 권한 절대 불변** — HTTP scope IT(account+ledger)가 entitled 토큰 GET 통과 + POST→403 증명, converter 유닛테스트가 entitled 토큰 `SCOPE_*` 미획득 단언. mutation RED-on-removal 양 서비스 확인. **🔴 잔여(별개 원인)**: console `operators-profile`(super-admin `tenant_id=*`, `entitled_domains=[]`)는 이 entitlement 수정으로 안 풀림 → iam-platform `TASK-BE-518`(ready/) 에 주석, federation acme 카드가 CI 그린 확인 후 종결. 분석·구현=Opus 4.8(security-critical). [[project_finance_forbidden_onion_sweep_2026_07_18]] [[project_enforcement_straggler_sibling_parity]] [[env_shared_issuer_authenticated_is_not_authorized]]

- `TASK-FIN-BE-046-account-service-scope-enforcement-gap.md` — **DONE (2026-07-16, 3-dim verified — spec PR #2618 squash `754089941`, impl PR #2619 squash `a7b71dccd`)**. account-service **OAuth2 scope enforcement (Option 1 — enforce)**. Found by **live full-stack verification 2026-07-16**: `iam-integration.md` § Token 검증 규칙 #5 + Error table declared downstream scope enforcement, but `SecurityConfig` gated `/api/finance/**` with `.authenticated()` only → a `finance.read`-only token performed every write (open account 201, hold, transfer). **Fix**: `ActorContextJwtAuthenticationConverter` lifts the JWT `scope` claim (array / space-delimited / `scp`) into `SCOPE_*` authorities (roles→`ROLE_*` unchanged, so the app-layer operator gate on `/kyc/upgrade` is intact); `SecurityConfig` requires `SCOPE_finance.write` on writes (POST/PUT/PATCH/DELETE `/api/finance/**`) and `finance.read`|`finance.write` on reads — **role-OR-scope** (operator/admin role admits without scope) so the operator gate is not shadowed and the platform-console operator read consumer (ADR-MONO-013) keeps working. Insufficient scope for an authenticated caller → 403 `PERMISSION_DENIED` (already in the registry — no new code). **Regression test** `ScopeEnforcementHttpIntegrationTest` (7 cases: read→write 403, read→transfer 403, read→GET 404, write→POST 201, write→GET 404, operator-no-scope→GET 404, unprivileged→403) — the coverage whose absence let this ship; `CrossTenantHttpIntegrationTest` token given scope. Spec reconciled (`iam-integration.md` #5 + Error table split gateway-presence-FORBIDDEN vs service-scope-PERMISSION_DENIED, `account-api.md` PERMISSION_DENIED row). **3-dim**: (a) both MERGED (`754089941`, `a7b71dccd`); (b) `origin/main` tip=`a7b71dccd`; (c) pre-merge failing required=0 — **Integration (finance-platform, Testcontainers) Linux SUCCESS** (the authority: local host flaked non-deterministically on `redis:7-alpine` Testcontainers startup, rotating unrelated classes across re-runs, under resident-stack load) + Build&Test + Error code registry SUCCESS. **Live re-verified** on the rebuilt image through the gateway with a real GAP token: `finance.read`→POST 403, →transfer 403, →GET 404; `finance.write`→POST 201 (the finding opened with a live 201 and closed with a live 403). 구현=Opus 직접(security-critical). 분석=Opus 4.8. 사용자 "Option 1 방향 확정 시 scope 강제 + 회귀 테스트까지 구현" 선택. [[project_iam_e2e_covers_operator_flow_not_gateway_user_path]] [[env_git_merges_text_not_meaning_and_lies_about_staleness]]

- `TASK-FIN-BE-010-ledger-service-reconciliation-matching.md` — **DONE (2026-06-12, 3-dim verified — spec PR #1362 squash `228ce6e8`, impl PR #1363 squash `ada59bf1`)**. ledger-service **4th increment** — **reconciliation matching** (fintech F8 no-auto-close). The ledger reconciles its **clearing-account** entries (CASH_CLEARING/SETTLEMENT_SUSPENSE) against an ingested **external statement** (bank/PG): 1:1 by (amount, currency, direction); unmatched either side → **`ReconciliationDiscrepancy`** in an **OPEN operator queue — NEVER auto-closed/adjusted (F8)**; operator manually resolves (matched-manually/written-off/accepted + reason + audit). Emits `finance.ledger.reconciliation.completed.v1` + `.discrepancy.detected.v1` via the **existing FIN-BE-009 outbox** (generic TopicResolver — no relay change; `OutboxAutoConfiguration` still excluded). REST: ingest / resolve / statement-detail / discrepancy-queue / detail. Flyway `V4` (4 tables, **VARCHAR(36) String ids** [not CHAR — entity maps String], BIGINT minor money; discrepancy cols mirror the account-service placeholder). Codes: `RECONCILIATION_DISCREPANCY`=recorded-entity marker + `RECONCILIATION_{ACCOUNT_INVALID→422,STATEMENT_NOT_FOUND→404,DISCREPANCY_NOT_FOUND→404,ALREADY_RESOLVED→409}`. **F8 핵심**: `ReconciliationDiscrepancy.open()`만 OPEN 설정·`resolve()`=유일 mutator(operator-only, OPEN 가드→409)·`ReconciliationMatcher`는 기록만(no balancing entry/no auto-resolve). **3-dim**: (a) MERGED+`ada59bf1`; (b) `git log origin/main` tip=`ada59bf1`; (c) pre-merge failing required=0 (**Integration (finance Testcontainers) pass 2m2s** = real Kafka: post→ingest(150000 match+70000 unmatched-ext+99000 unmatched-int)→1 match+2 OPEN discrepancies[not auto-closed]→consume both events→resolve one[other OPEN]→re-resolve 409→non-clearing 422; Build&Test pass 2m31s). **구현=backend-engineer(Opus dispatch, 47 files) + dispatcher 독립 재검증** (`:check --rerun-tasks` SUCCESS; grep-zero float/KafkaTemplate-in-usecases; F8 single-mutator 확인; IT base `@BeforeEach cleanLedgerState`=전 트랜잭션 테이블 FK순 정리[ledger_account 보존]=FIN-BE-009 교차클래스 교훈 선제적용). **⚠️IT-only 트랩 1종**: V4 id 컬럼 `CHAR(36)`→`VARCHAR(36)`(엔티티가 plain String 매핑→Hibernate `ddl-auto=validate`가 VARCHAR 기대→`Schema-validation wrong column type [char] expecting [varchar]`; Docker-free `:check`는 Flyway+검증 미실행→finance IT만 적발). 분석=Opus 4.8. 사용자 "finance ledger reconciliation matching" 선택. **OUT(forward-declared)**: period-lock(`RECONCILIATION_PERIOD_LOCKED`)·fuzzy/N:M·multi-currency·수동분개. [[project_monorepo_template_strategy]] [[feedback_spring_boot_diagnostic_patterns]]

- `TASK-FIN-BE-009-ledger-service-gl-ap-feed-outbox.md` — **DONE (2026-06-12, 3-dim verified — spec PR #1355 squash `cf5449c4`, impl PR #1358 squash `097a098a`)**. ledger-service **3rd increment** — the **GL/AP feed**: terminal consumer → **publishing consumer** via a per-service transactional outbox. Emits **`finance.ledger.entry.posted.v1`** (every posted entry, in `PostJournalEntryUseCase.post`'s `@Transactional`) + **`finance.ledger.period.closed.v1`** (in `CloseAccountingPeriodUseCase.close`'s `@Transactional`) — the external accounting/ERP/AP forward interface. **★Outbox path = per-service `OutboxRow` (NOT libs `OutboxWriter`)**: the libs `OutboxAutoConfiguration` entity-scans `ProcessedEventJpaEntity` (mapped `processed_events`) → collides with the ledger's OWN consumer-dedupe `processed_events`; so `LedgerOutboxJpaEntity implements OutboxRow` + `LedgerOutboxPublisher extends AbstractOutboxPublisher` (ADR-MONO-004, wms inventory precedent) — `OutboxAutoConfiguration`+`OutboxMetricsAutoConfiguration` **excluded preserved**, own `ledger_outbox` (V3, MySQL `id CHAR(36)` UUIDv7/`payload TEXT`). Append-side `OutboxLedgerEventPublisher` builds the canonical envelope (`{eventId,eventType,occurredAt,tenantId,source=finance-platform-ledger-service,aggregateType,aggregateId,payload}`) atomically (guard-rejected posting rolls back → no row); relay reuses the EXISTING `KafkaTemplate` (`@RetryableTopic` DLT). `@EnableScheduling` added. **3-dim**: (a) MERGED+`097a098a`; (b) `git log origin/main` tip=`097a098a`; (c) pre-merge failing required=0 (**Integration (finance-platform, Testcontainers) pass 1m45s** = real Kafka round-trip: produce→post→outbox→relay→consume `entry.posted.v1` [envelope + balanced-lines payload] + `period.closed.v1`; rejected posting into a closed window → no entry/no row; Build&Test pass 2m15s). **구현=backend-engineer(Opus dispatch, 19→22 files) + dispatcher 독립 재검증** (`:check --rerun-tasks` SUCCESS; grep-zero KafkaTemplate-in-usecases/float/libs-OutboxWriter/ProcessedEventJpaEntity; `OutboxAutoConfiguration` excluded). **⚠️IT-only 트랩 3종(finance IT가 적발, Docker-free `:check` 통과)**: ①JpaConfig `@EnableJpaRepositories`/`@EntityScan` `…persistence.jpa`→`…infrastructure` (신규 `…outbox` 미스캔→UnsatisfiedDependency, §19/§20 부트스트랩) ②IT window instant nanos→`truncatedTo(MICROS)` (MySQL DATETIME(6) 라운드트립, §15) ③교차클래스 격리=`@BeforeEach DELETE accounting_period`+snapshot (now커버 CLOSED 기간이 static 컨테이너서 형제클래스 poison: 포스팅 LEDGER_PERIOD_CLOSED / open 422 overlap; FIN-BE-008부터 잠재, 2번째 now커버 클래스가 발현). 분석=Opus 4.8. 사용자 "finance ledger GL/AP feed (outbox 도입)" 선택. [[project_monorepo_template_strategy]] [[feedback_spring_boot_diagnostic_patterns]]

- `TASK-FIN-BE-008-ledger-service-period-close.md` — **DONE (2026-06-12, 3-dim verified — spec PR #1350 squash `35f6eca6`, impl PR #1353 squash `05cd3dea`)**. ledger-service **2nd increment** — the **period-close** forward-declared by TASK-FIN-BE-007 / architecture.md § Increment Scope. `AccountingPeriod` aggregate (OPEN→CLOSED state machine, tenant-scoped **non-overlap** `[from,to)` half-open windows; `covers`/`overlaps` pure) + a **posting-path guard** (`PostJournalEntryUseCase` rejects an entry whose `postedAt` is covered by a CLOSED period → `LEDGER_PERIOD_CLOSED` 422 → **DLT** on the consumer path; **net-zero** — no covering closed period [incl. none defined] → posting byte-unchanged) + a **close-time trial-balance snapshot** (`PeriodBalanceSnapshot` per-account + grand totals, in balance, == live trial balance at close; immutable insert-only rows). Operator REST: `POST /periods` (open 201), `POST /periods/{id}/close` (200 + snapshot), `GET /periods` (list), `GET /periods/{id}` (detail + snapshot). **Terminal consumer preserved — `finance.ledger.period.closed.v1` emission DEFERRED to the GL/AP-feed outbox increment** (architecture.md § Increment Scope decision; events contract sequences outbox introduction there). Flyway `V2__create_accounting_period.sql` (`accounting_period` + `period_balance_snapshot`); no deploy-wiring change (ledger-service already wired by FIN-BE-007 — V2 runs in the existing finance Integration job). Error codes: claim `LEDGER_PERIOD_CLOSED` (drop v2-planned) + add `ACCOUNTING_PERIOD_{NOT_FOUND→404,OVERLAP→422,ALREADY_CLOSED→409,INVALID_WINDOW→422}`. **3-dim**: (a) MERGED+`05cd3dea`; (b) `git log origin/main` tip=`05cd3dea`; (c) pre-merge failing required=0 (**Integration (finance-platform, Testcontainers) pass 1m43s** = real Kafka: post→open window covering now→close→snapshot==live trial balance + status CLOSED + entryCount, then a late completed event into the closed window posts **no** entry [→DLT]; non-overlapping/abutting window 201, overlapping 422, re-close 409, list/detail, cross-tenant 403; Build&Test pass 2m7s). **구현=backend-engineer(Opus dispatch, 31 files / 5 unit+slice + 1 IT) + dispatcher 독립 재검증** (`:check --rerun-tasks` BUILD SUCCESSFUL; grep-zero KafkaTemplate/publish/float/finance_db; OutboxAutoConfiguration still excluded; net-zero guard confirmed; GlobalExceptionHandler status map exact per contract). 분석=Opus 4.8. 사용자 "finance 기간마감 (period close)" 선택. [[project_monorepo_template_strategy]] [[feedback_spring_boot_diagnostic_patterns]]

- `TASK-FIN-BE-007-ledger-service-bootstrap-first-increment.md` — **DONE (2026-06-12, 3-dim verified — impl PR #1348 squash `9c11b2b9`, spec PR #1345 `48235406`)**. Bootstrapped finance-platform's **2nd service** — the ADR-MONO-008 § D3 v2 **double-entry general ledger** — as a first increment: **event-driven auto-journal + read**. New Hexagonal+DDD `apps/ledger-service/` (`com.example.finance.ledger`), **`rest-api + event-consumer` dual-type, terminal consumer** (Outbox+OutboxMetrics auto-config excluded — erp read-model precedent). Consumes account-service `finance.transaction.{completed,reversed}.v1` → auto-posts **balanced double-entry `JournalEntry`** per a fixed Posting Policy (TOPUP→DR CASH_CLEARING/CR wallet · WITHDRAW reverse · CAPTURE→DR wallet/CR SETTLEMENT_SUSPENSE · TRANSFER→DR/CR two wallets · HOLD/RELEASE→no entry · reversed→swap); **`Σdebit==Σcredit` invariant self-validated in the JournalEntry factory → `LEDGER_ENTRY_UNBALANCED`**; immutable (reversal-only, F3); idempotent (`processed_events` dedupe on source eventId). Read REST (entry / per-account running balance / **trial balance** = live invariant) + dual-accept tenant gate + RS256 JWKS + append-only `audit_log` + Flyway `finance_ledger_db` (separate schema — downstream derivation, **no write-back to `finance_db`**). **Boundary**: no account-service mutation, terminal (no outbox/`KafkaTemplate` publish). Period-close / GL-AP feed `entry.posted.v1` / reconciliation / manual posting / multi-currency forward-declared (later increments). Deploy wiring atomic (settings.gradle + finance docker-compose `ledger.local`+`ledger-mysql` + ci.yml `:check`+`:integrationTest`). **3-dim**: (a) MERGED+`9c11b2b9`; (b) `git log origin/main` tip=`9c11b2b9`; (c) pre-merge failing required=0 (**Integration (finance-platform, Testcontainers) pass 1m42s** = real Kafka produce→consume→post→read: TOPUP→balanced entry + trial-balance inBalance, dedupe→one entry, TRANSFER→two wallet lines, reversed→balanced reversal, cross-tenant→403, HOLD→no entry; Build&Test + E2E smokes GREEN). **구현=backend-engineer(Opus dispatch, 87 files/43 unit+slice+6 IT) + dispatcher 독립 재검증**(scope leak 0, grep-zero float/finance_db/KafkaTemplate, `:check --rerun-tasks` BUILD SUCCESSFUL, Outbox excluded). 분석=Opus 4.8. 사용자 "finance ledger-service 부트스트랩 (도메인 심화)" 선택. [[project_monorepo_template_strategy]] [[feedback_spring_boot_diagnostic_patterns]]
- `TASK-FIN-BE-006-tenant-gate-entitlement-trust-dual-accept-pilot.md` — **DONE** (ADR-MONO-019 § 3.3 step 3 파일럿, 1 도메인=finance; impl bundled branch + separate close chore; BE-303 3-dim verified). 분석=Opus 4.8 / 구현=Opus (backend-engineer dispatch). **impl PR #960** (squash `df1efa5a`). finance tenant 격리 게이트 `tenant_id == finance` 고정 → **entitlement-trust dual-accept**: legacy(`tenant_id ∈ {finance,*}`) ∪ 서명 토큰 `entitled_domains ∋ finance`. 양 enforcement 지점(`TenantClaimValidator` decode + `TenantClaimEnforcer` filter)이 공유 정적 헬퍼 `TenantClaimValidator.isEntitled(jwt,domain)` 로 동일 적용 — **거부 = !legacyOk && !entitled** (fail-closed; claim 형 anomaly→false, NPE 無, blanket-trust 無). **net-zero**(GAP claim 미발급 → claim 부재 → legacy만 → 기존 동작 byte-identical). 격리 IT: entitled 타테넌트(acme+[finance])→404(게이트 통과, NOT 403) / non-entitled(acme+[wms])→403 TENANT_FORBIDDEN / 기존 crossTenant·missingToken 무변경. architecture.md § Multi-tenancy + Failure #3 갱신. **BE-303 3-dim (impl #960)**: (a) state=MERGED + mergeCommit `df1efa5a`; (b) `git log origin/main` tip `df1efa5a` 일치; (c) pre-merge `gh pr checks 960` failing=0. **BE-299 re-stage**: staged done/ blob Status=done ✓. **CI 1-pass**: finance Integration(Testcontainers) GREEN 1m44s. **scope-lock**: 다른 도메인/console-bff/GAP claim populate/legacy 제거/step 2 artifact 0. **후속**: ① wms/scm/erp/gap+console-bff 게이트 복제(본 blueprint) ② GAP auth-service `entitled_domains` populate(shared, step 2 와 함께 — auth→account `/internal/tenant-domain-subscriptions` BE-322 조회). **메타**: ① finance 이중 enforcement(validator decode + enforcer filter) → 양쪽 동일 dual-accept 必(한쪽만 고치면 decode-pass/filter-block split) → 공유 정적 헬퍼로 단일 진실. ② 서명 claim 신뢰 안전성 = RS256/JWKS 검증 후에만 read(AllowedIssuersValidator 선차단). ③ blueprint = 5-도메인 복제 시 각 도메인의 enforcement 지점 수 먼저 확인(finance=2).

- `TASK-FIN-BE-005-platform-console-operator-read-consumer-reconciliation.md` — **finance-side spec reconciliation for ADR-MONO-013 Phase 5 — the scm Phase 4 `TASK-SCM-BE-015` analog (spec PR + impl PR + close chore, per finance INDEX strict PR Separation Rule)**. 분석=Opus 4.7 / 구현=Opus 4.7 / 리뷰=Opus 4.7. **spec PR #639** (squash `95c543a1`): author FIN-BE-005 → `tasks/ready/` + INDEX ready list. **impl PR #640** (squash `8b5d60aa`): spec-only `(B) document/accept` under governing ADR-MONO-013 (finance domain governance = ADR-MONO-008, untouched; **no finance ADR**). Additive — `iam-integration.md` new `## platform-console Operator Read Consumer (ADR-MONO-013)` section (after `## Token 검증 규칙`, before `## Error Responses`) + 참조 bullets recording `platform-console` (GAP's own `platform-console-web` client; NOT `finance-platform-internal-services-client`, NOT deferred `finance-platform-user-flow-client`) as a sanctioned external operator read consumer of finance's v1-live read surface (`GET /accounts/{id}` · `/balances` · `/transactions`), validated by the **existing** `AllowedIssuersValidator` + `TenantClaimValidator` `tenant_id ∈ {finance,*}` + `X-Token-Type=user` chain — **no new client/route/code/auth-model change**; **read-only** (finance write + v2 `admin-service` excluded); **single-org preserved** (`multi-tenant` non-declaration unaffected); fintech producer obligations cross-ref'd (F5 minor-units / confidential+F7 / honest regulated-state — producer-authoritative, not new finance requirements); `PROJECT.md` § GAP IdP Integration clarifying bullet (**frontmatter byte-unchanged** — finance deliberately excludes `multi-tenant`/`integration-heavy`). **finance-shape honesty**: finance has no `gateway-public-routes.md` (gateway v1-deferred) so the honest finance shape is the SCM-BE-015 *subset* (**2 files**, not scm's 3); `account-api.md` / `account-service/architecture.md` authoritative + byte-unchanged; **no `last_updated` invention** (these finance docs carry no such field — not cargo-culted from scm's `gateway-public-routes.md` precedent). **Unblocks `TASK-PC-FE-009`** (platform-console `tasks/backlog/` — ADR-MONO-013 Phase 5 finance console section): its `backlog → ready` move is gated on this task reaching `done/`, which this close chore satisfies. **Objective merge verification** at each stage: #639 `state=MERGED, mergeCommit=95c543a1, mergedAt=2026-05-20T01:05:38Z, origin/main tip matches` → #640 `state=MERGED, mergeCommit=8b5d60aa, mergedAt=2026-05-20T01:06:52Z, origin/main tip matches` → close chore PR (this). closed via this close chore PR.
- `TASK-FIN-BE-004-idempotency-concurrency-exactly-once-fix.md` — **Fix issue found in TASK-FIN-BE-001 — concurrent same-Idempotency-Key fund movement exactly-once** (spec PR #609 `e26f1d9b` + impl PR #610 `18892d4e`; surfaced by MONO-115 run `26071043853`, finance IT 11/12 residual after FIN-BE-003). **Root cause (BE-301, neither clean H1 nor H2 = production design gap)**: `IdempotentExecution.run` was non-atomic check-then-act → an 8-thread same-key burst let N threads MISS then each move funds; `architecture.md` already specified "Redis primary (SET NX-EX)" atomic claim the original impl never realised. **Fix = claim-before-execute, DB-authoritative**: port `claim()→{EXECUTE|REPLAY|CONFLICT|IN_PROGRESS}`+`complete()`+`release()`; `IdempotencyKeyJpaRepository` native atomic `insertClaim` (composite PK `(idempotency_key,endpoint,tenant_id)` = the concurrency gate; `save` merges so can't fail-on-dup) + `markCompleted` + `delete`; `RedisOrDbIdempotencyStore` DB-PK-authoritative atomic gate (sentinel `response_status=0`=IN_PROGRESS, ≥200=REPLAY, dup→CONFLICT/REPLAY/IN_PROGRESS, expired sentinel reclaimable, Redis=best-effort REPLAY accelerator never the authority, fail-CLOSED on DB down → 503); `IdempotentExecution.run` (signature UNCHANGED → 4 controller call sites untouched) claim-before-execute loop (EXECUTE→action→complete(2xx)/release(fail|non-2xx); REPLAY→stored; CONFLICT→409; IN_PROGRESS→bounded poll ≤5s≪req timeout then re-claim, stall→409 deterministic no-re-movement, never faked success); IT rewritten to drive the **real** wrapper ×8 threads, assertion **STRENGTHENED** to `actionExecuted==1` exact (green only via the genuine prod guarantee — never weakened; green-wash prohibited). **2-cycle CI history (cycle-burn discipline, BE-301 root-caused not guessed)**: cycle-1 (#610 run `26073035851`) — atomic gate worked (MySQL 1062 Duplicate entry) but `@Transactional(REQUIRES_NEW)` on `claim()` + catching `DataIntegrityViolationException` *inside* that tx and continuing JPA work poisoned the rollback-only tx → `UnexpectedRollbackException`; cycle-2 fix = new `IdempotencyKeyTx` separate `@Component` with single-statement `REQUIRES_NEW` methods (dup propagates OUT), non-`@Transactional` `RedisOrDbIdempotencyStore` orchestrator catches the dup *outside* any tx then reads in a fresh tx (ComplianceFailureRecorder REQUIRES_NEW precedent). impl=Opus 직접(correctness-critical, agent 미사용) / **dispatcher BE-301**: scope=port+repo+IdempotencyKeyTx+store+IdempotentExecution+IT only (no V1/migration/contract/`architecture.md`[impl now COMPLIES with existing SET-NX-EX spec]/ADR/ci.yml/controller/FIN-BE-002·003); cycle-2 NET diff = 2 files `infrastructure/idempotency/**`; `:check --rerun-tasks` 117/0/0/0 (11-XML). **CI-PROVEN** via MONO-115 `finance-integration-tests` run `26073397866` = conclusion **success, 12/12 ALL PASSED** (AccountLifecycle ×5, AuditAndImmutability F3/F6, CrossTenant 401+403, IdempotencyConcurrency exactly-once + CONFLICT/REPLAY, OwnerRefEncryptionAtRest F7) — FIN-BE-002/003 not regressed. Merge ⇒ main `finance-integration-tests` RED→GREEN = the honest green-wash chain **TRUE TERMINAL**: FIN-BE-001 honest gap → MONO-115 CI built → FIN-BE-002 schema → FIN-BE-003 behavioural → **FIN-BE-004 concurrency** — each step surfaced, tracked, user-approved (strategy A), never silently dropped, never green-washed; finance v1 behavioural-proof gap fully closed. closed via this close chore PR.
- `TASK-FIN-BE-003-pii-roundtrip-and-it-jwks-fix.md` — **Fix issues found in TASK-FIN-BE-001 — owner_ref encrypt-at-rest round-trip + cross-tenant IT JWKS** (spec PR #606 `f24e4fa3` + impl PR #607 `83c8b398`; surfaced by MONO-115 run `26036483067` post-FIN-BE-002). **D1** (×8, prod): `AccountRepositoryAdapter` set plaintext back onto the JPA-managed `saved` entity post-`save()` → tx-commit dirty-flush re-persisted plaintext over the ciphertext envelope (F7 violation) → later `decryptFromString(plaintext-with-hyphen)` threw `Illegal base64 character 2d`. Fix = `jpa.saveAndFlush` then `EntityManager.detach` BEFORE restoring plaintext for the caller; `decrypt()` detaches BEFORE writing the decrypted value → column always the `v1:` envelope, caller gets a detached plaintext domain object. **D2** (×1, test): base `AbstractAccountIntegrationTest` registered a dead `localhost:9` `jwk-set-uri` that non-deterministically shadowed `CrossTenantHttpIntegrationTest`'s duplicate registration. Fix = base owns the ONLY `jwk-set-uri` registration → reachable MockWebServer serving a mutable JWK set; CrossTenant publishes its public JWK via new `publishJwks(...)` in `@BeforeAll`. **AC-3**: new `OwnerRefEncryptionAtRestIntegrationTest` (raw `owner_ref` = `v1:` envelope; hyphen-bearing ref exercises the exact `2d` regression). impl=Opus 직접(correctness-critical, agent 미사용) / **dispatcher BE-301**: scope=adapter(prod)+2 IT+1 new IT(test) only, no V1/migration/contract/architecture.md/ADR/enum-mapping/ci.yml; `:check --rerun-tasks` 117/0/0/0. **CI-PROVEN** via MONO-115 run `26071043853`: **9 fail → 1 fail (11/12 GREEN)** — D1/D2/AC-3 all green (AccountLifecycle 5/5, AuditImmutability F3/F6, CrossTenant 403+401, F7 raw-column, Idempotency CONFLICT). **⚠️ 정직 (green-wash 금지)**: AC-1 ("4 IT/12 test green") **NOT met** — 1 remaining: `IdempotencyConcurrencyIntegrationTest.concurrentSameKeyMovesFundsOnce` L92 `performed>1` (concurrent same-key `placeHold` not exactly-once) = **distinct concurrency defect class**, NOT a regression (the test had never run to completion before — masked by the prior context-load/decrypt cascade), OUT of FIN-BE-003 scope per its own **Failure Scenario A** → tracked as **TASK-FIN-BE-004** (user-approved strategy A: merge #607 + separate fix-task, no scope-expand). main `finance-integration-tests` remains **honest+isolated RED** (1/12; other 17 jobs + finance `:check` green) until FIN-BE-004 — NOT silently dropped, NOT green-washed. closed via close chore PR (this).
- `TASK-FIN-BE-002-enum-schema-validation-fix.md` — **Fix issue found in TASK-FIN-BE-001 — enum schema-validation** (spec PR #602 `cc318aca` + impl PR #604 `c4d33fd5`). Hibernate 6.6.4 + `MySQLDialect` mapped `@Enumerated(EnumType.STRING)` → native MySQL `ENUM(...)` for schema-validation vs Flyway `CHAR(3)`/`VARCHAR` → `SchemaManagementException` on `accounts.currency` → 4 IT/11 test context-load cascade. **cycle 1** (global `hibernate.type.preferred_enum_jdbc_type=VARCHAR`) **insufficient** (governs only un-annotated default enums, not explicit `@Enumerated(EnumType.STRING)`; CI-falsified, same error) → **cycle 2 definitive**: per-field `@JdbcTypeCode` exact-match DDL on all 13 enum fields (4 `SqlTypes.CHAR` for `currency` CHAR(3) + 9 `SqlTypes.VARCHAR`), application.yml net-reverted (dead config removed). evidence: GAP (same Hibernate 6.6.4/MySQL/ddl-auto:validate, IT green) proved VARCHAR-enum tolerant but CHAR(3) `currency` is the only mismatch; existing `@JdbcTypeCode(SqlTypes.JSON)` in AuditLog = working precedent. impl=backend-engineer(Sonnet dispatch cycle1) + **dispatcher BE-301 직접 cycle2·독립 재검증**: 13 `@Enumerated`↔`@JdbcTypeCode` pairing (4 CHAR/9 VARCHAR grep-confirmed), imports 6/6, NET diff=6 entity .java +23 (application.yml net-identical to main), `:check --rerun-tasks` 117/0/0/0 (11-XML re-parsed). **CI-PROVEN** via MONO-115 `finance-integration-tests` (run `26036483067`): `SchemaManagementException` ELIMINATED, ApplicationContext loads, **0→2 IT PASS** — the enum schema-validation scope is DELIVERED & CI-proven. **⚠️ 정직 (green-wash 금지)**: AC-1 ("4 IT/11 test green") **NOT met** — 9 IT still fail on **2 distinct pre-existing TASK-FIN-BE-001 defects** surfaced once context-load unblocked: (1) PiiEncryptor `owner_ref` persistence encrypt/decrypt asymmetry (×8, `Illegal base64 character 2d`), (2) test-profile JWKS unreachable for the cross-tenant HTTP+JWT path (×1). Both **OUT of FIN-BE-002 scope** (its own Failure Scenario B) → tracked as **TASK-FIN-BE-003** (user-approved strategy A: merge #604 + separate fix-task, no scope-expand). main `finance-integration-tests` remains **honest+isolated RED** (other 17 jobs + finance `:check` green) until FIN-BE-003 merges — NOT silently dropped, NOT green-washed. closed via close chore PR (this).
- `TASK-FIN-BE-001-account-service-bootstrap.md` — **finance-platform 첫 도메인 서비스 `account-service` Hexagonal 구현 (spec-first 2-PR + close chore)**. 분석=Opus 4.7 / 구현=backend-engineer(Opus dispatch) / 리뷰=Opus 4.7 (dispatcher BE-301 독립 F1-F8 재검증). **spec PR #597** (squash `5a4aae42`): architecture.md(Hexagonal, ADR-MONO-012 canonical, HARDSTOP-09/10; Service Type=`rest-api` single — outbox≠event-consumer, scm-procurement 선례·platform/service-types INDEX L48) + account-api.md + finance-account-events.md + platform/error-handling.md fintech codes(HARDSTOP-03 차단→precedent-exact 교정). **impl PR #598** (squash `ce2d16ce`): domain(Account/Balance/Transaction+2 상태기계+Money minor-units+KycGate, framework-free) / application(`AccountApplicationService` 단일 @Transactional 경계, gate→balance 순서, ComplianceFailureRecorder REQUIRES_NEW) / infra(JPA adapters+outbox+StubCompliance+PiiEncryptor AES-GCM+RS256) / presentation(controllers≡contract) / Flyway V1 MySQL InnoDB 11-table append-only audit. 116 file/7089+. **dispatcher BE-301 독립 재검증 11/11** (agent report 불신·재실측: F5 float=0 / F4 gate-before-balance 全 fund / F1·F2 단일 Tx·단일 writer / F3 immutable reversal-only / F6 append-only DDL / tenant {finance,*} / V1 MySQL valid / `:check` 117/0/0/0 XML-파싱 / scope clean / contract paths). **CI #598 all green** (Build&Test=finance `:check` 117 unit/slice + GAP/scm/master IT + E2E + boot jars). **⚠️ 정직 gap (green-wash 금지)**: finance ci.yml 은 Docker-free `:check` 만 wire — **5 Testcontainers IT 클래스(idempotency-concurrency exactly-once / cross-tenant 403 / audit append-only / SETTLED immutable, 실 MySQL+Redis+Kafka)는 CI 미실행** (scm 의 TASK-MONO-048 "Integration(scm) CI job" 동형 부재). IT 는 compile-clean + F1-F8 구조 독립검증됨이나 행위 증명은 CI 미수행 → **follow-up = TASK-MONO-115 candidate: "Integration (finance-platform, Testcontainers)" CI job 신설 (scm MONO-048 analog, `.github/workflows/ci.yml` monorepo-level)**. deviation 4건(topUp internal-only/gateway v1-deferred→Traefik direct/HoldExpirySweeper deferred-flag/reconciliation-queue v1 no-resolve) 全 architecture.md-compliant. closed via close chore PR (this).

## archive

(empty)
